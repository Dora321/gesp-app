# GESP 题库解析大模型自动化重组（重写）执行方案

这份详细方案旨在供你或另一个具备代码编写能力的大模型（如 Cursor、Claude 3.5 Sonnet 等）直接阅读并执行，以通过外接大模型 API 彻底重写所有低质量的解析字典。

## 方案总览

- **目标**：扫描 `src/data/gesp/` 目录下的 `.js` 试卷文件，针对所有的**单选题**（`type: 'single'`）和**判断题**（`type: 'judge'`），使用指定的大模型 API 自动生成高质量的“逐项解析”，并无损写回。
- **难点挑战**：由于数据文件是 `.js` 而非 `.json`，其中包含大量的单行注释 `//`、特定的变量导出 `export const` 以及特殊转义。因此，**不能直接将其解析为 JSON 再强制序列化写回**（这样会破坏文件原有的物理结构和注释）。需要采用**“AST 解析读取/大正则精准替换写回”**混合方案。

---

## 二、 核心 Prompt（提示词）工程设计

调用外部模型生成解析文本时，核心的提示词设定决定了生成的最终质量。在编写生成重写脚本时，必须严格套用以下 Prompt 结构：

### [System Prompt]

````text
你是一个专业的信奥赛（GESP/CSP-J/S）C++算法资深讲师。
你的任务是根据提供的题目题目信息生成极其专业、严谨且通俗易懂的题目解析。

【强制纪律约束】：
1. 你的返回格式必须直接是纯 Markdown 文本，绝不包含任何客套话、解释或前置说明（不要答复"好的"，直接输出解析块内容即可）。
2. **逐项分析要求**：对于每一个非正确选项，必须通过推演说明【具体错在什么地方，执行后会得到什么】。严禁出现"此说法不正确"等没用的套话，严禁跨选项复制粘贴文案。
3. 文本中的 C++ 代码、关键字、输出占位和标点符号必须严谨地包裹在反引号 \` 中。
4. **输出格式模板** (只返回下述内容，勿添加首尾多余的 ```markdown):
**答案：[原位保留标准答案结果如 A 或 正确]**

**解析：**
[先用一段连贯的话从核心概念层面解释这个题的知识侧重以及判断基准。如果是读代码题，请简短解说其执行流程。]

[接着如果考题具有选项（单选题），请一定附带如下的选项逐项精确分析：]
- **A [可缩写的选项主体]**：错。[具体的错误原因及纠正论证]。
- **B [可缩写的选项主体]**：对。[符合条件的论证]。
...其它选项

**考点：** [简短归纳1～2个相关技术考点，如"基本语法"、"循环语句"、"强制类型转换"]
````

### [User Message]

```text
题型：{{type}}
题干：{{question_text}}
备选选项：{{options_array}}
正确的正确答案是：{{answer_string}}

请严格按照以上的 System 制定的模板结构生成新解析：
```

---

## 三、 代码执行具体技术架构 (`scripts/auto-regenerate.js`)

接手执行逻辑开发任务的模型需要编写名为 `scripts/auto-regenerate.js` 的脚本，此脚本在编码时需囊括以下底层核心建设：

### 1. 结构解析读取器

不可随意全量替换！文件加载进内存后，可以使用简单的状态机或 Node `vm` 虚拟沙盒环境临时挂载执行（把 `export const paperData =` 前缀切掉替换为 `const paperData = ` 然后 `eval`），由于原始数据可能十分巨大，稳妥的方案是先用 `eval` 或 `JSON 解析转译` 取得真实的题目数据结构树，再以此结构为遍历依据向 LLM 喂数据。

### 2. 爬虫级并发控制器 (Concurrency & Rate Limiting)

题库合计可能超过上千道选择/判断题。全速异步请求会导致 API 服务提供商立即产生 `429 Too Many Requests` 以及并发风暴挂掉。

- 代码中必须引入 `p-limit` 将最大发包并发强制设定在 `3 ~ 5` 之间。
- 对于网络抛出的 `400/429/500` 需要配备标准的指数退避（Exponential Backoff）重试（Retry）机制。

### 3. "深钻式"文本热更与落盘保存机制

拿到大模型返回的最纯净的新解析 `$NewExplanation` 后，我们如何将其无损写回？
**绝对禁止使用 `JSON.stringify(paperData)` 整个文件覆盖重写！**（原因：这会干掉开发者的空行、手写注释甚至单引号双引号风格）。

**推荐的安全替换方案是针对解析内容打「包围框定位法」**：

```javascript
// 在代码中利用特殊的正则包围并匹配单个题目的 explanation 部分：
// 获取旧的 explanation 字符串转义并构造正则
const escapedOldExpl = oldExplanation.replace(/[.*+?^$\{()|[\\]\\\\]/g, '\\\\$&');
// 构建精准替换，只替换原始串内部的 explanation 值
fileContent = fileContent.replace(
    new RegExp(\`explanation:\\s*\\\`\${escapedOldExpl}\\\`\`),
    \`explanation: \\\`\${newExplanation}\\\`\`
);
```

---

## 四、 传递给另一个模型执行的推荐提示词（Prompt 模板）

你可以将此方案发送给 Cursor 的 Composer 或其他智能辅助工具，只需在聊天框附上此文档并粘贴以下要求话术，对方即可无缝全自动化执行：

> **“你好。我们的项目面临着本地存储数据中的文本字段大量存在大模型早期乱生成的无意义字符（重复、截断），我打算现在就对接新模型 API 将其彻底大换血自动重组。
> 你面前拥有的是我为你准备好的【技术执行总纲方案】。请你扮演高级 Node.js 开发工程师，阅读这份方案设计（尤其是其中的写回策略与并发设计要求）。阅毕后，请一次成型地帮我编写出负责驱动并执行此业务的 `scripts/auto-regenerate.js` 脚本，以及相关 package.json 的依赖写入，辅助我一键启动重写！”**
