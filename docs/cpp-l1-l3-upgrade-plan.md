# C++ L1～L3 课程升级规划

日期：2026-09-20  
状态：实施中；第一批审计与样板课纠错已启动。  
范围：优先升级 L1～L3 共 48 课及对应课程总览、练习、学习反馈。

## 1. 目标与现状

将学习目标落实为可观察的表现：学生能解释概念、追踪执行、独立编程、设计边界数据，并把方法迁移到新题。

已核查课程目录、三级课程支持数据、公共教学组件、上机题数据及部分课例；本轮不是对 48 课的完整内容审计。现有课程流程检查通过，只说明结构与关联满足现有检查规则，不代表内容均已验收。

| 已确认的现状 | 升级方向 |
| --- | --- |
| L1、L2、L3 各 16 课，已有目标、产出、自查和前后课链接 | 沿用课程身份与已有学习记录，先校准内容和前置关系 |
| L1 的独立上机题覆盖 16 课；L2 覆盖 6、9、10、16 课；L3 覆盖 5、8、14、16 课 | L2、L3 按知识性质补齐独立任务；通识课采用操作或解释任务 |
| 已有预测题、迁移题、代码追踪和掌握度组件 | 统一任务质量、反馈方式和使用顺序 |
| MiniQuiz 直接显示答案与解析 | 区分复习卡与测评题；测评先作答再反馈 |
| 学习证据同时接纳正确作答、尝试和提交产物 | 分别呈现参与、答对、独立完成，避免混为同一种掌握证明 |
| 字符、类型、循环、模拟、数组在相邻等级重复出现 | 明确每次重访的新增能力，安排诊断后复习 |
| L1 第 9 课检查项将 for 的两个分号写成“三个分号” | 纳入 P0 纠错清单，连同正文、练习和检查项一起核对 |
| L2 第 9 课将 i*i<=n 描述为“整数安全” | 补充数据范围与乘法溢出前提，核对讲解、演示和参考程序 |
| 现有页面包含紫蓝渐变和多种视觉风格 | 新改内容统一米白、暖灰、墨黑与单一点缀色 |

考纲核对以 [CCF GESP 认证标准](https://gesp.ccf.org.cn/101/attach/1725701770182688.pdf) 和 [2026-01-28 修订说明](https://gesp.ccf.org.cn/101/1002/10255.html) 为依据，核验于 2026-09-20。官方说明：C++ 考题默认按 C++11；一级删除误列的位运算；三级一维数组不包括变长数组。目前的课次分布不能直接当作官方考点等级认定。

## 2. 优先级

| 优先级 | 工作 | 交付与完成条件 |
| --- | --- | --- |
| P0 | 全量核查 48 课的概念、代码、答案、边界和前置知识 | 逐课审计表；阻断性错误有修复与验证记录 |
| P0 | 对齐官方考纲与站点教学范围 | 每项标注本级核心、复习或拓展；来源可追溯 |
| P0 | 建立训练证据规则 | 正确作答、尝试、学生自查、教师核验分别记录；本机练习不宣称在线判题通过 |
| P1 | 重构每课教学过程 | 每个目标对应讲解、练习和验收，减少无递进的重复 |
| P1 | 补齐分层训练、阶段测评与补救路径 | 基础、变式、挑战三层任务；错误能指向具体回学内容 |
| P1 | 统一课程入口与关键交互 | 学生能从诊断进入适合课次，顺利完成练习、复盘和下一课 |
| P2 | 整理教师材料和复习安排 | 每课教学提示、常见错因、作业与参考解答；安排下一课和阶段末的间隔复习 |
| P2 | 统一全阶段视觉细节 | 纸墨风、移动端与键盘操作通过检查 |

## 3. 分级内容安排

先沿用每级 16 课的结构。正式调序以考纲映射和前置知识审计为依据；如需移动内容，同步设计旧链接和学习记录的兼容方式。

| 阶段与现有课次 | 内容升级重点 | 学生可验收的产出 |
| --- | --- | --- |
| L1 1～4 | 程序结构、输入输出、变量、整数与小数、整除取余；把生活描述翻译成表达式 | 独立完成输入—计算—输出程序，解释类型与计算结果 |
| L1 5～8 | 字符、逻辑、分支；用反例讲链式比较、边界归属和条件顺序 | 门票或成绩分类程序，附临界值测试表 |
| L1 9～12 | 循环执行顺序、累加与计数、停止条件、break/continue；核定嵌套循环的拓展定位 | 循环跟踪表，能解释最后一轮与结束状态 |
| L1 13～16 | 数学条件、简单模拟、筛选统计与综合复盘 | 独立完成综合题，提交测试与错因说明 |
| L2 1～4 | 存储与网络、字符转换、类型转换、switch；先测再补 L1 基础 | 存储/网络说明任务，以及转换、菜单分支程序 |
| L2 5～8 | 嵌套循环、图形、流程图和数学函数；建立行列与状态模型 | 行号—空格—符号数量表，流程图与代码互译 |
| L2 9～12 | 质数、数位、因数倍数；核定数组初探的衔接定位 | 能处理 0、1、平方数和末尾 0 等边界的程序 |
| L2 13～16 | 多步模拟、调试、综合训练 | 状态表、独立程序、失败用例与修复解释 |
| L3 1～4 | 进制、固定宽度补码、位运算；区分表示模型与 C++ 运算规则 | 进制推导、位变化表和掩码练习，注明位宽与范围 |
| L3 5～9 | 数组、字符串和综合；突出初始化、下标、遍历和输入边界 | 统计、查找、字符处理程序，附越界与空输入条件分析 |
| L3 10～12 | 枚举、模拟、加密；说明候选范围、检查条件和更新顺序 | 能解释不遗漏、不重复的枚举或模拟方案 |
| L3 13～16 | 图形综合、进制编程、逻辑挑战与模拟测评 | 完成从读题、建模、编码到验收的全过程 |

跨级递进应写入目标：例如 L1 的字符课侧重字符与编码差值，L2 侧重字符分类与转换，L3 侧重字符串遍历与统计；循环从读懂单层执行，进阶到嵌套建模，再到数组遍历和枚举。

## 4. 每课统一的教学要求

每课建议围绕 2～3 个可验证目标组织内容。教学环节按概念性质调整，不机械填满页面。

1. 前置诊断：2～3 个短任务；错误指向具体先修内容。
2. 问题导入：一个明确问题，让学生先预测结果或提出方案。
3. 概念讲解：生活例子、精确定义、适用范围和反例相互对应。
4. 执行观察：展示代码、变量或数据结构、输出的对应变化。
5. 引导练习：从补关键步骤过渡到完整解决，提示逐层展开。
6. 独立任务：代码课至少 1 道上机任务；通识课采用分类、解释或操作任务。
7. 变式与挑战：基础题更换条件后再练；挑战题标明拓展与前置知识。
8. 出门检测：一道新情境任务和一句解释，生成具体复习建议。

每道编程任务包含：题意、输入输出、范围、样例、分层提示、可编译参考实现、边界测试及常见错误解释。错误程序须清楚标注，不能作为可直接使用的参考代码。

每级在第 4、8、12 课后安排短诊断，第 16 课完成综合测评，共 12 个阶段节点。原创诊断与官方真题明确标识；自编综合测评不默认宣称符合官方全真考试规格。

## 5. 实施批次与依赖

| 批次 | 范围 | 交付物 | 进入下一批的条件 |
| --- | --- | --- | --- |
| A：基线审计 | 48 课、三级总览、练习关联 | 考纲映射、前置关系、逐课问题清单、修订后课程目标 | 范围、错误与缺口明确，可追踪到课次 |
| B：样板验证 | L1-9 for 循环、L2-9 质数、L3-5 数组 | 3 节完整样板课及题目、交互、测试和教学提示 | 概念正确，练习可独立完成，关键页面通过验收 |
| C：L1 全阶段 | 其余 15 课与 L1 总览 | L1 16 课升级包、4 个诊断节点、训练与补救路径 | 内容和代码验收通过 |
| D：L2 全阶段 | 其余 15 课与 L2 总览 | L2 16 课升级包；补齐未覆盖课次的独立任务 | 重复知识有新增能力，任务不依赖未教知识 |
| E：L3 全阶段 | 其余 15 课与 L3 总览 | L3 16 课升级包；补齐未覆盖课次的独立任务 | 表示模型、位运算、下标与输入边界完成专项核查 |
| F：整体验收 | 48 课与跨级衔接 | 验收报告、教师使用说明、剩余问题与来源清单 | 无阻断性问题；已知限制明确 |

样板阶段优先解决跨级共享的教学交互；后续按 L1 → L2 → L3 推进。批次规模明确，工期在基线审计和样板课完成后按实际工作量估算。

## 6. 验收标准

| 维度 | 验收要求 |
| --- | --- |
| 内容正确 | 48 课逐课核查；已发现 P0 错误清零；定义、示例、答案与范围一致 |
| 知识递进 | 每课有先修要求；新语法先讲后用；拓展明确标注 |
| 编程可靠 | 认证相关参考实现按默认 C++11 编译并运行样例和适用边界；项目使用更高标准的练习另行标注 |
| 练习覆盖 | 48 课均有与目标一致的独立任务；编程课有上机训练，通识课有等价产出 |
| 学习反馈 | 每个核心目标有对应检测；答案适时揭示；尝试、自查与验证结果能区分 |
| 阶段评估 | 12 个阶段节点都有题目、答案、评价规则与回学建议 |
| 页面体验 | 桌面与手机可操作；代码长行可滚动；交互有键盘焦点与明确标签 |
| 回归检查 | 课程流程、内部链接、相关测试与构建通过；共享组件修改检查其他等级 |
| 教学有效性 | 样板课先做教学走查；有真实试学条件后收集完成率、典型错因和提示使用情况，再调整难度 |

教学有效性不能仅由页面测试推断。缺少真实试学数据时，交付中明确写为“内容与功能验收完成，教学效果待试学验证”。

## 7. 落地位置

- `src/lessons/cpp/l1/`、`l2/`、`l3/`：逐课内容与互动。
- `src/data/cppL1CourseFlow.js` 等：目标、前后课关系、复习与练习入口。
- `src/data/cppProgrammingPractice.js`：独立上机题、参考程序和测试数据。
- `src/lessons/cpp/CppLessonComponents.jsx`：预测、跟踪、迁移和掌握度交互。
- `src/utils/lessonEvidence.js`：学习证据规则与兼容方案。
- `src/courses/CourseLevel1.jsx` 等及专题模块：总览与逐课学习保持一致。
- `scripts/`：复用已有流程、上机题编译、页面与无障碍检查；按实际风险补验证。

建议第一批实际交付为：48 课审计表、P0 修复清单、三级课程目标矩阵，以及 3 节样板课。样板通过后扩展到全量课程。

### 第一批执行记录（2026-09-20）

- 已建 [逐课基线审计](./cpp-l1-l3-audit.md)，覆盖 48 课的主题、官方等级定位和后续核查点；目前为初筛，逐题核验仍待继续。
- 样板 L1-9、L2-9、L3-5 已完成首轮纠错和边界训练补强；并把 L1-8 `switch`、L1-10 `do-while`、L2-8 数学函数、L3-12 函数入门缺口纳入本批修复。
- 验证结果：代码规范、课程流程、96 项项目测试、项目构建和页面冒烟检查均通过；L1～L3 的 24 道独立上机参考程序按 C++11 编译并通过 47 组样例，全部 44 道按 C++17 编译并通过 87 组样例。
- 下一步：逐课核对题源、代码与答案，补 L2/L3 独立任务，核查三级函数教学位置和学习证据规则。

### 第二批执行记录（2026-09-20）

- 为 L2、L3 各补 12 道独立上机题，三级合计 48 课均有任务；L2-12 标三级数组前瞻，L3-13 标图形复习拓展。
- 每题包含输入输出、范围、两组样例、分层提示、边界要求和参考实现。新增 24 题均按 C++11 编译与样例验证；总题数由 44 增至 68，样例由 87 增至 135。
- 后续仍需开展逐课题源与答案核对、学习证据区分和教学试学；题数补齐不等于教学效果已验证。
- 第二批验证：L1～L3 的 48 道参考程序以 C++11 编译并通过 95 组样例；全站 68 道参考程序以 C++17 编译并通过 135 组样例。96 项项目测试、代码规范、课程流程、构建和 68 个上机入口的页面检查均通过。

### 第三批执行记录（2026-09-20）

- L1～L3 的离课检查现在区分“已尝试”“迁移题已对照自查”“预测题答对”和“产出已提交”。单纯写下答案并展开解析不再触发完成。
- 迁移题增加明确的对照确认步骤；未核验的自查与产出在页面上如实标注。课程目录及首页回学入口把这些课的进度称为“已完成自查”，避免与系统判题混同。
- 已增加证据分级测试及真实迁移题页面流程检查。历史进度不做自动改写；此前记录的“已过关”仍保留原状态。
- 第三批验证：97 项项目测试、代码规范、课程流程与项目构建通过；离课检查覆盖 48 个桌面/手机页面组合，并验证预测题系统核对及迁移题“仅尝试不完成”的真实操作流程。
- 内容复核纠正了 L2-8 用 `sqrt(25)` 演示“可能截断为 4”的不成立示例；现在用 `sqrt(15)` 展示确定的浮点转整数截断，预测题与评价标准同步修订。
- L3-7 补 `cin` 接 `getline` 的残留换行提醒及空串下标检测；L3-8 改用 `string::size_type` 保存 `find` 结果，避免将无符号位置与 `-1` 混用。

### 第四批执行记录（2026-09-20）

- 深入核查 L3-2～L3-4 的补码、按位运算和移位：明确 8 位手算回绕与 C++ 有符号溢出的区别，修正 `<<` 结合顺序误述和掩码 4 的位号。
- 补充按位非的位宽前提、移位位数与结果范围限制；预备知识不再要求学生提前会下一课才教的 C++ 按位非运算。
- 二级数组链路统一改为“三级前瞻”：L2-12 课页与流程、L2-14 数组历史、L2-15 边界诊疗、L2-16 结课清单均作区分；修复 L2-14 示例缺少 `change` 读入及 L2-15 再次出现的 `i*i` 溢出写法。
- L3-13 图形打印标为二级复习与数字图形拓展，删除“三级真题喜欢”这种尚未逐题证实的断言。
- 回归结果：97 项项目测试、代码规范、课程流程和构建通过；扩展的课程页面检查覆盖 64 个桌面/手机组合，并验证预测题系统核对与迁移题对照自查流程。

### 第五批执行记录（2026-09-21）

- 处理 L1-2～L1-4 的先教后练断点：变量课补 `cin` 读入及键盘可操作的测验；整数与小数课补固定一位小数的格式输出，并把提前出现的数位拆解作业改为平均分变式。
- 整除与取余课明确除数不可为零、数位演示仅限三位正整数；奇数判断改为 `n % 2 != 0`，避免负奇数被误判。L1-7、L1-13 同步校正赋值表达式与余数符号的解释。
- L1-11、L1-16 将 `while(1)` 的退出路径说明改为准确表述，避免将 `break` 说成唯一退出方式。仍需继续逐课核对其余题源和答案；本批不代表 L1 全部内容已验收。
- 本地回归：97 项项目测试、代码规范、课程流程与 325 路由构建通过；L1～L3 的 48 道参考程序按 C++11 编译并通过 95 组样例。页面检查覆盖 64 个桌面/手机组合，并实际操作 L1-2 键盘答题和 L1-4 无效/有效数位输入。教学效果仍待真实试学验证。

### 第六批执行记录（2026-09-21）

- L1-5 改以 `char` 读入、原样输出和 `bool` 真假值为一级核心；独立上机题改为“字符名片”。ASCII 表、字符运算、大小写转换保留为明确标注的二级前瞻，避免把二级内容当成一级通关条件。
- 修正 `'AB'` 被一概判为语法错误、解码器显示的小写字母被样式强制大写、非 ASCII 字符仍显示编码值等误导。字符解码实验现仅面向 ASCII 可显示范围，并说明多字符普通字符字面量的值依赖实现；两道字符算术练习标为选学，原题出处待核。
- L1-6 补 `&&`、`||` 的短路求值和离课预测题；修正“先算术后逻辑”的笼统规则，明确 `!` 的优先级需要单独判断。课程流程与静态验收规则同步调整。
- 本批回归：97 项项目测试、代码规范、课程流程与 325 路由构建通过；L1～L3 的 48 道参考程序按 C++11 编译并通过 95 组样例。页面检查覆盖 64 个桌面/手机组合，并实际操作 L1-5 ASCII 解码器与 L1-6 短路预测题。
- 仍需继续逐课核对 L1～L3 的题源、代码、答案与跨课先修关系；真实试学效果尚未验证。

### 第七批执行记录（2026-09-21）

- L1-14 模拟课移除与课程目标不符的二维嵌套循环、已会模拟等先修条件；避坑案例改用一级已学的变量、循环和条件，不再提前要求二维数组、`memset` 或函数。迁移题允许先手推再概括公式，周期问题说明改为可逐步模拟，取模是常用简化方法。
- L1-15 统计课修正 `i % 2 = 1` 的错误诊断：这是左侧不可赋值导致的编译错误；奇数判断统一使用 `i % 2 != 0`。依据官方 2023 年 12 月一级《小杨报数》和 2024 年 12 月一级《奇数和偶数》核对输出格式：前者每个数字一行，后者同一行空格分隔两个计数。
- 报数演示限定可视化范围，空白、非整数和越界输入不再显示错误结果；原题与页面演示范围分别标明。验证：97 项项目测试、课程流程、相关文件代码规范及 325 路由构建通过；页面实际操作覆盖报数无效输入恢复和两道题的输出格式。
- 此批仍未完成 L1 全部题源逐题核验，也未开展真实学生试学。

### 第八批执行记录（2026-09-21）

- L1-16 一级总复习的数值转换演示改为 C++ 的向 0 截断，`-3.9` 现显示 `-3`；空白和超出演示范围的输入不再显示误导结果。补上 `char` 类型回顾。
- 运算优先级复习细化为括号、一元运算、乘除模、加减、大小关系、相等比较、`&&`、`||`，并用 `!2 + 3` 解释 `!` 的位置与括号作用。
- “图书馆里的老鼠”演示现在按照官方试卷的整数范围和“至少剩一本完整书”前提校验输入；无效输入不再展示与参考代码不一致的结果。
- 本批验证：97 项项目测试、代码规范、课程流程与 325 路由构建通过；页面实际操作覆盖 `-3.9 → -3`、空白输入、`!` 优先级说明以及老鼠题无效输入与有效结果。

### 第九批执行记录（2026-09-21）

- L2-2 ASCII 课改正“整数 0 在 ASCII 表里是 NUL”的混淆，先修条件只要求一级已教的字符、加法和引号区别。字符位置与 C++ 表达式输出类型分开讲解；未核实原题出处的两道题标为课堂练习。
- L2-3 类型转换课的实验室现在显示固定操作数与加法表达式，并依所选类型算出实际值和结果类型；`char + char`、`char + double` 等组合不再共用错误的固定结果。迁移题改用负小数向 0 截断，明确 `-3.9 → -3`。
- 本批验证：97 项项目测试、课程流程、代码规范和 325 路由构建通过；页面实际切换 `char + char → 130`、`double + char → 67.5`，并检查整数 0、字符 `'0'` 与 NUL 的说明。

### 第十批执行记录（2026-09-21）

- L2-1 存储课把 ROM 与硬盘/固态硬盘分开，不再用“速度最慢但量大”概括 ROM；速度动画移除未经限定的真实周期数字，明确只示意相对速度。
- 容量阶梯改用 B、KiB、MiB、GiB、TiB 表示每级 1024 的二进制换算；另说明商品硬盘常用十进制 GB，以及试题另有单位约定时按题目计算。课程流程与复习任务同步修订。
- IP 判断题限定在基于 IP 的网络，不再声称所有网络通信都必须使用 IP。DNS 示例不再固定真实站点当前地址；未经核实来源的三题标为课堂练习，并修正题干错字。
- 本批验证：97 项项目测试、代码规范、课程流程与 325 路由构建通过；页面实际检查容量单位、DNS 分步显示、课堂练习题与 IP 判断题范围。

### 第十一批执行记录（2026-09-21）

- L2-4 将 `case` 标签说明改为“整数常量表达式”：`case 1 + 1:` 和符合条件的 `constexpr` 值可以使用，普通运行时变量不行；`switch` 表达式不能是 `double`。范围判断题明确问 `case score >= 90:`，避免与合法的 `switch(score >= 90)` 混淆。
- 穿透实验现在只切换 `case 2` 的 `break`，`case 3` 和 `default` 保持不变；追踪结果与代码逐步一致。菜单无效选项的交互输出与展示代码统一，并给输入框添加关联标签。
- 本批验证：97 项项目测试、代码规范、课程流程与 325 路由构建通过；页面实际切换菜单异常输入和 `break` 前后代码/追踪结果，C++11 编译验证常量表达式标签及布尔表达式 `switch`。

### 第十二批执行记录（2026-09-21）

- L2-5 将“总次数 = 外层 × 内层”限定为内层每轮次数相同的情况；新增 `j <= i` 时 1+2+3=6 的对照，要求先逐轮计数。变量重新初始化说明也限定于 `for (int j=1; ...)` 这类写法。
- 迁移题补齐 `i`、`j` 的循环头声明；矩形打印题的问法限定到打印场景，并为行数/列数滑块关联输入标签。
- 本批验证：97 项项目测试、代码规范、课程流程与 325 路由构建通过；页面实际将行列改为 2×3 验证总数 6，并检查变长内层反例与迁移题声明。

### 第十三批执行记录（2026-09-21）

- L2-6 将 `n-i` 个前导空格加 `i` 个星号的图形统一称为“右对齐三角”，不再误称“金字塔”；追踪器里的可视化星号与展示代码一致。
- 图形实验只提供 n×n 时标“正方形（矩形特例）”，避免把方阵演示说成一般矩形。倒三角迁移题补循环变量声明，尺寸滑块补关联标签。
- 代码规范与 325 路由构建通过；页面实际切换 4×4 正方形和 4 行右对齐三角，核对空格与星号位置。

### 第十四批执行记录（2026-09-21）

- L2-7 流程图课补输入/输出的平行四边形，矩形只讲处理步骤；分数追踪实验在每个条件后标明“是/否”，让学生能对应菱形的分支箭头。掌握检查与课程流程同步到四种常见符号。
- 97 项项目测试、代码规范、课程流程与 325 路由构建通过；页面实际调整分数到 60 并检查两次判断路径，四种符号的布局和文案已核对。

### 第十五批执行记录（2026-09-21）

- L2-10 数位课修正追踪器代码与输出分隔不一致的问题；统一限定 0～9999 的非负整数，讲清 n=0 时循环不执行、统计数字 0 需要单独处理，以及 1200 反转为整数 21。
- L2-11 因数课限定正整数范围，明确取余除数不能为 0；公因数代码改为包含头文件的完整 C++11 程序，输出公因数列表而不是只计数。
- 97 项项目测试、代码规范、课程流程与 325 路由构建通过；页面实际验证 0、1200 与平方数 49；直接从课页提取的公因数程序按 C++11 编译，3 组输入输出通过。

### 第十六批执行记录（2026-09-21）

- L2-8 修正数学实验台把整数 `x/y` 错按浮点除法展示的矛盾；现在并列显示 `ceil(x/y)` 与 `ceil(1.0*x/y)`，补 `max/min`，并说明页面保留三位小数只是显示格式。
- L2-9 质数实验可操作 n=0，新增完全平方数 49 的含等号试除题；“包含 n 自己”的错误循环题限定 n≥2，移除未核实的“高频”措辞。
- 97 项项目测试、代码规范、课程流程与 325 路由构建通过；页面实际验证 7/3 两种 `ceil` 结果分别为 2、3，及 0、49 的质数判断。

### 第十七批执行记录（2026-09-21）

- L3-1 进制实验现支持十进制 0 并显示二/八/十六进制的 0；短除法新增 0 特例预测题。学习目标从“任意进制数”收束为本课实际讲授的二、八、十六进制非负整数。
- 位权公式改用明确的数学排版和上标，解释数学乘方与 C++ `^` 按位异或的区别，避免把公式代码块当作可复制的 C++ 表达式。
- 97 项项目测试、课程流程、325 路由构建及课页代码规范通过；页面实际操作 0、37 并检查位权排版和 0 特例题。

### 第十八批执行记录（2026-09-21）

- L3-6 数组统计实验不再静默跳过小数或非法字符，限定最多 50 个、单值 -10⁹～10⁹ 的整数；空输入保留 `sum=0`、`count=0`，平均数与最值显示为不可用，不再捏造 0。
- 求和模板改用 `long long` 并写明 `n > 0` 前提；最值模板明确只有非空数组才能访问 `a[0]`。前缀和作为进阶内容，改用 C++11 固定容量并检查 `1 <= l <= r <= n`。

### 第十九批执行记录（2026-09-21）

- L3-14 进制编程统一限定非负整数与 2～16 进制，保留 `n=0` 特判，并为实验滑块补关联标签。
- `fromBase` 现在拒绝空串、非法字符和不属于当前进制的位值，使用 `long long` 滚动计算并在乘加前检查溢出；补充完整 `toBase` 函数，非法进制返回空串。

### 第二十批执行记录（2026-09-21）

- L3-9 字符频率实验与课页代码统一为只统计小写 `a`～`z`，大写和其他字符不再被实验台静默转成小写。
- 频率与首次出现去重模板均在访问 26 项数组前检查字符范围；修复综合表中 `seen[c - 'a']` 漏引号及迁移题多余标点。

### 第二十一批执行记录（2026-09-21）

- L3-10 将两个可区分骰子的题目明确为有序结果，避免与 `a <= b` 的无序组合去重混淆；配对实验按 `a <= target/2`、`b <= target-a` 缩小范围。
- 复杂度预测题现在明确“两层都跑 1～n”才是 1 亿对，并说明能否通过还取决于时限和单次操作成本；提前停止限定为题目只需任意一个答案。

### 第二十二批执行记录（2026-09-21）

- L3-11 坐标实验支持连续或空格分隔的 R/L/U/D，拒绝非法指令并限制 40 步，非法字符不再被记录成一次原地移动。
- 模拟顺序不再概括成固定的“先更新再判断”：越界规则先检查下一状态，到达目标则在提交状态后判断，示例代码和离课检查同步更新。

### 第二十三批执行记录（2026-09-21）

- L3-12 的加密实验现在分别处理大写和小写，数字、空格、标点原样保留；输入与偏移滑块补关联标签。
- 删除未经核实的历史用法及具体真题样例断言，讲清 `0 ≤ k ≤ 25` 时加解密公式的适用条件；更大或负的偏移量先规范。
- 课程真题入口由不匹配的 2025-06 卷改为题库中包含《凯撒密码》的 2026-03 卷。

### 第二十四批执行记录（2026-09-21）

- L3-15 的综合样题改为完整 C++11 程序：`getline` 读整行、只统计小写字母、没有小写时明确输出，并列时按字典序最小输出。
- 拆题草稿改为明确标注的伪代码，补自然语言、流程图、伪代码三种算法描述的用途；策略实验的关键词只给候选方法，不代替输入范围和输出要求核对。

### 第二十五批执行记录（2026-09-21）

- L3-16 考前清单从零开始，标明“自查勾选”且不自动判断对错，按钮提供按压状态；数组下标小测加非空前提。
- 本批验证：97 项项目测试、代码规范、课程流程和 325 路由构建通过；直接提取 L3-15 课页 C++11 程序编译并验证正常输入、非小写、空行和并列；页面实测混合大小写加密、算法描述与清单从 0/6 到 1/6，浏览器控制台无错误。

### 第二十六批执行记录（2026-09-21）

- 核对 L3-12 关联的 2026-03 三级真题题库：选择题解析改正 `y+3→b`，说明 C 选项虽判断越界正确却在赋值时漏加 3，不再留下自我否定的推导。
- 编程题解析把解密偏移量范围改为 `0～25`，修复代码提示中的括号和运算符优先级；参考程序显式包含 `<string>` 并使用 C++11 范围循环。
- 本批验证：题库结构校验无错误，相关真题追溯测试 13 项通过；参考程序用 C++11 严格警告选项编译，并通过官方样例及正反向跨 `A/Z` 的两组边界输入。

### 第二十七批执行记录（2026-09-21）

- L3-13 从重复的基础图形目标收束为二级复习、居中金字塔数量规律和镂空图形坐标条件；数字图形继续作为选做迁移。
- 实验台把 n×n 图形标为正方形并补滑块标签与按钮状态，追踪输出统一为代码中的 `*`；输出示例单独展示，避免与可复制 C++ 代码混在一起。
- 课程入口改指向真正对应的 L2-6 图形打印复习课，不再把无图形输出题的三级卷标成此课真题；L3-14 先修提示改为进制和字符串知识。
- 本批验证：代码规范、课程与链接检查、97 项测试、325 路由构建通过；页面实测正方形与居中金字塔输出、按钮状态、二级复习入口，控制台无错误。
