# C++ 课程与 GESP 上机训练

当前共有 68 道原创课堂上机题，采用“课内读题、学生本机编程、样例与边界验收、阶段真题迁移”的方式。L1～L3 每课一题，L4～L8 各四道关键节点练习。课程首页的“上机与备考路线”可以直接进入相应课次的上机题。

## 课堂怎么用

1. 学完本课知识点，让学生先口述输入、处理、输出，并手算一组样例。
2. 学生新建 `.cpp` 文件独立编程；先不展开提示和参考实现。
3. 核对样例，再根据题目要求补一组边界数据。不要在程序输出中添加提示语。
4. 交付源代码、运行结果、边界数据与结果，以及一句错因复盘。
5. 达到题目所列前置学习节点后，再进入原题与阶段综合卷。先独立完成，再阅读解析；错因归入读题、算法、边界、格式，再回课内补练。

页面上的勾选是学生自查，保存在当前浏览器，不作为在线判题结果，也不自动授予课程掌握状态。真题提交使用洛谷入口；本网站未新增在线编译服务。

## 布点

| 当前课程 | 上机课次 | 训练主线 |
| --- | --- | --- |
| 一级 | 1–16，每课一题 | 输出、变量、运算、字符、逻辑、分支、循环、模拟、统计 |
| 二级 | 1–16，每课一题 | 存储换算、ASCII、类型转换、嵌套分支与循环、流程图、数学函数、数位、模拟与调试；第 12 课数组为三级前瞻 |
| 三级 | 1–16，每课一题 | 进制、固定位宽编码、位运算、数组、字符串、枚举、模拟、函数入门；第 13 课图形为复习拓展 |
| 四级 | 3、8、11、16 | 函数与数组、矩阵、选择排序、结构体排名 |
| 五级 | 2、3、10、16 | GCD/LCM、高精加法、二分、区间贪心 |
| 六级 | 2、4、12、16 | 树遍历、BFS、0/1 背包、网格 DP |
| 七级 | 5、7、9、16 | 图存储、最短转发次数、线性 DP、稳定排序 |
| 八级 | 2、7、9、16 | 组合数、Floyd、区间 DP、快速幂 |

等级与课次沿用当前站点课程位置，不能据此把拓展教学内容认定为同级必考点。例如一级的嵌套循环题已标为课程拓展。此轮完成教学练习与项目已有真题的衔接，没有对官方考纲做全量重新核定。

## 内容与验收

- 每题包含题面、输入输出格式、数据范围、思路提示、边界要求和折叠参考实现。
- 67 道输入题各有两组样例；L1 第 1 课为固定输出任务，提供一组无输入样例，共 135 组。
- 学生可下载不含答案的 Markdown 题目单；L1～L3 参考实现已按 C++11 验证，其他等级沿用 C++17，并优先使用本课已学方法。
- 真题入口来自项目现有题库，与题号、题名和所属试卷交叉验证。原题与课堂题的格式、范围可能不同，迁移前须重新读题。
- 可运行 `node scripts/validate-cpp-practice.mjs` 编译所有参考程序并逐一验证样例。L1～L3 的 C++11 兼容性可设置 `CPP_PRACTICE_STD=c++11`、`CPP_PRACTICE_LEVELS=1,2,3` 后运行同一脚本（PowerShell 使用 `$env:CPP_PRACTICE_STD='c++11'` 和 `$env:CPP_PRACTICE_LEVELS='1,2,3'`）。需要本机 C++ 编译器。
- 运行开发服务后，用 `CPP_PRACTICE_BASE_URL=http://127.0.0.1:4187 node scripts/check-cpp-practice-ui.mjs` 检查课次入口、首页路线、移动布局、折叠答案、题单下载和自查记录。

新增题目统一维护在 `src/data/cppProgrammingPractice.js`，无需逐个修改课程页面。新增节点时要同步检查前置知识、边界样例和真题迁移说明。
