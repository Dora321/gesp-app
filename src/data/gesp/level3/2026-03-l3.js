// 2026年3月 GESP C++ 三级真题
// 数据说明：
// - 客观题 1~15：依据官方 PDF 回填；少量代码/流程图题按官方版式做等价转写。
// - 判断题 16~25：答案表与题面均由官方 PDF 复核。
// - 编程题 26~27：题名、题意、样例与参考代码来自官方 PDF。
export const paperData = {
    id: '2026-03-l3',
    title: '2026年3月 GESP C++ 三级真题',
    level: 3,
    year: 2026,
    month: 3,
    session: 13,
    timeLimit: 90 * 60,
    source: {
        officialPdf: 'https://gesp.ccf.org.cn/101/attach/1734775052173344.pdf',
        notes: '少量流程图题按官方 PDF 做等价文字整理，保持答案与考点一致。',
    },
    questions: [
        {
            id: 1,
            type: 'single',
            question: `如果字符变量 \`_1\` 的值是字符 \`1\`，那么 \`(int)_1\` 的值是（ ）。`,
            options: ['1', '-1', '49', '+1 或者 -1'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (49)**
            
**选项逐项分析：**
- **A 1**：❌ 错误。字符 \`'1'\` 在内存中是以其 ASCII 码存储的，直接强转为 \`int\` 得到的是编码值而非数值本身。
- **B -1**：❌ 错误。无此转换逻辑。
- **C 49**：✅ 正确。在 ASCII 码表中，字符 \`'0'\` 的码值是 48，字符 \`'1'\` 的码值紧随其后，是 49。
- **D +1 或者 -1**：❌ 错误。类型转换结果是确定的单一数值。

**考点：** 字符在内存中的存储原理（ASCII）、强类型转换。`,
            tags: ['字符与ASCII', '类型转换'],
        },
        {
            id: 2,
            type: 'single',
            question: `\`a\`、\`b\` 是整型变量，各自有互不相同的初始值。下列程序实现了什么效果（ ）。\n\`\`\`cpp\na = a ^ b;\nb = a ^ b;\na = a ^ b;\n\`\`\``,
            options: ['`a`、`b` 的值从始至终都没有改变', '`a`、`b` 的值实现了互换', '`a`、`b` 的值互换了以后，又还回去了，相当于没有变化', '`a`、`b` 的值最后和原值不一样，没有任何意义'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (a、b 的值实现了互换)**

**选项逐项分析：**
- **A**：❌ 错误。异或运算具有自反性，经过这三次操作，变量的位模式发生了重组。
- **B**：✅ 正确。这是经典的“不利用临时变量交换两数”的位运算技巧。
  - 第一步：\`a = a ^ b\` (a 存储了两个原始值的差异信息)
  - 第二步：\`b = a ^ b\` (即 \`a_orig ^ b_orig ^ b_orig = a_orig\`)，此时 b 拿到了 a 的初值。
  - 第三步：\`a = a ^ b\` (即 \`a_orig ^ b_orig ^ a_orig = b_orig\`)，此时 a 拿到了 b 的初值。
- **C**：❌ 错误。逻辑推导错误。
- **D**：❌ 错误。这种方法在嵌入式开发等对内存极其敏感的场景下非常有意义。

**考点：** 位运算的属性（结合律、自反性）、异或交换律应用。`,
            tags: ['位运算', '异或'],
        },
        {
            id: 3,
            type: 'single',
            question: `关于下列正确的程序段，说法正确的是（ ）。\n\`\`\`cpp\nchar str1[] = "Hello";\nchar str2[] = {'H', 'e', 'l', 'l', '0'};\ncout << str1 << endl;\ncout << str2 << endl;\n\`\`\``,
            options: ['字符数组 `str1` 和 `str2` 完全相同', '这段程序多次执行将输出不同的结果', '字符数组 `str1` 和 `str2` 不相等', '这两个赋值方式完全相同'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (字符数组 str1 和 str2 不相等)**

**选项逐项分析：**
- **A**：❌ 错误。\`str1\` 使用字符串字面量初始化，编译器会自动在末尾添加字符串结束符 \`'\\0'\`，其长度为 6。\`str2\` 仅包含 5 个指定字符，没有 \`'\\0'\`。
- **B**：❌ 错误。虽然 \`str2\` 缺少结束符会导致 \`cout\` 输出时可能出现乱码（直到撞上内存中下一个 \`0\`），但程序本身的定义是静态确定的。
- **C**：✅ 正确。由于 \`str1\` 包含隐藏的结束符而 \`str2\` 不包含，两者的内存布局和逻辑含义均不相同。
- **D**：❌ 错误。一种是字符串初始化，一种是字符列表初始化，语义和内存占用均有差异。

**考点：** 字符串字面量与普通字符数组的区别、\`'\\0'\` 终止符的作用。`,
            tags: ['字符串', '字符数组'],
        },
        {
            id: 4,
            type: 'single',
            question: `关于以下程序段，说法正确的是（ ）。\n\`\`\`cpp\nint x = 10;\ncout << (x++) + (++x) << endl;\n\`\`\``,
            options: ['C++11 标准中，这是未定义行为，不同的环境有可能出现不同的结果', '22', '21', '20'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (C++11 标准中，这是未定义行为)**

**选项逐项分析：**
- **A**：✅ 正确。在 C++11 之前的标准和部分后续语境中，在同一个序列点（Sequence Point）内多次修改同一个变量的值（如 \`x++\` 和 \`++x\` 出现在同一个加法表达式中）属于**未定义行为 (Undefined Behavior)**。不同的编译器或优化等级可能给出 21, 22 或其他结果。
- **B / C / D**：❌ 错误。这些选项试图给出一个确定的数值，但在 C++ 标准规范下，这种代码是不安全的，不应产生依赖。

**考点：** 表达式求值顺序、自增运算符的副作用、未定义行为。`,
            tags: ['运算符', '未定义行为'],
        },
        {
            id: 5,
            type: 'single',
            question: `8 位二进制下，十进制数 \`-15\` 的补码是（ ）。`,
            options: ['11110000', '10001111', '10010000', '11110001'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (11110001)**

**解析：**
计算负数补码的标准流程：
1. **原码**：15 的二进制是 \`0000 1111\`。
2. **反码**：符号位不变（此处指逻辑取反），数值位按位取反得到 \`1111 0000\`。
3. **补码**：反码加 1，即 \`1111 0000 + 1 = 1111 0001\`。

- **A / B / C**：❌ 错误。未能正确执行“取反加一”的步进逻辑。

**考点：** 计算机数值表示（原码、反码、补码）的转换规则。`,
            tags: ['二进制', '补码'],
        },
        {
            id: 6,
            type: 'single',
            question: `三进制数 \`2102(3)\` 转换成十进制是（ ）。`,
            options: ['63', '65', '67', '69'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (65)**

**解析：**
进制转换公式：$\sum (digit \times base^{position})$。
对于三进制数 $2102_{(3)}$：
1. $2 \times 3^3 = 2 \times 27 = 54$
2. $1 \times 3^2 = 1 \times 9 = 9$
3. $0 \times 3^1 = 0 \times 3 = 0$
4. $2 \times 3^0 = 2 \times 1 = 2$
$54 + 9 + 0 + 2 = 65$。

- **A / C / D**：❌ 错误。计算过程中权值分配或相加出现疏漏。

**考点：** 任意进制向十进制的转换方法。`,
            tags: ['进制转换'],
        },
        {
            id: 7,
            type: 'single',
            question: `二进制数 \`10110101\` 是某数的 8 位补码，该数的十进制是（ ）。`,
            options: ['-73', '-75', '-77', '75'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (-75)**

**解析：**
已知补码求原码的逆向推导：
1. **识别符号位**：首位为 \`1\`，确定该数为**负数**。
2. **反向操作（取反加一）**：
   - 将 \`1011 0101\` 除去符号位以外按位取反（或整体取反再加一，结果一致）：\`0100 1010\`。
   - 加 1 得到：\`0100 1011\`。
3. **计算绝对值**：$64 + 8 + 2 + 1 = 75$。
4. **组合结果**：加上之前的负号，得 **-75**。

- **A / C**：❌ 错误。
- **D 75**：❌ 错误。忽略了首位符号位的含义。

**考点：** 补码与十进制负数之间的双向转换。`,
            tags: ['二进制', '补码'],
        },
        {
            id: 8,
            type: 'single',
            question: `已知 \`unsigned char c = 0x0F;\`（十六进制 \`0F\` = 二进制 \`00001111\`），执行 \`c = c << 3;\` 后，\`c\` 的十进制值是（ ）。`,
            options: ['64', '72', '80', '120'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (120)**

**解析：**
1. **初始状态**：\`0x0F\` 对应二进制 \`0000 1111\`。
2. **移位操作**：\`<< 3\`（左移 3 位），高位丢弃，低位补 0。
   - 结果：\`0111 1000\`。
3. **转十进制**：
   - 权值计算：$2^6(64) + 2^5(32) + 2^4(16) + 2^3(8) = 120$。

- **A / B / C**：❌ 错误。移位位数或位权换算错误。

**考点：** 位运算符（左移 \`<<\`）的原理与数值计算。`,
            tags: ['位运算', '左移'],
        },
        {
            id: 9,
            type: 'single',
            question: `补码的情况下，关于按位取反运算，用笔计算的情况下，以下说法错误的是（ ）。`,
            options: ['`~5` 的结果是 `-6`（`int` 类型，32 位）', '`~0` 的结果是 `0`（`int` 类型，32 位）', '`~(-3)` 的结果是 `2`（`int` 类型，32 位）', '`~8` 的结果是 `-9`（`int` 类型，32 位）'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (~0 的结果是 0)**

**解析：**
在计算机中，按位取反运算符 \`~\` 作用于整数 $x$ 的结果通常遵循公式：\`~x = -(x + 1)\`。
- **A ~5**：\`-(5 + 1) = -6\`。✅ 正确。
- **B ~0**：\`-(0 + 1) = -1\`。在 32 位补码下，\`~0\` 会将所有 \`0\` 变为 \`1\`，结果是 \`-1\`。❌ 错误（该说法本身错误，故为正确答案）。
- **C ~(-3)**：\`-(-3 + 1) = -(-2) = 2\`。✅ 正确。
- **D ~8**：\`-(8 + 1) = -9\`。✅ 正确。

**考点：** 按位取反运算符 (\`~\`) 的补码运算性质。`,
            tags: ['位运算', '按位取反'],
        },
        {
            id: 10,
            type: 'single',
            question: `执行以下 C++ 代码后，\`sub\` 的值是（ ）。\n\`\`\`cpp\nstring str = "GESP2026";\nstring sub = str.substr(4, 2);\n\`\`\``,
            options: ['20', '02', '2026', '026'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (20)**

**解析：**
标准库 \`string::substr(pos, len)\` 函数：
- \`pos\`: 子串的起始下标（从 0 开始）。
- \`len\`: 子串的长度。
对于 \`"GESP2026"\`：
- 下标：0(G), 1(E), 2(S), 3(P), **4(2)**, 5(0), 6(2), 7(6)。
- 从下标 4 开始，截取 2 个字符，即 **"20"**。

- **B 02**：❌ 错误。起始位置计算正确但截取内容偏移。
- **C 2026**：❌ 错误。这是截取到末尾的结果，长度不止 2。
- **D 026**：❌ 错误。

**考点：** C++ 标准库字符串子串截取函数 \`substr\` 的参数定义。`,
            tags: ['字符串', 'substr'],
        },
        {
            id: 11,
            type: 'single',
            question: `执行以下代码后，输出结果是（ ）。\n\`\`\`cpp\nint arr[] = {5, 10, 15, 20, 25, 30};\nint count = sizeof(arr) / sizeof(arr[0]);\ncout << count;\n\`\`\``,
            options: ['4', '6', '24', '30'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (6)**

**解析：**
在 C++ 中，\`sizeof\` 运算符返回整个对象所占的字节数。
- **总大小**：数组 \`arr\` 包含 6 个 \`int\` 元素，每个 \`int\` 占 4 字节，故 \`sizeof(arr)\` = 24。
- **单个大小**：\`sizeof(arr[0])\` = 4。
- **元素个数**：$24 / 4 = 6$。

- **A / C / D**：❌ 错误。

**考点：** 使用 \`sizeof\` 计算静态数组长度的经典方法。`,
            tags: ['数组', 'sizeof'],
        },
        {
            id: 12,
            type: 'single',
            question: `执行以下代码后，输出结果是（ ）。\n\`\`\`cpp\nchar s[10] = "abcde";\nint a = sizeof(s) / sizeof(s[0]);\nint b = strlen(s);\ncout << a - b;\n\`\`\``,
            options: ['4', '5', '6', '10'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (5)**

**解析：**
区分 \`sizeof\` 和 \`strlen\` 是 C/C++ 字符串处理的基础：
- **sizeof(s)**：返回数组定义的**总容量**。这里定义为 \`char s[10]\`，故为 10。
- **strlen(s)**：返回字符串的**逻辑长度**（不含结束符 \`'\\0'\`）。\`"abcde"\` 包含 5 个有效字符，故为 5。
- **结果**：$10 - 5 = 5$。

- **A / C / D**：❌ 错误。混淆了数组容量与有效字符串长度的概念。

**考点：** \`sizeof\` (空间占用) 与 \`strlen\` (有效长度) 的本质区别。`,
            tags: ['字符串', 'sizeof'],
        },
        {
            id: 13,
            type: 'single',
            question: `以下问题中，最不适合用枚举法解决的是（ ）。`,
            options: ['找出 `1~100` 之间所有能被 `7` 整除的数', '找出 `100~200` 之间的所有质数', '计算 `1+2+3+...+1000` 的和', '找出三位数中个位、十位、百位数字之和等于 `10` 的数'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (计算 1+2+3+...+1000 的和)**

**解析：**
枚举法适合于寻找符合特定条件的离散解，但当存在高效数学公式时应优先使用公式。
- **A / B / D**：✅ 适合。需要遍历区间检查整除、质数性或位属性，没有简单的 $O(1)$ 公式。
- **C**：❌ 最不适合。此问题是经典的等差数列求和，直接利用高斯公式 $Sum = n(n+1)/2$ 即可在常数时间内求得结果，无需从 1 枚举到 1000 做累加。

**考点：** 算法效率评估、枚举法的适用场景与局限性。`,
            tags: ['枚举', '算法思想'],
        },
        {
            id: 14,
            type: 'single',
            question: `用枚举法解决“鸡兔同笼问题：头共 35 个，脚共 94 只，求鸡和兔的数量”，以下枚举逻辑最合理的是（ ）。`,
            options: [
                '枚举鸡的数量 `x`（`0~35`），兔的数量 `y = 35 - x`，判断 `2*x + 4*y == 94`',
                '枚举兔的数量 `y`（`0~94`），鸡的数量 `x = 35 - y`，判断 `2*x + 4*y == 94`',
                '枚举所有整数 `x`（`0~100`）和 `y`（`0~100`），判断 `x+y==35 && 2*x+4*y==94`',
                '枚举脚的总数 `sum`（`0~94`），判断 `sum == 94`',
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：A (枚举鸡的数量 x（0~35），兔的数量 y = 35 - x，判断...)**

**解析：**
枚举法的核心优化原则是：**减少枚举变量，缩小搜索范围**。
- **A**：✅ 最合理。已知头数固定为 35，只需枚举“鸡”的数量，则“兔”的数量自动确定。枚举次数仅 36 次。
- **B**：❌ 不合理。题目已知头数 35，脚数 94。兔子的数量不可能达到 94。
- **C**：❌ 效率低。使用了双重循环（或两组变量），枚举次数达 $101 \times 101$ 次。
- **D**：❌ 逻辑错误。枚举目标与求解目标不匹配。

**考点：** 枚举算法的优化（约束条件的应用、变量降维）。`,
            tags: ['枚举', '应用题'],
        },
        {
            id: 15,
            type: 'single',
            question: `模拟“字符串加密”：规则为“每个字符 ASCII 码 + 3，若超过 \`z\`（122）则从 \`a\` 重新开始”，以下代码中正确的条件判断是（ ）。\n\`\`\`cpp\nfor (int i = 0; i < str.length(); i++) {\n // 需补充条件：\n _______________________\n else {\n str[i] += 3;\n }\n}\n\`\`\``,
            options: [
                '```cpp\nif (str[i] > 122) str[i] = str[i] + 3 - 26;\n```',
                '```cpp\nif (str[i] > 122) str[i] = str[i] - 26;\n```',
                '```cpp\nif (str[i] + 3 > 122) str[i] = str[i] - 26;\n```',
                "```cpp\nif (str[i] + 3 > 'z') str[i] = 'a' + (str[i] + 3 - 'z') - 1;\n```",
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (if (str[i] + 3 > 'z') str[i] = 'a' + (str[i] + 3 - 'z') - 1;)**

**解析：**
加密逻辑要求在超过 \`'z'\` 时“循环绕回”。
- **越界判定**：条件应为 \`str[i] + 3 > 'z'\` 或 \`str[i] + 3 > 122\`。
- **重定位计算**：
  - 溢出量为 \`str[i] + 3 - 'z'\`。
  - 从 \`'a'\` 开始偏置，但需注意 \`'a'\` 本身占据一个位点，故需减 1。
  - 例如 \`str[i]\` 为 \`'y'\` (121)，加上 3 变为 124。溢出量 $124 - 122 = 2$。新字符应为 \`'a' + 2 - 1 = 'b'\`。符合规律（y->z->a->b）。

- **A / B / C**：❌ 错误。计算逻辑未能精准实现字符集的闭环映射。

**考点：** 字符集的循环模拟（模运算思想应用）。`,
            tags: ['字符串', '模拟'],
        },
        {
            id: 16,
            type: 'judge',
            question: `定义 \`int arr[5] = {1,2,3};\`，则 \`arr[3]\` 的值为 \`0\`，\`arr[5]\` 是合法下标。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. **部分初始化**：对于定义了长度的数组，若初始化列表长度小于定义长度，剩余元素（如 \`arr[3]\` 和 \`arr[4]\`）会自动补 \`0\`。因此 \`arr[3]\` 的值确实为 \`0\`。
2. **合法下标**：C++ 数组下标从 \`0\` 开始。定义为 \`arr[5]\` 的数组，其合法下标范围是 \`0\` 到 \`4\`。\`arr[5]\` 属于**越界访问**。
由于后半句描述错误，整体判定为错误。

**考点：** 数组初始化规则、数组下标范围与越界判定。`,
            tags: ['判断题', '数组'],
        },
        {
            id: 17,
            type: 'judge',
            question: `定义 \`double arr[10];\`，未手动初始化时，数组中所有元素的默认值为 \`0.0\`。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
在 C++ 中，局部变量（在函数内部定义的非静态变量）存储在**栈区**，编译器不会对其进行自动清零初始化。其初始值是内存中残留的随机数据（通常称为“垃圾值”）。只有全局变量或静态变量才会被自动初始化为 \`0.0\`。

**考点：** 变量的作用域、存储类型及默认初始化规则。`,
            tags: ['判断题', '数组'],
        },
        {
            id: 18,
            type: 'judge',
            question: `定义 \`int arr[] = {1,2,3};\`，则 \`sizeof(arr)\` 的结果为 \`12\`（\`int\` 占 4 字节）。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
\`sizeof\` 作用于数组名时，返回的是数组所占的总内存空间。
- 元素个数：根据初始化列表 \`{1, 2, 3}\` 自动推导为 3。
- 单个元素空间：\`int\` 在标准环境下占 4 字节。
- 总空间：$3 \times 4 = 12$ 字节。

**考点：** \`sizeof\` 运算符对数组空间大小的计算逻辑。`,
            tags: ['判断题', 'sizeof'],
        },
        {
            id: 19,
            type: 'judge',
            question: `下面的流程图是用来求 \`1+2+3+...+10\` 的和。请判断：这个流程图的逻辑正确还是错误？`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. **初始化**：\`sum = 0\`, \`i = 1\`。
2. **循环条件**：若 \`i <= 10\` 则执行循环体。
3. **累加与步进**：\`sum = sum + i\`, \`i = i + 1\`。
4. **终止条件**：当 \`i\` 增加到 11 时跳出。
此逻辑完整实现了 $1+2+...+10$ 的算术级数求和。

**考点：** 流程图逻辑分析、循环累加算法。`,
            tags: ['判断题', '流程图'],
        },
        {
            id: 20,
            type: 'judge',
            question: `下面流程图的功能是计算 \`5\` 对 \`2\` 取余数，输出结果为 \`1\`。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
求余运算（取模）在流程图中通常通过循环减法或直接取模判定。
$5 \div 2 = 2 \dots 1$。
该流程图能够正确处理除数为 2 的取余逻辑并输出余数 1。

**考点：** 流程图算法实现、取模运算逻辑。`,
            tags: ['判断题', '流程图'],
        },
        {
            id: 21,
            type: 'judge',
            question: `已知大写字符 \`A\` 的 ASCII 编码的十六进制表示为 \`0x41\`，计算字符 \`m\` 的 ASCII 编码的八进制表示为 \`155\`（八进制）。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. **确定 m 的十进制码**：已知 \`A\` 为 65。\`m\` 是小写字母。
   - \`A(65)\` -> \`Z(90)\`
   - \`a(97)\` -> \`m\` 是第 13 个小写字母 ($97 + 13 - 1 = 109$)。
2. **进制转换 (10 -> 8)**：
   - $109 \div 8 = 13 \dots 5$
   - $13 \div 8 = 1 \dots 5$
   - $1 \div 8 = 0 \dots 1$
   - 从下往上读得 **155**。

**考点：** 字符集映射（ASCII）、十进制与八进制转换。`,
            tags: ['判断题', '字符与ASCII'],
        },
        {
            id: 22,
            type: 'judge',
            question: `在 C++ 位运算中，各种不同的运算符有优先级的区分，使用括号能够解决优先级的问题。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
C++ 位运算符的优先级通常较低（如 \`&\` 低于 \`+\`），容易产生逻辑隐患。例如 \`a & 3 == 0\` 会被解析为 \`a & (3 == 0)\`。在编写代码时，使用括号 \`( )\` 可以强制指定优先级，确保逻辑的清晰性与准确性。

**考点：** 运算符优先级关系、括号对执行顺序的显式控制。`,
            tags: ['判断题', '位运算'],
        },
        {
            id: 23,
            type: 'judge',
            question: `由于在 \`0~255\` 范围内，\`char\` 类型和 \`int\` 类型可以互换，因此在这里 \`x\` 和 \`y\` 相等。\n\`\`\`cpp\nchar x = '1';\nint y = 1;\n\`\`\``,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
- \`char x = '1'\`：\`x\` 存储的是字符 \`1\` 的编码值，即 **49**。
- \`int y = 1\`：\`y\` 存储的是整数数值 **1**。
在进行比较时，数值 49 不等于 1。

**考点：** 字符字面量（编码值）与整型数值的本质区别。`,
            tags: ['判断题', '字符与ASCII'],
        },
        {
            id: 24,
            type: 'judge',
            question: `在 C++ 语言中，表达式 \`((0xf0 + 0x15) == 255)\` 的值为 \`true\`。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. **十六进制转换**：\`0xf0 = 15 \times 16 + 0 = 240\`；\`0x15 = 1 \times 16 + 5 = 21\`。
2. **求和**：$240 + 21 = 261$。
3. **判定**：$261 \neq 255$。

**考点：** 十六进制数值的计算与比较。`,
            tags: ['判断题', '十六进制'],
        },
        {
            id: 25,
            type: 'judge',
            question: `如果 \`a\` 为 \`int\` 类型的变量，且 \`a\` 的二进制最低位为 \`0\`，则表达式 \`((a & 3 & 1) == 0)\` 的值为 \`true\`。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. **前提条件**：\`a\` 的最低位为 \`0\`，意味着 \`(a & 1) == 0\`。
2. **逻辑展开**：表达式 \`(a & 3 & 1)\`。由于位与运算满足交换律和结合律，原式等价于 \`(a & 1) & 3\`。
3. **计算结果**：因为 \`(a & 1)\` 是 \`0\`，所以 \`0 & 3\` 结果必然是 \`0\`。
表达式成立。

**考点：** 位运算规律、数值奇偶性与最低位的关系。`,
            tags: ['判断题', '位运算'],
        },
    ],
    programmingQuestions: [
        {
            id: 26,
            type: 'programming',
            title: '二进制回文串',
            problemNumber: 'B4499',
            score: 25,
            description: '对于一个正整数 `n`，把它转换成不含前导零的二进制表示；如果这个二进制序列从左向右读与从右向左读完全相同，则称该数为二进制回文数。例如，`9` 的二进制表示为 `1001`，是二进制回文数；`12` 的二进制表示为 `1100`，不是二进制回文数。给定一个正整数 `n`，请计算在 `1` 到 `n` 的范围内二进制回文数的数量。',
            inputDescription: '输入一行，包含一个正整数 `n`。',
            outputDescription: '输出一行，包含一个数，表示在 `1` 到 `n` 的范围内二进制回文数的数量。',
            samples: [
                {
                    input: '15',
                    output: '6',
                },
            ],
            explanation: `**解析：**
本题要求统计 $1 \dots n$ 范围内其二进制表示（不含前导零）为回文串的数字个数。
1. **进制转换**：通过反复取模（\`% 2\`）和整除（\`/ 2\`），可以将十进制数转换为二进制位数组。
2. **回文判定**：使用双指针或对称索引检查数组首尾字符是否一致。
3. **范围枚举**：由于 $n$ 的范围通常在 $10^6$ 以内，直接 $O(n \log n)$ 暴力枚举每个数即可通过。

**核心逻辑提示：**
\`\`\`cpp
// 核心：十进制转二进制并存入数组
int t = i, pos = 0;
while (t) {
    bits[pos++] = t % 2;
    t /= 2;
}
// 核心：判定回文
for (int j = 0; j < pos / 2; j++) {
    if (bits[j] != bits[pos - 1 - j]) { /* 不回文 */ }
}
\`\`\`

**考点：** 进制转换（十进制转二进制）、数组应用、回文判定逻辑。`,
            tags: ['编程题', '进制转换', '枚举'],
            template: '#include <iostream>\nusing namespace std;\n\nint main() {\n    int n;\n    cin >> n;\n    // 在此编写代码\n    return 0;\n}',
            referenceCode: '#include <iostream>\nusing namespace std;\n\nint main() {\n    int n, a[50] = {0}, ans = 0;\n    cin >> n;\n    for (int i = 1; i <= n; i++) {\n        int t = i, pos = 0;\n        while (t) {\n            a[pos++] = t % 2;\n            t /= 2;\n        }\n        bool ok = true;\n        for (int j = 0; j < pos; j++)\n            if (a[j] != a[pos - j - 1]) {\n                ok = false;\n                break;\n            }\n        ans += ok;\n    }\n    cout << ans;\n    return 0;\n}',
        },
        {
            id: 27,
            type: 'programming',
            title: '凯撒密码',
            problemNumber: 'B4500',
            score: 25,
            description: '凯撒密码是一种替换加密技术，明文中的所有字母都会按照固定偏移量进行整体平移。例如偏移量是 `3` 时，`A` 会替换成 `D`，`B` 会替换成 `E`，……，`X` 会替换成 `A`，`Y` 会替换成 `B`，`Z` 会替换成 `C`。现在给你一组已破解的凯撒密码明文与密文，以及一个使用相同偏移量的待破解密文，请输出它对应的明文。',
            inputDescription: '输入共三行：第一行是已破解的凯撒密码明文；第二行是对应密文；第三行是待破解的凯撒密码密文。',
            outputDescription: '输出一行，包含一个字符串，表示待破解的凯撒密码对应的明文。',
            samples: [
                {
                    input: 'ABCDEFGVWXYZ\nDEFGHIJYZABC\nWKHTXLFNEURZQIRAMXPSVRYHUWKHODCBGRJ',
                    output: 'THEQUICKBROWNFOXJUMPSOVERTHELAZYDOG',
                },
            ],
            explanation: `**解析：**
凯撒密码的本质是字符集的循环位移。
1. **计算偏移量**：通过已知的明文 $P$ 和密文 $C$，偏移量 $d = P[0] - C[0]$。注意 $d$ 可能是负数，需通过 \`(d % 26 + 26) % 26\` 归一化到 $0 \dots 255$ 范围。
2. **解密变换**：对于待破解字符 $X$，明文 $X' = (X - 'A' + d) \pmod{26} + 'A'$。
3. **逐字符处理**：利用 \`string\` 或字符数组遍历输入并输出转换后的结果。

**核心逻辑提示：**
\`\`\`cpp
// 计算偏移量（以第一个字符为例）
int shift = (plain[0] - cipher[0] % 26 + 26) % 26;

// 变换逻辑
char original = (target[i] - 'A' + shift) % 26 + 'A';
\`\`\`

**考点：** 字符串处理、凯撒加密原理、模运算的负数处理技巧。`,
            tags: ['编程题', '字符串', '模拟'],
            template: '#include <iostream>\n#include <cstring>\nusing namespace std;\n\nint main() {\n    string s1, s2, s3;\n    cin >> s1 >> s2 >> s3;\n    // 在此编写代码\n    return 0;\n}',
            referenceCode: '#include <iostream>\n#include <cstring>\nusing namespace std;\n\nint main() {\n    string s1, s2, s3;\n    cin >> s1 >> s2 >> s3;\n    int d = s1[0] - s2[0];\n    d = (d % 26 + 26) % 26;\n    for (int i = 0; i < s3.length(); i++) {\n        int ch = (s3[i] - \'A\' + d) % 26;\n        cout << (char)(ch + \'A\');\n    }\n    return 0;\n}',
        },
    ],
};
