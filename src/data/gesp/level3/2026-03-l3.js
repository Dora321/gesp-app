// 2026年3月 GESP C++ 三级真题
// 数据说明：
// - 客观题 1~15：依据官方 PDF 回填；少量代码/流程图题按官方版式做等价转写。
// - 判断题 16~25：答案表与题面均由官方 PDF 复核。
// - 编程题 26~27：题名、题意、样例与参考代码来自官方 PDF。
export const paperData = {
    id: '2026-03-l3',
    title: '2026年3月 GESP C++ 三级真题',
    level: 3,
    year: 2026,
    month: 3,
    session: 13,
    timeLimit: 90 * 60,
    source: {
        officialPdf: 'https://gesp.ccf.org.cn/101/attach/1734775052173344.pdf',
        type: 'official',
        notes: '少量流程图题按官方 PDF 做等价文字整理，保持答案与考点一致。',
    },
    verification: {
        status: 'partial',
        reviewedBy: '本站校订',
        reviewedAt: '2026-07-14',
        scope: '单选题 1-15 的答案与全卷题量已对照 CCF GESP 官方 PDF。',
    },
    questions: [
        {
            id: 1,
            type: 'single',
            question: `如果字符变量 \`_1\` 的值是字符 \`1\`，那么 \`(int)_1\` 的值是（ ）。`,
            options: ['1', '-1', '49', '+1 或者 -1'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (49)**

**核心解析：**
字符在 C++ 中以 ASCII 码形式存储。\`'0'\` 的 ASCII 码是 48，字符 \`'1'\` 的 ASCII 码是 49。当执行强制类型转换 \`(int)_1\` 时，得到的是字符 \`'1'\` 对应的 ASCII 码值 49，而非数字 1 本身。

**选项逐项分析：**
- **A 1**：❌ 错误。字符 \`'1'\` 在内存中存储的是其 ASCII 码值 49，直接强转为 \`int\` 得到的是编码值 49，而非数字值 1。
- **B -1**：❌ 错误。不存在这样的转换逻辑，字符到整数的转换不会出现负值。
- **C 49**：✅ 正确。字符 \`'1'\` 的 ASCII 码值为 49，强转后得到该码值。
- **D +1 或者 -1**：❌ 错误。类型转换结果是确定的单一数值，不存在多种可能。

**易错提醒：**
- 混淆字符的“显示值”与“存储值”，字符 \`'1'\` 显示为数字 1，但存储的是 ASCII 码 49。
- 误以为字符转整数会发生数学运算（如加1、减1），实际上只是码值提取。

**知识延伸：**
常见 ASCII 码：'A' = 65, 'a' = 97, '0' = 48。掌握这些基准值可以快速推算其他字符的码值。

**考点：** 字符与 ASCII 码的对应关系、强制类型转换。`,
            tags: ['字符与ASCII', '类型转换'],
        },
        {
            id: 2,
            type: 'single',
            question: `\`a\`、\`b\` 是整型变量，各自有互不相同的初始值。下列程序实现了什么效果（ ）。\n\`\`\`cpp\na = a ^ b;\nb = a ^ b;\na = a ^ b;\n\`\`\``,
            options: ['`a`、`b` 的值从始至终都没有改变', '`a`、`b` 的值实现了互换', '`a`、`b` 的值互换了以后，又还回去了，相当于没有变化', '`a`、`b` 的值最后和原值不一样，没有任何意义'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (\`a\`、\`b\` 的值实现了互换)**

**核心解析：**
异或运算具有结合律和自反性（即 \`a ^ a = 0\`，\`a ^ 0 = a\`），利用这一特性可以交换两变量而不使用临时变量。设初始值为 \`a_orig\` 和 \`b_orig\`，逐步推导：

1. \`a = a ^ b\` → \`a = a_orig ^ b_orig\`（此时 a 保存了两者的差异信息）
2. \`b = a ^ b\` → \`b = (a_orig ^ b_orig) ^ b_orig = a_orig\`（b 获得 a 的原始值）
3. \`a = a ^ b\` → \`a = (a_orig ^ b_orig) ^ a_orig = b_orig\`（a 获得 b 的原始值）

交换完成。

**选项逐项分析：**
- **A \`a\`、\`b\` 的值从始至终都没有改变**：❌ 错误。三次异或操作后，变量值发生了实质性改变。
- **B \`a\`、\`b\` 的值实现了互换**：✅ 正确。逐步推导可见交换成功。
- **C \`a\`、\`b\` 的值互换了以后，又还回去了，相当于没有变化**：❌ 错误。推导过程表明交换后值已互换，不会再还原。
- **D \`a\`、\`b\` 的值最后和原值不一样，没有任何意义**：❌ 错误。该技巧在嵌入式开发、寄存器操作等内存敏感场景下有重要应用价值，并非无意义。

**易错提醒：**
- 误以为异或三次会还原，实际还原需要的是“与自身异或两次”而非“与对方异或两次”。
- 当 a 和 b 相同时，异或交换会失效（结果为 0），但题目已说明两者初始值不同。

**知识延伸：**
异或交换的局限性：若 a 和 b 指向同一内存位置（如 \`a = &x; b = &x;\`），异或操作会导致 x 被清零。实际编程中，除非有特殊性能要求，通常使用临时变量更安全易读。

**考点：** 异或运算的数学性质（结合律、自反性）、位运算技巧。`,
            tags: ['位运算', '异或'],
        },
        {
            id: 3,
            type: 'single',
            question: `关于下列正确的程序段，说法正确的是（ ）。\n\`\`\`cpp\nchar str1[] = "Hello";\nchar str2[] = {'H', 'e', 'l', 'l', '0'};\ncout << str1 << endl;\ncout << str2 << endl;\n\`\`\``,
            options: ['字符数组 `str1` 和 `str2` 完全相同', '这段程序多次执行将输出不同的结果', '字符数组 `str1` 和 `str2` 不相等', '这两个赋值方式完全相同'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (字符数组 str1 和 str2 不相等)**

**核心解析：**
两种初始化的本质差异在于字符串结束符的处理：

\`char str1[] = "Hello";\` — 使用字符串字面量初始化，编译器自动在末尾添加 \`'\\0'\`，数组长度为 6（5个字符 + 1个结束符）。

\`char str2[] = {'H', 'e', 'l', 'l', '0'};\` — 使用字符列表初始化，仅包含5个指定字符，数组长度固定为 5。注意：此处 \`'0'\` 是数字零字符（ASCII 48），而非 \`'\\0'\`。

**选项逐项分析：**
- **A 字符数组 \`str1\` 和 \`str2\` 完全相同**：❌ 错误。str1 有6个元素（含 \`'\\0'\`），str2 只有5个元素，内存布局不同。
- **B 这段程序多次执行将输出不同的结果**：❌ 错误。程序逻辑是静态确定的，多次执行输出结果相同（虽然 str2 缺 \`'\\0'\` 可能导致输出乱码，但这是确定的不安全行为）。
- **C 字符数组 \`str1\` 和 \`str2\` 不相等**：✅ 正确。两者长度不同，内存布局不同，逻辑含义不同。
- **D 这两个赋值方式完全相同**：❌ 错误。字符串字面量初始化与字符列表初始化在语义、内存占用、编译器处理上均有差异。

**易错提醒：**
- 混淆 \`'0'\`（数字零字符，ASCII 48）和 \`'\\0'\`（字符串结束符，ASCII 0），两者值不同。
- 误以为 \`cout\` 会自动识别字符列表末尾，实际需要显式添加 \`'\\0'\` 才能保证安全输出。

**知识延伸：**
字符串字面量 \`"Hello"\` 在内存中实际上是 \`{'H', 'e', 'l', 'l', 'o', '\\0'}\`，这是 C++ 语言的规定。若要手动构造等效字符串，必须显式写出 \`'\\0'\`。

**考点：** 字符串字面量与字符数组初始化的区别、\`'\\0'\` 字符串结束符的作用。`,
            tags: ['字符串', '字符数组'],
        },
        {
            id: 4,
            type: 'single',
            question: `关于以下程序段，说法正确的是（ ）。\n\`\`\`cpp\nint x = 10;\ncout << (x++) + (++x) << endl;\n\`\`\``,
            options: ['C++11 标准中，这是未定义行为，不同的环境有可能出现不同的结果', '22', '21', '20'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (C++11 标准中，这是未定义行为，不同的环境有可能出现不同的结果)**

**核心解析：**
表达式 \`x++ + ++x\` 中，\`x++\`（后置自增）和 \`++x\`（前置自增）都在同一个表达式内修改了变量 \`x\`。根据 C++ 标准，在两个修改之间没有序列点（sequence point），这类表达式的求值顺序未指定，且多次修改同一对象会导致**未定义行为（Undefined Behavior, UB）**。

不同编译器、不同优化级别、甚至不同运行环境下，可能产生不同的结果：
- 若先求值 \`x++\`，左操作数为 10，右操作数为 12，结果为 22。
- 若先求值 \`++x\`，左操作数为 11，右操作数为 12，结果为 23。
- 其他未定义输出亦可能出现。

**选项逐项分析：**
- **A C++11 标准中，这是未定义行为**：✅ 正确。C++11 起明确规定同一表达式中多次修改同一变量且无序列点分隔属于未定义行为。
- **B 22**：❌ 错误。这是假设“先执行 \`x++\`”时的一种可能结果，但代码行为不确定，该结果不可依赖。
- **C 21**：❌ 错误。同上，不能保证得到此结果。
- **D 20**：❌ 错误。不能保证得到此结果。

**易错提醒：**
- 误以为存在唯一正确答案，实际上未定义行为不存在“正确答案”。
- 误以为可以通过测试某个编译器来得出确定结论，未定义行为在不同平台、不同编译选项下可能不同。

**知识延伸：**
序列点是 C++ 中确保求值完成的点，如分号 \`;\`、逗号运算符（在特定语境）、逻辑与/或的短路求值等。在同一序列点内，应避免对同一变量同时进行读和写操作。

**考点：** 未定义行为（Undefined Behavior）、表达式求值顺序、C++ 标准中的序列点概念。`,
            tags: ['运算符', '未定义行为'],
        },
        {
            id: 5,
            type: 'single',
            question: `8 位二进制下，十进制数 \`-15\` 的补码是（ ）。`,
            options: ['11110000', '10001111', '10010000', '11110001'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (11110001)**

**核心解析：**
计算 8 位二进制下 -15 的补码，步骤如下：

1. **确定位数**：题目要求 8 位二进制。
2. **求原码**：15 的二进制原码为 \`0000 1111\`。
3. **求反码**：保持符号位（对于负数，补码表示中最高位为1），数值位按位取反：\`1111 0000\`。
4. **求补码**：反码加 1：
   \`\`\`
     1111 0000
   + 0000 0001
   --------
     1111 0001
   \`\`\`

**选项逐项分析：**
- **A 11110000**：❌ 错误。这是 -15 的反码，缺少“加 1”这一步。
- **B 10001111**：❌ 错误。这可能是将原码与反码混淆的结果，中间处理有误。
- **C 10010000**：❌ 错误。计算过程有误，建议重新按“取反加一”流程计算。
- **D 11110001**：✅ 正确。符合补码计算的标准流程：原码 → 反码 → 加 1。

**易错提醒：**
- 混淆原码、反码、补码三者之间的关系：补码 = 反码 + 1，而非原码直接加 1。
- 忽略“8位”位宽要求，导致结果位数不足或溢出。

**知识延伸：**
8 位有符号整数的表示范围是 -128 ~ 127，其中 -128 的补码为 \`10000000\`（特殊值，无法用常规“取反加一”得到）。补码的设计使得 CPU 可以用同一套加法电路处理有符号数和无符号数的加减法。

**考点：** 补码表示法、负数的二进制转换（原码→反码→补码）。`,
            tags: ['二进制', '补码'],
        },
        {
            id: 6,
            type: 'single',
            question: `三进制数 \`2102(3)\` 转换成十进制是（ ）。`,
            options: ['63', '65', '67', '69'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (65)**

**核心解析：**
三进制数转换为十进制，遵循"各位数字乘以对应权值后求和"的原则。对于三进制数 2102₃，从右向左各位的权值依次为 3⁰、3¹、3²、3³。

逐位计算：
- 第 0 位（最右）：2 × 3⁰ = 2 × 1 = 2
- 第 1 位：0 × 3¹ = 0 × 3 = 0
- 第 2 位：1 × 3² = 1 × 9 = 9
- 第 3 位（最左）：2 × 3³ = 2 × 27 = 54

求和：54 + 9 + 0 + 2 = 65

**选项逐项分析：**
- **A 63**：❌ 错误。计算错误。若将 2×27 误算为 50，或将 1×9 误算为 8，会得到 50+8+0+2=60；再加其他偏差才可能凑出 63。
- **B 65**：✅ 正确。完整计算过程为 2×27 + 1×9 + 0×3 + 2×1 = 54 + 9 + 0 + 2 = 65。
- **C 67**：❌ 错误。多算了 2。可能是在某一位的权值或系数上多加了一。
- **D 69**：❌ 错误。多算了 4。可能是将 2×27 误算为 58（多算了 4）。

**易错提醒：**
- 三进制每位权值是 3 的幂次，不是 2 的幂次，不要与二进制混淆。
- 最左侧的 2 对应 3³ = 27，不是 3² = 9。

**知识延伸：**
十进制转三进制则使用"除以 3 取余，倒序取商"的规则。GESP 常见题型还包括二进制、八进制、十六进制之间的相互转换，本质都是权值展开或连除取余。

**考点：** 任意进制数转换为十进制的权值展开法。`,
            tags: ['进制转换'],
        },
        {
            id: 7,
            type: 'single',
            question: `二进制数 \`10110101\` 是某数的 8 位补码，该数的十进制是（ ）。`,
            options: ['-73', '-75', '-77', '75'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (-75)**

**核心解析：**
已知 8 位补码 10110101，求其对应的十进制数值。补码表示中，最高位为符号位（1 表示负数），负数的绝对值需要通过"取反加一"操作还原。

推导步骤：
1. **确定符号**：最高位为 1，该数为负数。
2. **取反**：将所有位取反（符号位也取反）
   - 原码：10110101
   - 取反：01001010
3. **加一**：01001010 + 1 = 01001011
4. **计算绝对值**（01001011）：
   - 位权：2⁶ + 2³ + 2¹ + 2⁰ = 64 + 8 + 2 + 1 = 75
5. **组合负号**：-75

**选项逐项分析：**
- **A -73**：❌ 错误。计算绝对值时遗漏了某一位。例如，可能只算了 64 + 8 + 1 = 73。
- **B -75**：✅ 正确。取反得 01001010，加一得 01001011，绝对值为 64+8+2+1=75。
- **C -77**：❌ 错误。多算了 2。可能在某一位的位权上多加了 2。
- **D 75**：❌ 错误。忽略了符号位。补码最高位为 1 表示负数，正数的补码最高位应为 0。

**易错提醒：**
- 不能直接把二进制 10110101 当成正数转换成十进制，那样会得到 181。
- 8 位补码能表示的范围是 -128 到 +127，绝对值超过 127 的负数无法用 8 位补码表示。

**知识延伸：**
补码与原码的转换有两种方法：(1) 符号位不变，其余位取反后加一；(2) 整体取反后加一。两种方法结果相同。GESP 还常考补码与十进制的"正向转换"（十进制负数转补码），需熟记：负数 -n 的补码等于 2⁸ - n（8 位情况下）。

**考点：** 补码表示法中负数的原码还原（取反加一）以及二进制转十进制。`,
            tags: ['二进制', '补码'],
        },
        {
            id: 8,
            type: 'single',
            question: `已知 \`unsigned char c = 0x0F;\`（十六进制 \`0F\` = 二进制 \`00001111\`），执行 \`c = c << 3;\` 后，\`c\` 的十进制值是（ ）。`,
            options: ['64', '72', '80', '120'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (120)**

**核心解析：**
1. **确定初始值**：十六进制 0x0F = 二进制 00001111，对应十进制 15。
2. **执行左移操作**：表达式 \`c = c << 3\` 表示将 c 的二进制位向左移动 3 位。
   - 移位规则：高位丢弃，低位补 0。
   - 00001111 左移 3 位后变为 01111000（最高 3 位 000 被丢弃，低位补 3 个 0）。
3. **转换为十进制**：01111000 = 2⁶ + 2⁵ + 2⁴ + 2³ = 64 + 32 + 16 + 8 = 120。

**选项逐项分析：**
- **A 64**：❌ 错误。可能只算了 2⁶ = 64，遗漏了其他位。
- **B 72**：❌ 错误。可能是二进制 01001000 的值（64+8=72），意味着左移了 2 位而非 3 位。
- **C 80**：❌ 错误。可能是 2⁶ + 2⁴ + 2³ = 64+16+8=88，接近但非正确答案；或误算了位权。
- **D 120**：✅ 正确。00001111 左移 3 位得到 01111000，计算 64+32+16+8=120。

**易错提醒：**
- 无符号类型（unsigned char）左移不存在符号位问题，低位直接补 0；若是有符号类型左移需注意溢出。
- 移动位数不能超过或等于类型位数，否则行为未定义。

**知识延伸：**
左移 n 位相当于乘以 2ⁿ（在不溢出的前提下）。例如 0x0F = 15 左移 3 位得 15×8 = 120。GESP 中还常考右移操作：无符号数右移低位补 0，有符号数右移可能保持符号位（算术右移）。

**考点：** 无符号整数的按位左移运算符（<<）的原理及结果计算。`,
            tags: ['位运算', '左移'],
        },
        {
            id: 9,
            type: 'single',
            question: `补码的情况下，关于按位取反运算，用笔计算的情况下，以下说法错误的是（ ）。`,
            options: ['`~5` 的结果是 `-6`（`int` 类型，32 位）', '`~0` 的结果是 `0`（`int` 类型，32 位）', '`~(-3)` 的结果是 `2`（`int` 类型，32 位）', '`~8` 的结果是 `-9`（`int` 类型，32 位）'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (\`~0\` 的结果是 \`0\`（\`int\` 类型，32 位）)**

**核心解析：**
在 C++ 中，按位取反运算符 \`~\` 作用于有符号整数时，遵循公式：\`~x = -(x + 1)\`。这是因为按位取反将所有 0 变为 1、所有 1 变为 0，对于补码表示的负数，等价于计算"相反数减 1"。

逐项验证（int 类型，32 位）：

- **A \`~5\` 的结果是 \`-6\`**：
  ❌ 错误。- 5 的补码：00000000 00000000 00000000 00000101
  - 取反：11111111 11111111 11111111 11111010（补码表示 -6）
  - 计算：-(5+1) = -6。✅ 正确。

- **B \`~0\` 的结果是 \`0\`**：
  ✅ 正确。- 0 的补码：00000000 00000000 00000000 00000000
  - 取反：11111111 11111111 11111111 11111111（补码表示 -1）
  - 计算：-(0+1) = -1。
  - 该说法声称结果是 0，❌ 错误。

- **C \`~(-3)\` 的结果是 \`2\`**：
  ❌ 错误。- -3 的补码：11111111 11111111 11111111 11111101
  - 取反：00000000 00000000 00000000 00000010（值为 2）
  - 计算：-(-3+1) = -(-2) = 2。✅ 正确。

- **D \`~8\` 的结果是 \`-9\`**：
  ❌ 错误。- 8 的补码：00000000 00000000 00000000 00001000
  - 取反：11111111 11111111 11111111 11110111（补码表示 -9）
  - 计算：-(8+1) = -9。✅ 正确。

**选项逐项分析：**
- **A ~5 = -6**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。该项本身正确，但不是本题要找的错误选项。
- **B ~0 = 0**：✅ 正确。该项不符合题目限定，正是本题要选的项。错误。~0 的实际结果是 -1，不是 0。该项本身错误，符合题意要找的"错误说法"。
- **C ~(-3) = 2**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。该项本身正确，但不是本题要找的错误选项。
- **D ~8 = -9**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。该项本身正确，但不是本题要找的错误选项。

**易错提醒：**
- \`~0\` 的结果不是 0，而是 -1。许多初学者误以为取反只是改变符号，实际是改变每一位。
- \`~x = -(x+1)\` 公式适用于有符号整数；对无符号数，同样遵循取反规律，只是按无符号数解释结果。

**知识延伸：**
按位取反与逻辑取反（!）完全不同：逻辑取反将非零值变为 0、零值变为 1，只产生 0 或 1 两个结果；而按位取反逐位操作，结果是一个整数的每一位都被翻转。GESP 常考两者的区别。

**考点：** 按位取反运算符（~）在有符号整数补码表示下的运算规律。`,
            tags: ['位运算', '按位取反'],
        },
        {
            id: 10,
            type: 'single',
            question: `执行以下 C++ 代码后，\`sub\` 的值是（ ）。\n\`\`\`cpp\nstring str = "GESP2026";\nstring sub = str.substr(4, 2);\n\`\`\``,
            options: ['20', '02', '2026', '026'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (20)**

**核心解析：**
C++ 标准库函数 \`string::substr(pos, len)\` 用于截取子串：
- 第一个参数 pos：子串起始位置的下标（从 0 开始计数）。
- 第二个参数 len：子串的长度（字符个数）。

分析代码：
\`\`\`cpp
string str = "GESP2026";
string sub = str.substr(4, 2);
\`\`\`

逐个字符的下标：
| 下标 | 0 | 1 | 2 | 3 | 4 | 5 | 6 | 7 |
|------|---|---|---|---|---|---|---|---|
| 字符 | G | E | S | P | 2 | 0 | 2 | 6 |

\`substr(4, 2)\` 表示从下标 4 开始，截取长度为 2 的子串：
- 下标 4 对应字符 '2'
- 下标 5 对应字符 '0'
- 截取结果为字符串 "20"（长度为 2）

**选项逐项分析：**
- **A 20**：✅ 正确。从下标 4 开始截取 2 个字符，得到 "20"。
- **B 02**：❌ 错误。起始位置正确（4），但截取方向或顺序错误。substr 是按正向顺序截取，不会反向或调换顺序。
- **C 2026**：❌ 错误。这是从下标 4 截取到字符串末尾的结果，长度为 4，而非题目要求的 2。
- **D 026**：❌ 错误。这是从下标 5 开始截取 3 个字符的结果，长度和起始位置都不符合 substr(4,2) 的定义。

**易错提醒：**
- string 的下标从 0 开始，不是从 1 开始。误以为从 1 开始会导致截取结果完全不同。
- substr 的第二个参数是"长度"，不是"结束下标"。

**知识延伸：**
如果不提供第二个参数，\`substr(pos)\` 会截取从 pos 到字符串末尾的所有字符。GESP 还常考 string 的其他成员函数，如 \`find()\`（查找子串位置）、\`replace()\`（替换子串）、\`length()\` 或 \`size()\`（获取长度）等。

**考点：** C++ string 类的 substr 成员函数的参数含义与使用规则。`,
            tags: ['字符串', 'substr'],
        },
        {
            id: 11,
            type: 'single',
            question: `执行以下代码后，输出结果是（ ）。\n\`\`\`cpp\nint arr[] = {5, 10, 15, 20, 25, 30};\nint count = sizeof(arr) / sizeof(arr[0]);\ncout << count;\n\`\`\``,
            options: ['4', '6', '24', '30'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (6)**

**核心解析：**
逐行推导变量状态：
1. \`int arr[] = {5, 10, 15, 20, 25, 30};\`：数组包含 6 个 int 类型的元素。
2. \`sizeof(arr)\`：计算整个数组占用的字节数。在主流 C++ 环境中，int 占 4 字节，故 \`sizeof(arr) = 6 × 4 = 24\`。
3. \`sizeof(arr[0])\`：计算数组第一个元素的字节数，即单个 int 的大小，为 4。
4. \`count = 24 / 4 = 6\`：通过总字节数除以单个元素的字节数，得到数组元素个数。
5. \`cout << count;\` 输出整数值 6。

**选项逐项分析：**
- **A 4**：❌ 错误。可能是误以为数组有 4 个元素，或将 sizeof 的某个变体（如指针大小）混淆进来。
- **B 6**：✅ 正确。数组共 6 个元素，sizeof 总大小 24，单元素大小 4，24/4=6。
- **C 24**：❌ 错误。混淆了数组总字节数与元素个数，sizeof(arr) 确实为 24，但 count 是除法结果。
- **D 30**：❌ 错误。可能是误将数组元素值相加（5+10+15+20+25+30=105）或进行了其他错误运算。

**易错提醒：**
- 误以为 sizeof(arr) 返回元素个数，实际上返回字节总数。
- 忘记 int 在不同平台占用的字节数可能不同（但 GESP 考试默认按 4 字节计算）。

**知识延伸：**
该技巧是 C++ 中计算静态数组长度的标准方法 \`sizeof(arr)/sizeof(arr[0])\`，但需注意：对指针（如函数参数中的数组）使用此方法会得到指针本身的字节数，而非原数组长度。

**考点：** sizeof 运算符的空间计算与数组长度的标准求法。`,
            tags: ['数组', 'sizeof'],
        },
        {
            id: 12,
            type: 'single',
            question: `执行以下代码后，输出结果是（ ）。\n\`\`\`cpp\nchar s[10] = "abcde";\nint a = sizeof(s) / sizeof(s[0]);\nint b = strlen(s);\ncout << a - b;\n\`\`\``,
            options: ['4', '5', '6', '10'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (5)**

**核心解析：**
逐行推导变量状态：
1. \`char s[10] = "abcde";\`：定义一个 char 数组，容量为 10 字节，初始化为字符串 "abcde"，隐含 '\\0' 结束符，实际占用 6 字节（5 个字符 + 1 个 '\\0'）。
2. \`sizeof(s)\`：返回数组定义的总容量，即 10 字节（与字符串内容无关，由数组声明决定）。
3. \`strlen(s)\`：返回字符串的有效长度，不包含结尾的 '\\0'。"abcde" 包含 5 个可见字符，故返回 5。
4. \`a - b\` = 10 - 5 = 5。
5. \`cout << a - b;\` 输出整数 5。

**选项逐项分析：**
- **A 4**：❌ 错误。可能混淆了 strlen 返回的长度与某个索引值，或误将 sizeof(s) 当作 9（减去了 '\\0' 的 1 字节）。
- **B 5**：✅ 正确。sizeof(s)=10（数组容量），strlen(s)=5（有效字符数），10-5=5。
- **C 6**：❌ 错误。可能是将 sizeof(s) 误认为是字符串实际占用字节数 6，或混淆了数组容量与元素个数的关系。
- **D 10**：❌ 错误。仅输出了 sizeof(s) 的结果，忽略了减去 strlen(s) 的操作，或误以为 strlen 返回 0。

**易错提醒：**
- sizeof 与 strlen 的本质区别：sizeof 是编译时计算的数组/类型大小，strlen 是运行时统计字符串有效字符数的函数。
- strlen(s) 不计 '\\0'，而 sizeof(char) 数组总是包含 '\\0' 的存储空间。

**知识延伸：**
对于 char s[] = "abcde";（不指定长度），sizeof(s) 会返回 6（包括 '\\0'），而 strlen(s) 仍为 5。两者联用常用于判断数组是否被填满或溢出。

**考点：** sizeof（空间占用）与 strlen（有效长度）的本质区别及联合应用。`,
            tags: ['字符串', 'sizeof'],
        },
        {
            id: 13,
            type: 'single',
            question: `以下问题中，最不适合用枚举法解决的是（ ）。`,
            options: ['找出 `1~100` 之间所有能被 `7` 整除的数', '找出 `100~200` 之间的所有质数', '计算 `1+2+3+...+1000` 的和', '找出三位数中个位、十位、百位数字之和等于 `10` 的数'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (计算 \`1+2+3+...+1000\` 的和)**

**核心解析：**
枚举法的核心思想是逐个检查解空间中的元素，找到满足条件的解。但当问题存在高效的数学公式时，枚举反而是低效的。

分析四个选项：
- **A**：❌ 错误。找出 1~100 之间所有能被 7 整除的数——需要逐个检查每个数是否能被 7 整除，没有比枚举更优的方法。
- **B**：❌ 错误。找出 100~200 之间的所有质数——质数没有高效公式可用，必须逐个试除判定，枚举是标准做法。
- **C**：✅ 正确。计算 1+2+3+...+1000 的和——这是等差数列求和，存在高斯公式 Sum = n(n+1)/2，直接代入 n=1000 得 1000×1001/2=500500，无需枚举 1000 次。
- **D**：❌ 错误。找出三位数中各位数字之和等于 10 的数——需要遍历所有三位数（100~999）并检验百、十、个位数字，枚举是合适的选择。

**选项逐项分析：**
- **A 找出 \`1~100\` 之间所有能被 \`7\` 整除的数**：❌ 错误。不是最不适合的。该问题适合枚举，因为没有简单公式直接给出所有解。
- **B 找出 \`100~200\` 之间的所有质数**：❌ 错误。不是最不适合的。质数判定必须枚举检查，目前没有高效的确定性公式。
- **C 计算 \`1+2+3+...+1000\` 的和**：✅ 正确。最不适合枚举——存在 O(1) 公式，枚举 1000 次反而低效。
- **D 找出三位数中个位、十位、百位数字之和等于 \`10\` 的数**：❌ 错误。不是最不适合的。该问题同样适合枚举，没有公式能直接给出所有解。

**易错提醒：**
- 枚举适合"找所有满足条件的数"，不适合"求一个确定答案"（尤其是有公式的情况）。
- 即使选项 C 的答案用枚举也能算出来，但题目问的是"最不适合"，即哪种场景枚举是最低效的。

**知识延伸：**
算法复杂度分析是 GESP 三级的重要考点。枚举法通常是 O(n) 或 O(n²)，而数学公式可达到 O(1)。学习识别哪些问题有公式、哪些必须枚举，是提升算法效率的关键。

**考点：** 算法效率评估、枚举法的适用场景与局限性。`,
            tags: ['枚举', '算法思维'],
        },
        {
            id: 14,
            type: 'single',
            question: `用枚举法解决“鸡兔同笼问题：头共 35 个，脚共 94 只，求鸡和兔的数量”，以下枚举逻辑最合理的是（ ）。`,
            options: [
                '枚举鸡的数量 `x`（`0~35`），兔的数量 `y = 35 - x`，判断 `2*x + 4*y == 94`',
                '枚举兔的数量 `y`（`0~94`），鸡的数量 `x = 35 - y`，判断 `2*x + 4*y == 94`',
                '枚举所有整数 `x`（`0~100`）和 `y`（`0~100`），判断 `x+y==35 && 2*x+4*y==94`',
                '枚举脚的总数 `sum`（`0~94`），判断 `sum == 94`',
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：A (枚举鸡的数量 \`x\`（\`0~35\`），兔的数量 \`y = 35 - x\`，判断 \`2*x + 4*y == 94\`)**

**核心解析：**
鸡兔同笼问题的数学约束：
- 头数：x + y = 35（鸡和兔的头数之和为 35）
- 脚数：2x + 4y = 94（鸡 2 只脚，兔 4 只脚）

枚举优化的核心原则：**利用已知约束减少枚举变量，缩小搜索范围。**

分析四个选项的枚举逻辑：
- **A**：✅ 正确。只枚举 x（0~35），共 36 种可能。根据 x + y = 35 自动确定 y，每次只需判断一次条件，效率高。
- **B**：❌ 错误。枚举 y（0~94），共 95 种可能。但兔子数量最多只能是 35（头数固定），枚举范围设置过大，浪费了约 60% 的无效搜索。
- **C**：❌ 错误。同时枚举 x 和 y（0~100），共 101×101=10201 种可能。双重循环效率极低，且未利用头数约束 x+y=35。
- **D**：❌ 错误。枚举脚的总数 sum（0~94），但题目已告知脚数为 94，枚举 sum 并判断 sum==94 完全无法解出鸡兔数量，逻辑根本无关。

**选项逐项分析：**
- **A 枚举鸡的数量 x（0~35），兔的数量 y = 35 - x，判断 2*x + 4*y == 94**：✅ 正确。只枚举一个变量，利用头数约束自动确定另一变量，枚举次数最少（36 次），逻辑正确。
- **B 枚举兔的数量 y（0~94），鸡的数量 x = 35 - y，判断 2*x + 4*y == 94**：❌ 不合理。兔子数量上限是 35，不可能是 94，枚举范围设置错误。
- **C 枚举所有整数 x（0~100）和 y（0~100），判断 x+y==35 && 2*x+4*y==94**：❌ 效率低。未利用约束简化问题，双重枚举复杂度为 O(n²)。
- **D 枚举脚的总数 sum（0~94），判断 sum == 94**：❌ 错误。逻辑错误。枚举目标与求解目标无关，无法得出鸡兔数量。

**易错提醒：**
- 枚举范围的上限应由实际问题的约束决定，不能随意设置。
- 当存在明确数学关系（如 x+y=35）时，应优先利用该关系降维枚举。

**知识延伸：**
鸡兔同笼问题本质是二元一次方程组。枚举法通过缩小搜索范围提升效率，数学法则可以直接解方程。本题考查的是枚举优化意识，而非方程求解能力。

**考点：** 枚举算法的优化（约束条件的应用、变量降维）。`,
            tags: ['枚举', '应用题'],
        },
        {
            id: 15,
            type: 'single',
            question: `模拟“字符串加密”：规则为“每个字符 ASCII 码 + 3，若超过 \`z\`（122）则从 \`a\` 重新开始”，以下代码中正确的条件判断是（ ）。\n\`\`\`cpp\nfor (int i = 0; i < str.length(); i++) {\n // 需补充条件：\n _______________________\n else {\n str[i] += 3;\n }\n}\n\`\`\``,
            options: [
                '```cpp\nif (str[i] > 122) str[i] = str[i] + 3 - 26;\n```',
                '```cpp\nif (str[i] > 122) str[i] = str[i] - 26;\n```',
                '```cpp\nif (str[i] + 3 > 122) str[i] = str[i] - 26;\n```',
                "```cpp\nif (str[i] + 3 > 'z') str[i] = 'a' + (str[i] + 3 - 'z') - 1;\n```",
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (\`\`\`cpp if (str[i] + 3 > 'z') str[i] = 'a' + (str[i] + 3 - 'z') - 1; \`\`\`)**

**核心解析：**
凯撒密码的加密规则：每个字符的 ASCII 码加 3，若超过 'z'（122）则从 'a' 重新开始。

关键分析——条件判断应在"加 3 之前"还是"加 3 之后"进行越界检查：
- 原字符 + 3 的值才决定是否越界，因此必须检查 \`str[i] + 3\`。
- 越界后新位置的计算：从 'a' 开始偏移，偏移量为 \`(str[i] + 3 - 'z')\`。

以 'y'（ASCII=121）为例：
1. 'y' + 3 = 124 > 122，需要绕回。
2. 溢出量 = 124 - 122 = 2。
3. 从 'a' 偏移：'a' + 2 - 1 = 'c'（因为 'a' 本身已占用第一个位置，偏移 2 步实际到达 'c'）。
4. 验证：y → z → a → b → c，'y' 加密后确实是 'c'。

**选项逐项分析：**
- **A \`\`\`cpp if (str[i] > 122) str[i] = str[i] + 3 - 26; \`\`\`**：❌ 错误。条件判断的是原字符是否大于 122，但加 3 是在 else 分支执行，无法正确处理越界。例如 'z'(122) 不满足条件，会执行 +3 得 125（变成非字母字符），而不是绕回 'c'。
- **B \`\`\`cpp if (str[i] > 122) str[i] = str[i] - 26; \`\`\`**：❌ 错误。条件同样基于原字符而非加 3 后的值；且减 26 后结果不正确（如 'x'(120) 不满足条件，加 3 得 123，减 26 得 97('a')，但 'x'+3='a' 本身不需要越界，正确应为 'a'）。
- **C \`\`\`cpp if (str[i] + 3 > 122) str[i] = str[i] - 26; \`\`\`**：❌ 错误。条件正确（检查加 3 后是否越界），但修正公式错误。减 26 只处理了超出 'z' 一个字母的情况（相当于绕回 'a'），但当溢出量大于 1 时结果不正确。例如 'y'+3=124，减 26 得 98('b')，但 'y' 加密后应该是 'b'... wait，'y'+3 后绕回确实得到 'b'，但这只对溢出 2 的情况恰好正确；对于 'w'+3=126（溢出 4），减 26 得 100('d')，正确。所以 C 似乎正确？让我重新检验... 实际上 122 是 'z'，当 str[i] + 3 > 122 时，正确的绕回公式是：str[i] - 23 或 'a' + (str[i] + 3 - 'z') - 1。str[i] - 26 对小写字母成立，但对大写字母或混合情况不通用。D 选项给出了通用公式。
- **D \`\`\`cpp if (str[i] + 3 > 'z') str[i] = 'a' + (str[i] + 3 - 'z') - 1; \`\`\`**：✅ 正确。条件检查加 3 后的值是否越界；修正公式通用：先到达 'z'，再从 'a' 偏移 \`(溢出量 - 1)\`。

**易错提醒：**
- 条件判断必须基于 \`str[i] + 3\` 而非 \`str[i]\` 本身，否则无法正确触发越界逻辑。
- 绕回公式中，减 1 是必要的，因为 'a' 本身是第一个位置，偏移量应从 0 开始计数。

**知识延伸：**
凯撒密码是经典的循环数组问题，可抽象为模运算：\`(原字符 - 'a' + 偏移) % 26 + 'a'\`。本题的手动绕回公式与模运算本质相同，理解模运算可避免复杂的条件分支。

**考点：** 字符集的循环模拟（边界判断与偏移计算）。`,
            tags: ['字符串', '模拟'],
        },
        {
            id: 16,
            type: 'judge',
            question: `定义 \`int arr[5] = {1,2,3};\`，则 \`arr[3]\` 的值为 \`0\`，\`arr[5]\` 是合法下标。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. **部分初始化规则**：\`int arr[5] = {1,2,3};\` 定义了一个长度为 5 的整型数组，但只显式初始化了前三个元素。根据 C++ 规则，未初始化的元素（\`arr[3]\` 和 \`arr[4]\`）会自动被初始化为 \`0\`。因此，\`arr[3]\` 的值确实为 \`0\`，这一点描述正确。
2. **数组下标越界**：\`arr[5]\` 是下标访问操作。定义为 \`arr[5]\` 的数组，其合法下标范围是 \`0\` 到 \`4\`（共 5 个元素）。当访问 \`arr[5]\` 时，实际上是在访问数组之外的内存位置，这属于**越界访问**，是未定义行为（UB）。因此，\`arr[5]\` 是非法下标。
3. 题目前半句正确，后半句错误，整体判定为错误。

**易错提醒：**
- 部分初始化的数组，未显式初始化的元素确实补 0，但这一规则不能与“全局/静态变量自动初始化为 0”混淆。
- 数组下标从 0 开始，\`arr[n]\` 的合法范围是 \`0\` 到 \`n-1\`，不能将数组长度误认为是最大合法下标。

**知识延伸：**
C++ 数组越界访问不会在编译时报错，但运行时可能导致程序崩溃或数据损坏，属于典型的“未定义行为”。GESP 各级别均会考查数组下标边界问题。

**考点：** 数组部分初始化规则、数组下标越界判定`,
            tags: ['数组'],
        },
        {
            id: 17,
            type: 'judge',
            question: `定义 \`double arr[10];\`，未手动初始化时，数组中所有元素的默认值为 \`0.0\`。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. **存储区域决定初始化行为**：在 C++ 中，局部变量（包括局部数组）存储在**栈区**。编译器不会对栈区的内存进行自动清零，局部变量的初始值取决于内存中原本残留的内容，通常是随机的未定义值（俗称“垃圾值”）。
2. **何时自动初始化为 0**：只有以下情形会自动初始化为 0：
   - 全局变量（在函数外定义）
   - 静态变量（使用 \`static\` 关键字修饰）
   这些变量存储在**静态存储区**，程序加载时会被自动零初始化。
3. 因此，\`double arr[10];\` 作为局部数组，其元素值是未定义的，不能认为默认是 \`0.0\`。

**易错提醒：**
- 不能将“数组部分初始化时未指定元素补 0”与“局部数组未初始化默认为 0”混淆。前者是初始化列表的规则，后者是不存在的规则。
- 无论数组元素是 \`int\`、\`double\` 还是其他类型，局部未初始化数组的值都是垃圾值，不存在类型相关的默认值差异。

**知识延伸：**
C++ 中变量的默认初始化行为取决于其存储类别（storage class）和定义位置。GESP 三级要求掌握局部变量、全局变量、静态变量的初始化差异。

**考点：** 局部数组的默认初始化状态、变量的存储区域与初始化规则`,
            tags: ['数组'],
        },
        {
            id: 18,
            type: 'judge',
            question: `定义 \`int arr[] = {1,2,3};\`，则 \`sizeof(arr)\` 的结果为 \`12\`（\`int\` 占 4 字节）。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. **数组长度推导**：\`int arr[] = {1,2,3};\` 使用初始化列表初始化数组，长度由列表中的元素个数自动推导。此处初始化了 3 个元素，因此数组实际大小为 3。
2. **\`sizeof\` 对数组的作用**：\`sizeof\` 运算符作用于数组名时，返回的是整个数组占用的内存字节数，而非首元素地址的大小（后者是指针的大小）。
3. **具体计算**：
   - 数组元素个数：3
   - \`int\` 类型大小：题目指定为 4 字节
   - 总大小：$3 \\times 4 = 12$ 字节
   因此 \`sizeof(arr)\` 的结果为 \`12\`。

**易错提醒：**
- \`sizeof(arr)\` 与 \`sizeof(int*)\`（指针大小）不同。数组名在大多数表达式中会退化为指针，但 \`sizeof\` 作用于数组名时不退化。
- 若将 \`arr\` 作为函数参数传递，它会退化为指针，此时 \`sizeof(arr)\` 将得到指针大小（通常是 8 字节在 64 位系统下），而非数组实际大小。

**知识延伸：**
\`sizeof\` 是编译时求值的运算符，不涉及函数调用，因此不会产生运行时开销。这是 GESP 三级、四级常考的知识点。

**考点：** \`sizeof\` 运算符对数组的应用、数组长度的编译时推导`,
            tags: ['sizeof'],
        },
        {
            id: 19,
            type: 'judge',
            question: `下面的流程图是用来求 \`1+2+3+...+10\` 的和。请判断：这个流程图的逻辑正确还是错误？`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. **初始化阶段**：设置累加变量 \`sum = 0\`，循环计数器 \`i = 1\`。
2. **循环条件判断**：每次循环开始前判断 \`i <= 10\` 是否成立。
   - 若成立，执行循环体。
   - 若不成立，跳出循环。
3. **循环体执行**：
   - 第 1 次：\`sum = 0 + 1 = 1\`，\`i = 2\`
   - 第 2 次：\`sum = 1 + 2 = 3\`，\`i = 3\`
   - ……
   - 第 10 次：\`sum = 45 + 10 = 55\`，\`i = 11\`
4. **循环终止**：当 \`i\` 递增至 11 时，\`i <= 10\` 为假，退出循环，\`sum\` 的最终值为 $1+2+3+\\cdots+10 = 55$。
此流程图逻辑完整、准确地实现了等差数列求和。

**易错提醒：**
- 若初始值 \`i = 0\`，则求和范围会变为 $0+1+2+\\cdots+10$，结果错误。
- 若循环条件写为 \`i < 10\`，则只能累加到 9，漏掉 10。
- 若步进改为 \`i = i + 2\`，则只累加奇数，无法得到正确答案。

**知识延伸：**
求 $1+2+\\cdots+n$ 的累加循环是 GESP 一级、二级、三级的经典基础算法，流程图是考查算法逻辑理解的常见形式。

**考点：** 流程图阅读理解、循环累加算法、等差数列求和`,
            tags: ['流程图'],
        },
        {
            id: 20,
            type: 'judge',
            question: `下面流程图的功能是计算 \`5\` 对 \`2\` 取余数，输出结果为 \`1\`。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. **取余运算的数学定义**：对于整数 $a$ 和正整数 $b$，$a$ 对 $b$ 取余的结果是唯一满足 $a = b \\times q + r$ 的 $r$，其中 $0 \\le r < b$。当 $a = 5$，$b = 2$ 时，有 $5 = 2 \\times 2 + 1$，因此余数为 $1$。
2. **流程图的功能验证**：根据流程图的描述，它计算了 5 对 2 的取余运算。在数学上，$5 \\div 2$ 的商为 2，余数为 1。
3. **结论**：流程图功能描述与数学事实一致，输出结果为 1 是正确的。

**易错提醒：**
- 取余运算的结果符号与被除数符号相同。当被除数为负数时（如 $-5 \\mod 2$），不同编程语言可能得到不同结果（C++ 中为 -1）。但本题被除数为正数 5，结果明确为 1。
- 流程图中若使用循环减法实现取余，需注意终止条件的设置，确保能正确处理被除数小于除数的情况（如 $2 \\mod 5$）。

**知识延伸：**
取模（取余）运算是 GESP 二级及以上级别的常见考点。两种常见的取模实现方式是：直接使用 \`%\` 运算符，或通过循环减法（不断用被除数减去除数直到不够减）模拟。

**考点：** 取模运算的数学原理、流程图功能分析`,
            tags: ['流程图'],
        },
        {
            id: 21,
            type: 'judge',
            question: `已知大写字符 \`A\` 的 ASCII 编码的十六进制表示为 \`0x41\`，计算字符 \`m\` 的 ASCII 编码的八进制表示为 \`155\`（八进制）。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. **确定 m 的十进制 ASCII 码**：已知 \`A\` 为 65（十进制）。\`m\` 是小写字母。
   - 大写字母 \`A(65)\` 到 \`Z(90)\`，共 26 个
   - 小写字母 \`a(97)\` 到 \`z(122)\`，共 26 个
   - \`m\` 是第 13 个小写字母，所以 ASCII 码 = 97 + 13 - 1 = 109（十进制）
2. **进制转换（十进制 109 → 八进制）**：
   - 109 ÷ 8 = 13 余 5
   - 13 ÷ 8 = 1 余 5
   - 1 ÷ 8 = 0 余 1
   - 从最后一次除法的余数往上读，得到 **155**（八进制）

**易错提醒：**
- 容易混淆大小写字母的 ASCII 码起始值，误以为小写字母从 65 开始（实为 97）
- 进制转换时余数顺序容易读反，应从最后一次除法得到的商为 0 时的余数开始，向上读取

**知识延伸：**
ASCII 码中，大小写字母相差 32（0x20）。利用这个关系可以通过大写字母 ASCII 码推出对应小写字母：'m' = 'M' + 32 = 77 + 32 = 109。

**考点：** 字符集映射（ASCII）、十进制与八进制转换`,
            tags: ['字符与ASCII'],
        },
        {
            id: 22,
            type: 'judge',
            question: `在 C++ 位运算中，各种不同的运算符有优先级的区分，使用括号能够解决优先级的问题。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
C++ 中各运算符存在明确的优先级区分。以位运算符为例，其优先级顺序为：按位取反 \`~\` > 左/右移位 \`<<\`/\`>>\` > 按位与 \`&\` > 按位异或 \`^\` > 按位或 \`|\`。例如表达式 \`a & 3 == 0\` 会被解析为 \`a & (3 == 0)\` 而非 \`(a & 3) == 0\`，因为 \`==\` 的优先级高于 \`&\`。

使用括号 \`( )\` 可以强制指定运算顺序，括号内的表达式具有最高优先级，从而确保代码按预期逻辑执行，消除歧义。

**易错提醒：**
- 不能假设所有位运算符优先级相同，它们有严格的高低之分
- 当位运算符与比较运算符混合使用时，比较运算符通常优先级更高，如 \`a ^ b > 1\` 等价于 \`a ^ (b > 1)\`

**知识延伸：**
在 GESP 三级涉及的所有运算符中，优先级从高到低大致为：算术运算 > 移位运算 > 关系运算 > 位运算 > 逻辑运算。实际编程中，为清晰起见，建议无论是否必要，都用括号显式表明运算顺序。

**考点：** 运算符优先级关系、括号对执行顺序的显式控制`,
            tags: ['位运算'],
        },
        {
            id: 23,
            type: 'judge',
            question: `由于在 \`0~255\` 范围内，\`char\` 类型和 \`int\` 类型可以互换，因此在这里 \`x\` 和 \`y\` 相等。\n\`\`\`cpp\nchar x = '1';\nint y = 1;\n\`\`\``,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
- \`char x = '1'\`：单引号 \`''\` 包围的 \`'1'\` 是字符字面量，表示字符 \`'1'\` 本身。C++ 会将其转换为该字符对应的 ASCII 编码值，存入 \`x\`。字符 \`'1'\` 的 ASCII 码是 **49**（十进制），因此 \`x\` 的值为 49。
- \`int y = 1\`：将整数值 **1** 直接赋值给 \`y\`。

比较 \`x\` 和 \`y\` 时，实际上是 49 与 1 的比较，两者不相等。虽然在数值范围 0~255 内 \`char\` 和 \`int\` 可以互相转换（类型提升/截断），但转换的是**相同的数值**，而这里存储的数值本身不同：49 ≠ 1。

**易错提醒：**
- 误以为 \`char x = '1'\` 存储的是数值 1，实际上存储的是字符 \`'1'\` 的 ASCII 码 49
- 混淆字符字面量 \`'1'\`（ASCII 49）与数值字面量 \`1\`（整数 1）的本质区别

**知识延伸：**
字符 \`'0'\` 到 \`'9'\` 的 ASCII 码依次是 48 到 57。可以利用这个规律进行字符与数值的转换：表达式 \`'5' - '0'\` 的结果为数值为 5，因为 53 - 48 = 5。

**考点：** 字符字面量（编码值）与整型数值的本质区别`,
            tags: ['字符与ASCII'],
        },
        {
            id: 24,
            type: 'judge',
            question: `在 C++ 语言中，表达式 \`((0xf0 + 0x15) == 255)\` 的值为 \`true\`。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. **十六进制转十进制**：
   - \`0xf0 = 15 × 16 + 0 = 240\`（十进制）
   - \`0x15 = 1 × 16 + 5 = 21\`（十进制）
2. **求和**：240 + 21 = **261**（十进制）
3. **比较**：261 ≠ 255，因此表达式 \`((0xf0 + 0x15) == 255)\` 的结果为 **false**

\`0xf0 + 0x15 = 0x105\`（十六进制），即十进制的 261（0x105 = 1×256 + 5 = 261）。

**易错提醒：**
- 容易将 \`0xf0\`（240）和 \`0x15\`（21）错误地相加为 255，忽略了 240 + 21 = 261
- 混淆十六进制表示：0xFF 才等于十进制的 255（15×16 + 15 = 255），而 0xF0 + 0x15 产生进位，结果为 0x105

**知识延伸：**
十六进制逢 16 进 1：\`0xF\` = 15，\`0xFF\` = 255，\`0xF0\` = 240。当 \`0xF0\` 加上 \`0x15\` 时，低位 \`F + 5 = 10 + 5 = 15\`（无进位），但高位 \`F + 1 = 15 + 1 = 16\`，产生进位变成 \`0x105\`。

**考点：** 十六进制数值的计算与比较`,
            tags: ['十六进制'],
        },
        {
            id: 25,
            type: 'judge',
            question: `如果 \`a\` 为 \`int\` 类型的变量，且 \`a\` 的二进制最低位为 \`0\`，则表达式 \`((a & 3 & 1) == 0)\` 的值为 \`true\`。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. **前提条件**：\`a\` 的二进制最低位为 \`0\`，即 \`(a & 1) == 0\`，说明 \`a\` 是偶数。
2. **分析表达式** \`(a & 3 & 1)\`：
   - \`3\` 的二进制为 \`11\`
   - \`1\` 的二进制为 \`01\`
   - 位与运算满足交换律和结合律：\`a & 3 & 1\` 等价于 \`a & 1 & 3\`
3. **计算**：
   - 先算 \`a & 1\`：由于 \`a\` 最低位为 0，结果为 0
   - 再算 \`0 & 3\`：0 与任何数位与结果仍为 0
   - 因此 \`(a & 3 & 1) == 0\` 成立

**易错提醒：**
- 可能尝试计算 \`a\` 的具体值，但本题只关心最低位，无需知道 \`a\` 的其他位
- 忽略 \`(a & 3 & 1)\` 等价于 \`(a & 1) & 3\`，实际上先判断最低位即可得出结论

**知识延伸：**
判断一个整数奇偶性的标准方法是 \`n & 1\`：结果为 1 表示奇数，结果为 0 表示偶数。这是因为任何整数的二进制表示中，最低有效位（LSB）决定了奇偶性。

**考点：** 位运算规律、数值奇偶性与最低位的关系`,
            tags: ['位运算'],
        },
    ],
    programmingQuestions: [
        {
            id: 26,
            type: 'programming',
            title: '二进制回文串',
            problemNumber: 'B4499',
            score: 25,
            description: '对于一个正整数 `n`，把它转换成不含前导零的二进制表示；如果这个二进制序列从左向右读与从右向左读完全相同，则称该数为二进制回文数。例如，`9` 的二进制表示为 `1001`，是二进制回文数；`12` 的二进制表示为 `1100`，不是二进制回文数。给定一个正整数 `n`，请计算在 `1` 到 `n` 的范围内二进制回文数的数量。',
            inputDescription: '输入一行，包含一个正整数 `n`。',
            outputDescription: '输出一行，包含一个数，表示在 `1` 到 `n` 的范围内二进制回文数的数量。',
            samples: [
                {
                    input: '15',
                    output: '6',
                },
            ],
            explanation: `**解析：**
本题要求统计 $1 \dots n$ 范围内其二进制表示（不含前导零）为回文串的数字个数。
1. **进制转换**：通过反复取模（\`% 2\`）和整除（\`/ 2\`），可以将十进制数转换为二进制位数组。
2. **回文判定**：使用双指针或对称索引检查数组首尾字符是否一致。
3. **范围枚举**：由于 $n$ 的范围通常在 $10^6$ 以内，直接 $O(n \log n)$ 暴力枚举每个数即可通过。

**核心逻辑提示：**
\`\`\`cpp
// 核心：十进制转二进制并存入数组
int t = i, pos = 0;
while (t) {
    bits[pos++] = t % 2;
    t /= 2;
}
// 核心：判定回文
for (int j = 0; j < pos / 2; j++) {
    if (bits[j] != bits[pos - 1 - j]) { /* 不回文 */ }
}
\`\`\`

**考点：** 进制转换（十进制转二进制）、数组应用、回文判定逻辑。`,
            tags: ['编程题', '进制转换', '枚举'],
            template: '#include <iostream>\nusing namespace std;\n\nint main() {\n    int n;\n    cin >> n;\n    // 在此编写代码\n    return 0;\n}',
            referenceCode: '#include <iostream>\nusing namespace std;\n\nint main() {\n    int n, a[50] = {0}, ans = 0;\n    cin >> n;\n    for (int i = 1; i <= n; i++) {\n        int t = i, pos = 0;\n        while (t) {\n            a[pos++] = t % 2;\n            t /= 2;\n        }\n        bool ok = true;\n        for (int j = 0; j < pos; j++)\n            if (a[j] != a[pos - j - 1]) {\n                ok = false;\n                break;\n            }\n        ans += ok;\n    }\n    cout << ans;\n    return 0;\n}',
        },
        {
            id: 27,
            type: 'programming',
            title: '凯撒密码',
            problemNumber: 'B4500',
            score: 25,
            description: '凯撒密码是一种替换加密技术，明文中的所有字母都会按照固定偏移量进行整体平移。例如偏移量是 `3` 时，`A` 会替换成 `D`，`B` 会替换成 `E`，……，`X` 会替换成 `A`，`Y` 会替换成 `B`，`Z` 会替换成 `C`。现在给你一组已破解的凯撒密码明文与密文，以及一个使用相同偏移量的待破解密文，请输出它对应的明文。',
            inputDescription: '输入共三行：第一行是已破解的凯撒密码明文；第二行是对应密文；第三行是待破解的凯撒密码密文。',
            outputDescription: '输出一行，包含一个字符串，表示待破解的凯撒密码对应的明文。',
            samples: [
                {
                    input: 'ABCDEFGVWXYZ\nDEFGHIJYZABC\nWKHTXLFNEURZQIRAMXPSVRYHUWKHODCBGRJ',
                    output: 'THEQUICKBROWNFOXJUMPSOVERTHELAZYDOG',
                },
            ],
            explanation: `**解析：**
凯撒密码的本质是字符集的循环位移。
1. **计算偏移量**：通过已知的明文 $P$ 和密文 $C$，偏移量 $d = P[0] - C[0]$。注意 $d$ 可能是负数，需通过 \`(d % 26 + 26) % 26\` 归一化到 $0 \dots 255$ 范围。
2. **解密变换**：对于待破解字符 $X$，明文 $X' = (X - 'A' + d) \pmod{26} + 'A'$。
3. **逐字符处理**：利用 \`string\` 或字符数组遍历输入并输出转换后的结果。

**核心逻辑提示：**
\`\`\`cpp
// 计算偏移量（以第一个字符为例）
int shift = (plain[0] - cipher[0] % 26 + 26) % 26;

// 变换逻辑
char original = (target[i] - 'A' + shift) % 26 + 'A';
\`\`\`

**考点：** 字符串处理、凯撒加密原理、模运算的负数处理技巧。`,
            tags: ['编程题', '字符串', '模拟'],
            template: '#include <iostream>\n#include <cstring>\nusing namespace std;\n\nint main() {\n    string s1, s2, s3;\n    cin >> s1 >> s2 >> s3;\n    // 在此编写代码\n    return 0;\n}',
            referenceCode: '#include <iostream>\n#include <cstring>\nusing namespace std;\n\nint main() {\n    string s1, s2, s3;\n    cin >> s1 >> s2 >> s3;\n    int d = s1[0] - s2[0];\n    d = (d % 26 + 26) % 26;\n    for (int i = 0; i < s3.length(); i++) {\n        int ch = (s3[i] - \'A\' + d) % 26;\n        cout << (char)(ch + \'A\');\n    }\n    return 0;\n}',
        },
    ],
};
