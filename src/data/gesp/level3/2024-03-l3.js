// 2024年3月 GESP C++ 三级真题 (第5次认证)
// 数据说明：本卷以官方真题 PDF 为主完成回填。
//-客观题 1~25 的题面均来自官方 PDF 文本提取，未使用公开解析还原题面
//-判断题答案表在 PDF 文本层中未被完整提取，当前答案按题面语义逐题复核填写
//-编程题 1~2 题名、题意、样例与参考代码来自官方 PDF，同时与公开 OJ 题号 B3956/B3957 对齐
export const paperData = {
    id: '2024-03-l3',
    title: '2024年3月 GESP C++ 三级真题',
    level: 3,
    year: 2024,
    month: 3,
    session: 5,
    timeLimit: 90 * 60,
    source: {
        officialPdf: 'https://gesp.ccf.org.cn/101/attach/1602047101108256.pdf',
        notes: '客观题 25 题已补齐；判断题答案表系依据官方题面逐题复核补入；编程题已补 2 题摘要与参考代码。'
    },
    verification: {
        status: 'partial',
        reviewedBy: '本站校订',
        reviewedAt: '2026-07-06',
        scope: '第 17 题已对照官方 PDF 校订。'
    },
    questions: [
        {
            id: 1,
            type: 'single',
            question: `整数 -5 的 16 位补码表示是（ ）。`,
            options: ['1005', '1006', 'FFFA', 'FFFB'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (FFFB)**

**核心解析：**
本题考察负数在 16 位补码表示下的转换规则。负数的补码 = 反码 + 1。

推导步骤：
1. **取绝对值的二进制**：|−5| = 5，二进制（16位）为 \`0000 0000 0000 0101\`
2. **逐位取反得反码**：符号位保持 1，其余位取反，得到 \`1111 1111 1111 1010\`（即 FFFA）
3. **加 1 得补码**：反码 + 1 = \`1111 1111 1111 1011\`（进位向前传播，最终 FFFB）

**选项逐项分析：**
- **A (1005)**：❌ 错误。原码表示中符号位为 1、数值位为 5 的原码是 \`1000 0000 0000 0101\`，即 8005，不是 1005，且 1005 根本不是合法的十六进制表示（包含数字 5 后直接接 5 无意义）。
- **B (1006)**：❌ 错误。同 A，1006 也不是合法的补码表示（1006 本身也不是合法的十六进制数）。
- **C (FFFA)**：❌ 错误。这是 -5 的反码，而非补码。补码需要在反码基础上加 1。
- **D (FFFB)**：✅ 正确。符合“反码 + 1”的补码计算规则。

**易错提醒：**
- 混淆原码、反码、补码三者关系；补码是反码加 1，不是取反后直接使用
- 计算进位时需逐位传播，如 FFFA + 1 = FFFB（最后一位 A→B，无额外进位）

**知识延伸：**
补码设计使得 CPU 只需一套加法器即可完成有符号数的加减运算，简化了硬件电路设计，这也是现代计算机普遍采用补码表示有符号整数的原因。

**考点：** 负数的补码计算规则（取反加1）与十六进制转换`,
            tags: ['二进制', '补码', '进制转换'],
        },
        {
            id: 2,
            type: 'single',
            question: `如果 16 位短整数 -2 的十六进制表示是 "FFFE"，则短整数 -4 的十六进制是（ ）。`,
            options: ['FF04', 'FFFA', 'FFFC', 'FFFH'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (FFFC)**

**核心解析：**
已知 -2 的十六进制补码为 FFFE，可推导出 -4 的补码。在补码体系中，每减 1（往更负的方向），十六进制值减 1。

推导步骤：
1. **确认 -2 的二进制**：FFFE = \`1111 1111 1111 1110\`
2. **求 -4 的补码**：-4 = (-2) + (-2)。从 -2 的补码减去 1（相当于加 FFFF）：FFFE - 1 = FFFD；或直接计算：-4 的绝对值 4 = \`0000 0000 0000 0100\`，取反加一得 \`1111 1111 1111 1100\` = FFFC
3. **验证关系**：-2 → FFFE，-4 → FFFC，每递减 2，补码递减 2（十六进制下）

**选项逐项分析：**
- **A (FF04)**：❌ 错误。FF04 的最高两位是 04，明显不是负数的补码形式（负数补码最高位应为 F）。该值接近 -252，不符合预期。
- **B (FFFA)**：❌ 错误。FFFA = \`1111 1111 1111 1010\`，转换为十进制：原码 = -(2^15 - FFFA) ≈ -6，即 -6 的补码是 FFFA。
- **C (FFFC)**：✅ 正确。FFFC = \`1111 1111 1111 1100\`，对应十进制值 -4。
- **D (FFFH)**：❌ 错误。H 不是合法的十六进制字符（十六进制只包含 0-9, A-F），因此该选项本身不是合法表示。

**易错提醒：**
- 十六进制字符只能是 0-9 和 A-F，不能出现 G、H 等字母
- -4 不是 -2 减 1，而是减 2；在补码中需要理解“每减少 1，十六进制值减 1”

**知识延伸：**
在补码表示中，16 位无符号范围是 0~65535，有符号范围是 -32768~32767。FFFC 对应无符号值 65532，对应有符号值 65532 - 65536 = -4。

**考点：** 补码表示法中负数的十六进制转换与数值偏移规律`,
            tags: ['二进制', '补码', '进制转换'],
        },
        {
            id: 3,
            type: 'single',
            question: `下面 C++ 代码执行后的输出是（ ）。\n\`\`\`cpp\nint main() {\n cout << (3 | 16) << endl;\n return 0;\n}\n\`\`\``,
            options: ['3', '16', '19', '48'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (19)**

**核心解析：**
按位或运算符 \`|\` 的规则：只要对应位中至少有一个为 1，结果位就为 1。

逐行推导：
1. **确定操作数二进制**：3 = \`00011\`（5位），16 = \`10000\`（5位）
2. **按位对齐进行或运算**：
   \`\`\`
   00011
   |10000
   -----
   10011
   \`\`\`
3. **转换结果**：10011（二进制）= 16 + 2 + 1 = 19

**选项逐项分析：**
- **A (3)**：❌ 错误。该选项误认为按位或是“取交集”或“保留较小值”。3 | 16 不等于 3 或 16。
- **B (16)**：❌ 错误。该选项误认为按位或是“取并集”或“保留较大值”。实际上按位或是逐位独立运算。
- **C (19)**：✅ 正确。10011₂ = 16 + 2 + 1 = 19。
- **D (48)**：❌ 错误。48 = 32 + 16 = \`110000\`（二进制），与 3 | 16 的结果无关。

**易错提醒：**
- 按位或 \`|\` 与逻辑或 \`||\` 是完全不同的运算符；\`||\` 产生布尔值（0 或 1），\`|\` 产生位运算结果
- 3 | 16 = 19，而非 3 和 16 之间的任何值

**知识延伸：**
按位或常用于“设置某些位为 1”的场景。例如，若要将一个整数的低 4 位全部置 1，可执行 \`x | 0xF\`。

**考点：** 按位或运算 \`|\` 的二进制逐位计算规则`,
            tags: ['二进制', '补码', '位运算'],
        },
        {
            id: 4,
            type: 'single',
            question: `定义整数 int x = -5，则执行 C++ 代码 cout << (x == (x << 1 >> 1)) 输出是（ ）。`,
            options: ['0', '1', '-5', '5'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (1)**

**核心解析：**
分析表达式 \`x << 1 >> 1\` 是否等于原值 x（x = -5）。

逐行推导变量状态：
1. **初始值**：x = -5
2. **计算 x << 1（左移 1 位）**：
   - -5 的补码（假设 32 位）：\`...1111 1111 1111 1111 1111 1111 1111 1011\`
   - 左移 1 位，低位补 0：\`...1111 1111 1111 1111 1111 1111 1111 0110\`（即 -10）
3. **计算 >> 1（算术右移 1 位）**：
   - 算术右移：高位补符号位（1）
   - \`...1111 0110\` → 高位补 1：\`...1111 1011\`（即 -5）
4. **比较**：-5 == -5，结果为 true
5. **输出**：布尔值 true 在 cout 中输出为 1

**选项逐项分析：**
- **A (0)**：❌ 错误。该选项认为表达式结果为 false。但对于 x = -5，左移后即使溢出或改变，右移仍可能恢复原值，故不等于 0。
- **B (1)**：✅ 正确。x << 1 >> 1 在 32 位 int 下等于 -5，比较结果为真，输出 1。
- **C (-5)**：❌ 错误。cout 输出的是比较表达式的布尔值（0 或 1），而非 x 本身的值。
- **D (5)**：❌ 错误。该选项既不是布尔值，也不是 -5 的绝对值，无依据。

**易错提醒：**
- cout << (表达式) 输出的是表达式的值，而非变量 x 的值
- 对于 -5，左移后位可能丢失（算术溢出），但右移恢复时由于是算术右移（补符号位），在 32 位环境下恰好恢复原值

**知识延伸：**
C++ 标准未规定右移是算术右移还是逻辑右移，但实际实现中：对有符号负数通常采用算术右移（补符号位），对无符号数通常采用逻辑右移（补 0）。

**考点：** 算术右移与逻辑左移的组合效应，以及布尔表达式在 cout 中的输出形式`,
            tags: ['二进制', '补码', '位运算'],
        },
        {
            id: 5,
            type: 'single',
            question: `已知字符 "0" 的 ASCII 编码的十进制表示为 48，则执行下面 C++ 代码后，输出是（ ）。\n\`\`\`cpp\nint main() {\n string s = "316";\n int n = s.length();\n int x = 0;\n for (int i = 0; i < n; i++) x += s[i];\n cout << x << endl;\n return 0;\n}\n\`\`\``,
            options: ['10', '58', '154', '316'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (154)**

**核心解析：**
字符串中每个字符实际上存储的是其 ASCII 码值。循环累加的是字符对应的 ASCII 码，而非字符表示的数值。

逐行推导变量状态：
1. **初始化**：s = "316"，n = s.length() = 3，x = 0
2. **第一次循环**（i = 0）：
   - s[0] = '3'，ASCII 码 = 48 + 3 = 51
   - x = 0 + 51 = 51
3. **第二次循环**（i = 1）：
   - s[1] = '1'，ASCII 码 = 48 + 1 = 49
   - x = 51 + 49 = 100
4. **第三次循环**（i = 2）：
   - s[2] = '6'，ASCII 码 = 48 + 6 = 54
   - x = 100 + 54 = 154
5. **输出**：x = 154

**选项逐项分析：**
- **A (10)**：❌ 错误。该选项误以为循环是把字符串当作数值（3+1+6=10），忽略了 ASCII 编码机制。
- **B (58)**：❌ 错误。若将字符 '3'、'1'、'6' 误当数字 3、1、6 累加，3+1+6=10；若 ASCII 值误算（48+3+48+1+48+6=154）等，均不等于 58。
- **C (154)**：✅ 正确。'3'=51，'1'=49，'6'=54，累加 51+49+54=154。
- **D (316)**：❌ 错误。该选项误以为代码会输出字符串"316"作为数字 316，完全忽略了 ASCII 码机制。

**易错提醒：**
- '0' 的 ASCII 值是 48，不是 0
- \`s[i]\` 访问的是字符的 ASCII 码值，不是字符本身代表的数字
- 若想将字符串转为数字，应使用 \`s[i] - '0'\` 提取数位值

**知识延伸：**
字符运算利用了 ASCII 码的连续性：\`'9' - '0' = 9\`，\`'A' + 1 = 'B'\`。这是一个在 GESP 三级考试中频繁出现的考点。

**考点：** 字符型数据的 ASCII 码存储机制与字符串遍历累加`,
            tags: ['位运算', '字符与ASCII', '循环'],
        },
        {
            id: 6,
            type: 'single',
            question: `下面 C++ 代码执行后数组中大于 0 的数的特征是（ ）。\n\`\`\`cpp\nint main() {\n int a[20], i;\n for (i = 0; i < 20; i++) a[i] = i+1;\n for (int i = 0; i < 20; i++)\n if ((a[i] % 2) && (a[i] % 3)) a[i] = 0;\n for (i = 0; i < 20; i++)\n if (a[i]) cout << a[i] << " ";\n return 0;\n}\n\`\`\``,
            options: ['2 的倍数', '3 的倍数', '能被 2 或 3 整除的数', '能被 2 和 3 同时整除的数'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (能被 2 或 3 整除的数)**

**核心解析：**
逐步追踪代码逻辑：
1. 第一个循环将数组初始化为 \`a[i] = i + 1\`，即 \`a\` 存储 1, 2, 3, ..., 20。
2. 第二个循环遍历每个元素，条件 \`(a[i] % 2) && (a[i] % 3)\` 表示“既不能被 2 整除，又不能被 3 整除”。满足此条件的元素被置为 0。
3. 第三个循环输出所有大于 0 的元素。
4. 因此保留的数恰好是条件 \`(a[i] % 2) && (a[i] % 3)\` 的否定，即**能被 2 整除或能被 3 整除**的数。

具体验证：1-20 中能被 2 或 3 整除的数为 2, 3, 4, 6, 8, 9, 10, 12, 14, 15, 16, 18, 20；其余（1, 5, 7, 11, 13, 17, 19）因不能被 2 或 3 整除而被置为 0。

**选项逐项分析：**
- **A (2 的倍数)**：❌ 错误。该项本身正确（保留的数中包含 2 的倍数），但并非全部特征，因为 3 的倍数也被保留。
- **B (3 的倍数)**：❌ 错误。同理，该项本身正确，但并非全部特征，2 的倍数也被保留。
- **C (能被 2 或 3 整除的数)**：✅ 正确。完整描述了保留下来的数的特征。
- **D (能被 2 和 3 同时整除的数)**：❌ 错误。该项本身正确（6, 12, 18 等确实被保留），但这描述的是“且”关系，而题目逻辑是“或”关系，漏掉了仅被 2 或仅被 3 整除的数。

**易错提醒：**
- 容易混淆逻辑：\`(a % 2) && (a % 3)\` 为真意味着“既不是 2 的倍数也 不是 3 的倍数”，取反后是“能被 2 或能被 3 整除”。
- 不要将“或”误判为“且”，这是本题的核心思维陷阱。

**知识延伸：**
逻辑运算的德摩根定律在此类条件过滤题中常用：(¬p) ∧ (¬q) ⇔ ¬(p ∨ q)。GESP 三级常考通过条件取反实现“筛选保留”与“置零排除”的切换。

**考点：** 逻辑运算与数组条件过滤`,
            tags: ['位运算', '数组', '循环', '逻辑判断'],
        },
        {
            id: 7,
            type: 'single',
            question: `执行下面 C++ 代码后输出的第一个数是（ ）。\n\`\`\`cpp\nint main() {\n int a[20], i;\n for (i = 0; i < 20; i++) a[i] = i+1;\n for (; i > 0; i--) cout << a[i-1] << " ";\n return 0;\n}\n\`\`\``,
            options: ['20', '19', '1', '不确定'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (20)**

**核心解析：**
1. 第一个循环 \`for (i = 0; i < 20; i++)\` 结束后，循环变量 \`i\` 的值为 20（因为循环在 \`i=20\` 时不满足 \`i < 20\` 条件而退出，此时 \`i\` 已自增到 20）。
2. 第二个循环 \`for (; i > 0; i--)\` **没有**重新初始化 \`i\`，直接使用已有的 \`i=20\` 作为初值。
3. 首次执行 \`cout << a[i-1]\` 时，\`i-1 = 19\`，即访问 \`a[19]\`。
4. \`a[19]\` 在初始化阶段被赋值为 \`19 + 1 = 20\`。
5. 循环会继续倒序输出 a[19] 到 a[0]，第一个输出的数是 **20**。

**选项逐项分析：**
- **A (20)**：✅ 正确。首次输出的是 a[19]，其值为 20。
- **B (19)**：❌ 错误。这是 a[18] 的值，不是第一个输出的数。
- **C (1)**：❌ 错误。这是最后一个输出的数（a[0] = 0 + 1 = 1）。
- **D (不确定)**：❌ 错误。变量 \`i\` 在作用域内有明确初值，输出结果确定。

**易错提醒：**
- 误以为第二个循环会重新声明 \`i\`，但 \`int i\` 只在第一个循环中出现，第二个循环使用外层已有的 \`i\`。
- 忽略 \`i--\` 是先输出再递减，导致误判输出顺序。

**知识延伸：**
GESP 三级常考循环变量在嵌套或连续循环中的状态持久性。关键原则：若无重新赋值，循环变量保留上一次结束时的值。

**考点：** 循环变量状态的持久性与数组索引偏移`,
            tags: ['位运算', '数组', '循环'],
        },
        {
            id: 8,
            type: 'single',
            question: `在下列代码的横线处填写（ ），可以使得输出是 GESP IS INTERESTING。\n\`\`\`cpp\nint main() {\n string str = "gEsP is Interesting";\n int x = str.length();\n for (int i = 0; i < x; i++)\n if ((str[i] >= "a"[0]) && (str[i] <= "z"[0]))\n ________________________;\n cout << str << endl;\n return 0;\n}\n\`\`\``,
            options: ["str[i] += 'a'-'A'", 'str[i] += 20', "str[i] += 'A'-'a'", '无法实现'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (str[i] += 'A'-'a')**

**核心解析：**
1. 目标是将小写字母转为大写字母，同时保留非小写字符不变。
2. 条件 \`str[i] >= 'a' && str[i] <= 'z'\` 判断是否为小写字母。
3. 小写字母转大写需要**减少**其 ASCII 码值。已知 ASCII 码中 'A'=65，'a'=97，差值为 32。
4. \`'A' - 'a' = 65 - 97 = -32\`，将 -32 加到小写字母上，正好得到对应的大写字母。
5. 非小写字符（'E'、'S'、'P'、空格等）不进入 if 分支，保持原样。

**选项逐项分析：**
- **A (str[i] += 'a'-'A')**：❌ 错误。\`'a'-'A' = 32\`，加 32 会将大写字母转为更大码值的字符（变成乱码），或把小写字母转为下一个字母，而非大写。
- **B (str[i] += 20)**：❌ 错误。20 不是正确的大小写转换偏移量，ASCII 码表中大小写字母的差值恒为 32。
- **C (str[i] += 'A'-'a')**：✅ 正确。\`-32\` 的偏移量准确实现小写转大写。
- **D (无法实现)**：❌ 错误。代码逻辑清晰，可以实现。

**易错提醒：**
- 混淆 \`'a'-'A'\`（正值 32）和 \`'A'-'a'\`（负值 -32）的方向：小写转大写需要**减去** 32。
- 误以为偏移量是 20 或其他数值，忽略了 ASCII 表中字母顺序固定的规律。

**知识延伸：**
字符的算术运算本质是对 ASCII 码值进行加减。GESP 三级常见考点还包括：字符与整数的混合运算、数字字符 '0'-'9' 与对应数值的转换（'0' + n = 对应数字字符）。

**考点：** ASCII 码表规律与字符加减运算`,
            tags: ['位运算', '循环', '逻辑判断'],
        },
        {
            id: 9,
            type: 'single',
            question: `假设英文句子由若干词构成。下面 C++ 代码统计输出的词数是（ ）。\n\`\`\`cpp\nint main() {\n string str = "gEsP is Interesting !";\n int x = str.length();\n int nwords = 0;\n for (int i = 0; i < x; i++)\n if (str[i] == " "[0]) {\n nwords++;\n while (str[++i] == " "[0]);\n }\n cout << nwords << endl;\n return 0;\n}\n\`\`\``,
            options: ['1', '2', '3', '4'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (3)**

**核心解析：**
1. 字符串 \`"gEsP is Interesting !"\` 的长度 x 为 24（包含空格和感叹号）。
2. 循环遍历每个字符，当遇到空格时执行：
   - \`nwords++\`：计数加 1
   - \`while (str[++i] == " "[0]);\`：跳过所有后续连续空格，将 i 移动到下一个非空格字符之前。
3. 统计的是**空格段落数**，即连续空格的第一个空格出现一次计一个。
4. 字符串中的空格分布：第一个空格在 "gEsP" 和 "is" 之间，第二个空格在 "is" 和 "Interesting" 之间，第三个空格在 "Interesting" 和 "!" 之间。共 **3 个**独立空格段落。
5. 程序输出 nwords = **3**。

**选项逐项分析：**
- **A (1)**：❌ 错误。只统计了第一个空格，未理解循环会累加计数。
- **B (2)**：❌ 错误。遗漏了第三个空格（"Interesting" 后的空格）。
- **C (3)**：✅ 正确。准确统计了三个空格段落。
- **D (4)**：❌ 错误。该句子有 4 个单词（gEsP、is、Interesting、!），但程序统计的是空格段落数，而非单词数。

**易错提醒：**
- 误将单词数当作空格数：虽然 4 个单词对应 3 个空格，但程序统计的是空格段落而非单词。
- 未理解 \`while\` 循环的跳跃机制：遇到一个空格后会跳过其后所有连续空格，避免重复计数。

**知识延伸：**
统计“独立单词数”通常需要更复杂的逻辑（如判断空格前后是否为字母）。本题的 \`while (str[++i] == " "[0]);\` 技巧常用于跳过连续分隔符，GESP 三级字符串处理题中需注意循环变量在多重跳跃中的位置更新。

**考点：** 字符串处理与多分支跳跃逻辑`,
            tags: ['位运算', '字符与ASCII', '字符串', '循环'],
        },
        {
            id: 10,
            type: 'single',
            question: `C++ 的字符变量的码值是整数，下面字面量形式的字符码值最大的是（ ）。`,
            options: ['100', '075', '0x70', '0x60'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (0x70)**

**核心解析：**
逐项将各选项转换为十进制数值进行比较：
- **A (100)**：❌ 错误。十进制字面量，直接为 100。
- **B (075)**：❌ 错误。八进制字面量（前缀 0 表示八进制），转换为十进制：$7 \\times 8^1 + 5 \\times 8^0 = 7 \\times 8 + 5 = 56 + 5 = 61$。
- **C (0x70)**：✅ 正确。十六进制字面量（前缀 0x 表示十六进制），转换为十进制：$7 \\times 16^1 + 0 \\times 16^0 = 7 \\times 16 + 0 = 112$。
- **D (0x60)**：❌ 错误。十六进制字面量，转换为十进制：$6 \\times 16^1 + 0 \\times 16^0 = 6 \\times 16 + 0 = 96$。

比较结果：61 < 96 < 100 < 112，最大值为 **112**，对应选项 **C (0x70)**。

**选项逐项分析：**
- **A (100)**：❌ 错误。转换为十进制为 100，小于 112。
- **B (075)**：❌ 错误。八进制 075 等于十进制 61，是四个选项中最小的。
- **C (0x70)**：✅ 正确。十六进制 0x70 等于十进制 112，为最大值。
- **D (0x60)**：❌ 错误。十六进制 0x60 等于十进制 96，虽大于 61 但小于 112。

**易错提醒：**
- 忽略八进制的前导 0：将 075 误认为十进制 75。
- 混淆进制转换规则：十六进制每位权重为 16^n，八进制每位权重为 8^n，十进制每位权重为 10^n。
- 未注意十六进制的字母 digits（A-F）对应 10-15，但本题数字部分不含字母。

**知识延伸：**
C++ 中整型常量可以写成十进制（无前缀）、八进制（前缀 0）或十六进制（前缀 0x）。字符字面量的码值也可以用这些进制表示，如 \`'\\x41'\` 表示 ASCII 65（GESP 三级偶有涉及）。

**考点：** C++ 整型常量的表示法（八进制 0、十六进制 0x）与数值转换`,
            tags: ['进制转换', '字符与ASCII'],
        },
        {
            id: 11,
            type: 'single',
            question: `下面 C++ 程序执行的结果是（ ）。\n\`\`\`cpp\nint main() {\n int a[20], i;\n int cnt = 0;\n for (i = 0; i < 20; i++) a[i] = i+1;\n for (; i > 1; i--)\n if ((a[i-1]+a[i-2]) % 3) cnt++;\n cout << cnt << endl;\n return 0;\n}\n\`\`\``,
            options: ['5', '6', '10', '12'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (12)**

**核心解析：**
逐行追踪代码执行过程，确定循环条件和计数逻辑

1. **第一个 for 循环**：\`for (i = 0; i < 20; i++) a[i] = i + 1;\`
   - i 从 0 递增到 19，共执行 20 次
   - 循环结束时 i = 20
   - 数组 a 的内容为：a[0]=1, a[1]=2, a[2]=3, ..., a[19]=20

2. **第二个 for 循环**：\`for (; i > 1; i--)\`
   - 注意：此处 i 未重新初始化，保持为 20
   - 等价于 \`for (i = 20; i > 1; i--)\`
   - i 的取值依次为：20, 19, 18, ..., 2
   - 共执行 19 次

3. **循环体分析**：每次循环检查 \`(a[i-1] + a[i-2]) % 3\`
   - i=20 时：a[19]+a[18] = 20+19 = 39
   - i=19 时：a[18]+a[17] = 19+18 = 37
   - ...
   - i=2 时：a[1]+a[0] = 2+1 = 3

4. **模运算判断**：\`(a+b) % 3\` 为真（即非零）表示不能被 3 整除
   - 上述 19 个和为：39, 37, 35, 33, 31, 29, 27, 25, 23, 21, 19, 17, 15, 13, 11, 9, 7, 5, 3
   - 能被 3 整除的有：39, 33, 27, 21, 15, 9, 3（共 7 个）
   - 不能被 3 整除的有：19 - 7 = 12 个
   - 故 cnt = 12

**选项逐项分析：**
- **A 5**：❌ 错误。计算错误，未正确统计所有相邻和的模运算结果。
- **B 6**：❌ 错误。低估了符合条件的数目。
- **C 10**：❌ 错误。计算不准确。
- **D 12**：✅ 正确。19 个相邻和对中，有 12 个不能被 3 整除。

**易错提醒：**
- 忽略第二个循环中 i 已保持为 20 的状态，未重新初始化
- 将数组下标与元素值混淆，如把 a[i] 误写为 i
- 遗漏某些相邻和的计算

**知识延伸：**
在多循环结构中，前一个循环的计数器状态会延续到后续循环，这种特性常被用于巧妙的代码设计。相邻元素的索引关系（如 a[i-1] 与 a[i-2]）是数组操作中的高频考点。

**考点：** 数组索引计算、相邻元素求和与模运算判断`,
            tags: ['位运算', '数组', '循环'],
        },
        {
            id: 12,
            type: 'single',
            question: `定义字符数组 char str[20] = {'G', 'E', 'S', 'P'};，则 str 的字符串长度为（ ）。`,
            options: ['4', '5', '19', '20'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (4)**

**核心解析：**
字符数组的部分初始化规则与字符串结尾标记

1. **数组声明与初始化**：\`char str[20] = {'G', 'E', 'S', 'P'};\`
   - 数组总大小为 20 个 char
   - 只显式初始化了前 4 个元素：str[0]='G', str[1]='E', str[2]='S', str[3]='P'
   - 剩余 16 个元素自动初始化为 0（即 '\\0'）

2. **字符串长度计算**：
   - C++ 中 \`strlen(str)\` 从首字符开始计数，直到遇到首个 '\\0' 停止
   - str[4] 及之后的元素均为 '\\0'
   - 因此字符串长度为 4

3. **关键区分**：
   - 数组总大小：20（存储空间）
   - 字符串长度：4（有效字符数，不含结尾 '\\0'）

**选项逐项分析：**
- **A 4**：✅ 正确。前四个字符为 'G'、'E'、'S'、'P'，第 5 个位置开始为 '\\0'，字符串长度为 4。
- **B 5**：❌ 错误。误认为包含结尾 '\\0'，或混淆了数组大小与字符串长度的概念。
- **C 19**：❌ 错误。19 是数组中 '\\0' 的数量（20-4=16 个自动初始化为 0），而非字符串长度。
- **D 20**：❌ 错误。20 是数组声明的总大小，与实际存储的字符串长度无关。

**易错提醒：**
- 混淆 \`strlen\`（字符数）与 \`sizeof\`（总字节数）的返回值
- 认为部分初始化的数组其余元素是随机值，实际上全局/静态数组自动初始化为 0
- 忽略 '\\0' 作为字符串结束标记的作用

**知识延伸：**
字符数组的初始化规则：若初始化列表长度小于数组大小，其余元素自动填充为 '\\0'。这一特性使得用列表初始化声明字符串时无需手动添加 '\\0'，与 \`char str[20] = "GESP";\` 等价。

**考点：** 字符数组的部分初始化规则与字符串结尾标记 ('\\0')`,
            tags: ['字符与ASCII', '字符串', '字符数组', '数组'],
        },
        {
            id: 13,
            type: 'single',
            question: `定义整型变量 int a = 3, b = 16，则 a | b 的值和 a+b 的关系是（ ）。`,
            options: ['大于', '等于', '小于', '等于或小于'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (等于)**

**核心解析：**
按位或运算与算术加法在特定条件下的等价性

1. **确定操作数**：
   - a = 3，二进制为 00011（5 位表示）
   - b = 16，二进制为 10000（5 位表示）

2. **逐位分析**：
   \`\`\`
        00011  (a = 3)
      | 10000  (b = 16)
      --------
        10011  → 十进制 19
   \`\`\`
   - a 的 5 个二进制位：第 0 位为 1，第 1 位为 1，其余为 0
   - b 的 5 个二进制位：第 4 位为 1，其余为 0
   - 两个数的二进制位完全不重叠（没有共同为 1 的位）

3. **数学结论**：
   - 当两个整数的二进制表示没有相同位置同时为 1 时，按位或的结果等于算术和
   - a | b = 3 | 16 = 19
   - a + b = 3 + 16 = 19
   - 两者相等

**选项逐项分析：**
- **A 大于**：❌ 错误。3 | 16 = 19，3 + 16 = 19，两者相等而非大于。
- **B 等于**：✅ 正确。a 和 b 的二进制位无重叠，按位或与加法结果相同，均为 19。
- **C 小于**：❌ 错误。两者相等，不存在小于关系。
- **D 等于或小于**：❌ 错误。虽然包含"等于"的可能性，但"或小于"的部分使表述不精确，本题两者精确相等。

**易错提醒：**
- 将位运算符号 \`|\` 误认为逻辑或 \`||\`，后者返回的是布尔值 0 或 1
- 混淆按位或与异或的规则：异或要求相同为 0、不同为 1；或只要有 1 则为 1
- 未考虑进位问题：普通加法可能产生进位，而按位或是独立处理每一位

**知识延伸：**
按位或与加法等价的前提是"无进位冲突"——即两个操作数在相同二进制位上不同时为 1。这正是计算机中快速计算"无重叠集合并集大小"的原理。GESP 常见考点还包括：\`a & b\`、\`a ^ b\` 与 \`a + b\` 的关系分析。

**考点：** 按位或运算与算术加法的关系分析`,
            tags: ['程序分析'],
        },
        {
            id: 14,
            type: 'single',
            question: `小杨的父母最近刚刚给他买了一块华为手表，他说手表上跑的是鸿蒙，这个鸿蒙是（ ）。`,
            options: ['小程序', '计时器', '操作系统', '神话人物'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (操作系统)**

**核心解析：**
鸿蒙（HarmonyOS）是华为公司研发的操作系统

1. **题目背景**：小杨的华为手表运行的是鸿蒙系统
2. **鸿蒙定位**：鸿蒙（HarmonyOS）是华为公司自主研发的分布式操作系统，主要应用于智能手机、平板、智能手表、物联网设备等
3. **技术特性**：采用微内核设计，支持跨设备协同和分布式能力

**选项逐项分析：**
- **A 小程序**：❌ 错误。小程序是运行在操作系统之上的应用形式（如微信小程序），不是操作系统本身。
- **B 计时器**：❌ 错误。计时器是手表的基础功能组件，而非操作系统。
- **C 操作系统**：✅ 正确。鸿蒙是华为自主研发的国产操作系统。
- **D 神话人物**：❌ 错误。虽然"鸿蒙"一词源自中国神话传说（《庄子·在宥》），但华为将其作为操作系统名称，该选项与题意不符。

**易错提醒：**
- 注意"鸿蒙"的双重含义：神话概念与操作系统名称，本题考察的是实际应用层面
- 区分操作系统（如鸿蒙、Windows、iOS）与应用程序的区别

**知识延伸：**
除鸿蒙外，主流操作系统还包括：桌面端的 Windows、macOS、Linux；移动端的 Android（基于 Linux）、iOS。了解国产操作系统的发展是 GESP 计算机基础模块的常见考点。

**考点：** 计算机基础常识及国产软件发展情况`,
            tags: ['计算机基础'],
        },
        {
            id: 15,
            type: 'single',
            question: `中国计算机学会（CCF）在 2024 年 1 月 27 日的颁奖典礼上颁布了王选奖，王选先生的重大贡献是（ ）。`,
            options: ['制造自动驾驶汽车', '创立培训学校', '发明汉字激光照排系统', '成立方正公司'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (发明汉字激光照排系统)**

**核心解析：**
王选院士的核心学术贡献

1. **历史背景**：2024 年 1 月 27 日，CCF（中国计算机学会）颁发王选奖
2. **王选简介**：王选（1937-2006），中国科学院院士、中国工程院院士，计算机文字信息处理专家
3. **核心贡献**：主持研制成功汉字激光照排系统，引发中国印刷业告别"铅与火"的历史性变革，使中国在印刷领域进入世界先进行列
4. **历史地位**：被誉为"当代毕昇"，是计算机汉字信息处理技术的奠基人

**选项逐项分析：**
- **A 制造自动驾驶汽车**：❌ 错误。与王选院士的研究方向完全不符。
- **B 创立培训学校**：❌ 错误。王选的主要贡献是学术研究和技术发明，而非教育培训。
- **C 发明汉字激光照排系统**：✅ 正确。这是王选院士的核心技术贡献，奠定了中国印刷行业数字化基础。
- **D 成立方正公司**：❌ 错误。方正集团确实由王选参与创立，但"成立公司"属于产业行为，而非王选获得王选奖所表彰的"重大贡献"。本题明确问的是"重大贡献"，应以技术发明为准。

**易错提醒：**
- 王选奖表彰的是"技术贡献"而非"商业成就"，D 选项虽与王选相关，但不符合题意
- 注意区分"发明"（原创性技术贡献）与"创办"（产业运营行为）的区别

**知识延伸：**
CCF 王选奖（原"CCF 创新奖"）设立于 2010 年，每年评选一次，表彰在计算机领域做出重大技术发明的科技工作者。汉字激光照排系统的研制成功解决了汉字信息处理的关键难题，是中国计算机技术发展史上的里程碑事件。

**考点：** 计算机科学史与杰出科学家贡献`,
            tags: ['程序分析'],
        },
        {
            id: 16,
            type: 'judge',
            question: `任意整数 a 的二进制反码与补码都有 1 位不同。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
首先明确反码与补码的定义：
- 对于正数：原码、反码、补码三者相同。
- 对于负数：反码是原码除符号位外逐位取反，补码是反码加 1。

题干声称"任意整数 a 的二进制反码与补码都有 1 位不同"，这显然过于绝对。以 8 位二进制为例：

假设 \`a = -1\`，原码为 \`10000001\`，反码为 \`11111110\`，补码为 \`11111111\`。从反码到补码（加 1），最低位由 0 变 1，且产生进位传播，多位发生变化，远不止 1 位。

退一步讲，即使仅看反码到补码的转换，当反码最低位为 1 时，加 1 会产生进位并向高位传播，导致多位改变。只有当反码最低位为 0 时，才恰好只有 1 位变化。

因此，并非所有整数都满足"反码与补码只有 1 位不同"这一特征。

**易错提醒：**
- 混淆正负数的表示规则：正数的反码与补码完全相同。
- 误以为补码只是简单翻转一位，忽略了加 1 产生的进位传播。

**知识延伸：**
补码的设计初衷是用加法器统一处理加减运算，理解补码的转换规则是掌握有符号整数表示的基础。

**考点：** 二进制、补码、反码`,
            tags: ['二进制', '补码'],
        },
        {
            id: 17,
            type: 'judge',
            question: `对整型变量 int a = 3，执行 C++ 代码 a << 2 将把 2 输出到 a 中。`,
            sourcePage: 5,
            sourceVerified: true,
            reviewedBy: '本站校订',
            reviewedAt: '2026-07-06',
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
本题需要区分表达式求值与赋值操作。

\`a << 2\` 是 C++ 中的左移位运算符表达式，它根据变量 \`a\` 的当前值计算出一个新的值，本身**不会修改变量 a 的原值**。因此"将 2 输出到 a 中"这一描述存在两个问题：

1. \`a << 2\` 是表达式而非语句，不会产生任何"输出"行为；
2. 对于 \`a = 3\`（二进制 \`11\`），\`a << 2\` 的实际结果是 \`12\`（二进制 \`1100\`），而不是 2。

若要真正将 2 赋值给 \`a\`，应使用：\`a = 2;\` 或 \`a <<= 2;\`（后者会将 a 的值改为 12）。

**易错提醒：**
- 将位运算符表达式 \`a << 2\` 与复合赋值 \`a <<= 2\` 混淆，后者才修改变量 a 的值。
- 误以为左移是乘以 2 的幂次后直接"输入"到原变量，实际上只是产生一个新值供使用。

**知识延伸：**
位运算符（\`<<\`、\`>>\`、\`&\`、\`|\`、\`^\`、\`~\`）均产生新值，不改变操作数本身；复合赋值版本（如 \`<<=\`）才会修改变量。

**考点：** 位运算、左移运算符`,
            tags: ['位运算'],
        },
        {
            id: 18,
            type: 'judge',
            question: `下面 C++ 代码可以计算 1 到 100 的累加和，采用的是穷举法。\n\`\`\`cpp\nint main() {\n int i, sum = 0;\n for (int i = 1; i <= 100; i++) sum += i;\n cout << sum << endl;\n return 0;\n}\n\`\`\``,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
分析代码执行流程：

\`\`\`cpp
int i, sum = 0;
for (int i = 1; i <= 100; i++) sum += i;
\`\`\`

- 循环变量 \`i\` 从 1 开始，逐步递增到 100，共执行 100 次迭代。
- 每次迭代将当前的 \`i\` 值累加到 \`sum\` 中。
- 循环结束后，\`sum = 1 + 2 + 3 + ... + 100 = 5050\`。

穷举法（枚举法）的核心思想是：将所有可能的解一一列举出来进行验证或计算。本题中，程序明确地遍历了从 1 到 100 的每一个整数，逐个累加求和，完全符合穷举法的定义。

**易错提醒：**
- 不能将"累加求和"与"穷举法"对立起来；穷举法可以是遍历所有情况并执行某种操作（如本题的累加）。
- 区分穷举法与其他算法（如二分查找、递归分治等）的关键在于是否系统地覆盖了所有可能的输入情况。

**知识延伸：**
穷举法是 GESP 三级的重要考点，适用于解空间有限且规模可控的问题（如暴力破解、计数问题等）。

**考点：** 穷举法、循环、累加`,
            tags: ['位运算', '枚举', '算法思维', '循环'],
        },
        {
            id: 19,
            type: 'judge',
            question: `一个 int 类型变量 a，执行操作 (a << 2 >> 2) 后的值一定是 a。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
分析表达式 \`(a << 2 >> 2)\` 对有符号整数 \`a\` 的影响：

1. **左移丢失高位**：执行 \`a << 2\` 时，\`a\` 的二进制表示向左移动 2 位，低位补 0。**原最高两位（不含符号位）的信息被移出并丢失**，即使后续右移也无法恢复。

2. **右移不恢复**：执行 \`>> 2\` 时，虽然可能右移回来，但左移阶段丢失的高位数据已不可挽回。

举例说明：假设 \`a = 48\`（二进制 \`00110000\`，8 位），\`a << 2 = 0011000000\`（10 位，结果取决于实现，此处取最低 8 位为 \`10000000\`），再 \`>> 2\` 得到 \`00100000\`（即 32），与原值 48 不等。

3. **负数更复杂**：对于负数，左移可能改变符号位，右移时符号位扩展规则进一步复杂化，结果更不确定。

因此，\`(a << 2 >> 2)\` 的值**不一定等于 \`a\`**，仅当 \`a\` 的高 2 位为 0 时才恰好相等，但题目使用了"一定"这一绝对表述。

**易错提醒：**
- 左移丢失的高位信息是"永久性丢失"，右移无法补救。
- 不要被简单的代数思维误导（误以为 \`(x << 2) >> 2 = x\`），需考虑位溢出。

**知识延伸：**
无符号整数的左移右移是纯逻辑移位；有符号整数的右移是算术移位（符号位扩展），但左移仍可能导致溢出。

**考点：** 位运算、逻辑移位、算术移位`,
            tags: ['位运算'],
        },
        {
            id: 20,
            type: 'judge',
            question: `在 C++ 语言中，(010 << 1) 执行结果是 100。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
本题涉及 C++ 中的进制字面量表示与位运算。

\`010\` 是 C++ 的**八进制字面量**表示（以 0 开头），而非十进制数：
- \`010\`（八进制）= \`1 × 8¹ + 0 × 8⁰\` = \`8\`（十进制）

执行左移运算：
- \`010 << 1\` = \`8 << 1\` = \`16\`（十进制）

十六进制表示中，16 的正确表示为 \`0x10\`，而非 \`100\`。

若将 \`010\` 误认为是十进制 10，则 \`10 << 1 = 20\`（十进制），其十六进制也不是 \`100\`。

若按二进制理解，\`100\`（二进制）= \`4\`（十进制），与 16 无关。

无论哪种解读，结果都不是题目所说的 \`100\`。

**易错提醒：**
- C++ 中以 \`0\` 开头的整数常量是八进制，这是 GESP 常考的易错点。
- 不要将二进制的 \`100\`、十进制的 \`100\`、八进制的 \`100\`（等于十进制 64）混淆。

**知识延伸：**
C++ 整数进制前缀：\`0x\` / \`0X\` 表示十六进制，\`0\` 表示八进制，无前缀表示十进制。

**考点：** 进制转换、八进制字面量、位运算`,
            tags: ['进制转换', '位运算'],
        },
        {
            id: 21,
            type: 'judge',
            question: `执行下面 C++ 代码后将输出 2。\n\`\`\`cpp\nint main() {\n string str = "gEsP is Interesting";\n int x = str.find("s");\n cout << x << endl;\n return 0;\n}\n\`\`\``,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
逐字符分析字符串 \`"gEsP is Interesting"\` 中每个字符的位置下标：
- 位置 0：\`g\`
- 位置 1：\`E\`
- 位置 2：\`s\`
- 位置 3：\`P\`
- 位置 4：空格 \`' '\`
- 位置 5：\`i\`
- 位置 6：\`s\`

\`string\` 类的 \`find("s")\` 方法返回子串 \`"s"\` 首次出现的下标位置。该字符串中第一个 \`'s'\` 出现在位置 2（\`gEsP\` 中的 \`s\`），因此 \`x = 2\`，输出结果为 \`2\`。

**易错提醒：**
- 字符串下标从 0 开始计数，容易误认为从 1 开始而错判结果。
- 注意区分大小写，字符串中还有另一个 \`'s'\` 在 \`"is"\` 中（位置 6），但 \`find\` 只返回首次匹配的位置。

**知识延伸：**
\`string::find()\` 是 C++ 标准库中常用的字符串查找函数，若未找到则返回 \`string::npos\`（通常为 -1 或最大的无符号整数值）。

**考点：** 字符串、string::find() 方法`,
            tags: ['位运算', '字符串'],
        },
        {
            id: 22,
            type: 'judge',
            question: `在 C++ 语言中，字符数组被定义时，它的大小可以调整。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
在 C++ 语言中，字符数组（\`char arr[size]\`）属于普通数组，其本质是一段连续的固定大小的内存空间。数组大小 \`size\` 在编译时确定，定义后无法改变。数组不提供自动扩容、缩容或重新分配内存的功能，这与 \`std::vector\` 或 \`std::string\` 等容器不同，后者在需要时可以通过 \`push_back()\`、\`resize()\` 等方法动态调整。

**易错提醒：**
- 不能将字符数组与 \`std::string\` 混淆。\`std::string\` 虽然表面看起来可以"改变长度"，但其底层也是通过动态分配内存实现的，而非数组本身可调整大小。
- 使用 \`new\` 动态创建的字符数组（如 \`char* p = new char[n]\`）同样在 \`new\` 时确定大小，之后若想改变只能重新分配新内存并释放旧内存。

**知识延伸：**
若需要在运行时调整字符序列的大小，应使用 \`std::string\`（处理文本）或 \`std::vector<char>\`（处理原始字符数据），而非普通字符数组。

**考点：** 字符数组、数组长度固定性`,
            tags: ['字符串', '字符数组', '数组'],
        },
        {
            id: 23,
            type: 'judge',
            question: `对定义的数组 int a[7] = {2, 0, 2, 4, 3, 1, 6}，可以用简单循环就找到其中最小的整数。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
对于任意数组（包括本题的 \`int a[7] = {2, 0, 2, 4, 3, 1, 6}\`），线性扫描算法可以在一次遍历内找到最小值。具体做法是：
1. 初始化 \`minVal = a[0]\`（或设为足够大的数）；
2. 遍历数组 \`a[1]\` 到 \`a[6]\`，若遇到比 \`minVal\` 更小的元素则更新 \`minVal\`；
3. 遍历结束后 \`minVal\` 即为最小值。

对于数组 \`{2, 0, 2, 4, 3, 1, 6}\`，最小值 \`0\` 位于下标 1 的位置，算法能够正确识别。

**易错提醒：**
- 找最大值与找最小值的思路完全对称，只需将比较符号从 \`<\` 改为 \`>\` 即可。
- 若数组有负数，初始化最小值时不能设为 0，应设为 \`INT_MAX\` 或数组首元素。

**知识延伸：**
该线性扫描算法的时间复杂度为 O(n)，是寻找极值问题的标准解法。若数组有序（如递增排列），可直接通过下标访问获取最小值，但那是利用了有序性的额外信息，而非通用方法。

**考点：** 数组、线性扫描找最小值`,
            tags: ['数组'],
        },
        {
            id: 24,
            type: 'judge',
            question: `小杨今年春节回奶奶家了，奶奶家的数字电视要设置 IP 地址并接入到 WIFI 盒子才能收看节目，那这个 WIFI 盒子具有路由器的功能。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
路由器（Router）的核心功能是连接不同网络，并根据 IP 地址等信息将数据包转发到目标网络。题目中描述的"WIFI 盒子"具备以下功能：
1. **IP 地址设置**：这是路由器的典型功能，允许用户配置 WAN/LAN 口参数；
2. **接入网络**：通过 WAN 口连接上游网络（如光猫）；
3. **网络分享**：通过 Wi-Fi 无线信号将网络连接分享给多个设备。

在家用场景中，这种同时具备路由器功能和无线接入点（AP）功能的设备通常称为"无线路由器"。题目中"WIFI 盒子"的工作描述符合路由器的定义，因此具有路由器的功能。

**易错提醒：**
- 纯 AP（无线接入点）只负责无线信号发射，不具备 IP 地址分配和路由转发功能。题目明确提到"设置 IP 地址"，说明该设备具备路由器的核心功能。
- 企业级网络中路由器、交换机、AP 可能分别部署，但家用场景常将多功能集成在一个设备中。

**知识延伸：**
家庭路由器通常还集成 DHCP 服务器功能（自动分配 IP 地址）、NAT（网络地址转换）等技术，这些都是在 OSI 模型第三层（网络层）工作的典型路由功能。

**考点：** 网络基础知识、路由器功能`,
            tags: ['计算机基础'],
        },
        {
            id: 25,
            type: 'judge',
            question: `任何一个 for 循环都可以转化为等价的 while 循环。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
\`for\` 循环的语法结构为 \`for(初始化; 条件; 迭代)\`，其执行流程可以完全等价为以下 \`while\` 循环：
\`\`\`cpp
初始化;
while(条件) {
    循环体;
    迭代;
}
\`\`\`
反之，任意 \`while\` 循环也可以转换为 \`for\` 循环：
\`\`\`cpp
for(; 条件;) {
    循环体;
}
\`\`\`
或者写成 \`for(; 条件; 迭代)\` 的形式，其中初始化部分放在循环之前。

**易错提醒：**
- 转换时需注意变量作用域。例如 \`for(int i=0; ...)\` 中 \`i\` 的作用域仅限于循环内，而等价的 \`while\` 写法中若在外部声明变量，则作用域会扩大。
- \`continue\` 语句在两种循环中的行为一致，都会跳转到迭代部分执行。

**知识延伸：**
虽然 \`for\` 和 \`while\` 循环在表达能力上等价，但在实际编程中：
- 当循环变量初始化和迭代逻辑与循环条件紧密相关时，\`for\` 循环写法更紧凑、意图更明确；
- 当循环条件与预先初始化的变量无关时，\`while\` 循环更自然。

**考点：** 循环结构、for 与 while 循环的等价转换`,
            tags: ['循环'],
        }
    ],
    programmingQuestions: [
        {
      id: 26,
      type: 'programming',
      samples: [
        { input: `3
aAc`, output: `-61` }
      ],
      question: `
# [GESP202403 三级] 字母求和

## 题目描述

小杨同学发明了一种新型密码，对于每一个小写英文字母，该小写字母代表了一个正整数，即该字母在字母顺序中的位置，例如字母 \`a\` 代表了正整数 $1$，字母 \`b\` 代表了正整数 $2$；对于每一个大写英文字母，该大写字母代表了一个负整数，即该字母的 ASCII 码的相反数，例如字母 \`A\` 代表了负整数 $-65$。小杨同学利用这种放缩对一个整数进行了加密并得到了一个由大写字母和小写字母组成的字符串，该字符串中每个字母所代表数字的总和即为加密前的整数，例如 \`aAc\` 对应的加密前的整数为 $1+(-65)+3=-61$。

对于给定的字符串，请你计算出它对应的加密前的整数是多少。

## 输入格式

第一行一个正整数 $n$，表示字符串中字母的个数。
第二行一个由大写字母和小写字母的字符串 \`T\`，代表加密后得到的字符串。

## 输出格式

输出一行一个整数，代表加密前的整数。
`,
      explanation: `
      **解析：**
      遍历字符串：若是小写字母，加上 c-\'a\'+1；若是大写字母，减去其 ASCII 码值。按题意直接模拟即可。
      `,
      template: '#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n\n    // 在此编写代码\n    return 0;\n}',
      score: 25,
      tags: ['编程题', '字符串', '模拟'],
      referenceCode: '#include <iostream>\n#include <assert.h>\n#include <cstdlib>\n#include <cstdio>\n#include <cstring>\nusing namespace std;\nconst int N = 100005;\nchar str[N];\nint main() {\n    int n;\n    cin >> n;\n    cin >> str;\n    assert(n == strlen(str));\n    int ans = 0;\n    for (int i = 0; i < n; i++) {\n        if (str[i] >= \'a\' && str[i] <= \'z\')\n            ans += str[i]-\'a\'+1;\n        else if (str[i] >= \'A\' && str[i] <= \'Z\')\n            ans -= str[i];\n        else\n            assert(false);\n    }\n    cout << ans << endl;\n    return 0;\n}',
      answer: '',
    },
        {
      id: 27,
      type: 'programming',
      samples: [
        { input: `5
1 4 3 3 5`, output: `3` }
      ],
      question: `
# [GESP202403 三级] 完全平方数

## 题目描述

小杨同学有一个包含 $n$ 个非负整数的序列 $A$，他想要知道其中有多少对下标组合 $\\langle i,j\\rangle$（$1 \\leq i < j \\leq n$），使得 $A_i + A_j$ 是完全平方数。

如果 $x$ 是完全平方数，则存在非负整数 $y$ 使得 $y \\times y = x$。

## 输入格式

第一行一个非负整数 $n$，表示非负整数个数。
第二入行包含 $n$ 个非负整数 $A_1, A_2, \\dots A_n$，表示序列 $A$ 包含的非负整数。

## 输出格式

输出一行一个整数表示答案。
`,
      explanation: `
      **解析：**
      直接双重循环枚举所有下标对，计算两数之和后判断其是否为完全平方数；三级数据范围下，$O(N^2)$ 的直接枚举即可通过。
      `,
      template: '#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n\n    // 在此编写代码\n    return 0;\n}',
      score: 25,
      tags: ['编程题', '枚举', '数学'],
      referenceCode: '#include <bits/stdc++.h>\nusing namespace std;\nconst int N = 1010;\nint a[N];\nint main(){\n    int n;\n    cin >> n;\n    for(int i = 1; i <= n; i++) cin >> a[i];\n    int ans = 0;\n    for(int i = 1; i <= n; i++){\n        for(int j = i+1; j <= n; j++){\n            int m = a[i]+a[j];\n            int t = sqrt(m+1e-7);\n            if(t * t == m) ans++;\n        }\n    }\n    cout << ans << "\\n";\n}',
      answer: '',
    }
    ]
};
