// 2025年3月 GESP C++ 三级真题 (第9次认证)
// 数据说明：本卷以官方真题 PDF 为主完成回填。
//-客观题 1~15：题面、选项与单选答案可由官方 PDF 文本层直接提取并整理。
//-判断题 16~25：题面来自官方 PDF；官方 PDF 文本层未完整带出答案表，当前答案依据官方题面+公开解析交叉复核填写。
//-编程题 26~27：题名、题意、样例与参考代码主体来自官方 PDF；其中个别数学符号/数据范围因 PDF 文本层缺字，按官方题意做等价整理。
export const paperData = {
    id: '2025-03-l3',
    title: '2025年3月 GESP C++ 三级真题',
    level: 3,
    year: 2025,
    month: 3,
    session: 9,
    timeLimit: 90 * 60,
    source: {
        officialPdf: 'https://gesp.ccf.org.cn/101/attach/1670868226801696.pdf',
        notes: '客观题 25 题已尽量补齐；其中判断题答案因官方 PDF 文本层未完整带出答案表，现依据官方题面与公开解析交叉复核。编程题 2 题已补题意、样例与参考代码；少量公式符号按官方题意等价整理。'
    },
    questions: [
        {
            id: 1,
            type: 'single',
            question: `Base64 编码将每 3 字节的输入数据编码为 4 字节的输出数据。如果输入数据长度不是 3 的倍数，会用 = 号填充。在 Base64 编码中，如果输入字符串的长度为 10 字节，编码后的字符串长度是多少（ ）`,
            options: ['12 字节', '13 字节', '14 字节', '16 字节'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (16 字节)**

**核心解析：**
1. Base64 编码的基本规则是：将每 3 字节的输入数据转换为 4 字节的输出数据。
2. 对于 10 字节的输入数据，按 3 字节为一组划分：10 = 3 + 3 + 3 + 1，共需要 4 个编码块。
3. 每个编码块无论输入是完整的 3 字节还是不足 3 字节的尾部（不足部分会填充 \`=\` 号），输出始终为 4 字节。
4. 因此编码后总长度 = 4 块 × 4 字节/块 = 16 字节。

**选项逐项分析：**
- **A (12 字节)**：❌ 错误。若按 10 ÷ 3 × 4 = 13.33... 取整得到 12 或 13 都不对。Base64 的块结构决定了输出必须是 4 的整数倍。
- **B (13 字节)**：❌ 错误。13 不是 4 的倍数，不符合 Base64 的块对齐规则。
- **C (14 字节)**：❌ 错误。同样不是 4 的倍数，不符合 Base64 的输出格式。
- **D (16 字节)**：✅ 正确。4 个完整块的输出，长度为 16 字节。

**易错提醒：**
- 容易误认为可以用 10 × (4/3) ≈ 13.33 取整得到 13 字节，但 Base64 编码必须按块处理，不足 3 字节的尾部也要占满一个 4 字节块。
- 注意填充符 \`=\` 不改变输出块的数量，它只是块内的填充内容。

**知识延伸：**
Base64 常用于在文本协议（如电子邮件、JSON、URL）中传输二进制数据，其 64 个可打印字符集为 A-Z、a-z、0-9、\`+\`、\`/\`。

**考点：** Base64 编码的块结构和长度计算规则`,
            tags: ['字符与ASCII', '字符串', '计算机基础'],
        },
        {
            id: 2,
            type: 'single',
            question: `UTF-8 编码规则如下：\n1 字节：0xxxxxxx\n2 字节：110xxxxx 10xxxxxx\n3 字节：1110xxxx 10xxxxxx 10xxxxxx\n4 字节：11110xxx 10xxxxxx 10xxxxxx 10xxxxxx\n以下哪个字节序列是合法的 UTF-8 编码（ ）`,
            options: ['0xC0 0x80', '0xF0 0x90 0x80 0x80', '0x80 0x80 0x80', '0xFF 0xFE 0xFD'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (0xF0 0x90 0x80 0x80)**

**核心解析：**
1. UTF-8 是变长编码，根据首字节的高位模式判断字符占用的字节数：1 字节以 \`0\` 开头，2 字节以 \`110\` 开头，3 字节以 \`1110\` 开头，4 字节以 \`11110\` 开头。
2. 后续字节（续字节）必须以 \`10\` 开头，用于承载编码点的剩余位。
3. 逐项检验各选项：
   - 0xF0 = 11110000，以 \`11110\` 开头，符合 4 字节 UTF-8 首字节格式。
   - 0x90 = 10010000，以 \`10\` 开头，是合法的续字节。
   - 0x80 = 10000000，以 \`10\` 开头，是合法的续字节。
   - 0x80 同上。四个字节整体符合 UTF-8 规范。

**选项逐项分析：**
- **A (0xC0 0x80)**：❌ 错误。\`0xC0 = 11000000\` 匹配 2 字节 UTF-8 首字节格式，但 \`0x80 = 10000000\` 是续字节。这个序列被称为"过长编码"（overlong encoding），它用更多字节表示一个本可以用更少字节表示的字符，违反 UTF-8 标准。
- **B (0xF0 0x90 0x80 0x80)**：✅ 正确。首字节 \`0xF0\` 表示 4 字节字符，后续三个字节均以 \`10\` 开头，格式完全合法。
- **C (0x80 0x80 0x80)**：❌ 错误。UTF-8 序列的首字节不能是续字节 \`10xxxxxx\`，\`0x80\` 是续字节，不能作为序列的开头。
- **D (0xFF 0xFE 0xFD)**：❌ 错误。\`0xFF\` (11111111) 和 \`0xFE\` (11111110) 在 UTF-8 编码体系中没有任何一种字符类型以此开头，因此它们不是合法的 UTF-8 首字节。

**易错提醒：**
- 2 字节 UTF-8 的首字节范围是 \`0xC0-0xDF\`，但 \`0xC0\` 和 \`0xC1\` (即 \`11000000\` 和 \`11000001\`) 虽然格式匹配，却是 RFC 3629 标准明确禁止的"过长编码"，用于表示本该用 1 字节的 ASCII 字符。
- 不能仅凭"格式看起来对"就判断合法，还需检查是否符合 Unicode 编码点范围。

**知识延伸：**
UTF-8 设计时向后兼容 ASCII（0x00-0x7F），单字节字符的最高位为 0。这一特性使得大量纯 ASCII 文本在 UTF-8 下与 ASCII 完全一致。

**考点：** UTF-8 编码的多字节表示规则与合法性判断`,
            tags: ['进制转换', '计算机基础'],
        },
        {
            id: 3,
            type: 'single',
            question: `在 8 位二进制原码表示中，八进制数 -5 的二进制形式是什么（ ）`,
            options: ['10000101', '11111010', '11111011', '00000101'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (10000101)**

**核心解析：**
1. 原码是计算机中表示有符号整数的早期方式，其格式为：最高位作为符号位（0 表示正，1 表示负），其余位表示数值的绝对值。
2. 对于题目要求的 8 位原码表示：
   - 符号位：\`-5\` 为负数，符号位应设为 1。
   - 数值位：5 的二进制绝对值是 \`0000101\`（注意原码的数值部分占 7 位，不含符号位）。
3. 拼接符号位和数值位：1 + 0000101 = 10000101。

**选项逐项分析：**
- **A (10000101)**：✅ 正确。符号位 1 表示负数，数值位 0000101 表示 5，符合 8 位原码的定义。
- **B (11111010)**：❌ 错误。这是 -5 的 8 位补码表示。计算过程：-5 的原码是 10000101，补码取反加一得 11111011... 不对，应该是：5 的反码 00000101，取反得 11111010，这是 -5 的反码或补码的中间步骤，但最终补码是 11111011。选项 B 实际是 -5 的反码 11111010。
- **C (11111011)**：❌ 错误。这是 -5 的 8 位补码表示。计算：原码 10000101 → 反码 11111010 → 补码 11111011。
- **D (00000101)**：❌ 错误。这是 +5 的原码表示，符号位为 0，表示正数，不符合题目要求的 -5。

**易错提醒：**
- 原码和补码的数值部分表示方式不同：原码的数值部分就是绝对值的二进制，补码的数值部分是通过"取反加一"规则得到的。
- 在原码中，+0 (00000000) 和 -0 (10000000) 有两种表示，这增加了电路设计的复杂性，也是后来采用补码的原因之一。

**知识延伸：**
8 位二进制可以表示的范围：原码为 -127 到 +127（其中 +0 和 -0 各占一个编码），而补码为 -128 到 +127（补码的 10000000 专用于表示 -128）。

**考点：** 二进制原码表示法、符号位规则`,
            tags: ['二进制', '补码', '进制转换'],
        },
        {
            id: 4,
            type: 'single',
            question: `十进制数 111.111 的二进制表示可以是下面的（ ）`,
            options: ['1101111.0001110001', '1101110.1001110001', '1101111.1001110001', '1101111.0011110001'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (1101111.0001110001)**

**核心解析：**
1. **整数部分转换**：十进制 111 转为二进制。
   - 111 ÷ 2 = 55 余 1
   - 55 ÷ 2 = 27 余 1
   - 27 ÷ 2 = 13 余 1
   - 13 ÷ 2 = 6 余 1
   - 6 ÷ 2 = 3 余 0
   - 3 ÷ 2 = 1 余 1
   - 1 ÷ 2 = 0 余 1
   - 逆序排列得 1101111（验证：64+32+8+4+2+1 = 111）。

2. **小数部分转换**：十进制 0.111 转为二进制，采用乘 2 取整法。
   - 0.111 × 2 = 0.222，整数部分为 0
   - 0.222 × 2 = 0.444，整数部分为 0
   - 0.444 × 2 = 0.888，整数部分为 0
   - 0.888 × 2 = 1.776，整数部分为 1
   - 0.776 × 2 = 1.552，整数部分为 1
   - 0.552 × 2 = 1.104，整数部分为 1
   - 0.104 × 2 = 0.208，整数部分为 0
   - 0.208 × 2 = 0.416，整数部分为 0
   - 0.416 × 2 = 0.832，整数部分为 0
   - 0.832 × 2 = 1.664，整数部分为 1
   - 取整数部分序列：0001110001...

3. **组合**：1101111.0001110001。

**选项逐项分析：**
- **A (1101111.0001110001)**：✅ 正确。整数部分 1101111 和小数部分前 10 位 0001110001 均与推导结果一致。
- **B (1101110.1001110001)**：❌ 错误。整数部分 1101110 = 110，不等于 111；小数部分首位是 1 而不是 0，数值差异明显。
- **C (1101111.1001110001)**：❌ 错误。整数部分正确，但小数部分首位应为 0（乘法前三次取整均为 0），此处为 1，与实际运算过程不符。
- **D (1101111.0011110001)**：❌ 错误。整数部分正确，但小数部分前三位应为 000 而不是 001，第二次乘 2 取整结果为 0，不是 1。

**易错提醒：**
- 小数乘 2 取整时，必须从原小数（如 0.111）开始计算，不能用累积结果的小数部分（如 0.222）作为下一轮乘法的起点而遗漏了整数位 0。
- 十进制小数的二进制转换可能无限进行，本题取 10 位小数是题目要求。

**知识延伸：**
并非所有十进制小数都能精确转换为二进制小数。例如 0.1、0.2 等十进制小数在二进制下是无限循环的，这导致浮点数精度问题的根源之一。

**考点：** 十进制小数转二进制的乘 2 取整法`,
            tags: ['二进制', '补码', '进制转换'],
        },
        {
            id: 5,
            type: 'single',
            question: `在 C++ 中，补码的主要作用是（ ）`,
            options: ['提高浮点数的精度', '简化整数的加减法运算', '增加整数的表示范围', '优化内存分配'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (简化整数的加减法运算)**

**核心解析：**
补码是现代计算机系统中表示有符号整数的标准方式。其核心设计目标是通过统一的编码规则，使加法和减法可以使用同一套硬件电路（加法器）来实现。具体而言：
- 一个负数的补码加上它的绝对值，结果会产生一个溢出的 0（模 2ⁿ 下等价于 0）。
- 因此，\`a - b\` 可以转化为 \`a + (b 的补码)\`，无需单独的减法电路。
- 补码还消除了原码中 +0 和 -0 的歧义（补码的 100...0 表示 -2ⁿ⁻¹，而非 -0）。

**选项逐项分析：**
- **A (提高浮点数的精度)**：❌ 错误。浮点数精度由尾数和指数的表示方式决定（IEEE 754 标准），与整数的补码表示无关。补码是针对整数的编码方式。
- **B (简化整数的加减法运算)**：✅ 正确。补码的核心价值在于统一了加减法运算。加法器只需实现二进制加法和溢出检测，即可同时完成加法和减法，极大简化了 ALU（算术逻辑单元）的硬件设计。
- **C (增加整数的表示范围)**：❌ 错误。补码并不增加可表示的数值个数。对于 n 位二进制，补码和原码能表示的整数个数相同（都是 2ⁿ 个），但补码的表示范围是 -2ⁿ⁻¹ 到 +2ⁿ⁻¹-1，比原码的 -2ⁿ⁻¹+1 到 +2ⁿ⁻¹-1 多表示一个数（-2ⁿ⁻¹）。这是补码的"副作用"，而非设计目标。
- **D (优化内存分配)**：❌ 错误。内存分配由操作系统和程序逻辑决定，与整数的编码方式（补码与否）无关。

**易错提醒：**
- 补码的提出确实"顺便"解决了 -0 的问题并扩展了负数下界（能表示 -128 而非 -127），但这些是补码相比原码的优势，而非补码被设计出来的主要原因。
- 区分"设计目标"和"附带效果"：简化运算是目标，增加表示范围是效果。

**知识延伸：**
除了补码，还有反码（ones' complement）和移码（biased representation）等有符号整数表示方法。反码仍存在 +0 和 -0 两种表示，移码常用于浮点数的指数部分。

**考点：** 补码的设计意图与运算优势`,
            tags: ['二进制', '补码', '计算机基础'],
        },
        {
            id: 6,
            type: 'single',
            question: `在 C++ 中，一个 8 位有符号整数（使用补码表示）的范围是（ ）`,
            options: ['-128 到 127', '-127 到 128', '-256 到 255', '0 到 255'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (-128 到 127)**

**核心解析：**
8位有符号整数的补码表示法将1位用于符号位，剩余7位用于表示数值。

逐位分析补码表示的范围：
1. **最小值 -128**：二进制 \`10000000\` 是补码表示的最小值。若按普通二进制解释是128，但补码规定 \`10000000\` 代表 -128。
2. **最大值 127**：二进制 \`01111111\` 代表最大正数，即 $2^7 - 1 = 127$。
3. **总数**：$127 - (-128) + 1 = 256 = 2^8$，恰好覆盖所有8位组合。

**选项逐项分析：**
- **A -128 到 127**：✅ 正确。符合8位补码表示法的标准范围。
- **B -127 到 128**：❌ 错误。该范围的总数量为256个，但无法用补码精确表示。
- **C -256 到 255**：❌ 错误。这是16位有符号整数的范围，8位无符号整数的范围是0到255。
- **D 0 到 255**：❌ 错误。这是有符号位为0时的无符号整数范围，而非有符号整数的范围。

**易错提醒：**
- 补码表示中，符号位为1时不能简单按二进制转换，要用“$2^n$ 减去最高位权重”的规则。
- 有符号整数最小负数的绝对值比最大正数大1，这是因为0占用了正数的一个名额。

**知识延伸：**
对于n位有符号整数，其范围是 $-2^{n-1}$ 到 $2^{n-1} - 1$。32位有符号整数的范围是 -2147483648 到 2147483647。

**考点：** 有符号整型（补码表示）的数值范围计算`,
            tags: ['二进制', '补码'],
        },
        {
            id: 7,
            type: 'single',
            question: `在 C++ 中，以下代码的输出是什么（ ）\n\`\`\`cpp\nint a = -5;\nunsigned int b = a;\ncout << b;\n\`\`\``,
            options: ['-5', '5', '4294967291', '编译错误'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (4294967291)**

**核心解析：**
本题涉及有符号整数到无符号整数的隐式类型转换，核心在于理解“位模式不变，解释方式改变”。

逐行推导：
1. \`int a = -5;\`：变量 a 在内存中以32位补码存储，\`-5\` 的补码为 \`0xFFFFFFFB\`（即二进制全1减去5）。
2. \`unsigned int b = a;\`：将 a 的值赋给无符号变量 b。此时内存中的位模式 \`0xFFFFFFFB\` 完全不变，但编译器将其解释为无符号数。
3. \`cout << b;\`：输出无符号整数 b 的值。\`0xFFFFFFFB\` 作为无符号数的数值为 $2^{32} - 5 = 4294967291$。

**选项逐项分析：**
- **A -5**：❌ 错误。有符号数赋值给无符号数后，其类型已变为 unsigned int，不会输出负号。
- **B 5**：❌ 错误。虽然绝对值保留为5，但转换并非简单的取绝对值操作。
- **C 4294967291**：✅ 正确。无符号32位整数的最大值 $2^{32} - 1 = 4294967295$ 减去5得到此结果。
- **D 编译错误**：❌ 错误。C++ 允许有符号整数隐式转换为无符号整数，不会产生编译错误。

**易错提醒：**
- 有符号到无符号的转换不会改变内存中的位组合，只改变解释方式。
- 当有符号负数转为无符号时，实际值 = $2^{32} + \\text{原负数}$，即 $4294967296 - 5 = 4294967291$。
- 本题假设为32位环境，若为16位则结果会是 65531。

**知识延伸：**
无符号整数运算中可能出现“回绕”（wraparound）现象。例如 \`unsigned int x = -1;\` 实际值为 $2^{32} - 1 = 4294967295$。

**考点：** 补码内存表示与有符号/无符号类型的强制转换机制`,
            tags: ['二进制', '补码', '位运算', '类型转换', '运算符', '未定义行为', '计算机基础'],
        },
        {
            id: 8,
            type: 'single',
            question: `下列程序的作用是（ ）\n\`\`\`cpp\nint main() {\n int decimal = 25;\n cout << oct << decimal;\n return 0;\n}\n\`\`\``,
            options: ['将十进制数转换成八进制数', '将八进制数转换成十进制数', '将二进制数转换成八进制数', '将八进制数转换成 16 进制数'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (将十进制数转换成八进制数)**

**核心解析：**
\`oct\` 是 C++ 标准库 \`<iostream>\` 提供的流操纵符（Stream Manipulator），用于设置输出流的进制格式。

代码执行步骤：
1. \`int decimal = 25;\`：定义整型变量 decimal，值为十进制数25。
2. \`cout << oct\`：设置 cout 的输出进制为八进制（注意：此操作仅改变后续输出的格式解释，不改变数据本身）。
3. \`cout << decimal;\`：以八进制形式输出 decimal 的值。25 的八进制表示为 \`31\`（因为 $25 = 3 \\times 8 + 1$）。

因此，程序的作用是将十进制数转换为八进制形式输出。

**选项逐项分析：**
- **A 将十进制数转换成八进制数**：✅ 正确。\`cout << oct\` 使得后续输出的整数以八进制形式显示。
- **B 将八进制数转换成十进制数**：❌ 错误。该选项描述的是读取八进制输入并转换为十进制输出，与代码功能相反。
- **C 将二进制数转换成八进制数**：❌ 错误。代码中 decimal 的初始值为十进制，题目未涉及二进制表示。
- **D 将八进制数转换成16进制数**：❌ 错误。八进制转十六进制需要使用 \`hex\` 操纵符，且该选项描述的功能与代码不符。

**易错提醒：**
- \`cout << oct\` 不会改变变量 decimal 本身的值，只改变输出的显示格式。
- 要恢复十进制输出，需使用 \`cout << dec\`。
- 八进制数在 C++ 源码中以 \`0\` 开头（如 \`025\` 表示八进制25），但 \`oct\` 操纵符影响的是输出而非输入。

**知识延伸：**
常用的进制输出操纵符包括：\`oct\`（八进制）、\`dec\`（十进制，默认值）、\`hex\`（十六进制）。C++ 未提供直接的二进制输出操纵符，需通过手动转换实现。

**考点：** C++ I/O 流操纵符（oct, hex, dec）的使用`,
            tags: ['二进制', '补码', '进制转换', '位运算'],
        },
        {
            id: 9,
            type: 'single',
            question: `下面程序是将十进制转十六进制，横线处应该填入的是（ ）\n\`\`\`cpp\n#include <iostream>\nusing namespace std;\nint main() {\n int decimal = 255;\n ________________________\n return 0;\n}\n\`\`\``,
            options: ['cout << oct << decimal;', 'cout << decimal << decimal;', 'cout << hex << decimal;', '不能正确执行'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (cout << hex << decimal;)**

**核心解析：**
本题要求将十进制数转换为十六进制输出。根据题干描述，横线处应填写设置十六进制输出格式的语句。

逐项分析各选项：
- \`oct\` 对应八进制输出，与题目要求的十六进制不符。
- \`decimal << decimal\` 是位左移操作，语法错误且无法输出。
- \`hex\` 对应十六进制输出，符合题目要求。
- \`cout << hex << decimal;\` 能正确执行，不会产生编译错误。

**选项逐项分析：**
- **A cout << oct << decimal;**：❌ 错误。\`oct\` 设置的是八进制输出格式，而非题目要求的十六进制。
- **B cout << decimal << decimal;**：❌ 错误。\`decimal\` 是整型变量名，重复书写 decimal 不会产生预期的格式化输出，且语法上会使两个 decimal 值连续输出（显示为"255255"）。
- **C cout << hex << decimal;**：✅ 正确。\`hex\` 是 C++ 流操纵符，设置输出格式为十六进制，使后续整数以十六进制形式显示。255 的十六进制表示为 \`ff\`。
- **D 不能正确执行**：❌ 错误。\`cout << hex << decimal;\` 是合法的 C++ 语句，能够正确编译和执行。

**易错提醒：**
- \`oct\`、\`dec\`、\`hex\` 只改变输出格式，不改变变量的值或类型。
- 使用 \`hex\` 后，输出的是十进制数25对应的十六进制表示，而非将字符串"255"转换为十六进制。

**知识延伸：**
设置进制格式后，该设置会持续有效，直到被其他进制操纵符改变。如果需要在某次输出后恢复十进制，应显式使用 \`cout << dec\`。

**考点：** C++ 输出格式控制（hex 操纵符）`,
            tags: ['进制转换', '位运算', '逻辑判断'],
        },
        {
            id: 10,
            type: 'single',
            question: `以下代码的说法正确的是什么（ ）\n\`\`\`cpp\n#include <iostream>\nusing namespace std;\nint main() {\n int a = 0b1101;\n int b = 0b1011;\n cout << (a ^ b);\n return 0;\n}\n\`\`\``,
            options: ['进行的是整体异或运算', '进行的是按位同或运算', '进行的是按位与运算', '进行的是按位异或运算'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (进行的是按位异或运算)**

**核心解析：**
本题代码使用二进制字面量定义两个整数，并通过 \`^\` 运算符进行运算。

逐行推导：
1. \`int a = 0b1101;\`：定义变量 a，其二进制值为 \`1101\`，即十进制 13。
2. \`int b = 0b1011;\`：定义变量 b，其二进制值为 \`1011\`，即十进制 11。
3. 按位异或（XOR）运算的规则：相同位得0，不同位得1。
   - 位1：$1 \\oplus 1 = 0$
   - 位2：$1 \\oplus 0 = 1$
   - 位3：$0 \\oplus 1 = 1$
   - 位4：$1 \\oplus 0 = 1$
   - 结果：$0110$（二进制），即十进制 6。

**选项逐项分析：**
- **A 进行的是整体异或运算**：❌ 错误。C++ 中不存在"整体异或"这一运算符或概念，异或运算必然是按位进行的。
- **B 进行的是按位同或运算**：❌ 错误。同或（XNOR）是异或的取反，即相同为1不同为0。C++ 中没有直接的同或运算符，需通过 \`~(a ^ b)\` 实现。
- **C 进行的是按位与运算**：❌ 错误。按位与是 \`&\` 运算符，而非 \`^\`。\`a & b\` 的结果是 \`1001\`（二进制9）。
- **D 进行的是按位异或运算**：✅ 正确。\`^\` 是 C++ 中的按位异或（Bitwise XOR）运算符。

**易错提醒：**
- \`^\` 在不同语言中含义不同：在 C++ 中是按位异或，在 Python 中可能是幂运算符。
- 不要混淆 \`^\`（异或）、\`&\`（与）、\`|\`（或）三个位运算符。

**知识延伸：**
按位异或的一个重要特性是：任何数与自身异或结果为0（$a \\oplus a = 0$），任何数与0异或结果为自身（$a \\oplus 0 = a$）。这使得异或在算法中常用于交换两数或查找唯一数。

**考点：** C++ 位运算符的符号含义辨析（^ 与 &、| 的区别）`,
            tags: ['位运算', '异或'],
        },
        {
            id: 11,
            type: 'single',
            question: `下面枚举法查找最大值索引程序中，横线处应该填写的是（ ）\n\`\`\`cpp\n#include <iostream>\nusing namespace std;\nint main() {\n int arr[] = {3, 7, 2, 9, 5};\n int maxIndex = 0;\n for (int i = 1; i < 5; i++) {\n ____________________________\n {\n maxIndex = i;\n }\n }\n cout << maxIndex;\n return 0;\n}\n\`\`\``,
            options: ['if (arr[maxIndex] > arr[i])', 'if (arr[i]-1 > arr[maxIndex])', 'if (arr[i]+1 > arr[maxIndex])', 'if (arr[i] > arr[maxIndex])'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (if (arr[i] > arr[maxIndex]))**

**核心解析：**
1. 初始时 \`maxIndex = 0\`，即认为 \`arr[0] = 3\` 是最大值。
2. 循环遍历 \`i = 1, 2, 3, 4\`，逐一比较 \`arr[i]\` 与当前最大值 \`arr[maxIndex]\`。
3. 当发现 \`arr[i]\` 比 \`arr[maxIndex]\` 更大时，更新 \`maxIndex = i\`。
4. 数组 \`{3, 7, 2, 9, 5}\` 中最大元素 \`9\` 位于下标 \`3\`，最终 \`maxIndex\` 应输出 \`3\`。

**选项逐项分析：**
- **A (if (arr[maxIndex] > arr[i]))**：❌ 错误。逻辑反了。该条件在"当前最大值大于 arr[i]"时成立，会导致不更新 maxIndex，无法找到真正的最大值。
- **B (if (arr[i]-1 > arr[maxIndex]))**：❌ 错误。\`arr[i]-1\` 与最大值判断无关，纯属无意义操作。
- **C (if (arr[i]+1 > arr[maxIndex]))**：❌ 错误。\`arr[i]+1\` 与最大值判断无关，纯属无意义操作。
- **D (if (arr[i] > arr[maxIndex]))**：✅ 正确。逐个比较发现更大元素时更新索引，符合枚举查找最大值的标准逻辑。

**易错提醒：**
- 容易误选 A，因为直观上觉得"最大值大于当前值就不动"是对的，但实际应该"当前值大于最大值才更新"。
- 枚举法查找最大值必须以当前发现的"更大值"为基准更新，不能反向比较。

**知识延伸：**
枚举法（线性查找）在 GESP 一级已学过基本框架，三级要求能在循环中维护"当前最优解"这一不变式。

**考点：** 枚举法查找最大值索引`,
            tags: ['位运算', '枚举', '算法思维', '循环'],
        },
        {
            id: 12,
            type: 'single',
            question: `以下代码的功能是将数组中的奇数和偶数分别放在数组的前半部分和后半部分，横线处应该填入的是（ ）\n\`\`\`cpp\nint arr[] = {1, 2, 3, 4, 5};\nint left = 0, right = 4;\nwhile (left < right) {\n while (arr[left] % 2 == 1 && left < right) left++;\n ________________________________\n if (left < right) {\n swap(arr[left], arr[right]);\n }\n}\nfor (int i = 0; i < 5; i++) {\n cout << arr[i] << " ";\n}\n\`\`\``,
            options: ['while (arr[left] % 2 == 0 && left < right) right--;', 'while (arr[right] % 2 == 0 && left < right) left--;', 'while (arr[right] % 2 != 0 && left < right) right--;', 'while (arr[right] % 2 == 0 && left < right) right--;'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (while (arr[right] % 2 == 0 && left < right) right--;)**

**核心解析：**
1. 双指针划分奇偶的核心思想：左指针 \`left\` 找偶数，右指针 \`right\` 找奇数，两者相遇时停止。
2. 代码已经写了 \`while (arr[left] % 2 == 1 && left < right) left++;\`，这行的作用是让 \`left\` 跳过奇数（停在偶数或交叉处）。
3. 横线处需要对称地从右侧找奇数：右指针应该跳过（向左移动）已经确认在"后半部分"的偶数，直到找到一个奇数为止。
4. \`arr[right] % 2 == 0\` 表示 \`right\` 指向偶数，确实应该在后半部分，直接 \`right--\` 跳过它。
5. 当 \`left < right\` 且 \`arr[right]\` 为奇数时，两指针停下，准备执行交换。

**选项逐项分析：**
- **A (while (arr[left] % 2 == 0 && left < right) right--;)**：❌ 错误。对左指针 \`left\` 操作的判断，但用了 \`right--\`，且 \`left\` 此时应该右移而非左移，逻辑混乱。
- **B (while (arr[right] % 2 == 0 && left < right) left--;)**：❌ 错误。右指针应左移（\`right--\`），不是 \`left--\`；且条件写的是偶数却用 \`left--\`，完全错误。
- **C (while (arr[right] % 2 != 0 && left < right) right--;)**：❌ 错误。\`arr[right] % 2 != 0\` 表示奇数，此时不应跳过（奇数是需要交换到前面的），而是应该停止等待交换。
- **D (while (arr[right] % 2 == 0 && left < right) right--;)**：✅ 正确。右指针跳过偶数（\`right--\`），直到找到奇数或两指针交叉，完美配合左侧找偶数、右侧找奇数的双指针框架。

**易错提醒：**
- 容易混淆指针移动方向：左指针找偶数时右移（\`left++\`），右指针找奇数时左移（\`right--\`）。
- 不能跳过奇数，奇数是待交换的目标；偶数才是"已经在正确位置"的。

**知识延伸：**
双指针划分算法（如荷兰国旗问题、快慢指针去重）是 GESP 三级高频考点，关键在于维护循环不变式：指针移动不能破坏"已处理区域的性质"。

**考点：** 双指针划分算法`,
            tags: ['数组', '循环', '逻辑判断'],
        },
        {
            id: 13,
            type: 'single',
            question: `下面程序最后能够得到 HelloC++ 的是（ ）\n\`\`\`cpp\nint main() {\n string str = "HelloWorld";\n ___________________\n cout << str;\n return 0;\n}\n\`\`\``,
            options: ['str.replace(0, 5, "C++");', 'str.replace(5, 5, "C++");', 'str.replace(1, 5, "C++");', 'str.replace(4, 5, "C++");'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (str.replace(5, 5, "C++");)**

**核心解析：**
1. 原字符串 \`str = "HelloWorld"\`。
2. 下标对应关系：\`H(0) e(1) l(2) l(3) o(4) W(5) o(6) r(7) l(8) d(9)\`。
3. \`str.replace(pos, len, str2)\` 的作用是从下标 \`pos\` 开始，删除 \`len\` 个字符，然后在该位置插入 \`str2\`。
4. 选项 B：\`str.replace(5, 5, "C++")\` 表示从下标 5 开始删除 5 个字符（即删除 \`"World"\`），然后插入 \`"C++"\`。
5. 结果：\`"Hello" + "C++" = "HelloC++"\`，正好符合要求。

**选项逐项分析：**
- **A (str.replace(0, 5, "C++");)**：❌ 错误。从下标 0 删除 5 个字符会删除 \`"Hello"\`，结果变成 \`"C++World"\`，与目标不符。
- **B (str.replace(5, 5, "C++");)**：✅ 正确。精确替换 \`"World"\` 为 \`"C++"\`。
- **C (str.replace(1, 5, "C++");)**：❌ 错误。从下标 1 删除 5 个字符会删除 \`"elloW"\`，结果变成 \`"HC++rld"\`，与目标不符。
- **D (str.replace(4, 5, "C++");)**：❌ 错误。从下标 4 删除 5 个字符会删除 \`"oWorl"\`，结果变成 \`"HellC++d"\`，与目标不符。

**易错提醒：**
- \`replace\` 的第一个参数是起始位置，第二个参数是长度，不能混淆。
- 必须准确数清楚下标位置，建议在草稿纸上写出每个字符对应的下标。

**知识延伸：**
\`string\` 类的 \`replace\`、\`substr\`、\`find\` 等方法联合使用是 GESP 三级字符串处理的常见题型，需要熟练掌握每个方法的下标规则。

**考点：** string 类的 replace() 方法`,
            tags: ['位运算', '逻辑判断'],
        },
        {
            id: 14,
            type: 'single',
            question: `想要得到字符串 World，下面程序横线处应该填入的是（ ）\n\`\`\`cpp\n#include <iostream>\n#include <string>\nusing namespace std;\nint main() {\n string str = "HelloC++";\n _________________\n _________________\n return 0;\n}\n\`\`\``,
            options: ['str.insert(4, "World");\ncout << str.substr(4, 4);', 'cout << str.substr(5, 5);', 'str.insert("World");\ncout << str.substr(5, 5);', 'str.insert(5, "World");\ncout << str.substr(5, 5);'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (str.insert(5, "World"); cout << str.substr(5, 5);)**

**核心解析：**
1. 原字符串 \`str = "HelloC++"\`。下标：\`H(0) e(1) l(2) l(3) o(4) C(5) +(6) +(7)\`。
2. 目标输出 \`"World"\`。
3. 选项 D 的两步操作：
   - 第一步：\`str.insert(5, "World")\` 在下标 5 处（即 \`C\` 的位置）插入 \`"World"\`，字符串变为 \`"HelloWorldC++"\`。
   - 第二步：\`str.substr(5, 5)\` 从下标 5 开始截取 5 个字符，结果为 \`"World"\`。
4. 两步配合精确得到目标输出。

**选项逐项分析：**
- **A (str.insert(4, "World"); cout << str.substr(4, 4);)**：❌ 错误。insert 在下标 4 插入会得到 \`"HellWorldoC++"\`，substr(4,4) 截取的是 \`"Worl"\`，不对。
- **B (cout << str.substr(5, 5);)**：❌ 错误。原始字符串 \`"HelloC++"\` 下标 5-9 是 \`"C++"\` 共3个字符，substr(5,5) 会取到越界的部分，结果不是 \`"World"\`。
- **C (str.insert("World"); cout << str.substr(5, 5);)**：❌ 错误。\`insert\` 方法必须有两个参数（位置和字符串），单参数 \`insert("World")\` 会报错或产生意外行为，编译不通过。
- **D (str.insert(5, "World"); cout << str.substr(5, 5);)**：✅ 正确。插入后截取，精确得到 \`"World"\`。

**易错提醒：**
- \`insert\` 必须指定插入位置（第一个参数），不能只传字符串。
- \`substr\` 的第二个参数是长度，不是结束下标，容易混淆。

**知识延伸：**
字符串操作中，插入 + 截取的组合是 GESP 三级高频题型，也可以用拼接和 replace 等方式实现，需理解每种方法的副作用（是否修改原字符串）。

**考点：** string 类的 insert() 与 substr() 联合应用`,
            tags: ['位运算', '字符串', 'substr', '逻辑判断'],
        },
        {
            id: 15,
            type: 'single',
            question: `有 n 个正整数，假设一个正整数是美丽数字当且仅当该正整数是 9 的倍数但不是 8 的倍数。下面的程序是编写计算 n 个正整数中美丽数字的数量，横线处应该填入的是（ ）\n\`\`\`cpp\nfor (int i = 1; i <= n; i++) {\n cin >> a;\n ________________________\n cnt++;\n}\n\`\`\``,
            options: ['if (a % 9 != 0 && a % 8 != 0)', 'if (a % 9 == 0 & a % 8 == 0)', 'if (a % 9 == 0 && a % 8 != 0)', 'if (a % 9 == 0 & a % 8 != 0)'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (if (a % 9 == 0 && a % 8 != 0))**

**核心解析：**
1. 题目定义"美丽数字"：该正整数**是 9 的倍数但不是 8 的倍数**。
2. "是 9 的倍数"用取模表示为 \`a % 9 == 0\`。
3. "不是 8 的倍数"用取模表示为 \`a % 8 != 0\`（余数不为 0 说明不是倍数）。
4. 两个条件必须同时满足，使用逻辑与运算符 \`&&\` 连接：\`a % 9 == 0 && a % 8 != 0\`。
5. 满足条件时执行 \`cnt++\`，统计美丽数字个数。

**选项逐项分析：**
- **A (if (a % 9 != 0 && a % 8 != 0))**：❌ 错误。该条件表示"既不是 9 的倍数又不是 8 的倍数"，与"是 9 的倍数但不是 8 的倍数"完全相反。
- **B (if (a % 9 == 0 & a % 8 == 0))**：❌ 错误。使用单 \`&\` 按位与运算符（虽然该上下文结果相同但不规范）；且逻辑是"既是 9 的倍数又是 8 的倍数"，与题目要求不符。
- **C (if (a % 9 == 0 && a % 8 != 0))**：✅ 正确。\`&&\` 逻辑与连接两个条件：满足 9 的倍数且不满足 8 的倍数。
- **D (if (a % 9 == 0 & a % 8 != 0))**：❌ 错误。该项本身符合规则，因此不是本题要选的项。使用单 \`&\` 按位与运算符（虽然结果正确但不符合规范习惯），且与 C 选项相比不够规范。

**易错提醒：**
- 区分"且"(\`&&\`)和"或"(\`||\`)：两个条件都要满足用 \`&&\`，满足其一即可用 \`||\`。
- "不是 8 的倍数"是 \`!= 0\`，容易误写成 \`== 0\`（变成"是 8 的倍数"）。
- 单 \`&\` 与 \`&&\` 在逻辑判断中有细微差别（C++ 中 \`&&\` 具有短路特性），应使用 \`&&\`。

**知识延伸：**
复合逻辑条件是 GESP 一级至三级反复出现的考点，需区分：
- \`a % 9 == 0 && a % 8 != 0\`：9 的倍数且非 8 的倍数
- \`a % 9 == 0 || a % 8 == 0\`：9 的倍数或 8 的倍数（或两者）

**考点：** 逻辑复合条件判定`,
            tags: ['位运算', '循环', '逻辑判断'],
        },
        {
            id: 16,
            type: 'judge',
            question: `判断一个三角形是否成立的条件只有：任意两边长度之和大于第三条边的长度。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
三角形成立的完整判断条件并非仅有"任意两边之和大于第三边"这一条。具体来说，还必须满足：

1. **边长为正数**：三条边长都必须大于 0。若边长为 0 或负数，即使满足两边之和大于第三边的形式，也不构成合法三角形。
2. **三角形不等式**：题目所述的"任意两边之和大于第三边"确实是核心条件，但若边长非正，该条件形同虚设。

以反例说明：若三条边为 0、1、2，则满足 0+1>2（不成立），但更根本的问题在于边长 0 本身就不是合法边长。再如边长为 -1、2、3，虽然 -1+2>3 不成立，但"-1 不是合法边长"这一前提已经被违反。

因此，完整的三角形判定需同时满足"边长为正"与"任意两边之和大于第三边"两个条件。题目用"只有"一词排除了边长为正这一必要前提，故该说法错误。

**易错提醒：**
- 考生容易忽略"边长为正"这一隐含前提，仅记住"两边之和大于第三边"
- 误以为三角形不等式是唯一条件，忽视了边长有效性的约束

**知识延伸：**
三角形不等式是平面几何的基础知识，其完整表述为：对于三条边 a、b、c，必须同时满足 a+b>c、a+c>b、b+c>a，且 a、b、c>0。在 GESP 三级考试中，常结合数组下标合法性、多边形顶点坐标等场景考查边界条件判断。

**考点：** 三角形不等式、逻辑条件的完整性`,
            tags: ['数学'],
        },
        {
            id: 17,
            type: 'judge',
            question: `这段程序进行的是判断一个从键盘输入的字符的 ASCII 是否是奇数，若是，输出 YES，否则，输出 NO。\n\`\`\`cpp\nint main() {\n char x;\n scanf("%c", &x);\n int ASCII = (int)x;\n cout << (x & 1 ? "YES" : "NO") << "\\n";\n return 0;\n}\n\`\`\``,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
逐行分析程序逻辑：

\`\`\`cpp
int main() {
    char x;
    scanf("%c", &x);              // 从键盘读取一个字符
    int ASCII = (int)x;           // 将字符强制转换为int（实际未使用）
    cout << (x & 1 ? "YES" : "NO") << "\\n";  // 判断最低位
    return 0;
}
\`\`\`

核心在于表达式 \`x & 1\`：
- 在 C++ 中，\`char\` 类型参与位运算时会自动提升为 \`int\`（整型提升）
- \`x\` 的值即该字符的 ASCII 码值
- \`x & 1\` 表示将 ASCII 值与二进制 \`00000001\` 进行按位与运算
- 运算结果取决于 ASCII 值的最低位：若最低位为 1，结果为 1（真）；若最低位为 0，结果为 0（假）
- ASCII 值为奇数 ⇔ 二进制最低位为 1 ⇔ \`x & 1\` 结果为真

因此，\`x & 1 ? "YES" : "NO"\` 的语义正是："ASCII 值是奇数吗？若是输出 YES，否则输出 NO"。程序实现与题目要求完全一致。

**易错提醒：**
- 考生可能疑惑为何不用 \`ASCII & 1\`（其实 \`ASCII\` 变量虽声明但未使用，直接用 \`x\` 即可）
- 注意 \`x & 1\` 判断的是二进制最低位，与"是否为奇数"的数学定义等价

**知识延伸：**
判断一个整数是否为奇数有两种常用方法：\`(n % 2 == 1)\`（取模法）或 \`(n & 1)\`（位运算法）。后者利用了奇数二进制最低位必为 1 的特性，效率更高，在嵌入式开发和底层编程中广泛应用。

**考点：** 位运算、字符与ASCII、整型提升`,
            tags: ['位运算', '字符与ASCII'],
        },
        {
            id: 18,
            type: 'judge',
            question: `闰年的定义：普通闰年是公历年份是 4 的倍数且不是 100 的倍数；世纪闰年必须是 400 的倍数。下面程序是判断是否是闰年的正确程序。\ncin >> n;\n\`\`\`cpp\ncout << ((n % 4 == 0 && n % 100 != 0) || (n % 400 == 0)) ? 1 : 0;\nreturn 0;\n\`\`\``,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
分析题目给出的闰年定义与代码逻辑。

**闰年定义：**
- 普通闰年：公历年份是 4 的倍数 **且不是** 100 的倍数
- 世纪闰年：**必须**是 400 的倍数

**代码表达式：**
\`\`\`cpp
cout << ((n % 4 == 0 && n % 100 != 0) || (n % 400 == 0)) ? 1 : 0;
\`\`\`

**逻辑拆解：**
1. \`n % 4 == 0 && n % 100 != 0\`：年份能被 4 整除且不能被 100 整除 → 覆盖普通闰年
2. \`n % 400 == 0\`：年份能被 400 整除 → 覆盖世纪闰年
3. 两者用 \`||\`（逻辑或）连接，表示满足任一条件即为闰年

**验证示例：**
| 年份 | n%4 | n%100 | n%400 | 表达式结果 | 实际类型 |
|------|-----|-------|-------|------------|----------|
| 2024 | 0   | 24≠0  | —     | 真         | 普通闰年 |
| 2000 | 0   | 0     | 0     | 真         | 世纪闰年 |
| 1900 | 0   | 0     | 100≠0 | 假         | 平年     |
| 2023 | 3≠0 | —     | —     | 假         | 平年     |

表达式与定义完全对应，故程序正确。

**易错提醒：**
- 世纪闰年的判断必须单独列出 \`n % 400 == 0\`，不能遗漏
- 不能写成 \`n % 400 == 0 || n % 4 == 0\`（这会将 1900 错误判定为闰年）

**知识延伸：**
本题的逻辑表达式体现了"且"与"或"的组合应用：(A && B) || C。其中 A=\`n%4==0\`，B=\`n%100!=0\`，C=\`n%400==0\`。在 GESP 后续级别中，还会涉及更复杂的多条件组合，如日期合法性判断、多边形顶点坐标有效性校验等。

**考点：** 闰年判断、逻辑表达式、&&与||的组合`,
            tags: ['位运算', '逻辑判断'],
        },
        {
            id: 19,
            type: 'judge',
            question: `C++ 语句 cout << (n % 15 == 0 ? "YES" : "NO"); 能够判断一个整数能否被 3 和 5 同时整除。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
分析代码逻辑与题目要求。

**代码：**
\`\`\`cpp
cout << (n % 15 == 0 ? "YES" : "NO");
\`\`\`

**数学原理：**
判断一个整数 n 能否被 3 和 5 同时整除，即判断 n 是否同时满足：
- n % 3 == 0
- n % 5 == 0

由于 3 和 5 的最小公倍数（LCM）为 15，因此：
- 若 n 能同时被 3 和 5 整除 ⇒ n 是 3 和 5 的公倍数 ⇒ n 是 15 的倍数 ⇒ n % 15 == 0
- 若 n % 15 == 0 ⇒ n 是 15 的倍数 ⇒ n 既是 3 的倍数也是 5 的倍数 ⇒ n 能同时被 3 和 5 整除

**充要性证明：**
n % 15 == 0 是 n % 3 == 0 且 n % 5 == 0 的**充分必要条件**。这是因为 15 = 3 × 5，且 3 和 5 互质。

**验证示例：**
| n   | n%3 | n%5 | n%15 | 同时被3和5整除？ |
|-----|-----|-----|------|------------------|
| 30  | 0   | 0   | 0    | ✅              |
| 45  | 0   | 0   | 0    | ✅              |
| 60  | 0   | 0   | 0    | ✅              |
| 10  | 1   | 0   | 10   | ❌              |
| 9   | 0   | 4   | 9    | ❌              |

表达式 \`n % 15 == 0\` 与"能同时被 3 和 5 整除"完全等价，程序正确。

**易错提醒：**
- 此结论成立的前提是 3 和 5 互质（最大公约数为 1）
- 若判断"能否被 4 和 6 同时整除"，不能简单用 \`n % 24 == 0\`，因为 4 和 6 的最小公倍数是 12，而非 24

**知识延伸：**
当判断多个整数能否同时整除某数时，可利用最小公倍数（LCM）简化。若 a 和 b 互质，则"a 的倍数且 b 的倍数"等价于"a×b 的倍数"。GESP 中常考 3 和 5、4 和 6、3 和 7 等组合的整除判断。

**考点：** 整除判断、最小公倍数、条件表达式`,
            tags: ['位运算'],
        },
        {
            id: 20,
            type: 'judge',
            question: `有 n 个同学，从中抽取任意个人数来参加学校组织的大合唱，共有 2 的 n 次幂个方法。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
分析"从 n 个同学中抽取任意人数参加大合唱"这一组合计数问题。

**问题本质：**
从 n 个不同元素（同学）中选取任意子集（包括空集和全集），求不同选法总数。

**计数方法：**
对每个同学独立做出"选"或"不选"的决策，共两个选项。由于 n 个同学的决策相互独立，根据乘法原理：

总选法数 = 2 × 2 × 2 × ... × 2 （共 n 个 2 相乘）= 2ⁿ

**枚举验证（以 n=3 为例）：**
| 选中的同学 | 选法编号 |
|------------|----------|
| 空集（都不选）| 1 |
| {A} | 2 |
| {B} | 3 |
| {C} | 4 |
| {A, B} | 5 |
| {A, C} | 6 |
| {B, C} | 7 |
| {A, B, C} | 8 |

共 8 = 2³ 种选法，符合 2ⁿ。

**"任意个人数"的理解：**
"任意个人数"指从 0 人到 n 人的所有可能，即全部子集。题目表述中的"抽取任意个人数"包含了"抽取 0 人"（即都不参加）的合法情形。

因此，从 n 个同学中抽取任意人数参加合唱的方法数确实为 2ⁿ。

**易错提醒：**
- 部分考生可能忽略"空集"也是一种合法选法（0 人参加合唱）
- 注意区分"从 n 人中选 k 人"（C(n,k)）与"从 n 人中选任意人"（2ⁿ）的区别

**知识延伸：**
集合 {a₁, a₂, ..., aₙ} 的子集总数恒为 2ⁿ，这是组合数学的基础结论。在编程中，可借助二进制位运算枚举子集：遍历 0 到 2ⁿ-1，其中第 i 位的 0/1 表示第 i 个元素是否被选中。这一技巧常用于状态压缩动态规划。

**考点：** 组合计数、子集、乘法原理`,
            tags: ['数学'],
        },
        {
            id: 21,
            type: 'judge',
            question: `若将一个正整数化为二进制数，在此二进制数中，我们将数字 1 的个数是偶数的这类二进制数称为 A 类数，否则就称其为 B 类数。判断 (2025)10 化为二进制后，1 的个数为偶数个，因此 2025 为 A 类数。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
将十进制数2025转换为二进制：
2025 ÷ 2 = 1012 余 1
1012 ÷ 2 = 506 余 0
506 ÷ 2 = 253 余 0
253 ÷ 2 = 126 余 1
126 ÷ 2 = 63 余 0
63 ÷ 2 = 31 余 1
31 ÷ 2 = 15 余 1
15 ÷ 2 = 7 余 1
7 ÷ 2 = 3 余 1
3 ÷ 2 = 1 余 1
1 ÷ 2 = 0 余 1

将余数从下往上读，得到 2025₁₀ = 11111101001₂。

逐个数一遍：1的个数为 1+1+1+1+1+1+1+1 = 8个。

8是偶数，因此2025的二进制表示中1的个数为偶数，符合题目定义的"A类数"。

**易错提醒：**
- 进制转换时容易遗漏最后商为1时的余数，需确保一直除到商为0为止
- 读余数时要按从下往上的顺序，即后得到的余数作为高位

**知识延伸：**
十进制转二进制是GESP三级的核心考点，该方法（除2取余法）适用于将十进制整数转换为任意进制，只需将除数改为目标进制基数即可。

**考点：** 进制转换、二进制中1的个数统计`,
            tags: ['二进制', '补码'],
        },
        {
            id: 22,
            type: 'judge',
            question: `该段程序将 n 不停地除以 2，并输出此时的商和余数，直到 n = 0 为止。\nlong long n;\ncin >> n;\n\`\`\`cpp\nwhile (n != 0) {\n cout << n / 2 << " " << n % 2 << "\\n";\n n /= 2;\n}\n\`\`\``,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
逐行分析程序逻辑：

\`\`\`cpp
long long n;
cin >> n;
\`\`\`
声明一个长整型变量n并从键盘输入。

\`\`\`cpp
while (n != 0) {
    cout << n / 2 << " " << n % 2 << "\\n";
    n /= 2;
}
\`\`\`
循环条件为 n != 0，当n变为0时循环终止。每次循环：
1. 输出 n / 2（商）和 n % 2（余数）
2. 将 n 除以2（n /= 2）

该程序完整模拟了十进制转二进制的"除2取余"过程，与题目描述"将n不停地除以2，并输出此时的商和余数，直到n=0为止"完全一致。

**易错提醒：**
- 注意区分 n / 2（商）和 n % 2（余数）各自的含义
- n /= 2 会修改n的值，若遗漏此语句将导致死循环

**知识延伸：**
该程序本质上是将十进制整数转换为二进制表示的标准算法，与第21题的转换方法原理相同，只是这里同时输出了每一步的商和余数，便于理解进制转换过程。

**考点：** 循环结构、整除与取模运算、进制的概念`,
            tags: ['位运算', '循环'],
        },
        {
            id: 23,
            type: 'judge',
            question: `两个 13 进制的数 A 和 B，在 10 进制下分别表示 10 和 11。 (A+B)13 = (18)13，也就是说 13 进制数 A 加上 13 进制数 B，和是 13 进制数 18。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
设两个13进制数分别为A和B。

根据题目条件：
- A在十进制下表示10，即 A = 10₁₀
- B在十进制下表示11，即 B = 11₁₀

验证A和B确实是合法的13进制数：
- 十进制10转换为13进制：10 < 13，所以A = (10)₁₃ ✓
- 十进制11转换为13进制：11 < 13，所以B = (11)₁₃ ✓

计算A + B的十进制值：
A + B = 10 + 11 = 21

将21转换为13进制：
21 ÷ 13 = 1 余 8
1 ÷ 13 = 0 余 1
所以 21₁₀ = (18)₁₃

因此 (A + B)₁₃ = 18₁₃，原命题成立。

**易错提醒：**
- 要确认题目中"A和B是13进制数"与"它们在10进制下分别表示10和11"这两个条件同时满足
- 验证进制数合法性时，需确保每位数字小于进制基数（13进制每位数字需小于13）

**知识延伸：**
本题综合考察了进制的双向转换：从十进制到指定进制（A=10、B=11），以及从十进制到指定进制（求A+B的结果）。这是GESP三级数制模块的典型考法。

**考点：** 进制的概念与转换、加法运算在不同进制下的表现`,
            tags: ['进制转换'],
        },
        {
            id: 24,
            type: 'judge',
            question: `k 进制，逢 k 进第二位，逢 k² 进百位，逢 k³ 进千位。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
理解k进制中各位的位权定义：

- 最低位（第0位，个位）：权值为 k⁰ = 1，逢k进一位
- 第1位：权值为 k¹ = k，逢k²进一位（进到百位需要k个k）
- 第2位：权值为 k²，逢k³进一位（进到千位）
- 第3位：权值为 k³，逢k⁴进一位

用具体例子验证（以十进制k=10为例）：
- 99 + 1 = 100：个位从9变0后进1，十位从9变0后再进1，所以是"逢10²进百位" ✓
- 999 + 1 = 1000：同理，逢10³进千位 ✓

对于任意k进制，该规则依然成立：
- 某位从k-1加1后变为0，并向高位进1
- 高位的1表示低位的k个单位，即 k¹ = k
- 因此"逢k进第二位"（第1位，即k位）

**易错提醒：**
- "逢k²进百位"中的"百位"特指k²位，不是十进制的百位
- 这里的"百位""千位"是位置名称，对应 k² 位、k³ 位

**知识延伸：**
位权是进制表示法的核心概念。任意k进制数的值等于各位数字乘以对应位权之和：数值 = Σ(各位数字 × k^(位序号))。理解位权是掌握进制转换的基础。

**考点：** 进制的位权概念、进位规则`,
            tags: ['进制转换'],
        },
        {
            id: 25,
            type: 'judge',
            question: `CCF（十九进制）= 21AC（十三进制）（不区分大小写）。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
分别计算两个数在不同进制下的十进制值：

**CCF₁₉ 的计算：**
- C 在十六进制/高进制中代表 12（需注意不能直接套用十六进制的C=12，此处是19进制，同样合法）
- F 在19进制中代表 15（19进制允许的数字是 0-18，其中10=A, 11=B, ..., 18=I）
- CCF₁₉ = C×19² + C×19¹ + F×19⁰
- CCF₁₉ = 12×361 + 12×19 + 15×1
- CCF₁₉ = 4332 + 228 + 15 = 4575

**21AC₁₃ 的计算：**
- 2、1 是合法的13进制数字
- A 在13进制中代表 10（13进制允许 0-12）
- C 在13进制中代表 12
- 21AC₁₃ = 2×13³ + 1×13² + A×13¹ + C×13⁰
- 21AC₁₃ = 2×2197 + 1×169 + 10×13 + 12×1
- 21AC₁₃ = 4394 + 169 + 130 + 12 = 4705

**比较结果：**
4575 ≠ 4705，因此 CCF₁₉ ≠ 21AC₁₃，原命题错误。

**易错提醒：**
- 不同进制下同一字母可能代表不同数值：19进制中最大数字是18（用I表示），而13进制中最大数字是12（用C表示）
- 不能混淆不同进制的字母表示规则

**知识延伸：**
本题涉及GESP常见的"数字字母"考点。在高于十进制的进制中，10-15通常用字母A-F表示，但具体含义取决于进制基数。做题时必须先明确当前进制下允许的最大数字，再确定字母对应的数值。

**考点：** 数制转换、不同进制中字母表示数字的规则、字符与数值的对应`,
            tags: ['进制转换', '字符与ASCII'],
        }
    ],
    programmingQuestions: [
        {
      id: 26,
      type: 'programming',
      samples: [
        { input: `1025`, output: `1000` }
      ],
      question: `
# [GESP202503 三级] 2025

## 题目描述

小 A 有一个整数 $x$，他想找到最小的正整数 $y$ 使得下式成立：

$(x \\ \\operatorname{and} \\ y) + (x \\ \\operatorname{or} \\ y) = 2025$

其中 $\\operatorname{and}$ 表示二进制按位与运算，$\\operatorname{or}$ 表示二进制按位或运算。如果不存在满足条件的 $y$，则输出 $-1$。

## 输入格式

一行，一个整数 $x$。

## 输出格式

一行，一个整数，若满足条件的 $y$ 存在则输出 $y$，否则输出 $-1$。
`,
      explanation: `
      **解析：**
      由按位恒等式 (x & y)+(x | y) = x+y，可知只要 2025-x > 0，答案就是 2025-x；否则不存在正整数解。官方 PDF 参考程序使用 1..2025 暴力枚举，但可直接化简。
      `,
      template: '#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n\n    // 在此编写代码\n    return 0;\n}',
      score: 25,
      tags: ['编程题', '位运算', '数学'],
      referenceCode: '#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n\n    long long x;\n    cin >> x;\n    long long y = 2025-x;\n    if (y > 0) cout << y << "\\n";\n    else cout << -1 << "\\n";\n    return 0;\n}',
      answer: '',
    },
        {
      id: 27,
      type: 'programming',
      samples: [
        { input: `6
Apple
banana
apple
Orange
banana
apple`, output: `apple` }
      ],
      question: `
# [GESP202503 三级] 词频统计

## 题目描述

在文本处理中，统计单词出现的频率是一个常见的任务。现在，给定 $n$ 个单词，你需要找出其中出现次数最多的单词。在本题中，忽略单词中字母的大小写（即 \`Apple\`、\`apple\`、\`APPLE\`、\`aPPle\` 等均视为同一个单词）。

请你编写一个程序，输入 $n$ 个单词，输出其中出现次数最多的单词。

## 输入格式

第一行，一个整数 $n$，表示单词的个数；

接下来 $n$ 行，每行包含一个单词，单词由大小写英文字母组成。

输入保证，出现次数最多的单词只会有一个。

## 输出格式

输出一行，包含出现次数最多的单词（输出单词为小写形式）。
`,
      explanation: `
      **解析：**
      把每个单词统一转为小写后，用 map / unordered_map 统计出现次数，最后输出频次最大的那个单词即可。
      `,
      template: '#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n\n    // 在此编写代码\n    return 0;\n}',
      score: 25,
      tags: ['编程题', '字符串', '哈希表'],
      referenceCode: '#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    int n;\n    cin >> n;\n    map<string, int> cnt;\n    int best = -1;\n    string ans;\n\n    for (int i = 0; i < n; ++i) {\n        string s;\n        cin >> s;\n        for (char &c : s) c = tolower(static_cast<unsigned char>(c));\n        int cur = ++cnt[s];\n        if (cur > best) {\n            best = cur;\n            ans = s;\n        }\n    }\n\n    cout << ans << "\\n";\n    return 0;\n}',
      answer: '',
    }
    ]
};
