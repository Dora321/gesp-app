// 2025年6月 GESP C++ 三级真题 (第10次认证)
// 数据说明：本卷以官方真题 PDF 为主完成回填。
//-客观题 1~15：题面、选项与单选答案可由官方 PDF 文本层直接提取并整理。
//-判断题 16~25：题面来自官方 PDF；官方 PDF 文本层未完整带出答案表，当前答案依据公开解析交叉复核填写。
//-编程题 26~27：题名、题意、样例与参考代码主体来自官方 PDF；少量公式变量/范围符号因 PDF 文本层缺字，按官方题意做等价整理。
export const paperData = {
    id: '2025-06-l3',
    title: '2025年6月 GESP C++ 三级真题',
    level: 3,
    year: 2025,
    month: 6,
    session: 10,
    timeLimit: 90 * 60,
    source: {
        officialPdf: 'https://gesp.ccf.org.cn/101/attach/1699464809021472.pdf',
        notes: '客观题 25 题已尽量补齐；其中判断题答案因官方 PDF 文本层未完整带出答案表，现依据公开解析交叉复核。编程题 2 题已补题意、样例与参考代码；少量变量符号按官方题意等价整理。'
    },
    questions: [
        {
            id: 1,
            type: 'single',
            question: `8 位二进制原码能表示的最小整数是（ ）。`,
            options: ['-127', '-128', '-255', '-256'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (-127)**

**核心解析：**
8位二进制原码的存储结构为：最高位为符号位（0表示正，1表示负），剩余7位为数值位。

1. **原码的表示范围推导**：正数部分，数值位最大为 \`1111111\`（7个1），对应真值 +127，其原码为 \`01111111\`。负数部分，数值位同样最大为 \`1111111\`，对应真值 -127，其原码为 \`11111111\`。

2. **关键区分**：原码中零有两种表示——\`00000000\`（正零）和 \`10000000\`（负零），这导致能表示的总数量为 2^8 - 1 = 255 个，而非 256 个。

3. **最小值确认**：最小整数即负数中绝对值最大者，为 -127，原码为 \`11111111\`。

**选项逐项分析：**
- **A -127**：✅ 正确。8位原码最小值为 \`11111111\`，对应真值 -127。
- **B -128**：❌ 错误。这是8位补码能表示的最小值（原码无法直接表示 -128，因为其绝对值超出7位数值位的表示范围）。
- **C -255**：❌ 错误。原码的数值位只有7位，无法表示绝对值为255的数。
- **D -256**：❌ 错误。原因同上，且-256的绝对值更大，超出原码表示范围更远。

**易错提醒：**
- 原码和补码的表示范围不同。8位原码范围是 -127 ~ +127，8位补码范围是 -128 ~ +127，两者不可混淆。
- 原码中零有正零和负零两种表示，这会"浪费"一个编码。

**知识延伸：**
原码、反码、补码三者在表示范围和零的编码上各有特点。原码和反码都有两个零，而补码通过规定 \`10000000\` 为 -128 消除了负零，实现了 -128 ~ +127 的连续表示，这是现代计算机普遍采用补码的根本原因。

**考点：** 原码的存储结构与数值范围计算`,
            tags: ['二进制', '补码'],
        },
        {
            id: 2,
            type: 'single',
            question: `反码表示中，零的表示形式有（ ）。`,
            options: ['1 种', '2 种', '8 种', '16 种'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (2 种)**

**核心解析：**
反码（ones' complement）是介于原码和补码之间的一种编码方式，其定义规则如下：

1. **正数的反码**：与原码完全相同，符号位为0，数值位保持原值。
2. **负数的反码**：符号位为1，数值位为原码数值位的按位取反（0变1，1变0）。

**零的两种表示**：
- **正零**（+0）：所有位均为0，即 \`00000000\`。这是数值0的"自然"表示。
- **负零**（-0）：符号位为1，数值位全0按位取反后全1，即 \`11111111\`。这源于负数反码的定义——将+0的原码 \`00000000\` 的数值位取反得到 \`10000000\`（符号位1，数值位全0），再将数值位取反得到 \`11111111\`。

**选项逐项分析：**
- **A 1 种**：❌ 错误。这是补码的特性——补码规定 \`10000000\` 代表 -128，从而消除了负零，0只有一种表示（\`00000000\`）。
- **B 2 种**：✅ 正确。反码中0有两种编码：正零 \`00000000\` 和负零 \`11111111\`。
- **C 8 种**：❌ 错误。反码中零的表示形式与位数无关，始终为两种。
- **D 16 种**：❌ 错误。理由同上。

**易错提醒：**
- 混淆反码和补码的零表示。原码和反码都有两个零（正零和负零），只有补码消除了负零。
- 注意"反码"和"补码"的中文名称接近，但概念完全不同。

**知识延伸：**
反码的存在主要是为了便于理解补码的运算机制。在早期的某些计算机系统中曾使用过反码表示，但因存在两个零以及运算不便等问题，后来被补码取代。GESP考试中常通过比较三种编码方式来考查对计算机底层数据表示的理解。

**考点：** 原码、反码、补码中"零"的编码多样性辨析`,
            tags: ['二进制', '补码'],
        },
        {
            id: 3,
            type: 'single',
            question: `补码 1011 1011 对应的真值是（ ）。`,
            options: ['-69', '-59', '-68', '-58'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (-69)**

**核心解析：**
给定补码 \`10111011\`，需要逆向推导出其对应的真值（十进制数）。

**推导步骤**：

1. **判断符号**：补码最高位为1，说明这是一个负数。

2. **逆向转换为原码**：
   - 第一步：对补码减1：\`10111011 - 1 = 10111010\`
   - 第二步：数值位（除符号位外的后7位）取反：\`10111010\` → 符号位保持1，数值位 \`0111010\` 取反得 \`1000101\`，整体为 \`11000101\`

3. **计算原码对应的真值**：\`11000101\` 中数值位 \`1000101\` = 64 + 4 + 1 = 65，因此原码表示 +65，符号为负，真值为 -65... 等等，让我重新计算。

**重新推导**：
- 补码 \`10111011\` → 减1得 \`10111010\`
- 符号位不变（为1），其余7位 \`0111010\` 取反得 \`1000101\`
- 原码为 \`11000101\`
- 数值位 \`1000101\` = 1×2^6 + 0×2^5 + 0×2^4 + 0×2^3 + 1×2^2 + 0×2^1 + 1×2^0 = 64 + 0 + 0 + 0 + 4 + 0 + 1 = 69
- 因此真值为 -69

**选项逐项分析：**
- **A -69**：✅ 正确。通过上述推导，验证无误。
- **B -59**：❌ 错误。该值可能是在计算数值位时出现错误（如漏算某一位或进制转换有误）。
- **C -68**：❌ 错误。可能是在数值位计算时少算了1，或取反操作有误。
- **D -58**：❌ 错误。数值位计算偏差较大，可能是对二进制位权理解不到位。

**易错提醒：**
- 补码减1后不要忘记再取反，不要误认为可以直接从补码读出数值。
- 取反操作仅针对数值位，符号位始终保持为1不变。
- 数值位的位权从右到左依次是 2^0、2^1、2^2、...、2^6，对应1、2、4、8、16、32、64。

**知识延伸：**
补码的快速转换技巧：对于负数补码，还可以直接计算"各位权值之和，符号为负"。从右往左扫描，遇到第一个1之前（包括这个1）的位按普通位权计算，之后的位取反后按位权计算。例如 \`10111011\`，从右找第一个1在右数第2位，其左侧各位取反后计算：-(1 + 4 + 64) = -69。

**考点：** 负数补码向真值的转换计算`,
            tags: ['二进制', '补码'],
        },
        {
            id: 4,
            type: 'single',
            question: `若 X 的 8 位补码为 0000 1010，则 X / 2 的补码是（ ）。`,
            options: ['0000 0101', '1000 0101', '0000 0101 或 1000 0101', '算术右移后结果取决于符号位'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (0000 0101)**

**核心解析：**
题目给定 X 的8位补码为 \`00001010\`，求 X/2 的补码表示。

**推导步骤**：

1. **求 X 的真值**：
   - 补码 \`00001010\` 最高位为0，是正数
   - 真值 = 1×2^3 + 1×2^1 = 8 + 2 = 10

2. **计算 X/2**：
   - 10 ÷ 2 = 5

3. **求5的补码**：
   - 5是正数，其补码等于原码
   - 5 = 4 + 1，对应二进制 \`00000101\`
   - 因此 X/2 的补码为 \`00000101\`

**补充验证——算术右移验证**：
由于 X 是正数，X/2 等价于 X 算术右移1位。算术右移时，正数高位补0，因此：
\`00001010\` 右移1位 → \`00000101\`，与上述结果一致。

**选项逐项分析：**
- **A 0000 0101**：✅ 正确。正数除以2后仍是正数，结果为5，二进制表示为 \`00000101\`。
- **B 1000 0101**：❌ 错误。该选项符号位为1，表示负数，但5是正数，不可能得到负数的补码。
- **C 0000 0101 或 1000 0101**：❌ 错误。同一道题的答案不可能有两个选项，逻辑上自相矛盾。
- **D 算术右移后结果取决于符号位**：❌ 错误。对于正数，算术右移高位补0是确定的，不存在"取决于符号位"的歧义。

**易错提醒：**
- 不要混淆"算术右移"和"逻辑右移"。算术右移在右移时复制符号位（正数补0，负数补1）；逻辑右移始终补0。
- 对于负数的除以2操作（即算术右移），结果与正数不同，但本题中X是正数，不涉及此情况。

**知识延伸：**
在计算机中，整数除以2可以用右移一位来高效实现。但需要注意：有符号整数的除法在C++中向零取整，而右移的舍入行为可能不同（取决于实现）。对于负数 X/2（如 X = -3），若采用算术右移得到 -2（向负无穷舍入），而 C++ 的整数除法得到 -1（向零舍入），两者结果不同。

**考点：** 补码性质与算术右移（除法）的等价性`,
            tags: ['二进制', '补码', '位运算'],
        },
        {
            id: 5,
            type: 'single',
            question: `二进制数 1101.101 对应的十进制数是（ ）。`,
            options: ['13.625', '12.75', '11.875', '14.5'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (13.625)**

**核心解析：**
将二进制小数 \`1101.101\` 转换为十进制，需要分别处理整数部分和小数部分。

**整数部分转换**：\`1101\`

按权展开求和，从最高位到最低位：
- 第3位（权值2^3=8）：1 × 8 = 8
- 第2位（权值2^2=4）：1 × 4 = 4
- 第1位（权值2^1=2）：0 × 2 = 0
- 第0位（权值2^0=1）：1 × 1 = 1
- 合计：8 + 4 + 0 + 1 = 13

**小数部分转换**：\`0.101\`

小数位的权值为负指数，按从左到右顺序：
- 第1位（权值2^-1=0.5）：1 × 0.5 = 0.5
- 第2位（权值2^-2=0.25）：0 × 0.25 = 0
- 第3位（权值2^-3=0.125）：1 × 0.125 = 0.125
- 合计：0.5 + 0 + 0.125 = 0.625

**最终结果**：13 + 0.625 = 13.625

**选项逐项分析：**
- **A 13.625**：✅ 正确。整数部分13、小数部分0.625，计算无误。
- **B 12.75**：❌ 错误。可能误将整数部分算作12（如只算了最高两位1×8+1×4=12），或将小数部分算作0.75（误认为.101 = .100 + .010 = 0.5+0.25=0.75）。
- **C 11.875**：❌ 错误。整数部分少算了1或2，小数部分可能误算。
- **D 14.5**：❌ 错误。整数部分多算了1（可能将1101误认为14=8+4+2），小数部分完全错误。

**易错提醒：**
- 二进制小数位的权值是 2^-1、2^-2、2^-3...，而不是 2^-1、2^-1、2^-1...，每位权值都是前一位的一半。
- 小数部分 .101 不等于 1/2 + 1/2 + 1/2，而应按位权展开：.101 = 1×2^-1 + 0×2^-2 + 1×2^-3 = 0.5 + 0 + 0.125。

**知识延伸：**
二进制与十进制的转换是GESP的基础考点。十进制转二进制时，整数部分用除2取余法，小数部分用乘2取整法。例如 13.625 的整数部分13用除2取余得1101，小数部分0.625乘2取整：0.625×2=1.25取1，0.25×2=0.5取0，0.5×2=1.0取1，得到.101。

**考点：** 二进制小数向十进制的转换原理`,
            tags: ['二进制', '补码', '进制转换'],
        },
        {
            id: 6,
            type: 'single',
            question: `补码加法中，若符号位无进位而次高位有进位，则说明（ ）。`,
            options: ['结果正确', '发生上溢', '发生下溢', '结果符号位错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (发生上溢)**

**核心解析：**
补码加法中判断溢出有一经典方法——双进位判定法。具体规则如下：

1. 将参与加法的两个数的符号位进位记为 $C_{sign}$，次高位（数值最高位）进位记为 $C_{next}$。
2. 若 $C_{sign}$ 与 $C_{next}$ 相同（均为 0 或均为 1），则无溢出。
3. 若 $C_{sign}$ 与 $C_{next}$ 不同（异或结果为 1），则发生溢出。

本题明确给出"符号位无进位（$C_{sign}=0$）而次高位有进位（$C_{next}=1$）"，二者异或结果为 1，说明发生了溢出。由于两个正数相加结果却因进位而改变了符号位，表现为"正正得负"，这种溢出称为**上溢**。

**选项逐项分析：**
- **A (结果正确)**：❌ 错误。该项本身错误。符号位与次高位进位不一致本身就是溢出标志，结果必然不正确。
- **B (发生上溢)**：✅ 正确。符合双进位判定法中"符号位无进位而次高位有进位"的组合，即正数相加产生上溢。
- **C (发生下溢)**：❌ 错误。该项本身错误。下溢是针对浮点数运算中尾数太小无法表示的情况；本题讨论的是整数补码加法，且下溢的特征是符号位有进位而次高位无进位（$C_{sign}=1, C_{next}=0$），与题目条件相反。
- **D (结果符号位错误)**：❌ 错误。该项本身正确性存疑。"符号位错误"是一个模糊描述，溢出导致的是结果数值错误（正负颠倒），而非符号位本身在硬件层面出错。溢出检测的本质是识别"结果符号位与预期不符"，但选项D的表述不够精准，不是标准术语。

**易错提醒：**
- 混淆上溢与下溢的进位特征：上溢对应 $C_{sign}=0, C_{next}=1$；下溢对应 $C_{sign}=1, C_{next}=0$。
- 误以为只要有进位就是溢出，实际上必须是符号位进位与次高位进位**不同**时才溢出。

**知识延伸：**
溢出检测的另一种常用方法是"结果符号位与两个操作数符号位的关系"：若两个正数相加结果为负，或两个负数相加结果为正，则发生溢出。这与双进位判定法本质相同，只是描述角度不同。

**考点：** 补码运算中的溢出检测原理（双进位判定法）`,
            tags: ['二进制', '补码'],
        },
        {
            id: 7,
            type: 'single',
            question: `八进制数 35.6 对应的十进制数是（ ）。`,
            options: ['29.75', '28.5', '27.625', '30.25'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (29.75)**

**核心解析：**
将八进制数转换为十进制，需要分别转换整数部分和小数部分，然后求和。

1. **整数部分转换**：八进制每位权重为 $8^n$。
   $35_8 = 3 \\times 8^1 + 5 \\times 8^0 = 3 \\times 8 + 5 \\times 1 = 24 + 5 = 29$

2. **小数部分转换**：八进制小数每位权重为 $8^{-n}$。
   $0.6_8 = 6 \\times 8^{-1} = 6 \\div 8 = 0.75$

3. **合并结果**：$29 + 0.75 = 29.75$

**选项逐项分析：**
- **A (29.75)**：✅ 正确。计算过程如上所述。
- **B (28.5)**：❌ 错误。该项本身错误。可能是将小数部分误算为 $6 \\times 8^0 = 6$，或混淆了进制转换规则。
- **C (27.625)**：❌ 错误。该项本身错误。可能是将小数部分误算为 $0.6_8 = 6 \\times 8^{-2} = 6/64 = 0.09375$，或者遗漏了整数部分。
- **D (30.25)**：❌ 错误。该项本身错误。可能是将整数部分误算为 $3 \\times 8^1 + 5 = 29$ 而后又加 1，或者将小数部分误算为 $0.5$。

**易错提醒：**
- 八进制小数部分小数点后第一位是 $8^{-1}$，不是 $8^0$；第二位的权重才是 $8^{-2}$。
- 计算时要明确：小数部分是几进制，就用几的负指数次幂作为权重。

**知识延伸：**
任意进制小数转换为十进制的通式：若 $R$ 进制数有整数部分 $a_n a_{n-1} ... a_1 a_0$ 和小数部分 $0.b_1 b_2 ... b_m$，则转换为十进制为 $\\sum_{i=0}^{n} a_i \\times R^i + \\sum_{j=1}^{m} b_j \\times R^{-j}$。该公式同样适用于二进制、十六进制等。

**考点：** 八进制小数向十进制的转换原理`,
            tags: ['进制转换'],
        },
        {
            id: 8,
            type: 'single',
            question: `二进制数 1010 | 1100 的结果是（ ）。`,
            options: ['1000', '1110', '1010', '1100'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (1110)**

**核心解析：**
按位或运算（\`|\`）的规则：对两个数的每一位进行比较，若至少有一个操作数的对应位为 1，则结果位为 1；否则结果位为 0。

逐位分析：
\`\`\`
  1010    （被加数）
| 1100    （加数）
------
  1110    （结果）
\`\`\`

- 第3位（从右向左，第0位开始）：\`0 | 0 = 0\`
- 第2位：\`1 | 0 = 1\`
- 第1位：\`0 | 1 = 1\`
- 第0位：\`1 | 1 = 1\`

最终结果为 \`1110\`。

**选项逐项分析：**
- **A (1000)**：❌ 错误。该项本身错误。这是按位与运算（\`&\`）的结果，不是按位或。
- **B (1110)**：✅ 正确。按位或运算的标准结果。
- **C (1010)**：❌ 错误。该项本身错误。这是被加数本身，没有进行任何位运算操作。
- **D (1100)**：❌ 错误。该项本身错误。这是加数本身，没有进行任何位运算操作。

**易错提醒：**
- 不要混淆按位或（\`|\`）与逻辑或（\`||\`）。\`||\` 是短路运算符，用于布尔表达式；\`|\` 对每一位独立进行或运算。
- 不要混淆按位或（\`|\`）与按位与（\`&\`）。\`&\` 要求两位均为 1 才结果为 1，而 \`|\` 只要求至少一位为 1。

**知识延伸：**
位运算在嵌入式编程和底层算法中广泛应用。例如，可以用 \`a | (1 << n)\` 将第 n 位置 1，用 \`a & ~(1 << n)\` 将第 n 位清零。这道题是 GESP 三级对位运算基础规则的考查。

**考点：** 位运算（按位或 \`|\`）的规则`,
            tags: ['二进制', '补码', '位运算'],
        },
        {
            id: 9,
            type: 'single',
            question: `以下哪个位运算可以交换两个变量的值（无需临时变量）（ ）。`,
            options: [
                'a = a ^ b; b = a ^ b; a = a ^ b;',
                'a = a & b; b = a | b; a = a & b;',
                'a = a | b; b = a ^ b; a = a ^ b;',
                'a = ~a; b = ~b; a = ~a;'
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：A (a = a ^ b; b = a ^ b; a = a ^ b;)**

**核心解析：**
异或运算具有以下性质：
1. **自反性**：\`x ^ x = 0\`
2. **恒等性**：\`x ^ 0 = x\`
3. **交换律**：\`x ^ y = y ^ x\`
4. **结合律**：\`(x ^ y) ^ z = x ^ (y ^ z)\`

利用异或的这些性质，三步实现变量交换的过程如下：

**初始状态**：假设 \`a = A\`, \`b = B\`

**第一步**：\`a = a ^ b\`
执行后 \`a = A ^ B\`，\`b\` 仍为 \`B\`。此时 \`a\` 保存的是 A 与 B 的差异信息。

**第二步**：\`b = a ^ b\`
此时 \`a = A ^ B\`，代入得 \`b = (A ^ B) ^ B = A ^ (B ^ B) = A ^ 0 = A\`。
执行后 \`b = A\`，成功获得了原 \`a\` 的值。

**第三步**：\`a = a ^ b\`
此时 \`a = A ^ B\`，\`b = A\`，代入得 \`a = (A ^ B) ^ A = B ^ (A ^ A) = B ^ 0 = B\`。
执行后 \`a = B\`，成功获得了原 \`b\` 的值。

**交换完成**：\`a\` 与 \`b\` 的值已互换。

**选项逐项分析：**
- **A (a = a ^ b; b = a ^ b; a = a ^ b;)**：✅ 正确。完整的三步异或交换算法，利用异或的自反性和恒等性实现。
- **B (a = a & b; b = a | b; a = a & b;)**：❌ 错误。该项本身错误。按位与（\`&\`）和按位或（\`|\`）不具备异或的自反性，无法通过类似步骤还原原始值。
- **C (a = a | b; b = a ^ b; a = a ^ b;)**：❌ 错误。该项本身错误。虽然后两步看起来类似，但第一步 \`a = a | b\` 会导致信息丢失（将 A 和 B 的共同为 1 的位也置 1），后续步骤无法恢复原始值。
- **D (a = ~a; b = ~b; a = ~a;)**：❌ 错误。该项本身错误。\`~\` 是按位取反，三次取反虽然能让 \`a\` 和 \`b\` 恢复原值，但并没有交换二者的值。

**易错提醒：**
- 异或交换算法要求两个变量使用不同的内存地址（不能是同一个变量，否则会清零）。
- 该算法虽然无需临时变量，但阅读性差，且在某些优化编译器下可能被认为不符合顺序执行的假设，不推荐在实际工程中使用。

**知识延伸：**
位运算在算法优化中常见。除异或交换外，还可利用加减法实现：\`a = a + b; b = a - b; a = a - b;\`。但加减法交换有溢出风险，异或法无数值溢出问题，但同样要求地址不同。

**考点：** 位运算（异或 \`^\`）在变量交换中的应用`,
            tags: ['位运算', '异或', '按位取反', '逻辑判断'],
        },
        {
            id: 10,
            type: 'single',
            question: `如何正确定义一个长度为 5 的整型数组（ ）。`,
            options: ['int array = new int[5];', 'array int[5];', 'int[] array = {1,2,3,4,5};', 'int array[5];'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (int array[5];)**

**核心解析：**
C++ 中定义数组的核心语法规则是：类型说明符后紧跟数组名，中括号 \`[]\` 必须直接跟在数组名之后，且中括号内应指定数组长度（对于静态数组）或留空（对于动态数组）。

选项 D \`int array[5];\` 完全符合这一语法规范：类型为 \`int\`，数组名为 \`array\`，长度为 5。这是 C++ 中声明固定长度整型数组的标准形式。

**选项逐项分析：**
- **A (int array = new int[5];)**：❌ 错误。该项本身错误。\`new int[5]\` 返回的是指向堆内存的指针类型 \`int*\`，不能赋值给 \`int\` 类型的变量。正确写法应为 \`int* array = new int[5];\`。
- **B (array int[5];)**：❌ 错误。该项本身错误。这不符合任何语言的数组声明语法。类型名 \`int\` 应在变量名 \`array\` 之前，而不是之后。
- **C (int[] array = {1,2,3,4,5};)**：❌ 错误。该项本身错误。这是 Java 或 C# 的语法风格。C++ 中数组方括号应位于变量名之后，且 C++ 中初始化数组可以直接写成 \`int array[5] = {1,2,3,4,5};\`。
- **D (int array[5];)**：✅ 正确。完全符合 C++ 数组声明的标准语法。

**易错提醒：**
- 在 C++ 中，\`int array[5];\` 是栈上分配的静态数组，内存由系统自动管理；\`new int[5]\` 是堆上分配的动态数组，需要手动 \`delete[]\` 释放。
- C++ 中数组长度必须是编译期常量。对于动态大小数组，应使用 \`vector<int>\` 或动态分配。

**知识延伸：**
C++ 数组定义的完整形式为 \`type name[size];\`，其中 \`type\` 是元素类型，\`name\` 是数组名，\`size\` 是元素个数（必须是常量表达式）。与 Java 的 \`int[] array\` 或 Python 的列表不同，C++ 数组在定义时大小必须确定，且访问时不会进行边界检查。

**考点：** C++ 数组定义语法规范`,
            tags: ['数组'],
        },
        {
            id: 11,
            type: 'single',
            question: `下面程序使用枚举法（穷举法）求解满足条件的三位数，横线处应该填入的是（ ）。\n\`\`\`cpp\n#include <iostream>\nusing namespace std;\nint main() {\n int count = 0;\n for (int i = 100; i <= 999; i++) {\n int a = i / 100;\n __________________\n int c = i % 10;\n if (a * a+b * b == c * c) {\n count++;\n }\n }\n cout << count << endl;\n return 0;\n}\n\`\`\``,
            options: ['int b = (i / 10) / 10;', 'int b = (i / 10) % 10;', 'int b = (i % 10) / 10;', 'int b = (i % 10) % 10;'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (int b = (i / 10) % 10;)**

**核心解析：**
本题使用枚举法遍历100\\~999的三位数，验证是否满足 a² + b² = c²（勾股数条件）。其中 a 是百位，b 是十位，c 是个位。

已知：
- \`int a = i / 100;\` —— 整除100得到百位
- \`int c = i % 10;\` —— 取模10得到个位
- 需要求十位 b

提取十位的思路：
1. \`i / 10\`：将三位数整除10，得到去掉个位后的两位数（百位+十位）
2. 对上述结果再 \`% 10\`：取模10得到这个两位数的个位，即原数的十位

例如 i = 345：
- \`i / 10 = 34\`，再 \`34 % 10 = 4\`，得到十位数字 4 ✓

**选项逐项分析：**
- **A int b = (i / 10) / 10;**：❌ 错误。\`i/10\` 得到34，再整除10得3，得到的是百位数字（与a相同），不是十位。
- **B int b = (i / 10) % 10;**：✅ 正确。先整除10再去模10，标准提取十位的方法。
- **C int b = (i % 10) / 10;**：❌ 错误。\`i % 10\` 得到个位数字（如345%10=5），再整除10得0，无法得到十位。
- **D int b = (i % 10) % 10;**：❌ 错误。\`i % 10\` 已是0\\~9的个位数，再对10取模结果不变，仍是原个位数字。

**易错提醒：**
- 整除 \`/\` 和取模 \`%\` 的顺序不能颠倒：先整除去掉低位，再取模提取目标位。
- 对于三位数 \`i = abc\`（a、b、c为各位数字），提取各位的标准写法是：百位 \`i/100\`、十位 \`i/10%10\`、个位 \`i%10\`。

**知识延伸：**
整数位值提取是枚举法、模拟法题目中的高频考点，可扩展为：对于任意 n 位数，提取第 k 位（从低位开始，k≥1）的公式为 \`(i / 10^(k-1)) % 10\`。

**考点：** 整数位值提取技巧（整除与取模的组合应用）`,
            tags: ['位运算', '枚举', '算法思维', '循环', '逻辑判断'],
        },
        {
            id: 12,
            type: 'single',
            question: `下面程序模拟了一个简单的小球反弹过程，横线处应该填入的是（ ）。\n\`\`\`cpp\n#include <iostream>\nusing namespace std;\nint main() {\n int height = 10;\n int distance = 0;\n for (int i = 1; i <= 5; i++) { // 模拟 5 次落地\n __________________\n height /= 2;\n distance += height;\n }\n cout << distance << endl;\n return 0;\n}\n\`\`\``,
            options: ['distance += height / 2;', 'distance += height;', 'distance += height * 2;', 'distance += height+1;'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (distance += height;)**

**核心解析：**
模拟小球从高度10米处自由落体的物理过程：
- 循环5次，模拟5次落地
- 每次落地前，小球从当前高度 \`height\` 下落
- 落地后高度减半：\`height /= 2\`
- 求小球经过的总路程

物理过程分析（第1次循环为例）：
1. 初始 \`height = 10\`
2. 小球下落10米，触地，此时总路程应为10米
3. 落地后反弹，高度变为 \`10 / 2 = 5\` 米
4. 循环继续...

关键点：**小球每次下落前的高度，就是它本次下落的距离**。因此横线处应填入 \`distance += height;\`，累加的是当前高度值（即下落距离）。

**选项逐项分析：**
- **A distance += height / 2;**：❌ 错误。累加的是 height/2，但小球实际下落的距离是完整的 height，漏掉了一半路程。
- **B distance += height;**：✅ 正确。每次下落距离等于当前高度 height，直接累加 height 即可。
- **C distance += height * 2;**：❌ 累加了 height×2，多算了路程。一来一回才需要×2，本题只累加下落过程。
- **D distance += height+1;**：❌ 错误。硬加1毫无物理依据，且会导致最终结果错误。

**易错提醒：**
- 本题只累加**下落**距离，不累加反弹上升的距离。若题目要求求"总路程"（往返），则需额外累加 \`height *= 2\` 或 \`height += height\`（因为落地后会反弹上升同样的高度）。
- 循环内语句顺序很重要：必须先累加下落距离，再更新高度（\`height /= 2\`），顺序颠倒会导致结果错误。

**知识延伸：**
此类物理模拟题目在GESP中常见，关键是根据实际物理过程准确建模。"弹跳高度减半"对应 \`height /= 2\`（整数除法会截断小数部分，若需精确值应用 \`double\` 类型）。

**考点：** 物理过程的循环逻辑模拟（下落距离与当前高度的关系）`,
            tags: ['位运算', '循环', '逻辑判断'],
        },
        {
            id: 13,
            type: 'single',
            question: `C++ 代码 \`string s = "GESP考试";\` 中，s 占据的字节数是（ ）。`,
            options: ['10', '8', '8 或 10', '取决于计算机采用什么编码'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (取决于计算机采用什么编码)**

**核心解析：**
C++ 中 \`std::string\` 内部以字节序列存储字符串内容。字符串占用的字节数取决于该字符串在不同字符编码下占用的字节数，而不同系统、不同编译器、不同配置可能采用不同的编码方式。

分析字符串 \`"GESP考试"\`：
- 英文字母 "G"、"E"、"S"、"P" 在常见编码下均占1字节，共4字节
- 中文字符 "考"、"试" 在不同编码下占用字节数不同：
  - **UTF-8 编码**：每个中文字符通常占3字节，2个中文字符共6字节，总计 **10字节**
  - **GBK/GB2312 编码**：每个中文字符占2字节，2个中文字符共4字节，总计 **8字节**
  - 其他编码方式可能还有不同的字节数

因此，s占据的字节数**完全取决于运行环境采用的字符编码**，无法给出唯一确定的答案。

**选项逐项分析：**
- **A 10**：❌ 错误。这是 UTF-8 编码下的字节数，但未说明编码前提，且不是所有环境都采用 UTF-8。
- **B 8**：❌ 错误。这是 GBK 编码下的字节数，但未说明编码前提，且不是所有环境都采用 GBK。
- **C 8 或 10**：❌ 错误。虽然列举了两种常见情况，但忽略了其他编码（如 UTF-16 占12字节、Latin-1 占8字节等），表述不严谨。
- **D 取决于计算机采用什么编码**：✅ 正确指出了字节数与编码的直接关系，这是本题的准确答案。

**易错提醒：**
- 字符数与字节数是两个概念：字符数是逻辑上的字符个数（"Gesp考试"有6个字符），字节数是物理存储占用的空间。
- 题目问的是"字节数"（bytes），而非"字符数"（characters），必须区分这两个概念。

**知识延伸：**
C++ 标准库 \`std::string\` 以 \`char\`（1字节）为基本存储单元，对中文字符的处理能力取决于编译器和运行环境的编码设置。若需要处理多语言文本，应使用 \`std::wstring\` 或 C++11 的 \`std::u16string\`/\`std::u32string\`。

**考点：** 字符编码与C++字符串底层存储的关系`,
            tags: ['字符与ASCII', '字符串', '计算机基础'],
        },
        {
            id: 14,
            type: 'single',
            question: `C++ 语句 \`string s = "Gesp Test";\` 执行 \`s.rfind("e")\` 以后，输出的是（ ）。`,
            options: ['1', '2', '6', '3'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (6)**

**核心解析：**
\`std::string\` 的 \`rfind()\` 方法（reverse find）从字符串的**末尾开始向左**查找指定字符或子串，返回第一次匹配到的位置索引（从0开始计数）。

分析字符串 \`"Gesp Test"\`：
- 索引分布：\`G(0), e(1), s(2), p(3), 空格(4), T(5), e(6), s(7), t(8)\`
- 共9个字符，索引范围 0\\~8

执行 \`s.rfind("e")\`：
1. 从索引8向左依次检查：t→s→e
2. 在索引6处找到字符 'e'，停止搜索
3. 返回位置 6

注意：\`find()\` 和 \`rfind()\` 都返回**从左到右计数的索引位置**，只是搜索方向不同。

**选项逐项分析：**
- **A 1**：❌ 错误。这是 \`find("e")\` 的返回值（正向查找找到的第一个 'e' 在索引1），不是 \`rfind\` 的结果。
- **B 2**：❌ 错误。不是任何标准方法的返回值，索引2处是字符 's'。
- **C 6**：✅ 正确。\`rfind\` 从后往前找，索引6处的 'e' 是从末尾方向看到的第一个 'e'。
- **D 3**：❌ 错误。索引3处是字符 'p'，不是 'e'。

**易错提醒：**
- \`rfind\` 的返回值仍是**从前往后的索引**，不是从后往前数的第几个。容易误以为返回"倒数第几个"。
- 若 \`rfind\` 未找到匹配，会返回 \`std::string::npos\`（通常定义为 -1 或一个很大的无符号值）。

**知识延伸：**
\`find()\` 系列方法包括：\`find()\`（正向首次）、\`rfind()\`（反向首次）、\`find_first_of()\`（正向首次匹配集合中任意字符）、\`find_last_of()\`（反向首次匹配集合中任意字符）等。掌握这些方法对处理字符串查找类题目非常重要。

**考点：** std::string 类的反向查找方法 rfind()`,
            tags: ['字符与ASCII', '字符串'],
        },
        {
            id: 15,
            type: 'single',
            question: `字符串 "Gesp考试" 的字符数是（ ）。`,
            options: ['10', '8', '6', '字符数多少取决于编码'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (6)**

**核心解析：**
本题问的是字符串的**字符数**（character count），即逻辑上由几个字符组成，而非占用的字节数。

分析字符串 \`"Gesp考试"\`：
- 英文字母：'G'、'e'、's'、'p' —— 共 **4** 个字符
- 中文字符：'考'、'试' —— 共 **2** 个字符
- 字符数 = 4 + 2 = **6**

字符数是一个**逻辑概念**，与字符本身的语义含义相关，与存储编码无关。无论采用 UTF-8、GBK 还是其他编码，"Gesp考试" 都包含这6个字符。

**选项逐项分析：**
- **A 10**：❌ 错误。这是 UTF-8 编码下字符串占用的**字节数**，不是字符数。UTF-8 中每个中文字符占3字节，"GESP"(4B) + "考试"(6B) = 10字节。
- **B 8**：❌ 错误。这是 GBK 编码下字符串占用的**字节数**，不是字符数。GBK 中每个中文字符占2字节，"GESP"(4B) + "考试"(4B) = 8字节。
- **C 6**：✅ 正确。字符数是逻辑概念，4个英文字符 + 2个中文字符 = 6个字符。
- **D 字符数多少取决于编码**：❌ 错误。该项本身错误。字符数是固定的逻辑数量，不会因编码改变；只有**字节数**才会因编码不同而变化。

**易错提醒：**
- 题干明确问"字符数"，部分考生会混淆"字符数"与"字节数"，看到10、8等数字就选了。
- 区分两个概念：字符数（characters）—— 有几个字符；字节数（bytes）—— 占多少存储空间。
- 在编程中获取字符数可用 \`s.length()\` 或 \`s.size()\`，获取字节数可用 \`s.length() * sizeof(char)\` 或专门的编码转换函数。

**知识延伸：**
C++ 中 \`std::string::length()\` / \`std::string::size()\` 返回的是 \`char\` 的个数，而非真正的 Unicode 字符数。在处理包含组合字符（如带重音符号的字母）的国际化文本时可能不准确，但在 GESP 范围内，中文字符被视为单个字符。

**考点：** 字符数与字节数的概念辨析`,
            tags: ['字符与ASCII', '字符串', '计算机基础'],
        },
        {
            id: 16,
            type: 'judge',
            question: `C++ 中 string 的 == 运算符比较的是字符串的内存地址，而非内容。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
\`string\` 类的 \`==\` 运算符是被重载过的，其比较逻辑是**内容比较**，即逐一比较两个字符串的每个字符是否相同，而不是比较内存地址。如果要比较地址，应使用 \`&s1 == &s2\` 这样的表达式。

**易错提醒：**
- 需要区分 C++ 中的 string 与 C 风格的字符数组。字符数组名在表达式中会退化为指针，比较的才是地址。
- string 类通过 \`operator==\` 的重载，将比较语义从地址改为内容，这是 C++ 面向对象设计的重要体现。

**知识延伸：**
C++ 标准库中所有容器（如 vector、map、set 等）的 \`==\` 运算符均被重载为内容比较，而非地址比较。

**考点：** 字符串、C++ 运算符重载`,
            tags: ['字符串', '计算机基础'],
        },
        {
            id: 17,
            type: 'judge',
            question: `string 的 substr(1, 3) 返回从下标 1 开始的 3 个字符的子串。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
\`string\` 的成员函数 \`substr(pos, len)\` 的语义是：从下标 \`pos\` 开始，截取 \`len\` 个字符组成新字符串返回。因此 \`substr(1, 3)\` 表示从下标 1 开始取 3 个字符。

**易错提醒：**
- 字符串下标从 0 开始计数，\`substr(1, 3)\` 取的是第 2、3、4 个字符（从 1 开始的三位）。
- 若原字符串长度不足，\`substr\` 会返回从 pos 开始到字符串末尾的所有字符，不会报错。

**知识延伸：**
\`substr\` 的两个参数分别是起始位置和长度。若只传一个参数 \`substr(pos)\`，则表示从 pos 开始到末尾的全部字符。

**考点：** 字符串、substr 函数`,
            tags: ['字符串', 'substr'],
        },
        {
            id: 18,
            type: 'judge',
            question: `x 是浮点数，(x >> 1) 等价于 x / 2。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
C++ 中的位运算（包括 \`>>\` 和 \`<<\`）**只能用于整数类型**（包括 \`int\`、\`long\`、\`char\` 等），不能直接用于浮点数类型（如 \`float\`、\`double\`）。对浮点数使用位移运算符会导致编译错误或未定义行为。在 C++ 中，浮点数除以 2 应该使用 \`x / 2.0\` 或 \`x * 0.5\`。

**易错提醒：**
- 位移运算的本质是二进制位的左移或右移，这只在整数的二进制表示中有意义。浮点数的 IEEE 754 表示中包含符号位、指数位和尾数位，随意位移会破坏其数据结构。
- 整数除法 \`x / 2\` 与位移 \`x >> 1\` 在某些情况下结果相似（如正整数），但两者有本质区别，且都不能直接用于浮点数。

**知识延伸：**
位移运算 \`>> 1\` 等价于除以 2 的向下取整（对于正整数），这是因为二进制右移一位相当于把数除以 2 并丢弃最低位。

**考点：** 位运算、数据类型限制`,
            tags: ['位运算'],
        },
        {
            id: 19,
            type: 'judge',
            question: `string("hello") == "hello" 的比较结果为 true。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
\`string("hello")\` 创建了一个内容为 "hello" 的 \`string\` 对象，C 风格字符串字面量 \`"hello"\` 在与 \`string\` 对象比较时，会通过 \`string\` 类重载的 \`operator==\` 隐式转换为一个临时 \`string\` 对象参与比较。两者内容完全相同，因此比较结果为 \`true\`。

**易错提醒：**
- 这里涉及隐式类型转换：C 风格字符串在需要时会被自动构造为 \`string\` 对象。
- 此类比较是内容比较而非地址比较，与第 16 题的考点相互呼应。

**知识延伸：**
\`string\` 类提供了多个比较运算符的重载，包括与 C 风格字符串的直接比较，这是标准库为方便使用而设计的。

**考点：** 字符串、类型转换`,
            tags: ['字符串'],
        },
        {
            id: 20,
            type: 'judge',
            question: `sort 可以直接用于排序 set 中的元素。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
\`set\`（std::set）是关联容器，其底层实现为红黑树，元素始终**自动保持有序**。更重要的是，\`set\` 的迭代器是双向迭代器（Bidirectional Iterator），而 \`std::sort\` 算法要求的是随机访问迭代器（Random Access Iterator），两者不兼容。因此不能对 \`set\` 直接使用 \`sort\`。

**易错提醒：**
- set 本身已经有序，无需也不应再排序。对 set 调用 sort 会导致编译错误。
- 如果确实需要排序 set 中的元素，可以先将元素拷贝到 vector 中，对 vector 使用 sort，再处理结果。

**知识延伸：**
C++ 中所有标准容器都有其特定的迭代器类型：\`vector\` 和 \`deque\` 提供随机访问迭代器，支持 sort；\`list\` 提供双向迭代器，虽不支持 sort 但自身有 \`sort()\` 成员函数；\`set\` 和 \`map\` 的迭代器是双向的，不支持 sort。

**考点：** 容器迭代器类型、排序算法适用范围`,
            tags: ['排序'],
        },
        {
            id: 21,
            type: 'judge',
            question: `(x & 1) == 0 可以判断整数 x 是否为偶数。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
在二进制表示中，偶数的最低位（LSB，Least Significant Bit）必为 0，奇数必为 1。表达式 \`x & 1\` 将整数 \`x\` 与二进制 \`0001\` 进行按位与运算：
- 若 \`x\` 为偶数，二进制末位为 0，则 \`x & 1 == 0\` 成立；
- 若 \`x\` 为奇数，二进制末位为 1，则 \`x & 1 == 1\` 不成立。
因此 \`(x & 1) == 0\` 可作为判断整数 \`x\` 是否为偶数的有效方法。

**易错提醒：**
- 有考生可能误以为负数的按位与规则不同，但负数在二进制补码表示下，其最低位同样遵循"奇数末位为1、偶数末位为0"的规律，故该方法对负数同样有效。

**知识延伸：**
位运算常用于状态标记、权限控制等场景。与判断偶数相反，判断奇数可用 \`(x & 1) == 1\` 或直接 \`x & 1\`。

**考点：** 位运算基础`,
            tags: ['位运算'],
        },
        {
            id: 22,
            type: 'judge',
            question: `string 的 substr(2, 10) 在字符串长度不足时会抛出异常。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
\`std::string::substr(pos, len)\` 的行为规则如下：
1. 若 \`pos >= size()\`，则抛出 \`std::out_of_range\` 异常；
2. 若 \`pos < size()\`，即使 \`len\` 超出剩余字符数，也只会截取从 \`pos\` 到字符串末尾的所有字符，不会抛异常。

因此，当字符串长度不足（如长度为 5，调用 \`substr(2, 10)\`）时，只要起始位置 2 是合法的，\`substr\` 会返回从位置 2 到末尾的子串（共 3 个字符），程序正常运行，不会抛出异常。

**易错提醒：**
- 部分考生将 \`substr\` 与数组下标访问混淆：\`s[i]\` 在 \`i >= size()\` 时会越界未定义行为，而 \`substr\` 对长度的处理更为宽松，仅对非法起始位置抛异常。

**知识延伸：**
\`substr\` 的参数含义是"从位置 pos 开始，截取最多 len 个字符"，这是一种"安全截断"的设计，避免因长度估计错误导致程序崩溃。

**考点：** string 类常用方法`,
            tags: ['字符串', 'substr'],
        },
        {
            id: 23,
            type: 'judge',
            question: `在数学纸面计算中，pow(2, 3) 的计算结果一定是 8，但是在 C++ 中，如果遇到数据类型是浮点数，那就不一定正确。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
在数学纸面计算中，\`2³ = 8\` 是精确值。但在 C++ 中，\`pow(2, 3)\` 返回的是 \`double\` 类型（函数原型为 \`double pow(double, double)\`）。浮点数采用 IEEE 754 标准表示，存在以下精度问题：
1. **存储误差**：十进制小数 0.1、0.2 等无法被二进制浮点数精确表示，存储时存在舍入误差；
2. **运算误差**：浮点运算过程中误差可能累积。

因此，\`pow(2, 3)\` 的实际返回值可能略微小于或大于 8，如 \`7.999999999999999\` 或 \`8.000000000000001\`。直接与整数 \`8\` 比较（如 \`pow(2, 3) == 8\`）可能得到 \`false\`。

**易错提醒：**
- 考试中若出现 \`pow\` 函数与整数直接比较，应优先考虑浮点精度问题；
- 对于需要精确整数结果的场景，建议将 \`pow\` 结果取整或使用整型乘法 \`2 * 2 * 2\`。

**知识延伸：**
\`pow\` 函数定义在 \`<cmath>\` 中，其返回值类型是 \`double\`。当需要整数幂时，GESP 三级要求的整型表达式计算（如 \`1 << 3\`）比 \`pow\` 更高效且无精度问题。

**考点：** 浮点数精度、pow 函数`,
            tags: ['浮点数'],
        },
        {
            id: 24,
            type: 'judge',
            question: `在 C++ 中，枚举的底层类型可以是非整型（如 float 或 double）。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
C++ 标准规定，枚举类型（无论是枚举类 \`enum class\` 还是普通枚举 \`enum\`）的底层类型（underlying type）必须是整型。枚举值在底层以整数形式存储，可以是 \`int\`、\`unsigned int\`、\`char\` 等整型，但绝不能是 \`float\`、\`double\` 或其他浮点类型。

**易错提醒：**
- 部分考生可能混淆了"枚举可以存储整型值"与"枚举底层可以是浮点型"这两个概念。前者正确，后者错误。

**知识延伸：**
枚举类（C++11 起）可以显式指定底层类型，如 \`enum class Color : unsigned char { Red, Green, Blue };\`，但指定的类型仍必须是整型。若不显式指定，编译器会根据枚举值范围自动选择合适的整型。

**考点：** 枚举类型基础`,
            tags: ['枚举', '算法思维'],
        },
        {
            id: 25,
            type: 'judge',
            question: `函数声明 \`double f();\` 返回 int 时，会自动转换为 double。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
C++ 的类型转换规则之一是：若 \`return\` 语句中表达式的类型与函数声明的返回类型不一致，编译器会自动将返回值转换为函数声明的返回类型。

对于函数声明 \`double f();\`：
- 若函数体中有 \`return 42;\`，其中 \`42\` 是 \`int\` 类型；
- 编译器会自动将 \`int\` 类型的 \`42\` 转换为 \`double\` 类型的 \`42.0\` 后返回。

这种转换发生在函数调用点，由编译器隐式完成，属于隐式类型转换的一部分。

**易错提醒：**
- 隐式转换可能带来精度损失或数据截断的风险（如 \`double\` 转 \`int\` 会丢失小数部分），但 \`int\` 转 \`double\` 不会丢失数据，只是类型发生变化；
- 若返回类型是 \`int\`，而实际返回值是 \`double\`，则会丢失小数部分，这是另一种隐式转换的危害场景。

**知识延伸：**
函数返回值的隐式转换是 C++ 类型系统的一部分。理解这一规则有助于避免因类型不匹配导致的意外结果，也是后续学习函数重载、模板等高级特性的基础。

**考点：** 隐式类型转换、函数返回类型`,
            tags: ['类型转换'],
        }
    ],
    programmingQuestions: [
        {
      id: 26,
      type: 'programming',
      samples: [
        { input: `4
71 69 83 80`, output: `13 1` },
        { input: `6
1 2 4 8 16 32`, output: `6 0` }
      ],
      question: `
# [GESP202506 三级] 奇偶校验

## 题目描述

数据在传输过程中可能出错，因此接收方收到数据后通常会校验传输的数据是否正确，奇偶校验是经典的校验方式之一。

给定 $n$ 个非负整数 $c_1, c_2, \\ldots, c_n$ 代表所传输的数据，它们的校验码取决于这些整数在二进制下 1 的数量之和的奇偶性。如果这些整数在二进制下共有奇数个 1，那么校验码为 1；否则校验码为 0。你能求出这些整数的校验码吗？

## 输入格式

第一行，一个正整数 $n$，表示所传输的数据量。

第二行，$n$ 个非负整数 $c_1, c_2, \\ldots, c_n$，表示所传输的数据。

## 输出格式

输出一行，两个整数，以一个空格分隔：

第一个整数表示 $c_1, c_2, \\ldots, c_n$ 在二进制下 1 的总数量；

第二个整数表示校验码（0 或 1）。
`,
      explanation: `
      **解析：**
      遍历所有输入数字，统计其二进制中 1 的个数之和 s，答案即为 \`s\` 和 \`s % 2\`。
      `,
      template: '#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n\n    // 在此编写代码\n    return 0;\n}',
      score: 25,
      tags: ['编程题', '位运算', '统计'],
      referenceCode: '#include <cstdio>\nusing namespace std;\n\nint n, v;\n\nint main() {\n    scanf("%d", &n);\n    for (int i = 1; i <= n; i++) {\n        int c;\n        scanf("%d", &c);\n        while (c) {\n            v += c & 1;\n            c >>= 1;\n        }\n    }\n    printf("%d %d\\n", v, v & 1);\n    return 0;\n}',
      answer: '',
    },
        {
      id: 27,
      type: 'programming',
      samples: [
        { input: `4
1 4 3 3`, output: `16` },
        { input: `15
314 15926 53589793 238462643 383279502 8 8 4 1 9 7 1 6 9 3`, output: `4508143253` }
      ],
      question: `
# [GESP202506 三级] 分糖果

## 题目描述

有 $n$ 位小朋友排成一队等待老师分糖果。第 $i$ 位小朋友想要至少 $a_i$ 颗糖果，并且分给他的糖果数量必须比分给前一位小朋友的糖果数量更多，不然他就会不开心。

老师想知道至少需要准备多少颗糖果才能让所有小朋友都开心。你能帮帮老师吗？

## 输入格式

第一行，一个正整数 $n$，表示小朋友的人数。

第二行，$n$ 个正整数 $a_1, a_2, \\ldots, a_n$，依次表示每位小朋友至少需要的糖果数量。

## 输出格式

输出一行，一个整数，表示最少需要准备的糖果数量。
`,
      explanation: `
      **解析：**
      从左到右贪心。设前一位拿到 prev 颗，则当前位至少应拿 \`max(ai, prev+1)\` 颗，把这些值累加即可。
      `,
      template: '#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n\n    // 在此编写代码\n    return 0;\n}',
      score: 25,
      tags: ['编程题', '贪心', '数组'],
      referenceCode: '#include <cstdio>\n#include <algorithm>\nusing namespace std;\n\nconst int N = 1005;\nint n, a[N];\nlong long ans;\n\nint main() {\n    scanf("%d", &n);\n    for (int i = 1; i <= n; i++) {\n        scanf("%d", &a[i]);\n        a[i] = max(a[i-1]+1, a[i]);\n        ans += a[i];\n    }\n    printf("%lld\\n", ans);\n    return 0;\n}',
      answer: '',
    }
    ]
};
