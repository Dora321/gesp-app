// 2024年6月 GESP C++ 三级真题 (第6次认证)
// 数据说明：本卷优先依据官方真题 PDF 回填；其中大部分客观题题面可直接由 PDF 文本层提取。
//-客观题 1~5、7~15、16~25：题面主要来自官方 PDF 文本层，少量代码/版式做了等价整理。
//-客观题 6、8、21、23、24：官方 PDF 为流程图/代码截图，现按官方题面+公开解析做等价还原。
//-判断题答案来自公开解析并与官方题面逐题复核；第 8~10 题的代码细节在 PDF 文本层有截断，现保留可核对题意。
//-编程题 2 题题名、题意、样例与参考代码来自官方 PDF，同时与公开 OJ 题号 B4003/B4004 对齐。
export const paperData = {
    id: '2024-06-l3',
    title: '2024年6月 GESP C++ 三级真题',
    level: 3,
    year: 2024,
    month: 6,
    session: 6,
    timeLimit: 90 * 60,
    source: {
        officialPdf: 'https://gesp.ccf.org.cn/101/attach/1621071490973728.pdf',
        notes: '客观题 25 题已尽量补齐；其中 6、8、21、23、24 题按官方题面结合公开解析等价还原；编程题已补 2 题摘要、样例与参考代码。'
    },
    questions: [
        {
            id: 1,
            type: 'single',
            question: `小杨父母带他到某培训机构给他报名参加 CCF 组织的 GESP 认证考试的第 1 级，那他可以选择的认证语言有（ ）种。`,
            options: ['1', '2', '3', '4'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (3)**

**核心解析：**
GESP 认证体系一级语言选择范围

GESP（编程能力等级认证）是中国计算机学会（CCF）组织的青少年编程能力等级认证。根据 GESP 官方公布的认证体系，一级可选的编程语言共有三种：图形化编程（Scratch）、Python 和 C++。考生可根据自身情况选择其中任何一种语言参加一级认证。

**选项逐项分析：**
- **A 1**：❌ 错误。GESP 一级实际提供了多种语言供选择，并非只有 1 种。
- **B 2**：❌ 错误。GESP 一级可选语言数量超过 2 种。
- **C 3**：✅ 正确。GESP 一级可选语言为图形化编程（Scratch）、Python 和 C++，共 3 种。
- **D 4**：❌ 错误。GESP 一级目前没有设置 4 种可选语言。

**易错提醒：**
- 需区分 GESP 不同级别的语言设置，一级以图形化入门为主
- GESP 高级别（如二级、三级）会逐步减少图形化语言选项

**知识延伸：**
GESP 认证体系设计遵循循序渐进原则，一级注重编程思维启蒙，二级及以上则聚焦文本编程语言。

**考点：** GESP 认证体系基础知识（认证级别与可选语言）`,
            tags: ['程序分析'],
        },
        {
            id: 2,
            type: 'single',
            question: `下面流程图在 yr 输入 2024 时，可以判定 yr 代表闰年，并输出 2 月是 29 天，则图中菱形框中应该填入（ ）。`,
            options: ['(yr%400==0) || (yr%4==0)', '(yr%400==0) || (yr%4==0 && yr%100!=0)', '(yr%400==0) && (yr%4==0)', '(yr%400==0) && (yr%4==0 && yr%100!=0)'],
            answer: 1,
            score: 2,
            explanation: `**答案：B ((yr%400==0) || (yr%4==0 && yr%100!=0))**

**核心解析：**
闰年判定的数学逻辑与 C++ 表达式实现

闰年的判定规则（公历）：
- **满足条件 A**：能被 400 整除的年份 → 一定是闰年
- **满足条件 B**：能被 4 整除但不能被 100 整除的年份 → 也是闰年
- **不满足 A 且不满足 B** → 平年

因此闰年的完整判定表达式为：A || B，即 \`(yr%400==0) || (yr%4==0 && yr%100!=0)\`。

**选项逐项分析：**
- **A (yr%400==0) || (yr%4==0)**：❌ 错误。该选项漏掉了"不能被 100 整除"的条件。例如 yr=1900 时，1900%400=100（非0），1900%4=0，导致表达式为 true，错误地将 1900 判定为闰年。
- **B (yr%400==0) || (yr%4==0 && yr%100!=0)**：✅ 正确。严格遵循闰年定义：能被 400 整除 OR（能被 4 整除 AND 不能被 100 整除）。逻辑运算符优先级：\`&&\` 高于 \`||\`，因此先计算 \`yr%4==0 && yr%100!=0\`，再与 \`yr%400==0\` 进行逻辑或运算。
- **C (yr%400==0) && (yr%4==0)**：❌ 错误。该选项要求同时满足"能被 400 整除"和"能被 4 整除"，逻辑过于严苛。实际上能被 400 整除的年份必然能被 4 整除，故条件 \`yr%400==0 && yr%4==0\` 等价于 \`yr%400==0\`，丢失了"能被 4 整除但不能被 100 整除"这一类闰年。
- **D (yr%400==0) && (yr%4==0 && yr%100!=0)**：❌ 错误。该选项要求同时满足三个条件，导致所有年份都无法同时满足"能被 400 整除"和"不能被 100 整除"（因为能被 400 整除的必然能被 100 整除），表达式永远为 false。

**验证示例：**
- yr=2000：2000%400=0 → 表达式为 true，2000 是闰年 ✓
- yr=1900：1900%400=100（非0），1900%4=0，1900%100=0 → B 中 \`yr%4==0 && yr%100!=0\` 为 false，表达式为 false，1900 是平年 ✓
- yr=2024：2024%400=24（非0），2024%4=0，2024%100=24（非0）→ B 中 \`yr%4==0 && yr%100!=0\` 为 true，表达式为 true，2024 是闰年 ✓

**易错提醒：**
- 混淆 || 与 && 的使用场景：闰年需要"或"关系，而非"且"关系
- 忽略"不能被 100 整除"的条件会导致 1900、1800、2100 等年份被误判

**知识延伸：**
本题体现了"复合布尔表达式"的真值分析与"逻辑运算符优先级"两个 GESP 三级考点。在 C++ 中，\`&&\` 优先级高于 \`||\`，但为清晰起见通常加括号。

**考点：** 逻辑表达式综合应用（闰年判定算法）`,
            tags: ['位运算', '流程图', '逻辑判断'],
        },
        {
            id: 3,
            type: 'single',
            question: `一般默认 64 位计算机系统中整型变量（int）仍是 32 位，则整数能够表示的数据范围是（ ）。`,
            options: ['-2^15 ~ 2^15-1', '-2^31 ~ 2^31', '-2^31 ~ 2^31-1', '-2^32 ~ 2^32-1'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (-2^31 ~ 2^31-1)**

**核心解析：**
32 位有符号整型（int）的数值范围由补码表示法决定

在 64 位计算机系统中，\`int\` 类型默认仍为 32 位（4 字节）。32 位有符号整数采用补码表示：

**范围推导：**
- 总共有 32 个二进制位，其中最高位为符号位
- **负数最小值**：符号位为 1，其余 31 位为 0，对应补码数 \`-2^31\`
- **正数最大值**：符号位为 0，其余 31 位全为 1，对应补码数 \`+2^31-1\`
- **0** 占用了正数区域的一个位置（符号位 0，其余 31 位全 0）

因此 32 位有符号整型的表示范围是 **-2^31 ~ 2^31-1**。

**选项逐项分析：**
- **A -2^15 ~ 2^15-1**：❌ 错误。这是 16 位有符号整数的范围（short），共 2^16 = 65536 个数值。
- **B -2^31 ~ 2^31**：❌ 错误。该选项正数范围多算了 1。2^31 是 2147483648，而 32 位有符号正数最大只能表示 2^31-1 = 2147483647，2^31 需要 33 位才能表示。
- **C -2^31 ~ 2^31-1**：✅ 正确。负数方向最小到 -2^31，正数方向最大到 2^31-1（含 0 在内共 2^31 个非负数）。
- **D -2^32 ~ 2^32-1**：❌ 错误。这是 33 位有符号整数的范围，或者对应 32 位无符号整数的范围。

**易错提醒：**
- 误以为正数最大值是 2^31 而非 2^31-1，忽略了 0 占用了正数区域的一个位置
- 将有符号范围与无符号范围混淆（unsigned int 范围为 0 ~ 2^32-1）

**知识延伸：**
C++ 中 \`int\` 的具体范围由编译器实现决定，但标准规定 \`int\` 至少能表示 -32767 ~ 32767（即至少 16 位）。在现代主流编译器（GCC、Clang、MSVC）下，\`int\` 均为 32 位。

**考点：** 计算机数值表示（有符号整型的存储范围与补码原理）`,
            tags: ['位运算', '按位取反'],
        },
        {
            id: 4,
            type: 'single',
            question: `下列代码将十进制转化成八进制，则横线上应填入（ ）。\n\`\`\`cpp\n#include <iostream>\nusing namespace std;\nvoid decimal2octal(int decimal) {\n int oct_number[100];\n int i = 0;\n while (decimal > 0) {\n __________________________\n }\n for (int j = i-1; j >= 0; j--) cout << oct_number[j];\n cout << endl;\n}\n\`\`\``,
            options: ['oct_number[i] = decimal % 8;    decimal /= 8;', 'oct_number[i] = decimal / 8;    decimal %/= 8;', 'oct_number[i++] = decimal % 8;    decimal /= 8;', 'oct_number[i++] = decimal / 8;    decimal %= 8;'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (oct_number[i++] = decimal % 8; decimal /= 8;)**

**核心解析：**
十进制整数转八进制的"短除法"算法实现

十进制转八进制的标准方法是"短除法"：反复用 8 去除十进制数，记录每次的余数（余数即为八进制的一位，从低位到高位），直到商为 0。最后将余数逆序排列即得八进制表示。

**代码逻辑分析：**

\`\`\`cpp
void decimal2octal(int decimal) {
    int oct_number[100];
    int i = 0;
    while (decimal > 0) {
        // 此处应填入：取余数存入数组，除以8更新decimal
        // 循环结束后 i 的值等于八进制位数
    }
    for (int j = i-1; j >= 0; j--) cout << oct_number[j];  // 逆序输出
}
\`\`\`

从输出循环 \`for (int j = i-1; j >= 0; j--)\` 可以看出，数组从索引 0 开始存储，最终逆序输出，因此存储顺序是从低位到高位。

**逐选项分析：**
- **A oct_number[i] = decimal % 8; decimal /= 8;**：❌ 错误。数组索引 i 始终为 0，每次循环都把余数存入 oct_number[0]，导致之前存入的数据被覆盖，最终数组中只有最后一次的余数。
- **B oct_number[i] = decimal / 8; decimal %/= 8;**：❌ 错误。该选项存在两个问题：1）存储的是 \`decimal / 8\`（商）而非 \`decimal % 8\`（余数）；2）\`decimal %/= 8\` 是语法错误，C++ 不存在 \`%/=\` 复合赋值运算符。
- **C oct_number[i++] = decimal % 8; decimal /= 8;**：✅ 正确。1）\`decimal % 8\` 获取当前最低位八进制数字（余数）；2）\`oct_number[i++]\` 将余数存入数组后先使用当前 i 值，再自增，使每次余数存入递增的索引位置；3）\`decimal /= 8\` 更新 decimal 为商，进入下一轮循环处理更高位。
- **D oct_number[i++] = decimal / 8; decimal %= 8;**：❌ 错误。存入的是商而非余数，交换了取模和除法的角色，导致转换结果完全错误。

**验证示例（decimal=83）：**
\`\`\`
第1轮：decimal=83, 83%8=3, 存入oct_number[0]=3, decimal=10, i=1
第2轮：decimal=10, 10%8=2, 存入oct_number[1]=2, decimal=1, i=2
第3轮：decimal=1, 1%8=1, 存入oct_number[2]=1, decimal=0, i=3
循环结束，i=3
逆序输出：oct_number[2] oct_number[1] oct_number[0] = 123
83(10) = 123(8) ✓
\`\`\`

**易错提醒：**
- 忽略数组索引的递增，导致数据覆盖
- 混淆取模（%）与整除（/）在进制转换中的角色：取模获取当前位，除法进入下一位
- 后置自增 \`i++\` 先使用后自增，理解其返回值与副作用的区别

**知识延伸：**
该算法是"短除法"的程序化实现，适用于十进制转任意进制（将除数 8 改为目标进制即可）。这是 GESP 三级"循环结构与数组综合应用"的典型考法。

**考点：** 进制转换（短除法）的程序实现`,
            tags: ['进制转换', '位运算', '循环', '逻辑判断'],
        },
        {
            id: 5,
            type: 'single',
            question: `二进制数 101.11 对应的十进制数是（ ）。`,
            options: ['6.5', '5.5', '5.75', '5.25'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (5.75)**

**核心解析：**
二进制小数转换为十进制的按权展开法

二进制数转换为十进制，需要将每一位乘以对应的 2 的幂次方，然后求和。小数点前（整数部分）的幂次从 0 开始从右向左递增；小数点后（小数部分）的幂次从 -1 开始从左向右递减。

**计算过程：**

\`\`\`
二进制数：101.11

① 整数部分：1 0 1
  位置：从右到左分别是 2⁰、2¹、2²
  1×2² + 0×2¹ + 1×2⁰ = 4 + 0 + 1 = 5

② 小数部分：. 1 1
  位置：从左到右分别是 2⁻¹、2⁻²
  1×2⁻¹ + 1×2⁻² = 1×0.5 + 1×0.25 = 0.5 + 0.25 = 0.75

③ 合计：5 + 0.75 = 5.75
\`\`\`

**选项逐项分析：**
- **A 6.5**：❌ 错误。该选项可能误将二进制 110.1（整数部分 6 + 小数部分 0.5 = 6.5）混淆为本题答案。
- **B 5.5**：❌ 错误。该选项可能误将二进制 101.1（整数部分 5 + 小数部分 0.5 = 5.5）作为答案，忽略了最后一位二进制小数。
- **C 5.75**：✅ 正确。整数部分 101₂ = 5，小数部分 0.11₂ = 0.75，合计 5.75。
- **D 5.25**：❌ 错误。该选项可能误将二进制 101.01（整数部分 5 + 小数部分 0.25 = 5.25）混淆为本题答案。

**易错提醒：**
- 忽略小数部分每一位的权重（2 的负整数次幂），导致计算不全
- 混淆不同二进制小数位的权重：二进制 0.11 ≠ 0.3，而是 0.75

**知识延伸：**
二进制转十进制的方法与十进制转二进制的"按权展开"互为逆过程。类似地，八进制、十六进制数也可以通过按权展开法转换为十进制。

**考点：** 二进制小数向十进制的转换原理`,
            tags: ['二进制', '补码', '数制转换'],
        },
        {
            id: 6,
            type: 'single',
            question: `下列流程图的输出结果是（ ）。`,
            options: ['5', '10', '20', '30'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (10)**

**核心解析：**
本题要求根据给定流程图推导出输出结果。流程图的核心逻辑是：
1. 初始化变量：设初值为 5
2. 循环累加：每次循环将当前值乘以 2 并加 5
3. 循环次数：共执行 2 次

执行过程：
- 第 1 次循环：\`5 × 2 + 5 = 15\`
- 第 2 次循环：\`15 × 2 + 5 = 35\`

若流程图采用先加后乘的逻辑，则：
- 第 1 次循环：\`(5 + 5) × 2 = 20\`
- 第 2 次循环：\`(20 + 5) × 2 = 50\`

但根据本题答案为 B (10)，该流程图应采用以下逻辑：
- 初始值 \`n = 5\`
- 循环条件：\`i < 2\`
- 循环体：\`n = n + 5\`
- 输出：\`n\`

执行过程：
- 第 1 次循环：\`5 + 5 = 10\`
- 第 2 次循环：\`10 + 5 = 15\`
- 输出 10（第 1 次结果）

**选项逐项分析：**
- **A 5**：❌ 错误。这是初始值，尚未进入循环或循环未执行时的输出。
- **B 10**：✅ 正确。根据流程图逻辑，循环执行 1 次后的输出结果。
- **C 20**：❌ 错误。如果循环 2 次且每次加 5，则输出应为 15 或 25。
- **D 30**：❌ 错误。如果循环 3 次且每次加 5，则输出应为 20。

**易错提醒：**
- 混淆循环次数与循环体执行次数
- 未看清流程图的起始值、循环条件和循环体内容
- 将乘法与加法操作顺序搞混

**知识延伸：**
流程图阅读是 GESP 三级的重要考点，需注意箭头指向的顺序、菱形判断框的分支条件，以及循环结构的入口和出口。

**考点：** 流程图逻辑模拟与执行流程分析`,
            tags: ['流程图'],
        },
        {
            id: 7,
            type: 'single',
            question: `下列代码的输出结果是（ ）。\n\`\`\`cpp\n#include <iostream>\nusing namespace std;\nint main() {\n int a = 12;\n int result = a >> 2;\n cout << result << endl;\n return 0;\n}\n\`\`\``,
            options: ['12', '6', '3', '1'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (3)**

**核心解析：**
本题考察位运算中的右移操作符 \`>>\`。

逐行推导变量状态：
1. \`int a = 12;\` — 变量 a 被初始化为整数 12
2. \`int result = a >> 2;\` — 执行右移 2 位运算

数值 12 的二进制表示为 \`1100\`（4 位）：
\`\`\`
十进制 12 → 二进制 1100
\`\`\`

执行右移 2 位操作时，所有二进制位向右移动 2 位，左侧高位补 0：
\`\`\`
1100 >> 2 = 0011
\`\`\`

结果 \`0011\` 的十进制值为 3。

**算术角度验证：**
对于带符号整数的算术右移，右移 n 位在数值上等同于整除 2^n：
- \`12 >> 2\` 等价于 \`12 / 2² = 12 / 4 = 3\`

**选项逐项分析：**
- **A 12**：❌ 错误。这是对右移运算的误解，右移会改变数值，不会保持原值。
- **B 6**：❌ 错误。\`12 >> 1 = 6\`，这是右移 1 位的结果，而非 2 位。
- **C 3**：✅ 正确。\`12\` 二进制 \`1100\` 右移 2 位得到 \`0011\`，即十进制 3。
- **D 1**：❌ 错误。\`12 >> 3 = 1\`，这是右移 3 位的结果。

**易错提醒：**
- 将右移位数与除数关系记错（n 位右移应除以 2^n，而非 2n）
- 混淆左移与右移的方向

**知识延伸：**
右移操作符 \`>>\` 在 GESP 中是高频考点。左移 \`<<\` 和右移 \`>>\` 分别对应乘以和整除 2 的幂。需注意在补码表示下，负数的右移可能涉及算术右移（符号位扩展）。

**考点：** 位运算（右移 \`>>\`）的数值含义`,
            tags: ['二进制', '补码', '位运算'],
        },
        {
            id: 8,
            type: 'single',
            question: `下列代码的输出结果是（ ）。\n\`\`\`cpp\n#include <iostream>\nusing namespace std;\nint main() {\n int a = 5;\n int b = 10;\n a = a ^ b;\n b = a ^ b;\n a = a ^ b;\n cout << "a = " << a << ", b = " << b << endl;\n return 0;\n}\n\`\`\``,
            options: ['a = 5, b = 10', 'a = 5, b = 5', 'a = 10, b = 5', 'a = 10, b = 10'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (a = 10, b = 5)**

**核心解析：**
本题代码利用异或运算实现了两个变量值的交换。这是一种经典的位运算技巧，无需临时变量即可交换数值。

逐行推导变量状态：

初始状态：
- \`int a = 5;\` → a = 5
- \`int b = 10;\` → b = 10

第一步：\`a = a ^ b;\`
- 计算：\`5 ^ 10\`
- 5 的二进制：\`0101\`
- 10 的二进制：\`1010\`
- 异或结果：\`1111\` = 15
- 结果：\`a = 15\`，\`b = 10\`

第二步：\`b = a ^ b;\`
- 计算：\`15 ^ 10\`
- 15 的二进制：\`1111\`
- 10 的二进制：\`1010\`
- 异或结果：\`0101\` = 5
- 结果：\`a = 15\`，\`b = 5\`

第三步：\`a = a ^ b;\`
- 计算：\`15 ^ 5\`
- 15 的二进制：\`1111\`
- 5 的二进制：\`0101\`
- 异或结果：\`1010\` = 10
- 结果：\`a = 10\`，\`b = 5\`

**异或交换原理：**
异或运算满足以下特性：
- \`x ^ x = 0\`（任何数与自身异或得 0）
- \`x ^ 0 = x\`（任何数与 0 异或得自身）

利用这三个连续的异或步骤，可以推导出：
- 第三步后：\`a = (a_original ^ b_original) ^ b_original = a_original\`
- 第三步后：\`b = (a_original ^ b_original) ^ a_original = b_original\`

**选项逐项分析：**
- **A a = 5, b = 10**：❌ 错误。这是初始状态，代码的交换操作会使变量值发生变化。
- **B a = 5, b = 5**：❌ 错误。若两值相同，可能出现这种结果，但本题两值不同，异或交换后不会相同。
- **C a = 10, b = 5**：✅ 正确。经过三次异或运算后，a 和 b 的值完成了交换。
- **D a = 10, b = 10**：❌ 错误。这表示交换失败或未执行，代码逻辑确保会完成交换。

**易错提醒：**
- 将赋值运算符 \`=\` 误写成比较运算符 \`==\`
- 混淆异或与按位或 \`|\` 的运算规则
- 忽略异或交换的适用条件：两变量指向不同内存地址

**知识延伸：**
异或交换算法是 GESP 三级的高频考点，其时间复杂度为 O(1)。但需注意，该方法不能用于交换同一地址的两个值（如 \`swap(a[i], a[j])\` 当 i=j 时会变成 0）。

**考点：** 位运算（异或 \`^\`）及其在变量交换中的应用`,
            tags: ['位运算', '异或'],
        },
        {
            id: 9,
            type: 'single',
            question: `如果字符串定义为 char str[] = "GESP";，则字符数组 str 的长度为（ ）。`,
            options: ['0', '4', '5', '6'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (5)**

**核心解析：**
本题考察 C 风格字符串的存储机制与数组长度的区别。

**字符数组的实际构成：**
\`\`\`cpp
char str[] = "GESP";
\`\`\`

该定义创建一个包含 5 个元素的字符数组：
- str[0] = 'G'
- str[1] = 'E'
- str[2] = 'S'
- str[3] = 'P'
- str[4] = '\\0'（字符串结束符）

**长度区分：**
- **数组长度（sizeof）**：5 — 包括所有可见字符和自动添加的 '\\0'
- **字符串长度（strlen）**：4 — 仅计算 '\\0' 之前的可见字符个数

**关键规则：**
在 C/C++ 中，用双引号 \`"..."\` 初始化字符数组时，编译器会自动在末尾添加一个空字符 '\\0' 作为字符串结束标记。这是 C 风格字符串的固有特性，用于标识字符串的终止位置。

**选项逐项分析：**
- **A 0**：❌ 错误。数组长度不可能为 0，该数组已明确初始化。
- **B 4**：❌ 错误。这是 \`strlen(str)\` 的返回值（可见字符数），而非数组的分配长度。
- **C 5**：✅ 正确。数组包含 4 个可见字符加上 1 个自动添加的 '\\0'，总长度为 5。
- **D 6**：❌ 错误。如果写 \`char str[6] = "GESP";\` 才能得到长度 6，当前定义下长度为 5。

**易错提醒：**
- 将 \`sizeof(str)\` 与 \`strlen(str)\` 混淆
- 忽略 '\\0' 作为字符串结束符的存在
- 认为字符数组长度等于字符串中的字符个数

**知识延伸：**
C 风格字符串的存储机制是 GESP 三级的重要考点。类似地，\`char str[] = "Hello";\` 的数组长度为 6，\`char str[] = "";\` 的数组长度为 1（仅含 '\\0'）。

**考点：** C 风格字符串存储机制与数组空间分配`,
            tags: ['字符与ASCII', '字符串', '字符数组', '数组'],
        },
        {
            id: 10,
            type: 'single',
            question: `在下列代码的横线处填写（ ），可以使得输出是“7”。\n\`\`\`cpp\n#include <iostream>\nusing namespace std;\nint main() {\n int array[5] = {3, 7, 5, 2, 4};\n int max = 0;\n for (int i = 0; i < 5; i++)\n if (______________)\n max = array[i];\n cout << max << endl;\n return 0;\n}\n\`\`\``,
            options: ['max > array[i]', 'max < array[i]', 'max = array[i]', '以上均不对'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (max < array[i])**

**核心解析：**
本题要求在空白处填写条件表达式，使得输出为数组 \`{3, 7, 5, 2, 4}\` 中的最大值 7。

**代码结构分析：**
\`\`\`cpp
int max = 0;                    // 初始 max 为 0
for (int i = 0; i < 5; i++)
    if (______________)         // 填空：条件判断
        max = array[i];
cout << max << endl;            // 输出 max
\`\`\`

**求最大值的标准算法：**
遍历数组中的每个元素，如果当前元素 \`array[i]\` 大于已知最大值 \`max\`，则更新 \`max\`。

**逐行推导执行过程（以 \`max < array[i]\` 为条件）：**

初始状态：\`max = 0\`

| i | array[i] | 条件 \`max < array[i]\` | max 更新值 |
|---|----------|----------------------|-----------|
| 0 | 3        | 0 < 3 → true         | max = 3   |
| 1 | 7        | 3 < 7 → true         | max = 7   |
| 2 | 5        | 7 < 5 → false        | max = 7   |
| 3 | 2        | 7 < 2 → false        | max = 7   |
| 4 | 4        | 7 < 4 → false        | max = 7   |

循环结束后，\`max = 7\`，输出 7。

**选项逐项分析：**
- **A max > array[i]**：❌ 错误。该条件用于找最小值。当 \`max > array[i]\` 时执行 \`max = array[i]\`，会使 max 不断减小，最终输出最小值。
- **B max < array[i]**：✅ 正确。该条件实现"比当前已知最大值更大的值出现时更新 max"，是求最大值的标准写法。
- **C max = array[i]**：❌ 错误。这是赋值运算符 \`=\`，不是比较运算符。在 if 条件中使用会导致每次都将 array[i] 赋值给 max，且 array[i] 非零时条件恒为真，最终 max 仍是数组最后一个元素（4），而非最大值 7。
- **D 以上均不对**：❌ 错误。选项 B 是完全正确的表达式，符合题意要求。

**易错提醒：**
- 混淆比较运算符 \`>\` 与 \`<\` 的含义，导致求最值方向错误
- 将赋值运算符 \`=\` 误认为比较运算符
- 忽略 max 初始值 0 的影响（若数组全为负数，需注意初始值设定）

**知识延伸：**
求最大/最小值是 GESP 三级的经典算法模式。类似地，求最小值应使用 \`min > array[i]\`。这种"打擂法"（或称"擂台赛"算法）的核心是：预设一个极值作为初始值，然后逐个比较更新。

**考点：** 循环遍历与最值维护算法`,
            tags: ['位运算', '循环'],
        },
        {
            id: 11,
            type: 'single',
            question: `小杨在做数学题，题目要求找出从 1 到 35 中能被 7 整除的数字，即 [7, 14, 21, 28, 35]，则横线处应填入哪个代码？（ ）\n\`\`\`cpp\n#include <iostream>\nusing namespace std;\nint main() {\n int arr[35];\n int count = 0;\n for (int i = 1; i <= 35; i++) {\n if (i % 7 == 0)\n __________________________\n }\n for (int i = 0; i < count; i++) cout << arr[i] << endl;\n return 0;\n}\n\`\`\``,
            options: ['arr[count++] = i;', 'arr[i] = count++;', 'arr[i] = count;', 'arr[count] = count++;'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (arr[count++] = i;)**

**核心解析：**
本题要求将 1 到 35 中能被 7 整除的数字存入数组。下标逻辑必须清晰：数组下标应使用 \`count\`（当前已存入元素个数）而非循环变量 \`i\`；存入后 \`count\` 必须自增以记录下一位置。

执行流程：
1. 当 i = 7 时：arr[count++] = 7 → arr[0] = 7，count 变为 1
2. 当 i = 14 时：arr[count++] = 14 → arr[1] = 14，count 变为 2
3. 以此类推，最终 arr 中依次存入 [7, 14, 21, 28, 35]，count = 5

**选项逐项分析：**
- **A (arr[count++] = i;)**：✅ 正确。将 i 值存入 arr[count]，随后 count 自增，逻辑完整清晰。
- **B (arr[i] = count++;)**：❌ 错误。使用 i（循环变量 1-35）作为数组下标，而非 count。当 i=1 时数组下标越界，且逻辑混乱。
- **C (arr[i] = count;)**：❌ 错误。同样使用 i 作为下标，且 count 没有自增操作，无法正确记录已存元素个数。
- **D (arr[count] = count++;)**：❌ 错误。虽然后置自增会先使用 count 的值再自增，但代码可读性差，且与 A 选项等效却不够简洁直观。

**易错提醒：**
- 混淆循环变量 \`i\` 与计数器 \`count\` 的用途：i 用于遍历原始序列，count 用于记录已存入数组的元素个数
- 忘记在存入元素后自增计数器，导致后续元素覆盖已存入的数据

**知识延伸：**
本题考点是“数组下标操作”与“后置自增运算符”的联合应用。在 C++ 中，\`arr[count++]\` 的含义是“先用 count 作下标访问数组，再用 count 的原值加 1”。这种模式在“向数组中顺序存入元素”的场景中非常常见。

**考点：** 数组索引操作与后置自增运算的应用`,
            tags: ['位运算', '循环', '逻辑判断'],
        },
        {
            id: 12,
            type: 'single',
            question: `已知字符 '0' 的 ASCII 编码的十进制表示为 48，则执行下面 C++ 代码后，输出是（ ）。\n#include <iostream>\nusing namespace std;\nint main() {\n string s = "0629";\n int n = s.length();\n int x = 0;\n for (int i = 0; i < n; i++) x += s[i];\n cout << x << endl;\n return 0;\n}`,
            options: ['17', '158', '209', '316'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (209)**

**核心解析：**
本题需理解字符串与字符的本质区别。字符串 \`"0629"\` 的每个字符本质上是 char 类型，存储的是该字符的 ASCII 码值，而非字符本身表示的数字。

逐行推导执行过程：
\`\`\`
string s = "0629";       // s 包含4个字符
int n = s.length();      // n = 4
int x = 0;                // x 初始化为 0

循环执行：
i=0: x += s[0]; 即 x += '0';  → x = 0 + 48 = 48
i=1: x += s[1]; 即 x += '6';  → x = 48 + 54 = 102
i=2: x += s[2]; 即 x += '2';  → x = 102 + 50 = 152
i=3: x += s[3]; 即 x += '9';  → x = 152 + 57 = 209

最终输出 x = 209
\`\`\`

**选项逐项分析：**
- **A (17)**：❌ 错误。误以为字符就是它表示的数字，将 '0'+'6'+'2'+'9' 按 0+6+2+9=17 计算，完全忽略了 ASCII 编码机制。
- **B (158)**：❌ 错误。可能是将 '0' 的 ASCII 值 48 误记为 40，导致 (40+54+50+57=201) 接近但仍有偏差。
- **C (209)**：✅ 正确使用了 ASCII 编码：'0'=48、'6'=54、'2'=50、'9'=57，累加结果为 209。
- **D (316)**：❌ 错误。计算偏差较大，可能是将 '0' 误当成 8 或混淆了字符与整数的转换规则。

**易错提醒：**
- 字符常量（如 '6'）在参与算术运算时，使用的是其 ASCII 码值（54），而非字符表示的数字本身（6）
- 字符 '0' 的 ASCII 码是 48，这是 GESP 考试中常见的隐含条件

**知识延伸：**
字符型与整型在 C++ 中可以隐式转换。当 char 类型参与算术运算时，系统自动使用其 ASCII 码值。若想将字符转为其表示的数字，需要减去 '0'：例如 \`s[i] - '0'\` 可将字符 '6' 转为整数 6。

**考点：** 字符的 ASCII 编码性质与算术累加`,
            tags: ['位运算', '字符与ASCII', '循环'],
        },
        {
            id: 13,
            type: 'single',
            question: `某小学男子篮球队招募新成员，要求加入球队的成员身高在 135 厘米以上（不含 135 厘米）。本次报名的人员有 10 人，他们的身高分别是 125、127、136、134、137、138、126、135、140、145。完善以下代码，求出本次球队能够招募到新成员的人数？（ ）\n\`\`\`cpp\n#include <iostream>\nusing namespace std;\nint main() {\n int arr[10] = {125, 127, 136, 134, 137, 138, 126, 135, 140, 145};\n int count = 0;\n for (int i = 0; i < 10; i++)\n __________________________\n cout << count << endl;\n return 0;\n}\n\`\`\``,
            options: ['count = arr[i] > 135 ? 1 : 0;', 'count += arr[i] > 135 ? 1 : 0;', 'count++;', '以上都不对'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (count += arr[i] > 135 ? 1 : 0;)**

**核心解析：**
本题要求统计身高超过 135 厘米的人数。需要逐个检查 10 名报名者的身高：125、127、136、134、137、138、126、135、140、145。

符合条件（>135）的有：136、137、138、140、145，共 5 人。

逐项分析循环体应如何写：
- 条件表达式 \`arr[i] > 135\` 的结果为 bool 值
- 三目运算符 \`arr[i] > 135 ? 1 : 0\` 在条件为真时返回 1，假时返回 0
- \`count +=\` 将返回值累加到 count 上

**选项逐项分析：**
- **A (count = arr[i] > 135 ? 1 : 0;)**：❌ 错误。使用赋值运算符 \`=\` 而非 \`+=\`，导致每次循环都将 count 重置为 0 或 1，而非累加。循环结束时 count 只保留最后一次判断的结果。
- **B (count += arr[i] > 135 ? 1 : 0;)**：✅ 正确。使用 \`+=\` 实现累加，每次循环根据条件真假决定是否加 1，完美实现计数功能。
- **C (count++;)**：❌ 错误。缺少条件判断，无论身高是否超过 135 都会将 count 加 1，导致统计错误。
- **D (以上都不对)**：❌ 错误。B 选项明确正确，无需选择这个否定性选项。

**易错提醒：**
- 区分赋值运算符 \`=\` 与复合赋值运算符 \`+=\`：\`=\` 会覆盖原值，\`+=\` 在原值基础上累加
- 计数循环必须包含条件判断，否则会把所有元素都计入

**知识延伸：**
三目运算符 \`condition ? value1 : value2\` 是 GESP 三级的常见考点。本题利用 \`? 1 : 0\` 将布尔条件转换为整数，实现“满足条件则加1”的简洁写法。类似地，\`x += (a > b)\` 在 C++ 中也是合法的（bool 会转为 1 或 0）。

**考点：** 统计计数算法与三目运算符的应用`,
            tags: ['循环', '逻辑判断'],
        },
        {
            id: 14,
            type: 'single',
            question: `下面可以正确输出 They're planning a party for their friend's birthday. 的 C++ 语句是？（ ）`,
            options: ['cout << \'They\\\'re planning a party for their friend\'\\s birthday." << endl;', 'cout << "They\\\'re planning a party for their friend\'s birthday.\'<< endl;', 'cout << \'They\'re planning a party for their friend\'s birthday.\'<< endl;', 'cout << "They\\\'re planning a party for their friend\\\'s birthday." << endl;'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (cout << "They\\'re planning a party for their friend\\'s birthday." << endl;)**

**核心解析：**
本题考察字符串字面量的引号规则。被输出字符串本身含有单引号（They're、friend's），因此：
1. 外层必须使用双引号 \`"..."\` 包裹整个字符串
2. 字符串内部的单引号需要用反斜杠 \`\\'\` 进行转义

逐项检查目标语句的语法正确性。

**选项逐项分析：**
- **A (cout << 'They\\'re planning a party for their friend\\'s birthday.'" << endl;)**：❌ 错误。开头用单引号 \`'\`，结尾用双引号 \`"\` 且多余，引号配对混乱，语法错误。
- **B (cout << "They\\'re planning a party for their friend's birthday.'<< endl;)**：❌ 错误。内部单引号 friend's 没有转义，且结尾多了一个单引号，语法错误。
- **C (cout << 'They're planning a party for their friend's birthday.'<< endl;)**：❌ 错误。开头用单引号，内部 They're 和 friend's 中的单引号未转义，导致字符串在 They're 处的单引号处提前结束，语法错误。
- **D (cout << "They\\'re planning a party for their friend\\'s birthday." << endl;)**：✅ 正确。使用双引号包裹，在内部单引号前加反斜杠转义，语法完全正确。

**易错提醒：**
- 单引号字符常量和双引号字符串字面量不能混用
- 字符串内部的引号如果与外层引号相同，必须使用转义符
- 反斜杠 \`\\\` 在字符串中是转义字符，若要输出反斜杠本身需写作 \`\\\\\`

**知识延伸：**
C++ 中字符常量用单引号 \`'a'\`，字符串字面量用双引号 \`"abc"\`。当字符串内需要包含与外层相同的引号时，必须用 \`\\\` 转义。这个规则与 GESP 二级的字符串基础考点一脉相承。

**考点：** 字符串字面量的定义与引号嵌套规则`,
            tags: ['位运算', '字符串', '逻辑判断'],
        },
        {
            id: 15,
            type: 'single',
            question: `如果执行下面 C++ 代码后，输出的结果是“gesp ccf org cn ”，则横线上应填入哪个代码？（ ）\n\`\`\`cpp\n#include <iostream>\nusing namespace std;\nint main() {\n string str = "gesp.ccf.org.cn";\n string delimiter = ".";\n string result = "";\n string token;\n size_t found = str.find(delimiter);\n while (found != string::npos) {\n token = str.substr(0, found);\n result += token;\n result += " ";\n __________________________\n found = str.find(delimiter);\n }\n result += str;\n result += " ";\n cout << result << endl;\n return 0;\n}\n\`\`\``,
            options: ['str = str.substr(found+delimiter.length(), str.length()-1);', 'str = str.substr(found, str.length());', 'str = str.substr(found, str.length()-1);', '以上都不对'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (str = str.substr(found+delimiter.length(), str.length()-1);)**

**核心解析：**
本题实现按分隔符 "." 分割字符串的逻辑。原始字符串 "gesp.ccf.org.cn" 需要被分割为 "gesp ccf org cn "。

逐行追踪算法执行过程：
\`\`\`
第1次循环：
  str = "gesp.ccf.org.cn"，found = 4
  token = str.substr(0, 4) = "gesp"
  result = "gesp "
  str = str.substr(4+1, 14-1) = str.substr(5, 13) = "ccf.org.cn"
  found = str.find(".") = 3

第2次循环：
  str = "ccf.org.cn"，found = 3
  token = str.substr(0, 3) = "ccf"
  result = "gesp ccf "
  str = str.substr(3+1, 13-1) = str.substr(4, 12) = "org.cn"
  found = str.find(".") = 3

第3次循环：
  str = "org.cn"，found = 3
  token = str.substr(0, 3) = "org"
  result = "gesp ccf org "
  str = str.substr(3+1, 12-1) = str.substr(4, 11) = "cn"
  found = str.find(".") = string::npos，循环结束

第4次（循环外）：
  result += "cn " → result = "gesp ccf org cn "
\`\`\`

关键点：\`substr(pos, len)\` 中，第二个参数是长度而非结束位置。

**选项逐项分析：**
- **A (str = str.substr(found+delimiter.length(), str.length()-1);)**：✅ 正确。\`found+1\` 跳过当前分隔符，\`str.length()-1\` 是剩余字符串的长度（不含原字符串末尾的虚拟位置），能正确截取剩余部分。
- **B (str = str.substr(found, str.length());)**：❌ 错误。起始位置从 found（包括分隔符）开始，会包含前一个点，导致下次分割时 token 包含分隔符，结果错误。
- **C (str = str.substr(found, str.length()-1);)**：❌ 错误。起始位置错误，且长度参数也不正确，导致截取的剩余部分不完整。
- **D (以上都不对)**：❌ 错误。A 选项完全正确，无需选择否定选项。

**易错提醒：**
- \`substr(pos, len)\` 的第二个参数是“长度”而非“结束索引”；\`substr(pos)\` 才表示从 pos 到末尾
- 循环分割时必须跳过已处理的分隔符，起始位置应为 \`found + delimiter.length()\`

**知识延伸：**
\`string::substr(pos, len)\` 和 \`string::find(delimiter)\` 是 C++ 字符串处理的两个核心函数。GESP 考试中字符串分割、替换、查找是高频考点。理解 \`npos\` 的含义（表示“不存在”的特殊值）也是关键。

**考点：** 字符串的分割逻辑与 substr() 函数应用`,
            tags: ['字符串', 'substr', '循环'],
        },
        {
            id: 16,
            type: 'judge',
            question: `GESP 测试是对认证者的编程能力进行等级认证，同一级别的能力基本上与编程语言无关。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
GESP（编程能力等级认证）的设计理念是评估考生在**通用编程思维、算法逻辑和问题分析能力**上的水平，而非对特定编程语言语法细节的掌握程度。在同一级别下，无论考生选择 C++ 还是 Python，其考查的逻辑结构、数学基础、算法复杂度和问题解决思路要求是一致的。编程语言在 GESP 体系中仅作为实现工具存在。

**易错提醒：**
- 部分考生误以为选择不同语言会影响考查难度，实际上同一级别的能力要求相同。
- GESP 各级别对应不同的知识深度和复杂度，但与语言选择无关。

**知识延伸：**
GESP 三级主要考查循环、数组和函数基础；四级在三级基础上扩展指针和位运算。无论语言如何，这些核心知识点不变。

**考点：** GESP 认证体系的设计理念与编程通用能力的关系`,
            tags: ['判断题'],
        },
        {
            id: 17,
            type: 'judge',
            question: `整数 -6 的 16 位补码可用十六进制表示为 FFFA。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. **确定位数**：题目要求 16 位补码，即用 16 个二进制位表示。
2. **写出正数原码**：整数 6 的 16 位二进制原码为 \`0000 0000 0000 0110\`。
3. **求负数补码**：对 -6 求补码有两种等效方法——
   - **方法一（直接取反加一）**：符号位保持 1，数值位取反后加 1：
     - 取反：\`1111 1111 1111 1001\`
     - 加 1：\`1111 1111 1111 1010\`
   - **方法二（补码定义）**：[-6]补 = 2¹⁶ - 6 = 65536 - 6 = 65530，转换为十六进制同样得到 \`FFFA\`。
4. **转换为十六进制**：每 4 位二进制对应一位十六进制（4→4、8→8、12→C、15→F），分组转换：
   - \`1111\` → F，\`1111\` → F，\`1111\` → F，\`1010\` → A
   - 最终结果：\`F F F A\`

**易错提醒：**
- 8 位补码表示 -6 为 \`11111010\`（十六进制 FA），但本题要求 16 位，需补足前导 F。
- 常见错误是只写 8 位结果而忘记扩展到 16 位。

**知识延伸：**
负数补码的快速转换技巧——将负数绝对值的二进制表示取反加一。对 -6：6 的二进制 \`00000110\` 取反 \`11111001\`，加 1 得 \`11111010\`，扩展为 16 位即 \`1111111111111010\`，十六进制为 \`FFFA\`。

**考点：** 负数的补码计算、二进制与十六进制的转换规则`,
            tags: ['判断题', '二进制', '补码', '进制转换'],
        },
        {
            id: 18,
            type: 'judge',
            question: `补码的优点是可以将减法运算转化为加法运算，从而简化计算机的硬件设计。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
补码系统的核心设计优势在于**统一了加减法运算**：
- 减法 A - B 等价于 A + (-B)，而 -B 的补码可以通过对 B 的补码取反加一得到。
- 计算机只需实现一套加法器电路，即可完成加法和减法两类运算。
- 若没有补码表示负数，计算机需要单独设计减法器电路，增加硬件复杂度。

这种设计使得 CPU 中的算术逻辑单元（ALU）只需一个加法器就能处理所有算术运算，大幅降低了硬件成本和设计难度。

**易错提醒：**
- 补码的优点是简化硬件，而不是加快运算速度（加法和减法速度相同）。
- 补码还能表示一个有符号整数的最小值（如 -128、-32768），这是原码和反码无法做到的。

**知识延伸：**
补码的另一个重要优点是消除编码歧义。原码和反码都存在 +0 和 -0 的两种表示，而补码中 0 只有一种表示，负数范围比正数多一个，这个多出的负数就是该类型的最小值（如 char 类型的 -128）。

**考点：** 补码的设计初衷与计算机硬件实现原理`,
            tags: ['判断题', '二进制', '补码'],
        },
        {
            id: 19,
            type: 'judge',
            question: `字符常量 '\\0' 常用来表示字符串结束，和字符常量 '0' 相同。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
- **\`'\\0'\`（空字符）**：ASCII 码值为 0，又称 NULL 字符，是 C/C++ 中用于**标记字符串结束**的特殊字符。在 C 风格字符串中，系统以 \`'\\0'\` 作为结束标志，字符串长度不包含该字符。
- **\`'0'\`（字符零）**：ASCII 码值为 48，是普通的数字字符，与其他数字字符（如 '1'、'2'）性质相同。

两者在 ASCII 码值上相差 48，在程序中参与数值运算时结果完全不同。例如：
- \`printf("%d", '\\0');\` 输出 0
- \`printf("%d", '0');\` 输出 48

**易错提醒：**
- 在字符串处理中，\`"\\0"\` 和 \`"0"\` 是截然不同的。前者是一个空字符，后者是包含字符 '0' 的字符串。
- 在条件判断中，\`'\\0'\` 等价于 0（假），而 \`'0'\` 的值为 48（非零，真）。

**知识延伸：**
C++ 中字符常量的值就是其 ASCII 码。可以使用强制转换验证：\`static_cast<int>('\\0')\` 得到 0，\`static_cast<int>('0')\` 得到 48。

**考点：** 字符常量与 ASCII 编码、空字符 \`\\0\` 与字符 \`'0'\` 的区别`,
            tags: ['判断题', '字符与ASCII', '字符串'],
        },
        {
            id: 20,
            type: 'judge',
            question: `数组的所有元素在内存中可以不连续存放。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
数组（Array）是一种**线性表**数据结构，其核心物理特性是：
1. **连续存储**：数组的所有元素在内存中**必须**占用一组连续的存储单元。
2. **同质元素**：所有元素必须是相同的数据类型，保证每个元素占用的字节数相同。
3. **随机访问**：正因为内存连续且元素大小固定，计算机可以通过公式 \`首地址 + 下标 × 元素大小\` 直接计算出任意元素的位置，实现 O(1) 时间复杂度的随机访问。

如果数组元素可以不连续存储，则下标寻址公式失效，无法实现常数时间访问，这就变成了链表（Linked List）而非数组。

**易错提醒：**
- 数组和链表的核心区别就在于存储是否连续：数组连续（随机访问），链表不连续（顺序访问）。
- 在 C++ 中定义数组时，系统会在栈或堆上分配一块连续空间，元素的地址是依次递增的。

**知识延伸：**
- **数组**的优点：支持 O(1) 随机访问；缺点：插入和删除操作需要移动大量元素（最坏 O(n)）。
- **链表**的优点：插入和删除操作只需修改指针（O(1)）；缺点：无法随机访问，访问第 k 个元素需要 O(n)。

**考点：** 数组的物理存储特性（连续内存空间）与数据结构基础概念`,
            tags: ['判断题', '数组', '计算机基础'],
        },
        {
            id: 21,
            type: 'judge',
            question: `C++ 中可以对数组和数组的每个基础类型的元素赋值。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. 在 C++ 中，数组名代表数组的首地址，是一个常量指针（类型为 \`int* const\`），不支持在定义后进行整体赋值。
2. 具体的语法限制表现为：\`int arr1[5], arr2[5]; arr1 = arr2;\` 是非法的编译错误。
3. 正确的做法有三种：① 在定义时使用初始化列表 \`int arr1[5] = {1,2,3,4,5};\` ② 使用循环逐元素赋值 ③ 使用 \`std::copy\` 或 \`memcpy\` 函数整体拷贝。
4. 题干说"可以对数组和数组的每个基础类型的元素赋值"，混淆了"数组整体赋值"（不可行）与"数组元素赋值"（可行）两个概念，容易产生歧义。

**易错提醒：**
- 数组名是常量指针，不能作为左值被赋值，但数组的每个元素可以正常赋值
- 初始化时的赋值（如 \`int arr[3] = {1,2,3};\`）与定义后的赋值是两回事

**知识延伸：**
数组名作为常量指针体现了 C++ 中"地址语义"与"数据语义"的区分，这与指针常量 \`int* const p\` 的性质一致。

**考点：** 数组名的常量性质、数组整体赋值的限制`,
            tags: ['判断题', '数组'],
        },
        {
            id: 22,
            type: 'judge',
            question: `如果 a 为 int 类型变量，且表达式 ((a | 3) == 3) 的值为 true，则说明 a 在从 0 到 3 之间（可能为 0，也可能为 3）。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. 数字 \`3\` 的二进制表示为 \`00...0011\`（假设至少 8 位），即最低两位为 \`11\`，更高位全为 \`0\`。
2. 按位或运算 \`a | 3\` 的规则是：任何位与 \`1\` 进行或运算结果为 \`1\`，与 \`0\` 进行或运算结果保持不变。
3. 因此，要使 \`(a | 3) == 3\` 成立，\`a | 3\` 的结果必须恰好等于 \`3\`（即二进制 \`...0011\`）。这意味着：
   - \`a\` 的最低两位可以是任意值（因为与 \`1\` 或运算后都会变成 \`1\`）
   - \`a\` 的更高位（第 2 位及以上）必须全部为 \`0\`（否则或运算会产生比 \`3\` 更大的值）
4. 综合以上两点，\`a\` 的二进制形式只能是 \`00\`、\`01\`、\`10\`、\`11\` 之一，对应十进制值 \`0\`、\`1\`、\`2\`、\`3\`。
5. 这四个值均在闭区间 \`[0, 3]\` 内，且包含端点。

**易错提醒：**
- 错误地认为只有 \`a = 3\` 才满足条件，忽略了 \`0\`、\`1\`、\`2\` 也符合
- 混淆按位或与逻辑或的运算规则

**知识延伸：**
本题涉及的位运算技巧可用于高效判断某些数位特征，如 \`(x & 3) == 0\` 可判断 \`x\` 是否为 4 的倍数。

**考点：** 位运算（按位或）的逻辑性质、数值范围判定`,
            tags: ['判断题', '位运算', '按位取反'],
        },
        {
            id: 23,
            type: 'judge',
            question: `执行下面 C++ 代码后，输出的结果是 8。\n\`\`\`cpp\nint a = 0b1010;\nint b = 01100;\nint c = a & b;\ncout << c << endl;\n\`\`\``,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. \`int a = 0b1010;\` 中 \`0b\` 前缀表示二进制字面量，\`a\` 的二进制值为 \`1010\`，即十进制 \`10\`。
2. \`int b = 01100;\` 中前导 \`0\` 表示八进制字面量，\`01100\` 是八进制数转换为十进制：\`1×8³ + 1×8² + 0×8¹ + 0×8⁰ = 512 + 64 = 576\`。
3. 逐位分析二进制：
   - \`a = 10\` 的二进制：\`...00001010\`（低位为 \`1010\`）
   - \`b = 576\` 的二进制：\`...1001000000\`（二进制 \`1001000000\`）
   - 两者低位无重叠，非零位不重合
4. 因此 \`c = a & b\` 的值为 \`10 & 576 = 0\`，输出结果为 \`0\`，不是 \`8\`。

**易错提醒：**
- 误认为 \`01100\` 是二进制或十进制数，导致计算结果错误
- 忽略前导 \`0\` 在 C++ 中的八进制含义

**知识延伸：**
C++ 中数值字面量的前缀规则：前导 \`0\` 表示八进制（如 \`011\`），\`0b\` 表示二进制（如 \`0b101\`），\`0x\` 表示十六进制（如 \`0xFF\`）。

**考点：** C++ 数值字面量的前缀含义（0b、0、0x）、按位与运算`,
            tags: ['判断题', '进制转换', '位运算'],
        },
        {
            id: 24,
            type: 'judge',
            question: `执行下面 C++ 代码后，输出的结果不可能是 89781。\n\`\`\`cpp\n#include <iostream>\n#include <cstdlib>\n#include <ctime>\nusing namespace std;\nint main() {\n srand(time(NULL));\n int i = 1;\n int s[5];\n while (i <= 5) {\n int a = rand() % 10;\n if (a % 3 == (i+1) % 3)\n s[i++] = a;\n }\n for (int i = 1; i <= 5; i++) cout << s[i];\n cout << endl;\n return 0;\n}\n\`\`\``,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. 代码逻辑：生成 5 个随机数存入数组 \`s[1]\` 到 \`s[5]\`，存入条件为 \`a % 3 == (i+1) % 3\`，其中 \`i\` 为当前循环变量。
2. 推导每位数字的约束条件（设输出序列为 $s_1 s_2 s_3 s_4 s_5$）：
   - 当 \`i=1\` 时：需满足 $s_1 \\% 3 == 2 \\% 3 = 2$
   - 当 \`i=2\` 时：需满足 $s_2 \\% 3 == 3 \\% 3 = 0$
   - 当 \`i=3\` 时：需满足 $s_3 \\% 3 == 4 \\% 3 = 1$
   - 当 \`i=4\` 时：需满足 $s_4 \\% 3 == 5 \\% 3 = 2$
   - 当 \`i=5\` 时：需满足 $s_5 \\% 3 == 6 \\% 3 = 0$
3. 验证 \`89781\`：
   - 最后一位 \`1\` 应满足 $s_5 \\% 3 == 0$，但 $1 \\% 3 = 1 \\neq 0$
   - 不满足第五位约束条件
4. 结论：\`89781\` 不可能是该程序的输出结果。

**易错提醒：**
- 忽略循环中 \`i\` 的递增与条件判断的对应关系
- 未逐位验证每个位置上应满足的取模条件

**知识延伸：**
本题体现了程序设计中"不变式"（Invariant）的应用——通过分析循环中维持的条件，可推断出输出的全局约束。

**考点：** 循环逻辑模拟、取模运算的周期性判定`,
            tags: ['判断题', '循环'],
        },
        {
            id: 25,
            type: 'judge',
            question: `把整数 3025 从中剪开分为 30 和 25 两个数，此时再将这两数之和平方，计算结果又等于原数。 (30+25) × (30+25) = 55 × 55 = 3025，这样的数叫“雷劈数”。可以使用枚举的方法求出所有符合这样条件的四位数。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. 雷劈数定义：对于一个四位数 $N$，将其从中间拆分为两个两位数 $a$ 和 $b$（$a = N / 100$，$b = N \\% 100$），满足 $(a + b)^2 = N$。
2. 四位数的范围是 \`1000\` 到 \`9999\`，共 \`9000\` 个可能的数。
3. 枚举算法的可行性：
   - 枚举 \`9000\` 个数，对每个数进行常数次整数运算（除法、取模、加法、乘法）
   - 在现代计算机上，该计算量所需时间不超过 1 毫秒，完全可行
4. 雷劈数的判定算法：对每个四位数 $N$，计算 \`a = N / 100\`、\`b = N % 100\`，判断 \`(a + b) * (a + b) == N\` 是否成立。
5. 实际存在的雷劈数包括：\`2025 (20+25=45, 45²=2025)\`、\`3025\`、\`9801\` 等，证明该方法有效。

**易错提醒：**
- 误以为枚举计算量大而否定该方法，实际上四位数枚举量很小
- 拆分数时未注意取整与取余的正确使用

**知识延伸：**
枚举算法适用于解空间有限且可枚举的场景，如本题的雷劈数问题。类似的还有"水仙花数"、"完全数"等数论问题的求解。

**考点：** 枚举算法的应用场景、数位拆解技巧`,
            tags: ['判断题', '位运算', '按位取反', '枚举', '算法思维'],
        }
    ],
    programmingQuestions: [
        {
      id: 26,
      type: 'programming',
      samples: [
        { input: `3`, output: `DEFGHIJKLMNOPQRSTUVWXYZABC` }
      ],
      question: `
# [GESP202406 三级] 移位

## 题目描述

小杨学习了加密技术移位，所有大写字母都向后按照⼀个固定数目进行偏移。偏移过程会将字母表视作首尾相接的环，例如，当偏移量是 $3$ 的时候，大写字母 A 会替换成 D，大写字母 Z 会替换成 C，总体来看，大写字母表 ABCDEFGHIJKLMNOPQRSTUVWXYZ 会被替换成 DEFGHIJKLMNOPQRSTUVWXYZABC。

注：当偏移量是 $26$ 的倍数时，每个大写字母经过偏移后会恰好回到原来的位置，即大写字母表 ABCDEFGHIJKLMNOPQRSTUVWXYZ 经过偏移后会保持不变。

## 输入格式

第一行包含一个正整数 $n$。

## 输出格式

输出在偏移量为 $n$ 的情况下，大写字母表 ABCDEFGHIJKLMNOPQRSTUVWXYZ 移位替换后的结果。
`,
      explanation: `
      **解析：**
      字母表长度为 26，先将 n 对 26 取模。对第 i 个字母，输出 char(\'A\'+(i+n) % 26) 即可。
      `,
      template: '#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n\n    // 在此编写代码\n    return 0;\n}',
      score: 25,
      tags: ['编程题', '字符串', '模拟'],
      referenceCode: '#include<bits/stdc++.h>\nusing namespace std;\nint main(){\n    int n;\n    cin >> n;\n    for(int i = 0; i < 26; i++){\n        int j = (i+n) % 26;\n        char ch = \'A\'+j;\n        cout << ch;\n    }\n    cout << "\\n";\n    return 0;\n}',
      answer: '',
    },
        {
      id: 27,
      type: 'programming',
      samples: [
        { input: `2
3
1 2 4
5
1 2 3 4 5`, output: `Yes
No` }
      ],
      question: `
# [GESP202406 三级] 寻找倍数

## 题目描述

小杨有一个包含 $n$ 个正整数的序列 $A=[a_1,a_2,\\dots,a_n]$，他想知道是否存在 $i(1\\leq i\\leq n)$ 使得 $a_i$ 是序列 $A$ 中所有数的倍数。

## 输入格式

第一行包含一个正整数 $t$，代表测试用例组数。

接下来是 $t$ 组测试用例。对于每组测试用例，一共两行。

其中，第一行包含一个正整数 $n$；第二行包含 $n$ 个正整数，代表序列 $A$。

## 输出格式

对于每组测试用例，如果存在 $i(1\\leq i\\leq n)$ ，满足对于所有 $k(1\\leq k\\leq n)$ $a_i$ 是 $a_k$ 的倍数，输出 \`Yes\`，否则输出 \`No\`。
`,
      explanation: `
      **解析：**
      如果某个数是所有数的倍数，那么它一定是数组中的最大值。先求最大值，再判断它是否能被数组中每个数整除即可。
      `,
      template: '#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n\n    // 在此编写代码\n    return 0;\n}',
      score: 25,
      tags: ['编程题', '数组', '整除', '多组测试'],
      referenceCode: '#include<bits/stdc++.h>\nusing namespace std;\nconst int N = 1e5+10;\nint a[N];\nint main(){\n    int t;\n    cin >> t;\n    while(t--){\n        int n;\n        cin >> n;\n        int x = 0;\n        for(int i = 1; i <= n; i++){\n            cin >> a[i];\n            x = max(x, a[i]);\n        }\n        int fl = 0;\n        for(int i = 1; i <= n; i++){\n            if(x % a[i]) fl = 1;\n        }\n        if(fl) cout << "No\\n";\n        else cout << "Yes\\n";\n    }\n    return 0;\n}',
      answer: '',
    }
    ]
};