// 2023年6月 GESP C++ 三级真题
export const paperData = {
  id: '2023-06-l3',
  title: '2023年6月 GESP C++ 三级真题',
  level: 3,
  year: 2023,
  month: 6,
  session: 2,
  timeLimit: 5400,
  source: {
    officialPdf: 'https://gesp.ccf.org.cn/101/attach/1569361659691040.pdf',
    type: 'official-analysis',
    notes: '2023-06 三级目前对照的是 CCF 官方真题解析 PDF，而非单独试卷 PDF。',
  },
  questions: [
    {
      id: 1,
      type: 'single',
      question: `高级语言编写的程序需要经过以下（ ）操作，可以生成在计算机上运行的可执行代码。`,
      options: ['编辑', '保存', '调试', '编译'],
      answer: 3,
      score: 2,
            explanation: `**答案：D (编译)**

**核心解析：**
高级语言的源代码需要经过编译才能生成机器可执行代码。编译过程包括词法分析、语法分析、语义分析、优化和目标代码生成等阶段，最终将人类可读的源代码翻译成计算机CPU能直接执行的机器指令（可执行文件，如Windows下的.exe或Linux下的ELF文件）。

**选项逐项分析：**
- **A (编辑)**：❌ 错误。编辑是使用编辑器（如记事本、VS Code）输入或修改源代码的过程，仅产生文本文件，不涉及代码翻译。
- **B (保存)**：❌ 错误。保存是将内存中的代码写入磁盘存储介质，属于文件操作，不改变代码内容或生成可执行文件。
- **C (调试)**：❌ 错误。调试（Debug）是运行程序后通过断点、打印等方式排查逻辑错误，目的是找出并修复bug，不是生成可执行文件的步骤。
- **D (编译)**：✅ 正确。编译（Compilation）将高级语言源代码转换为目标机器的机器代码或中间代码（.o/.obj文件），再经链接后生成可执行文件。这是生成可执行代码的必经阶段。

**易错提醒：**
- 区分“源文件”到“可执行文件”的完整流程：编辑→保存→编译→链接→运行。缺少编译步骤，程序无法执行。
- 编译报错与运行时错误是不同阶段的问题，需要分别处理。

**知识延伸：**
C++语言的典型编译流程为：预处理（.cpp→.i）→编译（.i→.s/.o）→汇编→链接（生成.exe）。GESP各级考试常考编译型语言与解释型语言的区别。

**考点：** 程序开发流程与编译的基本概念`,
      tags: ['计算机基础']
    },
    {
      id: 2,
      type: 'single',
      question: `二进制数 11.01 在十进制下是（ ）。`,
      options: ['3.01', '3.05', '3.125', '3.25'],
      answer: 3,
      score: 2,
            explanation: `**答案：D (3.25)**

**核心解析：**
二进制数转换为十进制，需分别处理整数部分和小数部分，按位权展开求和。

1. **整数部分 \`11\`：**
   - 右侧为最低位（2⁰=1），左侧为高位（2¹=2）
   - \`1×2¹ + 1×2⁰ = 1×2 + 1×1 = 2 + 1 = 3\`

2. **小数部分 \`.01\`：**
   - 小数点后第1位权重为 2⁻¹ = 0.5，第2位为 2⁻² = 0.25
   - \`0×2⁻¹ + 1×2⁻² = 0×0.5 + 1×0.25 = 0 + 0.25 = 0.25\`

3. **总和：** \`3 + 0.25 = 3.25\`

**选项逐项分析：**
- **A (3.01)**：❌ 错误。该选项错误地只将整数部分加0.01，未正确计算二进制小数位的位权值。
- **B (3.05)**：❌ 错误。3.05 ≈ 3 + 0.05，对应二进制约为 11.00(0011...)₂，不是 11.01₂。
- **C (3.125)**：❌ 错误。3.125 = 3 + 0.125，对应二进制为 11.001₂，与 11.01₂ 不符。
- **D (3.25)**：✅ 正确。如核心解析所示，11.01₂ = 3.25₁₀。

**易错提醒：**
- 二进制小数位的位权是负指数：第1位是2⁻¹=0.5，第2位是2⁻²=0.25，不能用2、4、8...的正指数。
- 小数点后的0表示该位权值为0，不是简单的占位。

**知识延伸：**
常见二进制小数转换结果应熟记：0.1₂=0.5，0.01₂=0.25，0.11₂=0.75，0.001₂=0.125。这有助于快速判断选项。

**考点：** 进位制转换（二进制小数转十进制）`,
      tags: ['进制转换']
    },
    {
      id: 3,
      type: 'single',
      question: `已知大写字符'A'的 ASCII 编码的十六进制表示为 0x41，则字符'F'的 ASCII 编码的十六进制表示为（ ）。`,
      options: ['46', '47', '48', '49'],
      answer: 0,
      score: 2,
            explanation: `**答案：A (46)**

**核心解析：**
ASCII码表中，大写英文字母按字母顺序连续编码，每个字母的码值比前一个字母大1。

1. 已知 'A' = 0x41（十进制65）
2. 'F' 是 'A' 后的第5个字母（A→B→C→D→E→F，间隔5个位置）
3. 0x41 + 5 = 0x46
4. 0x46 转换为十进制：4×16 + 6 = 64 + 6 = 70，对应ASCII字符 'F'
5. 十六进制表示为 **46**（注意：题目要求填十六进制，选项也是十进制数字形式表示十六进制）

**选项逐项分析：**
- **A (46)**：✅ 正确。'F' = 'A' + 5 = 0x41 + 5 = 0x46 = 46。
- **B (47)**：❌ 错误。该选项对应 'G'（0x47），是 'F' 的下一个字母。
- **C (48)**：❌ 错误。该选项对应 'H'（0x48），或数字字符 '0'。
- **D (49)**：❌ 错误。该选项对应 'I'（0x49），或数字字符 '1'。

**易错提醒：**
- 'A' 到 'F' 的距离是5个位置（A是第0个，B是第1个...F是第5个），不是4。容易数成4导致选B。
- 进制转换结果要看清楚：0x46 的"46"是十六进制，需要换算成十进制还是直接写46都可以，但不要误以为是十进制46对应字符'.'。

**知识延伸：**
大写字母 'A'-'Z' 的ASCII范围是 0x41(65) 到 0x5A(90)，小写字母 'a'-'z' 是 0x61(97) 到 0x7A(122)，数字 '0'-'9' 是 0x30(48) 到 0x39(57)。

**考点：** ASCII码的连续性与进制转换`,
      tags: ['计算机基础']
    },
    {
      id: 4,
      type: 'single',
      question: `以下哪个不是 C++语言中的运算符？（ ）`,
      options: ['&', '&&', '*', '**'],
      answer: 3,
      score: 2,
            explanation: `**答案：D (**)**

**核心解析：**
C++语言有明确定义的运算符集合，包括算术运算符、关系运算符、逻辑运算符、位运算符、赋值运算符、条件运算符等。本题四个选项中，\`&\`、\`&&\`、\`*\` 都是C++中合法的运算符或符号，而 \`**\` 不是。

1. \`&\`：按位与运算符（双目）或取地址运算符（单目），如 \`a & b\`、\`&x\`
2. \`&&\`：逻辑与运算符，如 \`a && b\`
3. \`*\`：乘法运算符（双目）或解引用运算符（单目），如 \`a * b\`、\`*ptr\`
4. \`**\`：不是C++语言中的运算符，若写成 \`a**b\` 会导致语法错误

**选项逐项分析：**
- **A (&)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（该项本身正确）。\`&\` 在C++中既是按位与运算符（双目），也是取地址运算符（单目），是合法的运算符。
- **B (&&)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（该项本身正确）。\`&&\` 是C++中的逻辑与运算符，用于短路求值，是合法的运算符。
- **C (*)**：❌ 错误（该项本身正确）。\`*\` 在C++中既是乘法运算符（双目），也是指针解引用运算符（单目），是合法的运算符。
- **D (**✅ 正确。)**： 正确。C++语言语法中没有 \`**\` 运算符。若需要进行幂运算，需使用标准库函数 \`pow()\` 或手写循环/快速幂实现。

**易错提醒：**
- 注意 \`**\` 是Python中表示幂的运算符（如 \`2**3 = 8\`），但C++不支持。考试中常见将其他语言的语法误认为C++语法的陷阱。
- \`*\` 和 \`&\` 都是多义词（既是运算符又有其他含义），需要根据上下文判断其具体含义。

**知识延伸：**
C++支持的运算符约40余种，包括：算术（+ - * / %）、关系（< > == != <= >=）、逻辑（&& || !）、位（& | ^ ~ << >>）、赋值（= += -= *= /=）、三目（?:）、逗号运算符等。

**考点：** C++运算符的语法规范`,
      tags: ['计算机基础']
    },
    {
      id: 5,
      type: 'single',
      question: `如果字符串定义为 char str[] = "Hello";，则字符数组 str 的长度为（ ）。`,
      options: ['0', '5', '6', '7'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (6)**

**核心解析：**
字符数组的长度由其元素个数决定。当使用字符串字面量 \`"Hello"\` 初始化字符数组时，需要考虑隐藏的字符串结束符。

1. 可见字符：\`'H'\`, \`'e'\`, \`'l'\`, \`'l'\`, \`'o'\`，共 **5个字符**
2. 字符串结束符：编译器自动在末尾添加 \`'\\0'\`，用于标识字符串结束
3. 数组总长度：\`5 + 1 = 6\`

因此，\`sizeof(str)\` 或数组元素个数为 **6**。

**选项逐项分析：**
- **A (0)**：❌ 错误。数组已正确定义并初始化，不可能长度为0。
- **B (5)**：❌ 错误（该项本身正确）。5是可见字符的数量，但忽略了末尾的字符串结束符 \`'\\0'\`。strlen()函数返回的是到 '\\0' 之前的字符数，返回5，但 sizeof(str) 返回的是数组总长度6。
- **C (6)**：✅ 正确。包含5个可见字符和1个结束符 '\\0'，数组长度为6。
- **D (7)**：❌ 错误。7通常是多算了结束符或加上了其他字符，如误以为需要换行符。

**易错提醒：**
- 区分 \`strlen(str)\`（返回5）和 \`sizeof(str)\`（返回6）的区别：前者计数字符数（到'\\0'为止），后者计算数组字节数/元素个数。
- 如果定义为 \`char *p = "Hello";\`，指针p指向字符串字面量，此时 \`sizeof(p)\` 是指针大小（4或8字节），不是字符串长度。

**知识延伸：**
字符串结束符 \`'\\0'\` 的ASCII码值为0，它不是空格字符。在使用 \`gets()\`、\`strcpy()\` 等C风格字符串函数时，务必确保目标数组足够容纳原字符串加一个结束符。

**考点：** 字符数组的长度计算与字符串结束符`,
      tags: ['字符串']
    },
    {
      id: 6,
      type: 'single',
      question: `一个数组定义为 double array[3];，则这个数组占用内存的大小为（ ）。`,
      options: ['24', '12', '6', '3'],
      answer: 0,
      score: 2,
            explanation: `**答案：A (24)**

**核心解析：**
1. \`double\` 是 C++ 中的双精度浮点类型，在绝大多数编译环境（32 位或 64 位系统）下，其标准占用空间为 **8 字节**。
2. 数组 \`double array[3]\` 声明了 3 个 \`double\` 类型的元素。
3. 数组占用的总内存 = 元素个数 × 每个元素的字节数 = 3 × 8 = **24 字节**。

**选项逐项分析：**
- **A (24)**：✅ 正确。计算过程为 3 × 8 = 24 字节。
- **B (12)**：❌ 错误。若数组类型为 \`float\` 或 32 位 \`int\`（各占 4 字节），则 3 × 4 = 12 字节。本题明确指定了 \`double\` 类型。
- **C (6)**：❌ 错误。可能是将 \`double\` 误认为占 2 字节，或将 8 误解为 6。
- **D (3)**：❌ 错误。数组元素个数为 3，不代表总字节数也是 3。

**易错提醒：**
- 不能混淆"数组长度"（3 个元素）和"总字节数"（24 字节）。数组总大小等于长度乘以单元素大小。
- GESP 二级曾考过 \`int\` 占 4 字节、\`float\` 占 4 字节、\`char\` 占 1 字节；三级需要记住 \`double\` 占 8 字节。

**知识延伸：**
在 C++ 中，常见类型的大小如下：\`char\` = 1 字节，\`short\` = 2 字节，\`int\` = 4 字节，\`float\` = 4 字节，\`double\` = 8 字节（\`long double\` 在某些平台更大）。

**考点：** 基础数据类型在内存中的占用空间`,
      tags: ['计算机基础']
    },
    {
      id: 7,
      type: 'single',
      question: `以下数组定义，符合 C++语言语法的是（ ）。`,
      options: ['int a[];', 'int b[\'3\'];', 'int c[3.0];', 'int[3] d;'],
      answer: 1,
      score: 2,
            explanation: `**答案：B (int b['3'];)**

**核心解析：**
1. C++ 规定：定义数组时，\`[]\` 内的数组大小必须是一个**整型常量表达式**（或在初始化时由编译器自动推断）。
2. 选项 B：\`int b['3'];\` 中，\`'3'\` 是**字符常量**，其值为字符 '3' 的 ASCII 码，即整数 **51**。这是一个合法的整型常量。
3. 因此该语句等价于 \`int b[51];\`，语法完全合法。

**选项逐项分析：**
- **A (int a[];)**：❌ 错误。在不进行初始化的情况下，数组大小不能为空。编译器无法推断出一个未初始化数组的大小。
- **B (int b['3'];)**：✅ 正确。字符常量的 ASCII 值构成一个整型常量表达式，可以作为数组大小。
- **C (int c[3.0];)**：❌ 错误。数组大小必须是整数类型（\`int\`、\`char\`、\`enum\` 等），不能使用浮点型字面量。
- **D (int[3] d;)**：❌ 错误。这是 Java、C# 等语言的数组声明语法，C++ 的正确语法是 \`int d[3];\`。

**易错提醒：**
- \`'3'\` 是字符常量（值为 51），而 \`"3"\` 是字符串（包含 '3' 和 '\\0' 两个字符）。审题时不可混淆。
- 字符常量在 C++ 中本质上是整型，可以参与整型运算或作为数组大小。

**知识延伸：**
C++ 中以下情况允许数组大小留空：初始化列表推断（如 \`int a[] = {1, 2, 3};\`）或 C99/C++11 的变长数组（VLA）特性（在 GESP 范围内通常不涉及）。

**考点：** C++ 数组定义语法与类型转换规则`,
      tags: ['数组']
    },
    {
      id: 8,
      type: 'single',
      question: `下列关于进制的叙述，不正确的是（ ）。`,
      options: [
        '正整数的二进制表示中只会出现 0 和 1',
        '10 不是 2 的整数次幂，所以十进制数无法转换为二进制数',
        '从二进制转换为 8 进制时，可以方便地将每 3 位二进制位转换',
        '从二进制转换为 16 进制时，可以方便地将每 4 位二进制位转换'
      ],
      answer: 1,
      score: 2,
            explanation: `**答案：B (10 不是 2 的整数次幂，所以十进制数无法转换为二进制数)**

**核心解析：**
1. 进制转换的原理是：**任意十进制正整数都可以唯一地表示为以 2 为底的幂之和**（即二进制展开）。
2. 无论十进制数本身是否为 2 的幂，都可以通过**除 2 取余法**转换为二进制。例如：十进制 10 的二进制为 1010（10 = 8 + 2 = 2³ + 2¹）。
3. 选项 B 声称"10 不是 2 的整数次幂，所以无法转换为二进制"，这完全颠倒了进制转换的逻辑，是一个**严重的概念错误**。

**选项逐项分析：**
- **A (正整数的二进制表示中只会出现 0 和 1)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。二进制的基数是 2，每一位只能是 0 或 1。
- **B (10 不是 2 的整数次幂，所以十进制数无法转换为二进制数)**：✅ 正确。错误。这是对进制转换原理的根本性误解。任意十进制整数均可转换为二进制，与该数是否为 2 的幂无关。
- **C (从二进制转换为 8 进制时，可以方便地将每 3 位二进制位转换)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。因为 2³ = 8，每 3 位二进制对应 1 位八进制。
- **D (从二进制转换为 16 进制时，可以方便地将每 4 位二进制位转换)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。因为 2⁴ = 16，每 4 位二进制对应 1 位十六进制。

**易错提醒：**
- 不要将"某数是 2 的幂"与"某数可以转换为二进制"混为一谈。所有整数（包括 10、100、255 等）都可以转换为二进制。
- 二进制转八进制/十六进制的分组规则（3 位/4 位一组）是必须掌握的基础转换技巧。

**知识延伸：**
十进制转二进制的标准方法是"除 2 取余，倒序输出"；二进制转八进制/十六进制采用分组对照法；进制转换是 GESP 一级至四级的反复考点。

**考点：** 进制转换原理与计算机常识`,
      tags: ['进制转换']
    },
    {
      id: 9,
      type: 'single',
      question: `下列关于 C++语言中数组的叙述，不正确的是（ ）。`,
      options: [
        '数组必须先 definition 后使用',
        '数组的所有元素在内存中是连续存放的',
        '除了字符数组，在定义数组时“[ ]”内必须有常数',
        '不能对数组赋值，但可以对数组的每个基础类型的元素赋值'
      ],
      answer: 2,
      score: 2,
            explanation: `**答案：C (除了字符数组，在定义数组时"[ ]"内必须有常数)**

**核心解析：**
1. C++ 中，定义数组时若使用 \`{}\` 进行**初始化列表**（即在大括号中直接给出元素值），编译器可以从初始化值**自动推断**数组大小，此时 \`[]\` 内可以不写数字。
2. 示例：\`int a[] = {1, 2, 3};\` 完全合法，编译器自动确定数组大小为 3。**这一规则不限于字符数组，对所有类型的数组均适用**。
3. 因此，选项 C 的说法"除了字符数组...必须有常数"以偏概全，忽略了所有类型数组均可通过初始化推断大小的通用规则。

**选项逐项分析：**
- **A (数组必须先定义后使用)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。这符合 C++ 变量先定义后使用的基本规则。
- **B (数组的所有元素在内存中是连续存放的)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。数组的 핵심特征就是连续内存存储，元素地址依次递增。
- **C (除了字符数组，在定义数组时"[ ]"内必须有常数)**：✅ 正确。错误。所有类型的数组（包括 \`int\`、\`double\` 等）在初始化时都可以省略 \`[]\` 中的大小，由编译器自动推断。选项 C 将字符数组单独列出是不准确的。
- **D (不能对数组赋值，但可以对数组的每个基础类型的元素赋值)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。数组名代表首地址，是常量（不可修改），不能整体赋值；但可以通过下标访问元素并对其赋值。

**易错提醒：**
- 数组大小留空**不是字符数组的特权**，\`int\`、\`double\` 等类型同样可以在初始化时省略大小。
- \`int a[];\`（仅声明不定义/不初始化）仍然是非法的，因为没有初始化列表供编译器推断大小。

**知识延伸：**
C++11 引入了另一种推断大小的写法：\`std::array<int, 3> a = {1, 2, 3};\`。但在 GESP 范围内，主要考察传统 C-style 数组的初始化推断规则。

**考点：** C++ 数组的定义、内存布局与赋值限制`,
      tags: ['数组']
    },
    {
      id: 10,
      type: 'single',
      question: `一个 int 类型的值，做以下哪个操作，一定会变回原来的值？（ ）`,
      options: [
        '左移 3 位，再右移 3 位',
        '右移 3 位，再左移 3 位',
        '按位或 7，再按位与-8',
        '按位异或 7，再按位异或 7'
      ],
      answer: 3,
      score: 2,
            explanation: `**答案：D (按位异或 7，再按位异或 7)**

**核心解析：**
1. **异或运算的自反性（幂等性）**：对于任何整数 \`x\` 和 \`a\`，有 \`(x ^ a) ^ a = x\`。即一个数与同一个值进行两次异或，结果必然等于原值。
2. 选项 D 的操作：\`x ^ 7 ^ 7 = x ^ (7 ^ 7) = x ^ 0 = x\`，必然恢复原值。
3. 其他选项涉及移位或位运算组合时，都存在**数据丢失**的风险，无法保证一定恢复原值。

**选项逐项分析：**
- **A (左移 3 位，再右移 3 位)**：❌ 错误。左移 3 位可能将高位的数据移出并丢失（尤其当原值较大时）。即使右移回来，高位补的是 0（无符号）或符号位（有符号），原数据已不可恢复。
- **B (右移 3 位，再左移 3 位)**：❌ 错误。右移 3 位会将最低 3 位数据直接丢弃，左移回来后低位补 0，精度必然丢失。
- **C (按位或 7，再按位与 -8)**：❌ 错误。以正数为例：设 \`x = 10\`（二进制 1010），\`x | 7 = 15\`（1111），\`15 & (-8)\` 中 -8 的二进制补码为 \`...11111000\`，与 1111 按位与得 \`1000 = 8\`，与原值 10 不等。
- **D (按位异或 7，再按位异或 7)**：✅ 正确。异或两次必然抵消：\`x ^ 7 ^ 7 = x\`，与 \`x\` 的初始值无关，必然恢复。

**易错提醒：**
- 移位运算**不是可逆操作**：左移可能导致高位数据丢失，右移可能导致低位数据丢失，移位后再移回来无法保证恢复原值。
- 位运算组合（如选项 C）的效果与具体数值密切相关，不能用特殊值验证后推广为"一定恢复"。

**知识延伸：**
异或的这条性质 \`(x ^ a) ^ a = x\` 在算法中有重要应用，例如"两数交换"（无需临时变量）、"找出落单的数"（其他数均出现两次）等经典问题。

**考点：** 位运算的性质与数据丢失风险`,
      tags: ['位运算']
    },
    {
      id: 11,
      type: 'single',
      question: `如果 a 和 b 均为 int 类型的变量，下列表达式能正确判断“a 等于 b”的是（ ）。`,
      options: ['((a / b) == 1)', '((a & b) == a)', '((a ^ b) == 0)', '((a | b) == b)'],
      answer: 2,
      score: 2,
            explanation: `**答案：C ((a ^ b) == 0)**

**核心解析：**
判断两个 int 变量是否相等，本质上是要找到一种运算，当且仅当两数各位完全相同时结果为某个特定值。

逐项分析各选项的实现原理：

- **异或运算的性质：** 对于两个整数 a 和 b，表达式 \`a ^ b\` 会逐位比较：相同为 0，不同为 1。因此，当且仅当 a 和 b 完全相等时，\`a ^ b\` 的每一位都是 0，即整体值为 0。所以表达式 \`((a ^ b) == 0)\` 能够准确判断相等关系。

**选项逐项分析：**
- **A ((a / b) == 1)**：❌ 错误。整除判断要求 a 是 b 的整数倍且结果为 1。例如 a=3, b=2 时，3/2=1（整除截断），但 3≠2。即使 a=b≠1 时也不成立（如 a=b=2 时 2/2=1 碰巧成立，但 a=b=3 时 3/3=1 也成立）。此表达式不能准确判断相等。
- **B ((a & b) == a)**：❌ 错误。按位与 \`a & b == a\` 成立的条件是：a 的每一个为 1 的二进制位，在 b 中也必须为 1（即 a 的所有 1 位是 b 的子集）。这与相等不同。例如 a=3(011), b=7(111) 时，3&7=3 成立，但 3≠7。
- **C ((a ^ b) == 0)**：✅ 正确。如上所述，异或为 0 当且仅当两数相等。
- **D ((a | b) == b)**：❌ 错误。按位或 \`a | b == b\` 成立的条件是：b 包含 a 的所有 1 位（即 a 是 b 的子集），同样不能证明相等。例如 a=1(001), b=2(010) 时，1|2=3≠2；而 a=1, b=1 时虽然成立，但并非普遍规律。

**易错提醒：**
- 不能用整除 \`a/b == 1\` 判断相等，因为整除只能判断 a 是 b 的整数倍且商为 1。
- 位运算的与、或、异或都有"集合包含"或"相同位"的含义，不能简单等同于相等判断。

**知识延伸：**
异或运算的三大性质——结合律、交换律、自反性（a ^ a = 0）使其成为不借助临时变量交换两数的利器，也是判断相等的经典技巧。

**考点：** 位运算实现逻辑判断（异或的自反性）`,
      tags: ['位运算']
    },
    {
      id: 12,
      type: 'single',
      question: `如果 a 为 int 类型的变量，下列哪个表达式可以正确求出满足“小于等于 a 且是 4 的倍数”的整数中最大的？`,
      options: ['(a & (~3))', '((a << 2) >> 2)', '(a ^ 3)', '((a-1) | 3)+1'],
      answer: 0,
      score: 2,
            explanation: `**答案：A (a & (~3))**

**核心解析：**
题目要求"小于等于 a 且是 4 的倍数"的最大整数，即向下取整到最近的 4 的倍数（4 的倍数特征：二进制低两位为 00）。

- \`~3\` 的二进制表示：\`3\` 的二进制为 \`...00000011\`，取反后得到 \`...11111100\`，即低两位为 00，其余位全为 1。
- \`a & (~3)\`：将 a 的低两位强制清零，其余位保持不变。这恰好将 a 向下舍入到最接近的 4 的倍数。

**选项逐项分析：**
- **A (a & (~3))**：✅ 正确。~3 的低两位为 00，与操作清零 a 的低两位，实现向下取整到 4 的倍数。
- **B ((a << 2) >> 2)**：❌ 错误。左移 2 位再右移 2 位，对于正数相当于没有变化（对于负数有符号右移会保留符号位），不会产生 4 的倍数。
- **C (a ^ 3)**：❌ 错误。异或 3 只是翻转 a 的低两位，结果不一定是 4 的倍数。例如 a=5(101) 时，5^3=6(110)，不是 4 的倍数。
- **D ((a-1) | 3)+1**：❌ 错误。这是向上取整到 4 的倍数的常见写法，与题目要求的向下取整方向相反。

**易错提醒：**
- 向下取整到 4 的倍数应清除低位，而非增加低位。
- 区分向下取整（清除低位）和向上取整（补到下一个 4 的倍数）的位运算实现方式。

**知识延伸：**
向下取整到 2^n 的倍数，只需将低 n 位清零，即 \`x & (~((1<<n)-1))\` 或 \`x & (~(2^n-1))\`。向上取整则使用 \`(x + (2^n-1)) & (~(2^n-1))\`。

**考点：** 位运算在数值对齐（向下取整）中的应用`,
      tags: ['位运算']
    },
    {
      id: 13,
      type: 'single',
      question: `在下列代码的横线处填写（ ），可以使得输出是“24 12”\n\`int a = 12, b = 24; a = a ^ b; ___________; a = a ^ b;\``,
      options: ['a = a ^ b', 'b = a ^ b', 'a = a+b', 'b = a+b'],
      answer: 1,
      score: 2,
            explanation: `**答案：B (b = a ^ b)**

**核心解析：**
本题考查不借助临时变量交换两数的经典"异或交换算法"。异或运算满足以下性质：
- 交换律：a ^ b = b ^ a
- 结合律：(a ^ b) ^ c = a ^ (b ^ c)
- 自反性：a ^ a = 0
- 恒等律：a ^ 0 = a

交换算法三步曲：
1. \`a = a ^ b\`  → a 存储了 a 和 b 的差异信息
2. \`b = a ^ b\`  → 利用差异信息恢复原 a 的值赋给 b
3. \`a = a ^ b\`  → 再次利用差异信息恢复原 b 的值赋给 a

**逐行动手推演（设初始 a=12, b=24）：**

\`\`\`cpp
int a = 12, b = 24;

// 第一步（已给出）
a = a ^ b;
// 此时：a = 12 ^ 24 = 20（二进制逐位异或）

// 第二步（待填入）
b = a ^ b;
// 此时：b = (12 ^ 24) ^ 24 = 12 ^ (24 ^ 24) = 12 ^ 0 = 12

// 第三步（已给出）
a = a ^ b;
// 此时：a = (12 ^ 24) ^ 12 = 12 ^ 24 ^ 12 = 12 ^ 12 ^ 24 = 0 ^ 24 = 24
\`\`\`

最终 a=24, b=12，交换成功。题目只要求输出结果，验证了算法的正确性。

**选项逐项分析：**
- **A (a = a ^ b)**：❌ 错误。若填入此句，第二步变成 \`a = a ^ b\`，此时 a 和 b 的值都已被破坏，第三步无法正确恢复原值，交换失败。
- **B (b = a ^ b)**：✅ 正确。符合异或交换法的第二步，正确利用差异信息恢复原值。
- **C (a = a+b)**：❌ 错误。使用加法交换虽然可行（需配合减法），但与代码已有的异或逻辑不兼容，无法完成交换。
- **D (b = a+b)**：❌ 错误。理由同上，加法运算与异或逻辑混用会导致数值错误。

**易错提醒：**
- 异或交换法必须严格按三步顺序执行：先 a = a ^ b，再 b = a ^ b，最后 a = a ^ b。
- 任何一步填错都会破坏"差异信息"，导致无法正确恢复原值。

**知识延伸：**
异或交换法的优点是不需要临时变量，节省内存；但要注意当 a 和 b 是同一变量时（如 swap(x, x)）会导致结果变为 0，因为 x ^ x = 0。

**考点：** 异或运算的自反性与交换律（无临时变量两数交换）`,
      tags: ['位运算']
    },
    {
      id: 14,
      type: 'single',
      question: `在下列代码的横线处填写（ ），可以使得输出是“2”。
\`int array[] = {3, 11, 2, 5, 24}; int min = 0;\`
\`for (int i = 1; i < 5; i++) { if (___________) min = array[i]; }\``,
      options: ['min > array[i]', 'min < array[i]', 'min = array[i]', '以上均不对'],
      answer: 3,
      score: 2,
            explanation: `**答案：D (以上均不对)**

**核心解析：**
本题需要找到数组 {3, 11, 2, 5, 24} 中的最小值并赋值给 min，期望最终输出 2。

**关键陷阱：初始化值 min = 0**

数组所有元素 {3, 11, 2, 5, 24} 均大于 0，因此：
- 条件 \`min > array[i]\` 永远为假（0 > 3, 0 > 11, ... 均不成立）
- 条件 \`min < array[i]\` 永远为真，但 min 不会被更新（因为赋值语句在 if 内部）

**逐项分析：**

若 min 初始化为合理值（如 min = array[0] = 3），则 \`min > array[i]\` 能正确工作：
- i=1: 3 > 11? 否
- i=2: 3 > 2? 是 → min = 2
- i=3: 2 > 5? 否
- i=4: 2 > 24? 否
最终 min = 2

但题目中 min 初始化为 0，导致无论填入哪个条件都无法使 min 更新为 2。

**选项逐项分析：**
- **A (min > array[i])**：❌ 错误。虽然这是寻找最小值的常见正确逻辑，但由于 min 初始化为 0，而所有数组元素都大于 0，条件永远不成立，min 始终保持为 0。
- **B (min < array[i])**：❌ 错误。条件成立时 min 不变；条件不成立时才需要更新 min，但此条件的逻辑方向完全相反。
- **C (min = array[i])**：❌ 错误。这是赋值表达式而非条件判断，非零值恒为真，会导致每次循环都执行 \`min = array[i]\`，最终 min = array[4] = 24，而非期望的 2。
- **D (以上均不对)**：✅ 正确。由于 min 初始化值的缺陷，选项 A、B、C 均无法在给定代码框架下得到输出 2。

**易错提醒：**
- 查找类问题必须关注初始值是否合理。查找最小值时，初始值应设为数组第一个元素或一个足够大的数。
- 条件判断要区分"何时需要更新"：找最小值时，当发现更小的值才需要更新。

**知识延伸：**
初始值陷阱是 GESP 常见考点。正确的做法是将 min 初始化为 \`array[0]\`，或者使用 \`INT_MAX\`（需包含 \`<climits>\` 头文件）。

**考点：** 查找算法的初始化陷阱`,
      tags: ['算法思维']
    },
    {
      id: 15,
      type: 'single',
      question: `在下列代码的横线处填写（ ），可以使得输出不是“31”\n\`int array[] = {1, 2, 4, 8, 16}; int res = 0;\`\n\`for (int i = 0; i < 5; i++) { ___________ }\``,
      options: ['res = res+array[i]', 'res = res & array[i]', 'res = res | array[i]', 'res = res ^ array[i]'],
      answer: 1,
      score: 2,
            explanation: `**答案：B (res = res & array[i])**

**核心解析：**
题目要求使最终输出不是"31"。数组 {1, 2, 4, 8, 16} 的特点是每个数恰好占据二进制的一位：
- 1 = 0b00001
- 2 = 0b00010
- 4 = 0b00100
- 8 = 0b01000
- 16 = 0b10000

**逐选项推导最终结果：**

**选项A：res = res + array[i]**
\`\`\`cpp
res = 0 + 1 = 1
res = 1 + 2 = 3
res = 3 + 4 = 7
res = 7 + 8 = 15
res = 15 + 16 = 31  ❌ 不满足"不是31"
\`\`\`

**选项B：res = res & array[i]**
\`\`\`cpp
res = 0 & 1 = 0
res = 0 & 2 = 0
res = 0 & 4 = 0
res = 0 & 8 = 0
res = 0 & 16 = 0  ✅ 满足"不是31"（输出0）
\`\`\`

**选项C：res = res | array[i]**
\`\`\`cpp
res = 0 | 1 = 1
res = 1 | 2 = 3
res = 3 | 4 = 7
res = 7 | 8 = 15
res = 15 | 16 = 31  ❌ 不满足"不是31"
\`\`\`

**选项D：res = res ^ array[i]**
\`\`\`cpp
res = 0 ^ 1 = 1
res = 1 ^ 2 = 3
res = 3 ^ 4 = 7
res = 7 ^ 8 = 15
res = 15 ^ 16 = 31  ❌ 不满足"不是31"（位不重合时异或等于加法）
\`\`\`

**选项逐项分析：**
- **A (res = res + array[i])**：❌ 错误。累加结果为 1+2+4+8+16 = 31。
- **B (res = res & array[i])**：✅ 正确。res 初始为 0，任何数与 0 按位与结果都是 0，最终输出为 0 ≠ 31。
- **C (res = res | array[i])**：❌ 错误。按位或会合并所有为 1 的位，结果为 0b11111 = 31。
- **D (res = res ^ array[i])**：❌ 错误。由于数组元素二进制位不重合，异或结果等同于累加，结果仍为 31。

**易错提醒：**
- 初始值为 0 时，按位与运算具有"清零"特性（0 & x = 0），这是本题的关键。
- 数组元素二进制位不重合时，异或与加法等价。

**知识延伸：**
按位或的累积效果是"合并所有 1 位"，当操作数恰好对应二进制位 1、2、4、8、16 时，结果为 0b11111 = 31。这是判断"是否包含某些位"的常用技巧。

**考点：** 位运算的累积效果（与、或、异或）与加法的对比分析`,
      tags: ['位运算']
    },
    {
      id: 16,
      type: 'judge',
      question: `一个算法可以用不同的形式来描述，但不能用自然语言描述。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
算法的描述形式是多种多样的，包括但不限于：自然语言（如日常汉语描述解题步骤）、流程图（用图形符号表示执行流程）、伪代码（介于自然语言和编程语言之间的描述）、程序设计语言（如 C++ 代码）。题目断言算法"不能用自然语言描述"违反了这一基本常识，因此该说法错误。

**易错提醒：**
- 部分考生误以为算法必须用"专业"方式描述，实际上用日常语言描述算法步骤完全合法且常见。
- 流程图、伪代码、自然语言都是算法的合法描述形式，互相之间可以等价转换。

**知识延伸：**
在 GESP 二级考试中曾出现过要求用自然语言描述算法步骤的题目，这说明自然语言描述算法是受到认可的形式之一。

**考点：** 编程基础 - 算法的描述方式`,
      tags: ['编程基础']
    },
    {
      id: 17,
      type: 'judge',
      question: `CCF 官方网站域名是 gesp.ccf.org.cn，其中顶级域名是 gesp。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
域名采用分层结构，从右到左依次是顶级域名（TLD）、二级域名、三级域名等。以 gesp.ccf.org.cn 为例：
- 最右侧的 .cn 是顶级域名，代表中国（Country Code Top-level Domain）。
- org 是二级域名，代表组织机构。
- ccf 是三级域名，是 CCF 组织的专属名称。
- gesp 是四级域名，是 CCF 下的具体项目名称。
因此，题目将 gesp 误认为顶级域名是错误的。

**易错提醒：**
- 考生容易混淆"顶级"的概念，顶级域名是最右侧的部分（.cn、.com、.org 等），不是最左侧的部分。
- 常见顶级域名包括：.cn（中国）、.com（商业）、.org（非营利组织）、.net（网络服务）等。

**知识延伸：**
除了国家顶级域名（.cn、.us 等）和通用顶级域名（.com、.org、.net）外，还有新顶级域名如 .xyz、.top 等，考试中注意识别。

**考点：** 计算机基础 - 域名结构`,
      tags: ['计算机基础']
    },
    {
      id: 18,
      type: 'judge',
      question: `数据编码方式只有原码、反码、补码三种。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
题目声称数据编码方式"只有"原码、反码、补码三种，这是一个绝对化表述。实际上，除了这三种整数编码方式外，还有：
1. 移码（Excess/bias notation）：常用于浮点数的指数部分，通过偏移量表示数值。
2. 浮点数编码（如 IEEE 754 标准）：用科学计数法形式表示实数，包含符号位、指数位和尾数位。
3. BCD 码（Binary-Coded Decimal）：用二进制表示十进制数字。
4. 格雷码（Gray Code）：相邻数值只有一位不同的编码方式。
因此原题说法过于狭隘，遗漏了重要的编码方式。

**易错提醒：**
- 考生容易将"原码、反码、补码"与"所有编码方式"混淆，实际上前者主要用于有符号整数的表示。
- 在 C++ 中，整数在内存中通常以补码形式存储，但浮点数遵循 IEEE 754 标准。

**知识延伸：**
在 GESP 三级考试中，移码的概念可能以"8421码"或"余码"的形式出现，理解偏移量的概念有助于掌握浮点数指数位的表示原理。

**考点：** 计算机基础 - 数据编码方式`,
      tags: ['计算机基础']
    },
    {
      id: 19,
      type: 'judge',
      question: `长度为 n 的数组，合理的下标范围是从 0 到 n，包括 0 和 n。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
在 C++ 中，数组的下标从 0 开始。对于长度为 n 的数组，其有效下标范围是 0 到 n-1（共 n 个元素）。这是因为数组名在大多数情况下会被隐式转换为指向首元素的指针，首元素地址即数组起始地址，因此第 i 个元素的地址为 首元素地址 + i × 元素大小。如果允许下标为 n，则访问的是数组末尾之后的位置，这属于越界访问，是未定义行为。

**易错提醒：**
- 常见错误是记成"0 到 n"，把下标范围误认为比实际多 1。
- 在循环遍历数组时，for (int i = 0; i < n; i++) 是正确的写法，若写成 i <= n 会导致访问 arr[n]（越界）。

**知识延伸：**
数组越界是 C++ 程序中的常见错误，可能导致程序崩溃、数据损坏或安全漏洞。GESP 后续级别可能会涉及动态数组（vector）与静态数组的边界检查对比。

**考点：** 数组 - 下标范围`,
      tags: ['数组']
    },
    {
      id: 20,
      type: 'judge',
      question: `字符常量 '\\0' 常用来表示字符串结束，它和字符 '0' 是不同的。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
在 C++ 中，字符常量 '\\0'（转义序列）表示 ASCII 码值为 0 的字符，即空字符（Null Character），它专门用作 C-style 字符串的结束标志，存储在字符串末尾的额外位置，不计入字符串的实际长度。
而字符常量 '0' 是 ASCII 码值为 48 的数字字符 '0'，它是一个普通的可打印字符，用于在字符串中表示数字零。
两者在 ASCII 码值、用途和语义上都有本质区别：
- '\\0' 的整数值：0
- '0' 的整数值：48

**易错提醒：**
- 考生常将 '\\0' 和 '0' 混淆，前者是字符串结束符，后者是可显示的数字字符。
- 在字符串处理中，使用 strlen() 计算长度时不包括 '\\0'，而 sizeof() 会包括它。

**知识延伸：**
C++ 中 string 类自动管理字符串结束符，而 C-style 字符串需要程序员手动确保以 '\\0' 结尾。GESP 三级涉及字符串操作时，注意区分字符与字符串的处理方式。

**考点：** 字符串 - 字符常量与转义序列`,
      tags: ['字符串']
    },
    {
      id: 21,
      type: 'judge',
      question: `在 C++中，可以使用字符（如 '0'）作为数组下标。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
字符型数据在 C++ 中本质上是一个整数类型。以单引号包围的字符常量（如 '0'、'A'、'a'）在参与数值运算或作为下标时，会自动提升（implicit conversion）为该字符对应的整数值。例如，'0' 的 ASCII 码值为 48，'A' 为 65，'a' 为 97。因此，代码 \`arr['0']\` 实际上等价于 \`arr[48]\`，是合法的数组下标访问。

**易错提醒：**
- 容易忽略字符到整数的隐式转换，误以为必须手动将字符转换为整数才能用作下标。
- 字符 '0'～'9' 对应的 ASCII 码是 48～57，不是它们本身的数字值（0～9），使用时需注意区分。

**知识延伸：**
C++ 中字符类型（char）是有符号或无符号的整数类型，其取值范围通常为 -128～127 或 0～255，具体取决于实现。字符作为下标的机制与这一类型特性密切相关。

**考点：** 数组、字符类型与整数类型的隐式转换`,
      tags: ['数组']
    },
    {
      id: 22,
      type: 'judge',
      question: `在 C++中，数组被定义时，其大小就确定了。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
在标准 C++（C++98/03 及之后）中，使用静态语法定义的数组（如 \`int arr[10];\` 或 \`int arr[N];\`，其中 N 是常量表达式）其大小在编译阶段就已确定。数组一旦定义，其长度不可改变，既不能在定义后扩展，也不能在运行时动态调整。虽然 C99 引入了变长数组（VLA）允许运行时确定大小，C++11 起也引入了 \`std::vector\` 等动态容器，但这些已超出“数组”原始定义的范畴。本题语境下的“数组”即指静态分配的定长数组，其大小在定义时确定，此后不可变。

**易错提醒：**
- 混淆标准 C++ 数组与 C99 VLA、C++ 的 \`vector\` 等动态容器的行为，后者可以运行时改变大小。
- 将“数组大小确定”与“数组内容可变”混淆：数组大小固定，但数组元素的值可以修改。

**知识延伸：**
如果需要在运行时动态调整大小，应使用 \`std::vector\`、\`std::array\`（大小固定但更安全）或手动动态内存分配（\`new[]\`/\`delete[]\`）。GESP 三级要求掌握静态数组的基本概念和用法。

**考点：** 数组的概念与特性`,
      tags: ['数组']
    },
    {
      id: 23,
      type: 'judge',
      question: `编写程序时将所有十进制数改写为相同数值的二进制数，会使得运行效率更高。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
程序源码中使用的进制（如十进制 10、十六进制 0xA、二进制 0b1010）在编译阶段都会被编译器转换为等价的机器码表示。无论程序员写 10、0xA 还是 0b1010，编译后生成的指令和数据完全相同，运行时的效率也完全相同。选择哪种进制只是源码的可读性风格问题，不影响程序的执行效率。

**易错提醒：**
- 误以为二进制直接写出来程序就能“更接近机器”而提速，实际上编译器在编译期已完成转换。
- 将“源码可读性”与“运行效率”混为一谈：好的进制选择可能提升代码可读性，从而减少编程错误，但不改变运行速度。

**知识延伸：**
进制转换是 GESP 一级、二级的考点，理解数制的目的是帮助学员理解计算机内部的数据表示方式，但不应误认为进制选择会影响程序性能。

**考点：** 计算机基础、编译原理常识`,
      tags: ['计算机基础']
    },
    {
      id: 24,
      type: 'judge',
      question: `在 C++语言中，表达式 (0xf == 015) 的值为 true。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
在 C++ 中，以不同前缀表示不同进制的整数常量：
- \`0xf\` 以 \`0x\` 开头，是十六进制数，转换为十进制为 15（即二进制 1111₂）。
- \`015\` 以 \`0\` 开头（且第二位非 8/9），是八进制数，转换为十进制为 1×8¹ + 5 = 13（即二进制 1101₂）。

由于 15 ≠ 13，二者不相等，因此表达式 \`(0xf == 015)\` 的结果为 \`false\`（0）。

**易错提醒：**
- 容易忽略 \`0\` 开头的整数是八进制，将 \`015\` 误认为十进制的 15。
- 在调试输出时注意 printf/scout 的默认输出都是十进制，但源码中的进制不影响比较逻辑。

**知识延伸：**
C++ 支持三种整型进制表示：无前缀表示十进制、\`0x\` 前缀表示十六进制、\`0\` 前缀表示八进制。GESP 三级常通过进制转换和进制混淆来考察学员对进制的理解。

**考点：** 数制转换与整型常量表示`,
      tags: ['进制转换']
    },
    {
      id: 25,
      type: 'judge',
      question: `如果 a 为变量，且表达式 ((a | 3) == 3) 为 true，说明 a 在 0 到 3 之间。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
位运算表达式 \`(a | 3) == 3\` 的含义是：a 与 3 进行按位或运算后，结果等于 3。3 的二进制表示为 \`...000011₂\`（低位两位为 1，高位任意）。

对任意整数 x 进行 \`x | 3\` 运算时：
- 如果 x 的二进制位在第 0 位或第 1 位上有 1，这些位或上 3 后仍为 1，结果不会超过 3 在这两位上的表现。
- 如果 x 的二进制位在第 2 位或更高位上有 1（即 x ≥ 4），则 \`x | 3\` 的结果在第 2 位及以上必有 1，结果必然大于 3，不可能等于 3。

因此，\`(a | 3) == 3\` 成立，当且仅当 a 的二进制表示中第 2 位及更高位均为 0，即 a 只能取 0、1、2、3 四个值之一，满足“a 在 0 到 3 之间”。

**易错提醒：**
- 误认为 a 只能是 3，实际上 a 可以是 0、1、2、3 中的任意一个。
- 忽略位运算按位独立的特性：或运算的高位不会“溢出”到低位。

**知识延伸：**
位运算（|、&、^、~、<<、>>）是 GESP 三级、四级的重点考点。类似地，\`(a & 3) == 3\` 则表示 a 的低两位必须全为 1（即 a ≡ 3 mod 4）。掌握位运算的位独立性原则是解题关键。

**考点：** 位运算（按位或）的特性与分析`,
      tags: ['位运算']
    }
  ],
  programmingQuestions: [
    {
      id: 26,
      type: 'programming',
      template: `#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    // 在此填写代码\n    return 0;\n}`,
      score: 25,
      title: '春游',
      description: '老师带领同学们春游。已知班上有 N 位同学，每位同学有从 0 到 $N-1$ 的唯一编号。到了集合时间，老师确认是否所有同学都到达了集合地点，就让同学们报出自己的编号。到达的同学都会报出自己的编号，不会报出别人的编号，但有的同学很顽皮，会多次报出。你能帮老师找出有哪些同学没有到达吗？',
      inputDescription: '输入包含 2 行。第一行包含两个整数 N 和 M，表示班级有 N 位同学，同学们共有 M 次报出编号。约定 2 ≤ N, M ≤ 1000。\n第二行包含 M 个整数，分别为 M 次报出的编号。约定所有编号都在合理范围内（即小于 N 的非负整数）。',
      outputDescription: '输出一行。如果所有同学都到达，则输出 N；否则由小到大输出所有未到达的同学编号，空格分隔。',
      samples: [
        {
          input: `3 3
0 2 1`,
          output: '3',
          explanation: `
          **解析：**
          ???? 3 ????0, 1, 2????????? 0?2?1?????????????? 3?
          `
        },
        {
          input: `3 5
0 0 0 0 0`,
          output: '1 2',
          explanation: `
          **解析：**
          3 ?????? 0 ????????????? 1 ? 2??? 1 2?
          `
        }
      ],
      score: 25,
      explanation: `
      **解析：**
      使用布尔数组或 set 记录已到达的同学，最后遍历检查缺失项。如果缺失列表为空，则输出总人数 N。
      `,
      tags: ['数组', '模拟'],
      referenceCode: `
#include <iostream>
#include <vector>
using namespace std;

bool arrived[1005];

int main() {
    int N, M;
    cin >> N >> M;
    for (int i = 0; i < M; i++) {
        int id;
        cin >> id;
        arrived[id] = true;
    }
    bool all = true;
    bool first = true;
    for (int i = 0; i < N; i++) {
        if (!arrived[i]) {
            if (!first) cout << " ";
            cout << i;
            first = false;
            all = false;
        }
    }
    if (all) cout << N;
    cout << endl;
    return 0;
}
`
    },
    {
      id: 27,
      type: 'programming',
      template: `#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    // 在此填写代码\n    return 0;\n}`,
      score: 25,
      title: '密码合规检测',
      description: '网站注册时需要用户名和密码。本题的任务是编写程序检查用户输入密码的有效性。合规的密码需满足以下条件：\n1. 只能由 a-z 之间 26 个小写字母、A-Z 之间 26 个大写字母、0-9 之间 10 个数字以及 !@#$ 这四个特殊字符构成。\n2. 密码最短长度为 6 个字符，最长长度为 12 个字符。\n3. 大写字母、小写字母 and 数字这三类字符中，必须至少包含其中两种，并且至少包含一个 !@#$ 中的特殊字符。',
      inputDescription: '输入一行不含空格的字符串，约定其长度不超过 100。该字符串会通过英文逗号分隔为多段，每一段都作为一个待检测的密码。',
      outputDescription: '程序将输出若干行，每行输出一个合规的密码。输出顺序应与输入顺序保持一致，即先输入的合规密码先输出。',
      samples: [
        {
          input: 'seHJ12!@,sjdkffH$123,sdf!@&12HDHa!,123&^YUhg@!',
          output: 'seHJ12!@\nsjdkffH$123',
          explanation: `
          **解析：**
          第一、二个密码符合所有规则。第三个长度超过 12。第四个含有非法特殊字符 "^"。
          `
        }
      ],
      score: 25,
      explanation: `
      **解析：**
      通过 stringstream 或手动查找逗号来拆分字符串。对每个子串，检查长度、字符合法性、大写/小写/数字种数以及是否包含指定特殊字符。
      `,
      tags: ['字符串', '逻辑判断'],
      referenceCode: `
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
using namespace std;

bool check(string s) {
    if (s.length() < 6 || s.length() > 12) return false;
    bool hasUpper = false, hasLower = false, hasDigit = false, hasSpecial = false;
    for (char c : s) {
        if (c >= 'A' && c <= 'Z') hasUpper = true;
        else if (c >= 'a' && c <= 'z') hasLower = true;
        else if (c >= '0' && c <= '9') hasDigit = true;
        else if (c == '!' || c == '@' || c == '#' || c == '$') hasSpecial = true;
        else return false; // 非法字符
    }
    int types = (hasUpper ? 1 : 0)+(hasLower ? 1 : 0)+(hasDigit ? 1 : 0);
    return (types >= 2 && hasSpecial);
}

int main() {
    string line;
    getline(cin, line);
    stringstream ss(line);
    string pass;
    while (getline(ss, pass, ',')) {
        if (check(pass)) {
            cout << pass << endl;
        }
    }
    return 0;
}
`
    }
  ]
};
