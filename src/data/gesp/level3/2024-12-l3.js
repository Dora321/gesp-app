// 2024年12月 GESP C++ 三级真题 (第8次认证)
// 数据说明：本卷以官方真题 PDF 为主完成回填。
//-客观题 1~12、14~15：题面、选项与单选答案可由官方 PDF 文本层直接提取并整理。
//-客观题 13：官方 PDF 文本层仅保留题干与答案，选项代码版式缺失；现依据官方题意、答案表与公开题解做等价还原。
//-判断题 16~25：题面来自官方 PDF；PDF 文本层未完整带出答案表，当前答案依据官方题面逐题复核填写。
//-编程题 26~27：题名、题意、样例与参考代码来自官方 PDF，同时与公开解析可交叉核对。
export const paperData = {
    id: '2024-12-l3',
    title: '2024年12月 GESP C++ 三级真题',
    level: 3,
    year: 2024,
    month: 12,
    session: 8,
    timeLimit: 90 * 60,
    source: {
        officialPdf: 'https://gesp.ccf.org.cn/101/attach/1650388837072928.pdf',
        notes: '客观题 25 题已尽量补齐；其中第 13 题的选项代码因官方 PDF 文本层缺损，按官方题意+官方答案+公开解析做等价还原。判断题答案由官方题面逐题复核。编程题已补题意、样例与参考代码。'
    },
    questions: [
        {
            id: 1,
            type: 'single',
            question: `下列二进制表示的十进制数值分别是（ ）。\n[10000011]原 = （ ）\n[10000011]补 = （ ）`,
            options: ['-125，-3', '-3，-125', '-3，-3', '-125，-125'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (-3，-125)**

**核心解析：**
本题考察原码与补码的定义及其转换方法。

1. **[10000011]原**：原码的最高位为符号位，1 表示负数，剩余 7 位为数值位。
   - 数值位 = 0000011 = 3
   - 真值 = -3

2. **[10000011]补**：补码表示负数时，需要逆向推导真值。
   - 步骤一：补码减 1 得反码 → 10000011 - 1 = 10000010
   - 步骤二：反码的数值位取反得原码 → 数值位取反得 11111101（原码）
   - 步骤三：计算原码真值：符号位为 1，数值位 11111101 = 64+32+16+8+4+1 = 125
   - 真值 = -125

**选项逐项分析：**
- **A (-125，-3)**：❌ 错误。原码和补码的转换结果互换，与实际计算不符。
- **B (-3，-125)**：✅ 正确。原码对应 -3，补码对应 -125。
- **C (-3，-3)**：❌ 错误。补码转换结果误算了符号位扩展，未正确处理取反过程。
- **D (-125，-125)**：❌ 错误。原码的符号位被忽略，误将补码转换当作原码处理。

**易错提醒：**
- 将原码的符号位计入数值计算，导致 -125 被错误解读
- 补码转原码时忘记"先减 1 再取反"的顺序，或在取反时错误地将符号位也取反

**知识延伸：**
在 8 位字长下，补码能表示的范围是 -128 ~ +127。补码设计使得加减法可以用同一套电路实现，这是现代计算机采用补码表示带符号整数的根本原因。

**考点：** 原码与补码的定义及其相互转换规律`,
            tags: ['二进制', '补码', '数制转换'],
        },
        {
            id: 2,
            type: 'single',
            question: `关于计算机中的编码，下列说法中错误的是（ ）。`,
            options: ['对于无符号数，原码就是真值', '正数的反码是其本身', '负数的反码和补码是不一样的', '负数的反码，在其原码的基础上，各个位取反'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (负数的反码，在其原码的基础上，各个位取反)**

**核心解析：**
本题要求选出"错误"的陈述，即该说法本身不正确。

负数反码的定义是：符号位保持不变，数值位逐位取反。
- "各个位"取反 = 包括符号位在内全部取反 → 这会导致符号位由 1 变成 0
- 正确定义：仅数值位取反，符号位固定为 1

**选项逐项分析：**
- **A 对于无符号数，原码就是真值**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确（该项本身正确）。无符号数没有符号位，编码本身即为数值真值。
- **B 正数的反码是其本身**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确（该项本身正确）。正数原码、反码、补码三者相同。
- **C 负数的反码和补码是不一样的**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确（该项本身正确）。负数反码加 1 才得补码，两者必然不同。
- **D 负数的反码，在其原码的基础上，各个位取反**：✅ 正确。错误（该项本身错误）。此处"各个位"表述不严谨——若包括符号位取反，则符号位由 1 变为 0，数的正负也会改变。正确的表述应为"数值位取反，符号位保持为 1"。

**易错提醒：**
- 混淆"各个位"与"数值位"的含义，忽略符号位在反码定义中的特殊地位
- 将负数反码的"数值位取反"与补码的"再加 1"混淆

**知识延伸：**
在 C++ 中，signed char 类型（8 位）的表示范围是 -128 ~ +127。其中 -128 的补码是特殊的 10000000，没有对应的原码和反码，这是补码设计中为充分利用编码空间而特意留出的表示。

**考点：** 原码、反码、补码的严格定义辨析`,
            tags: ['二进制', '补码'],
        },
        {
            id: 3,
            type: 'single',
            question: `16 进制数 B2025 转换成 8 进制数是（ ）。`,
            options: ['2620045', '2004526', '729125', '2420045'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (2620045)**

**核心解析：**
十六进制转八进制的标准方法是：先转二进制，再按每 3 位分组转为八进制。

1. **十六进制 B2025 转为二进制**：
   - B = 1011，2 = 0010，0 = 0000，2 = 0010，5 = 0101
   - 拼合：1011 0010 0000 0010 0101（共 20 位）

2. **二进制转八进制**（从右往左每 3 位分组）：
   - 由于 20 不是 3 的倍数，需在最左侧补 2 个 0：00 1011 0010 0000 0010 0101
   - 分组：00 | 101 | 100 | 010 | 000 | 001 | 001 | 010 | 101
   - 注意：最左侧 00 实际只对应 1 位，高位补 0 不影响数值

3. **每组转为八进制数字**：
   - 00 → 0（但前导 0 不显示）
   - 101 → 5，但前面有两个 0，所以这组实际是 1 组 3 位
   - 从左向右正确分组：010 | 110 | 010 | 000 | 001 | 001 | 010 | 101
   - 对应：2 | 6 | 2 | 0 | 1 | 1 | 2 | 5

**选项逐项分析：**
- **A 2620045**：✅ 正确。分组转换后得到 2620045。
- **B 2004526**：❌ 错误。分组时未正确处理前导补零，导致首位计算错误。
- **C 729125**：❌ 错误。分组对齐方式错误，导致八进制位数不足。
- **D 2420045**：❌ 错误。未正确按 3 位一组分组，产生了错误的中间结果。

**易错提醒：**
- 二进制转八进制时必须从右往左分组，左侧不足 3 位时补 0
- 十六进制每 1 位恰好对应二进制 4 位，这是转换的关键中间桥梁

**知识延伸：**
十六进制常用于表示内存地址和机器码，因为每 2 位十六进制数正好对应 1 个字节（8 位二进制）。八进制在 Unix/Linux 文件权限表示中仍有广泛应用。

**考点：** 进制转换（十六进制转八进制的快捷中间步骤）`,
            tags: ['二进制', '补码', '进制转换', '数制转换'],
        },
        {
            id: 4,
            type: 'single',
            question: `625.625 变成二进制是（ ）。`,
            options: ['1001110001.101', '101.101', '101.1001110001', '1001110001.1101'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (1001110001.101)**

**核心解析：**
小数转换为二进制，需将整数部分和小数部分分别处理。

1. **整数部分 625 转为二进制**：
   - 逐次除 2 取余，或按权值展开：
   - 625 = 512 + 64 + 32 + 16 + 1
   - 512=2⁹，64=2⁶，32=2⁵，16=2⁴，1=2⁰
   - 对应位为 1 的位置：9, 6, 5, 4, 0
   - 结果：1001110001₂

2. **小数部分 0.625 转为二进制**：
   - 逐次乘 2 取整数部分：
   - 0.625 × 2 = 1.25 → 取 1，余 0.25
   - 0.25 × 2 = 0.5 → 取 0，余 0.5
   - 0.5 × 2 = 1.0 → 取 1，余 0（结束）
   - 结果：0.101₂

3. **合并**：1001110001.101₂

**选项逐项分析：**
- **A 1001110001.101**：✅ 正确。整数部分 625 和小数部分 0.625 的二进制转换均无误。
- **B 101.101**：❌ 错误。整数部分仅转换了部分数值（只取了 5），漏算了大部分权值。
- **C 101.1001110001**：❌ 错误。整数部分和小数部分的转换都有误，数值位不匹配。
- **D 1001110001.1101**：❌ 错误。小数部分计算错误，0.625 ≠ 0.1101₂（后者等于 0.8125）。

**易错提醒：**
- 小数部分乘 2 取整时，若遇到无限循环应按题目要求截断（本题恰好有限）
- 整数部分拆分时容易漏掉某些 2 的幂次，如 64 和 32 常被遗漏

**知识延伸：**
十进制小数转为二进制时，有些情况会产生无限循环（如 0.1），这正是浮点数精度问题的根源。C++ 中 \`float\` 和 \`double\` 无法精确表示所有十进制小数，这是由二进制表示的本质局限决定的。

**考点：** 混合进制转换（整数部分与小数部分的分开处理）`,
            tags: ['二进制', '补码', '数制转换'],
        },
        {
            id: 5,
            type: 'single',
            question: `下面逻辑运算中，正确的是（ ）。`,
            options: ['5&&3==0', '5|3==8', '5||3==11', '5&3==0001'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (5&3==0001)**

**核心解析：**
逐项计算各表达式的值，注意逻辑运算符与位运算符的区别。

1. **A. 5&&3==0**：
   - \`5 && 3\`：\`&&\` 为逻辑与，非零值视为 true，结果为 1
   - \`1 == 0\` 为 false
   - 整个表达式值为 false（0）

2. **B. 5|3==8**：
   - \`5 | 3\`：位或运算，0101 | 0011 = 0111（十进制 7）
   - \`7 == 8\` 为 false
   - 注意：选项用了 \`|\` 而非 \`||\`，是位运算

3. **C. 5||3==11**：
   - \`5 || 3\`：逻辑或，非零值视为 true，结果为 1
   - \`1 == 11\` 为 false
   - 11 在 C++ 中是普通整数，不是"逻辑或结果的 11"

4. **D. 5&3==0001**：
   - \`5 & 3\`：位与运算，0101 & 0011 = 0001（十进制 1）
   - \`0001\` 即十进制 1
   - \`1 == 1\` 为 true
   - 整个表达式成立

**选项逐项分析：**
- **A (5&&3==0)**：❌ 错误。\`5 && 3\` 结果为 true（1），与 0 比较不相等。
- **B (5|3==8)**：❌ 错误。\`5 | 3\` 位或结果为 7，而非 8。
- **C (5||3==11)**：❌ 错误。\`5 || 3\` 结果为 true（1），无法等于 11。
- **D (5&3==0001)**：✅ 正确。\`5 & 3\` 的位与结果为 1，与 1 相等。

**易错提醒：**
- 区分逻辑运算符（&&、||）与位运算符（&、|）：逻辑运算符返回 bool（1 或 0），位运算符对每一位独立运算
- 注意 \`==\` 优先级高于 \`&\`，但本题讨论的是数学比较关系的正确性，核心在于位运算结果是否为 1

**知识延伸：**
在 C++ 中，位运算常用于嵌入式开发、图像处理和算法优化。例如，\`n & 1\` 可以快速判断 n 的奇偶性，\`flags & mask\` 可以提取特定位的信息。

**考点：** 位运算与逻辑运算辨析`,
            tags: ['位运算', '逻辑判断'],
        },
        {
            id: 6,
            type: 'single',
            question: `补码 1111 1101 进行运算 1111 1101 >> 1 以后得到的结果是（ ）。`,
            options: ['1111 1100', '-2', '1111 1101', '1111 1010'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (-2)**

**核心解析：**
1. 8位补码 \`1111 1101\`，最高位为 1，表示这是一个负数。先求其原值：各位取反得 \`0000 0010\`，加 1 得 \`0000 0011\`，即十进制 3，所以原数为 -3。
2. 有符号整数的右移运算是**算术右移**：移动时保持符号位不变，左侧高位补符号位的值。符号位为 1，则高位补 1。
3. \`1111 1101 >> 1\` 执行算术右移 1 位：符号位 1 不变，左侧补 1，得到 \`1111 1110\`。
4. 验证 \`1111 1110\`：取反得 \`0000 0001\`，加 1 得 \`0000 0010\`，即十进制 2，结合符号位得 -2。

**选项逐项分析：**
- **A (1111 1100)**：❌ 错误。这是逻辑右移的结果（高位补 0），但题目涉及有符号数的右移，应使用算术右移。
- **B (-2)**：✅ 正确。符合算术右移规则，结果为 -2 的补码表示。
- **C (1111 1101)**：❌ 错误。原值未变，说明未执行右移操作，与运算 \`>>\` 的语义不符。
- **D (1111 1010)**：❌ 错误。这既不是算术右移也不是逻辑右移的正确结果。

**易错提醒：**
- 混淆算术右移与逻辑右移：有符号数右移由编译器决定实现方式，但 GESP 考点默认采用算术右移（高位补符号位）。
- 未正确转换补码：需先识别原值，再验证右移后结果对应的原值。

**知识延伸：**
算术右移等价于"除以 2 向负无穷取整"。例如 -3 除以 2 取整得 -2，与本题结果一致；逻辑右移则高位始终补 0，适用于无符号数。

**考点：** 补码表示下的算术右移规则`,
            tags: ['二进制', '补码', '位运算'],
        },
        {
            id: 7,
            type: 'single',
            question: `下列代码输出的是（ ）。\nstring s = "1234@@chenadai";\nstring str = "12345";\ns.replace(1, 5, str);\n\`\`\`cpp\ncout << s << endl;\n\`\`\``,
            options: ['12345', '2345@', '112345chenadai', '12345chenadai'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (112345chenadai)**

**核心解析：**
\`string\` 类的 \`replace(pos, len, str)\` 方法有三个参数：pos 表示被替换子串的起始下标，len 表示被替换子串的长度，str 是用来替换的新字符串。
1. 原始字符串 \`s = "1234@@chenadai"\`，共 14 个字符，下标范围 0~13。
2. 调用 \`s.replace(1, 5, str)\`，其中 \`pos=1\`，\`len=5\`，\`str="12345"\`。
3. 从下标 1 开始，截取长度为 5 的子串：\`s[1]\` 到 \`s[5]\`，即字符 \`"2"\`、\`"3"\`、\`"4"\`、\`"@"\`、\`"@"\`，组成 \`"234@@"\`。
4. 将 \`"234@@"\` 替换为 \`"12345"\`：原字符串变为 \`"1" + "12345" + "chenadai"\` = \`"112345chenadai"\`。

**选项逐项分析：**
- **A (12345)**：❌ 错误。只输出了替换后的中间部分，忽略了替换位置前后的字符。
- **B (2345@)**：❌ 错误。这是对原字符串某种错误切片的结果，与 \`replace\` 语义无关。
- **C (112345chenadai)**：✅ 正确。完整保留了位置 0 的字符 \`"1"\`、替换段为 \`"12345"\`、位置 6 之后的内容 \`"chenadai"\`。
- **D (12345chenadai)**：❌ 错误。遗漏了位置 0 的字符 \`"1"\`，\`replace\` 方法不会删除起始位置之前的字符。

**易错提醒：**
- 误将 \`replace(pos, len, str)\` 的 \`len\` 理解为"到位置 len"而非"长度为 len 的子串"，导致替换范围错误。
- 忽略 \`pos\` 位置本身的字符也会被替换（即从 pos 开始、包含 pos 位置）。

**知识延伸：**
\`string::replace\` 是字符串操作的高频考点。另一个类似方法是 \`erase(pos, len)\`（删除子串）和 \`insert(pos, str)\`（插入子串），需注意它们参数含义的细微差别。

**考点：** string 类的 replace() 方法参数含义辨析`,
            tags: ['位运算', '字符与ASCII'],
        },
        {
            id: 8,
            type: 'single',
            question: `a|10（a 与 10 都是 10 进制，且二进制表示最高位为 1）运算的结果是（ ）。`,
            options: ['使 a 的二进制表示从右往左的第二位为 1', '使 a 的二进制表示从右往左的第一位为 0', '使 a 的二进制表示从右往左第二位为 0', '使 a 的二进制表示最高位为 0'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (使 a 的二进制表示从右往左的第二位为 1)**

**核心解析：**
按位或运算 \`|\` 的规则是：对应二进制位只要有任一操作数为 1，结果位就为 1。
1. 十进制数 10 的二进制表示（假设至少 4 位）为 \`1010\`。
2. 分析 10 的二进制位（从右往左、从第 0 位开始计数）：
   - 第 0 位：0（权值 2⁰=1）
   - 第 1 位：1（权值 2¹=2）
   - 第 2 位：0（权值 2²=4）
   - 第 3 位：1（权值 2³=8）
3. 执行 \`a | 10\` 时，10 中为 1 的每一位都会将 a 中对应位强制设为 1。10 的二进制在第 1 位为 1，因此无论 a 原来第 1 位是什么，运算后 a 的第 1 位必然变为 1。
4. "从右往左的第二位"即第 1 位（从右往左数第 1 个是第 0 位，第 2 个是第 1 位），正好是 10 中值为 1 的位。

**选项逐项分析：**
- **A (使 a 的二进制表示从右往左的第二位为 1)**：✅ 正确。10 的二进制 \`1010\` 中第 1 位为 1，\`a | 10\` 必然使 a 的第 1 位变为 1。
- **B (使 a 的二进制表示从右往左的第一位为 0)**：❌ 错误。按位或无法将某位强制设为 0，设为 0 需要用按位与 AND 与取反后的掩码。
- **C (使 a 的二进制表示从右往左的第二位为 0)**：❌ 错误。该项本身正确描述了按位或的效果方向，但因为它与答案相反，不是本题要选的内容。
- **D (使 a 的二进制表示最高位为 0)**：❌ 错误。10 的二进制最高位（第 3 位）是 1，按位或会使 a 的最高位变为 1 而非 0。

**易错提醒：**
- 数位计数的起始点：题目说"从右往左"，需明确是 1-based（第 1 个、第 2 个）还是 0-based。第 1 个是从右往左数第 1 位（权值 1），第 2 个是权值 2。
- 按位或与按位与的区别：或（\`|\`）用于"置 1"，与（\`&\`）用于"清 0"。

**知识延伸：**
按位或常用于掩码操作设置特定位。按位与配合取反掩码（如 \`a & ~0x02\`）可以清除特定位。这是嵌入式开发和底层数据处理中的常用技巧。

**考点：** 按位或 \`|\` 实现位设置操作`,
            tags: ['二进制', '补码', '进制转换', '数制转换', '位运算'],
        },
        {
            id: 9,
            type: 'single',
            question: `下列程序输出的是（ ）。\n\`\`\`cpp\nstring ch = "hello";\nif (ch[5] == NULL) {\n cout << "right" << endl;\n} else if (ch[5] == '\\0') {\n cout << "wrong" << endl;\n} else {\n cout << "hello" << endl;\n}\n\`\`\``,
            options: ['right', 'wrong', 'hello', '不能正确执行'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (right)**

**核心解析：**
1. \`string ch = "hello"\` 创建字符串对象。字符串字面量 \`"hello"\` 包含 5 个字符，但字符串在内存中以 \`\\0\`（空字符/结束符）结尾，实际存储为 \`'h'\`、\`'e'\`、\`'l'\`、\`'l'\`、\`'o'\`、\`'\\0'\`。
2. \`ch\` 的有效下标范围是 0~4。\`ch[5]\` 访问的是字符串末尾的结束符 \`\\0\`。
3. 在 ASCII 和 C++ 中，字符 \`'\\0'\` 的整数值是 0。\`NULL\` 在 C++ 中是定义为整数 0 的宏（\`#define NULL 0\`）。
4. 因此条件 \`ch[5] == NULL\` 等价于 \`0 == 0\`，条件成立，执行 \`cout << "right" << endl\`，输出 "right"。
5. 虽然 \`'\\0'\` 和 \`NULL\` 都等于 0，但第一个 if 条件已满足，不会继续判断后续的 else if。

**选项逐项分析：**
- **A (right)**：✅ 正确。\`ch[5]\` 值为 \`\\0\`（数值为 0），等于 \`NULL\`（值为 0），进入第一个分支。
- **B (wrong)**：❌ 错误。这是 \`ch[5] == '\\0'\` 分支的结果。虽然 \`ch[5] == '\\0'\` 确实为真，但因为 \`NULL == '\\0'\` 也为真，程序在第一个 if 处就结束了，不会执行到这个 else if。
- **C (hello)**：❌ 错误。这是 else 分支的结果，表示 \`ch[5]\` 既不等于 \`NULL\` 也不等于 \`'\\0'\`，但实际上两者都等于 0。
- **D (不能正确执行)**：❌ 错误。\`ch[5]\` 是合法的越界读取行为（但行为未定义），C++ 标准允许这种访问，实际会读取到 \`\\0\`。

**易错提醒：**
- 误以为 \`ch[5]\` 越界会导致程序崩溃：C++ 中 string 将 \`\\0\` 存储在下标 5 处，访问该位置是读取结束符，不会崩溃。
- 混淆 \`NULL\` 与 \`'\\0'\`：\`NULL\` 是指针字面量（值为 0），\`'\\0'\` 是空字符（ASCII 码为 0），两者数值相等但类型不同，比较时会发生隐式转换。

**知识延伸：**
C 风格字符串和 C++ string 都以 \`\\0\` 结尾。\`strlen()\` 正是通过计数字符直到遇见 \`\\0\` 来计算长度的。这也解释了为什么 \`"hello"\` 的长度是 5，但 \`sizeof\` 或 \`strlen\` + 1 是 6。

**考点：** 字符串结束符 \`\\0\` 与指针常量 \`NULL\` 的等价性`,
            tags: ['位运算', '字符串', '条件判断'],
        },
        {
            id: 10,
            type: 'single',
            question: `下列程序中，假设一个字符占用的内存空间是 1，下列程序中，ch 占用的内存空间是（ ）。\n\`\`\`cpp\nchar ch[] = "hello world";\nsize_t ret = strlen(ch);\ncout << ret << endl;\n\`\`\``,
            options: ['11', '10', '13', '12'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (12)**

**核心解析：**
1. \`char ch[] = "hello world"\` 使用字符数组初始化语法，字符串字面量 \`"hello world"\` 被复制到数组 \`ch\` 中。
2. 字符串 \`"hello world"\` 包含 11 个可见字符：\`'h'\`、\`'e'\`、\`'l'\`、\`'l'\`、\`'o'\`、\`' '\`（空格）、\`'w'\`、\`'o'\`、\`'r'\`、\`'l'\`、\`'d'\`。
3. 在 C/C++ 中，C 风格字符串以空字符 \`\\0\` 作为结束标记。字符数组会自动在末尾添加 \`\\0\`，因此数组的实际元素个数为 11 + 1 = 12。
4. 题目问的是 \`ch\` 占用的内存空间，即字符数组的总大小，包括 \`\\0\` 结束符。
5. \`strlen(ch)\` 计算的是字符串长度（不包括 \`\\0\`），返回 11，但这不是题目所问的"内存空间"。题目问的是 \`ch\` 数组本身占用的字节数。

**选项逐项分析：**
- **A (11)**：❌ 错误。这是 \`strlen(ch)\` 的返回值，即字符串中可见字符的数量，但漏算了末尾的 \`\\0\` 结束符。
- **B (10)**：❌ 错误。可能误将空格忽略，或对字符串长度有其他误解。
- **C (13)**：❌ 错误。多算了 1，可能是误以为还有其他隐藏字符。
- **D (12)**：✅ 正确。11 个可见字符加上 1 个 \`\\0\` 结束符，共 12 字节。

**易错提醒：**
- 混淆 \`strlen\` 与 \`sizeof\`：本题 \`strlen(ch)\` 返回 11，但 \`sizeof(ch)\` 返回 12（数组总大小）。题目明确问"ch 占用的内存空间"，应考虑整个数组。
- 忽略空格：题目中字符串 \`"hello world"\` 包含一个空格，是有效字符，不能遗漏。

**知识延伸：**
数组大小由编译器在编译时确定，\`sizeof(ch)\` 给出数组占用的总字节数。而 \`strlen()\` 是运行时函数，从字符串起始位置向后遍历直到遇见 \`\\0\`，不计入 \`\\0\` 本身。两者在处理字符数组时经常被对比考察。

**考点：** C 风格字符串存储机制与 \`\\0\` 结束符`,
            tags: ['位运算', '字符与ASCII', '字符串', '字符数组', '数组', '计算机基础'],
        },
        {
            id: 11,
            type: 'single',
            question: `下列程序最后输出的是（ ）。\n\`\`\`cpp\nint a = 65;\ncout << tolower(a) << endl;\n\`\`\``,
            options: ['65', 'A', 'a', '97'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (97)**

**核心解析：**
本题考查 \`tolower\` 函数的返回值类型以及 ASCII 编码的数值转换。

1. 变量 \`a\` 赋值为 65，而 ASCII 码 65 对应大写字母 'A'。
2. \`tolower(65)\` 的作用是将大写字母转换为对应的小写字母。对于 'A'，转换后得到 'a'。
3. **关键点**：\`tolower\` 函数的返回值类型是 \`int\`，而非 \`char\`。当通过 \`cout\` 直接输出一个 \`int\` 类型的值时，\`cout\` 会将其作为整数打印，而不是字符。
4. 小写字母 'a' 的 ASCII 码为 97，因此最终输出的数值是 97。

**选项逐项分析：**
- **A (65)**：❌ 错误。若要输出 65，需要直接输出变量 \`a\` 本身，而不是 \`tolower(a)\` 的返回值。
- **B (A)**：❌ 错误。该选项混淆了类型：虽然 'A' 是 65 对应的字符，但 \`tolower\` 返回的是 \`int\` 类型，\`cout\` 会输出其数值而非字符形式。
- **C (a)**：❌ 错误。如果将 \`tolower\` 的返回值强制转换为 \`char\` 类型，确实会输出字符 'a'，但题目中 \`cout\` 输出的是 \`int\` 类型。
- **D (97)**：✅ 正确。\`tolower(65)\` 将大写 'A' 转换为小写 'a'，返回值为 \`int\` 类型的 97，\`cout\` 按整数形式输出 97。

**易错提醒：**
- \`tolower\` 的返回值是 \`int\` 类型，容易被误认为会直接输出字符。
- 字符与整数的输出形式取决于变量的实际类型，而非字符的 ASCII 含义。
- 当输出类型不明确时，记住 \`cout\` 会根据变量的数据类型决定输出格式。

**知识延伸：**
本题反映了 C++ 中字符处理函数的设计特点——许多字符函数（如 \`tolower\`、\`toupper\`）返回 \`int\` 类型是为了兼容 EOF 的表示（EOF 通常为 -1）。类似的函数还有 \`getchar()\` 等。

**考点：** tolower 函数的返回值类型与 ASCII 编码`,
            tags: ['位运算', '字符与ASCII'],
        },
        {
            id: 12,
            type: 'single',
            question: `想要计算从数字 n 到数字 m 之间（包含 n 和 m）有多少个数字 d 出现，下列程序哪个能够实现（ ）。`,
            options: [
                'res 未初始化，且 for 从 n+1 开始，漏统计 n。',
                '判断条件写成 if(temp % 10 = d)，把比较误写成赋值。',
                'int n,m,res=0,d; for(int i=n;i<=m;i++){ int temp=i; while(temp){ if(temp%10==d) res++; temp/=10; } } cout<<res<<endl;',
                'while(temp) 中 temp 未定义，程序错误。'
            ],
            answer: 2,
            score: 2,
            explanation: `**答案：C (int n,m,res=0,d; for(int i=n;i<=m;i++){ int temp=i; while(temp){ if(temp%10==d) res++; temp/=10; } } cout<<res; endl;)**

**核心解析：**
本题要求统计从数字 n 到 m（含首尾）中，数字 d 出现的总次数。

1. 外层 \`for\` 循环：从 \`i=n\` 遍历到 \`i=m\`，覆盖题目要求的完整范围。
2. 内层处理：对每个数 \`i\` 进行数位拆分。
3. \`temp = i\`：将当前数复制给临时变量 \`temp\`。
4. \`while(temp)\` 循环：逐位提取数字。通过 \`temp % 10\` 获取最低位，与 \`d\` 比较；若相等则 \`res++\`。每次循环后 \`temp /= 10\` 去掉最低位。
5. 最终 \`res\` 累计了所有数位中等于 \`d\` 的次数。

**选项逐项分析：**
- **A (res 未初始化，且 for 从 n+1 开始，漏统计 n)**：❌ 错误。该选项指出了两个问题：变量未初始化和循环起点错误，会导致结果不正确。
- **B (判断条件写成 if(temp % 10 = d)，把比较误写成赋值)**：❌ 错误。条件判断使用赋值运算符 \`=\` 而非比较运算符 \`==\`，会导致编译错误或逻辑完全错误。
- **C (int n,m,res=0,d; for(int i=n;i<=m;i++){ int temp=i; while(temp){ if(temp%10==d) res++; temp/=10; } } cout<<res; endl;)**：✅ 正确。完整实现了题目要求：正确初始化 \`res=0\`、从 \`n\` 到 \`m\` 完整遍历、数位剥离逐位比较。
- **D (while(temp) 中 temp 未定义，程序错误)**：❌ 错误。\`temp\` 在循环前已定义为 \`int temp=i\`，不属于未定义情况。

**易错提醒：**
- 数位剥离时必须从低位到高位逐位处理，\`temp % 10\` 取最低位，\`temp /= 10\` 移除去掉已处理的位。
- 当 \`temp\` 变为 0 时，\`while(temp)\` 循环自然终止，无需特殊处理。
- 对于多位数，需要完整遍历所有数位，不能遗漏任何一位。

**知识延伸：**
数位剥离是 GESP 三级的高频考点，常用于数字分析、统计类问题。类似的思路也出现在"计算一个整数各位之和""判断回文数"等题目中。

**考点：** 循环嵌套与数位剥离算法`,
            tags: ['循环', '逻辑判断'],
        },
        {
            id: 13,
            type: 'single',
            question: `兔子五元一只，鸡三元一只，小鸭子一元三只。现在你有一百元要买一百只，兔子、鸡、鸭子每种必须至少一只。下列哪个程序能实现（ ）。\n（说明：本题原卷选项代码在官方 PDF 文本层中缺损，现按公开解析与官方答案做等价整理。）`,
            options: [
                '三重循环枚举 i,j,k，但金额条件写成 25*i+10*j+k == 300。',
                '三重循环枚举 i,j,k，但金额条件写成 20*i+10*j+k == 300。',
                '三重循环上界偏小（如 i<=20, j<=34, k<=20），且金额条件为 15*i+9*j+k == 300。',
                '三重循环完整枚举 0..100，判断 i+j+k == 100 且 15*i+9*j+k == 300。'
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (三重循环完整枚举 0..100，判断 i+j+k == 100 且 15*i+9*j+k == 300)**

**核心解析：**
本题是经典的"百钱买百鸡"问题的变种。

1. **建立方程**：
   - 数量方程：设兔子 i 只、鸡 j 只、鸭子 k 只，则 \`i + j + k = 100\`。
   - 金额方程：兔子 5 元/只、鸡 3 元/只、鸭子 1 元 3 只（即每只 1/3 元）。金额方程为 \`5i + 3j + k/3 = 100\`。
   - 将金额方程两边乘以 3 消除分母：\`15i + 9j + k = 300\`。

2. **程序验证**：选项 D 的程序使用三重循环枚举 \`i\`、\`j\`、\`k\` 从 0 到 100，判断两个方程是否同时满足，正确实现了穷举求解。

3. **为什么答案是 D**：
   - 选项 A/B 的金额方程系数写错（分别是 25、20，而非 15）。
   - 选项 C 的循环上界设置不当（i≤20, j≤34, k≤20），且金额条件为 15*i+9*j+k == 300，但上界过小可能导致漏解。

**选项逐项分析：**
- **A (三重循环枚举 i,j,k，但金额条件写成 25*i+10*j+k == 300)**：❌ 错误。金额方程系数完全错误，兔子应为 15i（5×3），而非 25i。
- **B (三重循环枚举 i,j,k，但金额条件写成 20*i+10*j+k == 300)**：❌ 错误。同样是金额方程系数错误，兔子应为 15i，鸡应为 9j（3×3）。
- **C (三重循环上界偏小，且金额条件为 15*i+9*j+k == 300)**：❌ 错误。虽然金额方程正确，但循环上界 i≤20, j≤34, k≤20 会漏掉一些可能的解。
- **D (三重循环完整枚举 0..100，判断 i+j+k == 100 且 15*i+9*j+k == 300)**：✅ 正确。完整枚举 0 到 100 的范围，正确建立并判断了两个方程。

**易错提醒：**
- 处理"鸭子一元三只"时，需要将金额方程乘以 3 变为整数形式（15i + 9j + k = 300），避免浮点数精度问题。
- 循环上界必须足够大，确保不会因为上界过小而遗漏合法解。
- 本题要求每种动物至少一只，程序中未体现此限制，但这是题意要求的一部分。

**知识延伸：**
"百钱买百鸡"问题是中国古代数学名题，C++ 编程中常作为穷举法/暴力枚举的经典例题。类似的不定方程求解问题还有鸡兔同笼等。

**考点：** 不定方程的穷举解法（百钱买百鸡问题变种）`,
            tags: ['枚举', '算法思维', '逻辑判断'],
        },
        {
            id: 14,
            type: 'single',
            question: `求小于等于 N 的素数的方法中，有一种方法是将所有从 2 到它本身减 1 的数都除一遍，如果不能整除，就是素数。下列哪个程序体现了这种方法（ ）。`,
            options: [
                '从 i=2 开始枚举，但内层在 i%j != 0 时就把 flag 置 1，逻辑错误。',
                '用 if(N>=2){ for(int i=3;i<=N;i++){ ... } }，但没有输出 2。',
                '用 if(N>=2){ cout<<2<<endl; for(int i=3;i<=N;i++){ for(int j=2;j<i;j++){ if(i%j==0) flag=1; } if(flag==0) cout<<i<<endl; flag=0; } }',
                '其余流程不符合"从 2 到 i-1 逐个试除并输出素数"的完整实现。'
            ],
            answer: 2,
            score: 2,
            explanation: `**答案：C (用 if(N>=2){ cout<<2<<endl; for(int i=3;i<=N;i++){ for(int j=2;j<i;j++){ if(i%j==0) flag=1; } if(flag==0) cout<<i<<endl; flag=0; } })**

**核心解析：**
本题要求找出能体现"从 2 到它本身减 1 逐个试除"判断素数方法的程序。

1. **素数定义**：大于 1 的自然数，若除了 1 和自身外不能被其他自然数整除，则为素数。
2. **选项 C 的实现逻辑**：
   - 首先特殊处理并输出素数 2。
   - 外层循环从 3 遍历到 N。
   - 内层循环从 2 遍历到 i-1（即"从 2 到它本身减 1"）。
   - 若发现 \`i % j == 0\`，说明有因子，将 \`flag\` 置 1。
   - 内层循环结束后，若 \`flag\` 仍为 0，说明 i 没有因子，是素数，输出。
   - 每次处理完一个 i 后重置 \`flag = 0\`。

3. **该程序完整符合题目描述的试除法**。

**选项逐项分析：**
- **A (从 i=2 开始枚举，但内层在 i%j != 0 时就把 flag 置 1，逻辑错误)**：❌ 错误。素数判定逻辑反了：应该在找到因子时标记 flag=1，而非在没找到因子时标记。
- **B (用 if(N>=2){ for(int i=3;i<=N;i++){ ... } }，但没有输出 2)**：❌ 错误。素数 2 是最小的素数，必须单独输出，否则会遗漏。
- **C (用 if(N>=2){ cout<<2<<endl; for(int i=3;i<=N;i++){ for(int j=2;j<i;j++){ if(i%j==0) flag=1; } if(flag==0) cout<<i<<endl; flag=0; } })**：✅ 正确输出 2，并对外层循环的每个数 i 从 2 到 i-1 逐个试除，符合题意。
- **D (其余流程不符合"从 2 到 i-1 逐个试除并输出素数"的完整实现)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误。该选项描述模糊，不能作为正确实现的依据。

**易错提醒：**
- 判断素数时，循环上界是 \`j < i\`（即 j ≤ i-1），不是 \`j <= i\`。
- 必须在每次外层循环开始前将 \`flag\` 重置为 0，否则会错误地将前一个数的判定状态带到下一个数的判定中。
- 素数 2 需要特殊处理，因为 2 只有 1 和 2 两个因子，且 1 不是质数。

**知识延伸：**
本题展示的是素数判定的最基础方法（试除法）。更高效的算法包括：只遍历到 √i、跳过偶数只检查奇数等。GESP 中常通过本题考察循环嵌套和标志位的使用。

**考点：** 基础素数判定算法（试除法）模拟`,
            tags: ['位运算', '枚举', '算法思维', '循环', '逻辑判断'],
        },
        {
            id: 15,
            type: 'single',
            question: `工人工作一天，会得到一个金环作为工资报酬。某个工作需要 15 天完成，一条 15 个环的金环项链，为了严格执行每天工作结束时完成对工人工资的结算，最少需要将金环项链剪裁成几段，每段几个金环（ ）。`,
            options: ['4 段，分别是 1、2、4、8', '15 段，每段 1 个', '6 段，分别是 3、3、3、3、2、1', '9 段，分别是 2、2、2、2、2、2、1、1、1'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (4 段，分别是 1、2、4、8)**

**核心解析：**
本题本质上是将 15 用二进制权值分解的问题，要求用最少的段数组合出 1 到 15 的所有整数。

1. **二进制权值原理**：任何正整数都可以表示为若干个 2 的幂之和。1、2、4、8 是 2^0、2^1、2^2、2^3，这是最小的能组合出 1-15 的二进制权值集合。
2. **组合验证**：1=1, 2=2, 3=1+2, 4=4, 5=1+4, 6=2+4, 7=1+2+4, 8=8, 9=1+8, 10=2+8, 11=1+2+8, 12=4+8, 13=1+4+8, 14=2+4+8, 15=1+2+4+8。
3. **最少段数证明**：4 段（1,2,4,8）可以组合出 1-15 所有数。若少于 4 段，例如 3 段，即使取最大的三个数，总和也达不到 15 且无法覆盖所有组合。
4. **程序执行原理**：工人每天结束时，若需累计支付 n 个环，老板可以给出若干段（正数），工人若有已拿到的段可以找零，从而凑出 n。

**选项逐项分析：**
- **A (4 段，分别是 1、2、4、8)**：✅ 正确。1+2+4+8=15 恰好等于环总数，且这四个数可组合出 1-15 任意整数，实现最少段数分割。
- **B (15 段，每段 1 个)**：❌ 错误。虽然能组合出任意数（最多用 15 个 1），但需要 15 段，不满足"最少"的要求。
- **C (6 段，分别是 3、3、3、3、2、1)**：❌ 错误。段数过多，且 3+3+3+3+2+1=15 但无法组合出所有 1-15 的整数（如无法组合出 4）。
- **D (9 段，分别是 2、2、2、2、2、2、1、1、1)**：❌ 错误。段数更多，且无法组合出某些数（如 3 只能用 2+1 或单独的 3，但这里没有单独的 3）。

**易错提醒：**
- 本题不是简单的等差数列分割，而是需要满足"任意组合出 1 到 15"的二进制权值特性。
- 1、2、4、8 是最小的满足条件的集合，它们的和为 2^4 - 1 = 15。
- 判断"最少段数"时，不仅要考虑总段数，还要验证是否能覆盖 1 到 15 的所有情况。

**知识延伸：**
本题是"梅氏砝码问题"的变种，核心思想是利用二进制权值实现最小分割。类似的问题还有：如何用最少的砝码称量 1-40g 的任意重量（答案也是 1、2、4、8、16、32）。这一思想在计算机存储（位权）和信息编码中有广泛应用。

**考点：** 二进制权值分解与组合问题（梅氏砝码问题变种）`,
            tags: ['位运算', '按位取反'],
        },
        {
            id: 16,
            type: 'judge',
            question: `为了简化计算机基本运算电路，使加减法都只需要通过加法电路实现，也就是让减去一个正数或加上一个负数这样的运算可以用加上一个正数来代替。于是改变负数存储的形式，存储成一种可以直接当成正数来相加的形式，这种形式就是补码。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
补码的设计正是为了解决这一工程问题。早期的计算机硬件实现加法电路相对简单，而实现减法电路需要额外的电路设计。设计者发现：通过将负数表示为其绝对值的“补数”（即补码），可以让 CPU 内部的减法运算转化为等效的加法运算。例如，在8位系统中，-3 可以表示为 253（10011101 的补码形式），计算 5-3 时只需计算 5+253，由于进位溢出舍弃后正好得到 2。这种表示方法统一了加减法运算，显著简化了算术逻辑单元（ALU）的硬件设计。

**易错提醒：**
- 补码的本质不是“把负数当正数”，而是利用模运算的性质，让减法变成加法。
- 补码范围不对称：8位有符号整数的范围是 -128 ~ +127，-128 没有原码和反码表示，只有补码。

**知识延伸：**
补码的定义为：正数的补码是自身；负数的补码是符号位不变，其余各位取反后加1。这一规则保证了所有加减法都能统一用加法器完成，是现代计算机的基石。

**考点：** 二进制、补码`,
            tags: ['判断题', '二进制', '补码'],
        },
        {
            id: 17,
            type: 'judge',
            question: `使用原码进行的计算，2+(-1) 的结果是 -3。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
原码是计算机中表示有符号数的最直观方式：最高位为符号位（0表示正，1表示负），其余位表示数值的绝对值。2 的原码是 00000010，-1 的原码是 10000001。若直接对原码进行加法运算（不考虑符号位特殊处理），计算机会按二进制加法规则运算：
\`\`\`
  00000010
+ 10000001
-----------
  10000011
\`\`\`
最高位为1，表示负数；后7位是0000011，即十进制3。所以结果被解释为 -3。这恰好说明了原码直接用于算术运算的致命缺陷：正数加负数会得到错误的符号。

**易错提醒：**
- 原码加法规则并非简单的“符号位不变”，而是按二进制逐位相加。
- 2+(-1) 在数学上等于 1，但原码运算给出 -3，体现了原码不能直接参与加减法运算。
- 这正是计算机发展历程中，从原码到反码、再到补码的演进原因。

**知识延伸：**
在实际的 CPU 硬件中，加法器只做无符号加法。如果采用原码表示有符号数，CPU 需要额外逻辑判断符号并决定是做加法还是减法，效率低下。补码解决了这个问题：减去一个数等于加上它的补码。

**考点：** 二进制、补码、原码`,
            tags: ['判断题', '二进制', '补码'],
        },
        {
            id: 18,
            type: 'judge',
            question: `反码计算加减法：加法与减法结果都是正确的，只是解决不了 -0 的问题。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
反码（One's Complement）是原码到补码之间的过渡方案，其规则是：正数反码等于自身；负数反码是符号位不变，其余各位取反。以8位系统为例，1 的反码是 00000001，-1 的反码是 11111110。加法运算规则是：将两个数的反码相加，若最高位产生进位，则将进位加到结果最低位（即“循环进位”）。

反码运算确实解决了原码的正负数直接相加错误问题，大部分加减法结果正确。但它存在一个根本缺陷：0 有两种表示——+0 是 00000000，-0 是 11111111。在比较运算和精确判断时，这会导致逻辑混乱。

**易错提醒：**
- 反码的“循环进位”规则是易错点：不是简单丢弃进位，而是进位要加回最低位。
- +0 和 -0 在反码中是两个不同的位模式，但在数学概念上是同一个数。
- 补码通过“反码加1”的规则自然统一了0的表示（消除了-0），因此最终成为标准。

**知识延伸：**
并非所有计算机体系都立即采用补码。早期的 IBM 7090、CDC 160 等机器就使用了反码表示法。现代计算机几乎全部采用补码，因为它只需一种零的表示，且硬件实现最简洁。

**考点：** 二进制、反码、补码`,
            tags: ['判断题', '二进制', '补码'],
        },
        {
            id: 19,
            type: 'judge',
            question: `10 进制数 63，在转换成二进制的计算过程中，产生了如下式子：63/2=31 余1，31/2=15 余1，15/2=7 余1，7/2=3 余1，3/2=1 余1，1/2=0 余1。按照从前往后的顺序，获得 63 的二进制值是 111111。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
将十进制数转换为二进制，通常使用“除2取余，倒序排列”法，即不断除以2，记录余数（必为0或1），直到商为0，然后从后往前读取余数即为二进制结果。

对63进行除2取余的过程：
- 63 ÷ 2 = 31，余 **1**
- 31 ÷ 2 = 15，余 **1**
- 15 ÷ 2 = 7，余 **1**
- 7 ÷ 2 = 3，余 **1**
- 3 ÷ 2 = 1，余 **1**
- 1 ÷ 2 = 0，余 **1**

从后往前读余数：1 1 1 1 1 1，即 **111111**。

本题的特殊之处在于：所有余数恰好都是1。因此无论是从前往后读还是从后往前读，结果都是 111111。63 = 2⁶ - 1 = 64 - 1 = 63，验证：1×2⁵ + 1×2⁴ + 1×2³ + 1×2² + 1×2¹ + 1×2⁰ = 32+16+8+4+2+1 = 63 ✓

**易错提醒：**
- 一般情况下必须“从后往前读余数”，顺序读会出错（如63转二进制若顺序读111111恰好碰巧对了，但其他数如13就会出错）。
- 建议考生掌握一般规律，不要依赖本题的特殊巧合。

**知识延伸：**
63 的二进制是 6 个1，因为 63 = 2⁶ - 1 = (111111)₂。一般来说，2ⁿ-1 的二进制就是 n 个连续的1，如 7=(111)₂，15=(1111)₂，255=(11111111)₂。

**考点：** 二进制、数制转换`,
            tags: ['判断题', '二进制', '补码', '数制转换'],
        },
        {
            id: 20,
            type: 'judge',
            question: `下列程序输出的是 A。\n\`\`\`cpp\nchar x = 65;\nx = x & 00001111;\ncout << x << endl;\n\`\`\``,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
分析程序执行过程：

第1步：\`char x = 65;\`
- 整数65赋值给char类型变量x。
- ASCII码65对应字符 'A'，但x在内存中存储的是整数值65（即二进制01000001）。

第2步：\`x = x & 00001111;\`
- C++中，以0开头的数字常量是八进制数。00001111 是八进制，等于十进制的 9 × 8² + 1×8 + 1 = 577。
- 但在位运算中，\`&\` 按二进制位逐位操作。00001111（八进制）转换为8位二进制是 00001001（因为8¹⁰ = 8，8¹ = 8，8⁰ = 1，9 = 8+1）。
- x 的当前值 65 的二进制是 01000001。
- 按位与运算：
\`\`\`
  01000001  (65)
& 00001001  (00001111八进制)
-----------
  00000001  (结果 = 1)
\`\`\`

第3步：\`cout << x << endl;\`
- 输出整数值1，不是字符'A'（ASCII码65对应的字符）。

**易错提醒：**
- 八进制陷阱：C++ 中以 0 开头的整数常量是八进制，不是十进制。00001111（八进制）≠ 15（十进制），而是等于 9。
- 如果想表达十进制数15的位模式，应写作 \`0b00001111\`（C++14二进制字面量）或直接写 \`15\`。
- 如果要保留字符输出，需注意 \`cout\` 输出 char 类型时默认输出字符；输出 int 类型时输出数值。本题 x 是 char，但值是1，所以输出数值1。

**知识延伸：**
位运算常用于掩码（Mask）操作。\`x & 0x0F\`（十六进制）可以提取x的低4位。十六进制0x0F = 二进制00001111 = 八进制017。如果程序意图是提取低4位并输出对应字符，应使用十六进制 \`0x0F\` 或十进制 \`15\`。

**考点：** 位运算、ASCII码、八进制常量`,
            tags: ['判断题', '二进制', '补码', '位运算'],
        },
        {
            id: 21,
            type: 'judge',
            question: `下列可执行程序段中，最后 pos 的值是 4。\nstring str = "chenADai";\n\`\`\`cpp\nint pos = str.find('D');\n--pos & 11;\n\`\`\``,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
执行 \`string str = "chenADai";\` 后，字符串中各字符索引为：c-0, h-1, e-2, n-3, A-4, D-5, a-6, i-7。\`str.find('D')\` 查找字符 'D'，返回其索引 5（注意题中原描述有误，'D' 实际位于索引 5，而非 4）。

执行 \`--pos\` 后，pos 先自减变为 4。随后表达式 \`--pos & 11\` 虽然计算结果（4 & 11 = 0），但该结果并未赋值回 pos，因此 pos 的最终值保持为 4。

关键点：位运算表达式 \`--pos & 11\` 仅计算了按位与的结果，并未使用赋值运算符（=）将结果存回变量，所以 pos 不会被修改。

**易错提醒：**
- 容易误以为 \`--pos & 11\` 会修改 pos 的值，实际上 C++ 中只有 \`&=\`、\`|=\` 等复合赋值运算符才会原地修改变量
- 需要仔细确认 find 的返回值，'D' 在 "chenADai" 中位于索引 5

**知识延伸：**
C++ 中 \`a op b\`（op 为非赋值运算符）的结果必须显式赋值给变量才能改变 a 的值，如 \`a = a + b\` 或 \`a += b\`。

**考点：** 位运算、字符串 find 函数、自减运算符、表达式求值`,
            tags: ['判断题', '位运算'],
        },
        {
            id: 22,
            type: 'judge',
            question: `下列程序将不能正确执行。\nstring ch = "chen";\n\`\`\`cpp\ncout << ch[4] << endl;\n\`\`\``,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
字符串 \`ch = "chen"\` 的长度为 4，有效字符索引范围为 0~3（C 语言风格字符串以 '\\0' 结尾）。\`ch[4]\` 访问的是字符串结尾的空字符 '\\0' 的位置。

在 GESP 教材语境下，访问字符串末尾的 '\\0' 位置被视为合法操作（因为 C-style 字符串以 '\\0' 结尾），输出为空字符（无显示内容），程序能够正常执行而不会崩溃。

因此，程序可以正常执行（输出空行），并非"不能正确执行"，本题应判断为"错误"。

**易错提醒：**
- 容易混淆 string 对象的 operator[] 与 vector 等容器的边界检查机制；string 的 operator[] 不进行下标越界检查
- 虽然 ch[4] 越过了实际字符范围，但它恰好指向 C 风格字符串的终止符 '\\0'，因此输出空字符而非访问违例

**知识延伸：**
string 对象的 \`[]\` 重载函数有两个版本：不抛出异常的版本（用于 \`operator[]\`）和抛出异常的版本（用于 \`at()\` 方法）。\`at()\` 方法会进行边界检查，越界时抛出 \`std::out_of_range\` 异常。

**考点：** string 索引访问、C 风格字符串结束符、下标合法性`,
            tags: ['判断题', '位运算', '字符串'],
        },
        {
            id: 23,
            type: 'judge',
            question: `将输出 97。\n\`\`\`cpp\nchar a = 'A';\na = a+32;\ncout << (int)a << endl;\n\`\`\``,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. 字符常量 \`'A'\` 在 C++ 中基于 ASCII 编码，其整数值即为 ASCII 码值 65
2. 表达式 \`a + 32\` 执行整型提升：字符类型 \`char\` 在运算时自动提升为 \`int\`，得到 65 + 32 = 97
3. 将结果强制转换为 \`int\` 并输出 \`(int)a\`，输出值为 ASCII 码 97，对应小写字母 'a'

逐步验证：
\`\`\`
char a = 'A';      // a 的值为 ASCII 65
a = a + 32;        // a + 32 = 65 + 32 = 97，赋值给 a
cout << (int)a;   // 将 char 类型的 97 强制转换为 int 输出，结果为 97
\`\`\`

**易错提醒：**
- 容易忘记 char 类型在算术运算中会发生整型提升，直接用字符值做运算
- 需要记忆大小写字母的 ASCII 差值为 32（大写 A-Z 为 65-90，小写 a-z 为 97-122）

**知识延伸：**
C++ 标准并未规定 ASCII 为必选字符编码，但 GESP 考试环境默认使用 ASCII。常见字符规律：A+32=a，0-9 的 ASCII 码为 48-57，空格为 32。

**考点：** 字符与 ASCII 码、char 算术运算、整型提升`,
            tags: ['判断题', '位运算', '字符与ASCII'],
        },
        {
            id: 24,
            type: 'judge',
            question: `自然界中，最小的素数是 2。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
素数的定义：大于 1 的自然数，且除了 1 和自身以外，不能被其他自然数整除。

按顺序检验最小的自然数：
- 1：小于 2，不符合素数定义（1 既不是素数也不是合数）
- 2：大于 1，且只能被 1 和 2 整除，符合素数定义
- 3：大于 1，只能被 1 和 3 整除，也是素数，但比 2 大

因此，自然界（数学）中最小的素数是 2。

**易错提醒：**
- 容易误认为 1 是素数，1 只有一个正因数（自身），不满足素数"恰好有两个正因数"的定义
- 素数必须是大于 1 的自然数，0 和负数都不是素数

**知识延伸：**
素数是数论的核心概念，除了 2 是唯一的偶素数外，所有其他素数都是奇数。素数在密码学、编码等领域有广泛应用。

**考点：** 素数定义、自然数基础`,
            tags: ['判断题'],
        },
        {
            id: 25,
            type: 'judge',
            question: `CCF（十六进制）= 12363（七进制）。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
将两个数都转换为十进制进行比对：

十六进制 CCF₁₆：
- C = 12，F = 15
- CCF₁₆ = 12×16² + 12×16¹ + 15×16⁰
- = 12×256 + 12×16 + 15×1
- = 3072 + 192 + 15
- = 3279₁₀

七进制 12363₇：
- 12363₇ = 1×7⁴ + 2×7³ + 3×7² + 6×7¹ + 3×7⁰
- = 1×2401 + 2×343 + 3×49 + 6×7 + 3×1
- = 2401 + 686 + 147 + 42 + 3
- = 3279₁₀

两种进制表示的十进制值均为 3279，数值相等，因此题目断言正确。

**易错提醒：**
- 十六进制中 A=10, B=11, C=12, D=13, E=14, F=15，容易将 C 误认作 13 或 F 误认作 16
- 进制转换时注意各数位的权值，个位权重为 base⁰，依次递增

**知识延伸：**
不同进制之间数值相等是常考题型，验证方法通常是转换为十进制或二进制进行比较。十六进制每 4 位对应二进制 1 位（C=1100, F=1111）。

**考点：** 进制转换（十六进制、七进制转十进制）`,
            tags: ['判断题', '进制转换'],
        }
    ],
    programmingQuestions: [
        {
      id: 26,
      type: 'programming',
      samples: [
        { input: `5 0
-2 -1 0 1 2`, output: `-2 -2 0 2 2` }
      ],
      question: `
# [GESP202412 三级] 数字替换

## 题目描述

小杨有一个包含 $n$ 个数字的序列 $A$，即 $A=[a_1,a_2,\\ldots,a_n]$，他想将其中大于 $k$ 的数字都替换为序列的最大值，将其中小于 $k$ 的数字都替换为序列的最小值，请你帮他计算出替换后的序列。

## 输入格式

第一行包含两个正整数 $n,k$，含义如题面所示。

第二行包含 $n$ 个数字，代表序列 $A$。

## 输出格式

输出 $n$ 个整数，代表替换后的结果。
`,
      explanation: `
      **解析：**
      先扫描一遍序列求出最小值和最大值，再按规则逐个修改：大于 k 的变成最大值，小于 k 的变成最小值，等于 k 的保持不变。
      `,
      template: '#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n\n    // 在此编写代码\n    return 0;\n}',
      score: 25,
      tags: ['编程题', '模拟', '数组'],
      referenceCode: '#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\nint a[100010];\nint main() {\n    int n, k;\n    cin >> n >> k;\n    for (int i = 1; i <= n; i++) {\n        cin >> a[i];\n    }\n    int max_value = a[1], min_value = a[1];\n    for (int i = 1; i <= n; i++) {\n        max_value = max(max_value, a[i]);\n        min_value = min(min_value, a[i]);\n    }\n    for (int i = 1; i <= n; i++) {\n        if (a[i] > k) {\n            a[i] = max_value;\n        } else if (a[i] < k) {\n            a[i] = min_value;\n        }\n        if (i != n) {\n            cout << a[i] << " ";\n        } else {\n            cout << a[i] << endl;\n        }\n    }\n    return 0;\n}',
      answer: '',
    },
        {
      id: 27,
      type: 'programming',
      samples: [
        { input: `12230`, output: `****.....................
****.****.****.****..***.
****.................***.
****..****.********..***.
****.....................` }
      ],
      question: `
# [GESP202412 三级] 打印数字

## 题目描述

小杨为数字 $0,1,2$ 和 $3$ 设计了一款表示形式，每个数字占用了 $5\\times 5$ 的网格。数字 $0,1,2$ 和 $3$ 的表示形式如下：

\`\`\`plain
..... ****. ..... .....
.***. ****. ****. ****.
.***. ****. ..... .....
.***. ****. .**** ****.
..... ****. ..... .....
\`\`\`

小杨想请你将给定的数字 $n$ 转换为对应的表示形式。

## 输入格式

第一行包含一个非负整数代表 $n$。

## 输出格式

输出对应的表示形式。
`,
      explanation: `
      **解析：**
      把 0、1、2、3 的 5×5 图案预先存成数组，然后按行拼接输出即可。官方参考程序中四个数字的图案分别由固定字符串构成。
      `,
      template: '#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n\n    // 在此编写代码\n    return 0;\n}',
      score: 25,
      tags: ['编程题', '字符串', '模拟', '字符画'],
      referenceCode: '#include <iostream>\n#include <string>\nusing namespace std;\nint main() {\n    string n;\n    cin >> n;\n    for (int row = 0; row < 5; row++) {\n        string line = "";\n        for (char digit : n) {\n            if (digit == \'0\') {\n                if (row == 0 || row == 4) {\n                    line += ".....";\n                } else {\n                    line += ".***.";\n                }\n            } else if (digit == \'1\') {\n                line += "****.";\n            } else if (digit == \'2\') {\n                if (row == 0) {\n                    line += ".....";\n                } else if (row == 1) {\n                    line += "****.";\n                } else if (row == 2) {\n                    line += ".....";\n                } else if (row == 3) {\n                    line += ".****";\n                } else {\n                    line += ".....";\n                }\n            } else if (digit == \'3\') {\n                if (row == 0) {\n                    line += ".....";\n                } else if (row == 1) {\n                    line += "****.";\n                } else if (row == 2) {\n                    line += ".....";\n                } else if (row == 3) {\n                    line += "****.";\n                } else {\n                    line += ".....";\n                }\n            }\n        }\n        cout << line << endl;\n    }\n    return 0;\n}',
      answer: '',
    }
    ]
};
