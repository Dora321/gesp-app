// 历史占位文件：2023年3月并无正式 GESP C++ 三级认证。
// 保留此文件仅为兼容早期整理过程，勿作为正式真题卷使用。
export const paperData = {
    id: '2023-03-l3',
    title: '2023年3月 GESP C++ 三级（历史占位，非正式真题）',
    level: 3,
    year: 2023,
    month: 3,
    session: 1,
    note: '历史占位文件；2023-03 无正式三级认证，不应纳入正式题库入口。',
    isHistoricalPlaceholder: true,
    timeLimit: 90 * 60,
    questions: [
        {
            id: 1,
            type: 'single',
            question: `若要统计数组中每个数出现次数，最常用的数据结构是（ ）。`,
            options: ["栈","队列","映射/哈希表","并查集"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (映射/哈希表)**

**核心解析：**
统计数组中每个数出现的次数，本质上是建立“数 → 出现次数”的对应关系。映射（Map）或哈希表（Hash Table）正是为这种键值对应场景设计的：
1. 遍历数组中的每个元素 \`x\`
2. 执行 \`count[x]++\`，即在哈希表中将 \`x\` 作为键，其对应值加一
3. 整个过程平均时间复杂度为 O(n)，查询和更新操作均为 O(1)

其他数据结构无法高效完成此任务：栈和队列是线性存取结构，无法直接建立元素到频次的映射；并查集专门用于维护集合的合并与查询关系。

**选项逐项分析：**
- **A 栈**：❌ 错误。栈是后进先出（LIFO）结构，适用于函数调用栈、表达式求值、括号匹配等场景，不适合建立元素到频次的对应关系。
- **B 队列**：❌ 错误。队列是先进先出（FIFO）结构，主要用于广度优先搜索（BFS）、任务调度等按顺序处理场景。
- **C 映射/哈希表**：✅ 正确。映射（Map）或哈希表（Hash Table）能够建立“元素 → 出现次数”的关联，是统计频次最直接高效的数据结构。
- **D 并查集**：❌ 错误。并查集（Union-Find）用于维护若干不相交集合的合并与查询，常处理连通分量、等价类等问题。

**易错提醒：**
- 栈和队列虽然也是常用数据结构，但它们解决的是存取顺序问题，而非统计问题
- 并查集名称中的“并”“查”提示它处理的是集合合并查询，不是频次统计

**知识延伸：**
在 C++ 中，可用 \`unordered_map<int, int>\` 实现哈希表频次统计；在 Python 中可用 \`collections.Counter\`。统计频次是后续学习前缀和、分桶法等技巧的基础。

**考点：** 常用数据结构及其适用场景`,
            tags: ['数组与字符串']
        },
        {
            id: 2,
            type: 'single',
            question: `在无权图中求最短路，通常优先使用（ ）。`,
            options: ["DFS","BFS","二分","拓扑排序"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (BFS)**

**核心解析：**
在无权图中，边权相等（通常视为 1），求最短路即求经过最少边数的路径。BFS（广度优先搜索）的核心特性是“逐层扩展”：
1. 从起点开始，先访问所有距离为 1 的节点
2. 再访问所有距离为 2 的节点，以此类推
3. 第一次访问到某个节点时，所经过的路径必然是边数最少的路径

因此，当 BFS 第一次抵达终点时，该路径即为最短路，时间复杂度为 O(V+E)。

**选项逐项分析：**
- **A DFS**：❌ 错误。深度优先搜索（DFS）沿着一条路径尽可能深地探索，第一次到达某节点时未必是经过最少边的路径，无法保证找到最短路。
- **B BFS**：✅ 正确。广度优先搜索逐层向外扩散，在无权图中首次访问到某个节点时，经过的边数必然最少，因此适合求最短路。
- **C 二分**：❌ 错误。二分搜索（Binary Search）是针对有序序列的查找算法，时间复杂度 O(log n)，与图的最短路问题无关。
- **D 拓扑排序**：❌ 错误。拓扑排序用于处理有向无环图（DAG）中任务的依赖顺序问题，无法用于一般图的最短路求解。

**易错提醒：**
- DFS 和 BFS 虽然都是图遍历方法，但应用场景不同：DFS 适合连通性检测、路径搜索；BFS 适合最短路、分层遍历
- BFS 的“最短路”特指边数（无权情况下等于路径长度），有边权时需用 Dijkstra 等算法

**知识延伸：**
当图存在负权边时，需使用 Bellman-Ford 算法；当所有边权为正时，Dijkstra 算法更高效。这些是图论最短路问题的进阶考点。

**考点：** 图论搜索算法的性质与适用场景`,
            tags: ['算法思维']
        },
        {
            id: 3,
            type: 'single',
            question: `递归函数必须具备的关键要素是（ ）。`,
            options: ["输入语句","终止条件","循环变量","随机数"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (终止条件)**

**核心解析：**
递归函数的本质是函数调用自身，其执行依赖于“调用链”的建立与断开。终止条件（Base Case）的作用是：
1. 提供递归触发的终止点
2. 在满足特定条件时直接返回结果，不再调用自身
3. 确保调用链能够逐层返回，避免无限递归

没有终止条件的递归将无限调用自身，导致调用栈溢出（Stack Overflow），程序崩溃。

**选项逐项分析：**
- **A 输入语句**：❌ 错误。递归函数不一定需要从外部获取输入，例如计算阶乘 \`fact(5)\` 可以直接传入参数 5。
- **B 终止条件**：✅ 正确。递归必须有明确的终止条件（递归出口），否则将导致无限递归，引发栈溢出。
- **C 循环变量**：❌ 错误。循环变量（如 \`for\` 循环中的 \`i\`）是迭代逻辑的典型特征，递归依赖函数自身的调用而非循环变量。
- **D 随机数**：❌ 错误。随机数与递归没有必然联系，不能作为递归的必要条件。

**易错提醒：**
- 递归三要素：调用自身、终止条件、参数收敛（使参数趋近终止条件）
- 终止条件设置错误（如永远无法满足）会导致无限递归
- 每次递归调用参数必须“靠近”终止条件，否则递归无法结束

**知识延伸：**
常见的递归案例包括：阶乘 \`n! = n * fact(n-1)\`、斐波那契数列、树的遍历、汉诺塔问题等。递归与栈密切相关，每次递归调用都会在系统栈中压入一层帧。

**考点：** 递归算法的定义与结构要求`,
            tags: ['条件判断', '函数']
        },
        {
            id: 4,
            type: 'single',
            question: `已知 n=1e5，$O(N^2)$ 算法通常（ ）。`,
            options: ["稳定可过","可能超时","一定最优","与 n 无关"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (可能超时)**

**核心解析：**
分析 O(N²) 算法在 n = 10⁵ 规模下的运行时间：
1. 算法基本操作次数与 n² 成正比
2. 当 n = 10⁵ 时，n² = 10¹⁰（即 100 亿次操作）
3. 普通计算机每秒约执行 10⁸ 次基本运算
4. 10¹⁰ ÷ 10⁸ = 100 秒，远超竞赛通常 1-2 秒的时限

因此，在 n = 10⁵ 规模下，O(N²) 算法通常无法在规定时间内通过。

**选项逐项分析：**
- **A 稳定可过**：❌ 错误。10¹⁰ 次运算需要约 100 秒，即使在较宽松的评测环境下也可能超时，绝非“稳定可过”。
- **B 可能超时**：✅ 正确。在 10⁵ 规模下，O(N²) 算法运行时间极长，通常无法在竞赛或认证的时限内通过。
- **C 一定最优**：❌ 错误。对于 10⁵ 规模，应选择 O(N log N) 或 O(N) 复杂度的算法，O(N²) 反而是效率较低的选择。
- **D 与 n 无关**：❌ 错误。复杂度 O(N²) 意味着时间开销随 n 的增加呈平方级增长，n 越大开销越大。

**易错提醒：**
- “可能”表示存在边界情况（如常数极小、数据范围特殊），但大多数情况下会超时
- 不同评测机性能不同，但 10¹⁰ 量级的运算在任何环境下都是巨大负担
- 竞赛中通常认为 O(N²) 在 N > 10⁴ 时就可能成为性能瓶颈

**知识延伸：**
常用复杂度排序（从小到大）：O(1) < O(log n) < O(n) < O(n log n) < O(n²) < O(2ⁿ) < O(n!)。GESP 三级要求能够根据数据规模选择合适的复杂度算法。

**考点：** 算法时间复杂度与数据规模的匹配关系`,
            tags: ['程序分析']
        },
        {
            id: 5,
            type: 'single',
            question: `二分查找适用的前提是序列（ ）。`,
            options: ["元素互异","已排序","长度为偶数","从 0 开始编号"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (已排序)**

**核心解析：**
二分查找（Binary Search）的核心逻辑是利用数据的“单调性”进行快速定位：
1. 比较目标值与中间元素的大小
2. 根据比较结果，将搜索范围缩小一半
3. 重复上述过程直到找到目标或范围为空

这一过程能够成立的前提是序列具有单调性（即已排序）。只有有序序列才能保证：若目标大于中间值，则目标必在右半部分；反之在左半部分。如果序列无序，中间值的大小无法反映目标值的位置，二分查找将失效。

**选项逐项分析：**
- **A 元素互异**：❌ 错误。即使数组中存在相同的元素，二分查找依然可以正确运行，只是返回任意一个匹配位置（或最左侧/最右侧位置）。元素互异不是二分查找的必要条件。
- **B 已排序**：✅ 正确。二分查找的核心前提是利用数据的单调性，只在有序序列中才能通过比较中间值来缩小搜索范围，从而实现 O(log n) 的时间复杂度。
- **C 长度为偶数**：❌ 错误。二分查找对序列长度没有限制，偶数长度和奇数长度均可正常工作。
- **D 从 0 开始编号**：❌ 错误。数组下标从 0 或从 1 开始是编程语言的习惯问题，与二分查找的正确性无关。

**易错提醒：**
- 二分查找的三个前提：有序序列、随机访问（数组）、明确的比较操作
- 题目问的是“适用前提”，核心是“已排序”，其他选项都是无关条件
- 元素互异性不是必要条件，但若要查找“第一个出现的位置”或“最后一个出现的位置”，需要额外处理

**知识延伸：**
二分查找的标准实现需要注意边界条件（区间开闭、循环终止条件），常见错误包括死循环、遗漏元素等。GESP 四级会进一步考察二分答案等变形应用。

**考点：** 二分查找算法的前提条件与正确性保证`,
            tags: ['数组与字符串', '算法思维']
        },
        {
            id: 6,
            type: 'single',
            question: `前缀和数组 pre[i] 一般表示（ ）。`,
            options: ["第 i 项本身","前 i 项的累计信息","第 i 项最大值","后缀最小值"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (前 i 项的累计信息)**

**核心解析：**
前缀和（Prefix Sum）是一种预处理技术。对于数组 \`a[1], a[2], ..., a[n]\`，定义 \`pre[i] = a[1] + a[2] + ... + a[i]\`，即前 i 项的累计总和。该定义有明确的数学边界：索引 i 从 1 开始（或从 0 开始时 pre[0]=0）。这种预处理的目的是将区间求和的时间复杂度从 O(n) 降低到 O(1)。

**选项逐项分析：**
- **A (第 i 项本身)**：❌ 错误。这是原始数组 \`a[i]\` 的含义，不是前缀和。前缀和的"前"字明确表示包含从起点到当前位置的累计信息。
- **B (前 i 项的累计信息)**：✅ 正确。前缀和的核心价值正在于此：存储从数组起始位置到下标 i 为止的累计信息，从而支持常数时间区间查询。
- **C (第 i 项最大值)**：❌ 错误。前缀和是求和操作，不涉及比较/取极值。若需区间最大值，应使用线段树或 RMQ 等数据结构。
- **D (后缀最小值)**：❌ 错误。"后缀"与"前缀"方向相反（从某位置到末尾），且前缀和不保存最小值信息。

**易错提醒：**
- 前缀和是"累计求和"，不是"第 i 项"本身，也不是"最大值"或"最小值"。
- 注意下标边界：若 pre[0]=0，则 pre[i] = pre[i-1] + a[i]。

**知识延伸：**
前缀和数组是 GESP 三级的高频考点，除区间求和外，还可用于统计频数（如求子数组和为 k 的个数）。

**考点：** 前缀和的定义与基本性质`,
            tags: ['数组与字符串', '算法思维']
        },
        {
            id: 7,
            type: 'single',
            question: `在回溯搜索中，撤销选择的操作主要用于（ ）。`,
            options: ["节省输入时间","恢复现场以尝试下一分支","避免递归","加速排序"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (恢复现场以尝试下一分支)**

**核心解析：**
回溯算法（Backtracking）本质是对解空间树进行深度优先搜索。以八皇后问题为例：依次尝试在每一行放置皇后，若在某行无法放置（冲突），则"回溯"到上一行，撤销该行的选择，尝试该行的下一个位置。这个"撤销"动作正是回溯的核心——它恢复全局状态，使得后续分支的探索基于正确的、未被污染的现场。

**选项逐项分析：**
- **A (节省输入时间)**：❌ 错误。撤销选择与输入操作完全无关，时间复杂度不涉及 I/O 优化。
- **B (恢复现场以尝试下一分支)**：✅ 正确。撤销操作的本质是回退修改状态（栈恢复、变量还原、标志位清除等），确保当前分支探索结束后，能以干净的状态进入下一个分支继续搜索。
- **C (避免递归)**：❌ 错误。撤销选择并非用于"避免递归"，恰恰相反，它常与递归配合使用（递归深入时做选择，递归返回时撤销），以实现完整的深度优先遍历。
- **D (加速排序)**：❌ 错误。排序与回溯搜索属于不同问题域，撤销选择的作用域是搜索树的状态管理，与排序算法无关。

**易错提醒：**
- 回溯中的"撤销"是成对出现的：选择时记录/修改状态，返回时必须严格撤销，否则会导致后续分支状态错误（常见 bug 原因）。
- 不能把"撤销"理解为"删除数据"，它本质是状态恢复。

**知识延伸：**
回溯算法是 GESP 三级核心考点，典型应用包括：全排列、组合枚举、迷宫路径搜索、n 皇后问题等。

**考点：** 回溯算法的原理与状态管理`,
            tags: ['函数', '算法思维']
        },
        {
            id: 8,
            type: 'single',
            question: `若要求“最少操作次数”，常见建模方向是（ ）。`,
            options: ["贪心/BFS/DP 等最优化方法","随机模拟","仅用输出语句","删除条件判断"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (贪心/BFS/DP 等最优化方法)**

**核心解析：**
"最少操作次数"属于最优化问题范畴。最优化问题的目标是求解某个目标函数的最优值（最大/最小/最短/最少等）。在算法设计中，针对不同问题特征有对应的经典建模方法：
- **贪心**：每一步取局部最优，期望得到全局最优（适用于具有贪心选择性的问题，如找零钱）；
- **BFS（广度优先搜索）**：❌ 错误。无权图中求最短路径/最少步数的标准方法，层序遍历首次到达即最优；
- **DP（动态规划）**：❌ 错误。处理具有最优子结构和无后效性的问题，通过状态转移求全局最优解。

**选项逐项分析：**
- **A (贪心/BFS/DP 等最优化方法)**：✅ 正确。这三项涵盖了求"最少"类问题的三大经典算法模型，是竞赛和面试中的标准思路。
- **B (随机模拟)**：❌ 错误。随机化方法（如蒙特卡洛模拟）适用于概率估算或近似求解，无法保证确定性最优解，通常不能用于求精确的最少次数。
- **C (仅用输出语句)**：❌ 错误。"仅用输出语句"不构成任何算法，无法实现逻辑判断和状态转移，完全无法解决问题。
- **D (删除条件判断)**：❌ 错误。删除条件判断会导致程序逻辑混乱，无法正确遍历解空间或验证约束条件，反而会使问题无法求解。

**易错提醒：**
- 最优化问题必须选择合适的算法模型（BFS/DP/贪心），而非随意编写代码逻辑。
- 随机模拟不能保证得到"最少"，只能得到一个可行解。

**知识延伸：**
"最少操作次数"问题常见变形包括：最短路径、最少砝码、最少硬币找零、最少步数魔方等，各自适用的算法模型不同。

**考点：** 最优化问题的算法模型选择`,
            tags: ['程序分析']
        },
        {
            id: 9,
            type: 'single',
            question: `vector<int> v; 执行 v.push_back(7); 后，7 会被（ ）。`,
            options: ["插入到头部","追加到尾部","替换全部元素","自动排序"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (追加到尾部)**

**核心解析：**
\`vector\` 是 C++ STL 中的顺序容器，底层实现为动态数组。\`push_back\` 是 \`vector\` 最常用的成员函数之一，其语义从函数名即可推断："push"意为"推入/压入"，"back"意为"后部/尾部"，合起来即"追加到尾部"。调用该函数时，元素会被放置在 vector 当前末尾位置的下一个位置，容器容量不足时会自动扩容。

**选项逐项分析：**
- **A (插入到头部)**：❌ 错误。若要将元素插入到 \`vector\` 的头部，应使用 \`insert(v.begin(), value)\` 或 \`emplace(v.begin(), value)\`，\`push_back\` 不具备此功能。
- **B (追加到尾部)**：✅ 正确。\`push_back\` 是向 \`vector\` 末尾添加元素的标准方法，时间复杂度均摊为 O(1)。
- **C (替换全部元素)**：❌ 错误。替换全部元素应使用赋值操作（如 \`v = {values}\`）或遍历赋值，\`push_back\` 不会影响已有元素。
- **D (自动排序)**：❌ 错误。\`vector\` 不会在任何插入操作时自动排序。若需要有序容器，应使用 \`std::set\`（不重复）或 \`std::multiset\`（可重复）。

**易错提醒：**
- \`push_back\` 与 \`insert\` 不同：\`insert\` 可在任何位置插入，但效率较低（可能触发大量元素移动）；\`push_back\` 只操作末尾，效率更高。
- \`vector\` 不保证任何自动排序行为，排序需手动调用 \`std::sort\`。

**知识延伸：**
\`vector\` 的常用成员函数包括 \`push_back\`、\`pop_back\`（删除尾部元素）、\`size\`、\`empty\`、\`clear\` 等，是 GESP 一级至三级的高频考点。

**考点：** C++ STL 容器 vector 的基本操作`,
            tags: ['数组与字符串']
        },
        {
            id: 10,
            type: 'single',
            question: `对于区间 [l,r] 的和，使用前缀和可在 $O(1)$ 时间通过（ ）计算。`,
            options: ["pre[r]-pre[l]","pre[r]-pre[l-1]","pre[l]+pre[r]","pre[r+1]-pre[l-1]"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (pre[r]-pre[l-1])**

**核心解析：**
前缀和数组的定义：\`pre[i] = a[1] + a[2] + ... + a[i]\`（以 1 为起始下标为例）。

要计算闭区间 [l, r]（即从第 l 项到第 r 项）的和：
- \`pre[r]\` 包含：a[1] + a[2] + ... + a[l-1] + a[l] + ... + a[r]
- \`pre[l-1]\` 包含：a[1] + a[2] + ... + a[l-1]
- 两者相减：\`pre[r] - pre[l-1] = a[l] + a[l+1] + ... + a[r]\`

这正是闭区间 [l, r] 的所有元素之和。

**选项逐项分析：**
- **A (pre[r]-pre[l])**：❌ 错误。该表达式将 a[l] 也减去了，得到的是 [l+1, r] 区间（不含 l）的和，比目标区间少了 a[l]。
- **B (pre[r]-pre[l-1])**：✅ 正确。减去前 l-1 项的累计和，剩余部分恰好是第 l 项到第 r 项的完整区间。
- **C (pre[l]+pre[r])**：❌ 错误。该表达式计算的是 pre[l] + pre[r]，其中 pre[l] 包含了 a[1] 到 a[l]，与 pre[r] 存在大量重复相加，结果毫无意义。
- **D (pre[r+1]-pre[l-1])**：❌ 错误。该表达式对应的是 [l, r+1] 区间（若定义 pre[i] 为前 i 项和），范围向右偏移了一位。

**易错提醒：**
- 减去的下标是 l-1，不是 l。若 l=1，则 pre[l-1] = pre[0]，需要确保 pre[0]=0（常见初始化方式）。
- 区间下标边界必须清晰区分开区间、闭区间、前缀和定义方式。

**知识延伸：**
前缀和区间求和公式是 GESP 三级必考内容。若采用 0-based 下标（pre[0]=0，pre[i]=a[0]+...+a[i]），则区间 [l, r]（含）的和为 \`pre[r+1] - pre[l]\`，此处下标差异需特别注意。

**考点：** 前缀和的区间求和公式`,
            tags: ['算法思维']
        },
        {
            id: 11,
            type: 'single',
            question: `深度优先搜索（DFS）更贴近哪种过程（ ）。`,
            options: ["按层推进","一条路走到底再回退","随机跳转","只访问起点"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (一条路走到底再回退)**

**核心解析：**
深度优先搜索（Depth-First Search，DFS）的核心思想是沿着一条路径尽可能深入，直到无法继续前进时才回退到上一个岔路口，换一条分支继续探索。这种"不撞南墙不回头"的策略正是 B 选项所描述的特征。

以二叉树 DFS 为例：若从根节点出发，先沿左子树一路向下访问到叶子节点，此时已无法继续深入，于是回退到最近的祖先节点，转而向右子树深入。每条路径都走到底，走不通就回退——这正是 DFS 与"先访问完同一层所有节点再向下"的广度优先搜索（BFS）的本质区别。

**选项逐项分析：**
- **A (按层推进)**：❌ 错误。这是 BFS（广度优先搜索）的特征。BFS 先访问距离起点最近的所有节点（同一层），再访问下一层。
- **B (一条路走到底再回退)**：✅ 正确。DFS 的标准工作模式：沿着一条路径深入探索，遇到死路后回溯尝试其他路径。
- **C (随机跳转)**：❌ 错误。DFS 有明确的探索顺序（深度优先），并非随机。随机跳转更像是蒙特卡洛方法或随机化算法。
- **D (只访问起点)**：❌ 错误。DFS 会访问起点以及所有可达的节点，并非只访问起点。

**易错提醒：**
- DFS 的"深度"指的是沿某条路径向深处探索，而非指图的物理深度。
- BFS 才是"按层推进"的典型代表，混淆两者的特征是常见错误。

**知识延伸：**
DFS 可通过递归或显式栈（stack）实现，两者本质相同——栈保存的是待探索的路径节点信息。递归版本实际上是系统栈的隐式使用。DFS 常用于连通分量检测、拓扑排序、路径搜索等问题。

**考点：** 算法思维、搜索策略`,
            tags: ['算法思维']
        },
        {
            id: 12,
            type: 'single',
            question: `当状态具有“重叠子问题”时，优先考虑（ ）。`,
            options: ["动态规划","快速幂","并查集","双向链表"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (动态规划)**

**核心解析：**
"重叠子问题"（Overlapping Subproblems）是动态规划（Dynamic Programming，DP）的两个核心特征之一（另一个是最优子结构）。当一个问题可以被分解为多个相同的子问题，而这些子问题会被反复计算时，普通的递归方法会导致大量重复计算，效率低下。

动态规划的核心做法是：将子问题的计算结果保存下来（记忆化），下次遇到相同的子问题时直接查表，避免重复计算。以斐波那契数列为例，递归计算 F(5) 时，F(3) 会被计算两次，F(2) 会被计算三次——这就是典型的重叠子问题。用 DP 的自底向上方法或记忆化递归可以显著提升效率。

**选项逐项分析：**
- **A (动态规划)**：✅ 正确。重叠子问题是 DP 的标志性特征，DP 正是为解决此类问题而设计的算法范式。
- **B (快速幂)**：❌ 错误。快速幂用于在 O(log n) 时间内计算 a^n，其核心是分治与二进制位运算，与重叠子问题无关。
- **C (并查集)**：❌ 错误。并查集（Union-Find）用于处理集合的合并与查询，适用于等价关系判定等问题，其设计不针对重叠子问题。
- **D (双向链表)**：❌ 错误。双向链表是一种数据结构，用于高效地在序列中部进行插入/删除操作，与算法策略选择无关。

**易错提醒：**
- "重叠子问题"强调的是子问题被多次求解，而非子问题数量多。仅有大量不同子问题不能构成 DP 的充分条件。
- 动态规划适用于"问题能分解为子问题"且"子问题有重叠"的情况，两者缺一不可。

**知识延伸：**
DP 的实现方式分为自顶向下（记忆化递归）和自底向上（填表法）。经典 DP 问题包括：爬楼梯（LeetCode 70）、背包问题、最长公共子序列（LCS）、编辑距离等。判断一个问题是否适合 DP，是 GESP 三级考试的重要考点。

**考点：** 程序分析、算法选择`,
            tags: ['程序分析']
        },
        {
            id: 13,
            type: 'single',
            question: `若只需判断元素是否出现过，通常可用（ ）。`,
            options: ["set / unordered_set","queue","stack","priority_queue"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (set / unordered_set)**

**核心解析：**
判断元素是否出现过，本质上是"去重"或"存在性查询"问题。在 C++ 标准模板库（STL）中，set 和 unordered_set 是专门为此设计的集合容器：

- **set**：基于红黑树实现，内部元素自动有序（通常升序），查找、插入、删除的时间复杂度为 O(log n)。
- **unordered_set**：基于哈希表实现，元素无序但查找、插入、删除的平均时间复杂度为 O(1)。

两者都保证了每个元素在集合中只存储一次，非常适合用于"判断某元素是否已出现过"的场景。当需要频繁进行存在性查询时，set/unordered_set 优于数组或 vector 的线性查找。

**选项逐项分析：**
- **A (set / unordered_set)**：✅ 正确。这两种容器专为集合操作设计，支持高效的插入和查找，是判断元素出现与否的标准选择。
- **B (queue)**：❌ 错误。queue（队列）是先进先出（FIFO）的数据结构，用于任务调度、广度优先搜索等场景，不支持高效的元素查找。
- **C (stack)**：❌ 错误。stack（栈）是后进先出（LIFO）的数据结构，适用于深度优先搜索、表达式求值等场景，同样不支持高效查找。
- **D (priority_queue)**：❌ 错误。priority_queue（优先队列）是基于堆实现的，用于高效获取最大/最小元素，不支持按值查找特定元素。

**易错提醒：**
- 如果只需判断存在性且追求最高效率，应优先选择 unordered_set（哈希表），而非 set（红黑树）。
- 对于已排序的数据，set 额外提供了有序遍历的优势，但本题只需判断"是否出现"，无需有序性。

**知识延伸：**
set 和 unordered_set 的区别是 GESP 常考点：set 保证有序（O(log n) 查找），unordered_set 无序但平均 O(1) 查找。在面试和竞赛中，若无特殊需求（如需要有序遍历），unordered_set 通常是更好的选择。

**考点：** 条件判断、数据结构选择`,
            tags: ['条件判断']
        },
        {
            id: 14,
            type: 'single',
            question: `双重循环遍历 n×n 矩阵的时间复杂度通常是（ ）。`,
            options: ["$O(1)$","$O(log n)$","$O(N)$","$O(N^2)$"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (O(N^2))**

**核心解析：**
时间复杂度分析需关注循环层数与循环变量范围。

对于双重循环遍历 n×n 矩阵的典型代码：
\`\`\`cpp
for (int i = 0; i < n; i++) {           // 外层循环执行 n 次
    for (int j = 0; j < n; j++) {       // 内层循环每次执行 n 次
        // 操作                            // O(1) 操作
    }
}
\`\`\`

分析过程：
- 外层循环：\`i = 0, 1, 2, ..., n-1\`，共执行 **n** 次
- 内层循环：每次外层循环中，\`j = 0, 1, 2, ..., n-1\`，共执行 **n** 次
- 内层操作：常数时间 O(1)

总执行次数 = n × n = **n²**，因此时间复杂度为 **O(n²)**。

**选项逐项分析：**
- **A (O(1))**：❌ 错误。O(1) 表示常数时间，与问题规模 n 无关。双重循环显然随 n 增长，不可能是常数时间。
- **B (O(log n))**：❌ 错误。O(log n) 通常出现在二分查找等每次将问题规模减半的场景中。双重循环不涉及规模减半。
- **C (O(N))**：❌ 错误。O(n) 是线性时间，对应单层循环。双重循环的规模是两层线性相乘，即 n × n。
- **D (O(N^2))**：✅ 正确。双重线性循环的复杂度为 O(n × n) = O(n²)。

**易错提醒：**
- 时间复杂度关注的是增长趋势，系数可以忽略。双重循环就是 O(n²)，不要误写成 O(2n) 或 O(n+n)。
- 若内外层循环范围不同（如 n×m 矩阵），复杂度为 O(n×m)，只有两者相等时才简写为 O(n²)。

**知识延伸：**
时间复杂度从小到大排序：O(1) < O(log n) < O(n) < O(n log n) < O(n²) < O(2ⁿ) < O(n!)。GESP 考试中能正确分析嵌套循环的复杂度是基本要求，三级考生应能熟练判断 O(n²)、O(n³) 等常见多项式复杂度。

**考点：** 循环、算法思维、时间复杂度分析`,
            tags: ['循环', '算法思维']
        },
        {
            id: 15,
            type: 'single',
            question: `在图遍历中，visited 数组的作用是（ ）。`,
            options: ["记录输入顺序","防止重复访问和死循环","存放边权","统计内存"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (防止重复访问和死循环)**

**核心解析：**
在图遍历（无论是 DFS 还是 BFS）中，visited 数组（或 visited 集合）的作用是记录已被访问过的顶点，防止算法在存在回路的图中重复访问同一个节点，从而导致无限循环。

考虑一个简单环图：A → B → C → A → B → C → ...（若不加限制）
- 从 A 出发访问 B，再访问 C，再回到 A，若不加阻止会再次访问 B、C...形成死循环
- 使用 visited[A] = true 标记 A 已被访问，遇到 A 时检查 visited[A] 为真则跳过，避免无限循环

在 DFS 中，visited 是递归能否正确终止的关键；在 BFS 中，visited 确保每个顶点只被加入队列一次。

**选项逐项分析：**
- **A (记录输入顺序)**：❌ 错误。visited 数组记录的是"是否被访问过"，与输入顺序无关。记录输入顺序通常需要额外的 queue 或 vector。
- **B (防止重复访问和死循环)**：✅ 正确。这是 visited 数组的核心作用，尤其在存在环的图中至关重要。
- **C (存放边权)**：❌ 错误。边权（edge weight）存储在邻接矩阵或邻接表中，与 visited 无关。若要存储边权，需要专门的数据结构。
- **D (统计内存)**：❌ 错误。visited 是一个布尔数组，只记录访问状态，与内存统计毫无关系。内存统计通常需要 sizeof 操作或专门的工具。

**易错提醒：**
- 在无向图的 DFS/BFS 中，必须在第一次访问某顶点时就标记 visited，否则当从不同邻居回溯时可能重复访问同一个顶点。
- 对于树结构的遍历（树是特殊的无环图），虽然理论上也需要 visited，但因树无环，实际上不标记也能正常终止。但养成使用 visited 的习惯很重要。

**知识延伸：**
visited 数组是图遍历算法的"基础设施"。其变体包括：对于有权图可能需要 distance 数组记录最短路径；对于拓扑排序可能需要 indegree 数组。理解 visited 的作用有助于理解整个搜索算法的正确性。

**考点：** 数组与字符串、算法思维、图遍历基础`,
            tags: ['数组与字符串', '算法思维']
        },
        {
            id: 16,
            type: 'judge',
            question: `DFS 和 BFS 都可以用于图的遍历。`,
            options: ["正确","错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
图的遍历是指从图中某个顶点出发，按某种方式访问图中所有顶点，且每个顶点仅被访问一次。DFS（深度优先搜索）和 BFS（广度优先搜索）都是经典且完备的图遍历策略：
- DFS 通过递归或栈实现，沿着一条路径尽可能深地访问，直到无路可退再回溯；
- BFS 通过队列实现，按层次逐层扩展访问。

两者均能覆盖图的所有可达顶点，因此都可以用于图的遍历。

**易错提醒：**
- 部分考生误以为只有 BFS 是图的遍历，实际上 DFS 同样是合法且常用的图遍历方法，只是实现方式和访问顺序不同。
- 图的遍历不限于 BFS，DFS 在深度优先搜索中同样完整覆盖所有可达节点。

**知识延伸：**
DFS 和 BFS 不仅是遍历工具，还是许多高级算法的基础（如 DFS 用于连通分量、拓扑排序；BFS 用于最短路径、层序遍历）。

**考点：** 判断题、算法思维、图论基础`,
            tags: ['算法思维']
        },
        {
            id: 17,
            type: 'judge',
            question: `递归深度与系统栈空间无关。`,
            options: ["正确","错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
递归调用依赖系统提供的调用栈（call stack）。每进行一次递归调用，系统会在栈上分配一个新的栈帧（stack frame），用于保存局部变量、返回地址等信息。递归深度越大，累计占用的栈空间越多。当递归深度超过系统栈容量时，就会发生栈溢出（stack overflow）错误。因此，递归深度与系统栈空间密切相关，并非无关。

**易错提醒：**
- 考生常忽略递归的底层实现机制，以为递归只是"函数调用自己"而不消耗资源。
- 在 GESP 三级考试中，递归与栈的关系是核心考点，递归深度受限于栈空间是实际编程中必须考虑的问题。

**知识延伸：**
C++ 中可通过调整编译器选项或使用手动栈模拟来增加递归深度，但在竞赛和考试环境中，默认栈空间通常只有几 MB，递归深度过大时必须考虑改用迭代或尾递归优化。

**考点：** 判断题、函数、递归与栈`,
            tags: ['函数']
        },
        {
            id: 18,
            type: 'judge',
            question: `二分答案法要求可行性随答案具有单调性。`,
            options: ["正确","错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
二分答案法（Binary Search on Answer）的核心思想是：在一个可行答案区间 [L, R] 内，通过不断取中点并判断"答案是否可行"来逼近最优解。这一方法成立的前提是"可行性"随答案变化具有单调性，即：若答案 x 可行，则所有小于（或大于）x 的答案也可行。具体而言：
- 对于"最小化最大值"类问题，可行性随答案增大而单调不降；
- 对于"最大化最小值"类问题，可行性随答案增大而单调不升。

正是这种单调性保证了每次二分都能正确缩小区间，最终收敛到最优解。若不存在单调性，二分过程无法保证正确性。

**易错提醒：**
- 考生可能误以为只要涉及最优解就可以用二分答案，必须先验证单调性。
- 单调性不满足时，二分答案会得到错误结果，此时需考虑其他方法（如动态规划、贪心等）。

**知识延伸：**
二分答案法是处理"最大值最小化"或"最小值最大化"问题的利器，典型应用包括：最大化最小间距、最小化最大代价等 GESP 常考题型。

**考点：** 判断题、算法思维、二分查找`,
            tags: ['算法思维']
        },
        {
            id: 19,
            type: 'judge',
            question: `前缀和只能处理加法问题，不能做计数。`,
            options: ["正确","错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
前缀和（Prefix Sum）是一种预处理技术，通过预先计算数组的前缀和数组 \`prefix[i] = arr[0] + arr[1] + ... + arr[i]\`，使得区间 [l, r] 的累加和可在 O(1) 时间内得到。前缀和的本质是对数值进行累加，而"计数"问题不过是将"是否满足某条件"转化为 0/1 值后进行累加。例如：统计数组中大于 5 的元素个数，可将数组转换为 0/1 数组（满足条件为 1，否则为 0），再用前缀和累加。因此，前缀和完全可以用于计数类问题。

**易错提醒：**
- 考生容易将"前缀和只能处理加法问题"理解为只能处理数值相加，忽略了 0/1 编码后的累加即等价于计数。
- 在二维前缀和场景下，计数问题（如矩阵中满足条件的格子数）同样是典型应用。

**知识延伸：**
前缀和是 O(1) 区间查询的基础工具，与差分数组配合可实现动态区间修改+查询，在 GESP 四级考试中常结合二维数组考查。

**考点：** 判断题、算法思维、前缀和`,
            tags: ['算法思维']
        },
        {
            id: 20,
            type: 'judge',
            question: `在无权图中，BFS 首次到达某点时路径即最短。`,
            options: ["正确","错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
BFS（广度优先搜索）在无权图中按层次扩展顶点：从起始点出发，先访问所有距离为 1 的顶点，再访问距离为 2 的顶点，以此类推。由于 BFS 使用队列保证"先入先出"的访问顺序，每条边权值相同（均为 1），因此从源点到任意顶点的第一条路径必定是边数最少的路径，即最短路径。数学归纳法可证：首次到达某顶点时，BFS 已遍历完所有距离更短的路径，故不可能存在更短的未探索路径。

**易错提醒：**
- 考生需注意"首次到达"这一关键条件：BFS 保证的是首次访问时的路径最短，而非后续可能的更短路径（实际上不存在更短路径）。
- 此结论仅适用于无权图；若图有权，则需使用 Dijkstra 算法等有权最短路径算法。

**知识延伸：**
BFS 的"最短路径保证"是其在无权图问题中广泛应用的根本原因，如迷宫最短路、社交网络最近关系等问题都依赖这一性质。

**考点：** 判断题、算法思维、BFS`,
            tags: ['算法思维']
        },
        {
            id: 21,
            type: 'judge',
            question: `回溯算法不会重复进入同一状态。`,
            options: ["正确","错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
回溯算法（Backtracking）的核心思想是深度优先搜索（DFS）探索所有可能的解空间。在标准实现中，如果不借助额外的记忆化（如 memoization）或显式的状态访问记录，数据结构本身并不主动阻止重复进入同一状态。例如，在经典的八皇后问题或子集和问题中，递归回溯可能会经过同一个状态多次，尤其是在没有剪枝优化的情况下。只有在结合了“去重”机制（如使用 visited 集合、状态哈希或剪枝条件）时，才能保证不重复进入同一状态。因此，原命题“回溯算法不会重复进入同一状态”作为一个绝对陈述是不成立的。

**易错提醒：**
- 混淆了“回溯算法”与“记忆化搜索”。记忆化搜索通过记录已访问状态来避免重复，而普通回溯不一定包含此机制。
- 误以为回溯天然具有去重功能。实际上，去重是额外的优化手段，不是回溯的定义性特征。

**知识延伸：**
回溯算法与动态规划中的记忆化搜索（Memoization）密切相关——两者都涉及状态空间搜索，但记忆化在回溯基础上增加了状态记录以避免重复计算，这是从“指数级”优化到“多项式级”的关键区别。

**考点：** 判断题、算法思维、回溯算法`,
            tags: ['算法思维']
        },
        {
            id: 22,
            type: 'judge',
            question: `时间复杂度 $O(n log n)$ 在很多排序算法中常见。`,
            options: ["正确","错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
时间复杂度 O(n log n) 是计算机科学中极为常见的最优平均/最坏情况界限。在经典排序算法中，归并排序（Merge Sort）的最坏、平均、最好时间复杂度均为 O(n log n)；快速排序（Quick Sort）的平均时间复杂度为 O(n log n)，其最坏情况为 O(n²) 但在实际随机输入中极少发生；堆排序（Heap Sort）的时间复杂度始终为 O(n log n)。这些算法在 GESP 三级的教学范围内是高频考点，因此命题“时间复杂度 O(n log n) 在很多排序算法中常见”完全成立。

**易错提醒：**
- 注意区分“最好情况”“平均情况”“最坏情况”——如冒泡排序最好可达 O(n)，但平均和最坏是 O(n²)，并非 O(n log n)。
- O(n log n) 是基于比较的排序算法在通用情况下的理论最优下界（决策树模型证明），这一结论与具体实现无关。

**知识延伸：**
在 GESP 三级中，除排序外，O(n log n) 还出现在如归并排序求逆序对、分治策略的时间复杂度分析等场景中，理解这一复杂度的来源（通常来自“分而治之”策略中的 log n 层递归乘以每层的 O(n) 合并代价）对解题至关重要。

**考点：** 判断题、算法思维、时间复杂度`,
            tags: ['算法思维']
        },
        {
            id: 23,
            type: 'judge',
            question: `动态规划一定比贪心更简单。`,
            options: ["正确","错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
动态规划（Dynamic Programming）与贪心算法（Greedy Algorithm）是两种不同的算法范式，适用于不同类型的问题，不能简单地以“复杂性”做优劣比较。动态规划需要明确定义状态、设计状态转移方程、考虑子问题重叠和最优子结构，通常实现难度较高。但贪心算法虽然思路简单（局部最优推导全局最优），其正确性证明往往非常困难，且适用范围受限。两者各有难易场景：对于“硬币找零”这样的问题，贪心有时比 DP 更直接；而对于“背包问题”，DP 是唯一正确通用解法。因此“动态规划一定比贪心更简单”这一绝对化命题是错误的。

**易错提醒：**
- 误以为“简单=容易想到”。贪心算法的设计往往比 DP 更需要直觉和数学洞察。
- 混淆“实现难度”与“思维难度”。DP 代码通常更长（需要循环填表），但思路更结构化；贪心代码可能只有几行，但证明为何正确极为复杂。

**知识延伸：**
判断一个问题该用贪心还是 DP 是 GESP 三级的核心考点。关键判断依据是“是否存在最优子结构且子问题不独立”——若局部最优能推出全局最优（且可证明），则贪心可行；否则需要 DP 穷举子问题。

**考点：** 判断题、算法思维、动态规划与贪心对比`,
            tags: ['算法思维']
        },
        {
            id: 24,
            type: 'judge',
            question: `使用 long long 可降低部分整数溢出风险。`,
            options: ["正确","错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
C++ 中，\`int\` 类型通常为 32 位有符号整数，范围为 -2,147,483,648 到 2,147,483,647（约 ±2×10⁹）。\`long long\` 类型为 64 位有符号整数，范围约为 -9.22×10¹⁸ 到 9.22×10¹⁸。在涉及较大数值计算（如阶乘、斐波那契数列第50项以上、累加循环等）时，\`long long\` 能容纳更大的中间结果和最终结果，从而有效降低或消除整数溢出风险。注意原命题使用的是“降低部分”而非“消除所有”，这是严谨的表述——因为即使 \`long long\` 也有上限，对极大规模数据仍可能溢出。因此命题成立。

**易错提醒：**
- 误以为 \`long long\` 可以存储任意大的整数。实际上 64 位有符号整数的上限约为 9.2×10¹⁸，超过此值仍会溢出。
- 浮点数类型（如 \`double\`）可以表示更大的数量级，但会损失精度，并非整数溢出的通用替代方案。
- 在 GESP 中，常见考点是让考生识别哪些计算（递归、循环累加）需要使用 \`long long\` 避免溢出。

**知识延伸：**
GESP 二级已涉及 \`int\` 的范围限制，三级题目常通过组合数学（如计算排列数 C(20,10)）、大数累加或递归斐波那契来考察整数类型的选择，是高频易错考点。

**考点：** 判断题、程序分析、整数溢出与 long long`,
            tags: ['程序分析']
        },
        {
            id: 25,
            type: 'judge',
            question: `程序调试时构造边界样例没有意义。`,
            options: ["正确","错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
程序调试中，边界样例（Boundary/Edge Cases）恰恰是发现潜在缺陷最重要的手段之一。边界条件包括：空输入（空数组、空字符串）、单个元素、极值输入（最大值、最小值）、重复元素、已排序/逆序输入等。这些位置之所以关键，是因为程序员在设计代码时，注意力往往集中在“一般情况”的处理逻辑上，而边界情况容易被忽视。例如，在遍历数组时，若循环条件写成 \`i <= n\` 而非 \`i < n\`，会在处理最后一个元素时产生越界错误；处理空指针时若未做非空检查，会在边界输入下崩溃。因此，构造边界样例不仅有意义，而且是调试的标准最佳实践。

**易错提醒：**
- 误以为“复杂/随机”的测试用例比边界样例更重要。实际上，边界样例更可能暴露逻辑缺陷。
- 忽略边界条件是初学者最常见的 bug 来源之一，尤其是在循环、分支、递归终止条件等位置。

**知识延伸：**
在 GESP 的程序设计题中，题目往往会明确或隐含地提示“注意边界情况”，考生在自测时应主动构造这些样例来验证程序的正确性。软件工程中将其称为“边界值分析”（Boundary Value Analysis），是系统测试的基本方法。

**考点：** 判断题、程序分析、调试方法`,
            tags: ['程序分析']
        }
    ]
};
