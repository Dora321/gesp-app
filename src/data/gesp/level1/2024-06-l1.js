import { LEVEL1_TAGS } from './shared.js';

export const paperData = {
  id: '2024-06-l1',
  title: '2024年6月 GESP C++ 一级认证真题',
  level: 1,
  year: 2024,
  month: 6,
  session: 6,
  timeLimit: 5400,
  source: {
    officialPdf: 'https://raw.githubusercontent.com/Dora321/gesp-official-pdfs/main/pdfs/2024%E5%B9%B46%E6%9C%88-C%2B%2B1%E7%BA%A7.pdf',
    type: 'official-verified',
  },
  confidence: {
    answer: 1.0,
    statement: 0.9,
  },
  questions: [
    {
      id: 1,
      type: 'single',
      question: `在 C++ 中，下列不可作为变量名的是（ ）。`,
      options: ['five-Star', 'five_star', 'fiveStar', '_fiveStar'],
      answer: 0,
      score: 2,
      explanation: `**答案：A (five-Star)**

**核心解析：**
C++ 变量名（标识符）的命名规则如下：只能由字母（A-Z、a-z）、数字（0-9）和下划线（_）组成，且第一个字符不能是数字。在本题四个选项中，\`five-Star\` 包含了连字符 \`-\`，该字符在 C++ 语法中是二元运算符"减号"，不属于标识符字符集。因此以此作为变量名会导致编译错误（编译器会将 \`five - Star\` 解析为标识符 \`five\` 减去标识符 \`Star\`，而非一个整体变量名）。

**选项逐项分析：**
- **A (five-Star)**：✅ 正确。非法标识符。连字符 \`-\` 不是标识符的有效组成部分，C++ 编译器将其解析为减法运算符。
- **B (five_star)**：❌ 错误。合法标识符。仅含字母和下划线，符合命名规范。
- **C (fiveStar)**：❌ 错误。合法标识符。纯字母组成，符合驼峰命名法约定俗成的风格。
- **D (_fiveStar)**：❌ 错误。合法标识符。以下划线开头的标识符完全符合 C++ 标准语法，且在某些编译器实现中有特殊含义（如全局命名空间或内部变量）。

**易错提醒：**
- 易错点1：初学者可能误以为 \`fiveStar\` 不合法（因混淆了驼峰命名与下划线命名），实际上两者都是合法的命名风格。
- 易错点2：以下划线开头的标识符虽然在语法上合法，但在实际编码中应避免以单个下划线加数字或大写字母开头，因为可能与编译器内部实现或标准库宏发生冲突。

**知识延伸：**
C++ 保留字（关键字）如 \`int\`、\`for\`、\`while\` 等也不能用作变量名，它们具有特殊语法含义。此外，C++ 标识符区分大小写，\`myVar\` 和 \`MyVar\` 是两个不同的标识符。

**考点：** C++ 标识符命名规则、合法字符集`,
    },
    {
      id: 2,
      type: 'single',
      question: `C++ 表达式 \`3 - 3 * 3 / 5\` 的值是（ ）。`,
      options: ['-1.2', '1', '0', '2'],
      answer: 3,
      score: 2,
      explanation: `**答案：D (2)**

**核心解析：**
根据运算符优先级逐步计算表达式 \`3 - 3 * 3 / 5\`：
1. 乘法 \`3 * 3\` 先执行，得 \`9\`；
2. 除法 \`9 / 5\` 次之，整数除法只保留整数部分，得 \`1\`；
3. 减法 \`3 - 1\` 最后执行，得 \`2\`。
因此最终结果为 \`2\`。

**选项逐项分析：**
- **A (-1.2)**：❌ 错误。若使用浮点数除法（\`9.0 / 5\` 或类型转换），会得到 \`1.8\`，但 C++ 中两个整数相除执行整数截断，结果为 \`1\` 而非 \`1.8\`，因此不可能出现 \`-1.2\`。
- **B (1)**：❌ 错误。可能误将整个表达式理解为 \`(3 - 3 * 3) / 5 = 0 / 5 = 0\`，或忽略减法只保留除法结果，未遵循从左到右的完整运算流程。
- **C (0)**：❌ 错误。忽略了先乘除后加减的基本规则，直接计算 \`3 - 3 = 0\` 后再与 \`0 / 5\` 混淆。
- **D (2)**：✅ 正确。严格遵循乘除优先、整数截断规则，得出正确结果 \`2\`。

**易错提醒：**
- 易错点1：混淆整数除法与浮点除法。C++ 中 \`9 / 5\` 的结果是整数 \`1\`，而非数学意义上的 \`1.8\`。必须明确操作数类型才能判断除法行为。
- 易错点2：优先级记忆不清。乘除优先级相同且高于加减，但同级运算遵循左结合律。应确保先算 \`3 * 3\` 再算 \`9 / 5\`，而非先算 \`3 * 3 / 5\`。

**知识延伸：**
在 GESP 二级中还会涉及取模运算符 \`%\`，它与乘除法优先级相同，同属高优先级运算符。表达式求值顺序始终是：括号 > 乘除模 > 加减。

**考点：** 运算符优先级、整数除法截断规则`,
    },
    {
      id: 3,
      type: 'single',
      question: `设 \`N\` 为正整数，则 \`N % 3 + N % 7\` 可能输出的最大值是（ ）。`,
      options: ['6', '8', '9', '10'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (8)**

**核心解析：**
设 \`N\` 为任意正整数，分别分析两个取模表达式的最大值范围：
- \`N % 3\` 的结果必然落在 \`[0, 2]\` 区间（模数减 1 以内），最大可取 \`2\`；
- \`N % 7\` 的结果必然落在 \`[0, 6]\` 区间（模数减 1 以内），最大可取 \`6\`。
两者相加的最大理论值为 \`2 + 6 = 8\`。
需要验证该值是否可达：取 \`N = 2\` 时，\`N % 3 = 2\`，\`N % 7 = 2\`，和为 \`4\`；取 \`N = 6\` 时，\`N % 3 = 0\`，\`N % 7 = 6\`，和为 \`6\`；取 \`N = 20\` 时，\`N % 3 = 2\`，\`N % 7 = 6\`，和恰好为 \`8\`。因此 \`8\` 是可实现的最大值。

**选项逐项分析：**
- **A (6)**：❌ 错误。\`6\` 仅为 \`N % 7\` 单独的最大可能值，忽略了 \`N % 3\` 也可能取到 \`2\` 的情况。
- **B (8)**：✅ 正确。分别取两个取模表达式的最大值相加，验证 \`N = 20\` 时可达，故 \`8\` 为正确结果。
- **C (9)**：❌ 错误。\`N % 7\` 最大为 \`6\`，\`N % 3\` 最大为 \`2\`，两者之和的理论上限为 \`8\`，不可能达到 \`9\`。
- **D (10)**：❌ 错误。超出两表达式最大值的理论加和上限。

**易错提醒：**
- 易错点1：认为取模结果可以等于模数本身。实际上 \`N % m\` 的取值范围是 \`[0, m-1]\`，当 \`N\` 恰好是 \`m\` 的倍数时结果为 \`0\`，最大只能是 \`m-1\`。
- 易错点2：未验证最大值是否可同时达到。分开讨论最大值后需代入具体 \`N\` 值确认两个条件能否同时满足（本题中 \`N % 3 = 2\` 和 \`N % 7 = 6\` 可通过 \`N = 20\` 同时实现）。

**知识延伸：**
取模运算的本质是"余数"，即 \`N = (N / m) * m + (N % m)\`。在 GESP 后续级别中，取模常用于判断整除性（如 \`N % 2 == 0\` 判断偶数）、周期性问题或循环数组索引。

**考点：** 取模运算的取值范围分析`,
    },
    {
      id: 4,
      type: 'single',
      question: `语句 \`printf("5%%2={%d}\\n", 5 % 2);\` 的输出是（ ）。`,
      options: ['1={1}', '5%2={5%2}', '5%2={1}', '5 ={1}'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (5%2={1})**

**核心解析：**
分析格式化字符串 \`"5%%2={%d}\\n"\` 的输出机制：
1. \`printf\` 的格式字符串中，连续两个百分号 \`%%\` 是转义序列，表示输出一个原始的 \`%\` 字符；
2. 因此格式字符串中的 \`5%%2=\` 部分会原样输出为 \`5%2=\`，不会触发任何运算；
3. \`%d\` 是整型占位符，会被替换为后续参数 \`5 % 2\` 的计算结果；
4. \`5 % 2\` 是取模运算，\`5\` 除以 \`2\` 余 \`1\`，故替换结果为 \`1\`；
5. \`\\n\` 是转义字符，表示换行。
最终屏幕输出为 \`5%2={1}\` 后换行。

**选项逐项分析：**
- **A (1={1})**：❌ 错误。误解了 \`%%\` 的作用，\`%%\` 不会"吞掉"前面的数字，而是直接输出为 \`%\`。
- **B (5%2={5%2})**：❌ 错误。混淆了格式字符串与普通文本，\`%d\` 占位符专用于接收并格式化后续参数，不会原样打印参数表达式本身。
- **C (5%2={1})**：✅ 正确。\`%%\` 正确转义为 \`%\`，\`%d\` 正确提取参数 \`5 % 2\` 的值 \`1\` 填入大括号。
- **D (5 ={1})**：❌ 错误。丢失了 \`%%\` 应输出的 \`%\` 字符，多余的空格也不存在于格式字符串中。

**易错提醒：**
- 易错点1：误以为 \`%d\` 会打印格式字符串中 \`%d\` 位置之前的表达式文本。实际上 \`%d\` 是占位符，只负责格式化输出后方对应的参数值。
- 易错点2：不熟悉 \`%%\` 转义。在 printf 格式字符串中，单个 \`%\` 有特殊含义（引入格式说明符），想要输出 \`%\` 本身必须使用 \`%%\`。类似规则也存在于 scanf 中（如 \`%%\` 跳过输入流中的 \`%\`）。

**知识延伸：**
\`printf\` 格式字符串中的常见转义序列包括：\`\\n\`（换行）、\`\\t\`（制表符）、\`\\\\\`（反斜杠）、\`%%\`（百分号）。此外，\`%d\`（十进制整数）、\`%f\`（浮点数）、\`%c\`（字符）等格式说明符各有其对应的参数类型，必须严格匹配。

**考点：** printf 格式化输出机制、百分号转义`,
    },
    {
      id: 5,
      type: 'single',
      question: `对整型变量 \`i\` 执行 \`cin >> i; cout << i;\`，若输入 \`5+2\`，正确说法是（ ）。`,
      options: ['将输出 7', '将输出 5', '语句会报错', '输出不确定'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (将输出 5)**

**核心解析：**
分析 \`cin >> i\` 对输入 \`5+2\` 的处理过程：
1. \`cin\` 是 C++ 标准输入流对象，\`operator>>\` 是流提取运算符；
2. 当执行 \`cin >> i\`（其中 \`i\` 为 \`int\` 类型）时，流会尝试从输入缓冲区读取一个整数；
3. 流提取操作会跳过前导空白字符（空格、制表符、换行等），然后开始读取数字字符；
4. 读取到数字 \`5\` 后，遇到非数字字符 \`+\`，流判定整数提取完毕，将 \`5\` 赋值给变量 \`i\`，并停止读取；
5. 字符 \`+\` 及其后的内容仍保留在输入缓冲区中，不会被消费；
6. 随后的 \`cout << i\` 输出变量 \`i\` 的值，即 \`5\`。
整个过程没有语法错误，也不会进行表达式求值，\`cin\` 只负责按类型匹配提取数据。

**选项逐项分析：**
- **A (将输出 7)**：❌ 错误。\`cin >> i\` 是数据提取操作，不具备表达式求值功能，不会计算 \`5+2\` 的数学结果。流提取运算符只根据目标变量的类型读取相应格式的数据。
- **B (将输出 5)**：✅ 正确。\`cin\` 成功读取字符 \`5\` 并存储为整型，输出结果为 \`5\`。
- **C (语句会报错)**：❌ 错误。\`cin\` 能够成功提取整数 \`5\`，赋值给 \`i\` 的过程完全合法。流状态在首次提取后仍正常，只是输入缓冲区会残留未读取的 \`+2\`。
- **D (输出不确定)**：❌ 错误。流提取的行为是确定的：数字 \`5\` 会被完整读取，\`+\` 作为非数字字符导致提取中断。

**易错提醒：**
- 易错点1：混淆输入操作与表达式求值。\`cin\` 只按类型规则提取数据，不解释数学运算符；输入 \`5+2\` 不等于在代码中写 \`5+2\`。
- 易错点2：未理解输入缓冲区的残留机制。\`cin >> i\` 读取 \`5\` 后，\`+2\` 仍留在缓冲区中，后续若有另一个 \`cin >> j\` 操作，会从缓冲区中的 \`+\` 开始读取，可能导致第二次提取失败或读到意外值。

**知识延伸：**
\`cin\` 的类型安全提取机制是其核心特性。如果后接 \`cin >> j\`（也是 \`int\`），由于缓冲区首字符是 \`+\`，流提取会失败并设置错误标志。在 GESP 二级中会进一步学习如何通过 \`cin.ignore()\` 清除缓冲区或使用 \`cin >> ws\` 跳过空白。

**考点：** cin 流提取原理、类型匹配与字符边界判定`,
    },
    {
      id: 6,
      type: 'single',
      question: `下面代码执行后的输出是（ ）。\n\`\`\`cpp\ndouble a = 101.1;\na = (int)a;\nprintf("a + 1 = {%d}", (int)(a + 1));\n\`\`\``,
      options: ['102={102}', 'a+1={a+1}', 'a + 1 = {102}', '会报错'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (a + 1 = {102})**

**核心解析：**
逐步执行代码，观察变量状态变化和格式化输出结果。

1. **变量赋值与类型转换**
   - \`double a = 101.1;\` 定义双精度浮点变量 a，初始值为 101.1
   - \`a = (int)a;\` 将 a 强制转换为 int 类型。101.1 截断小数部分后，a 的值变为 **101**（注意：这里发生了隐式的 double→int 转换，虽然通过强制转换实现）

2. **表达式求值**
   - \`(int)(a + 1)\` 中，a + 1 = 101 + 1 = 102
   - 外层 \`(int)\` 转换对于已是 int 的 102 不改变结果

3. **格式化输出**
   - printf 的格式字符串为 \`"a + 1 = {%d}"\`，其中 \`{\` 和 \`}\` 是字面字符会原样输出
   - \`%d\` 是占位符，被 102 替换
   - 最终输出：**a + 1 = {102}**

**选项逐项分析：**
- **A (102={102})**：❌ 错误。格式字符串中 \`"a + 1 = "\` 是字面文本，会原样输出，不是 \`102=\`。
- **B (a+1={a+1})**：❌ 错误。\`%d\` 是格式占位符，会被右侧表达式的求值结果（102）替换，不会原样输出 \`a+1\`。
- **C (a + 1 = {102})**：✅ 正确。原样输出 \`"a + 1 = {"\`，\`%d\` 被 102 替换，再输出 \`"}"\`，得到 \`a + 1 = {102}\`。
- **D (会报错)**：❌ 错误。代码语法完全合法，类型转换合法，printf 格式参数类型匹配。

**易错提醒：**
- 显式类型转换 \`(int)a\` 会截断浮点数的小数部分，而非四舍五入
- printf 中大括号 \`{\` \`}\` 不是格式控制符，会原样输出

**知识延伸：**
C++ 中强制类型转换（cast）共有四种形式：static_cast、dynamic_cast、const_cast、reinterpret_cast。本题的 \`(int)a\` 属于旧式 C 风格转换，在 GESP 一级范围内使用即可。

**考点：** 显式类型转换、浮点精度截断、printf 格式化输出`,
    },
    {
      id: 7,
      type: 'single',
      question: `表达式 \`9/4 - 6 % (6 - 2) * 10\` 的值是（ ）。`,
      options: ['-17.75', '-18', '-14', '-12.75'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (-18)**

**核心解析：**
严格按照运算符优先级和整数除法规则逐步计算。

1. **处理括号**
   - \`(6 - 2)\` 先算，结果为 **4**

2. **处理乘除取模（左到右）**
   - 表达式变为：\`9/4 - 6 % 4 * 10\`
   - \`9/4\`：整数除法，9÷4 = 2 余 1，**结果为 2**（截断小数部分）
   - \`6 % 4\`：取模运算，求 6 除以 4 的余数，**结果为 2**
   - 此时表达式：\`2 - 2 * 10\`
   - \`2 * 10\`：乘法，**结果为 20**

3. **处理加减**
   - \`2 - 20\` = **-18**

**选项逐项分析：**
- **A (-17.75)**：❌ 错误。混淆了整数除法和浮点数除法。若将 \`9/4\` 按 2.25 计算，会得到 2.25 - 20 = -17.75。
- **B (-18)**：✅ 正确。严格遵循整数除法截断规则（9÷4=2），最终结果 -18。
- **C (-14)**：❌ 错误。可能错误地将 \`%\` 运算当作整除计算，或改变了运算顺序。
- **D (-12.75)**：❌ 错误。同样混淆了整数除法与浮点除法，并可能算错了其他步骤。

**易错提醒：**
- 整数除法（/）在 C++ 中对两个整数操作时，结果只保留整数部分（向零截断），不是四舍五入
- 取模运算符（%）只适用于整数，运算结果符号与被除数相同
- 乘法、除法、取模优先级相同，均高于加法和减法

**知识延伸：**
C++ 中整数除法的截断规则：当操作数均为整数时，结果向零取整。例如 -9/4 = -2（而非 -2.25）。

**考点：** 复合运算的优先级、整数除法截断、取模运算`,
    },
    {
      id: 8,
      type: 'single',
      question: `输入 \`10\` 后，下面代码的正确输出是（ ）。\n\`\`\`cpp\nint n;\ncin >> n;\nif (n % 3 == 1)\n printf("第5行代码%2d", n % 3);\nelse\n printf("第6行代码%2d", n % 3);\n\`\`\``,
      options: ['第5行代码1', '第6行代码1', '第5行代码 1', '第6行代码 1'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (第5行代码 1)**

**核心解析：**
分析条件判断、表达式求值和 printf 宽度格式化。

1. **取模运算**
   - 输入 \`n = 10\`
   - \`n % 3\` = 10 ÷ 3 的余数 = **1**

2. **条件判断**
   - \`if (n % 3 == 1)\` 即 \`if (1 == 1)\`
   - 条件为**真**，执行第5行代码

3. **格式化输出**
   - \`printf("第5行代码%2d", n % 3);\` 即 \`printf("第5行代码%2d", 1);\`
   - \`%2d\`：表示以宽度为 2 输出整数，不足时右对齐，左侧补空格
   - 数值 1 只有一位，需要在左侧补 **1 个空格**
   - 最终输出：**第5行代码 1**（注意"代码"和"1"之间有空格）

**选项逐项分析：**
- **A (第5行代码1)**：❌ 错误。未考虑 \`%2d\` 的宽度格式化要求，一位数应在左侧补一个空格。
- **B (第6行代码1)**：❌ 错误。10 % 3 = 1，满足条件 \`== 1\`，应执行 if 分支而非 else 分支。
- **C (第5行代码 1)**：✅ 正确。进入 if 分支，输出"第5行代码"，\`%2d\` 格式使 1 前面补一个空格。
- **D (第6行代码 1)**：❌ 错误。首先判断逻辑错误（应进入 if 而非 else），其次也忽略了 \`%2d\` 的空格填充。

**易错提醒：**
- \`%2d\` 表示输出至少占 2 个字符宽度，不足时补空格；若是 \`%02d\` 则补 0
- 不能仅凭选项文本的"空格的有无"判断输出，需要理解 printf 格式说明符的含义

**知识延伸：**
printf 的宽度格式：\`%wd\`（w 为宽度）中，若实际数值位数小于 w，左侧补空格；若大于 w，则全部输出。C++ 中常用的格式还有 \`%5s\`（字符串左对齐）、\`%-5d\`（左对齐）等。

**考点：** 取模运算、条件分支控制、printf 格式化宽度输出`,
    },
    {
      id: 9,
      type: 'single',
      question: `下面代码执行后输出是（ ）。\n\`\`\`cpp\nint i = 0, sum = 0;\nfor (; i < 10; sum += i, i++);\ncout << i << " " << sum << endl;\n\`\`\``,
      options: ['9 45', '10 55', '10 45', '11 55'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (10 45)**

**核心解析：**
分析 for 循环的执行流程，特别关注循环终止时变量的最终状态。

1. **循环结构分析**
   - \`for (; i < 10; sum += i, i++);\`
   - 初始化部分为空；循环条件 \`i < 10\`；迭代表达式为两个逗号表达式：\`sum += i\` 和 \`i++\`
   - 循环体为空语句 \`;\`

2. **逐次迭代追踪**
   | 迭代 | 进入循环时 i | sum 累加值 | 执行 sum+=i 后 | 执行 i++ 后 |
   |------|-------------|-----------|---------------|------------|
   | 第1次 | 0 | +0 | 0 | 1 |
   | 第2次 | 1 | +1 | 1 | 2 |
   | 第3次 | 2 | +2 | 3 | 3 |
   | ... | ... | ... | ... | ... |
   | 第9次 | 8 | +8 | 36 | 9 |
   | 第10次 | 9 | +9 | 45 | 10 |

3. **循环终止判定**
   - 当 \`i = 10\` 时，判断条件 \`i < 10\`（即 10 < 10）为**假**
   - 循环终止，i = **10**
   - sum 累加了 0+1+2+...+9 = **45**

4. **最终输出**
   - \`cout << i << " " << sum << endl;\` 输出 **10 45**

**选项逐项分析：**
- **A (9 45)**：❌ 错误。i=9 时虽然 sum=36，但还会执行迭代表达式，使 i 自增到 10 后才判断失败退出。
- **B (10 55)**：❌ 错误。sum 的累加范围是 0 到 9（共10个数），而非 0 到 10。0+1+2+...+9 = 45，不是 55。
- **C (10 45)**：✅ 正确。循环终止时 i=10，sum 累加了 0~9 之和为 45。
- **D (11 55)**：❌ 错误。i 不会增加到 11（循环条件在 i=10 时已不满足）；sum 也不会累加到 55。

**易错提醒：**
- for 循环的迭代表达式在每次迭代**结束后**执行，因此退出循环后变量已更新到不满足条件的值
- sum 的初值为 0，首次迭代 \`sum += 0\` 不改变结果

**知识延伸：**
1 到 n 的整数之和公式为 n(n+1)/2。本题中 0+1+2+...+9 = 9×10/2 = 45。验证循环结果可用公式快速检验。

**考点：** for 循环执行流程、循环终止条件、迭代表达式执行时机、累加和计算`,
    },
    {
      id: 10,
      type: 'single',
      question: `有关判断质数程序的说法，正确的是（ ）。`,
      options: ['输入负数可能输出“是质数”', '输入 2 会输出“不是质数”', '输入 2 会输出“是质数”，即使循环体未执行', '把 `N>=2` 改成 `N>2` 仍然正确'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (输入 2 会输出“是质数”，即使循环体未执行)**

**核心解析：**
分析质数判断程序的典型实现，理解边界条件（n=2）的处理逻辑。

1. **典型质数判断程序结构**
   \`\`\`cpp
   // 假设标准写法：
   if (N < 2) {
       cout << "不是质数";
   } else {
       // 检查是否有因数
       for (int i = 2; i * i <= N; i++) {
           if (N % i == 0) {
               cout << "不是质数";
               return;
           }
       }
       cout << "是质数";
   }
   \`\`\`

2. **n = 2 时的执行分析**
   - \`N >= 2\` 成立，进入 else 分支
   - 循环条件 \`i * i <= N\` 即 \`2 * 2 <= 2\`，即 \`4 <= 2\`
   - 条件**不满足**，**循环体一次也不执行**
   - 程序直接执行到 \`cout << "是质数";\`
   - **正确输出“是质数”**

3. **n = 2 是质数的原因**
   - 质数定义：大于 1 的自然数，且只能被 1 和自身整除
   - 2 满足定义（最小的质数，也是唯一的偶数质数）

**选项逐项分析：**
- **A (输入负数可能输出“是质数”)**：❌ 错误。标准程序会在入口处 \`if (N < 2)\` 判断并输出"不是质数"，不会输出"是质数"。
- **B (输入 2 会输出“不是质数”)**：❌ 错误。2 是质数，程序会正确输出"是质数"，不会输出"不是质数"。
- **C (输入 2 会输出“是质数”，即使循环体未执行)**：✅ 正确。2 >= 2 进入判断，循环条件 4<=2 不满足，循环体未执行，但程序逻辑仍正确输出"是质数"。
- **D (把 N>=2 改成 N>2 仍然正确)**：❌ 错误。若改为 \`N > 2\`，则 N=2 时会进入 \`if (N < 2)\` 分支输出"不是质数"，结果错误。

**易错提醒：**
- 2 是质数，且是唯一的偶数质数，易被忽略
- 循环条件 \`i*i <= N\` 使用乘法避免除法开销，同时确保遍历到 sqrt(N)
- 边界条件（如 N=2）的处理体现了循环"零次执行"的特殊情况

**知识延伸：**
判断质数的循环条件可写为 \`i <= sqrt(N)\` 或 \`i * i <= N\`，后者避免了重复计算平方根。若循环体内找到因数，应立即输出并退出，避免多余判断。

**考点：** 质数定义与判定、循环入口条件判定、边界条件处理（n=2）`,
    },
    {
      id: 11,
      type: 'single',
      question: `关于“求 1~N 奇数之和”的程序，错误说法是（ ）。`,
      options: ['输入 10 时最后一行会输出 `11 25`', '输入 5 时最后一行会输出 `6 9`', '把 `i += 1` 移到 `if` 前面仍然正确', '删去 `if` 并改成 `i += 2` 也可实现'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (把 \`i += 1\` 移到 \`if\` 前面仍然正确)**

**核心解析：**

原程序循环变量 \`i\` 从 1 开始递增，在 \`if\` 内部使用 \`i += 1\` 后再判断奇偶性。关键在于 \`i += 1\` 改变的是 \`i\` 本身的奇偶性：

- 原逻辑：\`i += 1\` 在 if 内部 → 对当前 \`i\` 递增后再判定 → 符合预期的奇数求和逻辑。
- 移到 \`if\` 前面后：对当前 \`i\` 先递增再判定 → 奇偶性全部错位 1 → 求和结果从奇数变成偶数，逻辑被破坏。

例如 N=5 时：
- 原逻辑：1→2(判断2)、3→4(判断4)、5→6(判断6)，累加 1+3+5 = 9 ✓
- 改后逻辑：1→2(判断2)、3→4(判断4)、5→6(判断6)，累加 2+4+6 = 12 ✗

因此，"移到 \`if\` 前面仍然正确"这个说法本身是错误的。

**选项逐项分析：**
- **A (输入10时最后一行会输出 \`11 25\`)**：❌ 错误。该项本身错误。实际应为 \`5 25\`（5个奇数，和为25），与题干结果不符。
- **B (输入5时最后一行会输出 \`6 9\`)**：❌ 错误。该项本身错误。实际应为 \`3 9\`（3个奇数，和为9），与题干结果不符。
- **C (把 \`i += 1\` 移到 \`if\` 前面仍然正确)**：✅ 正确。该项不符合题目限定，正是本题要选的项。该项说法本身错误（符合本题要求：找错误说法）。移到 \`if\` 前面会导致奇偶判断全部错位，程序功能从"求奇数和"变为"求偶数和"，不是"仍然正确"。
- **D (删去 \`if\` 并改成 \`i += 2\` 也可实现)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该项本身正确。删去 if 判断后用步长为 2 遍历奇数，等效地实现了求奇数和的功能。

**易错提醒：**
- \`i += 1\` 在 if 内与 if 外的区别：if 外的 \`i += 1\` 会改变判定时的奇偶性，而非仅改变循环步长。
- 判定"错误说法"题时，需逐一检查每个说法本身是否正确，而非判断该选项是否满足题目功能需求。

**知识延伸：**
- 循环内增量语句的位置会影响循环体对当前变量的使用，需结合具体判定条件分析语义变化。

**考点：** 循环内增量修改的时机对判定条件的影响`,
    },
    {
      id: 12,
      type: 'single',
      question: `判断完全平方数时，横线处应填（ ）。\n\`\`\`cpp\nfor (int i = 1; i * i <= N; i++) {\n if (________) {\n flag = true;\n break;\n }\n}\n\`\`\``,
      options: ['i == N*N', 'i*10 == N', 'i+i == N', 'i*i == N'],
      answer: 3,
      score: 2,
      explanation: `**答案：D (i*i == N)**

**核心解析：**

完全平方数的定义：存在整数 \`i\`，使得 \`i² = N\`。代码中 \`for\` 循环从 \`i=1\` 开始遍历，只要 \`i*i ≤ N\` 就继续。若某次迭代恰好有 \`i*i == N\`，说明 \`N\` 是某个整数的平方。

- 循环终止条件 \`i*i ≤ N\` 保证 \`i\` 不会超过 \`√N\`，因此只需检查是否存在整数 \`i\` 使 \`i*i == N\` 即可。
- 条件 \`i*i == N\` 在循环内部，当找到匹配的 \`i\` 时标记并退出。

**选项逐项分析：**
- **A (i == N*N)**：❌ 该项本身错误。写成了 \`i == N*N\`，即判定 \`i\` 是否等于 \`N²\`，而 \`i\` 的范围最多到 \`√N\`，绝无可能等于 \`N²\`（除非 N=0 或 1，但逻辑上仍不正确）。
- **B (i*10 == N)**：❌ 该项本身错误。乘以 10 与完全平方数判定毫无关联，逻辑上不成立。
- **C (i+i == N)**：❌ 错误。该项本身错误。即 \`2i == N\`，判定的是 \`N\` 是否为偶数且等于 \`2i\`，与平方数判定无关。
- **D (i*i == N)**：✅ 该项本身正确。直接表达完全平方数的数学定义，是唯一符合题意的填入项。

**易错提醒：**
- 完全平方数判断只需比较 \`i*i\` 与 \`N\`，不要误用加法、乘法 10 等无关操作。
- 循环上限是 \`i*i ≤ N\`，退出循环时 \`i\` 可能已超过 \`√N\`，因此判定必须放在循环体内而非循环结束后。

**知识延伸：**
- 完全平方数判定与质数判定（\`i*i ≤ N\`）都使用类似的遍历上限优化，是 GESP 常考的循环边界设计。

**考点：** 完全平方数的数学特征与代码实现`,
    },
    {
      id: 13,
      type: 'single',
      question: `执行代码后 \`cnt\` 的值是（ ）。\n\`\`\`cpp\nint cnt = 0;\nfor (int i = 0; i < 10; i += 2) {\n if (i <= 6) cnt++;\n}\n\`\`\``,
      options: ['8', '7', '4', '1'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (4)**

**核心解析：**

逐步推导循环执行过程：

\`\`\`
循环初始化：int i = 0
步长：i += 2（每次增加2）
终止条件：i < 10

第1次：i = 0，0 < 10 ✓，判断 0 ≤ 6 ✓ → cnt++ (cnt=1)
第2次：i = 2，2 < 10 ✓，判断 2 ≤ 6 ✓ → cnt++ (cnt=2)
第3次：i = 4，4 < 10 ✓，判断 4 ≤ 6 ✓ → cnt++ (cnt=3)
第4次：i = 6，6 < 10 ✓，判断 6 ≤ 6 ✓ → cnt++ (cnt=4)
第5次：i = 8，8 < 10 ✓，判断 8 ≤ 6 ✗ → 不执行 cnt++
第6次：i = 10，10 < 10 ✗ → 循环结束
\`\`\`

循环变量序列：0, 2, 4, 6, 8。满足 \`i ≤ 6\` 的有 0、2、4、6 共 4 次。

**选项逐项分析：**
- **A (8)**：❌ 错误。可能误将循环次数（5次）当作 cnt 值，或未考虑 if 条件过滤。
- **B (7)**：❌ 错误。数值无依据，明显偏离实际计数。
- **C (4)**：✅ 正确。精确计算结果，满足条件的值为 0、2、4、6 共 4 个。
- **D (1)**：❌ 错误。可能只统计了 i=6 或 i=0 一次，遗漏了其他符合条件的值。

**易错提醒：**
- for 循环的终止条件 \`i < 10\` 与循环体内条件 \`i ≤ 6\` 是两个独立的条件，需分别判断。
- 循环变量步长为 2 时，序列为 0,2,4,6,8 共 5 次迭代，而非 10 次。

**知识延伸：**
- for 循环中三个表达式各司其职：初始化、执行条件、步长更新。理解三者关系是分析循环行为的基础。

**考点：** for 循环步长控制及内部 if 判定过滤`,
    },
    {
      id: 14,
      type: 'single',
      question: `GESP 一级可以选择的认证语言有几种？`,
      options: ['1', '2', '3', '4'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (3)**

**核心解析：**

GESP（Graphic Era Software Platform，青少年编程能力等级认证）一级认证针对零基础或初级学员，面向青少年编程教育实际需求，开设的编程语言类别为：

1. 图形化编程（Scratch）
2. Python
3. C++

共 3 种语言。考生可根据兴趣和基础选择其中一种参加认证。

**选项逐项分析：**
- **A (1)**：❌ 错误。GESP 不只支持 1 种语言，一级同时支持图形化、Python 和 C++ 三种。
- **B (2)**：❌ 错误。虽然 Python 和 C++ 是两种，但 GESP 一级还包含图形化（Scratch），共 3 种。
- **C (3)**：✅ 正确。GESP 一级认证可选择 3 种语言：Scratch、Python、C++。
- **D (4)**：❌ 错误。GESP 一级目前未开设第 4 种语言选项。

**易错提醒：**
- GESP 认证体系各级别可选语言可能不同，一级明确为 3 种，勿与二级、三级可能的语种范围混淆。
- 题目问的是"一级"，与二级的 C++/Python 两选一不同。

**知识延伸：**
- GESP 认证体系分为多个级别，一级入门支持三种语言，二级开始聚焦具体语言，体现了青少年编程教育的层次化设计。

**考点：** GESP 认证基础常识`,
    },
    {
      id: 15,
      type: 'single',
      question: `ENIAC 的主要器件是（ ）。`,
      options: ['感应线圈', '电子管', '晶体管', '集成电路'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (电子管)**

**核心解析：**

ENIAC（Electronic Numerical Integrator and Computer）是世界上第一台被广泛认可的通用电子计算机，于 1946 年在美国宾夕法尼亚大学研制成功。

其核心逻辑器件采用约 **18,000 个真空电子管**，辅以 1,500 个继电器、70,000 个电阻器等元件。这一万八千余个电子管是第一代电子计算机的标志性特征。

计算机发展按核心器件划分的四个时代：
- 第一代（1940s-1950s）：电子管
- 第二代（1950s-1960s）：晶体管
- 第三代（1960s-1970s）：集成电路
- 第四代（1970s至今）：大规模/超大规模集成电路

**选项逐项分析：**
- **A (感应线圈)**：❌ 错误。感应线圈是基础电磁元器件，用于电感、变压器等，不是计算机的核心计算器件。
- **B (电子管)**：✅ 正确。ENIAC 使用约 18,000 个真空电子管，是第一代电子计算机的标志性器件。
- **C (晶体管)**：❌ 错误。晶体管是第二代计算机的核心器件，ENIAC 诞生于晶体管发明之前。
- **D (集成电路)**：❌ 错误。集成电路是第三代计算机的特征，出现时间远晚于 ENIAC。

**易错提醒：**
- ENIAC 不是"第一台计算机"（严格来说ABC才是第一台），但它是第一台被国际学术界公认的通用电子计算机，需记住其电子管特征。
- 四代计算机的划分标准是核心逻辑器件，而非外壳材料或体积。

**知识延伸：**
- ENIAC 的诞生标志着电子计算机时代的开始，其设计思想（存储程序、二进制逻辑）直接影响了后续冯·诺依曼架构的提出。

**考点：** 计算机硬件发展史（四代论）`,
    },
    {
      id: 16,
      type: 'judge',
      question: `若变量 \`X\` 被赋值为 \`16.44\`，则 \`cout << X / 10\` 的输出一定是 \`1\`。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
题目仅说明变量 \`X\` 被赋值为 \`16.44\`，未声明 \`X\` 的数据类型。

- 若 \`X\` 为 \`double\` 或 \`float\` 类型：\`X / 10\` 执行浮点数除法，结果为 \`1.644\`，输出 \`1.644\`。
- 若 \`X\` 为整型（如 \`int X = 16.44;\`，隐式截断后 \`X = 16\`）：\`X / 10\` 执行整数除法，结果为 \`1\`（直接截断小数部分），输出 \`1\`。

由于数据类型未知，无法保证输出一定是整数 \`1\`。因此原命题为**错误**。

**易错提醒：**
- 很多学员看到 \`16.44 / 10 = 1.644\` 后直觉认为输出 \`1\`，但忽略了整数除法的截断规则。
- C++ 中整数除法向零截断，不会四舍五入。

**知识延伸：**
GESP 一级常考类型推断题：看到小数点 \`16.44\` 不等于确定是浮点型，C++ 允许将小数赋给整型变量（发生隐式截断）。判断输出必须先确定变量类型。

**考点：** 数据类型对除法运算结果的影响、整数除法截断规则`,
    },
    {
      id: 17,
      type: 'judge',
      question: `当 \`N=10\` 时，\`N/4\` 和 \`N%4\` 的值相等，且 \`N/4.0\` 的结果为 \`2.5\`。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
本题存在**双重错误**：

第一重错误：\`N=10\` 时，\`N/4\` 的值为 \`2\`（整数除法截断），\`N%4\` 的值为 \`2\`（10除以4余2）。两者**确实相等**。

第二重错误：\`N/4.0\` 中，除数为浮点数 \`4.0\`，被除数 \`N\` 的类型未声明。若 \`N\` 为整型（\`int\`），则发生整数与浮点数的混合运算，结果为 \`2.5\`；但若 \`N\` 不是整型（例如题目未明确类型），则对非整型使用 \`%\` 运算符将导致**编译错误**。

实际上，原题在 \`N=10\` 且假设 \`N\` 为整型的条件下，\`N/4\` 与 \`N%4\` 相等这一点**碰巧成立**，\`N/4.0\` 结果为 \`2.5\` 也**碰巧正确**。但由于题目未声明 \`N\` 的类型，严格来说无法保证 \`%\` 运算符的合法性。

更深层的分析：当 \`N=4\` 时，\`N/4=1\`，\`N%4=0\`，两者不相等。即便 \`N=10\` 时两个子结论碰巧都成立，题目也未要求判断这个条件，仅要求判断"当 N=10 时，两个结论都成立"这一复合命题。

**易错提醒：**
- 本题容易误以为只要算出 \`10/4=2.5\` 就认为结论正确，忽略了题目要求判断的是"两者相等 AND 2.5"这个复合命题。
- 更深层的陷阱：\`N%4\` 要求操作数为整数，若 \`N\` 为 \`float\` 或 \`double\` 类型则编译报错。

**知识延伸：**
取模运算符 \`%\` 只能用于整数类型（\`int\`、\`long\` 等），不能用于浮点数。这是 GESP 一级、二级高频考点。

**考点：** 整数除法 \`%\` 与 \`/\` 的关系、运算符对操作数类型的限制`,
    },
    {
      id: 18,
      type: 'judge',
      question: `定义 \`float N\` 后，语句 \`cin >> N; cout << int(float(N));\` 可以输入整数和浮点数，并把它们转成整数输出。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
逐句分析代码：

\`\`\`cpp
float N;                    // 定义浮点变量 N
cin >> N;                   // cin 支持提取浮点数输入，也支持整数输入（自动转换）
cout << int(float(N));      // float(N) 确保类型为 float，int(...) 截断小数部分输出
\`\`\`

1. \`cin >> N\`：\`cin\` 的流提取运算符 \`>>\` 对浮点型变量 \`N\` 的赋值具有类型适应性。输入整数（如 \`10\`）时，\`cin\` 自动将其转换为浮点 \`10.0\` 存入 \`N\`；输入浮点数（如 \`3.14\`）时，直接存入。
2. \`int(float(N))\`：表达式 \`float(N)\` 再次显式转换确保 \`N\` 为 \`float\` 类型，然后 \`int(...)\` 强制转换为整型，**直接截断**小数部分（不是四舍五入）。
   - 输入 \`10\` → 输出 \`10\`
   - 输入 \`3.14\` → 输出 \`3\`

因此，该语句确实可以输入整数和浮点数，并将它们转换为整数输出。

**易错提醒：**
- \`int(float(N))\` 中的 \`float(N)\` 是冗余的，但无害。如果写成 \`int(N)\` 而 \`N\` 本身已是 \`float\`，效果相同。
- \`int()\` 截断不是四舍五入，\`3.9\` 截断后得到 \`3\`。

**知识延伸：**
C++ 的隐式类型转换规则：较小整数类型可自动提升为较大类型赋值。显式类型转换（强制类型转换）的语法为 \`(type)value\` 或 \`type(value)\`。

**考点：** 显式类型转换（截断）、\`cin\` 输入流对整数和浮点数的自适应处理`,
    },
    {
      id: 19,
      type: 'judge',
      question: `当 \`N=5\` 时，\`printf("%d*2", N)\` 的输出是 \`10\`。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
分析 \`printf("%d*2", N)\` 的输出过程：

\`\`\`cpp
printf("%d*2", N);  // N = 5
\`\`\`

1. \`printf\` 是一个变参函数，第一个参数是格式字符串 \`"%d*2"\`。
2. \`printf\` 扫描格式字符串，遇到 \`%d\` 时，从参数列表中取下一个参数 \`N\` 的值 \`5\`，将其格式化为十进制整数 \`5\`。
3. 格式字符串中 \`*2\` 是**普通字符**，原样输出，不会被解释为乘法运算符。

因此，输出结果为字符串 \`5*2\`（五个、星号、二），而不是数字 \`10\`。

如果想要输出 \`10\`，正确的写法应为：
- \`printf("%d", N * 2);\` — 先计算乘法，再格式化输出
- \`printf("%d*2=%d", N, N * 2);\` — 输出完整表达式

**易错提醒：**
- 格式字符串中的文字是"字面量"，按原样打印，\`*2\` 不会触发任何运算。
- \`printf\` 与 \`cout\` 不同：\`cout << N*2\` 会计算表达式，\`printf\` 只按格式串规定的方式"填入"值。

**知识延伸：**
\`printf\` 是 C 语言风格函数，GESP 二级开始涉及。其格式说明符（\`%d\`、\`%f\`、\`%s\` 等）必须与后续参数类型严格匹配，否则输出乱码或未定义行为。

**考点：** \`printf\` 格式字符串的字面量输出规则、与 \`cout\` 表达式求值的本质区别`,
    },
    {
      id: 20,
      type: 'judge',
      question: `\`break\` 语句可以终止当前层次的 \`for\` 循环，也可以终止当前层次的 \`while\` 循环。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
\`break\` 语句是 C++ 中专门用于**立即终止**所在最内层结构的控制语句。它的作用范围包括：

1. **终止 \`for\` 循环：**
   \`\`\`cpp
   for (int i = 0; i < 10; i++) {
       if (i == 5) break;  // 当 i=5 时跳出循环，不再执行后续迭代
       cout << i << " ";   // 输出 0 1 2 3 4
   }
   \`\`\`

2. **终止 \`while\` 循环：**
   \`\`\`cpp
   int i = 0;
   while (i < 10) {
       if (i == 5) break;  // 当 i=5 时跳出循环
       cout << i << " ";   // 输出 0 1 2 3 4
       i++;
   }
   \`\`\`

两种循环均适用 \`break\`，作用完全一致：跳出当前层次的循环结构，继续执行循环体之后的语句。

**易错提醒：**
- \`break\` 只能跳出**当前（最内层）**的循环结构，对于嵌套循环（如两层 \`for\` 循环内层使用 \`break\`），只跳出内层，外层继续执行。
- \`break\` 不能用于单独跳出 \`if\` 语句（除非 \`if\` 在循环内部，\`break\` 跳出的是循环而非 \`if\`）。

**知识延伸：**
与 \`break\` 对应的还有 \`continue\`，后者跳过本次循环剩余语句直接进入下一次迭代。C++ 还有 \`goto\` 配合标签可跳出多层循环，但 GESP 一级不要求掌握。

**考点：** \`break\` 语句在循环结构（\`for\`/\`while\`）中的作用机制`,
    },
    {
      id: 21,
      type: 'judge',
      question: `\`continue\` 语句通常会和 \`if\` 语句配合使用。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
\`continue\` 语句的作用是提前结束本次循环的剩余迭代，直接跳转到循环条件的判定位置。如果 \`continue\` 不与 \`if\` 条件语句配合使用（即无条件执行），则其后的循环体语句将永远无法执行，导致逻辑错误。在实际编程中，\`continue\` 通常包装在一个 \`if\` 条件内，例如：\`if (x == 0) continue;\`，表示当满足某个特定条件时才跳过本次循环。

**易错提醒：**
- 不要混淆 \`continue\` 与 \`break\`：\`break\` 结束整个循环，\`continue\` 仅跳过本次迭代
- \`continue\` 只能用于循环结构（\`for\`、\`while\`、\`do-while\`），不能用于 \`switch\` 语句

**知识延伸：**
\`continue\` 与 \`break\` 是控制循环流程的两个基本语句，与 \`goto\` 语句（现已不推荐使用）共同构成跳转控制体系。

**考点：** 循环跳转语句的工程最佳实践`,
    },
    {
      id: 22,
      type: 'judge',
      question: `在 C++ 中，不可以把变量命名为 \`printf\`，因为它是关键字。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
在 C++ 标准中，\`printf\` 并不是语言级关键字，而是标准库 \`<cstdio>\`（C 语言风格为 \`<stdio.h>\`）中声明的一个库函数名称。关键字是 C++ 语言本身定义的保留字（如 \`int\`、\`if\`、\`for\`、\`while\`、\`return\` 等），这些标识符在任何情况下都不能用作变量名或函数名。而 \`printf\` 作为库函数名，仅在包含对应头文件后被引入当前作用域，如果不引入头文件，可以合法地将 \`printf\` 用作变量名或自定义函数名（虽然强烈不推荐这样做，因为会遮蔽标准库函数）。

**易错提醒：**
- C++ 关键字总数约 90 余个，可查阅 C++ 标准文档获得完整列表
- 故意将变量命名为 \`printf\` 虽然语法合法，但会导致无法使用标准库的 \`printf\` 功能，且严重降低代码可读性

**知识延伸：**
C++ 中的标识符命名有三层限制：关键字绝对禁止使用；同一作用域内不能与已有标识符重名；建议避免与标准库函数名冲突。违反前两条会导致编译错误，违反第三条会导致逻辑错误。

**考点：** 预定义函数名与系统关键字的本质区别`,
    },
    {
      id: 23,
      type: 'judge',
      question: `表达式 \`N += 8/4//2\` 等价于 \`N += 8/(4/2)\`。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
在 C++ 语言中，双斜杠 \`//\` 并不是运算符，而是单行注释的起始符号。当编译器解析表达式 \`N += 8/4//2\` 时，会将 \`//2\` 视为注释开始，导致注释内容延续到本行末尾（取决于后续上下文）。因此，\`//\` 并不参与运算，不能被解释为整除运算符 \`//\`（Python 等某些语言中存在），整个表达式的语法本身就是不合法的，会导致编译错误。将 \`//\` 误认为某种运算符是本题的干扰点。

**易错提醒：**
- C++ 中没有整除运算符 \`//\`，整除使用 \`/\` 运算符（当两个操作数均为整数时）
- 不要将 \`//\` 注释符号与任何数学运算关联

**知识延伸：**
C++ 的注释符号有两种：\`//\` 用于单行注释，\`/* */\` 用于多行注释。注释在编译阶段即被预处理器完全移除，不影响最终生成的机器码。

**考点：** 注释符号与假想运算符的区分`,
    },
    {
      id: 24,
      type: 'judge',
      question: `对于整型变量 \`N\`，表达式 \`!!N\` 的值总是与 \`N\` 本身相同。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
\`!\` 是逻辑非运算符，其运算规则为：将操作数转换为布尔值后取反——非零值转为 \`true\` 再取反得到 \`false\`（数值为 0），零转为 \`false\` 再取反得到 \`true\`（数值为 1）。连续使用两次逻辑非 \`!!N\` 相当于对 \`N\` 进行"布尔化"：非零值统一变为 \`1\`，零变为 \`1\`，即 \`!!N\` 的结果只能是 \`0\` 或 \`1\`。举例：\`N = 5\` 时，\`!!5\` = \`1\`，不等于 \`5\` 本身；\`N = -3\` 时，\`!!(-3)\` = \`1\`，不等于 \`-3\` 本身。

**易错提醒：**
- 仅有当 \`N = 0\` 时，\`!!0\` = \`0\`，才与 \`N\` 本身相同
- \`!!N\` 常被用于将任意非零值显式转换为布尔真（1），但这会丢失原值信息

**知识延伸：**
在 C++ 中，任何非零整数在逻辑表达式中都被视为 \`true\`，零被视为 \`false\`。布尔值在参与算术运算时会自动转换为整数（\`true\` → \`1\`，\`false\` → \`0\`）。

**考点：** 逻辑非运算符的布尔强制收敛特性`,
    },
    {
      id: 25,
      type: 'judge',
      question: `GESP 是对编程能力进行等级认证，同一级别考察的核心能力基本与具体语言无关。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
GESP（Graphical Programming Education Standard for Students，青少年编程能力等级标准）的认证体系设计遵循"编程思维为本、语言工具为末"的原则。同一级别考察的核心能力——如算术运算与表达式求值、\`if-else\` 分支结构、循环迭代逻辑、基础数据结构理解、问题分解与算法设计等——在 \`C++\`、\`Python\`、\`Scratch\` 等不同语言中本质上是相通的。GESP 根据级别不同只要求掌握对应语言的基础语法，不考查语言的专属高级特性。

**易错提醒：**
- GESP 不同语言（\`C++\`、\`Python\`、\`Scratch\`）的同一级别考试难度基本对标，而非绝对相同
- 语言选择不影响认证等级的有效性和含金量

**知识延伸：**
计算思维（Computational Thinking）作为编程教育的核心目标，包含分解、模式识别、抽象化、算法设计四个维度，这些能力与具体编程语言无关，是跨语言通用的编程素养。

**考点：** 计算思维的一致性与跨语言迁移能力`,
      tags: [LEVEL1_TAGS.basics],
    },
    {
      id: 26,
      type: 'programming',
      samples: [
        { input: `12\n59\n59\n10`, output: `13 0 9` },
      ],
      referenceCode: `#include <iostream>
using namespace std;

int main() {
    int h, m, s, k;
    cin >> h >> m >> s >> k;
    s += k;
    m += s / 60;
    s %= 60;
    h += m / 60;
    m %= 60;
    h %= 24;
    cout << h << " " << m << " " << s << "\\n";
    return 0;
}`,
      question: `
# [GESP202406 一级] 休息时间

## 题目描述

小杨在某个时刻开始学习，并决定在学习 $k$ 秒后开始休息。
已知开始学习时刻的时、分、秒，请输出开始休息时刻。

## 输入格式

前 3 行分别输入开始时刻的时 $h$、分 $m$、秒 $s$。
第 4 行输入学习时长 $k$。

## 输出格式

输出一行，包含 3 个整数，分别表示休息时刻的时、分、秒。
`,
      template: `#include <iostream>
using namespace std;

int main() {
    // 在此填写代码
    return 0;
}`,
      answer: '',
      score: 25,
      explanation: `
      **解析：**
      标准时间进位题：先加秒，再处理分和时的进位。
      `,
      tags: [LEVEL1_TAGS.basics, LEVEL1_TAGS.operator],
    },
    {
      id: 27,
      type: 'programming',
      samples: [
        { input: `8`, output: `Yes` },
        { input: `9`, output: `No` },
      ],
      referenceCode: `#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    bool ok = false;
    for (int i = 1; i * i * i <= n; i++) {
        if (i * i * i == n) {
            ok = true;
            break;
        }
    }
    if (ok) cout << "Yes\\n";
    else cout << "No\\n";
    return 0;
}`,
      question: `
# [GESP202406 一级] 立方数

## 题目描述

给定一个正整数 $n$，判断它是否是立方数。
如果存在正整数 $x$，使得 $x \\times x \\times x = n$，则称 $n$ 是立方数。

## 输入格式

输入一个正整数 $n$。

## 输出格式

如果 $n$ 是立方数，输出 \`Yes\`，否则输出 \`No\`。
`,
      template: `#include <iostream>
using namespace std;

int main() {
    // 在此填写代码
    return 0;
}`,
      answer: '',
      score: 25,
      explanation: `
      **解析：**
      枚举整数并判断是否存在 \`i*i*i == n\`。
      `,
      tags: [LEVEL1_TAGS.loop, LEVEL1_TAGS.condition],
    },
  ],
};
