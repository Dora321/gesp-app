import { LEVEL1_TAGS } from './shared.js';

export const paperData = {
  id: '2023-03-l1',
  title: '2023年03月 GESP C++ 一级认证真题',
  level: 1,
  year: 2023,
  month: 3,
  session: 1,
  timeLimit: 5400,
  source: {
    officialPdf: '',
    type: 'public-recovery',
  },
  confidence: {
    answer: 1.0,
    statement: 1.0,
  },
  questions: [
    // 1-15 单选题
    {
      id: 1,
      type: 'single',
      question: `中国计算机学会（CCF）在（ ）年推出了 GESP 认证考试？`,
      options: ['2021', '2022', '2023', '2024'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (2022)**

**核心解析：**
中国计算机学会（CCF）在经过长期筹备后，于2022年正式推出了GESP编程能力等级认证项目。该认证旨在为青少年编程学习者建立一套从零基础到衔接CSP-J/S的完整评价体系。2022年是GESP认证的启动年份，此后逐步在全国范围内推广实施。

**选项逐项分析：**
- **A (2021)**：❌ 错误。2021年GESP认证尚未启动，此时还在筹备阶段。
- **B (2022)**：✅ 正确。CCF在2022年正式推出GESP认证考试，这是本题的唯一正确选项。
- **C (2023)**：❌ 错误。2023年是GESP认证已经开始实施的年份，而非推出的时间节点。
- **D (2024)**：❌ 错误。2024年GESP已运营成熟，明显晚于推出时间。

**易错提醒：**
- 容易混淆"推出"与"推广"的时间节点，2022年是起点而非发展期。
- 部分考生可能因对GESP发展历程不熟悉而随意猜测年份。

**知识延伸：**
GESP认证体系分为多个等级，其中一级至四级主要面向青少年编程初学者，与CSP-J/S形成进阶衔接关系。

**考点：** GESP认证体系的发展沿革`,
            tags: ['计算机基础'],
        },
    {
      id: 2,
      type: 'single',
      question: `第 2 代计算机主要使用的电子元件是（ ）。`,
      options: ['晶体管', '电子管', '集成电路', '超大规模集成电路'],
      answer: 0,
      score: 2,
      explanation: `**答案：A (晶体管)**

**核心解析：**
计算机发展史按核心电子元件划分为四代：第一代使用电子管，第二代使用晶体管，第三代使用集成电路，第四代使用超大规模集成电路。晶体管的发明是第二代计算机的标志性特征，实现了体积缩小、可靠性提升和能耗降低。

**选项逐项分析：**
- **A (晶体管)**：✅ 正确。第二代计算机（20世纪50-60年代）以晶体管为核心元件，相比电子管大幅提升了计算效率和稳定性。
- **B (电子管)**：❌ 错误。电子管是第一代计算机的核心元件，体积大、功耗高、寿命短。
- **C (集成电路)**：❌ 错误。集成电路是第三代计算机的标志元件。
- **D (超大规模集成电路)**：❌ 错误。大规模及超大规模集成电路是第四代计算机的核心，现代个人电脑的CPU即采用此技术。

**易错提醒：**
- 需牢记四代计算机与核心元件的对应关系，按时间顺序排列：电子管→晶体管→集成电路→超大规模集成电路。
- 第二代与第三代的区分易混淆，需注意晶体管是个别元件，集成电路是多个元件的集成。

**知识延伸：**
晶体管的工作原理基于半导体材料的导电特性，这一发明者获得了诺贝尔物理学奖，也为现代微电子技术奠定了基础。

**考点：** 计算机硬件迭代与核心半导体元件演进`,
            tags: ['计算机基础'],
        },
    {
      id: 3,
      type: 'single',
      question: `512MB 等于（ ）。`,
      options: [
        '512 * 1024 * 1024 字节',
        '512 * 1024 字节',
        '512 * 1000 * 1000 字节',
        '512 字节'
      ],
      answer: 0,
      score: 2,
      explanation: `**答案：A (512 * 1024 * 1024 字节)**

**核心解析：**
在计算机存储系统中，换算遵循二进制进位规则：1KB = 1024字节，1MB = 1024KB，1GB = 1024MB。因此512MB的正确换算为：512 × 1024KB = 512 × 1024 × 1024字节。注意区分计算机内部采用的二进制换算（1024进位）与硬盘厂商标注容量采用的十进制换算（1000进位）。

**选项逐项分析：**
- **A (512 * 1024 * 1024 字节)**：✅ 正确。严格遵循二进制换算标准，512MB = 512 × 1024KB = 512 × 1024 × 1024B。
- **B (512 * 1024 字节)**：❌ 错误。只计算了从MB到KB的一步换算，遗漏了从KB到字节的另一关键步骤。
- **C (512 * 1000 * 1000 字节)**：❌ 错误。采用了十进制进位（1000进位），这是硬盘厂商标注容量的惯例，而非计算机内存的实际换算标准。
- **D (512 字节)**：❌ 错误。数量级严重错误，仅相当于半千字节，与512MB相差甚远。

**易错提醒：**
- 换算时需明确每一步的单位转换，不能遗漏任何环节。
- 区分"厂商标注容量"与"实际可用容量"的差异：购买500GB硬盘在系统中可能显示为约465GB。

**知识延伸：**
在C++编程中，整型溢出往往也与2的幂次相关，如int类型最大值通常为2³¹-1 = 2147483647。

**考点：** 计算机存储计量层级与二进制进位规则`,
            tags: ['计算机基础'],
        },
    {
      id: 4,
      type: 'single',
      question: `下列哪个不是 C++ 语言的关键字？`,
      options: ['double', 'using', 'longlong', 'namespace'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (longlong)**

**核心解析：**
C++语言的关键字必须严格遵守标准规定的拼写形式。在表示长长整型时，C++标准规定的关键字是\`long long\`（两个单词中间有空格），而不是\`longlong\`（没有空格）。\`longlong\`会被编译器视为一个普通的用户自定义标识符，而非数据类型关键字。

**选项逐项分析：**
- **A (double)**：❌ 错误。\`double\`是C++标准关键字，用于声明双精度浮点类型。
- **B (using)**：❌ 错误。\`using\`是C++标准关键字，用于命名空间引入或类型别名声明。
- **C (longlong)**：✅ 正确。这是本题要求找出的"不是关键字"的选项，因为C++中不存在名为\`longlong\`的关键字。
- **D (namespace)**：❌ 错误。\`namespace\`是C++标准关键字，用于定义命名空间。

**易错提醒：**
- C++关键字必须严格匹配标准拼写，"long long"与"longlong"在编译器眼中是完全不同的符号。
- 容易误以为只要包含关键字的部分字符就能凑成关键字，如\`Int\`、\`INT\`均不是C++关键字（应写作\`int\`）。

**知识延伸：**
C++关键字数量约为50余个，均为小写字母。常见关键字包括：\`int\`、\`float\`、\`double\`、\`char\`、\`if\`、\`else\`、\`for\`、\`while\`、\`return\`等。

**考点：** C++标准关键字的准确拼写与空白符敏感性`,
            tags: ['变量与标识符', '数据类型'],
        },
    {
      id: 5,
      type: 'single',
      question: `执行语句 int a = 0.6; 后，变量 a 的值为（ ）。`,
      options: ['0.6', '0', '1', '报错'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (0)**

**核心解析：**
当执行\`int a = 0.6;\`语句时，发生了浮点数到整数的隐式类型转换。C++对于此类转换采用"向零取整"（truncation）机制，即直接截断小数部分，保留整数部分。0.6被截断后仅保留整数部分0，因此变量a的最终值为0。

**选项逐项分析：**
- **A (0.6)**：❌ 错误。整型变量\`int\`无法存储小数部分，编译器会执行类型转换而非保留原值。
- **B (0)**：✅ 正确。浮点数0.6经过"向零取整"后，小数部分被完全丢弃，只保留整数部分0。
- **C (1)**：❌ 错误。这是数学上"四舍五入"的结果，但C++的整数转换采用截断而非四舍五入。
- **D (报错)**：❌ 错误。浮点数到整数的隐式转换是C++语法所允许的，不会产生编译错误或运行时异常。

**易错提醒：**
- 需注意"截断"与"四舍五入"的区别：C++截断是直接丢弃小数部分，不进行四舍五入判断。
- 负数转换时同样适用截断规则：-0.6转换为int后结果为0。

**知识延伸：**
若需精确的四舍五入效果，可使用数学函数或先加0.5再截断的技巧：\`int a = (int)(0.6 + 0.5);\`

**考点：** 跨类型赋值中的隐式类型转换与截断规则`,
            tags: ['数据类型'],
        },
    {
      id: 6,
      type: 'single',
      question: `符合 C++ 语法的标识符是（ ）。`,
      options: ['8th_exam', 'exam 8th', 'exam#8th', 'exam_8th'],
      answer: 3,
      score: 2,
      explanation: `**答案：D (exam_8th)**

**核心解析：**
C++标识符命名必须遵守以下规则：①必须以字母（A-Z/a-z）或下划线（_）开头，不能以数字开头；②后续字符可以是字母、数字或下划线；③不能使用C++关键字作为标识符；④不能包含空格或其他特殊符号。

**选项逐项分析：**
- **A (8th_exam)**：❌ 错误。标识符以数字"8"开头，违反了"首字符不能为数字"的规则。
- **B (exam 8th)**：❌ 错误。标识符中包含空格，C++编译器会将空格视为语法分隔符，导致"exam"和"8th"被识别为两个独立的符号。
- **C (exam#8th)**：❌ 错误。字符"#"不属于标识符合法字符集（字母、数字、下划线），是预处理器符号，不可用于普通标识符。
- **D (exam_8th)**：✅ 正确。以字母"e"开头，后续包含下划线和数字"8th"，完全符合标识符命名规则。

**易错提醒：**
- 下划线虽可作为首字符，但不建议以双下划线"__"开头，因为此类标识符通常被标准库保留。
- 标识符区分大小写：\`Exam\`、\`exam\`、\`EXAM\`是三个完全不同的标识符。

**知识延伸：**
C++标识符最长长度未在标准中明确限制，但编译器通常有实现限制。良好命名习惯包括：见名知意、使用驼峰命名法或下划线分隔法。

**考点：** C++标识符命名规则与合法字符集`,
            tags: ['变量与标识符'],
        },
    {
      id: 7,
      type: 'single',
      question: `不符合 C++ 语法的表达式是（ ）。`,
      options: ['a = 3+2;', 'a = (b = 3)+2;', 'int a = 3.5;', 'int a = 3.5 % 2;'],
      answer: 3,
      score: 2,
      explanation: `**答案：D (int a = 3.5 % 2;)**

**核心解析：**
取模运算符\`%\`是C++中的整数专用运算符，要求两个操作数均为整数类型。在\`int a = 3.5 % 2;\`中，左操作数\`3.5\`是浮点型常量，右操作数\`2\`是整型。C++不允许对浮点数使用取模运算，因为取模运算本质上是求余操作，其数学定义要求操作数为整数。此语句会导致编译错误。

**选项逐项分析：**
- **A (a = 3+2;)**：❌ 错误（指它符合语法）。整型变量的简单赋值和算术运算，完全合法。
- **B (a = (b = 3)+2;)**：❌ 错误（指它符合语法）。嵌套赋值表达式是合法的C++语法，括号内的赋值表达式返回赋值后的值。
- **C (int a = 3.5;)**：❌ 错误（指它符合语法）。浮点数隐式转换为整型时虽然会丢失小数部分，但这是语法允许的，会产生截断结果0。
- **D (int a = 3.5 % 2;)**：✅ 正确（指它不符合语法）。取模运算符\`%\`要求操作数均为整数类型，对浮点数使用取模会导致编译错误。

**易错提醒：**
- 不能将数学中的"取余"概念直接套用到浮点数上，C++的\`%\`运算符专门用于整数域。
- 如果需要对浮点数求余，应使用\`<cmath>\`库中的\`fmod\`函数。

**知识延伸：**
整型除法\`/\`在C++中也遵循整数运算规则，\`7/2\`的结果为3（截断），若需精确除法应使用浮点除法\`7.0/2.0\`。

**考点：** 取模运算符\`%\`的操作数类型限制`,
            tags: ['数据类型', '运算符'],
        },
    {
      id: 8,
      type: 'single',
      question: `表达式 15 % 4 * 10 / 2 的计算结果是（ ）。`,
      options: ['15', '1.5', '1', '0'],
      answer: 0,
      score: 2,
      explanation: `**答案：A (15)**

**核心解析：**
本题涉及算术运算符的优先级和结合性。取模\`%\`、乘法\`*\`、除法\`/\`属于同一优先级，且均遵循左结合性（从左向右运算）。因此表达式\`15 % 4 * 10 / 2\`按照左到右的顺序逐步计算：
①计算\`15 % 4\`：15除以4得商3余3，结果为3
②计算\`3 * 10\`：结果为30
③计算\`30 / 2\`：结果为15
最终答案为15。

**选项逐项分析：**
- **A (15)**：✅ 正确。按照左结合性逐步计算：15%4=3，3*10=30，30/2=15。
- **B (1.5)**：❌ 错误。可能误以为\`%\`优先级最高或进行了浮点运算，忽略了整型运算规则。
- **C (1)**：❌ 错误。可能是只计算了\`15 % 4 = 3\`后错误地除以3，或误以为\`3 * 10 / 2 = 1\`。
- **D (0)**：❌ 错误。可能是误将\`%\`理解为其他运算或优先级判断完全错误。

**易错提醒：**
- \`%\`、\`*\`、\`/\`三者优先级相同，必须从左向右依次计算，不能跳序或颠倒顺序。
- 容易忽略\`%\`的优先级与\`*\`/\`/\`相同，而误以为应先做乘除后取模。

**知识延伸：**
在C++中，算术运算符优先级从高到低为：括号 > 正负号 > 乘除取模 > 加减。如需改变运算顺序，必须使用括号。

**考点：** 算术运算符的优先级与左结合性`,
            tags: ['运算符'],
        },
    {
      id: 9,
      type: 'single',
      question: `表达式 (5 && 2) 的计算结果是（ ）。`,
      options: ['true', 'false', '0', '1'],
      answer: 3, // C++ 中任何非零值作为布尔判断均为真。
      score: 2,
      explanation: `**答案：D (1)**

**核心解析：**
C++中的逻辑运算符\`&&\`用于执行逻辑与运算。在C++的逻辑表达式中，所有非零值都被视为"真"（true），零被视为"假"（false）。对于表达式\`(5 && 2)\`：操作数5和2均为非零值，因此都是真值。逻辑与运算\`&&\`要求两个操作数都为真时结果才为真，故\`5 && 2\`的结果为真。在C++中，布尔类型的结果在参与整数运算或按整数输出时，true被转换为1，false被转换为0。

**选项逐项分析：**
- **A (true)**：❌ 错误。虽然逻辑与运算结果在布尔语义上为真，但题目要求的是数值结果，C++会将其转换为整数。
- **B (false)**：❌ 错误。两个非零值进行逻辑与不可能得到假值，除非其中一个操作数为零。
- **C (0)**：❌ 错误。逻辑与的真值不可能对应整数0，0对应的是false。
- **D (1)**：✅ 正确。5和2均为非零真值，逻辑与结果为真，C++将真值转换为整数1输出。

**易错提醒：**
- 需区分布尔值\`true\`/\`false\`与整数\`1\`/\`0\`的关系：两者在C++中可以隐式转换，但含义不同。
- 逻辑与的运算规则：全真为真，一假则假；不能将\`&&\`与按位与\`&\`混淆。

**知识延伸：**
C++中的短路求值（short-circuit evaluation）特性：\`a && b\`当a为假时不再计算b，直接返回假。\`a || b\`当a为真时不再计算b，直接返回真。

**考点：** 逻辑运算符真值判断与布尔-整数类型转换`,
            tags: ['运算符'],
        },
    {
      id: 10,
      type: 'single',
      question: `执行语句 (a = 2) && (b = 0) 后，a 和 b 的值分别为（ ）。`,
      options: ['a = 2, b = 2', 'a = 0, b = 0', 'a = 1, b = 0', 'a = 2, b = 0'],
      answer: 3,
      score: 2,
      explanation: `**答案：D (a = 2, b = 0)**

**核心解析：**
本题考察赋值表达式的返回值和逻辑与运算符的短路求值特性。分析过程如下：
①执行\`(a = 2)\`：变量a被赋值为2，赋值表达式本身的值也是2（非零）。
②由于左侧表达式值为真（非零），逻辑与\`&&\`需要继续计算右侧表达式。
③执行\`(b = 0)\`：变量b被赋值为0，赋值表达式本身的值也是0（假）。
④整个表达式的最终逻辑值为假，但两个赋值操作的副作用已经完成，变量a的值为2，变量b的值为0。

**选项逐项分析：**
- **A (a = 2, b = 2)**：❌ 错误。变量b被赋值为0而非2，未能正确追踪b的赋值结果。
- **B (a = 0, b = 0)**：❌ 错误。变量a被赋值为2而非0，错误地以为a的赋值会受后续逻辑影响。
- **C (a = 1, b = 0)**：❌ 错误。变量a被赋值为2，误以为赋值表达式会返回1。
- **D (a = 2, b = 0)**：✅ 正确。两个赋值表达式均被执行，a获得值2，b获得值0。

**易错提醒：**
- 赋值表达式本身有返回值（被赋的值），这个返回值会参与后续运算。
- 逻辑与运算符\`&&\`的短路求值：若左操作数为假，则右操作数不计算；但本题左操作数为真，故右操作数仍被执行。
- 不能将赋值运算符\`=\`与比较运算符\`==\`混淆。

**知识延伸：**
赋值表达式可以链式使用，如\`a = b = c = 5\`，从右向左依次赋值。此外，赋值表达式还可以作为更复杂表达式的一部分，如\`if((n = readValue()) > 0)\`。

**考点：** 赋值表达式的返回值特性与逻辑运算符的短路求值`,
            tags: ['运算符'],
        },
    {
      id: 11,
      type: 'single',
      question: `判断变量 a 是否为偶数的正确表达式是（ ）。`,
      options: ['a / 2 == 0', 'a % 2 == 0', 'a % 2 = 0', 'a / 2 = 0'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (a % 2 == 0)**

**核心解析：**
偶数的本质定义是：能被 2 整除的整数，即除以 2 的余数为 0。在 C++ 中，取模运算符 \`%\` 用于求两个整数相除的余数。因此，判断变量 a 是否为偶数的正确表达式是 \`a % 2 == 0\`（a 除以 2 的余数等于 0）。

**选项逐项分析：**
- **A (a / 2 == 0)**：❌ 错误。此表达式判断的是 a 除以 2 的商是否等于 0。只有当 a 的绝对值小于 2 时（如 0、1、-1）才成立，无法判断一般整数的奇偶性。
- **B (a % 2 == 0)**：✅ 正确。取模运算 \`a % 2\` 得到 a 除以 2 的余数，余数为 0 表示 a 能被 2 整除，即 a 为偶数。
- **C (a % 2 = 0)**：❌ 错误。单等号 \`=\` 是赋值运算符，不能用于比较运算，会导致编译错误（或将 0 赋值给 \`a % 2\` 的结果）。
- **D (a / 2 = 0)**：❌ 错误。同样使用了赋值运算符 \`=\`，且整除判断逻辑也不正确。

**易错提醒：**
- 判断相等必须使用双等号 \`==\`，单等号 \`=\` 是赋值，会改变变量值或导致编译错误。
- 不能用整除 \`a / 2\` 来判断奇偶，因为整除只对绝对值很小的数有正确结果。

**知识延伸：**
取模运算 \`%\` 在 GESP 一级中常用于奇偶判断、周期性问题、循环队列等场景，是高频考点。

**考点：** 偶数判定的取模原理与相等比较运算符`,
            tags: ['运算符'],
        },
    {
      id: 12,
      type: 'single',
      question: `判断三条边 a, b, c 能否构成三角形的逻辑表达式是（ ）。`,
      options: [
        'a+b>c || a+c>b || b+c>a',
        'a+b<c && a+c<b && b+c<a',
        'a+b>c && a+c>b && b+c>a',
        'a+b==c'
      ],
      answer: 2,
      score: 2,
      explanation: `**答案：C (a+b>c && a+c>b && b+c>a)**

**核心解析：**
根据三角形构成的基本性质（三角形不等式）：任意两边之和必须大于第三边。这是欧几里得几何中的充要条件。三个条件必须**同时满足**，因此必须用逻辑与 \`&&\` 连接：
1. a + b > c（两边 a、b 之和大于第三边 c）
2. a + c > b（两边 a、c 之和大于第三边 b）
3. b + c > a（两边 b、c 之和大于第三边 a）

**选项逐项分析：**
- **A (a+b>c || a+c>b || b+c>a)**：❌ 错误。使用逻辑或 \`||\` 意味着只需满足其中任意一个条件即可。极端情况下，a=1、b=2、c=10 会满足 b+c>a（2+10>1），但显然无法构成三角形。
- **B (a+b<c && a+c<b && b+c<a)**：❌ 错误。条件完全反向，变成了任意两边之和小于第三边，这恰恰是三角形不成立的判定。
- **C (a+b>c && a+c>b && b+c>a)**：✅ 正确。三个不等式同时成立，确保三条边满足三角形不等式。
- **D (a+b==c)**：❌ 错误。只判断 a+b 是否等于 c，若相等则三条边恰好在一条直线上，不能构成三角形。

**易错提醒：**
- 必须使用 \`&&\`（逻辑与），不能用 \`||\`（逻辑或）。
- 必须同时检查三组组合，缺一不可。
- 不能用 \`>=\`，必须用严格大于 \`>\`。

**知识延伸：**
三角形不等式是一级几何与逻辑运算结合的经典题型，后续还会出现判断等腰三角形、直角三角形等扩展问题。

**考点：** 三角形不等式与逻辑运算符的综合应用`,
            tags: ['运算符'],
        },
    {
      id: 13,
      type: 'single',
      question: `执行下面 C++ 语句后，输出结果为（ ）。\n\n\`\`\`cpp\nprintf("5%%2={%d}\\n", 5 % 2);\n\`\`\``,
      options: ['5%2={1}', '5%%2={1}', '5%2={%d}', '5%%2={%d}'],
      answer: 0,
      score: 2,
      explanation: `**答案：A (5%2={1})**

**核心解析：**
\`printf\` 的格式字符串中，\`%%\` 表示输出一个普通的百分号 \`%\`，\`%d\` 会被后面的整数参数替换。

本题中：
- \`"5%%2="\` 输出为 \`5%2=\`
- \`5 % 2\` 的结果是 \`1\`
- \`%d\` 被替换为 \`1\`

所以最终输出为 \`5%2={1}\`。

**易错提醒：**
\`%%\` 只用于 \`printf\` 的格式字符串里表示输出百分号；真正的取模运算符仍然是单个 \`%\`。`,
            tags: ['输入输出', '运算符'],
        },
    {
      id: 14,
      type: 'single',
      question: `代码执行后的输出是 ( )。\n\`\`\`cpp\nint a = 0;\nfor (int i = 1; i <= 4; i++)\n a = a+i;\ncout << a;\n\`\`\``,
      options: ['4', '10', '15', '0'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (10)**

**核心解析：**
逐步追踪循环执行过程：
1. 初始状态：\`a = 0\`
2. 循环条件：\`i\` 从 1 到 4，每次执行 \`a = a + i\`
   - 第 1 次：\`i=1\`，\`a = 0 + 1 = 1\`
   - 第 2 次：\`i=2\`，\`a = 1 + 2 = 3\`
   - 第 3 次：\`i=3\`，\`a = 3 + 3 = 6\`
   - 第 4 次：\`i=4\`，\`a = 6 + 4 = 10\`
3. 循环结束后，\`i=5\` 不满足 \`i <= 4\`，循环结束
4. 输出 \`a\` 的值为 **10**

**选项逐项分析：**
- **A (4)**：❌ 错误。这是只取最后一次 i 的值（i=4），忽略了累加过程。
- **B (10)**：✅ 正确。1+2+3+4 的累加结果。
- **C (15)**：❌ 错误。这可能是计算了 1+2+3+4+5=15，但循环只到 i=4。
- **D (0)**：❌ 错误。这是忽略了循环累加，直接取初始值。

**易错提醒：**
- 注意循环变量的取值范围：\`i <= 4\` 意味着 i 取值为 1、2、3、4，不包括 5。
- 看清循环体是 \`a = a + i\`（累加），不是 \`a = i\`。

**知识延伸：**
等差数列求和公式为 \`n*(首项+末项)/2\`，对于 1+2+3+4，公式验证：4*(1+4)/2 = 10，与循环结果一致。

**考点：** for 循环的累加逻辑与变量状态追踪`,
            tags: ['循环', '输入输出'],
        },
    {
      id: 15,
      type: 'single',
      question: `代码执行后的输出是 ( )。\n\`\`\`cpp\nint a = 5, b = 10;\nif (a > b) a = a+b;\nelse a = b-a;\ncout << a;\n\`\`\``,
      options: ['15', '10', '5', '0'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (5)**

**核心解析：**
1. 初始值：\`a = 5\`，\`b = 10\`
2. 判断条件：\`a > b\` 即 \`5 > 10\`，结果为 **false（假）**
3. 执行 else 分支：\`a = b - a\` → \`a = 10 - 5 = 5\`
4. 输出 \`a\` 的值为 **5**

**选项逐项分析：**
- **A (15)**：❌ 错误。这可能是将 a 和 b 相加（5+10=15），混淆了 if 和 else 的执行逻辑。
- **B (10)**：❌ 错误。这可能只是取了 b 的值，忽略了 else 分支中的减法运算。
- **C (5)**：✅ 正确。执行 else 分支 \`a = b - a = 10 - 5 = 5\`。
- **D (0)**：❌ 错误。这可能是将 a 减去 b（5-10=-5 或 10-5=5 再减 5=0），或对条件判断理解有误。

**易错提醒：**
- if 条件为真时执行 if 分支，条件为假时执行 else 分支。
- else 分支的表达式是 \`b - a\`，不是 \`a - b\`。

**知识延伸：**
if-else 是条件分支结构的基础，条件判断遵循短路求值规则。在二级考试中还会出现嵌套 if、多重 else-if 等复杂结构。

**考点：** if-else 条件分支的执行流程与关系运算`,
            tags: ['条件判断', '输入输出'],
        },

    // 16-25 判断题
    {
      id: 16,
      type: 'judge',
      question: `在计算机存储单位中，1KB 等于 1000 字节。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
在计算机存储单位中，1KB（千字节）等于 **1024 字节**，而非 1000 字节。

这是因为计算机采用二进制存储，容量单位以 2 的幂次为基础：
- 1KB = 2¹⁰ 字节 = 1024 字节
- 1MB = 2²⁰ 字节 = 1024KB = 1,048,576 字节
- 1GB = 2³⁰ 字节 = 1024MB

命题声称 1KB 等于 1000 字节，这是 SI 制（国际单位制）的十进制换算规则（如 1kg=1000g），与计算机存储的二进制换算规则不符。

**易错提醒：**
- 计算机存储容量使用二进制换算（1024 为基准）。
- 硬盘、固态硬盘等厂商可能使用十进制标注（1KB=1000B），但操作系统显示时通常换算为二进制。
- 网络传输速率常使用十进制（如 1Mbps = 1000bps）。

**知识延伸：**
IEC（国际电工委员会）为消除歧义，引入了 KiB、MiB、GiB 等二进制前缀，但 GESP 一级仍使用传统的 KB、MB、GB 表示 1024 换算。

**考点：** 计算机存储单位的二进制换算规则（1KB=1024B）`,
            tags: ['计算机基础'],
        },
    {
      id: 17,
      type: 'judge',
      question: `编译器的主要作用是将高级语言编写的源代码翻译成目标代码。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
编译器（Compiler）是计算机程序开发的核心工具，其主要功能是将用高级语言（如 C++、Java、Python）编写的源代码（Source Code）翻译成目标代码（Target Code，通常是机器语言或汇编语言）。

这个翻译过程称为"编译"，通常包括以下阶段：
1. 词法分析：将源代码分解为token
2. 语法分析：检查语法结构
3. 语义分析：检查类型、作用域等
4. 优化与代码生成：生成目标代码

编译完成后，目标代码可以直接在计算机上运行（静态编译），或者需要进一步解释执行。

命题"编译器的主要作用是将高级语言编写的源代码翻译成目标代码"准确描述了编译器的核心功能，表述正确。

**易错提醒：**
- 解释器（如 Python 解释器）是逐行解释执行源代码，不生成独立的目标代码文件。
- C++ 属于编译型语言，源代码经过编译链接后生成可执行文件。

**知识延伸：**
GESP 二级会涉及集成开发环境（IDE）的概念，IDE 通常整合了编辑器、编译器、调试器等功能。

**考点：** 编译器的基本功能与高级语言到机器语言的转换`,
            tags: ['计算机基础'],
        },
    {
      id: 18,
      type: 'judge',
      question: `C++ 语言中，变量名不区分字母的大小写。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
C++ 语言是一种**大小写敏感（Case-Sensitive）**的语言。这意味着标识符（如变量名、函数名、类名）中的字母大小写被视为不同的字符。

具体示例：
- \`int a = 5;\` 和 \`int A = 10;\` 是两个完全不同的变量
- \`abc\`、\`Abc\`、\`ABC\` 是三个不同的标识符
- \`cout\` 不能写成 \`Cout\` 或 \`COUT\`，否则会导致编译错误
- \`main\` 函数名必须是全小写的 \`main\`，不能写成 \`Main\`

命题声称"变量名不区分字母的大小写"，这与 C++ 的实际规则相悖，故判为错误。

**易错提醒：**
- 初学者常混淆大小写，尤其是使用不习惯的大小写命名风格时。
- C++ 标准库中的标识符几乎全部使用小写（如 \`cout\`、\`cin\`、\`endl\`、\`string\`）。
- 自定义标识符虽然可以混合大小写，但必须保持一致。

**知识延伸：**
不同编程语言的大小写敏感性不同：Python、Java、C++ 是大小写敏感的；而 HTML、SQL（部分实现）对大小写不敏感。

**考点：** C++ 标识符的大小写敏感性规则`,
            tags: ['变量与标识符'],
        },
    {
      id: 19,
      type: 'judge',
      question: `C++ 数据类型中，char 类型通常占用 1 个字节的内存空间。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
根据 C++ 标准的规定，\`char\`（字符型）数据类型被定义为占用 **1 个字节**（Byte）的存储空间，即 8 个二进制位（bits）。

\`char\` 类型主要用于存储单个字符，对应 ASCII 码表中的 128 个标准字符（0-127）。虽然某些扩展字符集可能使用多字节编码（如 UTF-8 中的一些中文字符占 3-4 字节），但单个 \`char\` 变量在 C++ 中的标准大小始终是 1 字节。

C++ 中常见数据类型的典型大小：
- \`char\`：1 字节
- \`int\`：通常 4 字节（与平台相关）
- \`float\`：通常 4 字节
- \`double\`：通常 8 字节

命题描述准确，故判为正确。

**易错提醒：**
- \`char\` 类型存储的是字符的 ASCII 码值（整数），可以直接参与数值运算。
- 字符字面量用单引号，如 \`'A'\`、\`'5'\`。

**知识延伸：**
C++ 标准只保证 \`char\` 的大小为 1 字节，其他类型的大小由实现决定（但给出了最小值要求）。

**考点：** C++ 基本数据类型的内存占用与 char 类型定义`,
            tags: ['字符与ASCII', '计算机基础'],
        },
    {
      id: 20,
      type: 'judge',
      question: `在 C++ 中，执行语句 cout << 3 / 2; 的输出结果是 1.5。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
在 C++ 中，当两个操作数都是整型（int）时，\`/\` 运算符执行的是**整数除法（Integer Division）**，结果会**自动截断小数部分**，只保留整数部分。

分析代码 \`cout << 3 / 2;\`：
1. 操作数类型：3 和 2 都是整数字面量
2. 运算过程：3 ÷ 2 = 1 余 1
3. 结果：整数除法只保留商，丢弃余数，结果为 **1**

命题声称输出结果是 **1.5**，这是将浮点数除法的规则（3.0/2.0=1.5）错误地套用到了整数除法上。

**易错提醒：**
- 如果希望得到精确的小数结果，至少有一个操作数必须是浮点类型：\`3.0 / 2\` 或 \`3 / 2.0\` 或 \`3.0 / 2.0\` 的结果才是 1.5。
- 类型提升规则：混合运算中，较小的整数类型会自动提升为较大的类型。

**知识延伸：**
取模运算符 \`%\` 专门用于获取整数除法的余数：
- \`3 / 2 = 1\`（商）
- \`3 % 2 = 1\`（余数）

**考点：** C++ 整数除法的截断特性与浮点除法的区分`,
            tags: ['输入输出', '运算符'],
        },
    {
      id: 21,
      type: 'judge',
      question: `if 语句中的条件表达式必须是布尔类型（bool）。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
在 C++ 中，\`if\` 语句的条件表达式**并不强制要求必须是布尔类型（bool）**。

C++ 允许任何可以隐式转换为布尔值的表达式作为 if 语句的条件，转换规则如下：
- **整数值**：非零值为 true，零值为 false
- **指针类型**：非空指针为 true，空指针为 false
- **浮点类型**：非零值为 true，零值为 false

合法示例：
\`\`\`cpp
if (5)        // 合法，5 转换为 true
if (a)        // 合法，假设 a 是非零整数
if (ptr)      // 合法，ptr 是非空指针
\`\`\`

命题声称"必须是布尔类型"，这种说法过于绝对，忽略了 C++ 的隐式类型转换机制，故判为错误。

**易错提醒：**
- C++ 中 \`if (a == 5)\` 的比较表达式本身结果就是 bool 类型。
- 但 \`if (a)\` 中 a 是 int 类型，会隐式转换为 bool。

**知识延伸：**
C++11 引入了 \`nullptr\` 关键字，建议使用 \`if (ptr != nullptr)\` 替代 \`if (ptr)\` 来提高代码可读性。

**考点：** C++ 条件表达式的隐式布尔转换规则`,
            tags: ['条件判断', '数据类型'],
        },
    {
      id: 22,
      type: 'judge',
      question: `for 循环的三个参数（初始化、条件、迭代）都可以省略。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
\`for\` 循环的语法结构为：
\`\`\`cpp
for (初始化表达式; 条件表达式; 迭代表达式)
    语句;
\`\`\`

根据 C++ 语法，三个组成部分在技术层面都是**可选的（Optional）**：
1. **初始化部分**：可以省略，将初始化语句放在循环体外
2. **条件部分**：可以省略，默认为 true（死循环，需配合 break 使用）
3. **迭代表达式**：可以省略，将更新逻辑放在循环体内

极端示例 \`for (;;)\`：
- 省略所有三个部分
- 条件部分默认为 true
- 这是一个合法的死循环，等价于 \`while (true)\`

命题描述准确，故判为正确。

**易错提醒：**
- 省略条件部分时，循环不会自动终止，必须在循环体内使用 \`break\` 语句。
- 省略初始化部分时，循环变量需要在循环前定义。

**知识延伸：**
GESP 二级会涉及死循环的正确退出方式（如配合 break 或条件变量），以及 for 循环与 while 循环的等价转换。

**考点：** for 循环语法的三个可选组成部分`,
      tags: [LEVEL1_TAGS.loop]
    },
    {
      id: 23,
      type: 'judge',
      question: `break 语句只能用于跳出当前的循环结构（或 switch 语句）。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
\`break\` 语句在 C++ 中的作用是**立即终止当前最内层的循环结构**（for、while、do-while）或 **switch 语句**，并跳出该结构，执行其后的代码。

\`break\` 语句的作用范围严格限定于：
1. **循环结构**：for 循环、while 循环、do-while 循环
2. **switch 语句**：用于终止 case 分支的执行

\`break\` 不能用于：
- 终止 if 语句（if 不是循环或 switch）
- 跳出多重循环的某一层（只会跳出最内层）
- 跳出函数（需使用 return 语句）

命题"break 语句只能用于跳出当前的循环结构（或 switch 语句）"准确描述了 break 的作用范围，故判为正确。

**易错提醒：**
- \`break\` 只能跳出**一层**循环，在嵌套循环中需要多次使用或配合其他控制手段。
- \`continue\` 语句的作用是跳过本次循环的剩余部分，直接进入下一次迭代，而非终止整个循环。

**知识延伸：**
在二级考试中，嵌套循环中的 break 行为是常见考点，理解其"只跳出最内层"的特性至关重要。

**考点：** break 语句的作用范围与限制`,
            tags: ['循环', '条件判断'],
        },
    {
      id: 24,
      type: 'judge',
      question: `逻辑表达式 (5 > 2) || (3 < 1) 的值为 true。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
分析逻辑表达式 \`(5 > 2) || (3 < 1)\`：

1. **左操作数** \`(5 > 2)\`：关系运算结果为 **true**（5 确实大于 2）
2. **右操作数** \`(3 < 1)\`：关系运算结果为 **false**（3 不小于 1）
3. **逻辑或运算** \`||\`：只要一边为 true，结果就为 true

由于 \`(5 > 2)\` 已经为 true，根据逻辑或的**短路求值**特性：
- 左操作数为 true 时，右操作数不会被求值
- 整个表达式的结果确定为 **true**

命题"逻辑表达式 (5 > 2) || (3 < 1) 的值为 true"判断正确。

**易错提醒：**
- 逻辑或 \`||\` 只需要**至少一个**操作数为 true，结果就为 true。
- 逻辑与 \`&&\` 需要**所有**操作数都为 true，结果才为 true。
- 不要混淆 \`||\`（或）和 \`&&\`（且）的用法。

**知识延伸：**
在 GESP 二级中，短路求值特性会涉及更复杂的表达式，如 \`(a != 0 && b / a > 1)\` 这类利用短路避免除零错误的技巧。

**考点：** 逻辑运算符 \`||\`（或）的求值规则与短路特性`,
            tags: ['运算符'],
        },
    {
      id: 25,
      type: 'judge',
      question: `C++ 程序必须包含 iostream 头文件才能运行。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
\`iostream\` 是 C++ 标准库中用于输入/输出操作的**头文件**（header file）。它提供了 \`cin\`、\`cout\`、\`cerr\` 等标准流对象及相关类型和函数。

但是，C++ 程序**并不强制要求**包含 \`iostream\` 头文件才能运行，原因如下：

1. **头文件是编译时概念**：头文件的内容会在预处理阶段被展开到源文件中，不影响程序能否运行，只影响编译是否成功。
2. **不依赖 iostream 的程序**：使用 C 风格 I/O（\`printf\`、\`scanf\`）或仅进行计算、无 I/O 操作的程序无需包含 \`iostream\`。
3. **其他 I/O 方式**：程序可以通过文件 I/O、网络通信等方式进行数据交互。

命题"C++ 程序必须包含 iostream 头文件才能运行"过于绝对，忽略了 C++ 程序的多样性和其他 I/O 手段，故判为错误。

**易错提醒：**
- 如果使用 \`cout\`/\`cin\`，必须包含 \`<iostream>\` 并使用 \`using namespace std;\` 或 \`std::cout\`。
- C++ 程序可以完全不使用 iostream，例如纯计算程序或使用 C I/O 的程序。

**知识延伸：**
C++ 有多种 I/O 方式：
- \`<iostream>\`：面向对象的流 I/O
- \`<cstdio>\`：C 风格的 printf/scanf
- 文件 I/O：\`fstream\` 头文件

**考点：** C++ 头文件的可选性与 iostream 的作用`,
            tags: ['计算机基础'],
        },

    // 26-27 编程题
    {
      id: 26,
      type: 'programming',
      samples: [
        { input: `4`, output: `2` },
        { input: `6`, output: `2` }
      ],
      referenceCode: `#include <iostream>\nusing namespace std;\nint main() {\n    int n, count = 0;\n    cin >> n;\n    for (int i = 1; i * i <= n; i++) {\n        if (n % i == 0) {\n            count++;\n        }\n    }\n    cout << count << endl;\n    return 0;\n}`,
      question: `
# [GESP202303 一级] 长方形面积

## 题目描述

小明刚刚学习了如何计算长方形面积。他发现，如果一个长方形的长和宽都是整数，它的面积一定也是整数。现在，小明想知道如果给定长方形的面积，有多少种可能的长方形，满足长和宽都是整数？如果两个长方形的长相等、宽也相等，则认为是同一种长方形。约定长方形的长大于等于宽。正方形是长方形的特例，即长方形的长和宽可以相等。

## 输入格式

输入一行，包含一个整数 $A$，表示长方形的面积。约定 $2 \\leq A \\leq 1000$。

## 输出格式

输出一行，包含一个整数 $C$，表示有 $C$ 种可能的长方形。
`,
      template: `#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    // 在此填写代码\n    return 0;\n}`,

      score: 25,
      explanation: `
      **解析：**
      枚举宽 $i$ 从 1 到 $\\sqrt{N}$，如果 $N$ 能被 $i$ 整除，则对应一种长和宽都为整数的方案。LuoGu B3834。
      `,
      tags: [LEVEL1_TAGS.basics, LEVEL1_TAGS.operator, LEVEL1_TAGS.loop],
    },
    {
      id: 27,
      type: 'programming',
      samples: [
        { input: `2022 1`, output: `31` },
        { input: `2020 2`, output: `29` }
      ],
      referenceCode: `#include <iostream>\nusing namespace std;\nint main() {\n    int y, m;\n    cin >> y >> m;\n    if (m == 1 || m == 3 || m == 5 || m == 7 || m == 8 || m == 10 || m == 12) {\n        cout << 31 << endl;\n    } else if (m == 4 || m == 6 || m == 9 || m == 11) {\n        cout << 30 << endl;\n    } else {\n        if ((y % 4 == 0 && y % 100 != 0) || (y % 400 == 0)) {\n            cout << 29 << endl;\n        } else {\n            cout << 28 << endl;\n        }\n    }\n    return 0;\n}`,
      question: `
# [GESP202303 一级] 每月天数

## 题目描述

小明刚刚学习了每月有多少天，以及如何判断平年和闰年，想到可以使用编程方法求出给定的月份有多少天。你能做到吗？

## 输入格式

输入一行，包含两个整数 $A, B$，分别表示一个日期的年、月。约定 $2000 \\leq A \\leq 3000$，$1 \\leq B \\leq 12$。

## 输出格式

输出一行，包含一个整数，表示输入月份有多少天。
`,
      template: `#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    // 在此填写代码\n    return 0;\n}`,

      score: 25,
      explanation: `
      **解析：**
      考查条件判断与逻辑运算。需要特殊处理 2 月并判断闰年。LuoGu B3835。
      `,
      tags: [LEVEL1_TAGS.basics, LEVEL1_TAGS.condition],
    }
  ]
};
