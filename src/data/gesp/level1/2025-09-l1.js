import { LEVEL1_TAGS } from './shared.js';

export const paperData = {
  id: '2025-09-l1',
  title: '2025年09月 GESP C++ 一级认证真题',
  level: 1,
  year: 2025,
  month: 9,
  session: 11,
  timeLimit: 5400,
  source: {
    officialPdf: '',
    type: 'public-recovery',
  },
  confidence: {
    answer: 1.0,
    statement: 1.0,
  },
  questions: [
    {
      id: 1,
      type: 'single',
      question: `人工智能现在非常火，小杨就想多了解一下，其中就经常听人提到"大模型"。那么请问这里说的"大模型"最贴切意指( )。`,
      options: ['大电脑模型', '大规模智能', '智能单位', '大语言模型'],
      answer: 3,
      score: 2,
      explanation: `**答案：D (大语言模型)**

**核心解析：**
"大模型"是当前人工智能领域最核心的概念之一，其英文全称为 **Large Language Model (LLM)**。"大"字指代的并非物理硬件的体积，而是指模型参数的规模——现代大语言模型通常拥有数百亿至万亿级别的参数量，通过海量文本数据的预训练获得强大的语言理解和生成能力。题目中"最贴切"的表述要求选出一个在技术界约定俗成的专有名词，而非泛化的形容词或误解的俗称。

**选项逐项分析：**
- **A (大电脑模型)**：❌ 错误。该项本身错误。虽然大模型需要强大的算力支撑，但其"大"指的是模型内部权重参数的规模，而非外部物理计算机的尺寸。将"大"理解为硬件体积是对该术语的严重曲解。
- **B (大规模智能)**：❌ 错误。该项本身描述了部分特征，但并非计算机科学界对"大模型"的正式或约定称谓。"大规模智能"过于抽象宽泛，无法精确对应任何具体的技术实现。
- **C (智能单位)**：❌ 错误。该项本身错误。AI 领域目前没有统一的"智能单位"度量体系，大模型是一类具体的技术架构，而非衡量智能的标准单位。
- **D (大语言模型)**：✅ 正确。"大模型"正是 LLM (Large Language Model) 在中文语境中的简称，特指基于 Transformer 架构、采用大规模语料预训练的大语言模型，这是当前 AIGC 技术的核心基础。

**易错提醒：**
- 大模型的"大"不等同于计算机硬件体积大，切勿望文生义理解为"大电脑"或"大规模硬件"。
- 需区分日常口头简称与正式技术术语，"大规模智能"虽描述了部分特征但并非精准的学术名称。

**知识延伸：**
GESP 二级及以上可能会涉及机器学习基础概念，理解 LLM 的本质是"大规模参数 + 大规模语料 + Transformer 架构"的组合，有助于后续学习神经网络的层级结构。

**考点：** 现代人工智能 (AI) 领域核心术语"大模型"(LLM) 的定义辨析`,
      tags: [LEVEL1_TAGS.basics]
    },
    {
      id: 2,
      type: 'single',
      question: `小杨这学期刚开学就选修了一门编程课程，然后就想编写程序来计算1到10001之间的所有偶数的和。他希望程序用简单易懂且执行效率高，请问下列关于实现该程序时采用的主要控制结构哪个说法最不合适 ( )。`,
      options: ['循环结构', '循环和分支结合', '仅使用顺序结构', '不使用分支结构'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (仅使用顺序结构)**

**核心解析：**
本题要求计算 1 到 10001 之间所有偶数的和。分析任务本质：这需要对一段范围内的数值进行遍历、筛选（偶数判断）、累加，最后输出结果。

三大基础控制结构的特点：
- **顺序结构**：代码从上至下逐条执行，不重复、不跳转，如同笔直的流水线。
- **选择结构（分支）**：根据条件判断选择不同执行路径，如 if-else。
- **循环结构**：重复执行一段代码，如 for、while 循环。

对于"遍历范围、重复计算"这类需求，必须依赖循环结构。仅使用顺序结构意味着无法表达"重复执行相同操作"的逻辑——你无法用纯顺序代码描述"对 1 到 10001 的每个数做检查和累加"，因为顺序结构的定义就是线性的、无分支的、无循环的执行路径。强行用顺序结构实现，只能手写数千条加法语句，这不是"程序"，而是"手工作业"。

**选项逐项分析：**
- **A (循环结构)**：❌ 错误。该项本身正确/合适。对于"遍历范围内数据并累加"的场景，循环是最直接、最符合结构化编程精神的方案，例如 \`for(int i=1; i<=10001; i+=2)\` 直接遍历偶数，代码简洁高效。
- **B (循环和分支结合)**：❌ 错误。该项本身正确/合适。例如 \`for(int i=1; i<=10001; i++)\` 内部配合 \`if(i%2==0)\` 判断奇偶，也是常见写法，逻辑清晰。
- **C (仅使用顺序结构)**：✅ 正确（因"最不合适"被选为答案）。顺序结构只能线性执行，无法表达"重复"和"遍历"的语义，因此对于需要迭代处理的本题来说，它是最不合适的选择。
- **D (不使用分支结构)**：❌ 错误。该项本身正确/合适。通过调整循环步长（如 \`i+=2\`）完全可以不依赖分支判断实现偶数遍历，这是高效的写法，不违背题设"执行效率高"的要求。

**易错提醒：**
- 审题时注意"最不合适"是反向选择问法，应选出最不能完成任务的选项，而非最合适的。
- 循环结构并非必须配合分支使用——步长控制（如 \`i+=2\`）是规避分支的高效手段。

**知识延伸：**
结构化程序设计的三大控制结构各有适用场景：循环用于重复动作，分支用于条件分流，顺序用于线性步骤。理解三者的边界是 GESP 二级前后的核心考点，也是初学者建立编程思维的基石。

**考点：** 三大基础控制结构（顺序、选择、循环）的场景适用性判定`,
      tags: [LEVEL1_TAGS.loop, LEVEL1_TAGS.basics]
    },
    {
      id: 3,
      type: 'single',
      question: `关于下边的C++代码用于输入姓名，然后输出姓名，正确的说法是( )。\n\`\`\`cpp\nstring XingMing;\ncout << "请输入您的姓名：";\ncin >> XingMing;\ncout << XingMing;\n\`\`\``,
      options: [
        'XingMing 是汉语拼音，不能作为变量名',
        '可以将 XingMing 改为 Xing Ming',
        '可以将 XingMing 改为 xingming',
        '可以将 XingMing 改为 Xing-Ming'
      ],
      answer: 2,
      score: 2,
      explanation: `**答案：C (可以将 XingMing 改为 xingming)**

**核心解析：**
题目展示了一段完整的姓名输入/输出程序，其中变量名 \`XingMing\` 采用了汉语拼音的驼峰命名法。分析各选项是否满足 C++ 标识符的合法命名规则：C++ 规定标识符只能由字母（A-Z, a-z）、数字（0-9）和下划线（_）组成，且不能以数字开头；空格、运算符符号（如 \`+\`, \`-\`, \`*\`, \`/\`）均为语法分隔符或运算符，严禁混入标识符内部。

**选项逐项分析：**
- **A (XingMing 是汉语拼音，不能作为变量名)**：❌ 错误。该项本身错误。C++ 对标识符的内容没有任何语义限制，使用汉语拼音、英语单词、数字组合甚至无意义字符串（如 \`abc123\`）均属合法。标识符的命名只关乎词法规则，不关乎语义或语言背景。
- **B (可以将 XingMing 改为 Xing Ming)**：❌ 错误。该项本身不合法。空格在 C++ 中是天然的词法分隔符，用于区分不同的语法符号。若写成 \`Xing Ming\`，编译器会将它解析为两个独立的标识符 \`Xing\` 和 \`Ming\`，导致语法错误而非一个名为"Xing Ming"的变量。
- **C (可以将 XingMing 改为 xingming)**：✅ 正确。\`xingming\` 全部由小写英文字母组成，完全符合 C++ 标识符的词法规则。虽然 C++ 区分大小写，\`XingMing\` 和 \`xingming\` 是两个不同的标识符，但两者均为合法变量名，可以进行替换。
- **D (可以将 XingMing 改为 Xing-Ming)**：❌ 错误。该项本身不合法。减号 \`-\` 是 C++ 中的二元算术运算符（用于减法运算），不能作为标识符的一部分。若写成 \`Xing-Ming\`，编译器会将其解析为 \`Xing\` 减以 \`Ming\` 的表达式，产生语法错误。

**易错提醒：**
- C++ 区分大小写，\`XingMing\`（首字母大写驼峰）和 \`xingming\`（全小写）是两个完全不同的标识符，但两者都合法。
- 空格在 C++ 中并非"忽略"，而是语法层面的分隔符，会直接导致词法解析失败。

**知识延伸：**
标识符命名虽无语义限制，但 GESP 鼓励使用有意义的英文单词或符合习惯的拼写方式（如驼峰命名法 \`userName\`、下划线命名法 \`user_name\`），便于代码可读性和团队协作。

**考点：** 标识符 (Identifier) 合法命名规范：禁用运算符与空格`,
      tags: [LEVEL1_TAGS.basics, LEVEL1_TAGS.io]
    },
    {
      id: 4,
      type: 'single',
      question: `下列C++代码中a和b都是整型变量，执行后，其结果是( )。\n\`\`\`cpp\na = 13;\nb = 5;\ncout << a / b << a % a * b;\n\`\`\``,
      options: ['2.60', '23', '20', '以上都不准确'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (20)**

**核心解析：**
\`\`\`cpp
a = 13;
b = 5;
cout << a / b << a % a * b;
\`\`\`
需要逐行分析变量状态和表达式求值过程：

**第一步：变量赋值**
- \`a = 13\`，\`b = 5\`，均为 \`int\` 型。

**第二步：分析第一个输出表达式 \`a / b\`**
- \`13 / 5\` 为两个 \`int\` 型数据的除法运算。
- 根据整数除法规则，结果为截断取整的商：$13 \\div 5 = 2$（余 $3$）。
- 因此 \`a / b\` 的值为 \`2\`。

**第三步：分析第二个输出表达式 \`a % a * b\`**
- 运算符优先级：\`*\` 和 \`%\` 同级（均为乘法类运算符），结合性为左到右。
- 先算 \`a % a\`：\`13 % 13\`，任何整数与自身取模，余数必为 $0$。
- 再算 \`0 * b\`：\`0 * 5 = 0\`。
- 因此 \`a % a * b\` 的值为 \`0\`。

**第四步：输出流拼接**
- \`cout <<\` 操作符将两个表达式的值依次送入输出流。
- 两个值之间没有任何分隔符（无空格、无换行、无显式分隔字符）。
- 因此输出结果为字符 \`2\` 紧接字符 \`0\`，即屏幕上显示 \`20\`。

**选项逐项分析：**
- **A (2.60)**：❌ 错误。该项本身错误。未考虑整数除法截断取整的特性，且混淆了整数除法与浮点数除法。\`13 / 5\` 的结果是整数 \`2\`，而非 \`2.6\`。
- **B (23)**：❌ 错误。该项本身错误。误将第二个表达式计算为 \`13 % 13 * 5\` 的结果当作 \`3\`（可能是混淆了取模结果的余数），未注意到取模结果为 $0$，与 $5$ 相乘仍为 $0$。
- **C (20)**：✅ 正确。如上逐行推导，\`2\` 与 \`0\` 在输出流中无分隔地拼接，形成字符串 \`20\`。
- **D (以上都不准确)**：❌ 错误。该项本身错误。实际输出结果就是 \`20\`，属于准确结果。

**易错提醒：**
- 整数除法的截断取整是 GESP 一级高频考点：\`13 / 5\` 结果是 \`2\`，不是 \`2.6\`，也不是向上取整的 \`3\`。
- 运算符优先级中 \`*\` 与 \`%\` 同级，必须从左向右结合，\`a % a * b\` 不等同于 \`a % (a * b)\`。
- 任何数与自身取模必为 $0$：\`n % n = 0\`。

**知识延伸：**
当 \`cout\` 连续使用多个 \`<<\` 输出基本类型值时，各值在输出流中依次排列，不自动插入空格或换行。若需分隔，应显式插入 \`cout << " "\` 或 \`cout << endl\`。

**考点：** 整数除法规律、同数取模特性与输出流的紧凑字符拼接原则`,
      tags: [LEVEL1_TAGS.operator, LEVEL1_TAGS.basics]
    },
    {
      id: 5,
      type: 'single',
      question: `C++表达式 3 * 4 % 5 / 6 的值是( )。`,
      options: ['10', '5', '2', '0'],
      answer: 3,
      score: 2,
      explanation: `**答案：D (0)**

**核心解析：**
表达式 \`3 * 4 % 5 / 6\` 中涉及三类运算符：\`*\`（乘法）、\`%\`（取模）、\`/\`（除法）。根据 C++ 运算符优先级规则，三者属于同一优先级，结合性为从左到右。

**逐步推导过程：**

**第一步：计算 \`3 * 4\`**
- $3 \\times 4 = 12$。

**第二步：计算 \`12 % 5\`**
- 12 除以 5 商 $2$ 余 $2$。
- 因此 \`12 % 5\` = \`2\`。

**第三步：计算 \`2 / 6\`**
- 这是关键步骤：被除数 $2$ 小于除数 $6$。
- 整数除法结果为截断取整的商：$2 \\div 6 = 0$（因为 $0 \\times 6 = 0 \\leq 2 < 6$，下一个整数是 $1$，$1 \\times 6 = 6 > 2$）。
- 因此 \`2 / 6\` = \`0\`。

**最终结果：\`0\`**

**选项逐项分析：**
- **A (10)**：❌ 错误。该项本身错误。若将运算顺序错误地理解为先乘法后除法（忽略取模），可能误算为 \`12 / 6 = 2\` 或对整体数值估算失误，但 \`0\` 是唯一正确答案。
- **B (5)**：❌ 错误。该项本身错误。可能是对取模运算结果的误判或其他计算顺序错误导致。
- **C (2)**：❌ 错误。该项本身错误。可能是将取模结果 \`2\` 误认为是最终结果，忽略了后续还有除以 \`6\` 的操作。
- **D (0)**：✅ 正确。经过严格从左到右的推导，\`2 / 6\` 的整数除法结果为 $0$。

**易错提醒：**
- "小除大"是整数除法中的高频陷阱：任何小于除数的被除数除以该除数，整数商必为 $0$。
- 当多个运算符同优先级时，必须严格从左到右结合，不能跳跃或凭直觉重组计算顺序。
- 取模运算 \`%\` 的结果必然小于除数，如 \`12 % 5 = 2\`，后续的 \`2 / 6\` 是合法的独立运算。

**知识延伸：**
整数除法的截断特性与数学中的向下取整不同：C++ 中整数除法向零截断，正数除法结果直接取整数部分，负数场景下才涉及符号差异。一级阶段只需掌握正整数范围内的截断取整规则。

**考点：** 同阶运算符结合性与整数"小除大"归零现象`,
      tags: [LEVEL1_TAGS.operator]
    },
    {
      id: 6,
      type: 'single',
      question: `下面的C++代码中变量 N 和 M 都是整型，则执行时如果先输入10并输入1个空格后输入20并回车，其输出的数值是( )。\n\`\`\`cpp\nscanf("%d", &N);\nscanf("%d", &M);\nprintf("%d", N+M);\n\`\`\``,
      options: ['30', '1020', '{N+M}', '不输出，继续等待输入'],
      answer: 0,
      score: 2,
      explanation: `**答案：A (30)**

**核心解析：**
1. 第一个 \`scanf("%d", &N)\` 执行时，会跳过输入流中前导的空白字符（空格、制表符、换行），然后读取数字。当输入 \`10 20\` 时，第一个 scanf 读取 \`10\` 存入变量 N。
2. 第二个 \`scanf("%d", &M)\` 继续读取，跳过分隔的空格，读取数字 \`20\` 存入变量 M。
3. \`printf("%d", N+M)\` 执行算术加法：10 + 20 = 30，并按十进制整数格式输出。

**选项逐项分析：**
- **A (30)**：✅ 正确。N 接收值 10，M 接收值 20，两者相加结果为 30。
- **B (1020)**：❌ 错误。这是某些动态语言（如 JavaScript、PHP）中的字符串拼接行为。在 C++ 中，scanffmt 读取的是数值，+ 运算符执行的是算术加法，而非字符串拼接。
- **C ({N+M})**：❌ 错误。printf 的格式字符串中，花括号 \`{N+M}\` 不会被解释为变量或表达式，C++ 编译器会将其原样输出为普通字符，不存在任何变量替换机制。
- **D (不输出，继续等待输入)**：❌ 错误。两次 scanf 都成功读取了整数，不会进入等待状态。

**易错提醒：**
- 注意 scanf 在读取 %d 时会自动跳过前导空白字符，因此空格不会导致第二个 scanf 失败。
- printf 的格式字符串中不能写变量名或表达式，必须使用 %d、%s 等格式说明符。

**知识延伸：**
scanf 和 printf 是 C 语言风格的格式化输入输出函数，在 GESP 二级也会涉及更复杂的格式字符串，如 \`%d %d\` 同时读取两个整数。

**考点：** scanf 的空白符过滤机制与基础算术运算`,
      tags: [LEVEL1_TAGS.io, LEVEL1_TAGS.basics]
    },
    {
      id: 7,
      type: 'single',
      question: `当前是9月，编写C++代码求 N 个月后的月份。横线处应填入的代码是 ( )。\n\`\`\`cpp\nint N, M;\ncin >> N;\nM = ________;\nif (M == 0) printf("%d个月后是12月", N);\nelse printf("%d个月后是%d月", N, M);\n\`\`\``,
      options: ['N % 12', '9+N % 12', '(9+N) / 12', '(9+N) % 12'],
      answer: 3,
      score: 2,
      explanation: `**答案：D ((9+N) % 12)**

**核心解析：**
1. 当前是 9 月，要计算 N 个月后的月份，需要将月份序号与月份数相加：9 + N。
2. 由于月份是 1-12 的循环（12 月之后是 1 月），需要用取模运算处理周期。
3. 注意：月份序号通常取 1-12，但若当前是 9 月（序号为 9），则 9 + N 个月后，若恰好跨年，结果可能超过 12。
4. 使用 \`(9+N) % 12\` 将结果映射到 [0, 11] 范围。当结果为 0 时，表示 12 月，由 if 语句特殊处理并输出"12月"。
5. 例如 N=3 时，(9+3)%12=0，触发 if 分支输出"12月"（9月后3个月是12月）；N=4 时，(9+4)%12=1，输出"1月"（9月后4个月是1月）。

**选项逐项分析：**
- **A (N % 12)**：❌ 错误。该项未考虑起始月份 9，只是对 N 取模，结果范围 [0,11] 无法正确反映从 9 月开始的月份变化。
- **B (9+N % 12)**：❌ 错误。由于取模运算符 % 优先级高于加法 +，该表达式等价于 \`9 + (N % 12)\`，先对 N 取模再加 9，月份计算逻辑完全错误。
- **C ((9+N) / 12)**：❌ 错误。整除 / 运算得到的是跨过的完整年份数（如 N=15 时得到 2），不是月份序号。
- **D ((9+N) % 12)**：✅ 正确。将起始月份与月份数之和取模 12，得到月份在循环中的位置，由后续 if 语句处理 0 代表 12 月的特殊情况。

**易错提醒：**
- 运算符优先级：% 和 / 与 + 同级，从左到右计算，因此 \`9+N % 12\` 等价于 \`9 + (N % 12)\`，必须加括号写成 \`(9+N) % 12\`。
- 取模结果 0 代表 12 月，需要通过 if 语句特殊处理，因为月份序号通常不使用 0。

**知识延伸：**
取模运算常用于处理周期性问题的"绕回"场景，如星期几计算、时钟指针位置等，其核心思想是将数值约束在一个固定范围内。

**考点：** 周期性问题的取模 (Modulo) 模型应用与括号优先级`,
      tags: [LEVEL1_TAGS.operator, LEVEL1_TAGS.basics]
    },
    {
      id: 8,
      type: 'single',
      question: `下面C++代码执行后的输出是 ( )。\n\`\`\`cpp\nint n = 0;\nfor (int i = 0; i < 100; i++)\n n += i % 2;\ncout << n;\n\`\`\``,
      options: ['5050', '4950', '50', '49'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (50)**

**核心解析：**
1. 循环从 i=0 执行到 i=99，共 100 次迭代。
2. 表达式 \`i % 2\` 的结果取决于 i 的奇偶性：当 i 为偶数时结果为 0，当 i 为奇数时结果为 1。
3. 在 0 到 99 这 100 个整数中，偶数有 50 个（0,2,4,...,98），奇数有 50 个（1,3,5,...,99）。
4. 累加过程：偶数项贡献 50×0 = 0，奇数项贡献 50×1 = 50，总和为 50。
5. 最终输出 n 的值为 50。

**选项逐项分析：**
- **A (5050)**：❌ 错误。这是 0 到 100 的累加和（高斯求和公式 n×(n+1)/2 = 100×101/2 = 5050），但本题累加的是 \`i % 2\` 而非 i。
- **B (4950)**：❌ 错误。这是 1 到 99 的累加和，但本题的累加模式完全不同。
- **C (50)**：✅ 正确。100 次循环中，恰好有 50 次 \`i % 2\` 的值为 1，其余 50 次为 0，累加结果为 50。
- **D (49)**：❌ 错误。0 到 99 共 100 个数，奇数和偶数各占一半，不存在 49 个奇数的情况。

**易错提醒：**
- 不要混淆"累加 i 的值"和"累加 i % 2 的值"，本题累加的是取模结果，每个数最多贡献 1。
- i 从 0 开始而非 1 开始，因此偶数有 50 个、奇数也有 50 个，循环范围是闭区间 [0, 99]。

**知识延伸：**
取模运算常用于"计数满足某条件的数"的问题，如统计一段范围内偶数的个数、判断是否为 3 的倍数等，其结果限定在 [0, 模数-1] 范围内。

**考点：** 循环内取模表达式的求和与奇偶性统计`,
      tags: [LEVEL1_TAGS.loop, LEVEL1_TAGS.operator]
    },
    {
      id: 9,
      type: 'single',
      question: `C++表达式 abs(1.0)+floor(-1.0) 的值是( )。`,
      options: ['0', '1', '2', '0.0'],
      answer: 3,
      score: 2,
      explanation: `**答案：D (0.0)**

**核心解析：**
1. \`abs(1.0)\`：参数 1.0 是 double 类型，abs 函数返回 double 类型的 1.0。
2. \`floor(-1.0)\`：参数 -1.0 是 double 类型，floor 函数返回 double 类型。由于 -1.0 本身就是整数，floor(-1.0) 的结果仍为 -1.0。
3. 最终表达式：\`1.0 + (-1.0)\` = \`0.0\`。
4. 结果类型为 double，以浮点数 0.0 输出。注意：选项 D 是 "0.0"（带小数点），与选项 A 的整数 "0" 严格不同。

**选项逐项分析：**
- **A (0)**：❌ 错误。虽然数值上等于零，但类型不符。abs 和 floor 对浮点数的操作返回 double 类型，结果应为 0.0 而非整数 0。
- **B (1)**：❌ 错误。这是 abs(1.0) 的值或 floor(-1.0) 的值，但遗漏了另一项的贡献。
- **C (2)**：❌ 错误。这是 abs(1.0) 的绝对值加上 1（误将 floor(-1.0) 当作 1），计算完全错误。
- **D (0.0)**：✅ 正确。1.0 加上 -1.0 等于 0.0，结果以浮点数格式输出，包含小数点。

**易错提醒：**
- abs 和 floor 函数的返回类型取决于参数类型：对 double 参数返回 double，对 int 参数返回 int。本题参数是 1.0 和 -1.0（浮点字面量），必须输出浮点数格式。
- -1.0 本身就是整数点，floor 函数对整数点向下取整结果不变，但返回值类型仍为 double。

**知识延伸：**
C++ 标准库中的数学函数（如 fabs、floor、ceil）通常定义在 \`<cmath>\` 中，函数名和返回类型随参数类型变化，这是 GESP 三级可能涉及的函数重载概念。

**考点：** 浮点数学库函数的返回值类型与实数运算`,
      tags: [LEVEL1_TAGS.basics, LEVEL1_TAGS.operator]
    },
    {
      id: 10,
      type: 'single',
      question: `代码执行后的输出是 ( )。\n\`\`\`cpp\nint a, b;\ncin >> a >> b;\nif (a > b) a = b;\nb = a;\ncout << a << " " << b;\n\`\`\`\n如果输入 10 20：`,
      options: ['10 20', '10 10', '20 20', '20 10'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (10 10)**

**核心解析：**
1. 输入 10 20，变量初始化：a = 10，b = 20。
2. 执行 \`if (a > b)\`：判断条件 10 > 20，结果为假（false），if 语句内的赋值 \`a = b\` 不执行，a 保持为 10。
3. 执行 \`b = a\`：无论 if 条件如何，此语句无条件执行。b 的原值 20 被覆盖，b 变为 10（等于 a 的当前值）。
4. 执行 \`cout << a << " " << b\`：输出 a 和 b 的当前值，即 10 和 10，以空格分隔。

**选项逐项分析：**
- **A (10 20)**：❌ 错误。虽然 a 确实是 10，但 b = a 语句会将 b 的值从 20 改为 10，因此不会是原始输入的 20。
- **B (10 10)**：✅ 正确。a 保持原始输入值 10，b 被后续语句覆盖为 10，输出 10 10。
- **C (20 20)**：❌ 错误。虽然 b 被覆盖为 a 的值，但 a 的值始终是 10 而非 20，if 条件不满足时 a 不会变成 b。
- **D (20 10)**：❌ 错误。a 在 if 条件为假时不会获得 b 的值 20，a 保持原值 10。

**易错提醒：**
- if 语句内的赋值只在条件为真时执行；当条件为假时，该语句被跳过，变量保持原值。
- \`b = a\` 在 if 语句之后，是独立语句，无论条件真假都会执行，导致 b 的值被覆盖。

**知识延伸：**
顺序结构中，后面的赋值语句会覆盖前面的变量值，这体现了程序执行的"时间顺序"特性，在调试时需要特别注意变量状态的变化顺序。

**考点：** 选择结构的条件分支路径与顺序结构的变量覆盖`,
      tags: [LEVEL1_TAGS.condition, LEVEL1_TAGS.basics]
    },
    {
      id: 11,
      type: 'single',
      question: `代码执行后的输出是 ( )。\n\`\`\`cpp\nint n, ans = 0;\ncin >> n;\nwhile (n > 0) {\n ans = ans * 10+n % 10;\n n /= 10;\n}\ncout << ans;\n\`\`\`\n如果输入 123：`,
      options: ['123', '3', '321', '6'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (321)**

**核心解析：**
这是一道经典的数字反转算法题。代码通过循环逐次取出原数的最低位，并将其累加到答案的末尾。

逐行追踪变量状态（输入 n=123）：
1. 初始状态：n=123，ans=0。
2. 第1轮循环（n>0 成立）：
   - \`ans = ans * 10 + n % 10\` → \`ans = 0 * 10 + 3\` → \`ans=3\`
   - \`n /= 10\` → \`n=12\`
3. 第2轮循环（n>0 成立）：
   - \`ans = 3 * 10 + 12 % 10\` → \`ans=32\`
   - \`n /= 10\` → \`n=1\`
4. 第3轮循环（n>0 成立）：
   - \`ans = 32 * 10 + 1 % 10\` → \`ans=321\`
   - \`n /= 10\` → \`n=0\`
5. 第4轮：n=0，条件 n>0 不成立，循环结束。
6. 输出 ans=321。

**选项逐项分析：**
- **A (123)**：❌ 错误。原样输出输入值，说明没有理解取模和除法对数位的提取作用。
- **B (3)**：❌ 错误。只保留了第一轮提取的个位数3，未理解 ans 在每次循环中的累乘叠加机制。
- **C (321)**：✅ 正确。完整执行了数字反转的三轮循环，ans 最终等于原数的逆序。
- **D (6)**：❌ 错误。可能是误将各数位数字相加（1+2+3=6），混淆了数字反转与数位求和两种不同算法。

**易错提醒：**
- 易错点1：误以为 ans 每次都会被覆盖，实际上 ans=ans*10 使得之前提取的数字会左移一位，形成低位到高位的累加。
- 易错点2：未理解 n/=10 的作用是逐次丢弃最低位，每轮循环后 n 的有效位数减一。

**知识延伸：**
本题是数字反转的经典模版，其核心思想是"取个位×10的幂次累加"。类似地，若要统计一个整数的位数，只需在循环中不断 n/=10 并计数；若要将每位数字存入数组，则用取模运算逐个提取。

**考点：** 数字反转算法、while循环与取模/除法运算的综合应用`,
      tags: [LEVEL1_TAGS.loop, LEVEL1_TAGS.basics]
    },
    {
      id: 12,
      type: 'single',
      question: `代码执行后的输出是 ( )。\n\`\`\`cpp\nfor (int i = 1; i <= 5; i++) {\n if (i % 2 == 0) continue;\n cout << i;\n}\n\`\`\``,
      options: ['12345', '24', '135', '13'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (135)**

**核心解析：**
代码使用 for 循环遍历 1 到 5，当 i 为偶数时执行 continue 跳过本次循环的剩余语句。

逐次分析 i 的取值：
1. i=1：1 % 2 = 1 ≠ 0，条件不满足，不执行 continue，正常执行 \`cout << 1\`，输出 "1"。
2. i=2：2 % 2 = 0 == 0，条件满足，执行 continue，直接跳回 for 语句的 i++ 部分，本次不输出。
3. i=3：3 % 2 = 1 ≠ 0，条件不满足，正常输出 "3"。
4. i=4：4 % 2 = 0 == 0，条件满足，执行 continue，跳过输出。
5. i=5：5 % 2 = 1 ≠ 0，正常输出 "5"。
6. 循环结束，最终输出序列为 "135"。

**选项逐项分析：**
- **A (12345)**：❌ 错误。说明完全忽略了 if 语句的筛选作用，未意识到偶数 2 和 4 会被 continue 跳过。
- **B (24)**：❌ 错误。只输出了偶数，与 continue 的实际效果相反。continue 是跳过本次循环，不是跳过奇数。
- **C (135)**：✅ 正确。准确执行了 continue 对偶数的跳过逻辑，只输出了 1、3、5 三个奇数。
- **D (13)**：❌ 错误。遗漏了 i=5 的情况。循环条件 i<=5 包含 5，5 是奇数会正常输出。

**易错提醒：**
- 易错点1：混淆 break 和 continue 的作用。break 是跳出整个循环，continue 是跳过本次循环剩余语句进入下一次迭代。
- 易错点2：未仔细检查循环终止条件 i<=5，遗漏了边界值 i=5 的处理。

**知识延伸：**
continue 语句常用于过滤不需要处理的元素，使代码结构更清晰。类似地，在数组处理中可以用 continue 跳过不符合条件的元素，只对有效数据执行操作。

**考点：** continue 语句对循环执行流程的影响、for 循环边界条件`,
      tags: [LEVEL1_TAGS.loop, LEVEL1_TAGS.condition]
    },
    {
      id: 13,
      type: 'single',
      question: `15 % 4+7 / 2 的值是( )。`,
      options: ['6', '6.5', '7', '3'],
      answer: 0,
      score: 2,
      explanation: `**答案：A (6)**

**核心解析：**
本题涉及整数除法和取模运算，需要严格遵守运算符优先级。

优先级分析：\`*\`、\`/\`、\`%\` 优先级相同且高于 \`+\`，因此先计算 \`15 % 4\` 和 \`7 / 2\`，最后再加法。

逐步计算：
1. 15 % 4 = 3（15除以4余3，因为4×3=12，15-12=3）
2. 7 / 2 = 3（整除，向零取整，7除以2得3余1）
3. 3 + 3 = 6

最终结果为 6。

**选项逐项分析：**
- **A (6)**：✅ 正确。严格遵循整除和取模运算规则，计算正确。
- **B (6.5)**：❌ 错误。误认为 7/2=3.5，但 C++ 中两个整数相除执行整除运算，结果必为整数 3，不会出现小数。
- **C (7)**：❌ 错误。可能是误以为 7/2=4（四舍五入），但整除是直接截断小数部分，不是四舍五入。
- **D (3)**：❌ 错误。只计算了 15%4=3，遗漏了最后的加法步骤。

**易错提醒：**
- 易错点1：混淆整除与浮点除法。在 C++ 中，除号两侧都是整数时执行整除，若需浮点结果，至少有一个操作数应为浮点类型（如 7.0/2）。
- 易错点2：整除是向零取整（截断），而非四舍五入。例如 -3/2 = -1（截断），而不是 -2（四舍五入）。

**知识延伸：**
取模运算 a % b 的结果是 a 除以 b 的余数，满足：a = (a/b) * b + a%b。本题的 15 = 3×4 + 3 就是这一恒等式的体现。

**考点：** 运算符优先级、整除运算规则、取模运算`,
      tags: [LEVEL1_TAGS.operator]
    },
    {
      id: 14,
      type: 'single',
      question: `代码执行后的输出是 ( )。\n\`\`\`cpp\nint n = 10, sum = 0;\nwhile (n > 0) {\n if (n % 3 == 0) break;\n sum += n;\n n--;\n}\ncout << sum;\n\`\`\``,
      options: ['55', '19', '45', '10'],
      answer: 3,
      score: 2,
      explanation: `**答案：D (10)**

**核心解析：**
代码通过 while 循环累加 n 的值，直到遇到能被3整除的数时执行 break 终止循环。

逐次追踪执行过程：
1. 初始：n=10，sum=0。
2. 第1轮循环：
   - 判断：10 > 0 ✓，10 % 3 = 1 ≠ 0，不执行 break
   - 执行：sum += 10 → sum=10
   - 更新：n-- → n=9
3. 第2轮循环：
   - 判断：9 > 0 ✓，9 % 3 = 0 == 0，条件成立！
   - 执行 break：**立即终止整个 while 循环**
4. 循环结束，跳出循环体，输出 sum=10。

**选项逐项分析：**
- **A (55)**：❌ 错误。若没有 break，1到10的累加和确实是55。但 break 在 n=9 时触发，循环提前终止。
- **B (19)**：❌ 错误。可能是误算了 10+9=19，但 9 是 3 的倍数会触发 break，不会被累加。
- **C (45)**：❌ 错误。可能是误以为 break 不影响累加，但 break 的存在使得只有 10 被加入 sum。
- **D (10)**：✅ 正确。只有 n=10 时执行了累加，n 递减到 9 后触发 break，循环结束，sum=10。

**易错提醒：**
- 易错点1：未识别 n=9 是 3 的倍数这一关键条件。break 的触发条件是 n%3==0，而非 n>0 不成立。
- 易错点2：混淆 break 和 continue 的作用。break 是终止整个循环，continue 是跳过一次迭代继续循环。

**知识延伸：**
break 只能跳出一层循环（离它最近的那层）。如果存在多层嵌套循环，break 只退出其中一层，外层循环继续执行。

**考点：** break 语句的跳出机制、while 循环条件判断、整除判断`,
      tags: [LEVEL1_TAGS.loop, LEVEL1_TAGS.condition]
    },
    {
      id: 15,
      type: 'single',
      question: `在 IDE 中调试时，设置断点的目的是( )。`,
      options: ['终止程序运行', '让程序在特定位置暂停', '修改程序源代码', '提高运行速度'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (让程序在特定位置暂停)**

**核心解析：**
断点是 IDE（集成开发环境）调试功能中最常用的工具之一。其核心作用是在程序执行到某一行代码时**自动暂停**，使程序进入调试状态。

在断点处暂停后，开发者可以：
1. 查看当前变量的具体数值
2. 观察函数调用栈和内存状态
3. 单步执行（逐行或逐过程）后续代码
4. 分析程序运行轨迹，定位逻辑错误

这种"暂停-观察-单步"的调试模式是排查 bug 的基本方法。

**选项逐项分析：**
- **A (终止程序运行)**：❌ 错误。终止程序运行的是"停止调试"按钮或结束进程，断点不会主动终止程序。
- **B (让程序在特定位置暂停)**：✅ 正确。断点的本质功能就是让程序在指定位置暂停，以便开发者检查程序状态。
- **C (修改程序源代码)**：❌ 错误。修改源代码需要手动编辑代码文件，调试模式下可以修改变量值（修改 Watch/变量窗口），但这不是断点的目的。
- **D (提高运行速度)**：❌ 错误。设置断点反而会降低运行速度，因为程序在断点处需要暂停等待调试器响应。

**易错提醒：**
- 易错点1：将断点与"结束程序"混淆。实际上断点的作用是暂停而非终止，这是调试与运行的核心区别。
- 易错点2：认为调试必然能"修复"代码。断点只是帮助观察程序状态，发现问题后仍需人工分析原因并修改源代码。

**知识延伸：**
除普通断点外，调试器还支持条件断点（仅当特定条件满足时暂停）、数据断点（变量值变化时暂停）等高级功能，适用于不同场景的调试需求。

**考点：** IDE 调试工具的基本概念、断点的作用与使用场景`,
      tags: [LEVEL1_TAGS.basics]
    },
    {
      id: 16,
      type: 'judge',
      question: `在C++中，变量名必须以字母或下划线开头。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
C++ 标识符（变量名、函数名等）的命名规则规定：标识符只能由字母（A-Z、a-z）、数字（0-9）和下划线（_）组成，且首字符不能是数字。因此，变量名可以以字母或下划线开头，这一说法符合 C++ 标准规定。注意，虽然以单个下划线开头的标识符在语法上合法，但以下划线加大写字母开头的标识符通常被保留给编译器或标准库使用。

**易错提醒：**
- 虽然命题正确，但需注意：变量名可以以字母或下划线开头，但绝不能以数字开头。
- 以双下划线 \`__\` 或下划线加大写字母开头的标识符在 C++ 标准中属于保留标识符，不应使用。

**知识延伸：**
标识符命名规则是 C++ 最基础的语法要素之一。这一规则不仅适用于变量名，也适用于函数名、类名、数组名等所有用户自定义标识符。

**考点：** C++ 标识符（Identifier）的命名规则`,
      tags: [LEVEL1_TAGS.basics]
    },
    {
      id: 17,
      type: 'judge',
      question: `3 < x < 5 是检查 x 是否在 3 和 5 之间的正确写法。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
表达式 \`3 < x < 5\` 在 C++ 中并非表示“x 在 3 和 5 之间”。C++ 运算符具有左结合性，该表达式按以下步骤求值：
1. 先计算 \`3 < x\`，得到布尔值 \`true\`（转换为 1）或 \`false\`（转换为 0）。
2. 再计算 \`(1 或 0) < 5\`。由于 1 < 5 且 0 < 5 均成立，结果恒为 \`true\`。
3. 因此，无论 x 的实际值如何，只要 x 不是导致第一步产生非数值错误的情况，该表达式始终为真，完全失去了范围判断的功能。

正确写法应使用逻辑与运算符连接两个独立的比较条件：\`x > 3 && x < 5\`（或 \`x > 3 && x < 5\`）。命题声称 \`3 < x < 5\` 是正确写法，与 C++ 语言实际求值规则矛盾，故判定为错误。

**易错提醒：**
- 不能将数学中的区间表示直接套用到 C++ 表达式中。C++ 的比较运算符不能像数学那样“链式”使用。
- 布尔值参与数值比较时会发生隐式类型转换（true → 1，false → 0），这是导致该表达式失效的根本原因。

**知识延伸：**
要判断变量是否在开区间 (a, b) 内，必须使用逻辑与运算符连接两个独立条件：\`x > a && x < b\`。判断闭区间 [a, b] 则需用 \`x >= a && x <= b\`。

**考点：** C++ 关系运算符的求值顺序与逻辑与运算符的应用`,
      tags: [LEVEL1_TAGS.basics]
    },
    {
      id: 18,
      type: 'judge',
      question: `while 循环可能一次都不执行。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
while 循环是典型的“前测试循环”（Pre-test Loop）。在执行循环体之前，程序会先计算圆括号中的条件表达式。如果条件在首次检测时就为 false（如 \`int i = 10; while(i < 5)\`），循环体一次也不会执行，直接跳到循环之后的代码。这与 do-while 循环的“后测试”特性形成对比：do-while 的循环体至少执行一次，因为条件判断发生在循环体之后。

**易错提醒：**
- 区分 while 和 do-while 的关键：while 先判断条件后执行循环体，do-while 先执行循环体后判断条件。
- 如果需要循环体至少执行一次，应使用 do-while 结构。

**知识延伸：**
C++ 中的三种循环结构（while、do-while、for）中，只有 do-while 能保证循环体至少执行一次。for 循环同样具有 while 的前测试特性，因此 for 循环也可能一次都不执行。

**考点：** while 循环的条件前置检测机制`,
      tags: [LEVEL1_TAGS.loop]
    },
    {
      id: 19,
      type: 'judge',
      question: `for 循环的三个部分都可以省略。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
for 循环的标准形式为 \`for(初始化; 条件; 更新)\`，其三个表达式均可省略：
1. **初始化部分**：可省略，例如变量已在循环前定义。此时 for 头部第一个分号不能省略。
2. **条件部分**：可省略，省略后默认值为 true，形成死循环。此时 for 头部第二个分号不能省略。
3. **更新部分**：可省略，此时循环变量的更新逻辑需写在循环体内。

即使写成最简形式 \`for(;;)\`（两个分号保留），编译器仍将其解析为合法的无限循环，因为分号充当了空表达式的占位符。

**易错提醒：**
- 省略条件时默认恒为 true，可能导致死循环，需要在循环体内设置退出条件（如 break 语句）。
- 省略初始化时，循环变量的定义和更新需要手动管理，容易出错。

**知识延伸：**
\`for(;;)\` 与 \`while(true)\` 在功能上等效，但前者更简洁。在 GESP 二级以后的学习中，会涉及如何正确使用 break 语句退出死循环。

**考点：** for 循环语法的缺省规则与无限循环构造`,
      tags: [LEVEL1_TAGS.loop]
    },
    {
      id: 20,
      type: 'judge',
      question: `1 == 1 的值是 true。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
在 C++ 中，判等运算符 \`==\` 用于比较两个表达式的值是否相等。表达式 \`1 == 1\` 的左右操作数均为整数常量 1，它们在数值上相等，因此 \`==\` 运算符返回布尔值 true（在整数语境下等价于 1）。该命题在 C++ 标准语义下完全成立。

需要注意的是，在旧式 C++（C++98 之前的某些语境）中，布尔字面量可能不完全等同于整数，但 \`1 == 1\` 返回“相等”的逻辑判断结果这一事实始终成立。

**易错提醒：**
- 区分赋值运算符 \`=\` 与判等运算符 \`==\`。\`=\` 用于赋值，\`==\` 用于比较。错误地使用 \`=\` 而非 \`==\` 可能导致逻辑错误（尤其在 if 条件中）。
- \`==\` 运算符要求左右操作数类型可比较，不同类型可能触发隐式转换（如 char 与 int 比较时）。

**知识延伸：**
关系运算符 \`==\` 和 \`!=\` 的结果均为布尔类型。在需要整数值的语境中（如数组下标），true 可隐式转换为 1，false 可隐式转换为 0。

**考点：** 判等运算符（==）的求值与布尔类型`,
      tags: [LEVEL1_TAGS.operator]
    },
    {
      id: 21,
      type: 'judge',
      question: `!(3 > 5) 的值是 false。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
1. 先计算关系表达式 \`3 > 5\`：\`3\` 与 \`5\` 比较，显然 \`3\` 不大于 \`5\`，该表达式结果为 **false（假）**。
2. 再执行逻辑非运算 \`!\`：逻辑非 \`!\` 的作用是将真假反转，\`!false\` 的结果为 **true（真）**。
3. 因此 \`!(3 > 5)\` 的实际值是 \`true\`，而命题声称其值为 \`false\`，两者不符。

**易错提醒：**
- 容易忽略外层 \`!\` 的反转作用，只看 \`3 > 5\` 为假就认为整个表达式为假。
- 注意区分"表达式的值"与"命题的描述"，本题命题说的是"值是 false"，这是错误的。

**知识延伸：**
逻辑非 \`!\` 的优先级高于关系运算，但本题中 \`3 > 5\` 本身已有括号含义（整体先计算内部），实际上 \`!(3 > 5)\` 等价于 \`!(5 < 3)\` 的求值顺序，逻辑非将布尔结果取反是核心考点。

**考点：** 关系运算与逻辑非的综合求值`,
      tags: [LEVEL1_TAGS.operator]
    },
    {
      id: 22,
      type: 'judge',
      question: `在C++中，char 类型占1个字节。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
在 C++ 语言标准中，\`char\`（字符型）被明确定义为占用 **1 个字节**的存储空间。这是 C++ 的基础规范，与具体的编译器、操作系统或硬件平台无关。无论在何种环境下，一个 \`char\` 类型变量始终占据恰好 1 字节（8 比特）的内存。

**易错提醒：**
- 容易混淆"字符"与"字符串"：\`char\` 是单个字符类型占 1 字节，而 \`char[]\` 或 \`string\` 是字符串，占用的字节数取决于实际字符数量。
- 题目强调的是 C++ 标准规定，记住这条规则即可直接判断。

**知识延伸：**
\`char\` 是 C++ 中最小的寻址单位，也是构成 \`int\`、\`float\` 等更复杂类型的基本单元。在 GESP 一级中，记住 \`char\` 占 1 字节、\`int\` 通常占 4 字节是常见考点。

**考点：** 基础数据类型 \`char\` 的内存空间占用（1 字节）`,
      tags: [LEVEL1_TAGS.basics]
    },
    {
      id: 23,
      type: 'judge',
      question: `斐波那契数列第1项是1，第2项是1，则第3项是2。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
斐波那契数列的递推定义为：从第 3 项起，每一项等于前两项之和，即 F(n) = F(n-1) + F(n-2)。
- 已知第 1 项 F(1) = 1，第 2 项 F(2) = 1。
- 第 3 项 F(3) = F(2) + F(1) = 1 + 1 = **2**。

命题描述与数学定义完全一致。

**易错提醒：**
- 注意斐波那契数列从第 3 项才开始有递推关系，第 1、2 项是初始条件，需要直接记住。
- 部分教材或题目可能从 F(0)=0, F(1)=1 开始编号，但无论哪种约定，已知 F(1)=1, F(2)=1 时 F(3)=2 是确定无疑的。

**知识延伸：**
斐波那契数列在自然界（如向日葵种子排列、树枝分叉）中广泛存在，也是 GESP 各级别常见的编程输出考点，学员应能用循环或递归熟练输出该数列。

**考点：** 斐波那契数列的递推定义与项值推演`,
      tags: [LEVEL1_TAGS.basics]
    },
    {
      id: 24,
      type: 'judge',
      question: `代码 if (x = 1) 总是将 x 设置为 1。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
1. \`if (x = 1)\` 中的 \`x = 1\` 是**赋值表达式**，不是比较表达式 \`x == 1\`。
2. 赋值表达式的求值结果是被赋予的值，即整个 \`x = 1\` 表达式的结果为 \`1\`。
3. 在 C++ 中，非零值被视为 **true**，因此 \`if\` 条件总是成立。
4. 无论 \`x\` 原本是什么值，执行 \`if (x = 1)\` 后，\`x\` 必然被修改为 \`1\`。

命题"总是将 x 设置为 1"完全准确描述了这一行为。

**易错提醒：**
- 常见笔误是写成 \`if (x = 1)\` 而误以为是在比较，实际上这是赋值，条件恒为真。
- 这种写法在调试代码时可能被有意利用（如故意让某段代码块总是执行），但正常编程中应避免。
- \`if (x == 1)\` 才是比较是否相等的正确写法。

**知识延伸：**
C/C++ 中赋值表达式本身有返回值（所赋的值），这一特性使得 \`if (x = y)\` 这类代码能通过编译，但语义往往与 \`if (x == y)\` 不同。这是 GESP 二级及以上常见的语法陷阱考点。

**考点：** \`if\` 条件括号内赋值表达式与变量状态变更`,
      tags: [LEVEL1_TAGS.basics, LEVEL1_TAGS.condition]
    },
    {
      id: 25,
      type: 'judge',
      question: `break 只能用于循环中。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
\`break\` 语句的作用是跳出最近的一层结构。在 C++ 中，\`break\` 主要用于以下两种场景：
1. **循环结构**：\`for\`、\`while\`、\`do-while\` 循环中，用于提前终止循环。
2. **switch 结构**：在 \`switch-case\` 语句中，用于阻止 case 穿透（防止代码继续执行到下一个 case）。

由于 \`break\` 可以在 \`switch\` 语句中使用，而 \`switch\` 不是循环结构，因此"只能用于循环中"的说法过于绝对，不能成立。

**易错提醒：**
- 容易忽略 \`break\` 在 \`switch\` 中的作用，只想到循环中的跳出功能。
- 循环中的 \`break\` 只能跳出一层循环（对于嵌套循环，只跳出最内层）。
- \`switch\` 中若缺少 \`break\`，会产生 case 穿透现象，这也是常见的错误点。

**知识延伸：**
与 \`break\` 对应的还有 \`continue\`，后者只能用于循环中（跳过本次迭代剩余语句，直接进入下次循环），不能在 \`switch\` 中使用。这也是 GESP 中常考的对比知识点。

**考点：** \`break\` 语句的双重应用场景（循环结构与 switch 结构）`,
      tags: [LEVEL1_TAGS.basics]
    },
    {
      id: 26,
      type: 'programming',
      samples: [
        { input: `8\n7\n9\n10`, output: `3.00` },
        { input: `8\n7\n2\n11`, output: `2.20` }
      ],
      referenceCode: `#include <iostream>
#include <iomanip>
using namespace std;
int main() {
    long long x, y, n, p;
    cin >> x >> y >> n >> p;
    double p1 = p;
    if (p >= x) p1 -= y;
    double p2 = p * n / 10.0;
    if (p1 < p2) cout << fixed << setprecision(2) << p1 << "\\n";
    else cout << fixed << setprecision(2) << p2 << "\\n";
    return 0;
}`,
      question: `
# [GESP202509 一级] 商店折扣

## 题目描述

商店正在开展促销活动，给出了两种方案的折扣优惠。第一种方案是购物满 $x$ 元减 $y$ 元；第二种方案是直接打 $n$ 折，也就是说价格变为原先的 $n\\div 10$。这里的 $x, y, n$ 均是正整数，并且 $1 \\le y < x$，$1 \\le n < 10$。

需要注意的是，第一种方案中满减优惠只能使用一次。例如购物满 $10$ 元减 $3$ 元时，若挑选了价格总和为 $33$ 元的物品，只能减免 $3$ 元，需要支付 $30$ 元。

小明在商店挑选了价格总和为 $p$ 元的物品，结账时只能使用一种优惠方案。小明最少需要支付多少钱呢？

## 输入格式

四行，四个正整数 $x, y, n, p$，含义见题目描述。

## 输出格式

一行，一个小数，表示小明最少需要支付多少钱，保留两位小数。
`,
      template: `#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    // 在此填写代码\n    return 0;\n}`,
      answer: '',
      score: 25,
      explanation: `**解析：**

1. 计算方案一：若 \`p >= x\` 则 \`p1 = p - y\`，否则 \`p1 = p\`（满减只能用一次）
2. 计算方案二：\`p2 = p * n / 10.0\`（打 n 折）
3. 取两者最小值：\`min(p1, p2)\`
4. 输出保留两位小数：\`fixed << setprecision(2)\`

**关键点：** 满减只能用一次，不是每满 x 减 y。注意 p*n/10.0 要用浮点除法。`,
      tags: [LEVEL1_TAGS.basics, LEVEL1_TAGS.condition, LEVEL1_TAGS.operator],
    },
    {
      id: 27,
      type: 'programming',
      samples: [
        { input: `2`, output: `5` },
        { input: `5`, output: `55` }
      ],
      referenceCode: `#include <iostream>
using namespace std;
int main() {
    long long n;
    cin >> n;
    long long sum = 0;
    for(long long i = 1; i <= n; i++) {
        sum += i * i;
    }
    cout << sum << "\\n";
    return 0;
}`,
      question: `
# [GESP202509 一级] 金字塔

## 题目描述

金字塔由 $n$ 层石块垒成。从塔底向上，每层依次需要 $n \\times n, (n-1) \\times (n-1), \\cdots, 2 \\times 2, 1 \\times 1$ 块石块。请问搭建金字塔总共需要多少块石块？

## 输入格式

一行，一个正整数 $n$，表示金字塔的层数。

## 输出格式

一行，一个正整数，表示搭建金字塔所需的石块数量。
`,
      template: `#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    // 在此填写代码\n    return 0;\n}`,
      answer: '',
      score: 25,
      explanation: `**解析：**

1. 读入层数 n
2. 循环 i 从 1 到 n，累加 \`i * i\` 到 sum
3. 输出 sum

**关键点：** 结果可能很大，用 long long 避免溢出。公式为 1²+2²+...+n²，也可用公式 n(n+1)(2n+1)/6 直接计算。`,
      tags: [LEVEL1_TAGS.loop, LEVEL1_TAGS.operator],
    }
  ]
};
