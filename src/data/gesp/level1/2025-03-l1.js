import { LEVEL1_TAGS } from './shared.js';

export const paperData = {
  id: '2025-03-l1',
  title: '2025年03月 GESP C++ 一级认证真题',
  level: 1,
  year: 2025,
  month: 3,
  session: 9,
  timeLimit: 5400,
  source: {
    officialPdf: '',
    type: 'public-recovery',
  },
  confidence: {
    answer: 1.0,
    statement: 1.0,
  },
  questions: [
    // 1-15 单选题
    {
      id: 1,
      type: 'single',
      question: `2025年春节有两件轰动全球的事件，一个是DeepSeek横空出世，另一个是贺岁片《哪吒2》票房惊人，入了全球票房榜。下面关于DeepSeek与《哪吒2》的描述成立的是( )。`,
      options: [
        '《哪吒2》是一款新型操作系统',
        'DeepSeek是深海钻探软件',
        '《哪吒2》可以生成新的软件',
        'DeepSeek可以根据《哪吒2》的场景生成剧情脚本'
      ],
      answer: 3,
      score: 2,
      explanation: `**答案：D (DeepSeek可以根据《哪吒2》的场景生成剧情脚本)**

**核心解析：**
1. DeepSeek 是中国开发的通用大语言模型（LLM），属于生成式人工智能（AIGC），核心能力是处理和生成自然语言文本。
2. 《哪吒2》是动画电影，本身是被动的视听内容产品，不具备主动生成软件或执行计算任务的能力。
3. DeepSeek 作为语言模型，可以接受用户输入的场景描述，经过语义理解和逻辑推理，输出剧情脚本文本——这正是大语言模型的典型应用场景。

**选项逐项分析：**
- **A (《哪吒2》是一款新型操作系统)**：❌ 错误。《哪吒2》是动画电影作品，不是操作系统。操作系统负责管理计算机硬件与软件资源，而电影是文化娱乐产品，两者在本质属性上完全不同。
- **B (DeepSeek是深海钻探软件)**：❌ 错误。DeepSeek 是人工智能公司开发的语言模型，与深海钻探毫无关联。“DeepSeek”意为“深度求索”，强调的是 AI 领域的深度学习与探索。
- **C (《哪吒2》可以生成新的软件)**：❌ 错误。电影是音视频内容载体，不具备编译执行环境，无法生成可运行的软件程序。
- **D (DeepSeek可以根据《哪吒2》的场景生成剧情脚本)**：✅ 正确。大语言模型具备强大的语义理解和文本生成能力，可以基于提供的场景信息创作剧本、脚本等创意文本内容。

**易错提醒：**
- 容易将电影中的“特效生成”与“代码生成”混淆，电影制作依赖专业动画软件，而非电影本身能生成软件。
- 部分考生可能对 DeepSeek 的定位不熟悉，需注意它本质是 AI 语言模型，而非工业或钻探类软件。

**知识延伸：**
生成式人工智能（AIGC）可应用于剧本创作、代码辅助、图像生成等多种场景，是近年来信息技术领域的热点方向。

**考点：** 当年前沿科技与热门文化时事常识辨析`,
    },
    {
      id: 2,
      type: 'single',
      question: `在某集成开发环境中编辑一个源代码文件时不可以执行下面（ ）操作。`,
      options: [
        '修改变量定义',
        '保存代码修改',
        '撤销代码修改',
        '插入执行截图'
      ],
      answer: 3,
      score: 2,
      explanation: `**答案：D (插入执行截图)**

**核心解析：**
1. 源代码文件（如 .cpp、.h）是纯文本文件（Plain Text），其物理存储结构仅由字符组成。
2. IDE 的代码编辑器本质是一个高级文本处理工具，负责处理字符流，能够执行：修改变量（编辑文本内容）、保存代码（写入磁盘文件）、撤销操作（维护 Undo 历史栈）。
3. 截图是二进制图像数据，若强行插入文本文件，会破坏文件结构，导致编译器无法解析代码。

**选项逐项分析：**
- **A (修改变量定义)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（可以执行）。修改变量本质是编辑文本，是 IDE 编辑器最基本的功能。
- **B (保存代码修改)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（可以执行）。保存操作将内存中的文本写入磁盘，是 IDE 的核心功能之一。
- **C (撤销代码修改)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（可以执行）。撤销操作通过维护操作历史栈实现文本回退，是现代编辑器的标配功能。
- **D (插入执行截图)**：✅ 正确（不可执行）。截图是二进制图像数据，无法嵌入纯文本文件；即便强行保存，也会破坏代码文件的语法结构，编译器将无法识别。

**易错提醒：**
- 容易混淆“IDE 编辑器”与“Word 文档”的功能范围，IDE 代码窗口只处理纯文本。
- 注意“执行截图”与“调试时查看变量值”是不同概念，IDE 可以显示变量信息，但这不是向源码中“插入图片”。

**知识延伸：**
C++ 源代码文件必须保持纯文本格式，任何二进制数据（图片、音视频、压缩包）都不能直接混入源码，否则会导致编译失败。

**考点：** 源代码的纯文本物理属性与 IDE 编辑器职能判定`,
    },
    {
      id: 3,
      type: 'single',
      question: `在C++中，下列可以做变量的是( )。`,
      options: [
        'Var-1',
        '$1',
        '%%1',
        '_Var_1'
      ],
      answer: 3,
      score: 2,
      explanation: `**答案：D (_Var_1)**

**核心解析：**
1. C++ 标识符（变量名、函数名等）必须以字母（A-Z、a-z）、下划线（_）或部分实现允许的通用字符开头。
2. 标识符的后续字符可以是字母、下划线、数字（0-9），不能包含算术运算符、标点符号或其他特殊字符。
3. 逐项检验各选项是否符合命名规范。

**选项逐项分析：**
- **A (Var-1)**：❌ 错误。减号 \`-\` 是算术减法运算符，不属于合法标识符字符集。标识符中不能包含运算符。
- **B ($1)**：❌ 错误。美元符号 \`$\` 在 C++ 标准中不属于合法标识符字符。虽然某些编译器（如 GCC）在特定模式下可能支持扩展，但根据 GESP 考试标准，这属于非法字符。
- **C (%%1)**：❌ 错误。百分号 \`%\` 是取模运算符（求余运算符），不是标识符组成部分。
- **D (_Var_1)**：✅ 正确。该标识符首字符为下划线 \`_\`（合法），后续字符为字母 \`Var\` 和数字 \`1\`，全部符合 C++ 标识符命名规范。

**易错提醒：**
- 下划线 \`_\` 是合法的，但数字 \`1\` 不能作为首字符出现。
- 注意区分“键盘上能打出的字符”与“C++ 标识符合法字符”，如 \`@\`、\`#\`、\`$\` 等虽然能输入，但并非都符合标准。
- 以单下划线或双下划线开头的标识符在某些情况下可能被编译器或标准库保留，应谨慎使用。

**知识延伸：**
C++ 标识符区分大小写，\`var\`、\`Var\`、\`VAR\` 是三个不同的标识符。此外，C++ 标准保留了一批以双下划线开头的标识符供编译器实现使用。

**考点：** C++ 标识符（Identifier）命名规则`,
    },
    {
      id: 4,
      type: 'single',
      question: `以下哪个是C++语言的关键字？（ ）`,
      options: [
        'abs',
        'cin',
        'do',
        'endl'
      ],
      answer: 2,
      score: 2,
      explanation: `**答案：C (do)**

**核心解析：**
1. C++ 关键字是语言语法层面定义的保留词，具有特定语法功能，编译器根据这些词汇构建语法树。
2. 关键字不能被用作变量名、函数名等用户自定义标识符。
3. 库函数名、标准流对象、操纵符等虽然功能明确，但它们属于标准库提供的内容，而非语言本身的关键字。

**选项逐项分析：**
- **A (abs)**：❌ 错误。\`abs\` 是 \`<cstdlib>\` 或 \`<cmath>\` 头文件中定义的库函数（求绝对值），属于标准库提供的函数名，不是语言关键字。
- **B (cin)**：❌ 错误。\`cin\` 是 \`<iostream>\` 中预定义的标准输入流全局对象，属于标准库设施，不是语言关键字。没有包含头文件或 \`using namespace std;\` 时，编译器不识别 \`cin\`。
- **D (endl)**：❌ 错误。\`endl\` 是流操纵符，用于输出换行并刷新缓冲区，属于 \`<iostream>\` 库提供的功能，不是语言关键字。
- **C (do)**：✅ 正确。\`do\` 是 C++ 的循环关键字，与 \`while\` 配合构成 do-while 循环语法结构，是编译器词法分析阶段识别的保留字。

**易错提醒：**
- 常见混淆：把库函数名、标准流对象误认为“关键字”。关键字是语言语法层面的概念，与标准库无关。
- \`do\` 作为关键字出现时必须与 \`while\` 配合使用，形成 \`do { ... } while (condition);\` 结构。
- 判断一个词是否为关键字，可以尝试将其用作变量名——若编译器报错“不能将关键字用作标识符”，则说明它是关键字。

**知识延伸：**
C++ 共有约 30+ 个关键字（如 \`if\`、\`else\`、\`for\`、\`while\`、\`switch\`、\`case\`、\`break\`、\`continue\`、\`return\`、\`int\`、\`float\`、\`double\`、\`char\`、\`void\`、\`class\`、\`public\`、\`private\` 等）。考生应熟悉这些保留词的名称和用途。

**考点：** C++ 原生关键字与标准库保留标识符的区分`,
    },
    {
      id: 5,
      type: 'single',
      question: `下面的框架在L1和L2标记的两行处分别填写选项中的代码，哪组不能通过编译（ ）。\n\`\`\`cpp\nint main () {\n int i = 0;\n ______________ // L1\n break;\n ______________ // L2\n return 0;\n}\n\`\`\``,
      options: [
        'do { (L1) 和 } while (i != 0); (L2)',
        'for (; i < 10; i++) { (L1) 和 } (L2)',
        'if (i == 0) { (L1) 和 } (L2)',
        'switch (i) { (L1) 和 } (L2)'
      ],
      answer: 2,
      score: 2,
      explanation: `**答案：C (if (i == 0) { (L1) 和 } (L2))**

**核心解析：**
1. \`break\` 语句的作用是立即终止当前最内层的循环（\`for\`、\`while\`、\`do-while\`）或 \`switch\` 语句的执行，并将控制权转移到结构之外。
2. \`break\` 语句必须出现在循环体内部或 \`switch\` 语句的 \`case\` 分支中，否则编译器无法找到它要“跳出”的目标结构，会报错。
3. 逐项分析各选项：在循环或 \`switch\` 结构中，\`break\` 处于其管辖范围内，语法正确；而 \`if\` 结构本身不提供“跳出”语义，\`break\` 放在 \`if\` 内会导致编译错误。

**选项逐项分析：**
- **A (do { (L1) 和 } while (i != 0); (L2))**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（能编译）。\`break\` 位于 \`do-while\` 循环体内部，属于循环结构的一部分，编译器允许 \`break\` 终止该循环。
- **B (for (; i < 10; i++) { (L1) 和 } (L2))**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（能编译）。\`break\` 位于 \`for\` 循环体内部，循环结构为其提供了合法的“跳出”目标。
- **C (if (i == 0) { (L1) 和 } (L2))**：✅ 正确。该项不符合题目限定，正是本题要选的项。正确（不能编译）。\`break\` 被放置在 \`if\` 分支中，但 \`if\` 不是循环结构或 \`switch\` 语句，\`break\` 无法找到有效的跳转目标，编译器报错“break statement not within loop or switch”。
- **D (switch (i) { (L1) 和 } (L2))**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（能编译）。\`break\` 位于 \`switch\` 语句块内部，可以用于终止 \`case\` 分支的继续执行（防止 fall-through），语法完全合法。

**易错提醒：**
- \`break\` 只能用于循环和 \`switch\`，不能用于 \`if\` 语句。即使 \`if\` 嵌套在循环内部，直接在 \`if\` 内使用 \`break\` 仍然是合法的（因为它实际跳出的是外层循环），但题目中 \`break\` 单独出现在 \`if\` 块中则不行。
- 区分“\`break\` 在 \`if\` 中”与“\`if\` 在循环中，\`break\` 在循环中某个 \`if\` 内”两种情况——后者是合法的。

**知识延伸：**
\`break\` 与 \`continue\` 都用于控制循环流程。\`break\` 完全跳出循环，\`continue\` 跳过本次循环剩余语句进入下一次迭代。两者都不能直接用于 \`if\` 语句（除非该 \`if\` 位于循环或 \`switch\` 内部）。

**考点：** \`break\` 语句的作用域约束与语境限制`,
    },
    {
      id: 6,
      type: 'single',
      question: `下面C++语句（ ）执行后的输出是 \`__ 0322\`（注：前面有空格）。`,
      options: [
        'printf("__ %2d%02d", 3, 22)',
        'printf("__ %02d%2d", 3, 22)',
        'printf("__ %02d%02d", 3, 22)',
        'printf("____ %02d%02d", 3, 22)'
      ],
      answer: 1,
      score: 2,
      explanation: `**答案：B (printf("__ %02d%2d", 3, 22))**

**核心解析：**
本题考察 \`printf\` 格式说明符的宽度控制与补零规则。目标输出为 \`__ 0322\`，其中前导空格和下划线是固定文本。

1. 格式字符串 \`"__ %02d%2d"\` 解析：
   - 固定文本 \`"__ "\`：原样输出两个下划线和一个空格。
   - \`%02d\` 针对第一个参数 3：宽度为 2，不足时左侧补 \`0\`，所以 3 输出为 \`"03"\`。
   - \`%2d\` 针对第二个参数 22：宽度为 2，22 本身已是两位，原样输出 \`"22"\`。

2. 拼接结果：\`"__ " + "03" + "22"\` = \`"__ 0322"\`。与目标完全吻合。

**选项逐项分析：**
- **A (printf("__ %2d%02d", 3, 22))**：❌ 错误。\`%2d\` 输出 3 时，3 不满 2 位，在左侧补 1 个空格，输出 \`" 3"\`；后面 \`%02d\` 输出 22 原样显示。最终得到 \`"__  322"\`，与目标不符（多出一个空格，且 3 前无 0）。
- **B (printf("__ %02d%2d", 3, 22))**：✅ 正确。\`%02d\` 使 3 补零为 \`"03"\`，\`%2d\` 使 22 原样输出 \`"22"\`，恰好得到 \`"__ 0322"\`。
- **C (printf("__ %02d%02d", 3, 22))**：❌ 错误。第一个 \`%02d\` 输出 3 为 \`"03"\` 正确，但第二个 \`%02d\` 也要求补零处理（虽然 22 不需补零，结果仍为 22），关键问题在于题目第二处期望的是普通占位 \`%2d\` 而非 \`%02d\`，格式说明符不匹配。
- **D (printf("____ %02d%02d", 3, 22))**：❌ 错误。格式字符串开头有四个下划线而非两个，与目标输出 \`__ 0322\` 的固定文本部分不符。

**易错提醒：**
- \`%2d\` 与 \`%02d\` 的区别在于补零：前者用空格补位，后者用 \`0\` 补位。混淆会导致前导字符错误。
- 逐个分析每个格式说明符对应的参数值，是避免错位的关键。

**知识延伸：**
\`printf\` 的格式说明符遵循"宽度 + 类型"规则，当实际数值位数超过宽度时，按原样输出；当不足时，按指定填充方式补位。

**考点：** \`printf\` 格式化输出中 \`%0nd\`（零填充）与 \`%nd\`（空格填充）的区别及参数对应关系`,
    },
    {
      id: 7,
      type: 'single',
      question: `有关下列C++代码的说法，错误的是( )。\n\`\`\`cpp\nprintf("我爱码代码！");\n\`\`\``,
      options: [
        '配对双引号内的汉字改为英文 Hello，C++代码能正确执行',
        '配对双引号内的汉字改为 Hello代码！，C++代码能正确执行',
        '代码中的每个双引号，都可以改为两个单引号',
        '代码中的每个双引号，都可以改为三个双引号'
      ],
      answer: 2,
      score: 2,
      explanation: `**答案：C (代码中的每个双引号，都可以改为两个单引号)**

**核心解析：**
本题考察 C++ 中字符串字面量（用双引号包裹）与字符常量（用单引号包裹）的本质区别。

\`printf("我爱码代码！");\` 中，双引号 \`" "\` 界定的是**字符串字面量**，其内容可以是任意文本，包括中文、英文、标点符号等，这些内容仅作为数据传递给 \`printf\` 函数输出，不会影响代码本身的正确性。

**选项逐项分析：**
- **A (配对双引号内的汉字改为英文 Hello，C++代码能正确执行)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（指该说法成立）。双引号内改为 \`"Hello"\` 后仍是合法的字符串字面量，\`printf\` 可以正确执行。该项本身正确，但非本题答案。
- **B (配对双引号内的汉字改为 Hello代码！，C++代码能正确执行)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（指该说法成立）。双引号内改为 \`"Hello代码！"\` 同样是合法的字符串字面量，代码能正确执行。该项本身正确，但非本题答案。
- **C (代码中的每个双引号，都可以改为两个单引号)**：✅ 正确。该项不符合题目限定，正是本题要选的项。正确（指该说法错误）。C++ 中单引号 \`' '\` 用于界定**字符常量**，只能包含一个字符（或一个转义序列）。将双引号改为两个单引号，如 \`'我爱码代码！'\` 是非法的——编译器会报错。单引号无法替代双引号表示字符串。该项表述本身错误，因此是本题要找的"错误说法"。
- **D (代码中的每个双引号，都可以改为三个双引号)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（指该说法不成立）。标准 C++ 中不存在三引号语法，\`"""\` 不是合法的字符串界定符，代码无法正确执行。该项本身错误，但非本题答案。

**易错提醒：**
- 字符串必须用双引号，单引号只能用于字符常量。这是 C++ 的基本语法规则，不可混淆。
- 题目要求选"错误的是"，所以要选那个本身表述错误的选项。

**知识延伸：**
在 C++ 中，\`"a"\` 是包含字符 \`'a'\` 和字符串结束符 \`'\\0'\` 的字符串（占 2 字节），而 \`'a'\` 是单个字符常量（占 1 字节）。两者在内存布局和类型上完全不同。

**考点：** 字符串字面量（双引号）与字符常量（单引号）的本质区别`,
    },
    {
      id: 8,
      type: 'single',
      question: `C++表达式 \`16 / 4 % 2\` 的值是( )。`,
      options: [
        '8',
        '4',
        '2',
        '0'
      ],
      answer: 3,
      score: 2,
      explanation: `**答案：D (0)**

**核心解析：**
本题考察 C++ 中除法运算符 \`/\` 与取模运算符 \`%\` 的优先级和结合性。这两个运算符优先级相同，均为左结合性，应从左到右依次计算。

1. 计算 \`16 / 4\`：整数除法，$16 \\div 4 = 4$，结果为整数 **4**。
2. 计算 \`4 % 2\`：取模运算，即求 4 除以 2 的余数。因为 $4 = 2 \\times 2 + 0$，余数为 **0**。
3. 最终结果：**0**。

**选项逐项分析：**
- **A (8)**：❌ 错误。如果误以为 \`/\` 和 \`%\` 是不同优先级且 \`%\` 先算，或将除法结果与取模混淆，可能得到此错误答案。
- **B (4)**：❌ 错误。只执行了除法 \`16 / 4 = 4\`，而未继续执行取模运算。
- **C (2)**：❌ 错误。如果错误地先计算 \`4 % 2 = 0\`，再计算 \`16 / 0\` 会导致除零错误；或者混淆了运算顺序得到其他错误结果。
- **D (0)**：✅ 正确。严格遵循左结合性，先 \`16 / 4 = 4\`，再 \`4 % 2 = 0\`。

**易错提醒：**
- 算术运算符 \`/\` 和 \`%\` 优先级相同，必须从左到右计算，不能跳步或颠倒顺序。
- 取模运算 \`%\` 的结果是整除后的余数，当被除数是除数的整数倍时，余数为 0。

**知识延伸：**
在 GESP C++ 一级中，需记住：\`*\`、\`/\`、\`%\` 三者优先级相同且左结合；\`+\`、\`-\` 优先级较低，同样左结合。

**考点：** 同优先级算术运算符的左结合性，以及整数除法与取模运算的执行顺序`,
    },
    {
      id: 9,
      type: 'single',
      question: `N是C++的正整数，值为12，则 \`cout << (N % 3 + N / 5)\` 的输出是( )。`,
      options: [
        '6.4',
        '2.4',
        '6',
        '2'
      ],
      answer: 3,
      score: 2,
      explanation: `**答案：D (2)**

**核心解析：**
本题综合考察取模运算 \`%\` 与整数除法 \`/\` 在 C++ 中的行为。代入 $N = 12$ 逐步计算：

1. 计算 \`N % 3\` 即 \`12 % 3\`：
   - 12 能被 3 整除（$12 = 3 \\times 4 + 0$），余数为 **0**。

2. 计算 \`N / 5\` 即 \`12 / 5\`：
   - 这是整数除法，$12 \\div 5 = 2.4$。
   - C++ 的整数除法**向下取整**（更准确地说，是向零取整），小数部分被截断，只保留整数部分 **2**。

3. 合并结果：\`0 + 2 = 2\`。

**选项逐项分析：**
- **A (6.4)**：❌ 错误。误以为整数除法会保留小数结果，或者混淆了浮点数除法与整数除法的规则。
- **B (2.4)**：❌ 错误地认为 \`12 / 5\` 的结果是 2.4 而未截断，或误以为 \`12 % 3\` 有小数部分。
- **C (6)**：❌ 错误。可能将取模结果误当作商来使用（如 $12 \\div 3 = 4$，再加上 $12 \\div 5 = 2$，得到 $4 + 2 = 6$），或者混淆了 \`%\` 和 \`/\` 的含义。
- **D (2)**：✅ 正确区分了 \`%\` 得到余数 0，\`/\` 得到截断后的整数 2。

**易错提醒：**
- C++ 中整数除法 \`/\` 会**截断小数部分**，不会四舍五入，也不会保留小数格式。\`12 / 5\` 的结果不是 2.4，而是 2。
- \`%\` 是取模（求余数），不是取商。当被除数能被除数整除时，余数为 0。

**知识延伸：**
整数除法的截断特性在循环模拟（如每隔 N 个处理一次）、判断倍数关系等场景中经常用到。例如 \`year % 4 == 0\` 可用于判断闰年。

**考点：** C++ 整数除法的截断特性与取模运算的本质区别`,
    },
    {
      id: 10,
      type: 'single',
      question: `下面C++代码执行后的输出是( )。\n\`\`\`cpp\nint N = 10;\nprintf("{N}*{N}={%d*%d}", N, N, N * N);\n\`\`\``,
      options: [
        '10*10={10*10}',
        '100=10',
        'N*N=100',
        '{N}*{N}={10*10}'
      ],
      answer: 3,
      score: 2,
      explanation: `**答案：D ({N}*{N}={10*10})**

**核心解析：**
本题考察 \`printf\` 函数中格式字符串与参数列表的对应关系。核心规则是：格式字符串中的每个 \`%d\` 占位符依次对应参数列表中的参数，多余的参数会被忽略。

1. 分析格式字符串 \`" {N}*{N}={%d*%d} "\`：
   - \`{N}\` 和 \`{N}\` 是**普通文本**，不是变量替换，会原样输出。
   - 共有 **2 个** \`%d\` 占位符。

2. 分析参数列表：\`N, N, N * N\`
   - 第一个参数：N = 10
   - 第二个参数：N = 10
   - 第三个参数：N * N = 100

3. 对应关系：
   - 第一个 \`%d\` → 第一个参数 10
   - 第二个 \`%d\` → 第二个参数 10
   - 第三个参数 100 没有对应的占位符，被**忽略**。

4. 最终输出：\`{N}*{N}={10*10}\`。

**选项逐项分析：**
- **A (10*10={10*10})**：❌ 错误。误以为 \`{N}\` 会被替换为 10，或格式字符串本身写错了。
- **B (100=10)**：❌ 错误。如果只注意到两个 \`%d\` 对应前两个 10，误认为格式是 \`"%d=%d"\` 之类的变形，会得到此结果。
- **C (N*N=100)**：❌ 错误。混淆了 \`printf\` 的占位规则，误以为 \`{%d}\` 或 \`{N}\` 等会进行变量替换或计算。
- **D ({N}*{N}={10*10})**：✅ 正确识别了 \`{N}\` 为普通文本，\`%d\` 占位符依次对应前两个 N（都是 10），第三个参数 100 被忽略。

**易错提醒：**
- \`printf\` 中的 \`{N}\` 不会进行任何变量替换，它只是普通的大括号字符。
- 参数数量可以多于占位符数量，但多余的参数不会影响输出；反之，如果参数少于占位符，则会导致未定义行为。
- 格式字符串中的文字内容（如 \`{\` 和 \`}\`）原样输出，需仔细审题。

**知识延伸：**
\`printf\` 的格式字符串是"控制指令"，其中 \`%\` 开头的才是占位符，其他所有字符（包括空格、中文字符、大括号等）都作为普通文本原样输出。

**考点：** \`printf\` 格式字符串中占位符与参数的一一对应原则，以及非占位符文本的原样输出特性`,
    },
    {
      id: 11,
      type: 'single',
      question: `执行下面的C++代码，在键盘上先后输入100和200，输出是（ ）。\n\`\`\`cpp\nint first, second;\ncin >> first >> second;\ncout << (first / second * second) << endl;\n\`\`\``,
      options: [
        '200',
        '100',
        '1',
        '0'
      ],
      answer: 3,
      score: 2,
      explanation: `**答案：D (0)**

**核心解析：**
1. 读取输入：\`first = 100\`, \`second = 200\`。
2. 计算 \`first / second\`：执行整数除法 \`100 / 200\`。在 C++ 中整数除法结果取整，因此 \`100 / 200 = 0\`（小数部分被截断）。
3. 计算 \`0 * second\`：无论乘数多大，\`0 × 200 = 0\`。
4. 最终输出结果为 **0**。

**选项逐项分析：**
- **A (200)**：❌ 错误。该选项误认为 \`first / second\` 会保持 \`second\` 的值，或混淆了乘除法优先级。实际上整数除法已归零，后续乘法无法恢复。
- **B (100)**：❌ 错误。该选项可能是将 \`first / second * second\` 错误理解为 \`first\`，但实际运算顺序是从左到右，左侧结果已是 0。
- **C (1)**：❌ 错误。该选项可能是误以为 \`100 / 200 ≈ 0.5\`，再乘以 200 取整得到 1。但 C++ 整数除法直接截断，不存在四舍五入。
- **D (0)**：✅ 正确。整数除法 "大除小" 直接得到 0，链式传递导致最终结果为 0。

**易错提醒：**
- 整数除法的截断是单向的：一旦得到 0，后续任何运算都无法恢复原始信息。
- 不能用数学实数运算的经验套用到 C++ 整数运算上。

**知识延伸：**
在 GESP 二级中，整数除法的截断特性还会在取余运算 \`(a / b) * b + (a % b) == a\` 的验证中出现，需牢记商和余数的关系。

**考点：** 整数除法的截断规则与表达式求值顺序`,
      tags: [LEVEL1_TAGS.operator]
    },
    {
      id: 12,
      type: 'single',
      question: `下面C++代码执行后，将输出能被2整除且除以7余数为2的数。下列选项不能实现的是（ ）。\n\`\`\`cpp\nfor (int i = 0; i < 100; i++)\n if (_______________________)\n cout << i << " ";\n\`\`\``,
      options: [
        '((i % 2 == 0) && (i % 7 == 2))',
        '((!(i % 2)) && (i % 7 == 2))',
        '((!(i % 2)) && (!(i % 7)))',
        '((i % 2 != 1) && (i % 7 == 2))'
      ],
      answer: 2,
      score: 2,
      explanation: `**答案：C (((!(i % 2)) && (!(i % 7))))**

**核心解析：**
题目要求筛选条件：**能被 2 整除**（即 \`i % 2 == 0\`）**且除以 7 余数为 2**（即 \`i % 7 == 2\`）。

选项 A、B、D 的条件均可等价表达为上述要求，而选项 C 的后半部分条件不满足"余数为 2"这一要求。

**选项逐项分析：**
- **A (((i % 2 == 0) && (i % 7 == 2)))**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该选项直接使用相等比较判断偶数和余数，逻辑完全正确。
- **B (((!(i % 2)) && (i % 7 == 2)))**：❌ 错误。该项本身符合规则，因此不是本题要选的项。\`!(i % 2)\` 等价于 \`i % 2 == 0\`（因为当 \`i\` 为偶数时 \`i % 2 == 0\`，取反后为 true）。前半正确，后半符合要求。
- **C (((!(i % 2)) && (!(i % 7))))**：✅ 正确。该项不符合题目限定，正是本题要选的项。该选项本身错误。后半部分 \`!(i % 7)\` 等价于 \`i % 7 == 0\`，表示"能整除 7"，而非题目要求的"除以 7 余数为 2"。因此该选项不能实现题目要求。
- **D (((i % 2 != 1) && (i % 7 == 2)))**：❌ 错误。该项本身符合规则，因此不是本题要选的项。\`i % 2 != 1\` 等价于 \`i % 2 == 0\`（奇数取余为 1，偶数取余为 0），逻辑正确。

**易错提醒：**
- \`!(i % 2)\` 与 \`i % 2 == 0\` 等价，但 \`!(i % 7)\` 与 \`i % 7 == 2\` 不等价——前者判断是否整除，后者判断余数是否为特定值。
- 在复杂表达式中使用 \`!\` 时，务必明确其作用对象是哪个子表达式。

**知识延伸：**
\`!\`（逻辑非）运算符将非零值转为 false，零值转为 true。因此 \`!(i % 7)\` 的真值表恰好与 \`i % 7 == 0\` 相反，但与 \`i % 7 == 2\` 完全不同。

**考点：** 逻辑非运算符 \`!\` 与取模运算 \`==\` 的语义区分`,
    },
    {
      id: 13,
      type: 'single',
      question: `下面C++代码执行后输出是（ ）。\n\`\`\`cpp\nint tnt = 0;\nfor (int i = -1000; i < 1000; i++)\n tnt += i;\ncout << tnt << endl;\n\`\`\``,
      options: [
        '-1000',
        '0',
        '999',
        '1000'
      ],
      answer: 0,
      score: 2,
      explanation: `**答案：A (-1000)**

**核心解析：**
1. 循环范围：\`i\` 从 -1000 开始（含），到 \`i < 1000\` 结束（不含 1000），即参与运算的数为 \`[-1000, -999, ..., 0, ..., 999]\`，共 2001 个数。
2. 求和计算：将所有数配对相加。从 -999 到 999，每个正数都有对应的负数抵消，例如 \`(-999) + 999 = 0\`，\`(-998) + 998 = 0\`，依此类推。
3. 唯一剩余：所有配对均可抵消，最终只剩下没有配对对象的 \`-1000\`。
4. 结果：\`-1000 + 0 = -1000\`。

**选项逐项分析：**
- **A (-1000)**：✅ 正确。循环左闭右开，-1000 是唯一的"孤岛"，无法被任何数配对抵消。
- **B (0)**：❌ 错误。该选项忽略了 \`-1000\` 是奇数个元素中的唯一中心，未被抵消。若误以为所有数成对出现，则会错误得到 0。
- **C (999)**：❌ 错误。该选项可能误将循环终点理解为 999，并错误计算了部分和。
- **D (1000)**：❌ 错误。该选项可能混淆了循环的起点或终点，或将 1000 误认为参与了运算（实际上 1000 因 \`i < 1000\` 条件不满足而未参与）。

**易错提醒：**
- 循环条件 \`i < 1000\` 是严格小于，1000 本身不参与运算。
- 判定元素个数时，可使用公式：数量 = 终点 - 起点 = 1000 - (-1000) = 2000，但需注意含起点不含终点，实际为 2001 个。

**知识延伸：**
数列求和中的配对抵消法是 GESP 高频考点。在等差数列求和公式 \`S = n(a₁ + aₙ)/2\` 中，若数列为对称结构，配对抵消可快速简化计算。

**考点：** for 循环边界条件（含起点不含终点）与等差数列对称抵消`,
    },
    {
      id: 14,
      type: 'single',
      question: `下面C++代码执行后输出的是（ ）。\n\`\`\`cpp\nint i;\nfor (i = 1; i < 100; i += 5)\n continue;\ncout << i << endl;\n\`\`\``,
      options: [
        '101',
        '100',
        '99',
        '96'
      ],
      answer: 0,
      score: 2,
      explanation: `**答案：A (101)**

**核心解析：**
1. 循环初始化：\`i = 1\`。
2. 循环条件：\`i < 100\`，每次步进 \`i += 5\`。
3. 追踪 \`i\` 的变化序列：\`1 → 6 → 11 → 16 → ... → 91 → 96 → 101\`
4. 关键步骤：
   - 当 \`i = 96\` 时，检查 \`96 < 100\` 为 true，执行循环体。循环体内只有 \`continue\`，直接跳转到步进表达式。
   - 执行 \`i += 5\`，\`i\` 变为 **101**。
   - 下一轮检查 \`101 < 100\`，结果为 false，**循环终止**。
5. \`continue\` 语句的作用是跳过本次循环剩余语句并进入下一次迭代，它不会终止循环或改变循环变量的最终值。
6. 循环结束后，\`i\` 的值为 **101**，输出结果即为 101。

**选项逐项分析：**
- **A (101)**：✅ 正确。\`continue\` 仅跳过本次循环的剩余语句，不阻止步进表达式执行。循环退出时的 \`i\` 值为导致条件判断失败的第一个值。
- **B (100)**：❌ 错误。该选项可能是将 \`i < 100\` 误认为"小于等于 100"，或认为循环会在某次迭代后刚好停在 100。
- **C (99)**：❌ 错误。该选项可能是误以为循环会在 \`i = 99\` 时退出，但 99 满足 99 < 100，循环会继续执行。
- **D (96)**：❌ 错误。该选项可能是误认为 \`continue\` 会直接终止循环，或认为 96 是最后一个满足条件的值。实际上 96 之后还会执行步进。

**易错提醒：**
- \`continue\` 只跳过当前迭代的剩余语句，步进表达式 \`i += 5\` 仍会在本次迭代末尾执行。
- for 循环变量的最终值是**导致条件为假的第一个值**，而非最后参与循环体的值。

**知识延伸：**
在 GESP 二级中，循环变量在循环结束后的值是一个常见考点。无论循环是正常结束还是被 \`break\` 终止，变量都保留退出时的状态。

**考点：** for 循环执行流程、continue 语句作用、循环终止条件与变量最终值`,
    },
    {
      id: 15,
      type: 'single',
      question: `下面C++代码执行后输出的是（ ）。\n\`\`\`cpp\nint tnt = 0;\nfor (int i = 5; i < 100; i += 5){\n if (i % 2 == 0) continue;\n tnt += 1;\n if (i % 3 == 0 && i % 7 == 0) break;\n}\ncout << tnt << endl;\n\`\`\``,
      options: [
        '500',
        '450',
        '10',
        '1'
      ],
      answer: 2,
      score: 2,
      explanation: `**答案：C (10)**

**核心解析：**
1. 循环结构：外层 for 循环遍历 \`i\` 从 5 到 99，每次步进 5。即 \`i\` 依次为 \`5, 10, 15, 20, ..., 95\`。
2. 第一层过滤（continue）：
   - \`if (i % 2 == 0) continue;\`
   - 5 的倍数中，尾数为 0、2、4、6、8 的是偶数（如 10, 20, 30...），被跳过。
   - 尾数为 5 的 5 的倍数是奇数（如 5, 15, 25...），进入下一层。
3. 有效数字序列：5, 15, 25, 35, 45, 55, 65, 75, 85, 95。共 **10 个**。
4. 第二层过滤（break）：
   - \`if (i % 3 == 0 && i % 7 == 0) break;\`
   - 即检查 \`i\` 是否为 21 的倍数（3×7=21）。
   - 在 5~95 范围内，21 的倍数有 42, 63, 84。但这些数都是偶数（能被 2 整除），在第一层过滤时已被 continue 跳过。
   - 因此 break 条件在循环范围内**不会触发**，循环会完整执行 10 次后自然结束。
5. 累加过程：每次通过两层过滤，\`tnt += 1\`，最终 \`tnt = 10\`。

**选项逐项分析：**
- **A (500)**：❌ 错误。该选项可能是将 5~99 的 5 的倍数个数（19 个）误认为 500，或进行了错误的累加运算。
- **B (450)**：❌ 错误。该选项可能是计算了所有 5 的倍数的某个特征值，或混淆了累加次数与变量初始值。
- **C (10)**：✅ 正确。只有 10 个尾数为 5 的 5 的倍数通过第一层过滤，且 break 条件不会触发。
- **D (1)**：❌ 错误。该选项可能是只统计了第一个有效数字（5），或误认为 break 在第一次有效迭代时就触发了。

**易错提醒：**
- continue 和 break 的作用范围不同：continue 只跳过本次循环剩余语句并进入下一次迭代，break 才是真正终止循环。
- 判断 break 是否触发时，需考虑条件是否在实际参与运算的值范围内可能满足。

**知识延伸：**
多重过滤结构的分析需要逐层递进。本题中 continue 先排除偶数，break 再检查特定倍数，但后者在前者过滤后的有效范围内不会触发——这种"条件陷阱"设计在 GESP 各级别中均有出现。

**考点：** 多重控制流（continue/break）叠加分析、整除与取余的综合应用`,
    },

    // 16-25 判断题
    {
      id: 16,
      type: 'judge',
      question: `C++、Python都是高级编程语言，它们的每条语句最终都要通过机器指令来完成。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
这是一道关于编程语言执行原理的常识题。无论是 C++ 还是 Python，均属于高级编程语言，其设计初衷是让人类更容易理解和编写程序。然而，CPU（中央处理器）只能识别并执行机器指令（二进制代码，如 0 和 1 组成的指令序列）。

- **编译型语言（如 C++）**：源代码经过编译器翻译，生成可直接被 CPU 执行的机器码（或目标文件，经链接后成为可执行文件）。
- **解释型语言（如 Python）**：源代码由解释器逐行（或按模块）读取并转换为中间字节码或直接翻译为机器指令后执行。

无论采用哪种执行方式，高级语言的每一条语句，最终都必须转化为机器指令，才能驱动计算机的硬件完成具体操作。这是计算机科学的基本原理。

**易错提醒：**
- 容易误以为"解释型语言不需要机器指令"，实际上解释器本身也是由机器指令构成的，只是翻译过程在运行时发生。
- 编译型与解释型的区别在于翻译时机（编译时 vs 运行时），而非是否依赖机器指令。

**知识延伸：**
C++ 采用编译执行模式，执行效率通常高于解释型语言；Python 采用解释执行模式，开发效率高但运行效率相对较低。这是 GESP 二级可能涉及的考点。

**考点：** 高级语言与机器指令的关系、编译型与解释型语言的基本概念`,
    },
    {
      id: 17,
      type: 'judge',
      question: `在C++中表达式 \`N * 2 % N\` 中如果N的值为正整数，则其值为2。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
分析表达式 \`N * 2 % N\`，其中 N 为正整数。

取模运算 \`%\` 的结果是左操作数除以右操作数的**余数**。当被除数是除数的**倍数**时，余数为 0。

对于任意正整数 N：
- \`N * 2\` 是 N 的 2 倍，即 N 整除 N * 2
- 因此 \`(N * 2) % N\` 的余数为 0

举例验证：
- 当 N = 1：\`1 * 2 % 1 = 2 % 1 = 0\`
- 当 N = 3：\`3 * 2 % 3 = 6 % 3 = 0\`
- 当 N = 10：\`10 * 2 % 10 = 20 % 10 = 0\`

命题声称值为 2，这与数学事实不符，故判断为错误。

**易错提醒：**
- 切勿将 \`N * 2 % N\` 与 \`2 % N\` 混淆。后者在 N > 2 时才等于 0 或 2。
- 看到"倍数"关系时应立即联想到取模结果为 0，而非其他值。

**知识延伸：**
取模运算有一个重要性质：若 a 是 b 的倍数，则 a % b = 0。这是 GESP 一级高频考点，常与循环条件、数组索引等结合出题。

**考点：** 取模运算的基本性质、倍数关系下的取模结果`,
    },
    {
      id: 18,
      type: 'judge',
      question: `执行如下C++代码如果在键盘上输入10，执行后将输出20。\n\`\`\`cpp\nchar N;\ncin >> N;\nprintf("%d\\n", N * 2);\n\`\`\``,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
本题涉及 C++ 中 \`char\` 类型的数据读取特性和 ASCII 码机制。逐步分析如下：

1. **变量类型声明**：\`char N;\` 将 N 定义为字符型变量。

2. **输入过程**：当从键盘输入 \`10\` 时，\`cin >> N\` 只会读取**第一个字符**，即字符 \`'1'\`，而非字符串 \`"10"\` 或整数 \`10\`。这是因为 \`cin\` 根据变量的类型来决定读取多少数据——\`char\` 类型只匹配单个字符。

3. **字符的数值含义**：字符 \`'1'\` 在计算机中以 ASCII 码形式存储，其数值为 **49**（'0' 的 ASCII 码是 48，'1' 是 49，以此类推）。

4. **运算与输出**：执行 \`N * 2\` 即 \`49 * 2 = 98\`，最终通过 \`printf("%d\\n", ...)\` 输出整数 **98**。

因此，程序输出的不是 20，而是 98，命题与实际执行结果不符。

**易错提醒：**
- 不能想当然地认为 \`char\` 类型会读取完整的数字输入，它只读取单个字符。
- 进行字符的算术运算时，实际使用的是字符对应的 ASCII 码值，而非字符本身的视觉数字。

**知识延伸：**
\`char\` 类型在内存中占 1 字节（8 位），取值范围通常是 0~255（无符号）或 -128~127（有符号）。\`'A'\` 的 ASCII 码是 65，\`'a'\` 是 97，数字字符 \`'0'\` 到 \`'9'\` 的 ASCII 码依次是 48 到 57。

**考点：** \`char\` 类型的单字符输入特性、字符与 ASCII 码的数值关系`,
    },
    {
      id: 19,
      type: 'judge',
      question: `删除下面C++代码中的continue不影响程序的执行效果。\n\`\`\`cpp\nfor (int i = 0; i < 100; i++) {\n if (i % 2 == 0) {\n printf("偶数");\n continue;\n } else \n printf("奇数");\n}\n\`\`\``,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
分析代码结构与 \`continue\` 语句的作用：

\`\`\`cpp
for (int i = 0; i < 100; i++) {
    if (i % 2 == 0) {
        printf("偶数");
        continue;  // ← 此处
    } else
        printf("奇数");
}
\`\`\`

1. **循环逻辑**：循环遍历 i = 0 到 99，偶数时输出"偶数"，奇数时输出"奇数"。

2. **\`continue\` 的作用**：在循环体中，\`continue\` 跳过本次循环剩余语句，直接进入下一次循环的迭代。

3. **结构分析**：\`if-else\` 结构本身具有互斥性。当 \`i % 2 == 0\` 为真时，执行 \`printf("偶数")\` 后，程序流程必然到达整个 \`if\` 块的结束花括号，**绝不会**继续执行 \`else\` 分支的 \`printf("奇数")\`。

4. **删除 \`continue\` 的影响**：删除 \`continue\` 后，\`if\` 分支执行完 \`printf("偶数")\` 同样到达块结束，流程进入下一次循环，输出结果完全相同。

因此，\`continue\` 在此处属于冗余代码，删除后不影响程序执行效果。

**易错提醒：**
- \`continue\` 并非在任何位置都是冗余的。如果 \`if\` 块后面还有其他语句（非 \`else\` 分支的语句），则 \`continue\` 可能起到跳过后续代码的作用，此时不能随意删除。
- 本题的关键在于 \`if-else\` 结构本身已确保分支互斥，\`else\` 分支绝不可能在 \`if\` 条件为真时执行。

**知识延伸：**
\`break\` 用于跳出整个循环，\`continue\` 用于跳过本次循环剩余语句。这是 GESP 一级的常见考点，常在循环控制结构的综合题中出现。

**考点：** \`continue\` 语句的作用、循环中的分支互斥性、代码冗余判定`,
    },
    {
      id: 20,
      type: 'judge',
      question: `下面C++代码执行时将报错，因为 \`_\` 所在位置应该是变量名，而 \`_\` 不可以做变量名。\n\`\`\`cpp\nfor(int _ = 0; _ < 100; _++) cout<< "*" <<endl;\n\`\`\``,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
本题考查 C++ 标识符（Identifier）的命名规则。

C++ 中，标识符（即变量名、函数名等）的命名必须满足以下规则：
1. 由**字母**（A-Z, a-z）、**数字**（0-9）和**下划线**（\`_\`）组成。
2. 不能以**数字**开头。
3. 不能使用 C++ 关键字（如 \`int\`、\`for\`、\`while\` 等）。

题目代码中的 \`_\` 是**单一的下划线字符**，完全由合法字符组成，且以非数字字符（下划线）开头，因此它是一个合法的标识符。

C++ 标准库和许多代码实践中，\`_\` 常被用作：
- **不关心变量的占位符**（如 \`for(int _ = 0; _ < n; _++)\`）
- **私有成员的命名约定**（如 \`_member\`）

命题声称 \`_\` 不能作为变量名，这是对命名规则的误解，因此判断为错误。

**易错提醒：**
- 下划线 \`_\` 是合法的标识符字符，但以下划线开头的标识符在某些情况下可能与编译器内部实现或标准库产生冲突（这是更高级的考点，GESP 一级不要求掌握）。
- 标识符中可以包含下划线，但应避免使用连续下划线或以下划线开头加其他字符的命名风格。

**知识延伸：**
合法的标识符示例：\`_\`、\`_name\`、\`myVar\`、\`count2\`、\`MAX_VALUE\`。非法的标识符示例：\`2nd\`（以数字开头）、\`my-var\`（包含连字符）、\`class\`（关键字）。

**考点：** C++ 标识符的命名规则、下划线字符的合法性`,
    },
    {
      id: 21,
      type: 'judge',
      question: `下面C++代码被执行后，将先后输出3和5。\n\`\`\`cpp\nfor (int i = 3; i < 5; i += 2)\n printf("%d ", i);\n\`\`\``,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
让我们逐轮追踪这段 for 循环的执行过程：

1. **初始化阶段**：\`i = 3\`。
2. **第1轮循环**：条件 \`i < 5\` 为真（3 < 5），执行循环体，输出 \`3\`。
3. **第1轮结束后更新**：\`i += 2\`，\`i\` 变为 \`5\`。
4. **第2轮循环**：条件 \`i < 5\` 为假（5 不小于 5），循环终止。

因此，循环只执行了 1 次，只输出 \`3\`，并没有输出 \`5\`。虽然 \`i\` 确实经历过值 \`5\`，但该值从未被打印输出。命题称"将先后输出 3 和 5"，与实际执行结果不符。

**易错提醒：**
- 本题容易将 \`i\` 最终取到的值 \`5\` 误认为也会被输出，忽略 for 循环先判断条件再执行循环体的执行顺序。
- \`i < 5\` 是严格小于，不是小于等于，因此当 \`i\` 等于 \`5\` 时循环条件已经不满足。

**知识延伸：**
for 循环的执行顺序是：初始化 → 条件判断 → 循环体 → 更新表达式 → 条件判断 → ...，每次迭代都要重新判断条件是否满足。

**考点：** for 循环的执行顺序与循环终止条件判断`,
    },
    {
      id: 22,
      type: 'judge',
      question: `下面的C++代码执行后将先后输出7个true。\n\`\`\`cpp\nfor (int i = 0; i < 10; i++)\n cout << (i * 2 < i * i) << " ";\n\`\`\``,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
这道题涉及两个知识点：

**关于布尔值输出：** C++ 中 \`cout\` 默认会将 \`bool\` 类型值以整数形式输出，\`true\` 输出为 \`1\`，\`false\` 输出为 \`0\`。即使逻辑表达式 \`(i * 2 < i * i)\` 的结果为 \`true\`，屏幕上显示的也是数字 \`1\`，而不是字符串 \`true\`。

**关于表达式求值次数：** 即使忽略布尔输出的问题，题目声称输出"7个true"也是错误的。当 \`i = 0\` 时，\`0 * 2 < 0 * 0\` 即 \`0 < 0\` 为假；\`i = 1\` 时，\`2 < 1\` 也为假。只有当 \`i >= 2\` 时，\`i * 2 < i * i\` 才成立。不等式 \`2i < i²\` 等价于 \`i² - 2i > 0\`，即 \`i(i-2) > 0\`，解得 \`i > 2\` 或 \`i < 0\`。在 \`i\` 从 0 到 9 的范围内，共有 7 个值（2 到 8）使不等式成立，但这 7 个值对应的是 7 次输出 \`1\`，而不是 7 个 \`true\`。

**易错提醒：**
- 很多考生知道 \`cout << boolalpha\` 可以将布尔值输出为 \`true\`/\`false\`，但忽略了默认情况下是整数输出。
- 逻辑表达式 \`(i * 2 < i * i)\` 并非对所有 \`i\` 都成立，需要实际计算判断成立的范围。

**知识延伸：**
使用 \`cout << boolalpha;\` 可以让布尔值以 \`true\`/\`false\` 形式输出；如果希望恢复默认整数输出，使用 \`cout << noboolalpha;\`。

**考点：** cout 对 bool 类型的默认输出行为`,
    },
    {
      id: 23,
      type: 'judge',
      question: `在C++代码中，\`user_Name\`、\`_userName\`、\`user-Name\`、\`userName_\`都是合法的变量名。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
C++ 标识符（变量名）的命名规则是：只能由字母（包括大小写字母）、数字和下划线组成，且不能以数字开头。

- \`user_Name\`：合法，下划线可在任意位置
- \`_userName\`：合法，以下划线开头是允许的
- \`user-Name\`：非法，横线 \`-\` 是减法运算符，不能出现在标识符中，编译器会将其解析为减法运算
- \`userName_\`：合法，下划线可作为后缀

由于列表中包含非法标识符 \`user-Name\`，因此"都是合法的变量名"这一说法是错误的。

**易错提醒：**
- 容易混淆下划线 \`_\` 和连字符 \`-\` 的用法。下划线是标识符的有效字符，连字符不是。
- 以下划线开头的标识符虽然在语法上合法，但在实际编程中应避免使用以下划线开头的标识符，因为编译器或标准库可能已经使用或保留此类名称。

**知识延伸：**
C++ 标识符的完整规则：首字符可以是字母或下划线，后续字符可以是字母、数字或下划线。不能使用 C++ 关键字作为标识符。

**考点：** C++ 标识符命名规则`,
    },
    {
      id: 24,
      type: 'judge',
      question: `C++语言中continue语句可以来提前结束循环。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
\`continue\` 语句的作用是：跳过本次循环中 \`continue\` 之后的所有语句，直接进入下一次循环的迭代表达式和条件判断。它只能结束当前这一次循环的剩余部分，并不能终止整个循环。

举例说明：
\`\`\`cpp
for (int i = 0; i < 5; i++) {
    if (i == 2)
        continue;
    printf("%d ", i);
}
\`\`\`
当 \`i == 2\` 时，\`continue\` 跳过 \`printf\`，但循环仍然继续，\`i\` 会继续递增到 3、4，最终循环正常结束。程序输出：\`0 1 3 4\`（共 4 个数）。

如果要提前结束整个循环，应使用 \`break\` 语句，它可以直接退出循环体，终止所有后续迭代。

**易错提醒：**
- \`continue\` 和 \`break\` 的作用范围不同：\`continue\` 只影响当前这一次循环，\`break\` 影响整个循环结构。
- 两者都是控制流语句，但适用场景不同，需要根据实际需求选择。

**知识延伸：**
在嵌套循环中，\`break\` 和 \`continue\` 默认只影响包含它们的那一层循环。如果需要从内层循环直接跳到外层循环，需要使用 goto 或设置标志位等技巧。

**考点：** continue 语句与 break 语句的区别`,
    },
    {
      id: 25,
      type: 'judge',
      question: `C++中定义整型变量N，执行语句 \`scanf("%d", &N); cout << N / 3 * 5;\` 时输入3.6，则输出是5。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
这道题涉及两个关键知识点：\`scanf\` 的格式读取行为和整数除法的截断特性。

**第一步：输入解析**
执行 \`scanf("%d", &N);\` 时，用户输入 \`3.6\`。由于格式说明符是 \`%d\`（要求读取整数），\`scanf\` 会读取字符 \`3\`，遇到小数点 \`.\` 时发现不是合法整数字符，于是停止读取，将 \`3\` 赋值给变量 \`N\`，小数点和小数部分 \`6\` 留在输入缓冲区未被读取。

**第二步：表达式求值**
接下来执行 \`cout << N / 3 * 5;\`：
- 先计算 \`N / 3\`，即 \`3 / 3\`，由于 \`N\` 是整型，结果为整数 \`1\`。
- 再计算 \`1 * 5\`，结果为 \`5\`。
- 注意运算顺序是从左到右，\`(N / 3) * 5\` 不等于 \`N / (3 * 5)\`。

**第三步：输出结果**
\`cout\` 将整数 \`5\` 输出到屏幕，与命题描述完全一致。

**易错提醒：**
- 如果误以为 \`scanf("%d", &N)\` 会将 \`3.6\` 四舍五入或取整为 \`4\`，就会得出错误结论。\`%d\` 格式读取时，遇到非数字字符立即停止。
- 整数除法 \`3 / 3\` 的结果是 \`1\`，而不是 \`1.0\`，因为操作数都是整型。

**知识延伸：**
\`scanf\` 的 \`%d\` 格式会跳过前导空白字符，读取可选的正负号，然后连续读取数字字符，直到遇到非数字字符为止。读取到的数字串被转换为整数。

**考点：** scanf 的格式读取规则与整数除法运算`,
    },

    // 26-27 编程题
    {
      id: 26,
      type: 'programming',
      samples: [
        { input: `10\n2\n3`, output: `8` },
        { input: `5\n2\n4`, output: `3` }
      ],
      referenceCode: `#include <iostream>\nusing namespace std;\n\nint main() {\n    int n, x, y;\n    cin >> n >> x >> y;\n    int eaten = (y + x - 1) / x;\n    int remain = n - eaten;\n    if (remain < 0) remain = 0;\n    cout << remain << "\\n";\n    return 0;\n}`,
      question: `# [GESP202503 一级] 图书馆里的老鼠\n\n## 题目描述\n\n图书馆里有 $n$ 本书，不幸的是，还混入了一只老鼠，老鼠每 $x$ 小时能啃光一本书，假设老鼠在啃光一本书之前，不会啃另一本。请问 $y$ 小时后图书馆里还剩下多少本完整的书。\n\n## 输入格式\n\n三行，第一行一个正整数 $n$（$n \\le 10^5$），表示图书馆里书的数量；\n\n第二行，一个正整数 $x$（$x \\le 100$），表示老鼠啃光一本书需要的时间；\n\n第三行，一个正整数 $y$（$y \\le 10^7$），表示经过的总时间；\n\n## 输出格式\n\n一行，一个整数，表示 $y$ 小时后图书馆里还剩下多少本完整的书。`,
      template: `#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    // 在此填写代码\n    return 0;\n}`,
      answer: '',
      score: 25,
      explanation: `
      **解析：**
      这道题考验简单的整数除法和边界条件分析。由于老鼠吃书是连续的，所以哪怕一小部分被咬过也不算作好书。这意味着已经失去的书的数量为向上取整 \`ceil(y/x)\`，在 C++ 整数逻辑下对应公式为 \`(y + x - 1) / x\` 或直接使用条件判断。计算出总失去数量后从总数里剪除，若总数目全失则只保底 \`0\`。
      `,
      tags: [LEVEL1_TAGS.basics, LEVEL1_TAGS.condition, LEVEL1_TAGS.operator],
    },
    {
      id: 27,
      type: 'programming',
      samples: [
        { input: `5\n43\n58\n25\n67\n90`, output: `40\n60\n30\n70\n90` }
      ],
      referenceCode: `#include <iostream>\nusing namespace std;\n\nint main() {\n    int n;\n    cin >> n;\n    for(int i = 0; i < n; i++) {\n        int a;\n        cin >> a;\n        int rem = a % 10;\n        if (rem >= 5) a += 10 - rem;\n        else a -= rem;\n        cout << a << "\\n";\n    }\n    return 0;\n}`,
      question: `# [GESP202503 一级] 四舍五入\n\n## 题目描述\n\n四舍五入是一种常见的近似计算方法。现在，给定 $n$ 个整数，你需要将每个整数四舍五入到最接近的整十数。例如，$43$ 四舍五入后为 $40$，$58$ 四舍五入后为 $60$；如果个位是 $5$，向上取整，例如 $25$ 变为 $30$。\n\n## 输入格式\n\n共 $n+1$ 行，第一行，一个正整数 $n$（$n \\le 10^5$），表示接下来输入的整数个数。\n\n接下来 $n$ 行，每行一个整数 $a_i$（$\\le a_i \\le 10^9$），表示需要四舍五入的整数。\n\n## 输出格式\n\n$n$ 行，每行一个整数，表示每个整数四舍五入后的结果。`,
      template: `#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    // 在此填写代码\n    return 0;\n}`,
      answer: '',
      score: 25,
      explanation: `
      **解析：**
      可以使用取个位的方法处理：\`rem = a % 10\`。如果 \`rem >= 5\`，则需要进位（加上 \`10 - rem\`）；否则，直接舍去个位（减去 \`rem\`）。这里同样要注意处理使用循环结构，读入 n 然后进行多次计算。
      `,
      tags: [LEVEL1_TAGS.loop, LEVEL1_TAGS.basics],
    }
  ]
};
