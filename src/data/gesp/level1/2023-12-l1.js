import { LEVEL1_TAGS } from './shared.js';

export const paperData = {
  id: '2023-12-l1',
  title: '2023年12月 GESP C++ 一级认证真题',
  level: 1,
  year: 2023,
  month: 12,
  session: 4,
  timeLimit: 5400,
  source: {
    officialPdf: '',
    type: 'public-recovery',
  },
  confidence: {
    answer: 1.0,
    statement: 1.0,
  },
  questions: [
    // 1-15 单选题
    {
      id: 1,
      type: 'single',
      question: `下列不可做变量名的是( )。`,
      options: ['CCF_GESP', 'CCF-GESP', 'CCFGESP', 'ccfGesp'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (CCF-GESP)**

**核心解析：**
C++ 标识符（变量名、函数名等）的合法字符集有严格限制：只能由字母（大小写均可）、数字和下划线组成，且首字符不能为数字。

在本题四个选项中：
- **CCF_GESP**：❌ 错误。下划线 \`_\` 是合法字符
- **CCFGESP**：❌ 错误。纯字母组合，完全合法
- **ccfGesp**：纯字母组合，符合标识符规则 ✅
- **CCF-GESP**：❌ 错误。包含减号 \`-\`，而减号在 C++ 中是二元算术运算符（减法），不属于标识符合法字符集

编译器遇到 \`CCF-GESP\` 时，会将其解析为 \`CCF\` 减号 \`GESP\`，即两个标识符相减的表达式，而非一个整体变量名，因此该命名不合法。

**选项逐项分析：**
- **A (CCF_GESP)**：❌ 错误。该选项本身合法，下划线是标识符允许使用的字符。
- **B (CCF-GESP)**：✅ 正确。包含减号 \`-\`，减号是算术运算符而非标识符组成字符，因此不可用作变量名。
- **C (CCFGESP)**：❌ 错误。该选项本身合法，字母组合符合标识符命名规则。
- **D (ccfGesp)**：❌ 错误。该选项本身合法，字母组合符合标识符命名规则。

**易错提醒：**
- 初学者易混淆分隔符与标识符合法字符，以为下划线与减号都可用于分隔单词。实际上只有下划线 \`_\` 被允许出现在标识符中。
- 标识符命名中不能出现任何运算符字符，如 \`-\`、\`+\`、\`*\`、\`/\` 等。

**知识延伸：**
C++ 标识符命名规范是 GESP 一级高频考点，建议同时掌握：不能以数字开头、不能使用关键字作为标识符、区分大小写（如 \`sum\` 与 \`Sum\` 是不同变量）。

**考点：** C++ 标识符合法字符集约束`,
    },
    {
      id: 2,
      type: 'single',
      question: `C++表达式 10-3 * (2+1) % 10 的值是( )。`,
      options: ['0', '1', '7', '10'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (1)**

**核心解析：**
根据 C++ 运算符优先级规则（从高到低）：括号 \`()\` > 乘除取模 \`* / %\` > 加减 \`+ -\`。同级运算符遵循左结合律。

对表达式 \`10 - 3 * (2+1) % 10\` 逐步求值：
1. **第一步：括号** \`(2+1) = 3\`，原式化为 \`10 - 3 * 3 % 10\`
2. **第二步：乘除取模（左结合）**
   - 先算 \`3 * 3 = 9\`
   - 再算 \`9 % 10 = 9\`（9 除以 10 的余数为 9）
   - 此时表达式为 \`10 - 9\`
3. **第三步：加减**
   - \`10 - 9 = 1\`

最终结果为 \`1\`。

**选项逐项分析：**
- **A (0)**：❌ 错误。可能误认为先算乘法后取模与减法结合得到 0，但运算顺序应严格遵循优先级。
- **B (1)**：✅ 正确。严格按照优先级计算得到 1。
- **C (7)**：❌ 错误。可能误将表达式理解为 \`(10-3)*(2+1)%10 = 7*3%10 = 21%10 = 1\`，或算错某一步骤。
- **D (10)**：❌ 错误。可能忽略了所有运算，只保留了初始值 10。

**易错提醒：**
- 乘法 \`*\`、除法 \`/\`、取模 \`%\` 优先级相同，均高于加减，且遵循左结合律（从左到右计算）。
- 本题中 \`3 * 3 % 10\` 的正确计算顺序是先乘后模，而非先模后乘。

**知识延伸：**
取模运算 \`%\` 要求左右操作数均为整数，结果为两数相除的余数。在 GESP 后续级别中，取模常用于周期性问题的处理（如星期几、时间循环等）。

**考点：** 运算符优先级（括号 > 乘除模 > 加减）与左结合性`,
    },
    {
      id: 3,
      type: 'single',
      question: `小杨同学现在是上午10点，求 N 小时后的时间是几点（24小时制）。横线处应选 ( )。\n\`\`\`cpp\nint N;\ncin >> N;\ncout << ____________;\n\`\`\``,
      options: ['(10+N) % 12', '(10+N) % 24', '(10+N) / 24', '10+N'],
      answer: 1,
      score: 2,
      explanation: `**答案：B ((10+N) % 24)**

**核心解析：**
本题要求计算上午 10 点经过 N 小时后的时间（24小时制）。这是一个典型的周期性问题。

时间在 24 小时内循环：0 点到 23 点，之后回到 0 点。数学上使用取模运算 \`a % b\` 可以得到 a 除以 b 的余数，实现周期循环。

设起始时间为 10 点，经过 N 小时后总小时数为 \`10 + N\`。取模 24 后，结果范围恰好落在 \`[0, 23]\` 的合法小时区间内：
- 若 N = 5，则 \`(10+5) % 24 = 15\`，即下午 3 点 ✅
- 若 N = 15，则 \`(10+15) % 24 = 1\`，即凌晨 1 点 ✅
- 若 N = 24，则 \`(10+24) % 24 = 10\`，即回到 10 点 ✅

**选项逐项分析：**
- **A ((10+N) % 12)**：❌ 错误。使用模 12 适用于 12 小时制，本题要求 24 小时制。例如 N=15 时结果为 9，但正确应为凌晨 1 点（1 而非 9）。
- **B ((10+N) % 24)**：✅ 正确使用模 24 匹配 24 小时制的时间循环，无论 N 取何值都能得到 0-23 范围内的合法小时值。
- **C ((10+N) / 24)**：❌ 错误。整数除法得到的是经过的完整天数（0 或正整数），而非小时数。例如 N=25 时结果为 1，表示经过 1 天，而非当前小时。
- **D (10+N)**：❌ 错误。未进行取模处理。当 N 较大时会产生超过 23 的非法小时值，如 N=15 得 25，N=20 得 30，均非合法时间表示。

**易错提醒：**
- 混淆 12 小时制和 24 小时制的取模基数。做时间循环题时，必须明确题目要求的进制。
- 误用除法 \`a / 24\` 代替取模 \`a % 24\`：除法得到的是周期数，取模得到的才是当前周期的位置。

**知识延伸：**
取模运算 \`a % b\` 是处理周期性问题的核心工具。除本题的时间循环外，常见的应用场景还包括：星期几的计算（\`% 7\`）、角度循环（\`% 360\`）、数组索引循环等。

**考点：** 周期性现实问题的取模数学模型应用`,
    },
    {
      id: 4,
      type: 'single',
      question: `判断正整数 N 是否为偶数的正确表达式是( )。`,
      options: ['N % 2 == 0', 'N / 2 == 0', 'N % 2 = 0', 'N % 2 != 0'],
      answer: 0,
      score: 2,
      explanation: `**答案：A (N % 2 == 0)**

**核心解析：**
偶数的数学定义是：能被 2 整除的整数，即除以 2 的余数为 0。

在 C++ 中：
- \`%\` 是取模运算符，计算两数相除的余数
- \`==\` 是关系运算符，判断左右两边是否相等

因此表达式 \`N % 2 == 0\` 的含义是：N 除以 2 的余数等于 0，即 N 能被 2 整除，这正是偶数的判定条件。

**选项逐项分析：**
- **A (N % 2 == 0)**：✅ 正确表达了“余数为 0 即能被 2 整除”的偶数判定逻辑。
- **B (N / 2 == 0)**：❌ 错误。该表达式表示 N 除以 2 的**商**等于 0，这意味着 N 的绝对值小于 2（如 N=0、1、-1），与偶数判定无关。
- **C (N % 2 = 0)**：❌ 错误。使用了单等号 \`=\`，这是赋值运算符而非比较运算符。赋值表达式返回所赋的值（此处为 0），结果始终为假（如果编译器允许的话）。在 C++ 中这通常会导致编译错误，因为左操作数需为可赋值的左值。
- **D (N % 2 != 0)**：❌ 错误。该表达式判定 N 除以 2 的余数**不等于** 0，即 N 不能被 2 整除，这恰好是奇数的判定条件。

**易错提醒：**
- 区分 \`=\`（赋值）和 \`==\`（比较）：判断相等必须使用双等号 \`==\`，单等号 \`=\` 在比较表达式中是错误的（除非故意使用赋值结果进行判断）。
- 偶数判定用 \`== 0\`，奇数判定用 \`!= 0\`，两者互为否定关系。

**知识延伸：**
偶数判定 \`N % 2 == 0\` 是 C++ 中最基础的算术逻辑应用之一。在后续学习中，还会遇到：判定能否被 3 整除 \`N % 3 == 0\`、判定能否被 5 整除 \`N % 5 == 0\` 等扩展用法。

**考点：** 偶数判定的标准算术逻辑与算子辨析`,
    },
    {
      id: 5,
      type: 'single',
      question: `C++表达式 sqrt(9.0) 的结果是( )。`,
      options: ['3', '3.0', '9', '81'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (3.0)**

**核心解析：**
\`sqrt()\` 是 C++ 标准库 \`<cmath>\` 中定义的求平方根函数，其函数原型为：
\`\`\`cpp
double sqrt(double x);
\`\`\`
该函数接收一个 \`double\` 类型参数，返回值也是 \`double\` 类型。

分析 \`sqrt(9.0)\`：
1. 参数 \`9.0\` 是 \`double\` 类型常量
2. \`sqrt(9.0)\` 计算 9.0 的平方根，数学结果为 3.0
3. 函数返回 \`double\` 类型的 3.0

**关键点**：返回值类型由函数签名决定，而非由参数值或数学结果决定。虽然 \`sqrt(9.0)\` 的数学值是整数 3，但函数始终返回 \`double\` 类型的浮点数 3.0。

**选项逐项分析：**
- **A (3)**：❌ 错误。结果数值正确（3），但类型错误。\`sqrt\` 返回 \`double\` 类型，输出时若以整数形式显示可能截断，但题目问的是表达式的结果类型，应为浮点型。
- **B (3.0)**：✅ 正确。结果数值正确且类型匹配。\`sqrt\` 返回 \`double\`，9 的平方根在浮点表示下为 3.0。
- **C (9)**：❌ 错误。混淆了平方根与原数，\`sqrt(9.0)\` 求的是 9 的平方根，不是 9 本身。
- **D (81)**：❌ 错误。混淆了平方根与平方，\`sqrt(9.0)\` 的结果远小于 9，不可能是 81。81 是 9 的平方（\`9*9\`）。

**易错提醒：**
- 区分 \`sqrt\`（平方根）与 \`pow\`（幂函数）：\`sqrt(9)\` = 3，而 \`pow(9, 2)\` = 81。
- C++ 中浮点常量 \`3.0\` 与整型 \`3\` 是不同的数据类型，在类型要求严格的场合（如函数返回值）必须严格区分。

**知识延伸：**
\`<cmath>\` 库提供了丰富的数学函数，常见的有：\`abs()\`（整数绝对值）、\`fabs()\`（浮点绝对值）、\`pow(x, y)\`（x 的 y 次幂）、\`sin()\`、\`cos()\` 等。理解每个函数的参数类型和返回值类型是正确使用它们的基础。

**考点：** 数学库函数返回值类型的“浮点化”一致性原则`,
    },
    {
      id: 6,
      type: 'single',
      question: `下列哪个语句可以定义一个整型变量 a 并初始化为 10？`,
      options: ['int a = 10;', 'float a = 10;', 'char a = 10;', 'a = 10;'],
      answer: 0,
      score: 2,
      explanation: `**答案：A (int a = 10;)**

**核心解析：**
变量定义需要包含三个要素：
1. **数据类型**：指定变量存储什么类型的数据
2. **变量名**：变量的标识符
3. **初始化**（可选但推荐）：给变量一个初始值

本题要求定义一个**整型**变量 a 并初始化为 10。

- 选项 A \`int a = 10;\` 完整包含数据类型 \`int\`、变量名 \`a\`、初始值 \`10\`，符合要求 ✅

**选项逐项分析：**
- **A (int a = 10;)**：✅ 正确声明了一个 \`int\` 类型的变量 \`a\`，并初始化为 10。这是 C++ 中变量定义的标准形式。
- **B (float a = 10;)**：❌ 错误。该选项本身可以成功定义变量，但类型是 \`float\`（单精度浮点型），不是题目要求的整型变量。
- **C (char a = 10;)**：❌ 错误。该选项本身可以成功定义变量，但类型是 \`char\`（字符型），不是题目要求的整型变量。字符型虽然底层存储整数，但语义和用途与整型不同。
- **D (a = 10;)**：❌ 错误。缺少数据类型声明。这是赋值语句而非定义语句，在没有预先声明 \`a\` 的情况下会导致编译错误。

**易错提醒：**
- 定义变量时必须指定类型，仅写 \`a = 10;\` 是赋值而非定义。
- 选择类型时必须与题目要求严格匹配：\`int\` 是整型，\`float\`/\`double\` 是浮点型，\`char\` 是字符型。

**知识延伸：**
C++ 是一种强类型语言，每个变量都必须有明确的数据类型。常见的基本数据类型包括：\`int\`（整型）、\`float\`（单精度浮点）、\`double\`（双精度浮点）、\`char\`（字符）、\`bool\`（布尔）。变量定义时选择合适的数据类型是编程的基本功。

**考点：** 变量声明、定义与初始化的标准句法`,
    },
    {
      id: 7,
      type: 'single',
      question: `代码执行后的输出是 ( )。\n\`\`\`cpp\nint Sum = 0;\nfor (int i = 1; i <= 10; i++)\n Sum += i;\ncout << Sum;\n\`\`\``,
      options: ['45', '50', '55', '60'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (55)**

**核心解析：**
分析代码的循环逻辑：
\`\`\`cpp
int Sum = 0;          // Step 0: Sum = 0
for (int i = 1; i <= 10; i++)  // i 从 1 递增到 10（含 10）
    Sum += i;         // 每次循环将 i 的值累加到 Sum
cout << Sum;          // 输出最终 Sum
\`\`\`

逐次迭代过程：
| 循环次数 | i 的值 | 执行 Sum += i | Sum 当前值 |
|---------|--------|--------------|-----------|
| 初始化  | -      | -            | 0         |
| 第1次   | 1      | 0 + 1        | 1         |
| 第2次   | 2      | 1 + 2        | 3         |
| 第3次   | 3      | 3 + 3        | 6         |
| 第4次   | 4      | 6 + 4        | 10        |
| 第5次   | 5      | 10 + 5       | 15        |
| 第6次   | 6      | 15 + 6       | 21        |
| 第7次   | 7      | 21 + 7       | 28        |
| 第8次   | 8      | 28 + 8       | 36        |
| 第9次   | 9      | 36 + 9       | 45        |
| 第10次  | 10     | 45 + 10      | **55**    |

最终输出 \`55\`。

**选项逐项分析：**
- **A (45)**：❌ 错误。可能误以为循环范围是 1 到 9（不含 10），或者漏算了最后一次循环。
- **B (50)**：❌ 错误。计算有误。1+2+3+...+10 的正确结果是 55，不是 50。
- **C (55)**：✅ 正确。1 到 10 的自然数之和，利用等差数列公式或逐项累加均得 55。
- **D (60)**：❌ 错误。可能误以为循环到 11（1+2+...+11=66 的近似），或算错了求和结果。

**易错提醒：**
- \`i <= 10\` 包含 10，不是 \`i < 10\`。注意循环条件中的比较运算符是 \`<=\`（小于等于）还是 \`<\`（小于）。
- 本题是经典的等差数列求和问题：1+2+3+...+n 的和 = n*(n+1)/2 = 10*11/2 = 55。

**知识延伸：**
循环累加求和是 GESP 的高频题型。常见变式包括：累加奇数（1+3+5+...）、累加偶数（2+4+6+...）、累加特定数列等。理解循环边界（起始值、终止值、包含性）是解题关键。

**考点：** 计次循环（for循环）累加求和逻辑`,
    },
    {
      id: 8,
      type: 'single',
      question: `在 C++ 中，下列哪个运算符的优先级最高？`,
      options: ['+', '*', '&&', '!'],
      answer: 3,
      score: 2,
      explanation: `**答案：D (!)**

**核心解析：**
C++ 运算符优先级表（从高到低，取主要类别）：
1. **括号** \`()\` — 最高优先级
2. **一元运算符** \`!\` \`++\` \`--\` \`+\`（正） \`-\`（负）— 次高
3. **乘除取模** \`*\` \`/\` \`%\`
4. **加减** \`+\` \`-\`
5. **关系运算符** \`<\` \`<=\` \`>\` \`>=\`
6. **相等性运算符** \`==\` \`!=\`
7. **逻辑与** \`&&\`
8. **逻辑或** \`||\`
9. **赋值运算符** \`=\` 及其复合形式

本题四个选项中：
- \`!\`（逻辑非）属于一元运算符，优先级最高
- \`*\`（乘法）属于乘除模级别，优先级次之
- \`&&\`（逻辑与）属于逻辑运算符级别，优先级较低
- \`+\`（加法）属于加减级别，优先级最低

因此，优先级最高的是 \`!\`。

**选项逐项分析：**
- **A (+)**：❌ 错误。加法运算符属于加减级别，优先级较低。
- **B (*)**：❌ 乘法运算符属于乘除模级别，优先级低于一元运算符 \`!\`。
- **C (&&)**：❌ 错误。逻辑与运算符属于逻辑运算符级别，优先级低于算术运算符（一元运算和乘除模）。
- **D (!)**：✅ 正确。逻辑非是唯一的一元运算符，在给定的四个选项中优先级最高。

**易错提醒：**
- 一元运算符（如 \`!\`、\`++\`、\`--\`、负号 \`-\`）的优先级高于所有二元运算符（如 \`+\`、\`*\`、\`&&\` 等）。
- \`!\` 的优先级高于 \`&&\`，\`&&\` 的优先级高于 \`||\`。例如 \`!a && b\` 等价于 \`(!a) && b\`，而非 \`!(a && b)\`。

**知识延伸：**
记忆运算符优先级有技巧：一元（单目）运算符 > 乘除模 > 加减 > 移位 > 关系 > 相等 > 位与 > 位异或 > 位或 > 逻辑与 > 逻辑或 > 赋值。如果对优先级不确定，使用括号显式指定是最安全的方法。

**考点：** 运算符优先级中一元运算符的最高地位`,
    },
    {
      id: 9,
      type: 'single',
      question: `下列哪个函数用于在终端输出内容？`,
      options: ['cin', 'scanf', 'cout', 'abs'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (cout)**

**核心解析：**
C++ 标准库提供了一套完整的输入输出流机制，主要涉及两个核心对象：

- **\`cin\`**：标准输入流（Character Input），配合流提取运算符 \`>>\` 用于从键盘读取数据
- **\`cout\`**：标准输出流（Character Output），配合流插入运算符 \`<<\` 用于向屏幕输出数据

此外还有 \`cerr\`（标准错误流）和 \`endl\`（换行符）等辅助对象。

**选项逐项分析：**
- **A (cin)**：❌ 错误。\`cin\` 是输入流对象，用于从终端读取用户输入的数据，而非输出。
- **B (scanf)**：❌ 错误。\`scanf\` 是 C 语言标准库函数，用于格式化输入。虽然它可以用于输入，但它不属于 C++ 的流 I/O 体系，且本题语境下（其他选项均为 C++ 流对象）应选择 \`cout\`。
- **C (cout)**：✅ 正确。\`cout\` 是 C++ 标准输出流对象，用于将数据输出到终端显示。配合 \`<<\` 运算符，可输出各种类型的数据。
- **D (abs)**：❌ 错误。\`abs\` 是数学函数，用于计算整数的绝对值（如 \`abs(-5)\` 返回 5），与输入输出无关。

**易错提醒：**
- 区分 \`cin\` 和 \`cout\`：记忆技巧是 \`i\` 代表 input（输入），\`o\` 代表 output（输出）。数据从 \`cin\` 流进入程序，从 \`cout\` 流输出程序。
- \`cout\` 本身不是函数，而是全局流对象，使用时需要配合 \`<<\` 运算符。

**知识延伸：**
C++ 的 I/O 流机制是其标准库的重要特性。相比 C 语言的 \`printf\`/\`scanf\`，C++ 的流 I/O 具有类型安全、可扩展等优点。在 GESP 二级及以后，还会接触到文件流 \`fstream\`、字符串流 \`stringstream\` 等扩展应用。

**考点：** 标准 I/O 流对象的职能定位`,
    },
    {
      id: 10,
      type: 'single',
      question: `代码执行后的输出是 ( )。\n\`\`\`cpp\nint a = 5, b = 2;\nif (a / b > 2) cout << "OK";\nelse cout << "KO";\n\`\`\``,
      options: ['OK', 'KO', '2.5', '报错'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (KO)**

**核心解析：**
分析代码执行流程：
\`\`\`cpp
int a = 5, b = 2;
if (a / b > 2) cout << "OK";
else cout << "KO";
\`\`\`

**关键点：整数除法**
当两个整数相除时（如 \`5 / 2\`），C++ 执行整数除法，结果自动截断小数部分，只保留整数部分：
- \`a / b\` = \`5 / 2\` = \`2\`（而非 2.5）

**逐步执行：**
1. 计算 \`a / b\`：\`5 / 2\` = \`2\`（整数除法，向零截断）
2. 比较 \`2 > 2\`：\`false\`（2 不严格大于 2，两边相等）
3. 条件为假，执行 \`else\` 分支：输出 \`KO\`

**选项逐项分析：**
- **A (OK)**：❌ 错误。忽略了整数除法的截断特性，误以为 \`5/2 = 2.5 > 2\` 为真。
- **B (KO)**：✅ 正确。\`5/2\` 在整数除法下等于 2，不满足严格大于 2 的条件，因此执行 \`else\` 分支，输出 \`KO\`。
- **C (2.5)**：❌ 错误。整数除法不会产生小数结果。\`5/2\` 的结果是整数 2，不会输出 2.5。
- **D (报错)**：❌ 错误。代码语法完全正确，\`5/2\` 是合法的整数除法运算（虽然结果是整数），不会产生编译或运行错误。

**易错提醒：**
- C++ 中整数除以整数，结果仍是整数，小数部分被自动丢弃（向零截断）。这与数学中的除法不同。
- 严格大于 \`>\` 要求左值必须大于右值，相等的情况下判定为假。若题目改为 \`>=\`（大于等于），则 \`2 >= 2\` 为真，会输出 \`OK\`。

**知识延伸：**
若需要得到精确的小数结果（如 \`5/2 = 2.5\`），需要将操作数至少一个转换为浮点类型：\`5.0 / 2\` 或 \`5.0 / 2.0\` 或 \`(double)5 / 2\`。这种隐式/显式类型转换规则在 GESP 二级考试中是重要考点。

**考点：** 整数除法规律（截断特性）与关系判别控制流`,
    },
    {
      id: 11,
      type: 'single',
      question: `在 C++ 中，char 类型的变量占用的内存字节数是( )。`,
      options: ['1', '2', '4', '8'],
      answer: 0,
      score: 2,
      explanation: `**答案：A (1)**

**核心解析：**
C++ 的基本数据类型 \`char\` 是专门设计用来存储单个字符的。在 C++ 标准中，\`char\` 类型的大小被规定为恰好能容纳一个基本字符集的元素。在所有主流编译器（GCC、Clang、MSVC）实现中，\`char\` 固定占用 1 个字节（8 bit），这是语言规范的强制要求，与平台无关。

**选项逐项分析：**
- **A (1)**：✅ 正确。\`char\` 在内存中占 1 字节，这是 C++ 标准明确规定的最小存储单元。
- **B (2)**：❌ 错误。2 字节是 \`short\`（短整型）的典型大小。
- **C (4)**：❌ 错误。4 字节是 \`int\`（整型）的典型大小。
- **D (8)**：❌ 错误。8 字节是 \`long long\` 或 \`double\` 的典型大小。

**易错提醒：**
- 有学员误以为 \`char\` 存储汉字需要更多字节，实际上 \`char\` 只是单个字节的容器，汉字需要 \`wchar_t\` 或 UTF-8 编码下的多个 \`char\` 来表示。
- \`char\` 与 \`char*\`（字符指针）是不同类型，后者是指针类型，大小通常是 4 或 8 字节。

**知识延伸：**
C++ 中 \`sizeof(char)\` 的值始终为 1，\`sizeof\` 运算符返回的是字节数而非 bit 数。

**考点：** 基本数据类型 \`char\` 的内存占用标准`,
    },
    {
      id: 12,
      type: 'single',
      question: `二进制数 1101 转换为十进制数是( )。`,
      options: ['11', '12', '13', '14'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (13)**

**核心解析：**
二进制转十进制要使用“按权展开求和”。从右往左看，位权依次是 $2^0, 2^1, 2^2, 2^3$。

对二进制数 \`1101\` 展开：
1. 最右位：$1 \\times 2^0 = 1$
2. 次右位：$0 \\times 2^1 = 0$
3. 第三位：$1 \\times 2^2 = 4$
4. 最左位：$1 \\times 2^3 = 8$

因此结果为 $8 + 4 + 0 + 1 = 13$。

**选项逐项分析：**
- **A (11)**：❌ 错误。通常是把某一位的权值算低了，少加了 $2$。
- **B (12)**：❌ 错误。通常是遗漏最右边的 $1 \\times 2^0$。
- **C (13)**：✅ 正确。按权展开后求和为 $13$。
- **D (14)**：❌ 错误。通常是把中间的 $0 \\times 2^1$ 误当成了 $1 \\times 2^1$。

**易错提醒：**
- 位权一定从右往左从 $2^0$ 开始，不是从左往右数。
- 二进制 \`1101\` 不能按普通十进制数字相加，而要每一位乘以对应位权。

**知识延伸：**
二进制转十进制的逆过程是十进制转二进制，常用“除 2 取余、逆序排列”。四位二进制数的范围是 $0$ 到 $15$。

**考点：** 二进制到十进制的权值展开计算`,
    },
    {
      id: 13,
      type: 'single',
      question: `C++ 程序开发的最后一个步骤通常是( )。`,
      options: ['编辑', '编译', '运行/调试', '发布'],
      answer: 3,
      score: 2,
      explanation: `**答案：D (发布)**

**核心解析：**
软件开发遵循标准流程：编辑（编写源代码）→编译（将代码翻译为目标文件）→运行/调试（测试程序功能）→发布（将最终产品交付用户）。题干的“最后一个步骤”指的是所有开发测试工作完成后，将软件交付使用的环节，即“发布”。

**选项逐项分析：**
- **A (编辑)**：❌ 错误。编辑是开发的第一步，负责编写源代码。
- **B (编译)**：❌ 错误。编译是第二步，将源代码转换为机器语言。
- **C (运行/调试)**：❌ 错误。运行/调试是测试环节，在发布之前进行。
- **D (发布)**：✅ 正确。发布是将完成测试、确认无误的软件打包并交付给最终用户的最后一步。

**易错提醒：**
- 有学员误选“编译”，混淆了编译（生成可执行文件）与发布（交付产品）的区别。
- 开发流程的顺序是固定的，每个步骤都有其特定作用，不能颠倒。

**知识延伸：**
完整的开发流程还包括需求分析、设计、维护等环节。GESP 一级考察的是最简化的四步模型。

**考点：** 软件开发流程的基本阶段`,
    },
    {
      id: 14,
      type: 'single',
      question: `下列关于注释的说法，错误的是( )。`,
      options: [
        '注释不参与编译',
        '// 用于单行注释',
        '/* */ 用于多行注释',
        '注释必须写在代码之后'
      ],
      answer: 3,
      score: 2,
      explanation: `**答案：D (注释必须写在代码之后)**

**核心解析：**
本题要求选出“错误”的说法。A、B、C 三项均正确描述了 C++ 注释的特性。D 项“注释必须写在代码之后”是错误的，因为 C++ 注释的位置非常灵活，可以写在代码之前、之后、行首，甚至单独成行。

**选项逐项分析：**
- **A (注释不参与编译)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。注释是给程序员阅读的说明文字，编译器会忽略所有注释内容，不生成任何目标代码。
- **B (// 用于单行注释)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。\`//\` 是 C++ 的单行注释符号，从 \`//\` 到行尾的内容均为注释。
- **C (/* */ 用于多行注释)**：✅ 正确。\`/* ... */\` 包裹的内容可跨越多行，用于较长的说明。
- **D (注释必须写在代码之后)**：✅ 正确。错误（因此是本题答案）。注释可以写在代码之前（如函数说明）、单独成行，或在代码行的任意位置（如行尾注释）。

**易错提醒：**
- 本题是“找错误”的题目，容易误选其他选项，需仔细阅读题干。
- \`/* */\` 注释不能嵌套使用，但初学者常忽略这一细节。

**知识延伸：**
C++11 引入了 \`//\` 风格的注释，但 \`/* */\` 仍用于条件编译（如 \`#ifdef DEBUG\`）。

**考点：** C++ 注释的语法规则与灵活性`,
    },
    {
      id: 15,
      type: 'single',
      question: `典型的计算机体系结构采用的是( )。`,
      options: ['图灵机', '冯·诺依曼架构', '哈佛架构', '神经元架构'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (冯·诺依曼架构)**

**核心解析：**
1945 年冯·诺依曼提出的存储程序概念奠定了现代计算机的架构基础，其核心特点包括：存储程序（程序与数据共存于同一存储器）、二进制系统、顺序执行。这种架构被几乎所有通用计算机（个人电脑、服务器、工作站）采用。

**选项逐项分析：**
- **A (图灵机)**：❌ 错误。图灵机是计算理论的数学抽象模型，不是实际的计算机硬件架构。
- **B (冯·诺依曼架构)**：✅ 正确。现代计算机普遍采用冯·诺依曼架构，其“存储程序”概念是计算机发展的里程碑。
- **C (哈佛架构)**：❌ 错误。哈佛架构将程序存储器和数据存储器分开，主要应用于嵌入式系统和微控制器（如单片机），不属于“典型计算机体系”。
- **D (神经元架构)**：❌ 错误。神经元架构属于生物科学或前沿人工智能研究领域，并非现行计算机体系结构。

**易错提醒：**
- 哈佛架构与冯·诺依曼架构的区别在于存储器是否分离，这是常考的区分点。
- 图灵机是理论模型而非实际架构，不能混为一谈。

**知识延伸：**
冯·诺依曼瓶颈指的是 CPU 与内存之间数据传输速度受限的问题，这也是现代计算机架构优化的研究方向。

**考点：** 计算机体系结构主流标准`,
    },

    // 16-25 判断题
    {
      id: 16,
      type: 'judge',
      question: `C++ 是一种强类型语言，所有的变量都必须先定义后使用。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
C++ 是一门强类型、静态类型的编程语言。强类型意味着每 个变量都必须有明确的类型声明，且类型一旦确定就不能随意改变；静态类型意味着所有变量的类型检查在编译阶段完成。标准 C++ 语法要求变量必须先声明（指定类型和名称），后才能使用。编译器会检查所有变量引用是否有对应的定义声明，若未定义则报编译错误。这一规则区别于某些弱类型语言（如 Python、JavaScript）允许变量“即用即声明”的特性。

**易错提醒：**
- “先定义后使用”中的“定义”不仅包括声明类型，还包括为变量分配存储空间。
- 在同一作用域内，不能重复定义同名变量。

**知识延伸：**
C++11 引入了 \`auto\` 关键字，可自动推断变量类型，但仍需先声明后使用，只是类型由编译器推断得出。

**考点：** C++ 强类型语言特性与变量先定义后使用原则`,

    },
    {
      id: 17,
      type: 'judge',
      question: `逻辑表达式 true && false 的结果是 true。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
逻辑与运算符 \`&&\` 的真值规则是：仅当两侧操作数**同时为真**时，结果才为真。\`true && false\` 中，左操作数为真、右操作数为假，根据真值表，结果为 \`false\`。命题将结果描述为 \`true\`，与实际不符，因此判定为错误。

逻辑与 (\`&&\`) 真值表：
| A | B | A && B |
|---|---|--------|
| true | true | true |
| true | false | **false** |
| false | true | false |
| false | false | false |

**易错提醒：**
- 容易将“或运算”的概念套用到“与运算”上。逻辑或 (\`||\`) 才允许一侧为真即结果为真。
- \`&&\` 与按位与 \`&\` 不同，前者用于逻辑判断（短路求值），后者用于位运算。

**知识延伸：**
逻辑与和逻辑或具有“短路求值”特性：当左操作数已能确定结果时，右操作数不会被求值。例如 \`false && (1/0)\` 不会触发除零错误。

**考点：** 逻辑与运算符 \`&&\` 的真值规则`,

    },
    {
      id: 18,
      type: 'judge',
      question: `在 C++ 中，一条语句必须以分号结尾。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
在 C++ 语法中，分号 \`;\` 是语句结束的标志。编译器依靠分号来区分不同的语句。若缺少分号，编译器将无法正确判断语句边界，导致语法错误。分号的作用相当于自然语言中的句号，是语法层面的必需元素。例如：\`int a = 5;\` 和 \`a = a + 1;\` 都是完整的语句，必须以分号结尾。

**易错提醒：**
- 复合语句（用花括号 \`{}\` 包裹的语句块）内部每条语句仍需分号结尾，\`{}\` 本身不是语句结束标记。
- 宏定义、命名空间声明等特殊语法结构有时不以分号结尾，但这属于例外规则，不影响“语句需分号结束”的通用原则。

**知识延伸：**
C++ 中换行符不等于语句结束标记，因此可以将多条语句写在同一行（如 \`int a=1; int b=2;\`），也可以将一条语句跨多行书写。

**考点：** C++ 语句结束符分号的语法强制性`,

    },
    {
      id: 19,
      type: 'judge',
      question: `010 在 C++ 中表示一个八进制数。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
C++ 整数常量会根据前缀判断进制：普通数字默认十进制，以 \`0\` 开头的整数常量按八进制解释，以 \`0x\` 或 \`0X\` 开头的整数常量按十六进制解释。

\`010\` 以 \`0\` 开头，所以它确实表示一个八进制数。若换算成十进制，它的值是 \`1 * 8^1 + 0 * 8^0 = 8\`，但本题判断的是“是否表示八进制数”，因此说法正确。

**易错提醒：**
- \`010\` 不是十进制 10，而是八进制写法，对应十进制值 8。
- 八进制每一位只能是 0 到 7，\`08\` 或 \`09\` 不是合法的八进制整数常量。

**知识延伸：**
C++14 起还支持 \`0b\` 或 \`0B\` 前缀表示二进制整数常量；十六进制则使用 \`0x\` 或 \`0X\` 前缀。

**考点：** C++ 整数常量的进制前缀规则`,

    },
    {
      id: 20,
      type: 'judge',
      question: `for 循环一定会有结束条件，否则会无限执行。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
\`for\` 循环的标准语法结构为 \`for(初始化; 条件判断; 更新表达式)\`。其中“条件判断”部分（第二个表达式）控制循环是否继续执行。若该条件表达式恒为真（如 \`i >= 0\` 且 \`i\` 不断增大），或缺少使条件变为假的更新逻辑，循环将永远不会结束，形成“死循环”。因此，逻辑上有效的 \`for\` 循环**必然**包含一个最终能使条件变为假的机制，否则程序将无限执行直到被强制终止。

**易错提醒：**
- “结束条件”不一定是显式的判断表达式，也可能是更新表达式导致循环变量溢出，从而改变条件结果。
- 即使循环体中有 \`break\` 语句，那也是一种“跳出条件”机制，本质上仍是循环结束的一种方式。

**知识延伸：**
死循环在某些场景下是有意为之（如服务器主循环、事件处理框架），但前提是内部有其他机制（如信号处理）来终止程序。

**考点：** \`for\` 循环的结束条件与循环收敛性`,
      tags: [LEVEL1_TAGS.loop]
    },
    {
      id: 21,
      type: 'judge',
      question: `continue 语句用于跳出整个循环。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
\`continue\` 语句的作用是**跳过本次循环剩余的语句，直接进入下一次循环条件的判断**。它并不终止整个循环体。例如在 \`for\` 循环中遇到 \`continue\`，会立即跳转到更新表达式和条件判断处，准备开始下一轮迭代。

真正用于**跳出整个循环**的语句是 \`break\`。\`break\` 会终止当前循环，跳出循环体，执行循环之后的代码。两者功能完全不同。

**易错提醒：**
- \`continue\` 只跳过“本次”循环的剩余部分，不是“跳出整个循环”。
- 在嵌套循环中，\`break\` 只终止包含它的那一层循环，外层循环不受影响。

**知识延伸：**
\`continue\` 和 \`break\` 的作用域都是最近的循环语句（\`for\`、\`while\`、\`do-while\`）。在 \`switch\` 语句中，\`break\` 用于终止 \`case\` 执行。

**考点：** \`continue\`（跳过本次）与 \`break\`（终止循环）的功能区别`,

    },
    {
      id: 22,
      type: 'judge',
      question: `数组的下标从 1 开始。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
C++ 沿袭 C 语言的数组索引规则，采用**零基索引（Zero-based Indexing）**。数组下标从 \`0\` 开始，而非 \`1\`。若数组大小为 \`N\`，则有效下标范围是 \`0\` 到 \`N-1\`。第一个元素的下标是 \`arr[0]\`，第二个元素是 \`arr[1]\`，依此类推。

**易错提醒：**
- 使用 \`arr[N]\` 会访问越界位置，导致未定义行为（可能读取垃圾值或触发段错误）。
- 访问负数下标（如 \`arr[-1]\`）同样是越界访问。

**知识延伸：**
零基索引的优势在于：下标计算可直接作为偏移量使用（首地址 + 下标 × 元素大小）。某些语言（如 MATLAB）采用一基索引，但 C++ 坚持零基传统。

**考点：** C++ 数组下标从零开始的规则`,
      tags: [LEVEL1_TAGS.array]
    },
    {
      id: 23,
      type: 'judge',
      question: `标识符 Name 和 name 在 C++ 中是相同的。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
C++ 是一门**大小写敏感（Case Sensitive）**的语言。这意味着标识符中的每个字符都必须精确匹配，大写字母和小写字母被视为完全不同的字符。\`Name\` 和 \`name\` 在 C++ 中是两个不同的标识符，可以指向不同的内存位置（变量、函数等），它们之间没有任何关联。

**易错提醒：**
- Windows 文件系统默认不区分大小写，可能导致初学者混淆语言本身的大小写敏感性。
- 常见错误是在引用变量时大小写不一致，如定义 \`int MaxValue;\` 后写成 \`maxValue\`。

**知识延伸：**
C++ 标准库的头文件和函数名通常采用小写（如 \`<iostream>\`、\`std::cout\`），而用户自定义标识符可自由命名，但需保持大小写一致。

**考点：** C++ 标识符的大小写敏感性`,

    },
    {
      id: 24,
      type: 'judge',
      question: `! (10 > 5) 的值是 false。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
对表达式 \`!(10 > 5)\` 进行逐步分析：
1. 首先计算括号内的关系表达式 \`10 > 5\`，该比较结果为逻辑真（\`true\`）。
2. 然后应用逻辑非运算符 \`!\`，\`!\` 的作用是将布尔值取反：\`true\` 变为 \`false\`，\`false\` 变为 \`true\`。
3. 因此 \`!(10 > 5)\` 的结果为 \`false\`。

命题描述与实际计算结果一致，判定为正确。

**易错提醒：**
- 逻辑非 \`!\` 的优先级较高，但此处括号已明确求值顺序，无需担心优先级问题。
- \`10 > 5\` 的结果是布尔类型，不是数值，不能与数值直接比较。

**知识延伸：**
在 C++ 中，非零值在逻辑上下文中被视为 \`true\`，零值被视为 \`false\`。但关系运算的结果是标准布尔类型（\`bool\`），值为 \`true\` 或 \`false\`。

**考点：** 逻辑非运算符 \`!\` 对布尔表达式的取反作用`,
      tags: [LEVEL1_TAGS.operator]
    },
    {
      id: 25,
      type: 'judge',
      question: `编译器会将高级语言翻译成机器语言。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
编译器（Compiler）是计算机程序的一种，其核心功能是将**高级语言**（人类可读、符合语法规则的源代码，如 C++、Java）翻译成**机器语言**（CPU 能直接执行的二进制指令序列，通常表示为 0 和 1 的组合）。这一过程称为“编译”，是现代软件开发的关键步骤。

C++ 属于编译型语言，其编译流程大致为：源代码 (.cpp) → 预处理器处理 → 编译器生成目标文件 (.obj) → 链接器生成可执行文件 (.exe)。

**易错提醒：**
- 解释型语言（如 Python、JavaScript）通过解释器逐行执行代码，不需要编译步骤，与 C++ 的编译机制不同。
- Java 介于两者之间，通过 javac 编译成字节码（.class），再由 JVM 解释执行。

**知识延伸：**
广义的编译器还包括优化器、目标代码生成器等组件。GCC、Clang、MSVC 都是常见的 C++ 编译器。

**考点：** 编译器将高级语言转换为机器语言的核心功能`,

    },

    // 26-27 编程题
    {
      id: 26,
      type: 'programming',
      samples: [
        { input: `1
6`, output: `7` },
        { input: `5
3`, output: `1` }
      ],
      referenceCode: `#include <iostream>\nusing namespace std;\nint main() {\n    int x, y;\n    cin >> x >> y;\n    int res = (y+x-1) % 7;\n    if (res == 0) res = 7;\n    cout << res << endl;\n    return 0;\n}`,
      question: `
# [GESP202312 一级] 小杨的考试

## 题目描述

今天是星期 $X$，小杨还有 $N$ 天就要考试了，你能推算出小杨考试那天是星期几吗？（本题中使用 $7$ 表示星期日）

## 输入格式

输入 $2$ 行，第一行一个整数 $X(1\\le X \\le 7)$；第二行一个整数 $N(1≤N≤364)$。

## 输出格式

输出一个整数，表示小杨考试那天是星期几。
`,
      template: `#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    // 在此填写代码\n    return 0;\n}`,
      
      score: 25,
      explanation: `
      **解析：**
      考查周期性计算。可以使用 $(start+days-1) \\% 7$ 得到结果，注意余数为 0 时对应星期日 (7)。LuoGu B3921。
      `,
      tags: [LEVEL1_TAGS.basics, LEVEL1_TAGS.operator, LEVEL1_TAGS.condition],
    },
    {
      id: 27,
      type: 'programming',
      samples: [
        { input: `5
2`, output: `1
3
5` },
        { input: `10
3`, output: `1
2
4
5
7
8
10` }
      ],
      referenceCode: `#include <iostream>\nusing namespace std;\nbool contains(int n, int x) {\n    while (n > 0) {\n        if (n % 10 == x) return true;\n        n /= 10;\n    }\n    return false;\n}\nint main() {\n    int n, x, count = 0;\n    cin >> n >> x;\n    for (int i = 1; i <= n; i++) {\n        if (i % x == 0 || contains(i, x)) {\n            count++;\n        }\n    }\n    cout << count << endl;\n    return 0;\n}`,
      question: `
# [GESP202312 一级] 小杨报数

## 题目描述

小杨需要从 $1$ 到 $N$ 报数。在报数过程中，小杨希望跳过 $M$ 的倍数。例如，如果 $N=5$， $M=2$ ，那么小杨就需要依次报出 $1$、$3$、$5$。

现在，请你依次输出小杨报的数。

## 输入格式

输入 $2$ 行，第一行一个整数 $N（1 \\le N \\le 1,000）$；第二行一个整数 $M（2 \\le M \\le 100）$。

## 输出格式

输出若干行，依次表示小杨报的数。
`,
      template: `#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    // 在此填写代码\n    return 0;\n}`,
      
      score: 25,
      explanation: `
      **解析：**
      包含判断与整除判断。通过 while 循环拆解数字判断是否包含数字 $x$。LuoGu B3922。
      `,
      tags: [LEVEL1_TAGS.loop, LEVEL1_TAGS.condition],
    }
  ]
};
