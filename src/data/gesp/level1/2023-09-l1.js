import { LEVEL1_TAGS } from './shared.js';

export const paperData = {
  id: '2023-09-l1',
  title: '2023年9月 GESP C++ 一级真题',
  level: 1,
  year: 2023,
  month: 9,
  session: 3,
  timeLimit: 5400,
  source: {
    officialPdf: '',
    type: 'public-recovery',
  },
  questions: [
    {
      id: 1,
      type: 'single',
      question: `我们通常说的“内存”属于计算机中的（ ）。`,
      options: ['输出设备', '输入设备', '存储设备', '打印设备'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (存储设备)**

**核心解析：**
计算机硬件系统可分为五大基本组成部分：输入设备、输出设备、运算器、控制器和存储器。内存（Memory/RAM）位于计算机内部，用于临时存储CPU正在处理的程序指令和数据，其核心功能是提供高速的数据读写能力，因此属于存储设备的范畴。内存与硬盘、光盘等外存共同构成完整的存储体系，但内存特指内部存储器，具有访问速度快的特点。

**选项逐项分析：**
- **A (输出设备)**：❌ 错误。输出设备的功能是将数据从计算机内部向外传递，典型代表为显示器、打印机等。内存并不负责向外输出信息，而是为CPU提供数据暂存空间。
- **B (输入设备)**：❌ 错误。输入设备的功能是将外部信息引入计算机内部，典型代表为键盘、鼠标等。内存同样不负责接收外部输入。
- **C (存储设备)**：✅ 正确。内存（RAM）是计算机内部的核心存储部件，用于暂存运行中的程序和数据，虽然具有易失性（断电后数据丢失），但其存储功能本质不变。
- **D (打印设备)**：❌ 错误。打印设备是输出设备的具体类型之一，属于输出设备的子类，与内存的存储功能完全不同。

**易错提醒：**
- 计算机存储系统分为内存储器和外存储器，内存特指RAM，不要与外存（硬盘、U盘等）混淆
- 内存的“快”与“易失”是两个独立特性，不能因为内存速度快就将其归类为“运算器”

**知识延伸：**
在计算机五大组成中，存储器与运算器、控制器共同构成CPU核心，内存属于主存储器（主存），而硬盘属于辅助存储器（辅存），二者共同服务于数据存储需求。

**考点：** 计算机硬件系统的分类与功能分工`,
            tags: ['计算机基础', '输入输出'],
        },
    {
      id: 2,
      type: 'single',
      question: `以下 C++ 不可以作为变量的名称的是（ ）。`,
      options: ['redStar', 'RedStar', 'red_star', 'red star'],
      answer: 3,
      score: 2,
      explanation: `**答案：D (red star)**

**核心解析：**
C++语言的标识符（变量名、函数名等）必须是一个连续不间断的字符序列，不能包含空格字符。编译器在词法分析阶段使用空格作为标识符的分隔符，如果标识符内部出现空格，编译器将把空格前后的内容视为两个独立的语法符号，导致语法错误。

**选项逐项分析：**
- **A (redStar)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。合法。符合C++标识符命名规则，由字母和数字组成，且首字符为字母。
- **B (RedStar)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。合法。首字母大写是常见的驼峰命名法（camelCase），完全符合C++标识符规范。
- **C (red_star)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。合法。下划线是C++标识符允许使用的字符，可用于增强可读性（蛇形命名法/snake_case）。
- **D (red star)**：✅ 正确。该项不符合题目限定，正是本题要选的项。不合法。标识符内部包含空格，违反了“连续字符序列”的基本规则，编译器会报语法错误。

**易错提醒：**
- C++标识符只允许使用字母、数字和下划线三种字符，空格、加号、减号等符号均不允许出现在标识符内部
- 标识符虽然不能使用空格，但数字可以出现在标识符中间（只要不作为首字符）

**知识延伸：**
C++标识符命名规则与大多数主流编程语言一致。此外，C++还规定标识符不能使用保留关键字（如int、if、while等），且区分大小写（age和Age是两个不同的标识符）。

**考点：** C++标识符的合法命名规则`,
            tags: ['变量与标识符'],
        },
    {
      id: 3,
      type: 'single',
      question: `C++ 表达式 2-1 && 2 % 10 的值是（ ）。`,
      options: ['0', '1', '2', '3'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (1)**

**核心解析：**
逐步分析表达式 \`2-1 && 2%10\` 的求值过程：

1. **运算符优先级判断**：算术运算符（\`-\`、\`%\`）的优先级高于逻辑运算符（\`&&\`），因此先计算算术部分。

2. **第一算术运算**：\`2 - 1\` = 1
   - 结果为1（非零值），在C++逻辑运算中视为真（true）。

3. **第二算术运算**：\`2 % 10\` = 2
   - 2除以10余数为2，结果非零，在C++逻辑运算中视为真（true）。

4. **逻辑与运算**：此时表达式简化为 \`1 && 2\`
   - 两操作数均为非零（真），根据逻辑与规则，结果为真（true）。

5. **类型转换**：逻辑真值在输出或隐式转换为int时，映射为整数1。

因此最终输出值为 **1**。

**选项逐项分析：**
- **A (0)**：❌ 错误。该选项可能误以为表达式结果是假，但两个操作数均为非零，逻辑与结果应为真。
- **B (1)**：✅ 正确。最终逻辑与结果为真，转换为int后输出1。
- **C (2)**：❌ 错误。该选项可能混淆了算术运算结果与最终表达式值，忘记了对逻辑运算结果进行布尔值转换。
- **D (3)**：❌ 错误。该选项可能将 \`2-1\` 和 \`2%10\` 的结果直接相加，完全忽略了逻辑运算符 \`&&\` 的存在。

**易错提醒：**
- C++中逻辑运算符 \`&&\` 的运算对象可以是任意数值，非零即真，不必显式转换为bool类型
- 逻辑运算结果的输出值固定为0（假）或1（真），不会输出原始数值
- 运算符优先级决定计算顺序：\`&&\` 的优先级低于 \`-\` 和 \`%\`

**知识延伸：**
C++中的逻辑运算符 \`&&\`（与）、\`||\`（或）、\`!\`（非）遵循短路求值规则：当 \`&&\` 的左操作数为假时，右操作数不会被求值。本题中两个操作数均为真，需完整计算。

**考点：** 运算符优先级与逻辑运算的真值判定`,
            tags: ['运算符'],
        },
    {
      id: 4,
      type: 'single',
      question: `假设 a = 3, b = 4，执行 cout << "a+b=" << a+b; 后输出为（ ）。`,
      options: ['3+4=7', '7', 'a+b=7', 'a+b=a+b'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (a+b=7)**

**核心解析：**
分析语句 \`cout << "a+b=" << a+b;\` 的输出过程：

1. **第一个输出项 \`"a+b="\`**：
   - 被双引号包裹的内容是字符串字面量（string literal）。
   - 输出流会原封不动地打印该字符串，即输出 \`a+b=\`。

2. **第二个输出项 \`a+b\`**：
   - 这里的 \`a\` 和 \`b\` 是变量名（值为3和4）。
   - 输出流会对表达式 \`a+b\` 进行求值计算。
   - \`a + b\` = \`3 + 4\` = \`7\`。
   - 将计算结果7输出到屏幕。

3. **合龙输出**：字符串 \`"a+b="\` 的原样输出与表达式 \`a+b\` 的计算结果7按顺序拼接，得到最终输出 \`a+b=7\`。

**选项逐项分析：**
- **A (3+4=7)**：❌ 错误。该选项误以为 \`a+b\` 会被原样打印为字符串，实际上输出流会对变量进行求值计算。
- **B (7)**：❌ 错误。该选项只输出了表达式计算结果，忽略了字符串 \`"a+b="\` 的原样输出。
- **C (a+b=7)**：✅ 正确。字符串部分原样输出，变量部分计算求值后输出，二者拼接即为 \`a+b=7\`。
- **D (a+b=a+b)**：❌ 错误。该选项假设 \`a+b\` 整体被作为字符串原样打印，但C++的输出流会区分字符串字面量与变量表达式。

**易错提醒：**
- 区分字符串字面量（双引号包裹）与其内部内容的原样输出，字符串中的内容不会被当作C++代码执行
- \`cout\` 的 \`<<\` 操作符可以连续使用，多个输出项按从左到右的顺序依次输出
- 变量名不加引号时，输出的是变量的值，而非变量名字符串

**知识延伸：**
\`cout\` 是C++标准输出流对象，与C语言的 \`printf\` 不同，\`cout\` 使用类型安全的重载运算符 \`<<\` 进行输出，不需要格式说明符（如 \`%d\`），编译器会根据变量类型自动选择合适的输出方式。

**考点：** 输出流中字符串字面量与变量表达式的区别`,
            tags: ['输入输出', '运算符'],
        },
    {
      id: 5,
      type: 'single',
      question: `在 C++ 语言中，int 类型的变量 x、y、z 的值分别为 2、4、6，以下表达式的值为真的是（ ）。`,
      options: ['x > y || x > z', 'x != z-y', 'z > y+x', 'x < y || ! (x < z)'],
      answer: 3,
      score: 2,
      explanation: `**答案：D (x < y || ! (x < z))**

**核心解析：**
已知条件：x = 2，y = 4，z = 6。逐项验证每个表达式的逻辑值：

**选项A分析：**
- \`x > y\` 即 \`2 > 4\`，结果为假（false）。
- \`x > z\` 即 \`2 > 6\`，结果为假（false）。
- \`false || false\` = false，整个表达式为假。

**选项B分析：**
- \`z - y\` 即 \`6 - 4\` = 2。
- \`x != z - y\` 即 \`2 != 2\`，结果为假（false）。
- 因为相等判断为假，\`!=\` 运算结果为false。

**选项C分析：**
- \`y + x\` 即 \`4 + 2\` = 6。
- \`z > y + x\` 即 \`6 > 6\`，结果为假（false）。
- “大于”要求严格大于，等于不满足大于关系。

**选项D分析：**
- 前半部分 \`x < y\` 即 \`2 < 4\`，结果为真（true）。
- 逻辑或 \`||\` 具有短路特性：当左操作数为真时，无需计算右操作数，整个表达式直接确定为真。
- 因此整个表达式 \`true || ...\` = true，结果为真。

**选项逐项分析：**
- **A (x > y || x > z)**：❌ 错误。\`2 > 4\` 为假，\`2 > 6\` 为假，逻辑或连接两个假值结果仍为假。
- **B (x != z-y)**：❌ 错误。\`z - y = 2\`，与 \`x\` 的值相等，\`x != 2\` 为假。
- **C (z > y+x)**：❌ 错误。\`y + x = 6\`，\`z > 6\` 即 \`6 > 6\`，严格大于关系不成立，结果为假。
- **D (x < y || ! (x < z))**：✅ 正确。\`x < y\`（\`2 < 4\`）为真，逻辑或短路求值后整个表达式为真。

**易错提醒：**
- 逻辑或 \`||\` 的短路求值：当左操作数已确定为真时，右操作数不会被计算（本题中即使计算 \`!(x < z)\` 也不影响结果）
- 严格不等关系 \`>\` 和 \`<\` 与相等关系 \`==\` 和 \`!=\` 的区别：等于不满足严格大于/小于

**知识延伸：**
C++逻辑运算符 \`||\`（或）的真值表：左假右假→假，其他情况→真。逻辑与 \`&&\` 的真值表：左真右真→真，其他情况→假。这两个运算符都支持短路求值，可用于条件判断。

**考点：** 关系运算符与逻辑运算符的综合应用`,
            tags: ['数据类型', '运算符'],
        },
    {
      id: 6,
      type: 'single',
      question: `对 int 类型的变量 a、b、c，下列语句不符合 C++ 语法的是（ ）。`,
      options: ['c += 5;', 'b = c % 2.5;', 'a = (b = 3, c = 4, b+c);', 'a -= a = (b = 6) / (c = 2);'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (b = c % 2.5;)**

**核心解析：**
C++ 中取模运算符 \`%\` 要求左右操作数均为整数类型。\`2.5\` 是浮点型常量，违反了 \`%\` 运算符的类型约束，编译器将报错“invalid operands of types 'int' and 'double' to binary operator %"。

**选项逐项分析：**
- **A (c += 5;)**：❌ 错误。该项本身正确/合法，因此不是本题要选的不合法项。复合赋值表达式语法完全合法。
- **B (b = c % 2.5;)**：✅ 正确（此处指该选项不合法）。\`%\` 运算符的操作数必须为整数，\`2.5\` 为浮点数，类型不匹配。
- **C (a = (b = 3, c = 4, b+c);)**：❌ 错误。该项本身正确/合法，因此不是本题要选的不合法项。逗号表达式先依次执行 \`b=3\`、\`c=4\`，最后取 \`b+c\` 的值 \`7\` 赋给 \`a\`，语法正确。
- **D (a -= a = (b = 6) / (c = 2);)**：❌ 错误。该项本身正确/合法，因此不是本题要选的不合法项。虽涉及复合赋值的值，但语法层面可编译（注：实际运算顺序取决于编译器实现）。

**易错提醒：**
- \`%\` 与 \`/\` 不同：\`/\` 在 C++ 中允许整数除法和浮点数除法，但 \`%\` 只能用于整数。
- 误以为 \`%\` 可以处理小数是常见误区，需使用 \`fmod()\` 函数处理浮点取模。

**知识延伸：**
若需对浮点数求余，应使用 \`<cmath>\` 中的 \`fmod()\` 函数，例如 \`fmod(c, 2.5)\`。

**考点：** 取模运算符 \`%\` 操作数类型必须为整数的语法约束`,
            tags: ['数据类型'],
        },
    {
      id: 7,
      type: 'single',
      question: `下面 C++ 代码执行后的输出是（ ）。\n\`\`\`cpp\nint a = 12, b = 20;\nint c = a-b;\nif (c > 0) cout << a;\nelse cout << b;\n\`\`\``,
      options: ['12', '20', '8', '-8'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (20)**

**核心解析：**
1. 初始化：\`a = 12\`，\`b = 20\`。
2. 计算差值：\`c = a - b = 12 - 20 = -8\`。
3. 条件判定：\`if (c > 0)\` 即判断 \`-8 > 0\`，结果为 \`false\`。
4. 执行分支：条件不成立，跳过 \`if\` 分支，执行 \`else\` 分支中的 \`cout << b\`，输出 \`20\`。

**选项逐项分析：**
- **A (12)**：❌ 错误。若误认为 \`c = a - b\` 后 \`a\` 被修改，或误判 \`c > 0\` 为 \`true\`，会错误输出 \`a\` 的值。
- **B (20)**：✅ 正确执行了减法运算和条件分支逻辑。
- **C (8)**：❌ 错误。可能混淆了减法与绝对值计算，或误以为输出的是 \`b - a\`。
- **D (-8)**：❌ 错误。可能将变量 \`c\` 的值直接输出，或误判条件为输出 \`c\` 的值。

**易错提醒：**
- \`if-else\` 结构中，条件为 \`false\` 时执行 \`else\` 分支，而非跳过整个语句。
- 本题未输出 \`c\`，\`c\` 的值仅用于控制分支选择。

**知识延伸：**
\`if-else\` 是单向条件分支：条件为真执行第一分支，条件为假执行第二分支，两者互斥。

**考点：** if-else 条件分支与算术表达式求值`,
            tags: ['条件判断', '输入输出'],
        },
    {
      id: 8,
      type: 'single',
      question: `下面 C++ 代码执行后的输出是（ ）。\n\`\`\`cpp\nint m = 14;\nint n = 12;\nif (m % 2 && n % 2) cout << "A";\nelse if (m % 2 == 1 && n % 2 == 1) cout << "B";\nelse cout << "C";\n\`\`\``,
      options: ['A', 'B', 'C', '程序运行错误'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (C)**

**核心解析：**
1. 初始化：\`m = 14\`，\`n = 12\`。
2. 计算取模：\`m % 2 = 0\`（14 为偶数），\`n % 2 = 0\`（12 为偶数）。
3. 判定第一个条件：\`if (m % 2 && n % 2)\` 即 \`if (0 && 0)\`，结果为 \`false\`，跳过第一个分支。
4. 判定第二个条件：\`else if (m % 2 == 1 && n % 2 == 1)\` 即 \`if (0 == 1 && 0 == 1)\`，等价于 \`if (false && false)\`，结果为 \`false\`，跳过第二个分支。
5. 执行最终 \`else\`：输出 \`cout << "C"\`。

**选项逐项分析：**
- **A (A)**：❌ 错误。该选项未沿实际命中分支执行。第一个条件需 \`m\` 和 \`n\` 同时为奇数，但两者均为偶数。
- **B (B)**：❌ 错误。该选项未沿实际命中分支执行。第二个条件同样要求 \`m\` 和 \`n\` 同时为奇数，不满足。
- **C (C)**：✅ 正确。前两个条件均不满足，程序执行最终 \`else\` 分支输出 \`C\`。
- **D (程序运行错误)**：❌ 错误。\`&&\` 运算符在左侧为 \`false\` 时短路，不访问右侧表达式，不会引发除零或其他运行时错误。

**易错提醒：**
- \`m % 2\` 的值是 \`0\`（偶数）或 \`1\`（奇数），非布尔值时 \`0\` 等价于 \`false\`，非零等价于 \`true\`。
- \`if\` 与 \`else if\` 形成链式结构，一旦某个条件满足，后续分支不再判定。

**知识延伸：**
条件表达式中，整数 \`0\` 视为 \`false\`，非零视为 \`true\`：\`if (x)\` 等价于 \`if (x != 0)\`。

**考点：** 条件运算符与 if-else if-else 多重分支结构`,
            tags: ['条件判断', '输入输出'],
        },
    {
      id: 9,
      type: 'single',
      question: `下面 C++ 代码执行后的输出是（ ）。\n\`\`\`cpp\nint n = 7;\nif (n / 5 != 0) cout << 0;\nelse if (n / 3 != 0) cout << 1;\nelse if (n % 2 != 0) cout << 2;\nelse cout << 3;\n\`\`\``,
      options: ['0', '1', '2', '3'],
      answer: 0,
      score: 2,
      explanation: `**答案：A (0)**

**核心解析：**
1. 初始化：\`n = 7\`。
2. 判定第一个条件：\`n / 5 = 1\`（整数除法），\`1 != 0\` 为 \`true\`。
3. 命中第一个 \`if\` 分支：执行 \`cout << 0\`，输出 \`0\`。
4. 链式短路：因已执行 \`if\` 分支，后续 \`else if\` 链条全部被忽略，程序结束。

**选项逐项分析：**
- **A (0)**：✅ 正确。\`n / 5 = 1\` 不等于 \`0\`，第一个条件即成立，输出 \`0\`。
- **B (1)**：❌ 错误。该选项可能是混淆了 \`n / 3 != 0\` 的判定，但第一个条件已命中，后续分支不会执行。
- **C (2)**：❌ 错误。该选项可能基于 \`n % 2 != 0\`（\`7\` 为奇数），但即使 \`7 % 2 != 0\` 为 \`true\`，前面的条件已满足，不会执行到此。
- **D (3)**：❌ 错误。该选项可能是最终 \`else\` 分支，但前面的条件已满足，不可能执行到此。

**易错提醒：**
- if-else if-else 链中，一旦某个 \`if\` 或 \`else if\` 条件为 \`true\`，后续所有分支被短路，不再判定。
- \`n / 5\` 进行整数除法，结果为 \`1\` 而非 \`1.4\`，即使 \`n=5\` 时 \`n/5=1\` 仍不等于 \`0\`。

**知识延伸：**
整数除法 \`a / b\` 向下取整（向零截断），结果仍为整数，适合判断是否整除或比较商的大小。

**考点：** if-else if-else 链式结构的短路特性与整数除法`,
            tags: ['条件判断', '输入输出'],
        },
    {
      id: 10,
      type: 'single',
      question: `下面 C++ 代码段执行后的输出是（ ）。\n\`\`\`cpp\nint s = 0;\nfor (int i = 1; i <= 5; i++) s += i;\ncout << s;\n\`\`\``,
      options: ['1', '5', '15', '10'],
      answer: 2,
      score: 2,
      explanation: `**答案：C (15)**

**核心解析：**
累加器 \`s\` 初始值为 \`0\`，循环变量 \`i\` 从 \`1\` 递增到 \`5\`，每次将 \`i\` 加到 \`s\`：
- i=1：\`s = 0 + 1 = 1\`
- i=2：\`s = 1 + 2 = 3\`
- i=3：\`s = 3 + 3 = 6\`
- i=4：\`s = 6 + 4 = 10\`
- i=5：\`s = 10 + 5 = 15\`
循环结束，\`s\` 最终值为 \`15\`，输出 \`15\`。

**选项逐项分析：**
- **A (1)**：❌ 错误。可能误以为循环只执行一次，或将 \`s\` 初始值与循环变量混淆。
- **B (5)**：❌ 错误。可能只取最后一次循环的 \`i\` 值 \`5\`，或误将循环变量当作累加结果。
- **C (15)**：✅ 正确。完整计算了 1+2+3+4+5 的累加和。
- **D (10)**：❌ 错误。可能漏算了 \`i=5\` 这一次循环，只累加到 \`4\` 的结果。

**易错提醒：**
- for 循环的三个表达式：初始化只执行一次，条件判断和迭代表达式每次循环都执行。
- 循环体使用 \`s += i\` 而非 \`s = i\`，确保每次累加而非赋值。

**知识延伸：**
1+2+3+...+n 的累加结果等于 \`n*(n+1)/2\`，本题 n=5，结果为 \`5*6/2=15\`。

**考点：** for 循环的累加器模式与循环变量迭代`,
            tags: ['循环', '输入输出'],
        },
    {
      id: 11,
      type: 'single',
      question: `下面 C++ 代码段执行后的输出是（ ）。\n\`\`\`cpp\nint n = 5;\nwhile (n < 5) n += 2;\ncout << n;\n\`\`\``,
      options: ['7', '5', '4', '6'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (5)**

**核心解析：**
1. 变量初始化：\`int n = 5;\`，此时 n 的值为 5。
2. 进入 while 循环的条件判断：\`while (n < 5)\`，即判断 \`5 < 5\` 是否成立。
3. 由于 5 不小于 5，该条件表达式的值为 \`false\`。
4. while 循环体 \`{ n += 2; }\` 一次也不执行，程序直接跳过循环。
5. \`cout << n;\` 输出 n 的当前值，即初始值 5。

**选项逐项分析：**
- **A (7)**：❌ 错误。如果循环执行一次，n 会变为 7，但循环条件 \`n < 5\` 在首次判断时即为假，循环体不会执行。
- **B (5)**：✅ 正确。n 保持初始值 5 不变。
- **C (4)**：❌ 错误。该选项可能误认为 n 会减少，但循环体中是 \`n += 2\`，只会增加不会减少。
- **D (6)**：❌ 错误。若要得到 6，n 需经历 5→8→6 的过程，但第一次判断时 5 < 5 为假，循环不会开始。

**易错提醒：**
- while 循环在入口处进行条件判断，若初始条件不满足，循环体可能一次都不执行。
- 不能凭感觉认为循环"应该会执行几次"，必须严格按照"判断→执行"的顺序逐步推导。

**知识延伸：**
while 循环的"先判断后执行"机制意味着循环体可能执行 0 次、1 次或多次，这取决于首次判断时条件的真假。

**考点：** while 循环的条件前置判定与零执行可能性`,
            tags: ['循环', '输入输出'],
        },
    {
      id: 12,
      type: 'single',
      question: `下面 C++ 代码段执行后的输出是（ ）。\n\`\`\`cpp\nint n = 5; int cnt = 1;\nwhile (n >= 0) {\n cnt += 1;\n n -= 2;\n}\ncout << cnt;\n\`\`\``,
      options: ['3', '4', '5', '2'],
      answer: 1,
      score: 2,
      explanation: `**答案：B (4)**

**核心解析：**
逐步追踪循环执行过程：

| 轮次 | 进入循环前 n 的值 | 条件 n >= 0 | 执行后 cnt | 执行后 n |
|------|------------------|-------------|------------|----------|
| 初始 | 5 | 5 >= 0 为真 | - | - |
| 第1轮 | 5 | - | 1+1=2 | 5-2=3 |
| 第2轮 | 3 | 3 >= 0 为真 | 2+1=3 | 3-2=1 |
| 第3轮 | 1 | 1 >= 0 为真 | 3+1=4 | 1-2=-1 |
| 第4轮 | -1 | -1 >= 0 为假 | 循环结束 | - |

最终 cnt 的值为 4，输出即为 4。

**选项逐项分析：**
- **A (3)**：❌ 错误。可能只统计了前两次循环，漏掉了第三次 cnt 的递增。
- **B (4)**：✅ 正确。共执行了 3 次循环体，cnt 从 1 变为 4。
- **C (5)**：❌ 错误。可能误以为会多执行一次，或者初始 cnt 被多计算一次。
- **D (2)**：❌ 错误。可能只统计了第一次循环，忽略了后续的递增。

**易错提醒：**
- 当 n 变为 -1 时，\`-1 >= 0\` 为假，此时才退出循环，不要在 n=-1 时还让循环体执行。
- cnt 的初值为 1，每次循环体执行时都会 \`cnt += 1\`，共增加了 3 次。

**知识延伸：**
对于涉及循环计数器的问题，建议用表格法逐行记录变量变化，可有效避免遗漏执行轮次。

**考点：** while 循环的手动追踪与边界条件判定`,
            tags: ['循环', '输入输出'],
        },
    {
      id: 13,
      type: 'single',
      question: `下面C++代码用于求正整数的所有因数。在横线处应填写（ ）。\n\`\`\`cpp\nint n; cin >> n;\nfor (______) {\n if (n % i == 0) cout << i << endl;\n}\n\`\`\``,
      options: ['int i = 1; i < n; i+1', 'int i = 1; i < $n+1$; i+1', 'int i = 1; i < n; i = i+1', 'int i = 1; i <= n; i = i+1'],
      answer: 3,
      score: 2,
      explanation: `**答案：D (int i = 1; i <= n; i = i+1)**

**核心解析：**
要找出正整数 n 的所有因数，需要用 for 循环遍历从 1 到 n 的所有整数，对每个 i 检查 \`n % i == 0\` 是否成立。

for 循环的标准结构为：\`for(初始化; 循环条件; 步进表达式)\`

- 初始化部分：应将 i 设为 1，从最小因数开始。
- 循环条件：需包含 n 本身，因为 n 本身也是 n 的因数，所以条件应为 \`i <= n\` 或 \`i < n+1\`。
- 步进表达式：\`i+1\` 只是计算出一个新值但未赋值给 i，必须写成 \`i = i+1\` 才能让 i 递增。

**选项逐项分析：**
- **A (int i = 1; i < n; i+1)**：❌ 错误。步进表达式 \`i+1\` 没有赋值操作，会导致 i 始终为 1，循环陷入死循环。
- **B (int i = 1; i < $n+1$; i+1)**：❌ 错误。虽然条件 \`i < n+1\` 等价于 \`i <= n\`，但步进表达式 \`i+1\` 仍未赋值，仍会导致死循环。
- **C (int i = 1; i < n; i = i+1)**：❌ 错误。条件 \`i < n\` 导致循环不会检查 i == n 的情况，会漏掉 n 本身的因数。
- **D (int i = 1; i <= n; i = i+1)**：✅ 正确。初始化从 1 开始，条件 \`i <= n\` 覆盖到自身，步进表达式 \`i = i+1\` 正确实现递增。

**易错提醒：**
- 步进表达式 \`i++\` 与 \`i+1\` 的本质区别在于：前者是自增运算符（i 自增），后者仅是表达式（结果未保存）。
- 因数包括 1 和自身，漏掉上界会导致不完整的因数列表。

**知识延伸：**
for 循环的三个表达式缺一不可，特别是步进表达式若缺少赋值操作，将导致循环变量无法更新，引发死循环。

**考点：** for 循环三要素的完整性及步进表达式的赋值要求`,
            tags: ['循环', '条件判断'],
        },
    {
      id: 14,
      type: 'single',
      question: `下面 C++ 代码段执行后的输出是（ ）。\n\`\`\`cpp\nint n = 10;\nwhile (n > 0) {\n if (n % 3 == 0) break;\n n -= 2;\n}\ncout << n;\n\`\`\``,
      options: ['10', '8', '4', '6'],
      answer: 3,
      score: 2,
      explanation: `**答案：D (6)**

**核心解析：**
逐步追踪循环执行过程：

1. 初始状态：n = 10。
2. 第1轮循环：检查 \`n > 0\`（10 > 0 为真）→ 检查 \`n % 3 == 0\`（10 % 3 = 1，不为 0）→ 执行 \`n -= 2\`，n 变为 8。
3. 第2轮循环：检查 \`n > 0\`（8 > 0 为真）→ 检查 \`n % 3 == 0\`（8 % 3 = 2，不为 0）→ 执行 \`n -= 2\`，n 变为 6。
4. 第3轮循环：检查 \`n > 0\`（6 > 0 为真）→ 检查 \`n % 3 == 0\`（6 % 3 = 0，为真）→ 触发 \`break\`，立即跳出循环。

跳出循环后，n 的当前值为 6，输出 6。

**选项逐项分析：**
- **A (10)**：❌ 错误。如果从未进入循环体才会保持初始值 10，但实际上循环执行了多次。
- **B (8)**：❌ 错误。8 是 n 在第一次递减后的值，未考虑到会继续递减直到被 3 整除。
- **C (4)**：❌ 错误。若 n 继续递减到 4，输出会是 4，但循环在 n=6 时已被 break 终止。
- **D (6)**：✅ 正确。n 在递减过程中首次被 3 整除时触发 break，此时 n 的值为 6。

**易错提醒：**
- break 语句会立即终止所在循环，不执行循环体中 break 之后的任何语句，也不进行后续的循环条件判断。
- 不能忽略 n 的初始值 10 不满足 \`n % 3 == 0\`，循环会继续执行。

**知识延伸：**
break 语句只能跳出一层循环（在有多层嵌套循环时），它作用于最内层的循环结构。

**考点：** break 语句的强制终止循环机制与整除判断`,
            tags: ['循环', '条件判断'],
        },
    {
      id: 15,
      type: 'single',
      question: `下面 C++ 代码段执行后的输出是（ ）。\n\`\`\`cpp\nint s = 0;\nfor (int i = 1; i <= 10; i++) {\n if (i % 2 == 0) continue;\n s = s+i;\n}\ncout << s;\n\`\`\``,
      options: ['25', '10', '55', '30'],
      answer: 0,
      score: 2,
      explanation: `**答案：A (25)**

**核心解析：**
循环遍历 i 从 1 到 10，当 i 为偶数时执行 \`continue\` 跳过后续累加操作，只有奇数才会执行 \`s = s + i\`。

| i 的值 | i % 2 == 0 | 是否执行 continue | s 的变化 |
|--------|-----------|-------------------|----------|
| 1 | 假 | 否 | s = 0 + 1 = 1 |
| 2 | 真 | 是（跳过累加） | s 保持 1 |
| 3 | 假 | 否 | s = 1 + 3 = 4 |
| 4 | 真 | 是 | s 保持 4 |
| 5 | 假 | 否 | s = 4 + 5 = 9 |
| 6 | 真 | 是 | s 保持 9 |
| 7 | 假 | 否 | s = 9 + 7 = 16 |
| 8 | 真 | 是 | s 保持 16 |
| 9 | 假 | 否 | s = 16 + 9 = 25 |
| 10 | 真 | 是 | s 保持 25 |

最终 s = 1 + 3 + 5 + 7 + 9 = 25。

**选项逐项分析：**
- **A (25)**：✅ 正确。1+3+5+7+9 的累加和为 25。
- **B (10)**：❌ 错误。该选项可能是 2+4+6+8 的和，但这些偶数被 continue 跳过了，未参与累加。
- **C (55)**：❌ 错误。这是 1 到 10 的全部累加和 (1+2+3+...+10)，忽略了 continue 对偶数的过滤作用。
- **D (30)**：❌ 错误。该值接近 1+3+5+7+9+5=30 或其他错误累加，可能误算了偶数。

**易错提醒：**
- continue 语句会跳过本次循环中 \`continue\` 之后的剩余语句，直接进入下一次循环的条件判断，而不是终止整个循环。
- 不能将 continue 与 break 混淆，前者是"跳过本次"，后者是"终止全部"。

**知识延伸：**
continue 语句常用于条件过滤，与 if 语句配合可以方便地排除不需要处理的循环迭代。

**考点：** continue 语句的局部跳过特性与奇偶数筛选`,
            tags: ['循环', '条件判断'],
        },
    {
      id: 16,
      type: 'judge',
      question: `早期计算机内存不够大，可以将字库固化在一个包含只读存储器的扩展卡中协助处理汉字。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
在20世纪80年代的个人计算机时代，内存（RAM）价格高昂且容量有限（通常只有64KB至640KB），难以将庞大的中文字库全部加载到内存中使用。在此背景下，"汉卡"技术应运而生。汉卡是一种扩展卡，其上集成了ROM（只读存储器）芯片，固化了汉字的点阵字库数据。计算机通过汉卡直接读取汉字图形信息，无需占用宝贵的内存资源。这是一种通过专用硬件扩展来弥补内存不足的经典技术方案。题目所述与这一历史事实完全吻合。

**易错提醒：**
- 需要区分"内存"（RAM，随机存取存储器，可读写）与"只读存储器"（ROM，固化存储）的基本概念。
- 汉卡的技术本质是用硬件换内存空间，而非扩展内存容量。

**知识延伸：**
ROM 作为一种非易失性存储器，除了用于早期字库存储外，还广泛应用于计算机的BIOS（基本输入输出系统）固化存储。

**考点：** 计算机硬件发展史与存储器类型的基本认知`,
            tags: ['计算机基础'],
        },
    {
      id: 17,
      type: 'judge',
      question: `“神威·太湖之光”超级计算机是中国自主研制的超级计算机，在全球超级计算机 TOP500 排行榜中多次荣膺榜首。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
"神威·太湖之光"（Sunway TaihuLight）是由国家超级计算无锡中心部署、中国自主研制的超级计算机，全部采用国产申威处理器（SW26010）。该超级计算机在2016年6月至2017年11月期间，连续多次在全球超级计算机TOP500排行榜中位列第一，其浮点运算能力曾达到每秒12.5亿亿次。题目所述与事实完全一致。

**易错提醒：**
- "神威·太湖之光"与"天河二号"都是中国超级计算机的代表，但题目特指"神威·太湖之光"，不应与其他型号混淆。
- 蝉联榜首是指连续多次排名第一，而非仅一次。

**知识延伸：**
超级计算机的TOP500排名依据是Linpack基准测试测得的浮点运算性能（HPL），而非其他指标。

**考点：** 中国超级计算机发展概况与TOP500排名`,
      tags: [LEVEL1_TAGS.basics]
    },
    {
      id: 18,
      type: 'judge',
      question: `C++ 表达式 int(3.14) 的值为 3。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
在C++中，\`int(3.14)\` 采用了函数式类型转换（functional cast）的语法，将double类型的值3.14转换为int类型。类型转换过程中，当从浮点数转换为整数时，C++执行"截断"（truncation）操作，即直接丢弃小数部分，保留整数部分，不会进行四舍五入。

具体过程：
- 原始值：3.14
- 截断小数部分：.14 被丢弃
- 结果：3

因此，\`int(3.14)\` 的值确实为 3，命题正确。

**易错提醒：**
- C++的类型转换是"截断"而非"舍入"，这与数学上的四舍五入不同。
- 另一种转换写法 \`static_cast<int>(3.14)\` 同样执行截断操作。

**知识延伸：**
将负数转换为整数时，截断同样适用：例如 \`int(-3.9)\` 的结果为 -3（向零取整）。

**考点：** C++显式类型转换的截断机制`,
            tags: ['数据类型'],
        },
    {
      id: 19,
      type: 'judge',
      question: `C++ 语句 cout << (2, 3, "23") 的输出为 2,3,23。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
题目中的表达式 \`(2, 3, "23")\` 是一个逗号表达式（comma expression）。逗号表达式的求值规则为：
1. 从左到右依次计算各个子表达式的值。
2. 整个逗号表达式的结果是最后一个表达式的值。
3. 前面的表达式仅作为"副作用"被计算，其结果被丢弃。

因此：
- 首先计算 \`2\`（结果被丢弃）
- 然后计算 \`3\`（结果被丢弃）
- 最后计算 \`"23"\`（结果作为整个表达式的值）

执行 \`cout << (2, 3, "23")\` 时，流输出的是字符串 \`"23"\`，输出结果为 \`23\`（不包含引号）。

命题声称输出为 \`2,3,23\`，这与逗号表达式的实际行为不符，因此命题错误。

**易错提醒：**
- 逗号表达式的优先级是所有运算符中最低的，但题目中已有括号，不存在歧义。
- 逗号表达式不等于"依次输出多个值"，它只输出最后一个值。

**知识延伸：**
在 \`for\` 循环中可以使用逗号表达式，如 \`for(i=0, j=10; i<j; i++, j--)\`，但那是分隔符的用法，与运算符意义的逗号表达式不同。

**考点：** 逗号表达式的求值特性与结果判定`,
            tags: ['输入输出'],
        },
    {
      id: 20,
      type: 'judge',
      question: `C++ 的循环语句 for (int i = 0; i < 10; i += 2) 表示 i 从 0 开始到 10 结束但不包含 10，间隔为 2。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
分析 \`for (int i = 0; i < 10; i += 2)\` 的各部分：

1. **初始化** \`int i = 0\`：循环变量 i 从 0 开始。
2. **循环条件** \`i < 10\`：每次循环开始前判断 i < 10 是否成立。当 i = 10 时，10 < 10 为假，循环终止。i 的合法取值为 0, 1, 2, ..., 9，确实"不包含10"。
3. **步进表达式** \`i += 2\`：每次循环体执行完毕后，i 增加 2。

i 的取值序列：0 → 2 → 4 → 6 → 8 → 10（10 时退出）

可见 i 从 0 开始，到 9 结束（不包含 10），间隔为 2。命题与 for 循环的实际语义完全一致。

**易错提醒：**
- \`i += 2\` 表示每次递增 2，而非递增到 2 为止。
- 当 i 接近 10 时，由于条件 \`i < 10\` 的限制，i 不会达到 10。

**知识延伸：**
若要输出 0 到 10（含 10）的偶数，应使用条件 \`i <= 10\`。

**考点：** for循环的三个表达式语义及边界条件`,
            tags: ['循环', '数据类型'],
        },
    {
      id: 21,
      type: 'judge',
      question: `C++ 表达式 ('1'+'1') 的值为 '2'。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
在C++中，字符常量（如 \`'1'\`）的值为其对应的ASCII码值，而非字符本身代表的数字。

已知ASCII码值：
- \`'0'\` 的ASCII码值为 48
- \`'1'\` 的ASCII码值为 49
- \`'2'\` 的ASCII码值为 50

计算 \`'1' + '1'\`：
- \`'1'\` 的值为 49
- \`'1'\` 的值为 49
- 49 + 49 = 98

ASCII码值 98 对应的字符是 \`'b'\`（小写字母b）。

命题声称 \`'1' + '1'\` 的值为 \`'2'\`，但实际上：
- 若要得到字符 \`'2'\`，需要ASCII码值 50
- 98 ≠ 50，因此结果不是 \`'2'\`

命题错误。

**易错提醒：**
- 字符 \`'1'\` 不等于整数 \`1\`，两者在内存中的值相差48。
- 字符相加是ASCII码值相加，而非字符连接或数字相加。

**知识延伸：**
若要实现字符数字的加法得到正确结果，应先将字符转换为对应数值：\`(char)('1' - '0' + '1' - '0' + '0')\` 或使用 \`('1' - 48) + ('1' - 48)\`。

**考点：** 字符常量的ASCII码值与算术运算`,
            tags: ['字符与ASCII'],
        },
    {
      id: 22,
      type: 'judge',
      question: `在使用 C++ 的 cin 输入多个数时，可以用空格、回车或制表符作为分隔。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
C++标准库中的输入流对象 \`cin\` 在读取数据时，会将空白字符（whitespace）作为数据项之间的分隔符。标准定义的空白字符包括：
1. 空格（space，ASCII码32）
2. 换行符（newline，ASCII码10）
3. 制表符（tab，ASCII码9）
4. 回车符（carriage return，ASCII码13）
5. 垂直制表符、换页符等其他空白字符

当使用 \`cin >> x >> y\` 等方式读取多个数据时，用户可以在输入时自由选择空格、回车或Tab键作为分隔，程序均能正确识别。命题所述与C++输入流的标准行为一致。

**易错提醒：**
- 空白字符是"分隔符"而非"结束符"，连续输入多个空格仍然只算作一个分隔。
- 使用 \`cin.get()\` 等逐字符读取函数时，则不会自动跳过空白字符。

**知识延伸：**
与 \`cin\` 不同，\`cin.get()\` 或 \`cin.getline()\` 等函数会读取包括空白字符在内的内容。

**考点：** cin的空白字符分隔特性`,
            tags: ['输入输出'],
        },
    {
      id: 23,
      type: 'judge',
      question: `C++ 中的变量必须要先声明后使用。`,
      options: ['正确', '错误'],
      answer: 0,
      score: 2,
      explanation: `**答案：正确**

**判定依据：**
C++是一种静态类型、编译型语言。在编译阶段，编译器需要知道每个变量的类型和内存布局，才能生成正确的机器指令。

"先声明后使用"原则的具体含义：
1. **声明**：告知编译器变量的名称和类型，如 \`int x;\`
2. **编译检查**：编译器为该变量分配对应的内存空间
3. **使用**：在后续代码中引用该变量

如果在使用变量前未声明，编译器将报错"identifier not found"或类似错误，程序无法通过编译。

例如，以下代码会编译错误：
\`\`\`cpp
a = 5;      // 错误：a 未声明
int a;      // 声明太晚
\`\`\`

命题所述"变量必须先声明后使用"完全符合C++语言的语法规则和编译机制。

**易错提醒：**
- C++中函数可以先声明后定义（分离式编译），但变量不存在"声明后定义"的说法，必须在使用前完整声明。
- 全局变量和局部变量都必须遵循先声明后使用的规则。

**知识延伸：**
C++11引入了\`auto\`关键字，允许编译器自动推断变量类型，但仍需在使用时声明：\`auto x = 5;\`

**考点：** C++变量的声明时机要求`,
            tags: ['变量与标识符'],
        },
    {
      id: 24,
      type: 'judge',
      question: `C++ 中的单引号用来界定字符串，双引号用来界定字符。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
C++对字符字面量和字符串字面量有明确区分：

1. **单引号 \`' '\`**：用于界定**单个字符**（character），类型为 \`char\`。
   - 示例：\`'A'\` 表示字符A，\`'1'\` 表示字符1
   - 单引号内必须有且仅有一个字符（转义字符如 \`'\\n'\` 算一个字符）

2. **双引号 \`" "\`**：用于界定**字符串**（string），类型为 \`const char*\`（字符数组加终止符）。
   - 示例：\`"hello"\` 表示由5个字符加终止符\`'\\0'\`组成的字符串
   - \`"A"\` 表示包含字符'A'和终止符的字符串，长度为2

命题将两者角色颠倒："单引号用来界定字符串，双引号用来界定字符"，这与C++语法规则完全相反，因此命题错误。

**易错提醒：**
- \`'A'\` 和 \`"A"\` 在C++中是截然不同的类型和值：\`'A'\` 是值为65的char，\`"A"\` 是包含'A'和'\\0'的字符串。
- 混淆引号类型可能导致严重的类型错误或段错误。

**知识延伸：**
C++中没有原生的字符串类型，字符串是通过字符数组或标准库类（如 \`std::string\`）实现的。

**考点：** 字符字面量与字符串字面量的引号区分`,
            tags: ['字符与ASCII'],
        },
    {
      id: 25,
      type: 'judge',
      question: `C++ 语言中，if 语句必须带有 else 部分。`,
      options: ['正确', '错误'],
      answer: 1,
      score: 2,
      explanation: `**答案：错误**

**判定依据：**
C++的if语句语法有两种形式：

1. **单分支if**：\`if (条件) 语句;\`
   - 当条件为真时执行指定语句，为假时什么都不做

2. **双分支if-else**：\`if (条件) 语句1; else 语句2;\`
   - 当条件为真时执行语句1，为假时执行语句2

其中，\`else\` 子句是**可选的**（optional）。并非所有情况下都需要提供备选分支。

例如，以下代码是合法且常见的用法：
\`\`\`cpp
if (age >= 18) {
    cout << "成年人";
}
// 不满足条件时，程序直接继续执行后续代码，无需else
\`\`\`

命题声称"if语句必须带有else部分"，这与C++语法规则不符。else部分是条件分支的选配组件，而非必配零件。

**易错提醒：**
- if语句的核心功能是根据条件选择是否执行代码，不一定非要有"否则"的选项。
- else子句必须紧跟对应if的语句块，中间隔入其他语句会改变配对关系。

**知识延伸：**
在if-else嵌套时，注意else与最近未配对的if配对的规则，这可能导致意想不到的行为。

**考点：** if语句的单分支语法与else的选配性质`,
            tags: ['条件判断'],
        },
    {
      id: 26,
      type: 'programming',
      samples: [
        { input: `1
1
1
20`, output: `Yes
10` },
        { input: `1
1
1
5`, output: `No
5` }
      ],
      referenceCode: "#include <iostream>\nusing namespace std;\nint main() {\n    int x, y, z, q;\n    cin >> x >> y >> z >> q;\n    int cost = 2 * x + 5 * y + 3 * z;\n    if (q >= cost) {\n        cout << \"Yes\" << endl << q - cost << endl;\n    } else {\n        cout << \"No\" << endl << cost - q << endl;\n    }\n    return 0;\n}",
      question: `
# [GESP202309 一级] 买文具

## 题目描述

开学了，小明来到文具店选购文具。签字笔 $2$ 元一支，他需要 $X$ 支；记事本 $5$ 元一本，他需要 $Y$ 本；直尺 $3$ 元一把，他需要 $Z$ 把。小明手里有 $Q$ 元钱。请你通过编程帮小明算算，他手里的钱是否够买他需要的文具。

## 输入格式

第一行包含一个正整数，是小明购买签字笔的数量。约定 $1 \\le X \\le 10$。

第二行包含一个正整数，是小明购买记事本的数量。约定 $1 \\le Y \\le 10$。

第三行包含一个正整数，是小明购买直尺的数量。约定 $1 \\le Z \\le 10$。

第四行包含一个正整数 $Q$，是小明手里的钱数（单位：元）。

## 输出格式

输出 $2$ 行。如果小明手里的钱够买他需要的文具，则第一行输出 \`Yes\`，第二行输出小明会剩下的钱数（单位：元）；否则，第一行输出 \`No\`，第二行输出小明缺少的钱数（单位：元）。
`,
      template: `#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    // 在此填写代码\n    return 0;\n}`,
      
      score: 25,
      explanation: `
      **解析：**
      简单的四则运算。注意使用 long long 以防溢出（虽然一级范围通常较小）。LuoGu B3862。
      `,
      tags: [LEVEL1_TAGS.basics, LEVEL1_TAGS.operator],
    },
    {
      id: 27,
      type: 'programming',
      samples: [
        { input: `7
1
10`, output: `7` },
        { input: `7
10
20`, output: `31` }
      ],
      referenceCode: "#include <iostream>\nusing namespace std;\nint main() {\n    long long k, l, r, sum = 0;\n    cin >> k >> l >> r;\n    for (long long i = l; i <= r; i++) {\n        if (i % 10 == k || i % k == 0) {\n            sum += i;\n        }\n    }\n    cout << sum << endl;\n    return 0;\n}",
      question: `
# [GESP202309 一级] 小明的幸运数

## 题目描述

所有个位数为 $k$ 的正整数，以及所有 $k$ 的倍数，都被小明称为“ $k$ 幸运数”。小明想知道正整数 $L$ 和 $R$ 之间（包括 $L$ 和 $R$）所有 $k$ 幸运数的和，你能帮帮他吗？

## 输入格式

输入 $3$ 行。第一行包含一个正整数 $k$，第二行包含一个正整数 $L$，第三行包含一个正整数 $R$。约定 $2 \\le k \\le 9$，$1 \\le L \\le R \\le 1000$。

## 输出格式

输出 $1$ 行，符合题意的幸运数之和。
`,
      template: `#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    // 在此填写代码\n    return 0;\n}`,
      
      score: 25,
      explanation: `
      **解析：**
      遍历范围 $[L, R]$，判断每个数是否满足整除条件或个位条件。LuoGu B3863。
      `,
      tags: [LEVEL1_TAGS.loop, LEVEL1_TAGS.condition, LEVEL1_TAGS.operator],
    }
  ]
};
