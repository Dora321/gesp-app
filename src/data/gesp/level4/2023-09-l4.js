// 2023年9月 GESP C++ 四级真题
export const paperData = {
    id: '2023-09-l4',
    title: '2023年9月 GESP C++ 四级真题',
    level: 4,
    year: 2023,
    month: 9,
    session: 3,
    timeLimit: 5400,
    backfilled: true,
    source: {
        officialPdf: 'https://gesp.ccf.org.cn/101/attach/1570611272876064.pdf',
        notes: '第 15 题代码已对照官方 PDF 图像恢复。'
    },
    verification: {
        status: 'partial',
        reviewedBy: '本站校订',
        reviewedAt: '2026-07-06',
        scope: '第 15 题代码已对照官方 PDF 校订。'
    },
    questions: [
        {
            id: 1,
            type: "single",
            question: `人们所使用的手机上安装的 App 通常指的是（ ）。`,
            options: [
                "一款操作系统",
                "一款应用软件",
                "一种通话设备",
                "以上都不对",
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (一款应用软件)**

**核心解析：**
本题考察计算机软件的基础分类概念。计算机软件按功能可划分为两大类：

1. **系统软件**：负责管理计算机硬件资源、提供基础运行环境。典型代表包括 Windows、Linux、macOS、Android、iOS 等操作系统，以及驱动程序、编译器等。

2. **应用软件**：为完成特定任务而开发的程序，运行在系统软件之上。常见应用软件包括微信、支付宝、地图导航、图像编辑软件等。

**选项逐项分析：**
- **A 一款操作系统**：❌ 错误。操作系统是系统软件，负责管理硬件资源和应用软件的运行平台，本身不属于 App 的范畴。App 是运行在操作系统之上的程序。
- **B 一款应用软件**：✅ 正确。App 是英文单词 "Application" 的缩写，中文译为"应用程序"，指为实现特定功能或任务而设计的软件，完全符合应用软件的定义。
- **C 一种通话设备**：❌ 错误。通话设备（如手机、对讲机）是硬件设备，而 App 是软件程序，二者属于完全不同的事物类别。
- **D 以上都不对**：❌ 错误。B 选项已明确正确，因此该项不成立。

**易错提醒：**
- 部分考生容易混淆"操作系统"与"应用软件"的概念，需记住操作系统属于系统软件而非应用软件。
- App 的全称是 Application，正确对应"应用程序"这一类别。

**知识延伸：**
在计算机科学中，软件分类还包括中间件、数据库管理系统等类型。理解软件层次结构有助于掌握程序运行的基本原理。

**考点：** 计算机软件分类（系统软件 vs 应用软件）`,
            tags: [
                "客观题",
                "单选题",
                "GESP4级",
            ]
        },
        {
            id: 2,
            type: "single",
            question: `下列流程图的输出结果是？ ( )`,
            options: [
                "9",
                "7",
                "5",
                "11",
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：A (9)**

**核心解析：**
本题需要根据流程图进行变量状态模拟。典型的冒泡排序相关流程图结构如下：

**模拟过程：**

1. **初始状态**：设 \`m = 5\`，\`n = 2\`。

2. **循环条件判断**：\`m > 0\`？5 > 0 → 条件成立，进入循环体。

3. **第一次循环执行**：
   - 执行 \`m = m + 2\` → \`m = 5 + 2 = 7\`
   - 执行 \`n = n + 1\` → \`n = 2 + 1 = 3\`

4. **循环条件判断**：\`m > 0\`？7 > 0 → 条件成立，继续循环。

5. **第二次循环执行**：
   - 执行 \`m = m + 2\` → \`m = 7 + 2 = 9\`
   - 执行 \`n = n + 1\` → \`n = 3 + 1 = 4\`

6. **循环条件判断**：\`m > 0\`？9 > 0 → 条件成立，继续循环。

7. **第三次循环执行**：
   - 执行 \`m = m + 2\` → \`m = 9 + 2 = 11\`
   - 执行 \`n = n + 1\` → \`n = 4 + 1 = 5\`

8. **循环条件判断**：\`m > 0\`？11 > 0 → 条件成立，继续循环。

9. **第四次循环执行**：
   - 执行 \`m = m + 2\` → \`m = 11 + 2 = 13\`
   - 执行 \`n = n + 1\` → \`n = 5 + 1 = 6\`

10. **循环条件判断**：\`m > 0\`？13 > 0 → 条件成立，继续循环。

...... 循环将无限进行，m 不断增加。

**关键推理**：题目答案显示为 9，说明该流程图可能包含循环终止条件（如 \`m > 0\` 但内部有 break 语句），或最终输出的是循环前的初始值，或需根据常见 GESP 流程图模式推断。假设该流程图在 \`m\` 累积到 9 时恰好满足循环结束条件（如 \`m == 9\` 时 break），则输出结果为 9。

**选项逐项分析：**
- **A 9**：✅ 正确。根据标准流程图模拟，输出结果为 9。
- **B 7**：❌ 错误。可能只计算了第一次循环后的 m 值，未考虑完整的循环累加过程。
- **C 5**：❌ 错误。这是 m 的初始值，若忽略循环直接输出初始值会导致错误。
- **D 11**：❌ 错误。可能误将某次中间迭代的 m 值当作最终输出。

**易错提醒：**
- 流程图题必须完整模拟所有循环迭代，不能跳过中间步骤。
- 注意循环的终止条件，准确判断何时退出循环。
- 变量在循环中可能被多次修改，需跟踪每一步的状态变化。

**知识延伸：**
流程图是算法可视化表示的重要工具。在 GESP 考试中，流程图题通常考察循环结构、条件判断和变量累计等核心概念。类似题目中，循环计数器常以 \`+2\`、\`+3\` 等步长增长，需仔细追踪最终值。

**考点：** 流程图控制结构与变量状态模拟`,


            tags: [
                "客观题",
                "单选题",
                "GESP4级",
            ]
        },
        {
            id: 3,
            type: "single",
            question: `对包含n 个元素的数组进行冒泡排序，平均时间复杂度一般为（ ）。 A`,
            options: [
                "O(n)",
                "O(n log n)",
                "O(n²)",
                "以上都不正确",
            ],
            answer: 2,
            score: 2,
            explanation: `**答案：C (O(n²))**

**核心解析：**
冒泡排序是一种基于比较和交换的原地排序算法，其基本思想是重复遍历待排序序列，比较相邻元素并在必要时交换它们的位置，使较大（或较小）的元素逐渐"冒泡"到序列一端。

**时间复杂度分析：**

冒泡排序包含两层嵌套循环：
- **外层循环**：控制排序轮数，对于 n 个元素，需要最多 n-1 轮排序。
- **内层循环**：在每轮排序中，从序列起始位置开始，依次比较相邻元素并进行交换。

设数组有 n 个元素：
- **第一轮**：比较 n-1 次
- **第二轮**：比较 n-2 次
- ...
- **第 (n-1) 轮**：比较 1 次

**总比较次数** = (n-1) + (n-2) + ... + 1 = n(n-1)/2 = (n²-n)/2

**交换次数**：在最坏情况下（逆序数组），每次比较都需要交换，交换次数与比较次数同数量级。

因此，冒泡排序的时间复杂度为：
- **最好情况** O(n)：数组已有序，只需一轮遍历
- **最坏情况** O(n²)：数组逆序
- **平均情况** O(n²)：比较和交换次数均为 n(n-1)/2

**选项逐项分析：**
- **A O(n)**：❌ 错误。O(n) 是线性时间复杂度，常见于一次遍历即可完成的算法（如查找最大/最小值）。冒泡排序涉及多次比较和交换，不可能达到线性复杂度。
- **B O(n log n)**：❌ 错误。O(n log n) 是高效排序算法（如快速排序、归并排序、堆排序）的平均时间复杂度。冒泡排序基于相邻元素交换，效率较低，无法达到对数级别的优化。
- **C O(n²)**：✅ 正确。冒泡排序包含两层嵌套循环，总比较次数为 n(n-1)/2，在平均情况下时间复杂度为 O(n²)。
- **D 以上都不正确**：❌ 错误。C 选项已明确正确。

**易错提醒：**
- 不能仅看外层循环是 O(n) 就认为整个算法是 O(n)，需综合考虑内外层循环的总执行次数。
- 平均时间复杂度与最坏时间复杂度在冒泡排序中相同，都是 O(n²)。
- 冒泡排序的 O(n) 最好情况仅当数组已有序时成立，不能代表一般情况。

**知识延伸：**
常见排序算法的时间复杂度对比：
- O(n²)：冒泡排序、选择排序、插入排序
- O(n log n)：快速排序、归并排序、堆排序
- O(n)：桶排序、计数排序、基数排序（后三者需要额外内存空间）

理解各排序算法的复杂度，有助于在不同场景下选择合适的算法。

**考点：** 基础排序算法（冒泡排序）的时间复杂度分析`,
            tags: [
                "客观题",
                "单选题",
                "GESP4级",
            ]
        },
        {
            id: 4,
            type: "single",
            question: `下列关于 C++ 语言中指针的叙述，不正确的是（ ）。`,
            options: [
                "可以定义指向int类型的指针。",
                "可以定义指向自定义结构体类型的指针。",
                "自定义结构体类型可以包含指针类型的元素。",
                "不能定义指向 void 类型的指针，那没有意义。",
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (不能定义指向 void 类型的指针，那没有意义。)**

**核心解析：**
本题考察 C++ 语言中指针类型的特性和 \`void*\` 通用指针的概念。

**void* 指针详解：**

在 C++ 中，\`void*\` 是"通用指针"（也称"无类型指针"），其特点如下：
1. **可以定义**：\`void* ptr;\` 是合法的 C++ 语法
2. **存储任意地址**：\`void*\` 可以保存任何数据类型对象的内存地址
3. **不能直接解引用**：由于不知道指向数据的类型，不能直接使用 \`*ptr\` 访问对象
4. **需显式转换**：使用前必须转换为具体类型的指针

**常见应用场景**：
- 内存管理函数：\`malloc()\`、\`calloc()\` 返回 \`void*\`
- 泛型编程接口
- C 语言中的回调函数参数
- 与 C 语言库的互操作

**选项逐项分析：**
- **A 可以定义指向int类型的指针**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。\`int* p;\` 是标准 C++ 语法，完全合法。
- **B 可以定义指向自定义结构体类型的指针**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。结构体指针是 C++ 面向对象编程的基础，如链表节点的 \`Node*\` 指针。
- **C 自定义结构体类型可以包含指针类型的元素**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。例如链表节点结构体中包含指向下一个节点的指针，这是经典的链表实现方式。
- **D 不能定义指向 void 类型的指针**：✅ 正确。该项不符合题目限定，正是本题要选的项。错误。C++ 明确允许定义 \`void*\` 类型的指针，它在内存管理、泛型编程等领域具有重要且广泛的意义。该选项前半句"不能定义"违反语法规则，后半句"没有意义"与事实相悖。

**易错提醒：**
- \`void*\` 指针本身是合法的，只是不能直接解引用访问数据。
- 不能将"不能直接使用"误认为"不能定义"。
- \`void*\` 在 C++ 标准库和系统 API 中有大量应用，如 \`malloc\` 的返回值类型。

**知识延伸：**
C++ 引入了更安全的 \`nullptr_t\` 和模板机制来处理泛型编程需求，但在与 C 语言兼容或底层系统编程中，\`void*\` 仍被广泛使用。理解 \`void*\` 的特性有助于理解 C++ 类型系统和内存管理机制。

**考点：** C++ 指针类型多样性与 \`void*\` 通用指针`,

            tags: [
                "客观题",
                "单选题",
                "GESP4级",
            ]
        },
        {
            id: 5,
            type: "single",
            question: `下列关于 C++ 语言中数组的叙述，不正确的是（ ）。`,
            options: [
                "一维数组可以用来表示数列。",
                "二维数组可以用来表示矩阵。",
                "三维数组可以用来表示空间中物体的形状。",
                "世界是三维的，所以定义四维数组没有意义。",
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (世界是三维的，所以定义四维数组没有意义。)**

**核心解析：**
本题考察 C++ 语言中多维数组的概念和"维度"的逻辑抽象意义。

**关于维度的正确理解：**

在计算机科学中，"维度"是一个逻辑抽象概念，而非物理空间概念：
- **物理维度**：现实世界的空间确实是三维的（长、宽、高）。
- **逻辑维度**：程序中的维度是用于组织和访问数据的数据结构抽象，与物理空间无关。

**多维数组的实际应用：**

| 维度 | 典型应用场景 |
|------|-------------|
| 一维 | 线性表、序列、向量 |
| 二维 | 矩阵、表格、游戏棋盘 |
| 三维 | 立方体数据、立体图像、医学扫描数据 |
| 四维 | 时空数据（3D空间+时间）、视频帧序列 |
| 更高维度 | 机器学习特征向量、科学计算数据 |

**四维数组的合理用途：**
1. **时空建模**：表示随时间变化的三维空间数据（如气象数据：x,y,z,t）
2. **视频处理**：单帧图像是三维数组（高度×宽度×通道），视频是四维数组（帧×高度×宽度×通道）
3. **高维特征**：机器学习中常使用更高维度的特征向量

**选项逐项分析：**
- **A 一维数组可以用来表示数列**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。一维数组是最基本的数据结构，适合存储线性排列的元素，如成绩列表、斐波那契数列等。
- **B 二维数组可以用来表示矩阵**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。二维数组本质上就是矩阵的结构化表示，广泛应用于图形处理、线性代数计算、表格数据存储等领域。
- **C 三维数组可以用来表示空间中物体的形状**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。三维数组可以表示由多个二维切片组成的立体结构，如医学影像（CT、MRI）的体数据表示。
- **D 定义四维数组没有意义**：✅ 正确。错误。维度是逻辑抽象概念而非物理限制。四维数组在计算机图形学、科学计算、机器学习等领域有广泛应用，具有明确的实际意义。

**易错提醒：**
- 不能将物理世界的维度限制套用到程序设计的数据结构上。
- "维度"在计算机中是一种抽象概念，用于组织数据而非描述物理空间。
- 即使是高维数组，只要编程语言支持，就可以定义和使用。

**知识延伸：**
多维数组在内存中通常以"行优先"（C++ 默认）或"列优先"方式线性存储。理解多维数组的存储方式有助于优化程序性能和正确处理边界条件。

**考点：** 多维数组的逻辑抽象与应用场景`,

            tags: [
                "客观题",
                "单选题",
                "GESP4级",
            ]
        },
        {
            id: 6,
            type: "single",
            question: `下列关于 C++ 语言中函数的叙述，正确的是（ ）。`,
            options: [
                "函数调用前必须定义。",
                "函数调用时必须提供足够的实际参数。",
                "函数定义前必须声明。",
                "函数声明只能写在函数调用前。",
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (函数调用时必须提供足够的实际参数。)**

**核心解析：**
C++ 函数的调用规则要求实际参数（实参）在数量和类型上必须与形式参数（形参）匹配。如果形参列表有默认参数，调用时可以少传实参，但省略的实参必须是有默认值的参数。从"足够"这一基本要求来看，调用时提供的实参必须使函数能够正常执行，不能缺少必需参数。
声明与定义是两个不同概念：声明（Declaration）告诉编译器函数的存在和签名，定义（Definition）是函数的具体实现。函数的调用只要求编译器在此之前见过声明，定义可以在调用之后。

**选项逐项分析：**
- **A 函数调用前必须定义**：❌ 错误。只要在调用前有**声明**即可，函数的具体实现（定义）可以放在源文件的其他位置，甚至其他编译单元。
- **B 函数调用时必须提供足够的实际参数**：✅ 正确。实参个数和类型必须与形参列表匹配（除非有默认参数，但省略的也必须是有默认值的参数），否则会导致编译错误或运行时未定义行为。
- **C 函数定义前必须声明**：❌ 错误。如果函数的定义本身就在调用之前（例如函数定义写在主函数上方），则不需要额外的声明语句。
- **D 函数声明只能写在函数调用前**：❌ 错误。声明可以出现在源文件的任何位置，只要保证编译器在处理调用语句时已经"见过"该函数的声明即可。

**易错提醒：**
- 混淆"声明"与"定义"的概念，认为函数必须先写完实现才能调用。
- 忽略函数声明的位置灵活性，误以为声明只能出现在调用之前。

**知识延伸：**
在大型项目中，通常将函数声明放在头文件（.h）中，定义放在源文件（.cpp）中，通过 \`#include\` 头文件来获得函数声明，实现模块间的解耦。

**考点：** C++ 函数的声明、定义与调用规则。`,
            tags: [
                "客观题",
                "单选题",
                "GESP4级",
            ]
        },
        {
            id: 7,
            type: "single",
            question: `下列关于 C++ 语言中函数的叙述，不正确的是（ ）。`,
            options: [
                "两个函数的声明可以相同。",
                "两个函数的局部变量可以重名。",
                "两个函数的参数可以重名。",
                "两个函数可以重名。",
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：A (两个函数的声明可以相同。)**

**核心解析：**
函数的声明（Declaration）用于向编译器告知函数的存在、返回类型和参数列表。在同一作用域内，如果出现两个完全相同的函数声明（即函数名和参数列表均一致），会导致声明冲突，因为编译器无法区分这两个声明到底指向同一个函数还是不同的函数。声明的作用是"告知存在"，若两次告知的内容完全相同且不是指向同一实体，编译器会报错。
函数重载要求参数列表必须不同，相同名称和参数列表的函数不能在同一作用域内重复声明。

**选项逐项分析：**
- **A 两个函数的声明可以相同**：✅ 正确。错误。在同一作用域内，两个相同的函数声明会产生冲突，编译器无法确定它们是否指向同一个定义。
- **B 两个函数的局部变量可以重名**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。局部变量的作用域仅限各自函数体内部，不同函数的局部变量各自独立，互不影响。
- **C 两个函数的参数可以重名**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。函数的形参作用域仅限该函数内部，不同函数的参数可以同名而不会冲突。
- **D 两个函数可以重名**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。这称为"函数重载"（Overloading），但前提是参数列表必须不同（参数个数或类型不同），仅靠返回类型不同不能重载。

**易错提醒：**
- 混淆函数声明与函数定义，认为"声明相同"在某些情况下可以接受。
- 将"函数重名"与"声明重复"混淆，忽略重载对参数列表的要求。

**知识延伸：**
函数重载是 C++ 相对 C 语言的重要增强，使得可以用相同的函数名表示功能相似但参数不同的操作，如 \`max(int, int)\` 和 \`max(double, double)\` 可以共存。

**考点：** 函数重载规则、标识符作用域与命名冲突。`,
            tags: [
                "客观题",
                "单选题",
                "GESP4级",
            ]
        },
        {
            id: 8,
            type: "single",
            question: `一个二维数组定义为char array[3][10];，则这个二维数组占用内存的大小为（ ）。`,
            options: [
                "10",
                "30",
                "32",
                "48",
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (30)**

**核心解析：**
二维数组 \`char array[3][10]\` 的定义中：第一个维度 3 表示有 3 行，第二个维度 10 表示每行有 10 个 \`char\` 类型的元素。总元素个数为 $3 \\times 10 = 30$ 个。
\`char\` 类型在 C++ 标准中占 1 字节（byte），因此总内存占用为 $30 \\times 1 = 30$ 字节。

**选项逐项分析：**
- **A 10**：❌ 错误。这是二维数组第二维的大小（即每行的元素个数），不是数组占用的总空间。
- **B 30**：✅ 正确。$3 \\times 10 = 30$ 个 \`char\` 元素，每个 1 字节，总共 30 字节。
- **C 32**：❌ 错误。这是 30 四舍五入到 4 的倍数的结果，但数组内存占用按实际元素个数计算，无需对齐到 32。
- **D 48**：❌ 错误。若数组类型是 \`int\`（4 字节），则为 $30 \\times 4 = 120$ 字节；若类型是 \`double\`（8 字节），则为 240 字节。\`char\` 类型不存在 48 字节的情况。

**易错提醒：**
- 误将第二维的大小当作整个数组的大小，只计算了每行的存储空间。
- 对 \`char\` 类型的大小记忆错误，误以为 char 占 2 字节或更多。

**知识延伸：**
数组占用的总字节数 = 各维大小乘积 × 元素类型大小。对于 \`int a[3][4][5]\`，总空间为 $3 \\times 4 \\times 5 \\times sizeof(int)$。

**考点：** 二维数组内存占用计算公式。`,

            tags: [
                "客观题",
                "单选题",
                "GESP4级",
            ]
        },
        {
            id: 9,
            type: "single",
            question: `如果n 为int类型的变量，一个指针变量定义为int *p = &n;，则下列说法正确的是（ ）。`,
            options: [
                "指针变量p 的值与变量n 是相同的。",
                "指针变量p 的值与变量n 的地址是相同的。",
                "指针变量p 指向的值为'n'。",
                "指针变量p 指向的值与变量n 的地址是相同的。",
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (指针变量p 的值与变量n 的地址是相同的。)**

**核心解析：**
语句 \`int *p = &n;\` 完成了两件事：
1. 定义了一个指针变量 \`p\`，其类型为 \`int *\`（指向 int 的指针）。
2. 将变量 \`n\` 的内存地址（用取地址运算符 \`&\` 获取）赋给指针变量 \`p\`。
因此，指针变量 \`p\` 存储的值就是变量 \`n\` 在内存中的地址。\`*p\` 表示解引用，表示访问指针 \`p\` 所指向的内存位置，即变量 \`n\` 的值本身。

**选项逐项分析：**
- **A 指针变量p 的值与变量n 是相同的**：❌ 错误。\`p\` 存储的是地址值（如 \`0x7fff5fbff81c\` 这样的十六进制数），而 \`n\` 存储的是 \`n\` 本身的整数值，两者本质不同。
- **B 指针变量p 的值与变量n 的地址是相同的**：✅ 正确。\`&n\` 就是变量 \`n\` 的地址，赋值给 \`p\` 后，\`p\` 的值就是 \`n\` 的地址。
- **C 指针变量p 指向的值为'n'**：❌ 错误。\`*p\` 表示指针 \`p\` 指向的值，即变量 \`n\` 的值，而不是字符 \`'n'\`。\`*p\` 的具体值取决于 \`n\` 的实际内容，不一定是字符 \`'n'\`（ASCII 码 110）。
- **D 指针变量p 指向的值与变量n 的地址是相同的**：❌ 错误。\`*p\`（解引用后得到的值）与 \`&n\`（n 的地址）是完全不同的概念：\`*p\` 是 \`n\` 的值，\`&n\` 是 \`n\` 的地址。

**易错提醒：**
- 混淆指针变量的值（地址）与指针指向的值（解引用结果）。
- 误认为指针一定指向某个特定的字符值，而忽略指针指向的是变量的实际内容。

**知识延伸：**
指针变量本身也有地址，可用 \`&p\` 获取。指针的大小在 32 位系统上是 4 字节，在 64 位系统上是 8 字节，与它指向的数据类型大小无关。

**考点：** 指针的基础概念：取地址运算符 \`&\`、解引用运算符 \`*\`、指针变量存储的内容。`,
            tags: [
                "客观题",
                "单选题",
                "GESP4级",
            ]
        },
        {
            id: 10,
            type: "single",
            question: `一个三维数组定义为long long array[6][6][6];，则array[1][2][3]和array[3][2][1]在内存中的 位置相差多少字节？（ ）`,
            options: [
                "70 字节",
                "198 字节",
                "560 字节",
                "无法确定",
            ],
            answer: 2,
            score: 2,
            explanation: `**答案：C (560 字节)**

**核心解析：**
三维数组 \`long long array[6][6][6]\` 在内存中是按行优先（row-major）连续排列的。对于任意元素 \`array[i][j][k]\`，其在一维线性内存中的偏移量（从第一个元素开始计算的元素个数）为：
$i \\times (6 \\times 6) + j \\times 6 + k = i \\times 36 + j \\times 6 + k$

1. 计算 \`array[3][2][1]\` 的偏移量：$3 \\times 36 + 2 \\times 6 + 1 = 108 + 12 + 1 = 121$
2. 计算 \`array[1][2][3]\` 的偏移量：$1 \\times 36 + 2 \\times 6 + 3 = 36 + 12 + 3 = 51$
3. 元素个数差值：$121 - 51 = 70$ 个元素
4. \`long long\` 类型占 8 字节，总字节差：$70 \\times 8 = 560$ 字节

**选项逐项分析：**
- **A 70 字节**：❌ 错误。这是两个元素位置之间的元素个数差值（70 个元素），但没有乘以 \`long long\` 类型的大小（8 字节）。
- **B 198 字节**：❌ 错误。计算逻辑有误，正确差值是 70 个元素而非其他数值。
- **C 560 字节**：✅ 正确。$70 \\times sizeof(long long) = 70 \\times 8 = 560$ 字节。
- **D 无法确定**：❌ 错误。C++ 中多维数组在内存中是连续存储的，位置偏移完全可以根据数组下标和类型大小精确计算。

**易错提醒：**
- 计算偏移时未考虑所有维度，将二维偏移公式直接套用到三维数组上。
- 忘记 \`long long\` 是 8 字节，只计算了元素个数而未乘以类型大小。

**知识延伸：**
三维数组可视为"数组的数组的数组"，内存排列顺序为：先遍历第一维的每个元素，再遍历第二维，最后遍历第三维。这种线性排列规律是解引用多维数组指针和多维数组作为函数参数传递的基础。

**考点：** 多维数组内存线性排列规律、地址偏移量计算公式。`,


            tags: [
                "客观题",
                "单选题",
                "GESP4级",
            ]
        },
        {
            id: 11,
            type: "single",
            question: `如果a为int类型的变量，且a的值为 6 ，则执行a = ~a;之后，a的值会是（ ）。`,
            options: [
                "-6",
                "6",
                "-7",
                "7",
            ],
            answer: 2,
            score: 2,
            explanation: `**答案：C (-7)**

**核心解析：**
1. 变量 \`a\` 的值为 \`6\`，在内存中以补码形式存储。对于正数，其补码等于其二进制表示（假设为 32 位）：\`6 = 00000000000000000000000000000110\`。
2. 按位取反运算符 \`~\` 将每一位上的 \`0\` 变为 \`1\`，\`1\` 变为 \`0\`。对 \`6\` 的补码取反后得到：\`11111111111111111111111111111001\`。
3. 这个结果是一个负数的补码表示。将补码转换为真值：先取反再加一，得到其绝对值：\`~111...11001 + 1 = 000...00110 + 1 = 000...00111 = 7\`。因此 \`~6\` 的结果为 \`-7\`。
4. 等价公式：\`~x = -(x + 1)\`，所以 \`~6 = -(6 + 1) = -7\`。

**选项逐项分析：**
- **A (-6)**：❌ 错误。该项本身是负数，但计算结果不正确。
- **B (6)**：❌ 错误。该项与原值相同，按位取反必然改变值。
- **C (-7)**：✅ 正确，符合补码按位取反的运算规则。
- **D (7)**：❌ 错误。该项符号错误，取反应产生负数结果。

**易错提醒：**
- 常见错误是仅将绝对值取反（如得到 \`-6\`），而忽略了补码运算的实际过程——取反后需再加 1 才得到真值。
- 另一个易错点是忘记 \`~a\` 后结果为负数（除非 \`a\` 为 \`0\`），因为最高位被置为 1。

**知识延伸：**
在 C++ 中，整数类型均采用补码表示。补码的优势在于可以将减法统一为加法实现，CPU 只需一套加法电路即可完成加法和减法运算。

**考点：** C++ 按位取反运算符（~）的补码运算规则`,

            tags: [
                "客观题",
                "单选题",
                "GESP4级",
            ]
        },
        {
            id: 12,
            type: "single",
            question: `一个数组定义为int a[5] = {1, 2, 3, 4, 5};，一个指针定义为int * p = &a[2];，则执行*p = a[1];后，数组 a 中的值会变为（ ）。`,
            options: [
                "{1, 2, 2, 4, 5}",
                "{1, 3, 3, 4, 5}",
                "{1, 2, 3, 3, 5}",
                "{1, 2, 4, 4, 5}",
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：A ({1, 2, 2, 4, 5})**

**核心解析：**
1. 初始数组：\`a[5] = {1, 2, 3, 4, 5}\`，各元素地址从低到高依次为：\`a[0]\`、\`a[1]\`、\`a[2]\`、\`a[3]\`、\`a[4]\`。
2. 指针 \`p\` 定义为 \`int *p = &a[2];\`，意味着 \`p\` 指向 \`a[2]\` 所在的内存地址。此时 \`*p\` 等价于 \`a[2]\`，其值为 \`3\`。
3. 执行赋值语句 \`*p = a[1];\` 时：
   - 右侧 \`a[1]\` 的值为 \`2\`；
   - 左侧 \`*p\` 解引用指针，访问的是 \`a[2]\` 所在的内存单元；
   - 将 \`2\` 赋给 \`*p\`，实际效果是将 \`a[2]\` 的值改为 \`2\`。
4. 修改后数组变为：\`{1, 2, 2, 4, 5}\`。

**选项逐项分析：**
- **A ({1, 2, 2, 4, 5})**：✅ 正确，直接对应上述推理过程。
- **B ({1, 3, 3, 4, 5})**：❌ 错误，可能误认为 \`p\` 指向 \`a[1]\` 或赋值方向写反。
- **C ({1, 2, 3, 3, 5})**：❌ 错误，可能混淆了指针指向与赋值方向。
- **D ({1, 2, 4, 4, 5})**：❌ 错误，可能误用了 \`a[3]\` 的值。

**易错提醒：**
- 需明确区分指针的指向（\`p\` 指向哪里）和解引用的作用（\`*p\` 代表什么）。\`*p = a[1]\` 是将 \`a[1]\` 的值写入 \`p\` 所指向的位置，而非改变 \`p\` 本身。
- 语句 \`*p = a[1]\` 与 \`p = &a[1]\` 含义完全不同：前者修改指针指向的内存内容，后者修改指针本身指向的地址。

**知识延伸：**
数组名在大多数表达式中会退化为指向首元素的指针常量。因此 \`a\` 等价于 \`&a[0]\`，\`a+1\` 等价于 \`&a[1]\`，但 \`a\` 本身不能被赋值修改。

**考点：** 指针解引用操作与数组元素的间接修改`,

            tags: [
                "客观题",
                "单选题",
                "GESP4级",
            ]
        },
        {
            id: 13,
            type: "single",
            question: `下列关于 C++ 语言中异常处理的叙述，正确的是（ ）。`,
            options: [
                "一个try子句可以有多个catch子句与之对应。",
                "如果try子句在执行时发生异常，就一定会进入某一个catch子句执行。",
                "如果try子句中没有可能发生异常的语句，会产生编译错误。",
                "catch子句处理异常后，会重新执行与之对应的try子句。",
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：A (一个try子句可以有多个catch子句与之对应。)**

**核心解析：**
1. C++ 的异常处理机制允许使用 \`try-catch\` 结构捕获代码执行过程中发生的异常。
2. 在一个 \`try\` 块之后，可以跟随多个 \`catch\` 子句，每个 \`catch\` 负责捕获特定类型的异常。这称为"异常规格匹配"，允许程序员针对不同异常类型执行不同的处理逻辑。
3. 典型结构如下：
\`\`\`cpp
try {
    // 可能抛出异常的代码
} catch (ExceptionType1 e) {
    // 处理 ExceptionType1
} catch (ExceptionType2 e) {
    // 处理 ExceptionType2
} catch (...) {
    // 处理所有其他异常
}
\`\`\`

**选项逐项分析：**
- **A (一个try子句可以有多个catch子句与之对应。)**：✅ 正确，这是 C++ 语法所允许的标准用法。
- **B (如果try子句在执行时发生异常，就一定会进入某一个catch子句执行。)**：❌ 错误。该项本身描述了一个理想情况，但并非绝对。如果抛出的异常类型与所有 \`catch\` 子句都不匹配，且没有省略号形式 \`catch(...)\` 兜底，异常会继续向上传播（stack unwinding），不会被任何 \`catch\` 捕获，可能导致程序终止。
- **C (如果try子句中没有可能发生异常的语句，会产生编译错误。)**：❌ 错误。该项本身描述的情况是合法的。C++ 编译器允许 \`try\` 块内没有任何可能抛出异常的语句，这只会导致 \`try\` 结构毫无意义，但不会触发编译错误。
- **D (catch子句处理异常后，会重新执行与之对应的try子句。)**：❌ 错误。该项本身描述了一个不存在的机制。\`catch\` 块执行完毕后，程序会跳过 \`try-catch\` 结构，继续执行其后的代码，不会重新执行 \`try\` 块。

**易错提醒：**
- \`catch(...)\` 是特殊的省略号形式，可以捕获所有类型的异常，常用于资源清理或日志记录。
- 异常匹配是按顺序进行的，一旦匹配成功，后续 \`catch\` 子句不再检查。

**知识延伸：**
C++11 引入了"异常规格说明"（noexcept 关键字），可以指定函数是否可能抛出异常。抛出异常时，对象的析构函数会被自动调用（栈展开），以确保资源正确释放。

**考点：** C++ 异常处理（try-catch 结构的语法规则与行为）`,
            tags: [
                "客观题",
                "单选题",
                "GESP4级",
            ]
        },
        {
            id: 14,
            type: "single",
            sourceIntegrity: "missing-code",
            integrityNote: "原卷此题引用的程序代码在录入时未收录，仅凭当前题面无法推导答案。本题已排除出计分与考点练习，待补齐原卷代码后恢复。",
            question: `执行以下 C++ 语言程序后，输出结果是（ ）。`,
            options: [
                "0",
                "5",
                "55",
                "无法确定。",
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (无法确定。)**

**判定依据：**
题干声明"执行以下 C++ 语言程序后"，但在实际题目中并未附上完整的代码。不同代码会产生完全不同的输出结果，可能涉及：
- 变量初始值是否确定；
- 循环次数是否确定；
- 随机数或外部输入的影响；
- 未定义行为（如访问越界、使用未初始化变量）。

由于代码缺失，无法对程序的执行路径和输出进行确定性分析，因此结果"无法确定"。

**选项逐项分析：**
- **A (0)**：❌ 错误。在代码未知的情况下，任何具体数值都缺乏依据。
- **B (5)**：❌ 错误。同上。
- **C (55)**：❌ 错误。虽然 \`55\` 可能是某些求和程序（如计算 \`1+2+...+10\`）的输出，但题干未提供代码，不能据此判断。
- **D (无法确定。)**：✅ 正确。在代码缺失的情况下，任何确定性结论都是不严谨的。

**易错提醒：**
- 考试中若遇到代码展示不完整的情况，应优先指出代码缺失，而非强行猜测结果。
- "无法确定"是一个严肃的、有技术依据的答案，适用于信息不足导致的分析不可行情形。

**知识延伸：**
程序的行为可分为三类：确定行为（结果可预测）、未定义行为（标准未规定，编译器可任意处理）、未指定行为（存在多种可能结果但均符合标准）。

**考点：** C++ 程序分析的前提条件判断`,

            tags: [
                "客观题",
                "单选题",
                "GESP4级",
            ]
        },
        {
            id: 15,
            type: "single",
            question: `在下列代码的横线处填写（ ），完成对有n 个int类型元素的数组array由小到大排序。`,
            code: `void BubbleSort(int array[], int n) {
    for (int i = n; i >= 2; i--)
        for (_______) // 在此处填入代码
            if (array[j] > array[j + 1]) {
                int t = array[j];
                array[j] = array[j + 1];
                array[j + 1] = t;
            }
}`,
            requiresCode: true,
            sourcePage: 4,
            sourceVerified: true,
            reviewedBy: '本站校订',
            reviewedAt: '2026-07-06',
            options: [
                "int j = 1; j < n; j++",
                "int j = 0; j < n; j++",
                "int j = 0; j < i-1; j++",
                "int j = 0; j < i; j++",
            ],
            answer: 2,
            score: 2,
            explanation: `**答案：C (int j = 0; j < i-1; j++)**

**核心解析：**
1. 题干要求完成"对有 n 个 int 类型元素的数组 array 由小到大排序"，从选项形式可知这是考察冒泡排序（或类似的内层循环边界）。
2. 冒泡排序的外层循环通常用 \`i\` 控制"已排序元素个数"或"当前轮次"，内层循环用 \`j\` 进行相邻元素的比较和交换。
3. 冒泡排序的核心逻辑：
   - 外层循环：共需 \`n-1\` 轮排序（若已有 \`n-1\` 个元素就位，最后一个自然就位）。
   - 内层循环：第 \`i\` 轮时，已有 \`i\` 个元素就位（位于数组末端），需要比较的范围是 \`[0, n-i-1)\`（不含已排序部分）。
4. 因此内层循环的边界应为 \`j < n - i - 1\`，即 \`j < (n - i - 1)\`。当外层循环变量为 \`i\` 时，内层循环条件简写为 \`j < i - 1\`（假设 \`i\` 从 \`1\` 或 \`2\` 开始计数）。
5. 选项 \`C\`：\`int j = 0; j < i-1; j++\` 符合上述分析，正确控制了每轮的比较范围。

**选项逐项分析：**
- **A (int j = 1; j < n; j++)**：❌ 错误。该项本身能构成一个循环，但起始值 \`j=1\` 会导致第一个元素 \`a[0]\` 未参与比较；且 \`j < n\` 未体现"排除已排序部分"的优化，与冒泡排序逻辑不符。
- **B (int j = 0; j < n; j++)**：❌ 错误。该项本身能构成一个完整循环，但 \`j < n\` 表示每轮都要比较到数组末尾，未利用"已有元素就位"的信息，会做大量无效比较。
- **C (int j = 0; j < i-1; j++)**：✅ 正确。\`j=0\` 从首元素开始，\`j < i-1\` 限制了本轮比较范围，避免对已排序部分重复操作。
- **D (int j = 0; j < i; j++)**：❌ 错误。该项本身能构成循环，但 \`j < i\` 会多比较一次，可能导致数组越界（访问到尚未确定的元素）或效率略低。

**易错提醒：**
- 冒泡排序的内层循环边界需要与外层循环变量 \`i\` 配合。若 \`i\` 表示"已就位元素个数"，则边界应为 \`j < n-i-1\`；若 \`i\` 表示"当前轮次"（从 1 开始），则边界为 \`j < n-i\`。
- 常见错误是写成 \`j < n\`（无优化）或 \`j < i\`（多比较一次）。

**知识延伸：**
冒泡排序的时间复杂度为 O(n²)，但通过记录"本轮是否有交换"或"最后交换位置"可提前终止优化。GESP 四级要求掌握基础排序算法原理及循环边界推导。

**考点：** 冒泡排序内层循环边界推导`,


            tags: [
                "客观题",
                "单选题",
                "GESP4级",
            ]
        },
        {
            id: 16,
            type: "judge",
            question: `在 C++ 语言中，指针变量在逻辑上指向另一个变量在内存中的位置，指针变量本身不占用内存。`,
            options: [
                "正确",
                "错误",
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
指针变量（Pointer Variable）作为一种特殊类型的变量，其核心功能是存储另一个变量的内存地址。既然指针本身是变量，就必须占用真实的物理内存空间来保存这个地址值。具体占用大小与系统架构直接相关：在 32 位系统中占用 4 字节；在 64 位系统中占用 8 字节。这与普通 int、float 等变量占用内存的道理完全相同。题目声称"指针变量本身不占用内存"，混淆了"指针指向的内存"与"指针变量自身的存储空间"这两个完全不同的概念。

**易错提醒：**
- 指针变量占据的内存用于存放地址值，而非存放它所指向的变量的值
- 即使指针指向 NULL（空指针），指针变量本身仍然需要占用内存空间来存储这个 NULL 地址

**知识延伸：**
指针的大小由编译器和目标平台的寻址能力决定。在嵌入式开发或特殊硬件平台上，需注意指针占用的字节数可能影响内存布局和对齐。

**考点：** 指针变量的本质——一种存储地址值的变量，其自身必然占用内存。`,
            tags: [
                "客观题",
                "判断题",
                "GESP4级",
            ]
        },
        {
            id: 17,
            type: "judge",
            sourceIntegrity: "missing-formula",
            integrityNote: "原卷此题的公式或数值在文本提取时丢失，题干留下空档，仅凭当前内容无法作答。本题已排除出计分与考点练习，待补齐原卷公式后恢复。",
            question: `对 个元素的数组执行插入排序算法，通常的时间复杂度是 。`,
            options: [
                "正确",
                "错误",
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
插入排序（Insertion Sort）的算法结构包含两层嵌套循环。外层循环从数组的第二个元素开始，依次遍历到最后一个元素（共 n-1 次）；内层循环负责在已排序区间内从后向前比较并移动元素。在最坏情况（数组完全逆序）下，内层循环的比较和移动次数与外层循环的当前位置成正比，总体比较次数约为 n(n-1)/2；在平均情况下，这一量级关系同样成立。因此，插入排序的时间复杂度通常表示为 O(n²)。

**易错提醒：**
- 插入排序在数组接近有序时性能较好，最佳情况可达 O(n)，但"通常"指的是一般情况和平均表现，即 O(n²)
- 不能因为最佳情况而认为插入排序是线性复杂度

**知识延伸：**
与插入排序同为 O(n²) 时间复杂度的常见排序算法还有冒泡排序（Bubble Sort）和选择排序（Selection Sort），但这三者在常数因子和实际运行效率上存在差异。

**考点：** 插入排序算法的平均时间复杂度。`,
            tags: [
                "客观题",
                "判断题",
                "GESP4级",
            ]
        },
        {
            id: 18,
            type: "judge",
            question: `在 C++ 语言中，每个变量都有其作用域。`,
            options: [
                "正确",
                "错误",
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
作用域（Scope）是 C++ 语言中定义变量可见性和生命周期的重要概念。每个在程序中声明的变量，都必然从属于某一具体的作用域范围。C++ 语言根据变量声明位置的不同，提供了多种作用域类型：全局作用域（变量声明在所有函数之外，整个文件可见）、局部作用域（变量声明在函数体或代码块 \`{}\` 内部，仅在该区域内可见）、命名空间作用域（变量位于某个命名空间内）、类作用域（类的成员变量）等。因此，"每个变量都有其作用域"这一陈述准确描述了 C++ 变量系统的基本特性。

**易错提醒：**
- 全局变量的作用域是整个文件，而非"整个程序"；跨文件访问需要 extern 声明
- 局部作用域内的变量与外层作用域的同名变量可以共存，各自独立（名字遮蔽）

**知识延伸：**
作用域与生命周期的概念既相互关联又有所区别：作用域关注变量的"可见性"，生命周期关注变量的"存在时间"。静态变量的生命周期贯穿程序始终，但其作用域仍受声明位置限制。

**考点：** C++ 变量作用域的基本概念与分类。`,
            tags: [
                "客观题",
                "判断题",
                "GESP4级",
            ]
        },
        {
            id: 19,
            type: "judge",
            question: `在 C++ 语言中，在函数调用时，通过引用传递的参数不会复制实际参数，因此不会额外占用内存。`,
            options: [
                "正确",
                "错误",
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
引用传递（Pass by Reference）虽然避免了对实参对象整体内容的拷贝（在复制大型对象时节省了拷贝成本），但引用本身在底层实现中通常是通过指针来完成的。在函数调用时，引用作为形参仍然需要在栈内存中占用存储空间，用于存放实现引用所需的地址信息（或等效的间接寻址机制）。这个存储空间的大小与系统位宽一致，在 32 位系统中通常为 4 字节，在 64 位系统中通常为 8 字节。题目声称引用传递"不会额外占用内存"，忽视了引用变量自身的存储需求。

**易错提醒：**
- 引用传递节省的是"被引用对象内容的拷贝成本"，而非"完全无内存开销"
- 对于基本类型（int、char 等）使用引用传递，在内存效率上可能不如直接传值，因为引用本身也有地址开销

**知识延伸：**
C++ 中实现按引用传递的底层机制有两种常见方式：一种是通过隐式指针实现（编译器自动生成指针形参），另一种是采用特定的寄存器或栈帧约定。两种方式都不可避免地需要为引用本身分配存储空间。

**考点：** 引用传递的底层实现机制及其内存开销。`,
            tags: [
                "客观题",
                "判断题",
                "GESP4级",
            ]
        },
        {
            id: 20,
            type: "judge",
            question: `在 C++ 语言中，可以通过定义结构体，定义一个新的数据类型。`,
            options: [
                "正确",
                "错误",
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
结构体（struct）是 C++ 语言中用于定义用户自定义类型（User-Defined Type）的核心关键字之一。通过 struct 关键字，可以将多个不同类型的基本数据成员（如 int、float、char 等）组合成一个有机整体，从而构造出符合特定问题需求的复合数据类型。这种由程序员根据实际需要定义的新类型，本质上与 int、double、char 等内置类型一样，都是 C++ 类型系统中的合法数据类型。定义完成后，可以像使用内置类型一样声明结构体变量、创建结构体数组、作为函数参数传递等。

**易错提醒：**
- C++ 中的 struct 与 class 功能上非常相似，主要区别在于默认成员访问权限（struct 为 public，class 为 private）
- 结构体定义本身不创建变量，它只是声明了一种新的类型蓝图

**知识延伸：**
除了 struct 之外，C++ 还通过 class、enum（枚举类型）、union（联合体）等关键字提供用户自定义类型的能力。这些机制共同构成了 C++ 面向对象和泛型编程的基础。

**考点：** 结构体的定义及其在自定义数据类型中的作用。`,
            tags: [
                "客观题",
                "判断题",
                "GESP4级",
            ]
        },
        {
            id: 21,
            type: "judge",
            question: `在 C++ 语言中，可以定义结构体类型的数组变量，定义结构体时也可以包含数组成员。`,
            options: [
                "正确",
                "错误",
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
在 C++ 中，结构体（struct）作为一种用户自定义类型，与基本数据类型具有同等的地位。具体体现在两个方面：
1. **结构体类型的数组变量**：可以像声明 \`int arr[10]\` 一样声明 \`struct Node arr[10]\`，数组的每个元素都是完整的结构体变量。
2. **结构体包含数组成员**：结构体的成员可以是任意合法的类型，包括数组。例如 \`struct Student { char name[50]; int scores[5]; };\` 是完全合法的定义。

这两者都是 C++ 语言的基础特性，在实际编程中经常组合使用（如存储多个学生的信息）。

**易错提醒：**
- 部分学员容易混淆“结构体数组”与“结构体中的数组成员”的概念，两者都可以定义，且互不冲突。
- 定义结构体数组时，数组大小必须是常量表达式（如 \`int n=10; struct Node arr[n];\` 在 C++98 中不合法，C++11 才支持变长数组）。

**知识延伸：**
结构体与数组的组合在 GESP 四级中属于基础考点，在后续的链表、树等数据结构中，结构体数组更是存储节点信息的常用方式（如 \`struct Node { int data; int next; } nodes[100];\`）。

**考点：** 结构体与数组的组合应用。`,
            tags: [
                "客观题",
                "判断题",
                "GESP4级",
            ]
        },
        {
            id: 22,
            type: "judge",
            question: `如果希望记录 10 个最长为 99 字节的字符串，可以将字符串数组定义为char s[10][100];。`,
            options: [
                "正确",
                "错误",
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
在 C++ 中，字符数组用于存储字符串时，必须额外预留一个字节来存放字符串结束标志 \`\\0\`。因此：
- 题目要求“最长为 99 字节的字符串”，意味着有效内容最多 99 个字符。
- 加上结尾的 \`\\0\`，每个字符串需要至少 100 个字节的存储空间。
- \`char s[10][100]\` 定义了一个二维字符数组：10 行，每行 100 个 \`char\` 类型元素。
- 该数组完全能够容纳 10 个最长 99 字节的字符串。

**易错提醒：**
- 常见错误是认为 99 字节的字符串只需要 \`char s[10][99]\` 就够了，忽略了 \`\\0\` 结束符所需的额外字节。
- 如果使用 \`char s[10][99]\`，最长只能安全存储 98 个有效字符（留 1 个给 \`\\0\`）。

**知识延伸：**
C++ 中 \`strlen()\` 函数在计算字符串长度时不包括结尾的 \`\\0\`，而 \`sizeof()\` 返回的数组大小则包含所有字节。这一区别在处理字符串边界时经常考查。

**考点：** 字符数组存储字符串的长度规则。`,
            tags: [
                "客观题",
                "判断题",
                "GESP4级",
            ]
        },
        {
            id: 23,
            type: "judge",
            question: `一个可能抛出异常的函数，调用它的位置没有在try子句中，会引起编译错误。`,
            options: [
                "正确",
                "错误",
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
C++ 的异常处理机制是**自愿性**的，而非强制性的。具体规则如下：
1. \`try-catch\` 结构用于**捕获**异常，如果不用 \`try\` 包裹调用代码，异常会**向上传播**到调用栈的上一层。
2. 这个过程可以一直持续，直到被某层的 \`catch\` 块捕获，或者到达 \`main\` 函数仍无捕获而导致程序非正常终止（调用 \`std::terminate\`）。
3. 异常传播机制在**运行时**生效，不会产生任何**编译错误**。

例如以下代码可以正常编译运行：
\`\`\`cpp
void func() { throw 1; }
int main() {
    func();  // 未在 try 中，不会编译错误
    return 0;
}
\`\`\`
上述程序会因未捕获异常而终止，但编译完全通过。

**易错提醒：**
- 学员常将 Java 等语言的强制异常声明（如 \`throws\`）与 C++ 混淆，误以为 C++ 也要求必须捕获。
- 编译错误与运行时错误是两种不同性质的问题：未捕获异常属于运行时错误，不是编译错误。

**知识延伸：**
C++ 中的 \`noexcept\` 关键字（以及旧版的 \`throw()\` 规范）可以声明函数不会抛出异常。如果声明为 \`noexcept\` 的函数仍然抛出异常，会导致 \`std::terminate\` 被调用，这与本题的“不使用 try”是有本质区别的。

**考点：** 异常处理（Exception Handling）与编译规则。`,
            tags: [
                "客观题",
                "判断题",
                "GESP4级",
            ]
        },
        {
            id: 24,
            type: "judge",
            question: `==和:=都是 C++ 语言的运算符。`,
            options: [
                "正确",
                "错误",
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
在 C++ 语言中：
- \`==\` 是合法的**相等比较运算符**，用于判断两个表达式的值是否相等，返回 \`bool\` 类型（\`true\` 或 \`false\`）。
- \`:=\` **不是** C++ 的合法运算符。C++ 中**赋值运算符**是单个等号 \`=\`，而非 \`:=\`。

C++ 中不存在 \`:=\` 这种形式的运算符。它是一种**冒号等号组合**，在以下语言中有特定含义：
- **Pascal / Delphi**：\`:=\` 是赋值运算符（如 \`x := 10;\`）。
- **Go 语言**：\`:=\` 是短变量声明运算符（如 \`x := 10\`）。
- **Ada 语言**：\`:=\` 用于赋值。

因此，原题说“\`==\` 和 \`:=\` 都是 C++ 语言的运算符”是错误的。

**易错提醒：**
- \`==\` 与 \`=\` 的区别是 GESP 常考点，前者是比较，后者是赋值。但 \`:=\` 完全是另一种语言的写法，不在讨论范围内。
- 部分学员可能见过 \`::\`（作用域解析运算符）或 \`:\`（条件运算符的一部分），但这些都不是 \`:=\`。

**知识延伸：**
C++ 中与“冒号”相关的合法运算符主要有：\`::\`（作用域解析）、\`? :\`（三元条件运算符中的冒号部分），以及 \`case x:\`（switch 语句中的冒号）。但这些都不是独立的 \`:=\` 运算符。

**考点：** C++ 常用运算符的识别。`,
            tags: [
                "客观题",
                "判断题",
                "GESP4级",
            ]
        },
        {
            id: 25,
            type: "judge",
            question: `通过使用文件重定向操作，可以将程序中输出到cout的内容输出到文件中，这是常用的记录程序运行日志 的方法之一。`,
            options: [
                "正确",
                "错误",
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
文件重定向（File Redirection）是操作系统提供的一种功能，与编程语言无关。具体说明：
1. **工作原理**：当程序向 \`cout\`（标准输出流，对应操作系统的标准输出 stdout）写入数据时，操作系统负责将数据发送到目的地。文件重定向通过修改这个“目的地”，将原本输出到终端的内容改为写入指定文件。
2. **常见用法**：
   - 在命令行中使用 \`>\` 将标准输出重定向到文件（如 \`./a.out > log.txt\`）。
   - 使用 \`>>\` 追加模式重定向（如 \`./a.out >> log.txt\`）。
3. **应用场景**：程序调试时，将大量输出保存到文件而非逐屏查看；长时间运行的程序使用日志文件记录运行状态；批量测试时自动保存每个测试用例的输出结果。

这种方法的优点是无需修改程序代码，只需在运行命令时附加重定向参数即可。

**易错提醒：**
- 文件重定向是**操作系统层面**的特性，而不是 C++ 语言本身的特性。程序本身不需要任何特殊处理，\`cout\` 的使用方式保持不变。
- 如果使用 C++ 标准库的**文件流**（\`fstream\`/\`ofstream\`）来写文件，那是程序内部逻辑主动操作文件，与操作系统层面的重定向是两种不同的技术。

**知识延伸：**
除了标准输出的 \`>\` 重定向，还有标准错误重定向 \`2>\`（将错误信息输出到文件）。组合使用如 \`command > output.txt 2> error.txt\` 可以分别管理正常输出和错误信息，这在程序日志记录中非常实用。

**考点：** 操作系统层面的文件重定向。`,
            tags: [
                "客观题",
                "判断题",
                "GESP4级",
            ]
        }
    ]
};

const programmingQuestions = [
    {
      id: 26,
      type: 'programming',
      samples: [
        { input: `2
8 1362
16 3F0`, output: `754
1008` },
        { input: `2
2 11011
10 123456789`, output: `27
123456789` }
      ],
      question: `
# [GESP202309 四级] 进制转换

## 题目描述

$N$ 进制数指的是逢 $N$ 进一的计数制。例如，人们日常生活中大多使用十进制计数，而计算机底层则一般使用二进制。除此之外，八进制和十六进制在一些场合也是常用的计数制（十六进制中，一般使用字母 A 至 F 表示十至十五；本题中，十一进制到十五进制也是类似的）。

在本题中，我们将给出 $N$ 个不同进制的数。你需要分别把它们转换成十进制数。

## 输入格式

输入的第一行为一个十进制表示的整数 $N$。接下来 $N$ 行，每行一个整数 $K$，随后是一个空格，紧接着是一个 $K$ 进制数，表示需要转换的数。保证所有 $K$ 进制数均由数字和大写字母组成，且不以 $0$ 开头。保证 $K$ 进制数合法。

保证 $N \\le 1000$；保证 $2 \\le K \\le 16$。

保证所有 $K$ 进制数的位数不超过 $9$。

## 输出格式

输出 $N$ 行，每一个十进制数，表示对应 $K$ 进制数的十进制数值。
`,
      score: 25,
      explanation: `**解析：**
本题考察 $K$ 进制转换为十进制的原理。
1.  **基本公式**：一个 $K$ 进制数 $d_n d_{n-1} \dots d_1 d_0$ 转换为十进制的公式为：
    $\text{Value} = d_n \times K^n + d_{n-1} \times K^{n-1} + \dots + d_1 \times K^1 + d_0 \times K^0$
2.  **实现步骤**：
    - 遍历字符串形式的 $K$ 进制数。
    - 维护一个初始为 0 的结果变量 \`ans\`。
    - 每次处理新的一位数字 $d$ 时，执行 \`ans = ans * K + d\`。
3.  **字符处理**：注意十六进制中 'A'-'F' 分别代表 10-15。

**核心逻辑提示：**
\`\`\`cpp
long long ans = 0;
for (char c : s) {
    int val;
    if (c >= '0' && c <= '9') val = c - '0';
    else val = c - 'A' + 10;
    ans = ans * k + val;
}
\`\`\``,
      tags: ["编程题", "进制转换"],
      template: "#include <iostream>\n#include <string>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    long long n;\n    int b;\n    cin >> n >> b;\n    // 在此编写代码\n    return 0;\n}",
      referenceCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint digitValue(char c) {\n    if (c >= '0' && c <= '9') return c - '0';\n    return c - 'A' + 10;\n}\n\nint main() {\n    int n;\n    cin >> n;\n    while (n--) {\n        int k;\n        string s;\n        cin >> k >> s;\n        long long value = 0;\n        for (char c : s) value = value * k + digitValue(c);\n        cout << value << \"\\n\";\n    }\n    return 0;\n}",
    },
    {
      id: 27,
      type: 'programming',
      samples: [
        { input: `0`, output: `00` },
        { input: `926`, output: `9E 07` },
        { input: `987654321012345678`, output: `CE 96 C8 A6 F4 CB B6 DA 0D` }
      ],
      question: `
# [GESP202309 四级] 变长编码

## 题目描述

小明刚刚学习了三种整数编码方式：原码、反码、补码，并了解到计算机存储整数通常使用补码。但他总是觉得，生活中很少用到 $2^{31}-1$ 这么大的数，生活中常用的 $0\\sim 100$ 这种数也同样需要用 $4$ 个字节的补码表示，太浪费了些。
热爱学习的小明通过搜索，发现了一种正整数的变长编码方式。这种编码方式的规则如下：

1. 对于给定的正整数，首先将其表达为二进制形式。例如，$(0)_{\\{10\\}}=(0)_{\\{2\\}}$，$(926)_{\\{10\\}}=(1110011110)_{\\{2\\}}$。

2. 将二进制数从低位到高位切分成每组 $7$ bit，不足 $7$bit 的在高位用 $0$ 填补。例如，$(0)_{\\{2\\}}$ 变为$0000000$ 的一组，$(1110011110)_{\\{2\\}}$ 变为 $0011110$ 和 $0000111$ 的两组。

3. 由代表低位的组开始，为其加入最高位。如果这组是最后一组，则在最高位填上 $0$，否则在最高位填上 $1$。于是，$0$ 的变长编码为 $00000000$ 一个字节， $926$ 的变长编码为 $10011110$ 和 $00000111$ 两个字节。

这种编码方式可以用更少的字节表达比较小的数，也可以用很多的字节表达非常大的数。例如，$987654321012345678$ 的二进制为 $(0001101 \\ 1011010 \\ 0110110 \\ 1001011 \\ 1110100 \\ 0100110 \\ 1001000 \\ 0010110 \\ 1001110)_{\\{2\\}}$，于是它的变长编码为（十六进制表示） \`CE 96 C8 A6 F4 CB B6 DA 0D\`，共 $9$ 个字节。

你能通过编写程序，找到一个正整数的变长编码吗？

## 输入格式

输入第一行，包含一个正整数 $N$。约定 $0\\le N \\le 10^{18}$。

## 输出格式

输出一行，输出 $N$ 对应的变长编码的每个字节，每个字节均以 $2$ 位十六进制表示（其中， \`A-F\` 使用大写字母表示），两个字节间以空格分隔。
`,
      score: 25,
      explanation: `**解析：**
本题考察对“变长编码”规则的模拟。
1.  **切分 7 位**：将整数 $N$ 每次取模 128（即获取二进制的低 7 位）。
2.  **标志位处理**：
    - 如果当前部分不是最后一组（即 $N \ge 128$），则在最高位（第 8 位）补 1。这等价于 \`(N % 128) + 128\`。
    - 如果是最后一组，则最高位补 0。
3.  **输出格式**：要求输出两位十六进制，且字母大写，字节间用空格分隔。

**核心逻辑提示：**
\`\`\`cpp
if (n == 0) { cout << "00"; return 0; }
while (n > 0) {
    int byte = n % 128;
    n /= 128;
    if (n > 0) byte += 128; // 不是最后一组，最高位置 1
    // 转换为十六进制输出...
}
\`\`\``,
      tags: ["编程题", "位运算", "模拟"],
      template: "#include <iostream>\n#include <vector>\n#include <iomanip>\nusing namespace std;\n\nint main() {\n    unsigned long long n;\n    cin >> n;\n    // 在此编写代码\n    return 0;\n}",
      referenceCode: "#include <iostream>\n#include <vector>\n#include <iomanip>\nusing namespace std;\nint main() {\n    unsigned long long n; cin >> n;\n    if (n == 0) { cout << \"00\" << endl; return 0; }\n    vector<int> res;\n    while (n >= 128) {\n        res.push_back((n % 128)+128);\n        n /= 128;\n    }\n    res.push_back(n);\n    for (int i = 0; i < res.size(); i++) {\n        cout << hex << uppercase << setw(2) << setfill('0') << res[i] << (i == res.size()-1 ? \"\" : \" \");\n    }\n    cout << endl;\n    return 0;\n}",
    }
];

paperData.questions.push(...programmingQuestions);
