// 2025年6月 GESP C++ 四级真题
export const paperData = {
    id: '2025-06-l4',
    title: '2025年6月 GESP C++ 四级真题',
    level: 4,
    year: 2025,
    month: 6,
    session: 10,
    timeLimit: 5400,
    backfilled: true,
    questions: [
        {
            id: 1,
            type: "single",
            question: `在 C++ 中，声明一个指向整型变量的指针的正确语法是（ ）。`,
            options: [
                "int* ptr;",
                "*int ptr;",
                "int ptr*;",
                "ptr int;"
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：A (int* ptr;)**

**核心解析：**
C++ 中声明指针的语法规则是：类型名 + 星号 + 变量名，星号的位置可以灵活书写。\`int* ptr;\` 是标准写法，星号与类型名结合表示“指向 int 类型的指针”。C++ 编译器在解析时会将星号与变量名绑定，而非与类型名绑定，因此 \`int* ptr;\`、\`int *ptr;\`、\`int * ptr;\` 三种写法在功能上完全等价。但习惯上靠近类型写 (\`int*\`) 或靠近变量名写 (\`int *ptr\`) 均可，需注意同一声明中混用可能导致歧义（如 \`int* a, b;\` 中 a 是指针，b 反而只是 int）。

**选项逐项分析：**
- **A int* ptr;**：✅ 正确。符合 C++ 指针声明语法，星号紧跟类型名后，声明了一个名为 ptr 的整型指针变量。
- **B *int ptr;**：❌ 错误。星号不能写在类型名前方，这不符合 C++ 的语法规则，编译器无法识别。
- **C int ptr*;**：❌ 错误。星号位置错误，语法上不合法，C++ 不允许将声明符写在标识符之后。
- **D ptr int;**：❌ 错误。声明的基本顺序是“类型 标识符”，此处类型和变量名顺序颠倒，不符合语法。

**易错提醒：**
- 误以为 \`int*\` 是一个整体类型名，实际上星号是声明符的一部分，与变量名绑定。
- 在同一行声明多个指针时容易出错，如 \`int* a, b;\` 中 a 是指针而 b 是普通整型，应写作 \`int *a, *b;\`。

**知识延伸：**
指针声明的星号位置问题在 GESP 三级也涉及，指向不同类型对象的指针大小通常相同（均为地址宽度），但类型信息决定了指针解引用时读取的字节数。

**考点：** C++ 指针的定义与基础语法。`,
            tags: ["客观题", "单选题", "指针", "GESP4级"]
        },
        {
            id: 2,
            type: "single",
            question: `下面的函数接收一个 3 行 4 列的二维数组并输出其中元素，则横线上不能填写（ ）。\n\`\`\`cpp\nvoid printArray(________) {\n for (int i = 0; i < 3; ++i)\n for (int j = 0; j < 4; ++j)\n std::cout << arr[i][j] << " ";\n}\n\`\`\``,
            options: [
                "int arr[3][4]",
                "int arr[][4]",
                "int (*arr)[4]",
                "int** arr"
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (int** arr)**

**核心解析：**
二维数组作为函数参数传递时，编译器将其隐式转换为指向其第一行数组的指针。二维数组 \`int arr[3][4]\` 的实际类型是“包含 4 个整型元素的数组”，因此退化后的参数类型应为 \`int (*)[4]\`（指向含有 4 个整型元素的数组的指针）。\`int**\` 是“指向整型指针的指针”（二级指针），其内存布局与二维数组完全不同，不能直接接收二维数组。

**选项逐项分析：**
- **A int arr[3][4]**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。接受固定 3×4 的二维数组，维度完全匹配。
- **B int arr[][4]**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。二维数组作为参数时，第一维（行数）可以省略，但第二维（列数）必须显式指定，因为编译器需要根据列数来计算每行的偏移地址。
- **C int (*arr)[4]**：✅ 正确。显式声明为指向含有 4 个整型元素的数组的指针，与二维数组名退化后的类型完全一致。
- **D int** ✅ 正确。该项不符合题目限定，正是本题要选的项。arr**： 错误。\`int**\` 是二级指针，存储的是“指针的地址”，而二维数组在内存中是连续排列的 12 个整型数据，两者寻址方式完全不同，不能直接赋值或传参。

**易错提醒：**
- 不能将“指针的指针”与二维数组混为一谈。虽然二维数组可以用指针算术访问（如 \`*(arr + i*4 + j)\`），但其底层类型是 \`int*\` 而非 \`int**\`。
- 选项 B 中省略第一维是合法的，因为行数在函数内部通过循环上界控制。

**知识延伸：**
多维数组作为参数的规则可推广到更高维度：三维数组 \`int a[M][N][P]\` 退化后为 \`int (*)[N][P]\`。核心规律是：除了最低维度可以省略外，其他维度的大小必须保留，因为编译器依赖维度信息计算元素偏移。

**考点：** 二维数组作为函数参数的传递规则与二级指针的区别。`,
            tags: ["客观题", "单选题", "二维数组", "GESP4级"]
        },
        {
            id: 3,
            type: "single",
            question: `在 C++ 中，\`int arr[3][4]\` 和 \`int* arr = new int[12]\` 均可模拟一个 3 行 4 列的二维数组。关于这两种方式，下面说法错误的是（ ）。`,
            options: [
                "int arr[3][4] 在栈上分配空间，适合数组较小的情况",
                "int* arr = new int[12] 在堆上分配空间，数组较大时也适用",
                "这两种方式申请的内存空间都是连续的",
                "这两种方式申请的内存都能自动释放"
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (这两种方式申请的内存都能自动释放)**

**核心解析：**
C++ 的内存管理分为栈（Stack）和堆（Heap）两个区域。\`int arr[3][4]\` 是静态数组，在栈上分配，栈空间由编译器自动管理，函数返回时自动释放。而 \`int* arr = new int[12]\` 是动态内存分配，在堆上申请，必须由程序员显式调用 \`delete[]\` 释放。如果忘记释放，程序运行期间这块内存将持续占用，直至进程结束（部分操作系统会回收），这期间即为内存泄漏。

**选项逐项分析：**
- **A 栈上分配空间，适合数组较小**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。栈空间通常只有几 MB，过大的数组会导致栈溢出（Stack Overflow）。
- **B 堆上分配空间，数组较大适用**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。堆空间受物理内存和虚拟内存机制支持，可以分配较大的数组。
- **C 申请的内存空间都是连续的**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。\`int arr[3][4]\` 在栈上是连续排列的 12 个整型；\`new int[12]\` 在堆上同样申请一片连续的整型内存块。
- **D 这两种方式申请的内存都能自动释放**：✅ 正确。错误。\`new\` 表达式申请的堆内存不会自动释放，必须显式使用 \`delete[]\`（数组形式）释放，否则造成内存泄漏。

**易错提醒：**
- 误以为 C++ 会自动回收 \`new\` 申请的内存。实际上 \`new\` 与 \`malloc\` 对应，都需要配对使用 \`delete\` 和 \`free\`。
- 堆内存泄漏在短时间运行的程序中可能不明显，但在长期运行的服务或游戏中会导致内存逐渐耗尽。

**知识延伸：**
本题与 GESP 三级知识点“动态内存分配 new/delete”直接相关。配对原则为：\`new\` 对应 \`delete\`，\`new[]\` 对应 \`delete[]\`，混用或遗漏会导致未定义行为（如内存泄漏或崩溃）。

**考点：** C++ 内存模型（栈 vs 堆）及动态内存管理。`,
            tags: ["客观题", "单选题", "内存管理", "GESP4级"]
        },
        {
            id: 4,
            type: "single",
            question: `关于以下 C++ 代码，说法正确的是（ ）。\n\`\`\`cpp\nint main() {\n greet();\n return 0;\n}\nvoid greet() {\n cout << "Hello!" << endl;\n}\n\`\`\``,
            options: [
                "正确编译并输出 Hello!",
                "编译错误：找不到函数 greet()",
                "编译警告但可以运行",
                "链接错误"
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (编译错误：找不到函数 greet())**

**核心解析：**
C++ 遵循“先声明，后使用”的原则。编译器从上至下逐行分析代码，当 \`main\` 函数内部调用 \`greet()\` 时，编译器尚未遇到 \`greet\` 的声明或定义，因此无法确认 \`greet\` 是否存在、参数类型及返回值类型。编译器会在编译阶段报错，提示找不到标识符 \`greet\`。这不是链接错误，因为链接发生在编译通过之后；也不是警告，因为语法上无法为未声明的函数调用生成目标代码。

**选项逐项分析：**
- **A 正确编译并输出 Hello!**：❌ 错误。虽然函数 \`greet\` 本身定义正确，但因未声明就被调用，导致编译失败。
- **B 编译错误：找不到函数 greet()**：✅ 正确。编译器在处理 \`main\` 函数时，发现调用了未声明的标识符 \`greet\`，触发编译错误。
- **C 编译警告但可以运行**：❌ 错误。未声明函数调用是严重错误，会导致编译失败，不会产生可执行文件。
- **D 链接错误**：❌ 错误。链接错误发生在编译之后，此时源文件尚未生成目标文件，链接器无法介入。

**易错提醒：**
- 函数定义本身不是声明。必须显式提供函数声明（原型），或在定义之前调用（此时定义本身充当声明）。本题中 \`greet\` 的定义在 \`main\` 之后，且之前无声明，因此违反规则。
- 常见误解是“代码从上到下执行，调用时函数还没定义没关系”，但编译器并不按执行顺序分析，而是按源码顺序分析。

**知识延伸：**
解决方法有两种：一是将 \`greet\` 的定义移到 \`main\` 之前；二是在 \`main\` 之前添加前置声明 \`void greet();\`。这与 GESP 二级知识点“函数的基本使用”相关，但本题要求理解编译器的分析机制。

**考点：** 函数的声明（Declaration）与定义（Definition）顺序规则。`,
            tags: ["客观题", "单选题", "函数声明", "GESP4级"]
        },
        {
            id: 5,
            type: "single",
            question: `在 C++ 中，如果希望通过函数修改传入的结构体对象的内容，应该使用哪种参数传递方式？`,
            options: [
                "值传递或引用传递",
                "值传递或指针传递",
                "引用传递或指针传递",
                "仅指针传递"
            ],
            answer: 2,
            score: 2,
            explanation: `**答案：C (引用传递或指针传递)**

**核心解析：**
要通过函数修改外部结构体对象的内容，需要让函数内部能够访问到该对象在内存中的原始位置。C++ 中有三种参数传递方式：值传递（传递副本）、引用传递（传递对象本身别名）、指针传递（传递对象地址）。值传递会复制整个结构体，修改的是副本而非原对象，无法达到目的。引用和指针都允许函数通过地址间接访问原始数据，从而实现原地修改。

**选项逐项分析：**
- **A 值传递或引用传递**：❌ 错误。值传递产生对象副本，修改副本不会影响原对象，因此该选项前半部分错误。
- **B 值传递或指针传递**：❌ 错误。同上，值传递无法修改原对象，故该选项不正确。
- **C 引用传递或指针传递**：✅ 正确。引用传递通过别名直接操作原对象，指针传递通过地址间接操作原对象，两者均可实现修改外部数据的目的。
- **D 仅指针传递**：❌ 错误。过于绝对，引用传递同样可以达到目的，此选项排除了合法的引用传递方式。

**易错提醒：**
- 误以为指针是唯一能修改外部数据的方式，实际上引用更直观，且无需关心空指针问题（如 \`void foo(Type& obj)\` 调用时 obj 必须绑定到对象）。
- 结构体作为参数时，如果只读取不修改，应使用 \`const Type&\` 以提高效率并防止意外修改。

**知识延伸：**
本题知识点与 GESP 三级“函数与参数传递”相关。在实际工程中，通常优先使用引用传递（如 \`void modify(Student& s)\`），因为语法更简洁且不必显式取地址；而指针传递常用于需要表示“无对象”状态（如 \`nullptr\`）或需要移动对象指针的场景。

**考点：** C++ 函数参数传递方式（值、引用、指针）的功能区别。`,
            tags: ["客观题", "单选题", "参数传递", "结构体", "GESP4级"]
        },
        {
            id: 6,
            type: "single",
            question: `以下哪个选项正确描述了 C++ 中形参和实参的区别？`,
            options: [
                "形参是函数调用时传递给函数的具体值，实参是函数定义中声明的变量",
                "形参是函数定义中声明的变量，实参是函数调用时传递给函数的具体值",
                "形参和实参在函数调用时是完全相同的",
                "形参只在函数内部可见，实参在函数外部可见"
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (形参是函数定义中声明的变量，实参是函数调用时传递给函数的具体值)**

**核心解析：**
形参（Formal Parameter）和实参（Actual Argument）是函数调用机制中的一对基本概念，二者的区别在于出现的时机和角色：

1. **形参出现在函数定义阶段**：在声明函数时，用变量名作为占位符，用于接收调用者传来的数据。形参本身是一个局部变量，仅在函数体内有效。例如 \`void foo(int x)\` 中的 \`x\` 就是形参。

2. **实参出现在函数调用阶段**：调用函数时实际传递的具体数据，可以是常量、变量或表达式。例如 \`foo(5)\` 中的 \`5\` 或 \`foo(a+b)\` 中的 \`a+b\` 就是实参。

3. 调用发生时，实参的值会被复制（按值传递）或引用（按引用传递）给形参，二者在概念上是不同的存在。

**选项逐项分析：**
- **A (形参是函数调用时传递给函数的具体值，实参是函数定义中声明的变量)**：❌ 错误。该选项完全颠倒了形参和实参的定义。形参在定义处出现，实参在调用处出现，选项将其说反了。
- **B (形参是函数定义中声明的变量，实参是函数调用时传递给函数的具体值)**：✅ 正确。准确区分了形参（定义阶段的占位变量）和实参（调用阶段的具体数据）。
- **C (形参和实参在函数调用时是完全相同的)**：❌ 错误。形参是变量标识符，实参是具体数据，二者本质不同。即使实参是一个变量，传递给形参的也是该变量的值（按值传递）或引用（按引用传递），而非变量本身。
- **D (形参只在函数内部可见，实参在函数外部可见)**：❌ 错误。该选项描述的是形参的作用域特性，但实参的位置取决于其定义位置（可能是全局变量或局部变量），而非由"作为实参传递"这一行为决定。实参本身可以是任意作用域的变量、常量或表达式。

**易错提醒：**
- 容易混淆的关键点在于：形参的"形"是"形式"的意思，代表函数定义时的形式；实参的"实"是"实际"的意思，代表调用时的实际数据。
- 初学者常误以为形参和实参是同一个东西在不同地方的不同名称，实际上它们是两个完全不同的概念。

**知识延伸：**
在 C++ 中，形参与实参的传递方式分为按值传递和按引用传递两种。若形参前加上 \`&\` 符号（如 \`void foo(int& x)\`），则形参成为实参的引用，函数内部对形参的修改会直接影响到外部的实参变量。

**考点：** 形参与实参的基本概念与区别`,
            tags: ["客观题", "单选题", "函数参数", "GESP4级"]
        },
        {
            id: 7,
            type: "single",
            question: `运行如下代码会输出（ ）。\n\`\`\`cpp\nint value = 100;\nvoid print1() {\n int value = 50;\n cout << value << " ";\n cout << ::value << " ";\n}\nvoid print2() {\n cout << value << " ";\n}\nint main() {\n print1();\n print2();\n return 0;\n}\n\`\`\``,
            options: [
                "100 100 100",
                "50 50 50",
                "50 100 100",
                "50 50 100"
            ],
            answer: 2,
            score: 2,
            explanation: `**答案：C (50 100 100)**

**核心解析：**
本题考查变量的作用域规则和全局作用域解析符 \`::\` 的作用。需要分两部分分析：

**第一部分：print1() 函数的执行**

1. \`int value = 50;\` — 在 \`print1()\` 函数内部定义了一个局部变量 \`value\`，其值为 50。这个局部变量会遮蔽（shadow）同名的全局变量 \`value\`。

2. \`cout << value << " ";\` — 输出语句在 \`print1()\` 内部执行，此时局部变量 \`value\` 可见，因此输出 **50**（注意不是全局变量的 100）。

3. \`cout << ::value << " ";\` — 使用作用域解析符 \`::\` 前缀，表示强制访问全局作用域中的 \`value\`。在 \`::value\` 之前没有 \`::\` 跟着变量名的情况下，\`::\` 代表全局命名空间。因此输出 **100**。

**第二部分：print2() 函数的执行**

4. \`cout << value << " ";\` — 进入 \`print2()\` 函数，函数内部没有定义局部变量 \`value\`，因此直接访问全局变量 \`value\`（值为 100），输出 **100**。

**完整输出序列：**
- print1() 输出：\`50 100 \`
- print2() 输出：\`100 \`
- 最终结果：50 100 100

**选项逐项分析：**
- **A (100 100 100)**：❌ 错误。忽略了 \`print1()\` 中局部变量 \`value = 50\` 的遮蔽效应，错误地输出了两次 100。
- **B (50 50 50)**：❌ 错误地认为 \`::value\` 也被局部变量遮蔽，或者 \`print2()\` 中有局部变量。
- **C (50 100 100)**：✅ 正确。识别了局部变量遮蔽、全局作用域解析符的作用，以及 \`print2()\` 访问全局变量的行为。
- **D (50 50 100)**：❌ 错误地认为 \`print1()\` 中两个 \`cout\` 都输出局部变量 50，或者 \`print2()\` 输出 50。

**易错提醒：**
- \`::value\` 中的 \`::\` 是全局作用域解析符，它的作用是"强制回到全局作用域"寻找变量，完全忽略当前作用域中的同名变量。这是本题的关键考点。
- 如果在 \`print1()\` 中直接写 \`value\`（不带 \`::\`），则会优先使用局部变量，而非全局变量。

**知识延伸：**
变量的遮蔽规则遵循"最近作用域优先"原则。当嵌套作用域中出现同名变量时，内层变量会遮蔽外层变量。全局变量可以被局部变量遮蔽，但通过 \`::\` 可以显式指定访问全局版本。在实际编程中，应尽量避免在不同作用域中使用相同的变量名，以减少混淆。

**考点：** 变量的作用域、遮蔽效应、全局作用域解析符 \`::\``,
            tags: ["客观题", "单选题", "作用域", "GESP4级"]
        },
        {
            id: 8,
            type: "single",
            question: `小杨在整理一副扑克牌的所有红心扑克牌，使其从小到大排列。他的做法是：最开始抓到第 1 张扑克牌被认为已经排好序；然后抓第 2 张扑克牌，将其插入至有序部分的正确位置；不断循环步骤，每次将新抓到扑克牌插入至有序部分，直至抓完所有扑克牌，这样抓牌结束时就完成了扑克牌的排序。小杨这种整理扑克牌的方式与（ ）排序的方式最接近。`,
            options: [
                "冒泡排序",
                "插入排序",
                "选择排序",
                "直接排序"
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (插入排序)**

**核心解析：**
本题要求从整理扑克牌的操作过程识别对应的排序算法。逐步骤分析小杨的做法：

1. **初始状态**：第 1 张牌被认为已经排好序。此时有序部分有 1 张牌，无序部分有剩余所有牌。

2. **抓取第 2 张牌**：将第 2 张牌与有序部分的第 1 张牌比较，确定其应放置的位置（可能在前或后），插入到有序部分的正确位置。此时有序部分有 2 张牌。

3. **循环处理**：每次抓取一张新牌，将其与有序部分从后向前逐个比较，找到合适位置后插入。随着处理的牌增多，有序部分逐渐扩大。

4. **完成**：当所有牌都被插入到有序部分时，整个序列即为有序。

这种"将未排序部分的元素，逐个取出，在已排序部分中找到合适位置插入"的策略，正是**插入排序（Insertion Sort）**的核心思想，与人们整理手牌时的直觉操作完全一致。

**选项逐项分析：**
- **A (冒泡排序)**：❌ 错误。冒泡排序的核心是反复比较相邻元素，若顺序错误则交换，通过多轮交换使最大（或最小）元素逐渐"冒泡"到序列一端。小杨的做法并非交换相邻元素，而是将牌插入到有序部分的某个位置。
- **B (插入排序)**：✅ 正确。插入排序的思想是：维护一个有序序列，将新元素从后向前与有序序列中的元素逐个比较，找到第一个小于等于自己的元素后，插入到其后面。小杨整理扑克牌的过程与此完全一致。
- **C (选择排序)**：❌ 错误。选择排序的核心是：每轮从未排序部分中选择最小（或最大）的元素，将其与未排序部分的第一个元素交换（或直接放到已排序部分末尾）。小杨的做法是"插入"而非"选择并交换"，且不需要从未排序部分中寻找最值。
- **D (直接排序)**：❌ 错误。"直接排序"不是标准排序算法名称，可能是对某种排序算法的误称，或者根本不存在此排序算法。

**易错提醒：**
- 区分"插入"和"交换"是关键：插入排序是找到位置后将元素插入，后面的元素整体后移；冒泡排序是通过不断交换相邻元素来移动元素。
- 选择排序每轮选出一个最值放到确定位置；插入排序每轮将一个元素插入到已排序序列的适当位置。

**知识延伸：**
插入排序的时间复杂度在最好情况下（数据已接近有序）为 O(n)，在最坏和平均情况下为 O(n²)。其优点是对于少量数据或几乎有序的数据效率较高，且是稳定的排序算法（相等元素的相对顺序保持不变）。插入排序也常作为高级排序算法（如快速排序）中递归深度较大时的优化手段。

**考点：** 插入排序的基本原理与算法识别`,
            tags: ["客观题", "单选题", "排序算法", "GESP4级"]
        },
        {
            id: 9,
            type: "single",
            question: `以下哪种情况是使用插入排序的合适场景？`,
            options: [
                "数据量非常大，且乱序严重",
                "希望获得稳定排序，但不要求实时性",
                "数据几乎有序，只需少量调整",
                "想在交换次数最少的前提下排好大数组"
            ],
            answer: 2,
            score: 2,
            explanation: `**答案：C (数据几乎有序，只需少量调整)**

**核心解析：**
插入排序的性能特性决定了它最适合的场景。分析插入排序的时间复杂度：

- **最好情况（数据已有序）**：每轮只需比较一次就确定位置，时间复杂度 O(n)。
- **最坏情况（数据完全逆序）**：每轮需要比较并移动大量元素，时间复杂度 O(n²)。
- **平均情况**：时间复杂度 O(n²)。

插入排序的核心优势在于**对接近有序的数据效率极高**，因为已排序部分为新元素提供了现成的正确位置，只需少量比较和移动即可。

**选项逐项分析：**
- **A (数据量非常大，且乱序严重)**：❌ 错误。数据量大且乱序意味着时间复杂度会达到 O(n²)，性能会严重下降。此时应选择快速排序、归并排序等 O(n log n) 级别的算法。
- **B (希望获得稳定排序，但不要求实时性)**：❌ 错误。虽然插入排序确实是稳定排序，但"不要求实时性"说明时间效率不是首要考量。插入排序的优势在于对特定场景（几乎有序）的高效，而非通用场景下的稳定排序。选择插入排序主要是为了利用其优势场景，而非仅仅因为它稳定。
- **C (数据几乎有序，只需少量调整)**：✅ 正确。这是插入排序的最佳应用场景。当数据接近有序时，插入排序的比较和移动次数大幅减少，时间复杂度趋近于 O(n)，效率非常高。
- **D (想在交换次数最少的前提下排好大数组)**：❌ 错误。插入排序在插入过程中需要移动元素，涉及元素位置的改变。直接选择排序虽然交换次数较少（每轮最多一次交换），但其选择过程的时间复杂度仍是 O(n²)。对于大数组，若交换次数是核心考量，可能需要其他算法（如堆排序，交换次数约为 O(n log n) 次）。

**易错提醒：**
- 插入排序的"优势场景"是接近有序的数据，而非"需要稳定排序"这一泛化条件。稳定性只是插入排序的附加特性。
- O(n²) 的算法在大数据量下性能会急剧下降，不能仅因为"数据量小"就认为 O(n²) 算法一定最优，还需考虑数据本身的有序程度。

**知识延伸：**
插入排序的稳定性和简单性使其成为嵌入式系统、实时系统等资源受限环境中的常用选择。当递归式快速排序的子数组规模小于某个阈值（如 10）时，切换到插入排序（称为"子数组插入排序"）可以减少递归调用开销，这是快速排序的实际实现优化策略。

**考点：** 插入排序的性能特点与适用场景分析`,
            tags: ["客观题", "单选题", "插入排序", "GESP4级"]
        },
        {
            id: 10,
            type: "single",
            question: `以下关于递推算法基本思想的描述，正确的是（ ）。`,
            options: [
                "递推算法通过将问题分解为相互独立的子问题来解决",
                "递推算法从已知的基础情况出发，通过某种关系逐步推导出更大规模问题的解",
                "递推算法通常用于穷举所有可能的解决方案",
                "递推算法适用于在每一步做出局部最优选择以达到全局最优"
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (递推算法从已知的基础情况出发，通过某种关系逐步推导出更大规模问题的解)**

**核心解析：**
递推算法的核心在于利用问题本身存在的内在规律，通过已知的初始状态（或边界条件）和递推关系，逐步计算出目标结果。

递推的基本结构：
1. **初始条件**：确定最简单、最基础的情况下的解。
2. **递推关系**：建立从较小规模问题到较大规模问题的转换公式。
3. **终止条件**：当达到目标规模时停止计算。

典型的递推例子包括：斐波那契数列（F(n) = F(n-1) + F(n-2)，初始 F(1)=1，F(2)=1）、阶乘（n! = n × (n-1)!，初始 0!=1）、爬楼梯问题等。

**选项逐项分析：**
- **A (递推算法通过将问题分解为相互独立的子问题来解决)**：❌ 错误。"将问题分解为相互独立的子问题"是分治法（Divide and Conquer）的特征，如归并排序、快速排序等。递推的子问题之间往往存在依赖关系（如斐波那契数列中 F(n) 依赖于 F(n-1) 和 F(n-2)），并非相互独立。
- **B (递推算法从已知的基础情况出发，通过某种关系逐步推导出更大规模问题的解)**：✅ 正确。这准确描述了递推的本质：从基础情况出发，通过确定的递推公式，逐步求解更大规模的问题。
- **C (递推算法通常用于穷举所有可能的解决方案)**：❌ 错误。"穷举所有可能的解决方案"是暴力搜索或枚举法的特征，适用于解决方案数量有限且可逐一列举的问题。递推是计算特定解，而非列举所有解。
- **D (递推算法适用于在每一步做出局部最优选择以达到全局最优)**：❌ 错误。"每一步做出局部最优选择以达到全局最优"是贪心算法（Greedy Algorithm）的核心特征，如部分背包问题、最小生成树的 Prim 和 Kruskal 算法。递推不涉及"选择最优"的概念，而是根据确定的关系计算结果。

**易错提醒：**
- 递推与分治的关键区别：递推的子问题存在依赖关系（必须按顺序计算）；分治的子问题相互独立（可以并行计算）。
- 递推与贪心的关键区别：递推按照固定公式计算，解是确定的；贪心在每步做出选择，不一定有明确的事先公式。

**知识延伸：**
递推与递归（Recursion）是两个相关但不同的概念。递推是自底向上（从基础情况逐步计算到目标），通常用循环实现；递归是自顶向下（将问题分解为子问题，直到达基本情况），通常用函数自调用实现。有些问题（如斐波那契数列）既可以用递推也可以用递归实现，但递推通常效率更高（避免重复计算子问题）。

**考点：** 递推算法的基本原理与核心特征`,
            tags: ["客观题", "单选题", "递推算法", "GESP4级"]
        },
        {
            id: 11,
            type: "single",
            question: `给定如下算法，当输入 n=10 时，其时间复杂度为（ ）。\n\`\`\`cpp\nbool f(int arr[], int n, int target) {\n for (int i = 0; i < (1 << n); i++) {\n int sum = 0;\n for (int j = 0; j < n; j++) {\n if (i & (1 << j)) {\n sum += arr[j];\n }\n }\n if (sum == target) return true;\n }\n return false;\n}\n\`\`\``,
            options: [
                "$O(N)$",
                "O(n * $2^n$)",
                "O($2^n$)",
                "$O(N^2)$"
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (O(n * $2^n$))**

**核心解析：**
分析代码执行过程，需要分别考察两层循环的执行次数：

1. **外层循环**：条件为 \`i < (1 << n)\`，即 \`i < 2^n\`。由于 \`1 << n\` 等价于 $2^n$，外层循环执行 $2^n$ 次。外层循环的实质是枚举从 $0$ 到 $2^n-1$ 的所有整数，每个整数对应一个 $n$ 位二进制掩码，用来表示某个子集。

2. **内层循环**：条件为 \`j < n\`，即遍历从 $0$ 到 $n-1$ 的所有位。内层循环每次检查 \`i\` 的第 \`j\` 位是否为 $1$，若为 $1$ 则将 \`arr[j]\` 加入 \`sum\`。无论 \`i\` 的值如何，内层循环始终执行 $n$ 次。

3. **总时间复杂度**：外层循环 $2^n$ 次 × 内层循环 $n$ 次 = $O(n \\times 2^n)$。

**选项逐项分析：**
- **A $O(N)$**：❌ 错误。该线性复杂度无法容纳 $2^n$ 的子集枚举规模。
- **B O(n * $2^n$)**：✅ 正确。双层嵌套循环，外层 $2^n$ 次，内层 $n$ 次。
- **C O($2^n$)**：❌ 错误。忽略了内层循环固定的 $n$ 次开销。
- **D $O(N^2)$**：❌ 错误。$N^2$ 为平方级，而本算法具有指数级上界 $2^n$，两者规模差距极大。

**易错提醒：**
- 误以为 \`if (i & (1 << j))\` 只在部分情况下执行内层循环，实际上 \`j\` 的取值范围始终是 $0$ 到 $n-1$，内层循环体执行次数恒为 $n$。
- 混淆 \`(1 << n)\` 与 \`2^n\` 的关系：在 C++ 中左移操作符 \`<<\` 正是用来计算 $2^n$ 的。

**知识延伸：**
子集枚举是竞赛中的经典技巧，通过位掩码将集合的每个子集映射到一个 $[0, 2^n)$ 的整数。当 $n$ 较大时（如 $n > 25$），$2^n$ 可能超出整型表示范围，需谨慎处理。

**考点：** 循环嵌套下的时间复杂度分析（含位运算子集枚举）`,
            tags: ["客观题", "单选题", "时间复杂度", "GESP4级"]
        },
        {
            id: 12,
            type: "single",
            question: `下述斐波那契数列计算的时间复杂度是（ ）。\n\`\`\`cpp\nint fibonacci(int n) {\n if (n == 0) return 0;\n if (n == 1) return 1;\n return fibonacci(n-1)+fibonacci(n-2);\n}\n\`\`\``,
            options: [
                "$O(N)$",
                "$O(N^2)$",
                "$O(log n)$",
                "O($2^n$)"
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (O($2^n$))**

**核心解析：**
分析递归调用 fibonacci(n) 的执行过程：

设 $T(n)$ 为计算 fibonacci(n) 所需的基本操作数（比较、返回、加法等）。根据代码结构：

- 当 $n = 0$ 或 $n = 1$ 时，直接返回，$T(n) = O(1)$。
- 当 $n \\geq 2$ 时，执行一次加法和两次递归调用：$T(n) = T(n-1) + T(n-2) + O(1)$。

这是一个齐次递归方程，其解与斐波那契数列本身增长趋势相同。斐波那契数列第 $n$ 项的精确公式为 $F(n) = \\frac{\\phi^n - \\psi^n}{\\sqrt{5}}$，其中 $\\phi = \\frac{1+\\sqrt{5}}{2} \\approx 1.618$（黄金分割比），$\\psi = \\frac{1-\\sqrt{5}}{2} \\approx -0.618$。

由于 $|\\psi| < 1$，当 $n$ 增大时 $\\psi^n$ 可忽略，故 $F(n)$ 增长趋近于 $\\phi^n$，这是指数级增长。渐进上界记为 $O(2^n)$（常数底取 2 是对 $\\phi$ 的上界估计）。

**选项逐项分析：**
- **A $O(N)$**：❌ 错误。线性复杂度意味着每增加 1，执行时间线性增长。而递归版本中每增加 1，调用次数呈指数增长。
- **B $O(N^2)$**：❌ 错误。平方级仍属多项式增长，远慢于指数级。
- **C $O(log n)$**：❌ 错误。对数级增长极快（如二分查找），与指数级的递归斐波那契性质相反。
- **D O($2^n$)**：✅ 正确。递归树节点数呈指数级增长，常数底取 2 是合理的渐进表示。

**易错提醒：**
- 不能将递归方程的解误认为就是斐波那契数列项的通项公式，而应关注其增长趋势。
- 斐波那契数列增长速度约为 $1.618^n$，小于 $2^n$，但教材和竞赛中常简化为 $O(2^n)$ 以简化讨论。

**知识延伸：**
若对递归斐波那契应用记忆化搜索（Memoization）或自底向上的动态规划，时间复杂度可降为 $O(n)$，空间复杂度为 $O(n)$。这是算法优化中"以空间换时间"的典型案例。

**考点：** 递归算法的时间复杂度分析（指数级增长）`,
            tags: ["客观题", "单选题", "递归", "时间复杂度", "GESP4级"]
        },
        {
            id: 13,
            type: "single",
            question: `关于下面 C++ 程序的描述，（ ）最准确。\n\`\`\`cpp\nifstream in("data.txt");\nstring line;\nwhile (getline(in, line)) {\n cout << line << endl;\n}\n\`\`\``,
            options: [
                "将从标准输入读取每行，并输出到屏幕",
                "程序无法运行，因为 getline 只能读取 cin",
                "将 data.txt 中的每一行读取并输出到屏幕",
                "程序将创建 data.txt 并写入默认文本"
            ],
            answer: 2,
            score: 2,
            explanation: `**答案：C (将 data.txt 中的每一行读取并输出到屏幕)**

**核心解析：**
逐行分析代码的输入输出行为：

1. \`ifstream in("data.txt")\`：声明一个输入文件流对象 \`in\`，并尝试打开当前目录下的文件 \`data.txt\`。若文件不存在，打开操作失败，后续读取将返回 false。

2. \`while (getline(in, line))\`：\`getline\` 是 <string> 库函数，从第一个参数（任何 istream 对象）中读取字符直到遇到换行符'\\n'，将不含换行符的内容存入第二个参数 \`line\`。成功读取返回 true，遇到文件末尾（EOF）返回 false。

3. \`cout << line << endl\`：将读取的行内容输出到标准输出（屏幕），并附加换行符。

**选项逐项分析：**
- **A 将从标准输入读取每行，并输出到屏幕**：❌ 错误。标准输入对应的是 \`cin\`，而代码中使用的是 \`ifstream in("data.txt")\`，这是一个文件输入流而非标准输入流。
- **B 程序无法运行，因为 getline 只能读取 cin**：❌ 错误。\`getline\` 函数接受 \`istream&\` 类型的引用，可以绑定到任何输入流对象，包括文件流 \`ifstream\`、字符串流 \`istringstream\` 等，不限于 \`cin\`。
- **C 将 data.txt 中的每一行读取并输出到屏幕**：✅ 正确。文件输入流负责读取，\`cout\` 负责输出到屏幕，逻辑完整。
- **D 程序将创建 data.txt 并写入默认文本**：❌ 错误。\`ifstream\` 是"输入文件流"（input file stream）的缩写，专门用于读取文件，不能用于写入。若需要创建并写入文件，应使用 \`ofstream\`（输出文件流）。

**易错提醒：**
- 混淆 \`ifstream\` 与 \`ofstream\` 的用途：文件名中的 'i' 表示 input（输入），'o' 表示 output（输出）。
- 误认为 \`getline\` 只能与 \`cin\` 搭配，实际上它是一个通用的行读取函数。

**知识延伸：**
文件输入输出是程序与外部数据交互的重要方式。除 \`getline\` 外，还可用 \`>>\` 运算符按空格/换行分隔读取单词或数字，或用 \`in.eof()\` 检测文件末尾状态。

**考点：** C++ 文件输入输出（fstream）与 getline 函数的使用`,
            tags: ["客观题", "单选题", "文件操作", "GESP4级"]
        },
        {
            id: 14,
            type: "single",
            question: `在 C++ 中，异常处理机制（try-catch块）的主要目的是（ ）。`,
            options: [
                "提高程序的运行速度",
                "在程序发生运行时错误时，提供一种结构化的错误处理方式",
                "确保程序在编译时没有错误",
                "减少程序的内存占用"
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (在程序发生运行时错误时，提供一种结构化的错误处理方式)**

**核心解析：**
分析 try-catch 异常处理机制的设计目的：

1. **运行时错误的特点**：编译阶段只能检测语法错误和部分语义错误（如类型不匹配），但无法预知程序运行时会遇到的情况，例如：文件不存在、除数为零、数组越界访问、内存分配失败、网络连接中断等。

2. **结构化错误处理**：\`try-catch\` 允许程序员将"可能抛出异常的代码"与"异常发生时的处理代码"分离开来。\`try\` 块内执行正常业务逻辑；\`catch\` 块捕获并处理特定类型的异常。这种结构使错误处理逻辑清晰、可追溯。

3. **异常传播机制**：未被当前 \`catch\` 捕获的异常会沿着调用栈向上传播，直到被某个 \`catch\` 捕获或导致程序终止。

**选项逐项分析：**
- **A 提高程序的运行速度**：❌ 错误。异常处理机制需要额外的运行时支持（栈展开、类型检查等），反而会带来轻微的性能开销。
- **B 在程序发生运行时错误时，提供一种结构化的错误处理方式**：✅ 正确。这是异常处理机制的核心价值。
- **C 确保程序在编译时没有错误**：❌ 错误。异常是**运行时**概念，编译时完全正常的程序仍可能在运行时抛出异常。
- **D 减少程序的内存占用**：❌ 错误。异常处理需要维护额外的异常信息存储，通常会增加内存开销。

**易错提醒：**
- 错误地将异常处理与编译检查混淆：异常机制完全不参与编译检查。
- 误以为异常一定能"修复"错误，实际上 catch 块中的代码只是一种应对策略，可能只是记录日志、释放资源或提示用户。

**知识延伸：**
C++ 中使用 \`throw\` 关键字抛出异常，异常对象可以是内置类型（如 int、const char*）或自定义类型（如 \`std::runtime_error\`）。推荐抛出对象而非基本类型，以便 catch 时能区分不同的错误类型。

**考点：** C++ 异常处理机制（try-catch-throw）的目的与意义`,
            tags: ["客观题", "单选题", "异常处理", "GESP4级"]
        },
        {
            id: 15,
            type: "single",
            question: `为了提高冒泡排序的效率，如果某轮“冒泡”中没有执行任何交换操作，说明数组已经完成排序，可直接返回结果，则两条横线上分别应该填写（ ）。\n\`\`\`cpp\nvoid bubbleSortWithFlag(vector<int> &nums) {\n for (int i = nums.size()-1; i > 0; i--) {\n bool flag = false; // 横线 1\n for (int j = 0; j < i; j++) {\n if (nums[j] > nums[j+1]) {\n swap(nums[j], nums[j+1]);\n flag = true; // 横线 2\n }\n }\n if (!flag) break;\n }\n}\n\`\`\``,
            options: [
                "flag = true; flag = false;",
                "flag = false; flag = true;",
                "flag = true; flag = true;",
                "flag = false; flag = false;"
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (flag = false; flag = true;)**

**核心解析：**
分析冒泡排序优化中两个空格的逻辑含义：

1. **横线 1（第 6 行）**：位于外层循环的起始处。每次开始新一轮"冒泡"前，应假设本轮不会发生任何交换，即数组可能已经有序。因此应将 \`flag\` 初始化为 \`false\`。

2. **横线 2（第 11 行）**：位于内层循环的 if 语句内，执行 swap 之后。当且仅当发现了逆序对并进行了交换操作时，才说明数组中仍有乱序元素，此时应将 \`flag\` 设为 \`true\`，表示"本轮不是完全有序的"。

3. **提前终止条件（第 13 行）**：若内层循环结束后 \`flag\` 仍为 \`false\`，说明整趟冒泡没有任何交换，数组已经升序完成，可安全执行 \`break\` 跳出外层循环，从而避免无意义的比较。

**选项逐项分析：**
- **A flag = true; flag = false;**：❌ 错误。若横线 1 设为 \`true\`，则每轮开始时 \`flag\` 就已经是 \`true\`，即使没有发生交换也不会触发 \`break\`，优化失效。
- **B flag = false; flag = true;**：✅ 正确。逻辑如上述解析，实现"无交换则提前结束"的优化。
- **C flag = true; flag = true;**：❌ 错误。横线 1 为 \`true\` 会导致第一轮结束后 \`flag\` 状态不确定，且无法正确判断是否需要提前终止。
- **D flag = false; flag = false;**：❌ 错误。横线 2 设为 \`false\` 意味着即使发生了交换，\`flag\` 仍为 \`false\`，\`break\` 永远不会被执行，失去了优化意义。

**易错提醒：**
- 混淆两个空格的执行时机：横线 1 在每轮冒泡开始时重置，横线 2 在发现逆序并交换后标记。
- 错误地认为"什么都不做"（\`flag = false\`）是正确的，实际上只有发生交换时才应将 \`flag\` 设为 \`true\`。

**知识延伸：**
该优化使最好情况（数组已有序）的时间复杂度从 $O(n^2)$ 降为 $O(n)$。空间复杂度始终为 $O(1)$，因为只使用了一个布尔变量 \`flag\`。

**考点：** 冒泡排序的优化策略（Flag 哨兵机制）`,
            tags: ["客观题", "单选题", "冒泡排序", "GESP4级"]
        },
        {
            id: 16,
            type: "judge",
            question: `C++ 语言中，函数声明 \`int add(int, int);\` 是合法的。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
在 C++ 函数原型（Function Prototype）声明中，参数名是可选的组成部分。声明 \`int add(int, int);\` 明确指定了函数名 \`add\`、返回类型 \`int\` 以及两个 \`int\` 类型的参数。编译器只需要知道参数的类型和数量，就能进行参数类型检查和函数链接，参数名仅作为可选的文档用途。因此，该声明完全符合 C++ 语法规则，是合法的函数声明。

**易错提醒：**
- 混淆声明与定义：函数声明只需提供签名，函数定义才需要包含函数体。\`int add(int, int);\` 只是声明，不是定义。
- 误以为必须有参数名：参数名在声明中是可选的，但在函数定义中需要参数名以便在函数体内引用。

**知识延伸：**
函数声明时省略参数名的写法常用于只需要知道函数接口的场景，如头文件中声明库函数。在大型项目中，头文件中通常只放函数声明，具体实现在 .cpp 文件中。

**考点：** C++ 函数声明的语法规则、函数原型概念。`,
            tags: ["客观题", "判断题", "函数声明", "GESP4级"]
        },
        {
            id: 17,
            type: "judge",
            question: `在 C++ 中，通过值传递方式将变量传入函数后，在函数内部修改该参数的值，不会影响函数外部原始变量的值。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
这是 C++ "按值传递"（Pass-by-value）的核心特性。当调用 \`func(x)\` 时，系统将实参 \`x\` 的值复制一份，创建形参的存储空间。函数内部对形参的所有操作（赋值、修改等）都作用在这个独立的副本上，而实参 \`x\` 本身所在的内存单元并未被访问或改变。函数执行完毕后，形参副本被销毁，外部实参的值保持不变。

**易错提醒：**
- 区分值传递与引用传递：如果改为引用传递 \`func(int& x)\` 或指针传递 \`func(int* px)\`，则函数内部修改形参会影响外部变量。
- 区分参数传递与全局变量：若函数修改的是全局变量，自然会影响外部；若修改的是通过引用或指针传入的对象成员，也会影响外部。

**知识延伸：**
值传递虽然不会意外修改实参，但对于大型结构体（如包含大量成员的结构）会产生复制开销，此时引用传递或常量引用传递是更高效的选择。

**考点：** 函数参数传递机制、值传递的特性与限制。`,
            tags: ["客观题", "判断题", "参数传递", "GESP4级"]
        },
        {
            id: 18,
            type: "judge",
            question: `C++ 并不支持嵌套结构体，即在一个结构体内部定义另一个结构体是错误的。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
C++ 完全支持结构体嵌套（Nested Struct）。在一个结构体内部，既可以定义另一个结构体类型，也可以直接定义该类型的成员变量。例如，定义一个包含日期信息的学生结构体：
\`\`\`cpp
struct Date {
    int year, month, day;
};
struct Student {
    string name;
    Date birthday;  // 嵌套另一个结构体作为成员
};
\`\`\`
这种嵌套结构在处理复合数据类型时非常常用，能使代码结构更清晰、逻辑更分明。

**易错提醒：**
- 结构体嵌套不会导致内存碎片或访问效率低下，内部结构体的布局是连续的。
- 如果在结构体内部定义的嵌套结构体只在该结构体内部使用，可以将其声明为内嵌类型（如 \`struct Student { struct Date { ... }; Date birthday; };\`）。

**知识延伸：**
嵌套结构体与命名空间是不同概念。嵌套结构体是一种复合数据类型设计方式，而命名空间主要用于避免全局名字冲突。C++ 支持结构体、类、联合体（union）的相互嵌套。

**考点：** 结构体的复合与嵌套定义、数据类型的设计。`,
            tags: ["客观题", "判断题", "结构体", "GESP4级"]
        },
        {
            id: 19,
            type: "judge",
            question: `引用传递（Pass-by-reference）在函数调用时不会产生参数的副本，因此在处理大型对象时通常比值传递更高效。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
引用传递（Pass-by-reference）的底层实现通常是传递原变量的地址（4字节或8字节），而非复制整个对象的数据。以 \`void func(MyStruct& s)\` 为例，调用时只需将结构体的地址压栈，函数内部通过引用名直接操作原变量；而 \`void func(MyStruct s)\` 需要将结构体全部成员逐一复制到栈帧中。对于包含大量成员的结构体或类，这会带来显著的时间和空间开销。

**易错提醒：**
- 引用传递若不加 \`const\`，函数内部可能意外修改实参，这是副作用而非"更高效"的代价。
- 对于内置类型（如 int、double）直接传递值可能更高效，因为引用本身也需要传递地址。
- 编译器可能进行返回值优化（RVO）等优化，但引用传递在大型对象传递场景下的优势是本质性的。

**知识延伸：**
C++11 引入了"移动语义"（Move Semantics），通过右值引用可以在特定场景下避免大型对象的深拷贝，进一步提升性能。结合 \`const\` 引用 \`const T&\` 可以在不修改对象的前提下获得引用传递的效率优势。

**考点：** 引用传递的性能优势、按值传递与按引用传递的对比。`,
            tags: ["客观题", "判断题", "引用传递", "GESP4级"]
        },
        {
            id: 20,
            type: "judge",
            question: `在 C++ 中，定义二维数组时，必须明确地为数组的每一个元素都提供初始化值。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
在 C++ 中，定义二维数组时无需为每个元素都显式提供初始化值。未被显式初始化的元素会按以下规则自动填充：
1. **全局或静态数组**：未初始化元素默认初始化为 0。
2. **局部数组且使用大括号初始化**：未指定的元素也会被自动初始化为 0。
3. **局部数组且未使用大括号**（如 \`int a[2][2];\`）：元素值是未定义的垃圾值。

例如：
\`\`\`cpp
int a[2][2] = {1};        // 等价于 {{1, 0}, {0, 0}}，其余元素自动补0
int b[2][2] = {{1,2}, {3}}; // 等价于 {{1,2}, {3,0}}，最后一个元素自动补0
\`\`\`

**易错提醒：**
- \`int a[2][2] = {1};\` 不等于所有元素都是 1，只有第一个元素是 1，其余都会被初始化为 0。
- 如果数组是局部变量且完全不给初始值（如 \`int a[2][2];\`），元素是未定义的，不能假设为 0。

**知识延伸：**
数组初始化的省略规则是一维推广到多维的：初始化列表按顺序填充，多余位置补零。这与 C++ 的聚合初始化（Aggregate Initialization）规则一致，也是 GESP 二级、三级考试中的常见考点。

**考点：** 数组的部分初始化规则、聚合初始化的默认值填充。`,
            tags: ["客观题", "判断题", "二维数组", "GESP4级"]
        },
        {
            id: 21,
            type: "judge",
            question: `执行以下代码后，变量 \`res\` 的值将为 \`n\` 的阶乘。\n\`\`\`cpp\nint res = 1;\nfor (int i = 0; i < n; i++) {\n res *= i;\n}\n\`\`\``,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
逐行推导代码执行过程：
1. \`int res = 1;\` 初始化 \`res = 1\`。
2. 循环 \`for (int i = 0; i < n; i++)\` 遍历 \`i = 0, 1, 2, ..., n-1\`。
3. 当 \`i = 0\` 时，执行 \`res *= i\`，即 \`res = res * 0 = 1 * 0 = 0\`。
4. 随后的所有迭代中，由于 \`res\` 已被赋值为 0，无论 \`i\` 为何值，\`res *= i\` 的结果始终为 0。
5. 循环结束后，\`res = 0\`，而非 \`n\` 的阶乘。

正确实现阶乘应从 \`i = 1\` 开始（或 \`i = 1; i <= n; i++\`），这样才能逐步累积乘积。

**易错提醒：**
- 阶乘定义为 $1 \\times 2 \\times 3 \\times ... \\times n$，初值应为 1，但循环必须从 1 开始而非 0。
- 乘以 0 会将任何累积结果清零，这是本题的关键陷阱。

**知识延伸：**
阶乘问题中循环起始值是高频考点。类似的错误还包括循环边界使用 \`i < n\` 而非 \`i <= n\`，导致少乘一个数。

**考点：** 循环逻辑与阶乘定义。`,
            tags: ["客观题", "判断题", "循环控制", "GESP4级"]
        },
        {
            id: 22,
            type: "judge",
            question: `选择排序算法在任何情况下（无论输入数组是否已经有序）的比较次数都是相同的。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
选择排序的核心机制是：在每一轮中，扫描剩余未排序部分的所有元素以寻找极值（最小或最大）。
1. 第一轮扫描 $n-1$ 次比较。
2. 第二轮扫描 $n-2$ 次比较。
3. 第 $k$ 轮扫描 $n-k$ 次比较。
4. 总比较次数固定为 $\\frac{n(n-1)}{2}$，不随数组的初始状态（如是否已经有序）而改变。

无论数组是否已排序，选择排序都必须完整执行每一轮扫描，因此比较次数恒定不变。

**易错提醒：**
- 容易混淆"比较次数"和"交换次数"：选择排序的交换次数最多为 $n-1$ 次（每轮最多一次交换），与数组是否有序无关，但这是交换次数而非比较次数。
- 冒泡排序在有序情况下可以通过优化提前终止，但选择排序没有这种优化。

**知识延伸：**
与选择排序不同，冒泡排序在最佳情况（已排序）下可以通过设置标志位实现 $O(n)$ 的比较次数。插入排序在已排序时也只需 $O(n)$ 次比较。

**考点：** 选择排序（Selection Sort）的时间复杂度确定性。`,
            tags: ["客观题", "判断题", "选择排序", "GESP4级"]
        },
        {
            id: 23,
            type: "judge",
            question: `以下代码实现了选择排序的逻辑（升序）。\n\`\`\`cpp\nfor (int i = 0; i < n-1; i++) {\n int minIdx = i;\n for (int j = i+1; j < n; j++)\n if (arr[j] < arr[minIdx]) minIdx = j;\n swap(arr[i], arr[minIdx]);\n}\n\`\`\``,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
逐层分析代码逻辑：
1. 外层循环 \`for (int i = 0; i < n-1; i++)\`：遍历数组的前 $n-1$ 个位置作为排序边界。
2. \`int minIdx = i;\`：假设当前位置 \`i\` 为当前未排序部分的最小值。
3. 内层循环 \`for (int j = i+1; j < n; j++)\`：从 \`i+1\` 开始扫描剩余未排序部分。
4. \`if (arr[j] < arr[minIdx]) minIdx = j;\`：发现更小元素时更新 \`minIdx\`。
5. \`swap(arr[i], arr[minIdx]);\`：内层结束后，将找到的最小值交换到位置 \`i\`。

这完全符合选择排序升序排列的标准实现：每一轮找出剩余部分的最小值，放在已排序部分末尾。

**易错提醒：**
- 外层循环到 \`n-1\` 而非 \`n\`，因为最后一轮只有一个元素待排序，无需再比较。
- 若内层循环写成 \`j = i\` 而非 \`j = i+1\`，会导致自己与自己比较的冗余。

**知识延伸：**
选择排序是不稳定排序，因为相等元素的相对位置可能在交换时被改变。例如序列 [2, 2, 1] 排序后可能变为 [1, 2, 2]，两个 2 的相对顺序改变了。

**考点：** 选择排序算法的 C++ 经典实现。`,
            tags: ["客观题", "判断题", "选择排序", "GESP4级"]
        },
        {
            id: 24,
            type: "judge",
            question: `在 C++ 中，如果程序抛出了一个异常，但没有任何 \`catch\` 块与之匹配，程序将产生编译错误。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
编译与运行是两个完全不同的阶段：
1. **编译阶段**：编译器检查语法、类型匹配等静态问题，生成可执行文件。此阶段编译器无法预测程序运行时可能抛出的具体异常类型。
2. **运行阶段**：异常在程序执行时才被抛出（throw），此时由运行时系统查找匹配的 catch 块。
3. 若无匹配的 catch 块，程序会调用 \`std::terminate()\`，导致程序终止（通常显示错误信息或崩溃）。

因此，没有匹配的 catch 块不会产生**编译错误**，而是产生**运行时错误**。

**易错提醒：**
- 异常处理是运行时机制，编译时不会检查 catch 是否能匹配所有可能的 throw。
- 编写代码时应确保异常被正确捕获，避免程序因 \`std::terminate\` 而非正常退出。

**知识延伸：**
C++ 中可以使用 \`catch(...)\` 捕获所有类型的异常，作为最后一道防线。如果既没有匹配的 catch 也没有 \`catch(...)\`，程序将终止。

**考点：** C++ 异常处理的运行时特性与编译时检查的区别。`,
            tags: ["客观题", "判断题", "异常处理", "GESP4级"]
        },
        {
            id: 25,
            type: "judge",
            question: `使用 \`ofstream outFile("output.txt"); outFile << "Hello";\` 这种方式可以将字符串 "Hello" 写入到 output.txt 文件中。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
分析代码执行流程：
1. \`ofstream outFile("output.txt");\`：创建输出文件流对象 \`outFile\`，并关联到名为 \`output.txt\` 的文件。若文件不存在则创建，若存在则覆盖。
2. \`outFile << "Hello";\`：使用流插入运算符 \`<<\`，将字符串 \`"Hello"\` 写入到关联的文件中。

此写法与使用 \`cout\` 向控制台输出完全类似，只是输出目标从标准输出变为文件。这是 C++ 文件输出的标准且简洁的写法。

**易错提醒：**
- 使用完文件流后应调用 \`outFile.close();\` 关闭文件，确保数据写入磁盘。虽然文件流在对象销毁时会自动关闭，但显式关闭是良好习惯。
- 若文件打开失败（如只读目录），\`ofstream\` 对象会处于错误状态，写入操作不会生效。

**知识延伸：**
\`ofstream\` 是 C++ 标准库 \`<fstream>\` 提供的文件输出流类。相应的，\`ifstream\` 用于文件输入，\`fstream\` 可同时用于输入和输出。

**考点：** C++ 文件流（ofstream）的基础应用。`,
            tags: ["客观题", "判断题", "文件操作", "GESP4级"]
        },
        {
      id: 26,
      type: 'programming',
      samples: [
        { input: `3 5
2 2 2 4
.....
.>_<.
.....`, output: `>_<` },
        { input: `5 5
1 2 3 4
AbCdE
fGhIk
LmNoP
qRsTu
VwXyZ`, output: `Cd
hI` }
      ],
      question: `
# [GESP202506 四级] 画布裁剪

## 题目描述

小 A 在高为 $h$ 宽为 $w$ 的矩形画布上绘制了一幅画。由于画布边缘留白太多，小 A 想适当地裁剪画布，只保留画的主体。具体来说，画布可以视为 $h$ 行 $w$ 列的字符矩阵，其中的字符均为 ASCII 码位于 $33 \\sim 126$ 之间的可见字符，小 A 只保留画布中由第 $x_1$ 行到第 $x_2$ 行、第 $y_1$ 列到第 $y_2$ 列构成的子矩阵。

小 A 将画布交给了你，你能帮他完成画布的裁剪吗？

## 输入格式

第一行，两个正整数 $h, w$，分别表示画布的行数与列数。

第二行，四个正整数 $x_1, x_2, y_1, y_2$，表示保留的行列边界。

接下来 $h$ 行，每行一个长度为 $w$ 的字符串，表示画布内容。

## 输出格式

输出共 $x_2 - x_1 + 1$ 行，每行一个长度为 $y_2 - y_1 + 1$ 的字符串，表示裁剪后的画布。
`,
      score: 25,
      explanation: `**解析：**
本题考察对二维数组（矩阵）的基本操作。
1.  **输入理解**：获取画布的行数 $h$ 和列数 $w$，以及保留区域的边界 $x_1, x_2, y_1, y_2$。
2.  **坐标转换**：题目给出的行和列编号通常是从 1 开始的。在 C++ 数组中，对应的索引应该是 \`x-1\`。
3.  **提取子矩阵**：
    - 遍历行索引 $i$ 从 $x_1-1$ 到 $x_2-1$。
    - 遍历列索引 $j$ 从 $y_1-1$ 到 $y_2-1$。
    - 按行输出这些字符。

**核心逻辑提示：**
\`\`\`cpp
for (int i = x1 - 1; i < x2; i++) {
    for (int j = y1 - 1; j < y2; j++) {
        cout << canvas[i][j];
    }
    cout << endl;
}
\`\`\``,
      template: "#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n\n    // 在此编写代码\n\n    return 0;\n}",
      referenceCode: `#include <iostream>\n#include <vector>\n#include <string>\nusing namespace std;\n\nint main() {\n    int H, W;\n    cin >> H >> W;\n    int x1, x2, y1, y2;\n    cin >> x1 >> x2 >> y1 >> y2;\n    vector<string> canvas(H);\n    for (int i = 0; i < H; ++i) cin >> canvas[i];\n    for (int i = x1-1; i <= x2-1; ++i) {\n        for (int j = y1-1; j <= y2-1; ++j) {\n            cout << canvas[i][j];\n        }\n        cout << endl;\n    }\n    return 0;\n}`,
      tags: ["编程题", "矩阵操作", "GESP4级"],
      answer: '',
    },
        {
      id: 27,
      type: 'programming',
      samples: [
        { input: `5
1 60
3 70
2 80
4 55
4 50`, output: `8` },
        { input: `5
4 0
4 0
2 0
3 0
1 0`, output: `1` }
      ],
      question: `
# [GESP202506 四级] 排序

## 题目描述

体育课上有 $n$ 名同学排成一队，从前往后数第 $i$ 位同学的身高为 $h_i$，体重为 $w_i$。目前排成的队伍看起来参差不齐，老师希望同学们能按照身高从高到低的顺序排队，如果身高相同则按照体重从重到轻排序。在调整队伍时，每次只能交换相邻两位同学的位置。老师想知道，最少需要多少次交换操作，才能将队伍调整成目标顺序。

## 输入格式

第一行，一个正整数 $n$，表示队伍人数。

接下来 $n$ 行，每行两个正整数 $h_i$ 和 $w_i$，分别表示第 $i$ 位同学的身高和体重。

## 输出格式

输出一行，一个整数，表示最少需要的交换次数。
`,
      score: 25,
      explanation: `**解析：**
本题考察对排序算法交换次数的统计。
1.  **最少交换次数**：在只允许交换相邻元素的情况下，将一个序列调整为目标序列所需的最少交换次数等于该序列相对于目标序列的**逆序对**数量。
2.  **排序规则**：
    - 身高从高到低（降序）。
    - 身高相同时，体重从重到轻（降序）。
3.  **实现方式**：
    - 由于 $n$ 较小，可以直接使用**冒泡排序**进行模拟。
    - 在冒泡排序的过程中，每次发生交换就令计数器加 1。
    - 最终输出计数器的值。

**核心逻辑提示：**
\`\`\`cpp
struct Student { int h, w; };
bool cmp(Student a, Student b) {
    if (a.h != b.h) return a.h > b.h;
    return a.w > b.w;
}
// 冒泡排序统计交换次数
int ans = 0;
for (int i = 0; i < n; i++) {
    for (int j = 0; j < n - 1 - i; j++) {
        if (!cmp(sts[j], sts[j+1])) {
            swap(sts[j], sts[j+1]);
            ans++;
        }
    }
}
\`\`\``,
      template: "#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n\n    // 在此编写代码\n\n    return 0;\n}",
      referenceCode: `#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nstruct Student {\n    int h, w;\n};\n\nbool compare(const Student& a, const Student& b) {\n    if (a.h != b.h) return a.h > b.h;\n    return a.w > b.w;\n}\n\nint main() {\n    int n;\n    cin >> n;\n    vector<Student> sts(n);\n    for (int i = 0; i < n; ++i) cin >> sts[i].h >> sts[i].w;\n    \n    int swaps = 0;\n    for (int i = 0; i < n-1; ++i) {\n        for (int j = 0; j < n-1-i; ++j) {\n            if (compare(sts[j+1], sts[j])) {\n                swap(sts[j], sts[j+1]);\n                swaps++;\n            }\n        }\n    }\n    cout << swaps << endl;\n    return 0;\n}`,
      tags: ["编程题", "排序", "逆序对", "GESP4级"],
      answer: '',
    }
    ]
};

