// 2024年9月 GESP C++ 四级真题

const programmingQuestions = [
    {
      id: 26,
      type: 'programming',
      samples: [
        { input: `3
1 4
0110
5 5
00000
01100
01100
00001
01100
5 5
00000
01100
01110
00001
01100`, output: `No
Yes
No` }
      ],
      question: `
# [GESP202409 四级] 黑白方块

## 题目描述

小杨有一个 $n$ 行 $m$ 列的网格图，其中每个格子要么是白色，要么是黑色。
小杨想知道网格图中是否存在一个满足如下条件的子矩形：
- 子矩形由 $4$ 行 $4$ 列组成；
- 子矩形的第 $1$ 行和第 $4$ 行只包含白色格子；
- 对于子矩形的第 $2$ 行和第 $3$ 行，只有第 $1$ 个和第 $4$ 个格子是白色的，其余格子都是黑色的；

请你编写程序帮助小杨判断。

## 输入格式

第一行包含一个正整数 $t$，代表测试用例组数。 
接下来是 $t$ 组测试用例。对于每组测试用例，一共 $n+1$ 行。 
第一行包含两个正整数 $n,m$，含义如题面所示。 
之后 $n$ 行，每行一个长度为 $m$ 的 $01$ 串，代表网格图第 $i$ 行格子的颜色，如果为 $0$，则对应格子为白色，否则为黑色。

## 输出格式

对于每组测试用例，如果存在，输出 Yes，否则输出 No。
`,
      score: 25,
      explanation: `**解析：**
本题考察对子矩阵模式的匹配与多重循环遍历。
1.  **模式定义**：要求的 4x4 子矩形模式为：
    - 行 1, 4：全是 '0'（白色）。
    - 行 2, 3：首尾是 '0'，中间两个是 '1'（黑色），即 "0110"。
2.  **枚举策略**：遍历所有可能的左上角起点 $(i, j)$，其中 $0 \le i \le n-4$，$0 \le j \le m-4$。
3.  **模式匹配**：对于每一个起点，逐行检查是否符合上述 4x4 的特定布局。

**核心逻辑提示：**
\`\`\`cpp
for (int i = 0; i <= n - 4; i++) {
    for (int j = 0; j <= m - 4; j++) {
        // 检查行 1 和行 4
        if (g[i].substr(j, 4) == "0000" && g[i+3].substr(j, 4) == "0000") {
            // 检查行 2 和行 3
            if (g[i+1].substr(j, 4) == "0110" && g[i+2].substr(j, 4) == "0110") {
                found = true;
            }
        }
    }
}
\`\`\``,
      tags: ["编程题", "二维数组", "模拟"],
      template: "#include <iostream>\n#include <vector>\n#include <string>\nusing namespace std;\n\nint main() {\n    int T;\n    cin >> T;\n    // 在此编写代码\n    return 0;\n}",
      referenceCode: "#include <iostream>\n#include <vector>\n#include <string>\nusing namespace std;\nvoid solve() {\n    int n, m; cin >> n >> m;\n    vector<string> g(n);\n    for (int i = 0; i < n; i++) cin >> g[i];\n    bool ok = false;\n    if (n >= 2 && m >= 2) {\n        for (int i = 0; i < n-1; i++) {\n            for (int j = 0; j < m-1; j++) {\n                if (g[i][j] == '0' && g[i][j+1] == '0' && g[i+1][j] == '0' && g[i+1][j+1] == '0') {\n                    ok = true; break;\n                }\n            }\n            if (ok) break;\n        }\n    }\n    if (ok) cout << \"Yes\" << endl; else cout << \"No\" << endl;\n}\nint main() {\n    int T; cin >> T;\n    while (T--) solve();\n    return 0;\n}",
      answer: '',
    },
    {
      id: 27,
      type: 'programming',
      samples: [
        { input: `5
3 4 5 2 1
3
4 5
3 4
1 3`, output: `1 3 4 5 2` }
      ],
      question: `
# [GESP202409 四级] 区间排序

## 题目描述

小杨有一个包含 $n$ 个正整数的序列 $a$。

小杨计划对序列进行多次升序排序，每次升序排序小杨会选择一个区间 $[l,r]$（$l \\leq r$）并对区间内所有数字，即进行升序 $a_l, a_{l + 1}, \\dots a_r$ 排序。每次升序排序会在上一次升序排序的结果上进行。

小杨想请你计算出多次升序排序后的序列。

## 输入格式

第一行包含一个正整数 $n$，含义如题面所示。 
第二行包含 $n$ 个正整数 $a_1, a_2, \\dots a_n$，代表序列 $a$。 
第三行包含一个正整数 $q$，代表排序次数。 
之后 $q$ 行，每行包含两个正整数 $l, r$，代表将区间 $[l_i, r_i]$ 内所有数字进行升序排序。

## 输出格式

输出一行包含 $n$ 个正整数，代表多次升序排序后的序列。
`,
      score: 25,
      explanation: `**解析：**
本题考察对 \`std::sort\` 区间排序的熟练运用。
1.  **操作模拟**：题目给出了 $q$ 次操作，每次操作指定一个闭区间 $[l, r]$。
2.  **区间对应**：注意 C++ 中 \`std::vector\` 或数组下标是从 0 开始的，而题目给出的区间通常是 1-based。因此，区间 $[l, r]$ 对应到 \`std::sort\` 的迭代器应为 \`a.begin() + l - 1\` 到 \`a.begin() + r\`。
3.  **贪心/模拟**：由于每次排序都是在上一次结果的基础上进行的，直接按顺序执行所有排序请求即可。

**核心逻辑提示：**
\`\`\`cpp
while (q--) {
    int l, r;
    cin >> l >> r;
    // std::sort(first, last) 排序范围是 [first, last)
    sort(a.begin() + l - 1, a.begin() + r);
}
\`\`\``,
      tags: ["编程题", "排序", "模拟"],
      template: "#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    int n, m;\n    cin >> n >> m;\n    // 在此编写代码\n    return 0;\n}",
      referenceCode: "#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\nint main() {\n    int n, m; cin >> n >> m;\n    vector<int> a(n);\n    for (int i = 0; i < n; i++) cin >> a[i];\n    while (m--) {\n        int l, r; cin >> l >> r;\n        sort(a.begin()+l-1, a.begin()+r);\n    }\n    for (int i = 0; i < n; i++) cout << a[i] << (i == n-1 ? \"\" : \" \");\n    cout << endl;\n    return 0;\n}",
      answer: '',
    }
];

export const paperData = {
    id: '2024-09-l4',
    title: '2024年9月 GESP C++ 四级真题',
    level: 4,
    year: 2024,
    month: 9,
    session: 3,
    timeLimit: 5400,
    backfilled: true,
    questions: [
        {
            id: 1,
            type: "single",
            question: `在 C++ 中，（ ）正确定义了一个返回整数值并接受两个整数参数的函数。`,
            options: [
                "int add(int a, int b) { return a+b; }",
                "void add(int a, int b) { return a+b; }",
                "int add(a, b) { return a+b; }",
                "void add(int a, int b) { return a-b; }"
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：A (int add(int a, int b) { return a+b; })**

**选项逐项分析：**
- **A**：✅ 正确。定义完整：指明了返回类型（int）、函数名、带类型的形参列表（int a, int b）以及符合返回类型的 return 语句。
- **B**：❌ 错误。函数定义为 \`void\` 类型，表示不返回任何值，不能使用 \`return a+b;\` 返回结果。
- **C**：❌ 错误。C++ 是强类型语言，形参 \`a\` 和 \`b\` 必须指定数据类型。
- **D**：❌ 错误。虽然语法上可能通过编译，但题目要求是“计算并返回”，\`void\` 类型无法返回计算结果。此外，逻辑上 \`a-b\` 也不符合 \`add\` 的语义。

**考点：** C++ 函数定义的标准语法（返回类型、形参列表、return 语句）。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 2,
            type: "single",
            question: `在 C++ 中，形参与实参的关系描述正确的是（ ）。`,
            options: [
                "形参在函数调用时指定，实参在函数定义时传递",
                "形参在函数定义时指定，实参在函数调用时传递",
                "形参和实参可以互换",
                "形参和实参必须是完全相同的类型，不能有任何差异。"
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (形参在函数定义时指定，实参在函数调用时传递)**

**选项逐项分析：**
- **A**：❌ 错误。刚好将两者的概念记反了。
- **B**：✅ 正确。形参（Formal Parameter）是在定义函数时占位用的变量；实参（Actual Parameter）是在调用函数时实际传给函数的数据。
- **C**：❌ 错误。形参和实参在地位和生命周期上完全不同，不可互换。
- **D**：❌ 错误。实参可以隐式转换为形参类型（如将 \`int\` 传给 \`double\` 形参），不要求完全相同。

**考点：** 函数参数机制（形参 vs 实参的概念辨析）。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 3,
            type: "single",
            question: `运行以下代码，屏幕上将输出（ ）。\n\`\`\`cpp\nint var = 100;\nvoid function() {\n int var = 200;\n cout << var << " " << ::var << endl;\n}\n\`\`\``,
            options: ["100 100", "200 100", "200 200", "100 200"],
            answer: 0,
            score: 2,
            explanation: `**答案：B (200 100)**

**选项逐项分析：**
- **A 100 100**：❌ 错误。忽略了局部变量 \`var\` 会屏蔽（Shadow）同名的全局变量。
- **B 200 100**：✅ 正确。
  1. 在 \`function\` 内部，直接使用 \`var\` 会优先访问作用域最近的局部变量（值为 200）。
  2. 使用作用域解析运算符 \`::var\` 可以强制访问全局作用域下的变量（值为 100）。
- **C 200 200**：❌ 错误。误以为 \`::\` 也会访问到局部变量。
- **D 100 200**：❌ 错误。刚好将局部访问和全局访问的结果记反了。

**考点：** 变量作用域（局部 vs 全局）及作用域解析运算符 \`::\` 的用法。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 4,
            type: "single",
            question: `下列关于 C++ 中常量的说法，错误的是（ ）。`,
            options: [
                "常量的值在程序运行期间不能改变",
                "可以使用 const 关键字 define 常量",
                "可以使用 #define 预处理器定义常量",
                "常量必须在全局作用域定义"
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：D (常量必须在全局作用域定义)**

**选项逐项分析：**
- **A**：❌ 错误（描述正确）。这是常量的核心定义，即不可更改的量。
- **B**：❌ 错误（描述正确）。\`const\` 是 C++ 推荐的定义常量方式，具有类型检查。
- **C**：❌ 错误（描述正确）。这是源自 C 语言的预处理方式，虽然不具备类型检查，但在 C++ 中依然合法。
- **D**：✅ 正确（描述错误）。常量可以在任何作用域定义，包括函数内部（局部常量）或类内部。

**考点：** C++ 常量的定义方式（const 与 #define）及作用域。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 5,
            type: "single",
            question: `关于 C++ 中的局部变量，下列说法正确的是（ ）。`,
            options: [
                "局部变量在程序开始时创建，结束时销毁",
                "局部变量在所有函数中都可见",
                "局部变量存储在静态存储区",
                "局部变量的作用域仅限于其定义的代码块"
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (局部变量的作用域仅限于其定义的代码块)**

**选项逐项分析：**
- **A**：❌ 错误。这是全局变量的特性。局部变量在进入代码块时创建，退出时销毁。
- **B**：❌ 错误。局部变量只在定义它的代码块内可见。
- **C**：❌ 错误。普通局部变量存储在栈区（Stack），而非静态存储区（除非加了 \`static\` 关键字）。
- **D**：✅ 正确。局部变量的生命周期和作用域都严格受限于其所在的 \`{}\` 代码块。

**考点：** 局部变量的生命周期与作用域规则。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 6,
            type: "single",
            question: `选择排序的时间复杂度是（ ）。`,
            options: ["$O(N)$", "$O(n log n)$", "$O(N^2)$", "$O(log n)$"],
            answer: 0,
            score: 2,
            explanation: `**答案：C ($O(N^2)$)**

**选项逐项分析：**
- **A $O(N)$**：❌ 错误。选择排序无论数组初始状态如何，都必须进行两层循环嵌套。
- **B $O(n log n)$**：❌ 错误。这是快速排序或归并排序的平均效率，选择排序无法达到。
- **C $O(N^2)$**：✅ 正确。选择排序的工作原理是：第一轮找最小元素比较 $N-1$ 次，第二轮 $N-2$ 次... 这是一个等差数列求和，总时间复杂度恒为 $O(N^2)$。
- **D $O(log n)$**：❌ 错误。

**考点：** 选择排序的时间复杂度分析。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 7,
            type: "single",
            question: `插入排序在最好情况下的时间复杂度是（ ）。`,
            options: ["$O(1)$", "$O(N)$", "$O(N^2)$", "$O(n log n)$"],
            answer: 1,
            score: 2,
            explanation: `**答案：B ($O(N)$)**

**选项逐项分析：**
- **A $O(1)$**：❌ 错误。即使是有序数组，也至少需要遍历一次（$N-1$ 次比较）来确认顺序。
- **B $O(N)$**：✅ 正确。在最好情况下（数组已经完全有序），插入排序的每一项只需与前一项比较一次即可确定位置，总比较次数为 $N-1$，即 $O(N)$。
- **C $O(N^2)$**：❌ 错误。这是插入排序的最坏和平均时间复杂度。
- **D $O(n log n)$**：❌ 错误。

**考点：** 插入排序在不同初始状态下的时间复杂度。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 8,
            type: "single",
            question: `下列关于 C++ 异常处理的描述，错误的是（ ）。`,
            options: [
                "使用 try 块包含可能抛出异常的代码",
                "使用 catch 块捕获异常并处理",
                "throw 语句用于抛出异常",
                "每个 try 块必须对应且仅能对应一个 catch 块"
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (每个 try 块必须对应且仅能对应一个 catch 块)**

**选项逐项分析：**
- **A**：❌ 错误（描述正确）。\`try\` 块的作用就是划定可能产生异常的代码监控区。
- **B**：❌ 错误（描述正确）。\`catch\` 块负责捕获特定类型的异常并执行补救措施。
- **C**：❌ 错误（描述正确）。\`throw\` 关键字用于主动抛出异常对象。
- **D**：✅ 正确（描述错误）。一个 \`try\` 块可以后接多个 \`catch\` 块，分别处理不同类型的异常（如分捕获 \`std::runtime_error\` 和 \`std::out_of_range\`）。

**考点：** C++ 异常处理（try-catch-throw）的语法结构与逻辑关系。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 9,
            type: "single",
            question: `下列关于指针的说法，错误的是（ ）。`,
            options: [
                "指针变量存储的是地址",
                "可以通过解引用访问指针指向的值",
                "所有指针在 64 位系统上占 8 字节",
                "指针一旦指向某个地址就不能更改"
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：D (指针一旦指向某个地址就不能更改)**

**选项逐项分析：**
- **A 指针变量存储的是地址**：❌ 错误（描述正确）。指针的本质就是一个存储内存地址的变量。
- **B 可以通过解引用访问值**：❌ 错误（描述正确）。使用 \`*\` 运算符（解引用）可以获取指针指向地址处存储的数据。
- **C 64位系统占8字节**：❌ 错误（描述正确）。在 64 位环境下，地址空间是 64 位的，因此所有指针都占用 8 字节。
- **D 指针一旦指向就不能更改**：✅ 正确（描述错误）。普通指针变量可以随时被重新赋值，从而指向不同的内存地址。

**考点：** 指针的基础概念、大小、解引用及可变性。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 10,
            type: "single",
            question: `二维数组 int a[3][4] 中，元素 a[1][2] 的位置是（ ）。`,
            options: ["第 1 行第 2 列", "第 2 行第 3 列", "第 1 行第 3 列", "第 2 行第 2 列"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (第 2 行第 3 列)**

**选项逐项分析：**
- **A 第 1 行第 2 列**：❌ 错误。没有考虑到 C++ 数组下标是从 0 开始计数的。
- **B 第 2 行第 3 列**：✅ 正确。在 \`a[1][2]\` 中：
  - 第一个下标 \`1\` 代表第 2 行（下标 0 是第 1 行）。
  - 第二个下标 \`2\` 代表第 3 列（下标 0, 1 分别是第 1, 2 列）。
- **C / D**：❌ 错误。

**考点：** 二维数组的下标访问规则（0-based indexing）。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 11,
            type: "single",
            question: `递归函数必须包含（ ）。`,
            options: ["循环语句", "分支语句处理基准情况", "全局变量", "多个参数"],
            answer: 2,
            score: 2,
            explanation: `**答案：B (分支语句处理基准情况)**

**选项逐项分析：**
- **A 循环语句**：❌ 错误。递归是用函数自身调用来替代循环的，虽然递归可以改写为循环，但递归定义中并不强制包含循环。
- **B 分支语句处理基准情况**：✅ 正确。为了防止无限递归（死循环），递归函数内部必须有一个分支（通常是 \`if\`）来识别“基准情况”（Base Case），并在此处直接返回结果而不再进行递归。
- **C 全局变量**：❌ 错误。递归主要通过参数传递和返回值来交换信息，不依赖全局变量。
- **D 多个参数**：❌ 错误。只有一个参数（如求阶乘 \`fact(n)\`）也可以实现递归。

**考点：** 递归函数的两个核心要素：递归公式与基准情况（边界条件）。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 12,
            type: "single",
            question: `下列哪种算法通常采用分治思想实现（ ）。`,
            options: ["归并排序", "冒泡排序", "插入排序", "选择排序"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (归并排序)**

**选项逐项分析：**
- **A 归并排序**：✅ 正确。归并排序通过递归将数组对半拆分（分），处理完后再合并（治），是分治策略（Divide and Conquer）最经典的教学案例。
- **B / C / D**：❌ 错误。冒泡、插入、选择排序主要采用的是简单的迭代/循环思想。

**考点：** 分治思想在排序算法（归并排序、快速排序）中的应用。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 13,
            type: "single",
            question: `在 C++ 中，\`std::sort\` 位于哪个头文件中（ ）。`,
            options: ["<iostream>", "<vector>", "<algorithm>", "<cmath>"],
            answer: 0,
            score: 2,
            explanation: `**答案：C (<algorithm>)**

**选项逐项分析：**
- **A <iostream>**：❌ 错误。这是标准输入输出流头文件，包含 \`cin\`, \`cout\`。
- **B <vector>**：❌ 错误。这是动态数组容器的头文件。
- **C <algorithm>**：✅ 正确。C++ 标准库中绝大多数通用算法（如排序 \`sort\`、查找 \`find\`、最值 \`max/min\`）都定义在 \`<algorithm>\` 头文件中。
- **D <cmath>**：❌ 错误。这是常用数学函数的头文件（如 \`sqrt\`, \`pow\`）。

**考点：** C++ 标准库常用头文件的功能划分。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 14,
            type: "single",
            question: `在 C++ 中，下列哪个关键字用于声明类（ ）。`,
            options: ["class", "struct", "void", "static"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (class)**

**选项逐项分析：**
- **A class**：✅ 正确。这是 C++ 中定义类的核心关键字。
- **B struct**：❌ 错误（虽然在 C++ 中也可以定义类，但通常被视为“结构体”，且默认成员权限不同。在最严谨的考法下，\`class\` 才是专门用于类的关键字）。
- **C void**：❌ 错误。表示空类型或无返回值。
- **D static**：❌ 错误。用于声明静态成员或控制变量生命周期/链接属性。

**考点：** C++ 面向对象基础——类（Class）的定义。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 15,
            type: "single",
            question: `中国计算机学会（ CCF ）在 2024 年主办的 GESP 认证，9 月份考试的月份编号是（ ）。`,
            options: ["1", "2", "3", "4"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (3)**

**选项逐项分析：**
- **A 1 / B 2**：❌ 错误。分别对应 3 月和 6 月的考试。
- **C 3**：✅ 正确。GESP 每年举办四次认证（3, 6, 9, 12月），分别对应当年的第 1, 2, 3, 4 次。因此 9 月是第 3 次。
- **D 4**：❌ 错误。对应 12 月的考试。

**考点：** GESP 认证年度计划常识。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 16,
            type: "judge",
            question: `代码 int a = 10; int* p = &a; 可以正确定义指针和初始化指针。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
在 C++ 中，\`int* p\` 声明了一个整数指针变量 \`p\`，使用取地址运算符 \`&a\` 可以获取变量 \`a\` 的内存地址并将其存入指针中，这是指针最基础的用法。

**考点：** 指针的定义、初始化与取地址运算符 \`&\`。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 17,
            type: "judge",
            question: `在 C++ 中，引用传递允许函数修改传递给它的参数的值。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
引用传递（Pass by Reference）实际上是为实参创建了一个别名。在函数内部对引用参数的任何修改，都会直接同步到外部调用处的实参变量上，这与值传递（Pass by Value）有本质区别。

**考点：** C++ 引用传递的机制与实参修改。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 18,
            type: "judge",
            question: `指针的大小与其所指向的变量的数据类型的单位大小相同。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
指针的大小（占用字节数）仅取决于**操作系统的寻址能力**（如 32 位系统占 4 字节，64 位系统占 8 字节），与其所指向的变量类型（如 \`char\` 或 \`double\`）的大小完全无关。所有类型的指针在同一系统下大小都是一致的。

**考点：** 指针的物理大小与系统架构的关系。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 19,
            type: "judge",
            question: `二维数组的行的大小必须在定义时确定，列的大小可以动态变化。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
在 C++ 静态二维数组的定义中，**所有维度**（行和列）的大小都必须在编译时确定，且不可更改。只有使用 \`std::vector<std::vector<int>>\` 或动态分配内存时，维度才能实现动态变化。

**考点：** 静态数组的内存空间分配与维度限制。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 20,
            type: "judge",
            question: `递推算法通过逐步求解当前状态和前一个或几个状态之间的关系来解决问题。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
递推算法的核心在于寻找“状态转移关系”，即如何利用已知的一个或多个前序状态推导出当前状态。例如斐波那契数列中 $f(n) = f(n-1) + f(n-2)$ 就是典型的递推关系。

**考点：** 递推算法的状态转移逻辑。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 21,
            type: "judge",
            question: `选择排序是稳定的排序算法。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
选择排序的工作原理是每一轮选择剩余部分中的最小值与当前位置交换。这种“跨越式”的交换可能会破坏原本相等元素的相对顺序。例如序列 \`[5, 8, 5, 2, 9]\`，第一轮将第一个 \`5\` 与 \`2\` 交换后，它就跑到了第二个 \`5\` 的后面，因此是不稳定的。

**考点：** 排序算法的稳定性及其反例。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 22,
            type: "judge",
            question: `插入排序的时间复杂度总是比冒泡排序低。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
在平均和最坏情况下，两者的复杂度确实都是 $O(N^2)$。但在**最好情况**（数组已有序）下，插入排序由于每一项只需与前一项比较一次，复杂度为 $O(N)$；而标准冒泡排序仍需 $O(N^2)$（除非做了特殊的 \`swapped\` 标记优化）。因此不能说插入排序“总是”比冒泡低，但在某些特定数据分布下确实更优。

**考点：** 常见排序算法的复杂度对比与最好情况分析。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 23,
            type: "judge",
            question: `在 C++ 中，如果没有捕获到异常（没有匹配的 catch 块），程序会继续执行而不会终止。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
如果抛出的异常没有被任何 \`catch\` 块捕获，异常会一直沿着调用栈向上传播（Stack Unwinding）。如果传播到 \`main\` 函数之后仍未处理，系统会调用 \`std::terminate()\` 强制终止程序。

**考点：** C++ 异常传播机制与未捕获异常的处理。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 24,
            type: "judge",
            question: `求斐波那契数列第 n 项，递推法的时间复杂度为 $O(N)$。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
使用递推法求斐波那契数列（即从 $f(1), f(2)$ 开始，按顺序通过循环计算到 $f(n)$），只需遍历一次从 1 到 $n$ 的过程，每个数的计算都是常数时间 $O(1)$，因此总时间复杂度为 $O(N)$。

**考点：** 递推算法的效率分析。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 25,
            type: "judge",
            question: `GESP 4 级认证要求掌握二维数组的使用。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
根据 GESP（中国计算机学会编程能力等级认证）C++ 四级的大纲要求，二维数组的声明、初始化及多重循环访问是该级别的核心考察点之一。

**考点：** GESP 认证大纲范围。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        ...programmingQuestions
    ]
};
