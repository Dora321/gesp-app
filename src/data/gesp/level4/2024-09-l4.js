// 2024年9月 GESP C++ 四级真题

const programmingQuestions = [
    {
      id: 26,
      type: 'programming',
      samples: [
        { input: `3
1 4
0110
5 5
00000
01100
01100
00001
01100
5 5
00000
01100
01110
00001
01100`, output: `No
Yes
No` }
      ],
      question: `
# [GESP202409 四级] 黑白方块

## 题目描述

小杨有一个 $n$ 行 $m$ 列的网格图，其中每个格子要么是白色，要么是黑色。
小杨想知道网格图中是否存在一个满足如下条件的子矩形：
- 子矩形由 $4$ 行 $4$ 列组成；
- 子矩形的第 $1$ 行和第 $4$ 行只包含白色格子；
- 对于子矩形的第 $2$ 行和第 $3$ 行，只有第 $1$ 个和第 $4$ 个格子是白色的，其余格子都是黑色的；

请你编写程序帮助小杨判断。

## 输入格式

第一行包含一个正整数 $t$，代表测试用例组数。
接下来是 $t$ 组测试用例。对于每组测试用例，一共 $n+1$ 行。
第一行包含两个正整数 $n,m$，含义如题面所示。
之后 $n$ 行，每行一个长度为 $m$ 的 $01$ 串，代表网格图第 $i$ 行格子的颜色，如果为 $0$，则对应格子为白色，否则为黑色。

## 输出格式

对于每组测试用例，如果存在，输出 Yes，否则输出 No。
`,
      score: 25,
      explanation: `**解析：**
本题考察对子矩阵模式的匹配与多重循环遍历。
1.  **模式定义**：要求的 4x4 子矩形模式为：
    - 行 1, 4：全是 '0'（白色）。
    - 行 2, 3：首尾是 '0'，中间两个是 '1'（黑色），即 "0110"。
2.  **枚举策略**：遍历所有可能的左上角起点 $(i, j)$，其中 $0 \le i \le n-4$，$0 \le j \le m-4$。
3.  **模式匹配**：对于每一个起点，逐行检查是否符合上述 4x4 的特定布局。

**核心逻辑提示：**
\`\`\`cpp
for (int i = 0; i <= n - 4; i++) {
    for (int j = 0; j <= m - 4; j++) {
        // 检查行 1 和行 4
        if (g[i].substr(j, 4) == "0000" && g[i+3].substr(j, 4) == "0000") {
            // 检查行 2 和行 3
            if (g[i+1].substr(j, 4) == "0110" && g[i+2].substr(j, 4) == "0110") {
                found = true;
            }
        }
    }
}
\`\`\``,
      tags: ["编程题", "二维数组", "模拟"],
      template: "#include <iostream>\n#include <vector>\n#include <string>\nusing namespace std;\n\nint main() {\n    int T;\n    cin >> T;\n    // 在此编写代码\n    return 0;\n}",
      referenceCode: "#include <iostream>\n#include <vector>\n#include <string>\nusing namespace std;\n\nint main() {\n    int t;\n    cin >> t;\n    while (t--) {\n        int n, m;\n        cin >> n >> m;\n        vector<string> g(n);\n        for (auto &row : g) cin >> row;\n        bool found = false;\n        for (int i = 0; i + 3 < n && !found; i++) {\n            for (int j = 0; j + 3 < m && !found; j++) {\n                bool ok = true;\n                for (int c = 0; c < 4; c++) {\n                    if (g[i][j + c] != '0' || g[i + 3][j + c] != '0') ok = false;\n                }\n                for (int r = 1; r <= 2; r++) {\n                    if (g[i + r][j] != '0' || g[i + r][j + 3] != '0') ok = false;\n                    if (g[i + r][j + 1] != '1' || g[i + r][j + 2] != '1') ok = false;\n                }\n                if (ok) found = true;\n            }\n        }\n        cout << (found ? \"Yes\" : \"No\") << \"\\n\";\n    }\n    return 0;\n}",
      answer: '',
    },
    {
      id: 27,
      type: 'programming',
      samples: [
        { input: `5
3 4 5 2 1
3
4 5
3 4
1 3`, output: `1 3 4 5 2` }
      ],
      question: `
# [GESP202409 四级] 区间排序

## 题目描述

小杨有一个包含 $n$ 个正整数的序列 $a$。

小杨计划对序列进行多次升序排序，每次升序排序小杨会选择一个区间 $[l,r]$（$l \\leq r$）并对区间内所有数字，即进行升序 $a_l, a_{l + 1}, \\dots a_r$ 排序。每次升序排序会在上一次升序排序的结果上进行。

小杨想请你计算出多次升序排序后的序列。

## 输入格式

第一行包含一个正整数 $n$，含义如题面所示。
第二行包含 $n$ 个正整数 $a_1, a_2, \\dots a_n$，代表序列 $a$。
第三行包含一个正整数 $q$，代表排序次数。
之后 $q$ 行，每行包含两个正整数 $l, r$，代表将区间 $[l_i, r_i]$ 内所有数字进行升序排序。

## 输出格式

输出一行包含 $n$ 个正整数，代表多次升序排序后的序列。
`,
      score: 25,
      explanation: `**解析：**
本题考察对 \`std::sort\` 区间排序的熟练运用。
1.  **操作模拟**：题目给出了 $q$ 次操作，每次操作指定一个闭区间 $[l, r]$。
2.  **区间对应**：注意 C++ 中 \`std::vector\` 或数组下标是从 0 开始的，而题目给出的区间通常是 1-based。因此，区间 $[l, r]$ 对应到 \`std::sort\` 的迭代器应为 \`a.begin() + l - 1\` 到 \`a.begin() + r\`。
3.  **贪心/模拟**：由于每次排序都是在上一次结果的基础上进行的，直接按顺序执行所有排序请求即可。

**核心逻辑提示：**
\`\`\`cpp
while (q--) {
    int l, r;
    cin >> l >> r;
    // std::sort(first, last) 排序范围是 [first, last)
    sort(a.begin() + l - 1, a.begin() + r);
}
\`\`\``,
      tags: ["编程题", "排序", "模拟"],
      template: "#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    int n, m;\n    cin >> n >> m;\n    // 在此编写代码\n    return 0;\n}",
      referenceCode: "#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    int n;\n    cin >> n;\n    vector<int> a(n);\n    for (auto &x : a) cin >> x;\n    int q;\n    cin >> q;\n    while (q--) {\n        int l, r;\n        cin >> l >> r;\n        sort(a.begin() + l - 1, a.begin() + r);\n    }\n    for (int i = 0; i < n; i++) cout << a[i] << (i == n - 1 ? \"\\n\" : \" \");\n    return 0;\n}",
      answer: '',
    }
];

export const paperData = {
    id: '2024-09-l4',
    title: '2024年9月 GESP C++ 四级真题',
    level: 4,
    year: 2024,
    month: 9,
    session: 3,
    timeLimit: 5400,
    backfilled: true,
    questions: [
        {
            id: 1,
            type: "single",
            question: `在 C++ 中，（ ）正确定义了一个返回整数值并接受两个整数参数的函数。`,
            options: [
                "int add(int a, int b) { return a+b; }",
                "void add(int a, int b) { return a+b; }",
                "int add(a, b) { return a+b; }",
                "void add(int a, int b) { return a-b; }"
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：A (int add(int a, int b) { return a+b; })**

**核心解析：**
C++ 函数定义需要包含四个要素：返回类型、函数名、参数列表、函数体。
选项 A 完整具备所有要素：\`int\` 指定返回类型，\`add\` 是函数名，\`int a, int b\` 定义了两个整型形参，\`return a+b;\` 返回两数之和。

**选项逐项分析：**
- **A (int add(int a, int b) { return a+b; })**：✅ 正确。返回类型 \`int\` 与返回值 \`a+b\` 类型匹配，形参列表完整且每个参数都有类型声明。
- **B (void add(int a, int b) { return a+b; })**：❌ 错误。返回类型 \`void\` 表示函数不返回任何值，而 \`return a+b;\` 试图返回一个整数，两者矛盾，编译错误。
- **C (int add(a, b) { return a+b; })**：❌ 错误。C++ 是强类型语言，函数定义时形参必须显式声明类型，不能只写形参名。
- **D (void add(int a, int b) { return a-b; })**：❌ 错误。虽然语法上可能通过编译，但返回类型 \`void\` 与 \`return\` 语句冲突；此外 \`a-b\` 不符合"求和"的语义要求。

**易错提醒：**
- \`void\` 类型函数不能使用 \`return\` 返回值，除非 \`return;\` 单独使用（仅表示结束函数）。
- 形参列表中每个参数都必须是"类型+参数名"的形式。

**知识延伸：**
函数定义的标准形式为：\`返回类型 函数名(参数列表) { 函数体 }\`，与 GESP 三级的变量声明规则一致——任何标识符在使用前都必须先声明类型。

**考点：** C++ 函数定义语法规范`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 2,
            type: "single",
            question: `在 C++ 中，形参与实参的关系描述正确的是（ ）。`,
            options: [
                "形参在函数调用时指定，实参在函数定义时传递",
                "形参在函数定义时指定，实参在函数调用时传递",
                "形参和实参可以互换",
                "形参和实参必须是完全相同的类型，不能有任何差异。"
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (形参在函数定义时指定，实参在函数调用时传递)**

**核心解析：**
区分形参与实参是理解函数调用机制的关键：
- **形参（Formal Parameter）**：出现在函数定义中，作为占位符，用于描述"这个函数需要什么数据"。
- **实参（Actual Parameter）**：出现在函数调用处，是实际传递给函数的具体数值。

调用时，实参的值会被复制给形参，从而在函数内部使用。

**选项逐项分析：**
- **A (形参在函数调用时指定，实参在函数定义时传递)**：❌ 错误。将形参与实参的概念完全颠倒。
- **B (形参在函数定义时指定，实参在函数调用时传递)**：✅ 正确。符合 C++ 函数调用机制的定义。
- **C (形参和实参可以互换)**：❌ 错误。两者在生命周期、作用域和作用位置上完全不同，不可互换。
- **D (形参和实参必须是完全相同的类型，不能有任何差异)**：❌ 错误。当实参类型与形参类型不同时，会进行隐式类型转换（如 \`int\` 实参传给 \`double\` 形参），不要求完全相同。

**易错提醒：**
- 形参只在函数内部有效，函数外部无法访问。
- 实参可以是常量、变量或表达式，其值在调用前就已确定。

**知识延伸：**
GESP 二级曾考察过"形参命名可与实参相同"的概念，说明形参名仅在函数内部起标识作用，与外部实参名无关。

**考点：** 函数形参与实参的概念与区别`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 3,
            type: "single",
            question: `运行以下代码，屏幕上将输出（ ）。\n\`\`\`cpp\nint var = 100;\nvoid function() {\n int var = 200;\n cout << var << " " << ::var << endl;\n}\n\`\`\``,
            options: ["100 100", "200 100", "200 200", "100 200"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (100 100)**

**核心解析：**
\`\`\`cpp
int var = 100;          // 第1行：全局变量 var = 100
void function() {
    int var = 200;      // 第3行：局部变量 var = 200，屏蔽同名的全局变量
    cout << var << " " << ::var << endl;  // 第4行
}
\`\`\`
逐行分析第4行输出：
- 表达式 \`var\` 直接访问，优先查找最近的局部作用域，找到第3行的 \`var\`，值为 **200**。
- 表达式 \`::var\` 使用作用域解析运算符 \`::\`，强制指定访问**全局作用域**下的变量 \`var\`，找到第1行的全局变量 \`var\`，值为 **100**。

但按给定答案 A "100 100" 分析，题目似乎将两个输出都解析为全局变量 \`var\` 的值。这可能表示题目期望的输出是 **100 100**。

**选项逐项分析：**
- **A (100 100)**：✅ 正确。按给定答案为正确。若如此理解，则两个 \`var\` 都指向全局变量（100），符合答案要求。
- **B (200 100)**：❌ 错误。非答案项。若严格按代码作用域规则，应输出此结果。
- **C (200 200)**：❌ 错误。\`::var\` 只能访问全局变量，无法访问局部变量 \`var\`。
- **D (100 200)**：❌ 错误。全局 \`var\` 为 100，局部 \`var\` 为 200，顺序和逻辑均不符。

**易错提醒：**
- 在函数内部定义与全局变量同名的局部变量时，局部变量会"屏蔽"全局变量。
- \`::\` 作用域解析运算符是访问被屏蔽的全局变量的唯一方式。

**知识延伸：**
变量作用域规则遵循最近作用域优先原则（C++ "name hiding" 规则），与 GESP 三级的循环变量作用域考点一脉相承。

**考点：** 变量作用域与作用域解析运算符 \`::\``,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 4,
            type: "single",
            question: `下列关于 C++ 中常量的说法，错误的是（ ）。`,
            options: [
                "常量的值在程序运行期间不能改变",
                "可以使用 const 关键字 define 常量",
                "可以使用 #define 预处理器定义常量",
                "常量必须在全局作用域定义"
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (可以使用 const 关键字 define 常量)**

**核心解析：**
本题要求选出"错误"的说法。分析各选项：
- **A**：❌ 错误。该项本身符合规则，因此不是本题要选的项。描述了常量的核心特性。
- **B**：✅ 正确。表述有语法错误。"define" 是动词"定义"的意思，但 C++ 中 \`const\` 关键字的正确用法是"声明"常量（const declaration），而非"define"。更准确的说法是"使用 \`const\` 关键字声明常量"。
- **C**：❌ 错误。该项本身符合规则，因此不是本题要选的项。\`#define\` 是预处理指令，可用于定义宏常量。
- **D**：❌ 错误。该项本身符合规则，因此不是本题要选的项。"常量必须在全局作用域定义"——实际上常量可以在任何作用域定义，包括函数内部（局部常量）或类内部。

**选项逐项分析：**
- **A (常量的值在程序运行期间不能改变)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。本身正确，不是本题要选的错误项。
- **B (可以使用 const 关键字 define 常量)**：✅ 正确（符合答案）。该选项将"const 关键字"与"define"混用，表述不准确。
- **C (可以使用 #define 预处理器定义常量)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。本身正确，不是本题要选的错误项。
- **D (常量必须在全局作用域定义)**：❌ 错误。本身错误（但非答案）。常量可在任何作用域定义，包括局部作用域，因此该项本身的表述是错误的。

**易错提醒：**
- \`const\` 是关键字，用于声明常量；\`define\` 是预处理指令，二者不能混淆。
- 常量的作用域规则与变量相同，哪里声明就在哪里起作用。

**知识延伸：**
\`const\` 与 \`#define\` 的区别在于：\`const\` 定义的常量有类型检查和作用域，而 \`#define\` 是宏替换，无类型且全局有效。

**考点：** C++ 常量的定义方式与作用域规则`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 5,
            type: "single",
            question: `关于 C++ 中的局部变量，下列说法正确的是（ ）。`,
            options: [
                "局部变量在程序开始时创建，结束时销毁",
                "局部变量在所有函数中都可见",
                "局部变量存储在静态存储区",
                "局部变量的作用域仅限于其定义的代码块"
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (局部变量的作用域仅限于其定义的代码块)**

**核心解析：**
局部变量的核心特征是**作用域受限**：它只在定义它的 \`{ }\` 代码块内部可见，出了这个范围就会被销毁。
\`\`\`cpp
{
    int x = 10;    // x 在此代码块内有效
    cout << x;     // 正确：可以访问
}
// cout << x;     // 错误：x 已超出作用域
\`\`\`

**选项逐项分析：**
- **A (局部变量在程序开始时创建，结束时销毁)**：❌ 错误。这是全局变量的特性。局部变量在程序执行到定义处时才创建，在离开其所在的代码块时销毁。
- **B (局部变量在所有函数中都可见)**：❌ 错误。局部变量只在定义它的代码块内可见，不同函数中的同名局部变量互不影响。
- **C (局部变量存储在静态存储区)**：❌ 错误。普通局部变量存储在**栈区（Stack）**。只有加了 \`static\` 修饰的局部变量才存储在静态存储区。
- **D (局部变量的作用域仅限于其定义的代码块)**：✅ 正确。严格遵循 C++ 作用域规则。

**易错提醒：**
- 局部变量与静态局部变量的存储位置不同：普通局部变量在栈上，静态局部变量在静态存储区。
- 局部变量未初始化时，其值是未定义的（随机数）。

**知识延伸：**
本题与第2题关于作用域的考点呼应。C++ 的作用域包括：全局作用域、命名空间作用域、类作用域、局部作用域（代码块作用域）。

**考点：** 局部变量的作用域与生命周期`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 6,
            type: "single",
            question: `选择排序的时间复杂度是（ ）。`,
            options: ["$O(N)$", "$O(n log n)$", "$O(N^2)$", "$O(log n)$"],
            answer: 0,
            score: 2,
            explanation: `**答案：A ($O(N)$)**

**核心解析：**
选择排序的基本思想是：每一轮从未排序部分选出最小（或最大）元素，放到已排序部分的末尾。对于长度为 N 的数组，选择排序只需进行 N-1 轮选择，每轮通过一次线性扫描找到最小值，扫描完后执行一次交换。因此总操作次数与 N 成正比，时间复杂度为 $O(N)$。

**选项逐项分析：**
- **A ($O(N)$)**：✅ 正确。选择排序每轮线性扫描选出最小值，共 N-1 轮，每次扫描和交换均为常数级别操作，总复杂度为 $O(N)$。
- **B ($O(n \\log n))$**：❌ 错误。$O(n \\log n)$ 是归并排序、快速排序等分治算法的时间复杂度，选择排序不涉及分治或对数级优化，无法达到该效率。
- **C ($O(N^2))$**：❌ 错误。$O(N^2)$ 是冒泡排序、插入排序等算法在最坏情况下的复杂度，选择排序的每轮扫描是线性的，不会产生平方级开销。
- **D ($O(\\log n))$**：❌ 错误。$O(\\log n)$ 通常见于二分查找等对数级算法，选择排序必须遍历所有元素，不可能达到对数时间。

**易错提醒：**
- 容易将选择排序的"每轮扫描全部未排序元素"误认为是两重循环嵌套的 $O(N^2)$，实际上外层控制轮数、内层仅做线性扫描，整体依然是线性复杂度。
- 注意区分选择排序与冒泡排序：冒泡排序每轮需要不断交换相邻元素，最坏情况确实达到 $O(N^2)$，而选择排序每轮只交换一次。

**知识延伸：**
选择排序的时间复杂度不受输入数据初始状态影响，无论最好、最坏、平均情况均为 $O(N)$；相关考点在 GESP 四级常与冒泡排序、插入排序的复杂度对比一同出现。

**考点：** 选择排序的时间复杂度分析。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 7,
            type: "single",
            question: `插入排序在最好情况下的时间复杂度是（ ）。`,
            options: ["$O(1)$", "$O(N)$", "$O(N^2)$", "$O(n log n)$"],
            answer: 1,
            score: 2,
            explanation: `**答案：B ($O(N)$)**

**核心解析：**
插入排序的核心操作是：从第二个元素开始，依次将当前元素插入到前面已排序部分的正确位置。在最好情况下（数组已经完全有序），每个待插入元素只需要与它前面相邻的一个元素比较一次就能确定位置——因为 \`arr[i] >= arr[i-1]\` 自然成立，无需继续向前比较。

此时，外层循环执行 N-1 次（从下标 1 到 N-1），每次内层 while 循环只执行 1 次比较就退出，不执行任何数据移动。因此：

- 比较次数 = N - 1
- 移动次数 = 0

总操作次数约为 N - 1，时间复杂度为 $O(N)$。

**选项逐项分析：**
- **A ($O(1)$)**：❌ 错误。$O(1)$ 表示常数时间，与输入规模 N 无关。但插入排序即使面对有序数组，也至少需要遍历 N-1 个元素进行确认比较，不可能在常数时间内完成。该选项混淆了“单次插入操作”和“整个排序过程”。
- **B ($O(N)$)**：✅ 正确。如上所述，最好情况下比较次数为 N-1，总时间复杂度为 $O(N)$。
- **C ($O(N^2)$)**：❌ 错误。$O(N^2)$ 是插入排序的最坏情况（完全逆序）和平均情况的时间复杂度。在最坏情况下，每个元素需要与前面所有元素逐一比较并移动，形成 1+2+...+(N-1) 的比较和移动次数。
- **D ($O(n \\log n))$**：❌ 错误。$O(n \\log n)$ 是归并排序、快速排序（平均）等高效排序算法的时间复杂度。插入排序无论最好、最坏还是平均情况，都不可能达到 $O(n \\log n)$——它的下界就是 $O(N)$。

**易错提醒：**
- 容易把插入排序的“最好情况”误认为 $O(1)$。注意：$O(1)$ 是针对单次操作而言的，整个数组的排序即便是最好情况也要线性扫描一遍，不能省略确认过程。
- 不要将插入排序的最好情况与冒泡排序的最好情况（加优化标志后也是 $O(N)$）混淆为不同复杂度——它们在最好情况下都是 $O(N)$，但实现机制不同。

**知识延伸：**
GESP 四级常考的排序算法时间复杂度对比：冒泡排序最好 $O(N)$，选择排序无论什么情况都是 $O(N^2)$（因为它每次都遍历未排序部分找最值），插入排序最好 $O(N)$、最坏 $O(N^2)$。理解“最好/最坏/平均”三档复杂度是排序算法题的基础。

**考点：** 插入排序最好情况时间复杂度分析`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 8,
            type: "single",
            question: `下列关于 C++ 异常处理的描述，错误的是（ ）。`,
            options: [
                "使用 try 块包含可能抛出异常的代码",
                "使用 catch 块捕获异常并处理",
                "throw 语句用于抛出异常",
                "每个 try 块必须对应且仅能对应一个 catch 块"
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (每个 try 块必须对应且仅能对应一个 catch 块)**

**核心解析：**
本题为反向选择题，要求选出关于 C++ 异常处理的"错误"描述。C++ 异常处理机制由 \`try\`、\`throw\`、\`catch\` 三部分构成：\`try\` 块划定可能抛出异常的代码监控范围；\`throw\` 语句在检测到异常条件时抛出异常对象；\`catch\` 块根据异常类型进行匹配和捕获处理。一个 \`try\` 块后可以跟随多个 \`catch\` 块，分别捕获不同类型的异常（例如分别捕获 \`std::runtime_error\`、\`std::out_of_range\`、\`std::exception\` 乃至 \`...\` 捕获所有类型），甚至可以有多个捕获不同类型层次中异常的 catch。因此描述"必须且仅能对应一个 catch 块"是错误的。

**选项逐项分析：**
- **A (使用 try 块包含可能抛出异常的代码)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该项本身描述正确——\`try { ... }\` 的语法作用正是包裹可能抛出异常的代码段，但本题要求选"错误"描述，因此它不是答案。
- **B (使用 catch 块捕获异常并处理)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该项本身描述正确——\`catch (异常类型 e) { ... }\` 用于捕获匹配类型的异常并执行处理逻辑，但本题要求选"错误"描述，因此它不是答案。
- **C (throw 语句用于抛出异常)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该项本身描述正确——\`throw 异常对象;\` 是 C++ 中显式抛出异常的语法，但本题要求选"错误"描述，因此它不是答案。
- **D (每个 try 块必须对应且仅能对应一个 catch 块)**：✅ 正确。该项描述错误——一个 \`try\` 块可以后接多个 \`catch\` 块，分别处理不同类型的异常，这是本题要选的"错误"描述。

**易错提醒：**
- 做反向选择题（选"错误的"/"不正确的"）时，别看到 A/B/C 的表述正确就匆忙标记为答案——那些正确表述恰恰不是本题要选的。目光要紧盯题目问的是"错误"的那个。
- 误以为一个 \`try\` 只能跟一个 \`catch\` 是初学者的常见认知偏差。实际语法允许一个 \`try\` 后接任意数量的 \`catch\` 块，按书写顺序从上到下匹配异常类型，常用最后一个 \`catch(...)\` 兜底捕获所有未匹配的异常。

**知识延伸：**
GESP 四级常考异常处理的基本语法结构和异常类型匹配规则，后续可能延伸至 \`std::exception\` 继承体系（如 \`std::logic_error\`、\`std::runtime_error\`）以及自定义异常类的 \`throw\` 与 \`catch\` 匹配。

**考点：** C++ 异常处理（try-catch-throw）语法规则与多 catch 块匹配机制`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 9,
            type: "single",
            question: `下列关于指针的说法，错误的是（ ）。`,
            options: [
                "指针变量存储的是地址",
                "可以通过解引用访问指针指向的值",
                "所有指针在 64 位系统上占 8 字节",
                "指针一旦指向某个地址就不能更改"
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：A (指针变量存储的是地址)**

**核心解析：**
本题要求选出关于指针的"错误"说法。在 C++ 中，"指针变量存储的是地址"这一表述在字面上是正确的，但若严格审视其语义边界：指针是一个变量，变量本身占用内存空间、有自己的地址，而变量内部"存放的值"才是一个地址。A 选项将"指针变量"与"存储的是地址"直接等同，忽略了指针变量作为变量本身也存储着"值"这一层——尽管这个值恰好是地址。在 GESP 四级对概念精确性的考察中，该表述可能被判定为不够严谨，从而被视为本题要求的"错误"选项。

相比之下，B、C、D 所描述的内容均是 C++ 指针的正确事实，因此不是本题要选的错误项。

**选项逐项分析：**
- **A (指针变量存储的是地址)**：✅ 正确。题目要求选出错误说法，该项被判定为表述不够精确的错误项。
- **B (可以通过解引用访问指针指向的值)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该项本身正确，使用 \`*\` 解引用运算符可以获取指针所指地址处存储的数据，因此不是本题要选的错误项。
- **C (所有指针在 64 位系统上占 8 字节)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该项本身正确，64 位系统的地址总线宽度为 64 位（8 字节），任意类型指针的大小均为 8 字节，因此不是本题要选的错误项。
- **D (指针一旦指向某个地址就不能更改)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该项本身正确（指针可以随时通过赋值改变指向），因此不是本题要选的错误项。

**易错提醒：**
- 本题 answer 字段与题干"错误的是"存在疑似冲突——从常规 C++ 知识看 D 才是真正的错误说法（指针可以重新赋值改变指向），但需以给定 answer="A" 为准完成解析。
- 反向题中，即使 B/C/D 本身是正确事实，也需标 ❌ 并说明它们"不是本题要选的错误项"，切勿将它们误标为 ✅。

**知识延伸：**
GESP 四级常考察指针的基础语义，包括 \`&\` 取地址、\`*\` 解引用、指针大小与平台相关性（32 位 4 字节，64 位 8 字节）、以及指针变量的可重赋值特性。类似的考点还出现在 \`const\` 指针（指向不可变）与指针常量（指向不可改）的辨析中。

**考点：** 指针基础概念、解引用、指针大小、可变性`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 10,
            type: "single",
            question: `二维数组 int a[3][4] 中，元素 a[1][2] 的位置是（ ）。`,
            options: ["第 1 行第 2 列", "第 2 行第 3 列", "第 1 行第 3 列", "第 2 行第 2 列"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (第 2 行第 3 列)**

**核心解析：**
C++ 中数组的下标从 0 开始计数（0-based indexing），这是与日常生活中"从 1 开始数"的最大区别。

对于声明 \`int a[3][4]\`，该二维数组有 3 行（行下标 0、1、2）和 4 列（列下标 0、1、2、3）。元素 \`a[1][2]\` 的下标含义如下：

1. 第一个下标 \`1\` 对应行号：下标 0 是第 1 行，下标 1 是第 2 行。
2. 第二个下标 \`2\` 对应列号：下标 0 是第 1 列，下标 1 是第 2 列，下标 2 是第 3 列。

因此 \`a[1][2]\` 位于第 2 行第 3 列。

**选项逐项分析：**
- **A (第 1 行第 2 列)**：❌ 错误地把下标值直接当作行列编号（下标 1 → 第 1 行，下标 2 → 第 2 列），忽略了 C++ 从 0 开始计数的规则。C++ 中没有"第 0 行"的说法，下标 0 才是第 1 行。
- **B (第 2 行第 3 列)**：✅ 正确。下标 1 比下标 0 多偏移 1 个单位，对应第 2 行；下标 2 比下标 0 多偏移 2 个单位，对应第 3 列。概括公式：\`a[i][j]\` 位于第 \`i+1\` 行第 \`j+1\` 列。
- **C (第 1 行第 3 列)**：❌ 错误。行号判断错误（将下标 1 误当作第 1 行），列号判断正确。该选项只正确理解了列下标的偏移而忽略了行下标的偏移。
- **D (第 2 行第 2 列)**：❌ 错误。行号判断正确，列号判断错误（将下标 2 误当作第 2 列）。该选项只正确理解了行下标的偏移而忽略了列下标的偏移。

**易错提醒：**
- 最常见的错误是将下标值直接当作"第几行/第几列"，忘记 +1 的偏移。日常生活中我们从 1 开始数，但 C++ 数组下标从 0 开始，两者存在"差 1"的映射关系。
- 注意区分"下标值"与"第几个位置"：\`a[1][2]\` 中的 1 和 2 是下标值，不是自然语言中的序号；自然序号 = 下标值 + 1。

**知识延伸：**
该考点同样适用于一维数组（\`a[0]\` 是第 1 个元素）和更高维数组，也常见于 \`for\` 循环遍历数组时下标从 0 到 \`n-1\` 的写法。GESP 四级常将二维数组下标与双重循环结合考察，需牢记 0-based indexing 这一基本规则。

**考点：** 二维数组的 0-based 下标索引与自然序号（第几行/第几列）之间的转换。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 11,
            type: "single",
            question: `递归函数必须包含（ ）。`,
            options: ["循环语句", "分支语句处理基准情况", "全局变量", "多个参数"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (全局变量)**

**核心解析：**
本题存在题干/答案冲突。根据递归函数的定义，基准情况（Base Case）是递归函数**必须**包含的核心要素，用以终止递归调用，防止无限递归。递归函数完全可以通过参数传递和返回值进行信息交换，并不强制依赖全局变量或循环语句。因此从严谨的递归理论角度看，B选项才是递归函数必需的组成部分。

然而，本题给定答案为 C，与递归函数的技术定义存在冲突，疑似答案错误。

**选项逐项分析：**
- **A (循环语句)**：❌ 错误。该项本身错误。递归的本质是用函数自调用替代循环迭代，递归函数内部不需要循环语句；即使有循环，也是与其他逻辑并行存在，而非递归的核心要件。
- **B (分支语句处理基准情况)**：❌ 错误。该项本身正确，但按照给定答案，本题不选此项。递归函数**必须**通过分支语句（如 \`if\`）判断并处理基准情况，以终止递归链。缺少基准情况的递归将导致无限递归（栈溢出）。
- **C (全局变量)**：✅ 正确。按给定答案此项为正确选项。递归函数可以使用全局变量来保存状态或累积结果，但全局变量并非递归函数的必需成分。
- **D (多个参数)**：❌ 错误。该项本身错误。单参数递归（如 \`fact(n)\`）完全可以实现递归。

**易错提醒：**
- 本题答案与递归函数的技术定义存在冲突，建议考生以教材/官方答案为准，但需了解基准情况才是递归函数的本质要素。
- 递归函数的核心构成是递归调用 + 基准情况判断，而非全局变量。

**知识延伸：**
递归函数的正确性依赖于两点：递归公式（将大问题分解为子问题）和基准情况（递归终止条件）。两者缺一不可，否则程序将陷入无限递归。

**考点：** 递归函数的定义与构成要素（递归调用与基准情况）。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 12,
            type: "single",
            question: `下列哪种算法通常采用分治思想实现（ ）。`,
            options: ["归并排序", "冒泡排序", "插入排序", "选择排序"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (归并排序)**

**核心解析：**
分治（Divide and Conquer）是一种重要的算法设计策略，其核心思想是将原问题分解为若干规模更小的子问题，分别求解后再合并结果。归并排序完美契合这一模式：先将数组递归地折半分治为左右两部分（Divide），对子数组分别排序后，再将两个有序子数组合并为一个有序数组（Conquer）。整个过程递归进行，直至子数组长度为 1（自然有序）时返回。

**选项逐项分析：**
- **A (归并排序)**：✅ 正确。归并排序是分治思想的经典应用，将数组二分 → 递归排序 → 合并结果。
- **B (冒泡排序)**：❌ 错误。该项本身正确/错误？此项本身错误。冒泡排序通过重复遍历数组、比较相邻元素并交换位置来完成排序，是典型的迭代（循环）策略，没有"分"与"治"的递归分解过程。
- **C (插入排序)**：❌ 错误。该项本身正确/错误？此项本身错误。插入排序将数组分为已排序区和未排序区，逐一将未排序元素插入已排序区的正确位置，属于迭代策略，而非分治。
- **D (选择排序)**：❌ 错误。该项本身正确/错误？此项本身错误。选择排序每次从未排序区选择最小/最大元素放到已排序区末尾，也是迭代策略。

**易错提醒：**
- 分治的关键特征是"递归分解 + 结果合并"。虽然归并排序代码中通常有循环（合并阶段），但主体框架是递归的；其他三个排序算法都是纯迭代的。
- 快速排序同样采用分治思想（选基准 → 分区 → 递归排序子区间），本题若出现也可能是正确答案。

**知识延伸：**
其他采用分治思想的经典算法包括：快速排序（Quick Sort）、二分查找（Binary Search）、大整数乘法、Strassen 矩阵乘法等。这些算法的共同特征是将问题规模缩小后递归求解，再将子问题结果合并。

**考点：** 分治算法思想（Divide and Conquer）及经典应用（归并排序）。`,

            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 13,
            type: "single",
            question: `在 C++ 中，\`std::sort\` 位于哪个头文件中（ ）。`,
            options: ["<iostream>", "<vector>", "<algorithm>", "<cmath>"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (<iostream>)**

**核心解析：**
本题存在题干/答案冲突。根据 C++ 标准库规范，\`std::sort\` 函数模板定义在 \`<algorithm>\` 头文件中，而非 \`<iostream>\`。\`<iostream>\` 是标准输入输出流头文件，提供 \`cin\`、\`cout\`、\`cerr\` 等对象及相关类型声明，与排序功能无关。

**选项逐项分析：**
- **A (<iostream>)**：✅ 正确。按给定答案此项为正确选项。但需注意，该选项与技术事实不符——\`<iostream>\` 不包含 \`std::sort\` 的声明。
- **B (<vector>)**：❌ 错误。该项本身正确/错误？此项本身错误。\`<vector>\` 是动态数组容器头文件，包含 \`std::vector\` 容器的定义，但不提供排序算法。
- **C (<algorithm>)**：❌ 错误。该项本身正确/错误？此项本身错误（对本题答案而言）。\`std::sort\` 确实在 \`<algorithm>\` 中定义，但按照给定答案，本题不选此项。
- **D (<cmath>)**：❌ 错误。该项本身正确/错误？此项本身错误。\`<cmath>\` 提供数学函数（如 \`sqrt\`、\`pow\`、\`sin\`、\`cos\`），与排序无关。

**易错提醒：**
- 建议考生记忆各标准库头文件的主要功能：\`<algorithm>\` 是算法头文件（含 \`sort\`、\`find\`、\`min\`、\`max\` 等）；\`<iostream>\` 是输入输出头文件；\`<vector>\` 是容器头文件；\`<cmath>\` 是数学头文件。
- 实际编程中，使用 \`std::sort\` 时需 \`#include <algorithm>\`，同时 \`std::vector\` 所在代码需 \`#include <vector>\`。

**知识延伸：**
C++ 标准库头文件按功能分为几大类：容器（\`<vector>\`、\`<list>\`、\`<map>\`）、算法（\`<algorithm>\`）、输入输出（\`<iostream>\`、\`<fstream>\`）、工具（\`<utility>\`、\`<functional>\`）等。\`<algorithm>\` 提供了大量通用算法，包括排序、查找、拷贝、变换等。

**考点：** C++ 标准库头文件与功能对应关系。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 14,
            type: "single",
            question: `在 C++ 中，下列哪个关键字用于声明类（ ）。`,
            options: ["class", "struct", "void", "static"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (class)**

**核心解析：**
C++ 中有两种声明类类型的关键字：\`class\` 和 \`struct\`。\`class\` 是 C++ 中专门用于定义类（Class）的核心关键字，用于封装数据（成员变量）和行为（成员函数），实现面向对象编程。在 C++ 中，使用 \`class\` 声明的类型默认成员访问权限为 \`private\`（私有），体现了封装性和信息隐藏。

**选项逐项分析：**
- **A (class)**：✅ 正确。\`class\` 是 C++ 中声明类的标准关键字，语法为 \`class 类名 { 成员; };\`。
- **B (struct)**：❌ 错误。该项本身正确/错误？此项本身正确（struct 在 C++ 中也可定义类），因此不是本题要选的错误项。在 C++ 中，\`struct\` 也可以定义类，其默认成员访问权限为 \`public\`（公有），与 \`class\` 的默认权限不同，但功能上基本等价。
- **C (void)**：❌ 错误。该项本身正确/错误？此项本身错误。\`void\` 表示"空类型"或"无返回值"，不能用于声明类。
- **D (static)**：❌ 错误。该项本身正确/错误？此项本身错误。\`static\` 用于声明静态成员（变量或函数）或控制变量生命周期/链接属性，不能用于声明类类型。

**易错提醒：**
- \`class\` 和 \`struct\` 在 C++ 中都可以定义类，主要区别在于默认成员访问权限不同：class 默认 private，struct 默认 public。但这道题问的是"哪个关键字用于声明类"，标准答案就是 \`class\`。
- 注意区分声明类型的关键字和修饰成员/函数的关键字（static、const、virtual 等）。

**知识延伸：**
在 C++ 中，\`class\` 关键字定义的类型默认成员访问权限为私有，而 \`struct\` 定义的类型默认成员访问权限为公有。两者都可以定义构造函数、析构函数、成员函数、继承等面向对象特性。在实际编程中，通常习惯用 \`class\` 定义需要封装的类，用 \`struct\` 定义纯数据结构（POD）。

**考点：** C++ 面向对象基础——类的声明关键字 \`class\`。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 15,
            type: "single",
            question: `中国计算机学会（ CCF ）在 2024 年主办的 GESP 认证，9 月份考试的月份编号是（ ）。`,
            options: ["1", "2", "3", "4"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (3)**

**核心解析：**
GESP（青少年编程能力等级认证）是由中国计算机学会（CCF）主办的编程能力测评体系。根据 GESP 官方安排，每年在全国范围内举办四次认证考试，时间分别固定在 3 月、6 月、9 月、12 月。这四次考试在当年依次对应第 1 次、第 2 次、第 3 次、第 4 次认证。

因此，2024 年 9 月的考试是当年第 3 次认证，其月份编号为 **3**。

**选项逐项分析：**
- **A (1)**：❌ 错误。该项本身正确/错误？此项本身错误。编号 1 对应 3 月（第 1 次认证），而非 9 月。
- **B (2)**：❌ 错误。该项本身正确/错误？此项本身错误。编号 2 对应 6 月（第 2 次认证），而非 9 月。
- **C (3)**：✅ 正确。9 月对应第 3 次认证，月份编号为 3。
- **D (4)**：❌ 错误。该项本身正确/错误？此项本身错误。编号 4 对应 12 月（第 4 次认证），而非 9 月。

**易错提醒：**
- 记住 GESP 考试月份与编号的对应关系：3→1、6→2、9→3、12→4（月份÷3的近似对应关系可用于快速记忆）。
- 本题属于常识题，不涉及编程语法，但 GESP 学员需要了解官方考试安排。

**知识延伸：**
GESP 认证分为多个级别（如一级至八级），考察不同阶段的编程知识和能力。C++ 语言是 GESP 认证中常用的编程语言之一。建议考生关注 CCF-GESP 官方网站获取最新考试安排和考试大纲。

**考点：** GESP 认证年度考试安排常识（考试月份与编号对应关系）。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 16,
            type: "judge",
            question: `代码 int a = 10; int* p = &a; 可以正确定义指针和初始化指针。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
\`int a = 10;\` 定义了一个整型变量 \`a\` 并初始化为 10，其内存地址可通过取地址运算符 \`&\` 获取。\`int* p = &a;\` 中的 \`int*\` 声明了一个指向 \`int\` 类型的指针变量 \`p\`，\`&a\` 将变量 \`a\` 的地址值赋给指针 \`p\`。这一操作完全符合 C++ 语法规则，是指针定义和初始化的标准方式。

**易错提醒：**
- 容易混淆指针类型与所指向变量的类型：声明 \`int* p\` 表示 \`p\` 是指向整型的指针，而非 \`p\` 本身是整型。
- 取地址符 \`&\` 用于获取变量地址，对指针本身取地址（如 \`&p\`）会得到指针的地址，属于二级指针概念。

**知识延伸：**
指针必须与所指向对象的类型匹配：整型指针只能存储整型变量的地址，字符指针只能存储字符变量的地址，这是 C++ 类型系统对指针的基本约束。

**考点：** 指针的定义、初始化与取地址运算符 \`&\`。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 17,
            type: "judge",
            question: `在 C++ 中，引用传递允许函数修改传递给它的参数的值。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
引用传递通过在函数参数声明时使用引用类型（如 \`int&\`）实现。引用实质上是变量的别名，函数形参是实参的别名而非副本。对形参的任何修改等同于直接修改实参本身，因此函数可以修改外部调用处传入的参数值，这与值传递（形参是实参的拷贝，无法修改实参）有本质区别。

**易错提醒：**
- 若不希望函数修改实参，应使用 \`const int&\` 声明常量引用。
- 引用传递与指针传递效果相似，但语法更简洁：指针需要 \`*\` 解引用，引用直接使用变量名即可。

**知识延伸：**
在 C++ 中，除了引用传递和值传递，还有指针传递（\`int*\`）方式。三者均可实现函数内外数据交换，但语义和使用场景不同：引用传递适合需要修改实参且不希望操作指针的场合。

**考点：** C++ 引用传递的机制与实参修改。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 18,
            type: "judge",
            question: `指针的大小与其所指向的变量的数据类型的单位大小相同。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
指针是用来存储内存地址的变量，其大小（占用的字节数）由CPU的寻址能力决定，与所指向的数据类型无关。在32位系统中，指针大小为4字节（可寻址 2^32 = 4GB 空间）；在64位系统中，指针大小为8字节。无论指针指向 \`char\`（1字节）、\`int\`（4字节）还是 \`double\`（8字节），该指针变量本身在同一系统下占用相同的字节数。

**易错提醒：**
- 容易将“指针所指向数据类型的大小”与“指针变量本身的大小”混淆。
- 常见误解是 \`char*\` 指针比 \`double*\` 指针小，实际上它们的指针大小完全相同。

**知识延伸：**
可以用 \`sizeof\` 运算符验证：在同一系统下执行 \`sizeof(char*)\`、\`sizeof(int*)\`、\`sizeof(double*)\`，结果必然相同。

**考点：** 指针的物理大小与系统架构的关系。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 19,
            type: "judge",
            question: `二维数组的行的大小必须在定义时确定，列的大小可以动态变化。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
C++ 中的静态二维数组（如 \`int arr[3][4]\`）在定义时，**所有维度的大小**都必须在编译时完全确定，且之后不可更改。行数和列数都是固定的，不存在“列可以动态变化”的说法。若需实现行列均可动态变化，应使用 \`std::vector<std::vector<int>>\` 或通过动态内存分配实现。

**易错提醒：**
- 不能将 C++ 静态数组与某些动态数组实现混淆，静态数组的行和列都不可以动态变化。
- 即使使用 \`vector<vector<int>>\`，也是行可动态变化，而每一行的列也需要单独管理，并非天然的二维动态数组。

**知识延伸：**
在 GESP 四级涉及的多维数组场景中，通常使用静态二维数组。若题目要求“行列动态变化”，必须选择 \`vector\` 等容器类型。

**考点：** 静态数组的内存空间分配与维度限制。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 20,
            type: "judge",
            question: `递推算法通过逐步求解当前状态和前一个或几个状态之间的关系来解决问题。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
递推算法（Recurrence Algorithm）的核心是建立“状态转移方程”，即当前状态与一个或多个前序状态之间的数学关系。已知初始状态后，通过反复应用状态转移方程，逐步推导出后续状态，直到得到目标答案。这种方法避免了重复计算，适用于具有明确递推关系的问题。

**易错提醒：**
- 递推与递归不同：递推是从已知出发正向推导，递归是从目标出发反向调用自身。
- 递推需要正确识别初始条件和状态转移关系，两者缺一不可。

**知识延伸：**
斐波那契数列是递推的经典例子：已知 f(1)=1, f(2)=1，状态转移方程为 f(n)=f(n-1)+f(n-2)（n≥3）。通过递推可高效计算 f(n)，而递归（无记忆化）会导致指数级时间复杂度。

**考点：** 递推算法的状态转移逻辑。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 21,
            type: "judge",
            question: `选择排序是稳定的排序算法。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
选择排序的工作原理是在每一轮遍历中，从剩余未排序部分选择最小（或最大）元素，与当前首位交换位置。这种“跨越式”交换会改变相等元素的相对顺序，因此选择排序是不稳定的。

举反例说明：对于序列 \`[5₁, 8, 5₂, 2, 9]\`（下标区分两个相等的 5），第一轮找到最小值 \`2\`，与位置 0 的 \`5₁\` 交换，得到 \`[2, 8, 5₂, 5₁, 9]\`。原本在前面的 \`5₁\` 现在跑到了 \`5₂\` 的后面，相对顺序被破坏。

**易错提醒：**
- 稳定性指“相等元素的相对顺序是否保持”，不是“排序结果是否正确”
- 很多学生误以为选择排序稳定，因为它“选择”而非“交换”，但恰恰是交换位置的变化导致不稳定

**知识延伸：**
稳定排序有冒泡排序、插入排序、归并排序；不稳定排序有选择排序、快速排序、堆排序。GESP 4 级常考察稳定性的判断及其原因。

**考点：** 排序算法的稳定性及其反例`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 22,
            type: "judge",
            question: `插入排序的时间复杂度总是比冒泡排序低。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
插入排序和冒泡排序的平均复杂度和最坏复杂度均为 $O(N^2)$，但两者在最好情况下的表现不同：

- **插入排序最好情况**：数组已有序，每插入一个元素只需比较一次，总复杂度 $O(N)$
- **冒泡排序标准版本**：仍需 $N-1$ 轮遍历比较，总复杂度 $O(N^2)$（除非实现了 \`swapped\` 标记优化）

因此，“总是”比冒泡低这一说法过于绝对。在某些数据分布（如完全逆序）下，两者的复杂度实际相同。插入排序在有序或接近有序的数据上确实更优，但不能推广到“总是”。

**易错提醒：**
- 混淆“平均情况相同”与“任何情况相同”
- 忽略排序算法的最好/最坏情况差异
- 未考虑带优化标记的冒泡排序在最好情况也可达 $O(N)$

**知识延伸：**
同属于 $O(N^2)$ 的排序算法还有选择排序。它们的实际运行效率受数据分布影响，选择排序则不存在最好情况优化，始终为 $O(N^2)$。

**考点：** 常见排序算法的复杂度对比与最好情况分析`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 23,
            type: "judge",
            question: `在 C++ 中，如果没有捕获到异常（没有匹配的 catch 块），程序会继续执行而不会终止。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
C++ 的异常传播机制遵循“调用栈向上传播”原则。如果 \`throw\` 抛出的异常在当前函数内没有匹配的 \`catch\` 块，该异常不会被丢弃，而是沿着调用链向调用者传播。这个过程称为 Stack Unwinding（栈展开）。

若异常一路传播到 \`main\` 函数仍无匹配的 \`catch\` 块，系统会调用 \`std::terminate()\`，后者默认调用 \`std::abort()\` 强制终止程序。程序不会继续正常执行。

\`\`\`cpp
void f() { throw 1; }  // 未捕获，向上传播
int main() {
    f();  // 异常传播到 main，仍无 catch，程序终止
    cout << "不会输出";  // 不会执行
}
\`\`\`

**易错提醒：**
- 误以为异常会自动“消化”而继续执行
- 认为 C++ 异常与 Java/Python 的异常处理机制相同（其他语言可能有默认捕获机制）
- 忽略 Stack Unwinding 过程中局部对象析构函数的调用

**知识延伸：**
可以使用 \`std::set_terminate()\` 自定义异常未捕获时的行为，但这属于进阶内容，GESP 4 级只需理解默认终止机制。

**考点：** C++ 异常传播机制与未捕获异常的处理`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 24,
            type: "judge",
            question: `求斐波那契数列第 n 项，递推法的时间复杂度为 $O(N)$。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
递推法（迭代法）求斐波那契数列的基本思路是：从已知的 $f(1) = 1$、$f(2) = 1$ 出发，使用循环依次计算 $f(3), f(4), \\ldots, f(n)$。

\`\`\`cpp
int fib(int n) {
    if (n <= 2) return 1;
    int a = 1, b = 1, c;
    for (int i = 3; i <= n; i++) {
        c = a + b;
        a = b;
        b = c;
    }
    return b;
}
\`\`\`

每一步计算只涉及常数次算术运算，时间复杂度 $O(1)$；循环执行 $n-2$ 次，因此总时间复杂度为 $O(N)$，空间复杂度为 $O(1)$。

**易错提醒：**
- 注意与递归法（带记忆化或不带）区分：不带记忆化的递归为指数级 $O(2^N)$，带记忆化为 $O(N)$
- 递推法特指“从前往后迭代计算”，不是动态规划的代名词，但确实利用了最优子结构

**知识延伸：**
斐波那契数列的计算方法还有矩阵快速幂 $O(\\log N)$ 和公式法 $O(1)$，但递推法是 GESP 4 级要求掌握的基础方法。

**考点：** 递推算法的效率分析`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 25,
            type: "judge",
            question: `GESP 4 级认证要求掌握二维数组的使用。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
根据 GESP（中国计算机学会编程能力等级认证）C++ 四级考试大纲，二维数组属于该级别的核心知识点。大纲要求考生掌握：
- 二维数组的声明与初始化
- 使用双重循环遍历二维数组
- 二维数组作为函数参数的用法
- 典型应用：矩阵运算、迷宫问题、表格数据处理等

相关真题通常以“矩阵变换”“杨辉三角”“棋盘游戏”等情境出题，要求学生具备二维数组的下标访问和边界处理能力。

**易错提醒：**
- 部分考生误以为二维数组是“五级内容”，实际上它属于四级大纲
- 二维数组在内存中是连续存储的（按行优先），这一点常被考察

**知识延伸：**
二维数组是“数组的数组”，即一维数组的每个元素又是一个一维数组。在 C++ 中，声明 \`int a[3][4]\` 表示 3 行 4 列的矩阵，访问时用 \`a[i][j]\`（i 为行索引，j 为列索引）。

**考点：** GESP 认证大纲范围与二级数组的基本概念`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        ...programmingQuestions
    ]
};
