// 2025年3月 GESP C++ 四级真题
export const paperData = {
    id: '2025-03-l4',
    title: '2025年3月 GESP C++ 四级真题',
    level: 4,
    year: 2025,
    month: 3,
    session: 9,
    timeLimit: 5400,
    backfilled: true,
    questions: [
        {
            id: 1,
            type: "single",
            question: `关于下述代码，说法错误的是（ ）。\n\`\`\`cpp\nint multiply(int x, int y);\nint main() {\n int a = 4;\n int b = 5;\n int result = multiply(a, b);\n std::cout << "The result is: " << result << std::endl;\n return 0;\n}\nint multiply(int x, int y) {\n return x * y;\n}\n\`\`\``,
            options: [
                "函数multiply的定义应该放到函数main之前。",
                "函数声明int multiply(int x, int y);中明确指定了函数multiply()的返回值为整数类型。",
                "在main函数中，函数multiply通过multiply(a, b)被调用，其中a和b是定义在main函数中的变量，它们作为实参传递给了multiply函数的形参x and y。",
                "运⾏上述代码，将输出The result is: 20。",
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：A (函数multiply的定义应该放到函数main之前。)**

**核心解析：**
本题代码展示了一个完整的 C++ 程序，包含函数声明、main 函数和函数定义。代码的完整结构如下：
1. 第 1 行：\`int multiply(int x, int y);\` —— 这是函数声明（原型），告诉编译器函数的存在和签名。
2. 第 2-9 行：\`main\` 函数定义，在第 5 行通过 \`multiply(a, b)\` 调用该函数。
3. 第 10-12 行：\`multiply\` 函数的具体实现。

C++ 编译器的工作方式是：从上到下扫描源代码，当遇到函数调用时，只需要该函数已经被**声明**过（而非必须已经**定义**）即可。本题中，\`multiply\` 在第 1 行已经声明，因此即使定义在第 10 行（main 之后），编译器仍能正确处理调用。链接阶段会找到实际的函数体。

**选项逐项分析：**
- **A 函数multiply的定义应该放到函数main之前**：✅ 正确。错误。这是本题要找的错误选项。在 C++ 中，只要在调用函数之前提供了该函数的**前向声明**（函数原型），函数定义可以放在程序的任意位置，包括 main 函数之后。
- **B 函数声明int multiply(int x, int y);中明确指定了函数multiply()的返回值为整数类型**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。声明中的 \`int\` 表示返回类型为 \`int\`。
- **C 在main函数中，函数multiply通过multiply(a, b)被调用，其中a和b是定义在main函数中的变量，它们作为实参传递给了multiply函数的形参x and y**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。这是标准的按值传递（Pass by Value）过程：实参 \`a\`、\`b\` 的值被复制给形参 \`x\`、\`y\`。
- **D 运⾏上述代码，将输出The result is: 20**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。\`multiply(4, 5)\` 执行 \`4 * 5 = 20\`，main 中输出 "The result is: 20"。

**易错提醒：**
- 混淆**声明**（Declaration）和**定义**（Definition）的概念。声明只告诉编译器"有这个东西"，定义才是实际的实现代码。只要声明在前，定义可以在任何位置。
- 初学者常误以为函数必须按"先定义后使用"的顺序书写，实际上只要在调用前声明即可。

**知识延伸：**
C++ 中分离声明与定义的做法支持将函数声明放在头文件中，定义放在源文件中，这是大型项目模块化编程的基础。同时，这种机制也使得"前向引用"成为可能。

**考点：** C++ 函数的声明与定义分离、前向声明的作用域规则。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 2,
            type: "single",
            question: `执⾏下述代码将输出（ ）。\n\`\`\`cpp\nint x = 10;\nvoid func() {\n int x = 20;\n std::cout << x;\n}\nint main() {\n func();\n std::cout << x;\n return 0;\n}\n\`\`\``,
            options: [
                "2020",
                "2010",
                "1010",
                "编译错误",
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (2010)**

**核心解析：**
本题涉及变量作用域中的**遮蔽效应**（Shadowing）。代码中存在两个名为 \`x\` 的变量：全局变量和局部变量。

逐步执行分析：
1. 第 1 行：定义全局变量 \`int x = 10;\`，整个文件范围内 \`x\` 初始化为 10。
2. 第 3-6 行：\`func()\` 函数内部定义了一个局部变量 \`int x = 20;\`。注意：这是与全局变量**同名的另一个变量**，存储在栈上，与全局变量占用不同的内存位置。
3. 第 4 行：\`std::cout << x;\` —— 在 \`func()\` 内部，当前作用域中 \`x\` 指的是局部变量（值为 20），全局变量被局部变量遮蔽，因此输出 20。
4. 执行完 \`func()\` 后回到 \`main\`。
5. 第 8 行：\`std::cout << x;\` —— 在 \`main\` 中，没有定义局部变量 \`x\`，因此 \`x\` 指的是全局变量（值为 10），输出 10。

综合输出顺序：先执行 \`func()\` 输出 20，再执行 \`main\` 输出 10，结果为 **2010**。

**选项逐项分析：**
- **A 2020**：❌ 错误。\`func()\` 确实输出 20，但 \`main\` 中的 \`x\` 是全局变量 10 而非 20。
- **B 2010**：✅ 正确。分析过程如上。
- **C 1010**：❌ 错误。全局变量 \`x\` 在 \`func()\` 调用前后都是 10，不受 \`func()\` 内部局部变量的影响。
- **D 编译错误**：❌ 错误。C++ 允许局部变量与全局变量同名，这种现象称为变量遮蔽，代码合法且能正常编译运行。

**易错提醒：**
- 误以为 \`func()\` 修改了全局变量。实际上 \`func()\` 内部的 \`x\` 是独立的局部变量，操作的是栈上的一块新内存，全局变量不受影响。
- 混淆作用域：局部变量只在它所属的大括号 \`{}\` 范围内有效，超出该范围即销毁。

**知识延伸：**
变量遮蔽是 C++ 作用域规则的重要特性。作用域从内向外搜索同名变量，因此内层会"遮住"外层。若在 \`func()\` 内需要访问全局变量，可以使用作用域解析运算符 \`::x\`（如 \`std::cout << ::x;\`）。

**考点：** 变量的作用域（全局变量与局部变量）、变量遮蔽效应。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 3,
            type: "single",
            question: `执⾏下述代码后，变量a的值为（ ）。\n\`\`\`cpp\nint a = 10;\nint* p = &a;\n*p = 20;\n\`\`\``,
            options: [
                "10",
                "20",
                "随机值",
                "编译错误",
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (20)**

**核心解析：**
本题考查指针的**解引用赋值**操作。代码分为三步：

1. \`int a = 10;\`
   - 在栈上分配一个 \`int\` 类型的变量 \`a\`，初始值为 10。

2. \`int* p = &a;\`
   - 定义一个指向 \`int\` 的指针 \`p\`。
   - \`&a\` 是取地址运算符，获取变量 \`a\` 的内存地址。
   - 将该地址赋值给 \`p\`，此时 \`p\` 指向变量 \`a\`。

3. \`*p = 20;\`
   - \`*p\` 是解引用操作符，访问指针 \`p\` 所指向的内存位置（即变量 \`a\` 所在的地址）。
   - 通过解引用，将该内存位置的值从 10 修改为 20。
   - 由于 \`p\` 指向的就是 \`a\` 的地址，因此 \`a\` 的值被修改为 20。

最终变量 \`a\` 的值为 **20**。

**选项逐项分析：**
- **A 10**：❌ 错误。虽然 \`a\` 初始值为 10，但第 3 行通过指针修改了其值。
- **B 20**：✅ 正确。\`*p = 20\` 通过解引用直接修改了 \`a\` 的值为 20。
- **C 随机值**：❌ 错误。指针 \`p\` 被明确赋值为 \`&a\`，指向的是确定的有效地址（变量 \`a\` 的地址），不存在野指针问题，结果是确定的 20。
- **D 编译错误**：❌ 错误。代码语法完全正确，可以正常编译运行。

**易错提醒：**
- 区分 \`*p\` 的两种含义：在声明中 \`int* p\` 表示 \`p\` 是指针类型；在表达式中 \`*p\` 表示解引用操作。
- \`int* p = &a;\` 中，\`p\` 存储的是地址值（一个整数），但类型是指向 \`int\` 的指针；\`*p\` 才是访问它指向的 \`int\` 值。

**知识延伸：**
指针本质上存储的是内存地址。通过指针修改内存中的值是 C/C++ 中高效的操作方式，广泛应用于数组遍历、数据结构（如链表、树）和函数参数传递等场景。指针与数组名在很多情况下可以互换使用，但指针更灵活（可以移动），数组名是常量指针。

**考点：** 指针的定义、取值运算符 \`&\`、解引用运算符 \`*\`、通过指针修改变量值。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 4,
            type: "single",
            question: `以下哪种参数传递方式可以避免拷贝大型对象？`,
            options: [
                "只能用值传递",
                "只能用引用传递",
                "只能用指针传递",
                "引用传递和指针传递均可",
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (引用传递和指针传递均可)**

**核心解析：**
参数传递方式决定了实参和形参之间的数据交互模式。不同传递方式的特点如下：

**值传递：**
- 传递实参的**副本**（复制整个对象）。
- 对于大型对象（如包含数千个元素的数组、大型结构体），复制操作需要额外的时间和内存开销。
- 传递的只是数据，无法在函数内部修改外部变量。

**引用传递：**
- 使用 \`&\` 声明引用形参，如 \`void func(T& obj)\`。
- 形参成为实参的**别名**，共享同一块内存。
- 底层只传递地址（通常 4 或 8 字节），无需复制整个对象。
- 可以在函数内部修改外部变量。

**指针传递：**
- 传递实参的**地址**，如 \`void func(T* ptr)\`。
- 形参是指向原对象的指针，访问指针指向的内容即访问原对象。
- 同样只传递地址（4 或 8 字节），避免大型对象复制。
- 可以通过解引用修改外部变量。

**性能对比：**
假设传递一个包含 10000 个元素的 \`std::vector<int>\`：
- 值传递：复制 10000 个 \`int\`，耗时约 0.04ms（假设）。
- 引用/指针传递：只传递一个地址，耗时约 0.0001ns。

**选项逐项分析：**
- **A 只能用值传递**：❌ 错误。值传递无法避免拷贝，反而是开销最大的方式。
- **B 只能用引用传递**：❌ 错误。指针传递同样可以达到避免拷贝的效果。
- **C 只能用指针传递**：❌ 错误。引用传递同样可以达到避免拷贝的效果。
- **D 引用传递和指针传递均可**：✅ 正确。两者都只传递地址（4 或 8 字节），不复制整个对象，可以有效避免大型对象的拷贝开销。

**易错提醒：**
- 引用传递和指针传递都传递地址，但语法不同：引用传递通过别名直接操作，指针传递通过解引用操作。
- 如果不需要在函数内修改实参，建议使用 \`const\` 引用传递（如 \`const T& obj\`），既避免拷贝又防止意外修改。
- 值传递并非一无是处：对于小型对象（如 \`int\`、\`double\`），值传递的开销可以忽略，且语义更清晰。

**知识延伸：**
C++11 引入了**移动语义**（Move Semantics）和**右值引用**（\`T&&\`），可以进一步优化性能。对于临时对象或即将销毁的对象，移动而非复制可以避免不必要的拷贝开销。

**考点：** C++ 参数传递机制（值传递、引用传递、指针传递）、性能优化。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 5,
            type: "single",
            question: `执⾏下述代码，将输出（ ）。\n\`\`\`cpp\nvoid swap(int a, int &b) {\n int temp = a;\n a = b;\n b = temp;\n}\nint main() {\n int x = 1, y = 2;\n swap(x, y);\n std::cout << x << y;\n return 0;\n}\n\`\`\``,
            options: [
                "12",
                "21",
                "22",
                "11",
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (11)**

**核心解析：**
本题考查**混合参数传递方式**：值传递与引用传递的区别。\`swap\` 函数的声明为 \`void swap(int a, int &b)\`，第一个参数是值传递，第二个参数是引用传递。

逐步执行分析：
1. \`main\` 中初始化 \`int x = 1, y = 2;\`
2. 调用 \`swap(x, y)\`：
   - 实参 \`x = 1\` 传递给形参 \`a\`，创建副本 \`a = 1\`（值传递）。
   - 实参 \`y = 2\` 传递给形参 \`b\`，\`b\` 成为 \`y\` 的引用/别名（引用传递）。
3. 进入 \`swap\` 函数体：
   \`\`\`cpp
   int temp = a;  // temp = 1（a 的副本）
   a = b;         // a = 2（但 a 是副本，不影响实参 x）
   b = temp;      // b = 1，通过引用修改了实参 y
   \`\`\`
4. 函数返回后：
   - 形参 \`a\`（副本）被销毁，不影响实参 \`x\`，\`x\` 仍为 1。
   - 形参 \`b\`（引用）修改了实参 \`y\`，\`y\` 变为 1。
5. 最终 \`main\` 中：\`x = 1\`，\`y = 1\`，输出 \`std::cout << x << y\` 即 \`11\`。

**选项逐项分析：**
- **A 12**：❌ 错误。这是没有调用 \`swap\` 的原始输出，忽略了引用参数的作用。
- **B 21**：❌ 错误。这是期望的交换结果，但代码中 \`a\` 是值传递，不会影响 \`x\`。
- **C 22**：❌ 错误。不符合任何参数传递逻辑。
- **D 11**：✅ 正确。只有 \`y\` 被成功交换（通过引用传递），\`x\` 保持不变，结果为 11。

**易错提醒：**
- 误以为两个参数都是引用传递，从而期待 21 的结果。必须仔细看函数签名：\`int a\`（值传递）而非 \`int &a\`（引用传递）。
- 值传递的形参是实参的副本，在函数内部修改形参不会影响实参。

**知识延伸：**
如果要实现真正的交换，需要**两个参数都使用引用传递**：
\`\`\`cpp
void swap(int& a, int& b) {
    int temp = a;
    a = b;
    b = temp;
}
\`\`\`
或者使用指针传递（C 风格）：
\`\`\`cpp
void swap(int* a, int* b) {
    int temp = *a;
    *a = *b;
    *b = temp;
}
\`\`\`

**考点：** C++ 值传递与引用传递的区别、混合参数传递的效果分析。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 6,
            type: "single",
            question: `下面的描述中，（ ）正确定义一个名为Person的结构体并正确初始化了一个Person结构体的变量p。`,
            options: [
                "struct Person { string name; int age; }; Person p(\"Yang\", 10);",
                "struct Person { string name, int age; }; Person p; p.name = \"Yang\"; p.age = 10;",
                "struct Person { string name; int age; }; Person p = { \"Yang\", 10 };",
                "struct Person { string name; int age; }; Person p = new Person(\"Yang\", 10);",
            ],
            answer: 2,
            score: 2,
            explanation: `**答案：C (struct Person { string name; int age; }; Person p = { "Yang", 10 };)**

**核心解析：**
本题考查 C++ 结构体的定义语法与初始化方式。结构体 \`Person\` 包含两个成员：\`string\` 类型的 \`name\` 和 \`int\` 类型的 \`age\`，这是一个聚合类型（Aggregate Type）。

1. 聚合类型可以使用列表初始化语法 \`Type var = { value1, value2, ... };\` 进行初始化，大括号内的值按声明顺序依次赋值给各成员。
2. 选项 C 中 \`Person p = { "Yang", 10 };\` 正确地将 "Yang" 赋给 \`name\`，将 10 赋给 \`age\`。

**选项逐项分析：**
- **A struct Person { string name; int age; }; Person p("Yang", 10);**：❌ 错误。结构体本身并未定义任何构造函数，不能使用圆括号并传入参数的方式进行初始化。圆括号初始化语法仅当结构体显式定义了相应构造函数时才合法。
- **B struct Person { string name, int age; }; Person p; p.name = "Yang"; p.age = 10;**：❌ 错误。结构体成员定义时，不同类型的成员必须分别声明，成员之间用分号分隔。该选项将 \`string name, int age\` 写在一行且用逗号分隔，这是错误的语法，编译器无法正确识别成员定义。正确的写法应为 \`string name; int age;\`。
- **C struct Person { string name; int age; }; Person p = { "Yang", 10 };**：✅ 正确。这是 C++ 聚合初始化的标准语法，大括号内的值按成员声明顺序依次初始化。
- **D struct Person { string name; int age; }; Person p = new Person("Yang", 10);**：❌ 错误。\`new\` 运算符返回的是指向动态分配对象的指针，类型为 \`Person*\`，不能直接赋值给非指针类型的变量 \`p\`。若要使用 \`new\`，应写作 \`Person* p = new Person;\` 并通过其他方式赋值，或者结构体需定义接受参数的构造函数。

**易错提醒：**
- 结构体成员定义时，每项成员声明后必须以分号结尾，不可将多个成员用逗号写在同一行（除非它们类型相同）。
- 聚合初始化要求大括号内的值与成员声明顺序一致，数量也需匹配（可省略尾部元素但不能乱序）。

**知识延伸：**
C++11 引入了统一初始化（Uniform Initialization），使用大括号 \`{}\` 可用于初始化各种类型，包括数组、结构体、向量等，这一语法比传统的构造函数调用或逐个赋值更简洁直观。

**考点：** C++ 结构体的定义语法与聚合初始化。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 7,
            type: "single",
            question: `给定如下代码， 下面描述错误的是（ ）。\n\`\`\`cpp\nstruct Person {\n std::string name;\n int age;\n struct Address {\n std::string street;\n std::string city;\n };\n Address address;\n};\n\`\`\``,
            options: [
                "结构Person内嵌套结构Address",
                "Person 有一个Address 类型的 address 成员",
                "一个Person类型的变量p的address的初始化可以写成：p.address.street = \"123 Main St\"; p.address.city = \"Anytown\";",
                "结构的嵌套可以减少命名冲突，因此可以不必控制嵌套层次",
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (结构的嵌套可以减少命名冲突，因此可以不必控制嵌套层次)**

**核心解析：**
本题考查结构体嵌套的概念与工程实践原则。代码中 \`Address\` 结构体定义在 \`Person\` 结构体内部，形成了嵌套结构。

1. 选项 A、B、C 的描述均符合代码所展现的嵌套结构语义，是正确的陈述。
2. 选项 D 虽然承认嵌套可以减少命名冲突，但其结论"不必控制嵌套层次"违背了软件工程的基本原则。嵌套层数过多会导致代码耦合度升高、可读性下降、调试困难。

**选项逐项分析：**
- **A 结构Person内嵌套结构Address**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。\`Address\` 结构体的完整定义出现在 \`Person\` 结构体的大括号内部，因此它是 \`Person\` 的嵌套结构体。
- **B Person 有一个Address 类型的 address 成员**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。\`Person\` 中声明了 \`Address address;\` 成员变量，因此每个 \`Person\` 实例都包含一个 \`Address\` 子对象。
- **C 一个Person类型的变量p的address的初始化可以写成：p.address.street = "123 Main St"; p.address.city = "Anytown";**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。访问嵌套结构成员需逐层使用点运算符 \`.\`，先通过 \`p.address\` 访问内部的 \`Address\` 对象，再通过 \`p.address.street\` 和 \`p.address.city\` 访问其成员。
- **D 结构的嵌套可以减少命名冲突，因此可以不必控制嵌套层次**：✅ 正确。错误。嵌套确实有助于封装和减少全局命名污染，但工程实践中任何设计都需要权衡。过深的嵌套层次会增加代码的复杂性，降低可维护性。通常建议嵌套层数控制在 2-3 层以内，超出此范围应考虑重新组织代码结构（如拆分为独立结构体或使用组合替代继承）。

**易错提醒：**
- 嵌套结构体的成员访问需要逐层使用点运算符，漏写任何一层都会导致编译错误。
- 结构体嵌套是一种代码组织手段，而非解决命名冲突的唯一方法，过度依赖嵌套会适得其反。

**知识延伸：**
嵌套结构体在 C++ 中是一种 namespacing 机制，其作用域规则遵循 C++ 的嵌套类规则。嵌套结构体的名字在其外层作用域中不可直接访问，必须通过 \`外层类::嵌套类\` 的形式（如 \`Person::Address\`）在类外引用。

**考点：** 结构体嵌套的语法、成员访问方式及工程设计原则。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 8,
            type: "single",
            question: `假设int arr[2][3] = {{1,2,3},{4,5,6}};，则arr[1][2]的值是（ ）。`,
            options: [
                "2",
                "3",
                "5",
                "6",
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (6)**

**核心解析：**
本题考查二维数组的内存布局与下标访问规则。

1. \`int arr[2][3]\` 定义了一个 2 行 3 列的二维数组，共 6 个元素，按行优先顺序存储在内存中。
2. C++ 数组下标从 0 开始计算，因此 \`arr[1][2]\` 表示访问第 2 行（索引为 1）的第 3 个元素（索引为 2）。
3. 根据初始化列表 \`{{1,2,3},{4,5,6}}\`，第 0 行存放 {1, 2, 3}，第 1 行存放 {4, 5, 6}。
4. 第 1 行第 2 列的元素值为 6。

**选项逐项分析：**
- **A 2**：❌ 错误。2 是第 0 行第 1 列的元素，即 \`arr[0][1]\`。
- **B 3**：❌ 错误。3 是第 0 行第 2 列的元素，即 \`arr[0][2]\`。
- **C 5**：❌ 错误。5 是第 1 行第 1 列的元素，即 \`arr[1][1]\`。
- **D 6**：✅ 正确。6 是第 1 行第 2 列的元素，即 \`arr[1][2]\`。

**易错提醒：**
- 二维数组的两个维度各自独立从 0 开始计数，切勿混淆行索引和列索引。
- \`arr[1][2]\` 读作"arr 的第 1 行第 2 列"，而非"arr 的第 2 行第 3 列"。

**知识延伸：**
二维数组在内存中是连续存储的，\`arr[2][3]\` 的 6 个元素按 \`arr[0][0], arr[0][1], arr[0][2], arr[1][0], arr[1][1], arr[1][2]\` 的顺序排列。行优先存储意味着同一行的元素在内存中相邻。

**考点：** 二维数组的定义、初始化与下标访问（从 0 开始）。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 9,
            type: "single",
            question: `下面（ ）正确定义了二维数组。`,
            options: [
                "int arr[3,4];",
                "int arr[3][4];",
                "int arr(3,4);",
                "int a[3-4];",
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (int arr[3][4];)**

**核心解析：**
本题考查 C++ 中二维数组的定义语法。

1. C++ 定义二维数组使用两个方括号，每个维度用一个方括号包围其长度。
2. \`int arr[3][4]\` 定义了一个 3 行 4 列的二维整型数组，共 12 个元素。
3. 其他选项均使用了非法的数组定义语法。

**选项逐项分析：**
- **A int arr[3,4];**：❌ 错误。C++ 数组下标只能使用方括号 \`[]\`，且方括号内必须是单个常量表达式。C++ 不支持使用逗号分隔多个维度值，这种语法会被编译器解释为逗号运算符表达式，\`3,4\` 的结果为 4，作为数组大小，实际含义与预期完全不同。
- **B int arr[3][4];**：✅ 正确。这是 C++ 定义二维数组的标准语法，第一个维度指定行数，第二个维度指定列数。
- **C int arr(3,4);**：❌ 错误。圆括号 \`()\` 在变量声明中用于函数声明或调用构造函数。数组定义不能使用圆括号语法，此处会被误解析为函数声明。
- **D int a[3-4];**：❌ 错误。数组维度必须是正整数常量表达式。虽然 \`3-4\` 是合法的算术表达式，但其结果为 -1，负数不能作为数组大小，编译会报错。即使你想表达"3 减 4"的结果，也必须确保结果为正。

**易错提醒：**
- 二维数组定义必须是 \`类型 变量名[行数][列数];\`，两个维度都不能省略方括号。
- 数组大小表达式必须能计算出正整数值，否则编译失败。

**知识延伸：**
三维或更高维数组的定义遵循相同规则，例如 \`int arr[2][3][4]\` 定义了一个 2×3×4 的三维数组。C++ 标准规定数组维度至少为 1，且必须是编译期确定的常量表达式（C++14 之后可使用 constexpr）。

**考点：** 二维数组的定义语法与维度规范。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 10,
            type: "single",
            question: `小杨正在爬楼梯，需要爬 n 阶才能到达楼顶。如果每次可以爬 1 个或 2 个台阶，下面代码采用递推算法来计算一共有多少种不同的方法可以爬到楼顶，则横线上应填写（ ）。\n\`\`\`cpp\nint f(int n) {\n if (n == 1 || n == 2) return n;\n int f1 = 1;\n int f2 = 2;\n int res = 0;\n for (int i = 3; i <= n; i++) {\n ________________________________ // 在此处填入代码\n }\n return res;\n}\n\`\`\``,
            options: [
                "res += f1+f2; f1 = f2; f2 = res;",
                "res = f1+f2; f1 = f2; f2 = res;",
                "res += f1+f2; f2 = res; f1 = f2;",
                "res = f1+f2; f2 = res; f1 = f2;",
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (res = f1+f2; f1 = f2; f2 = res;)**

**核心解析：**
本题考查递推算法（动态规划思想）求解爬楼梯问题。题目要求填写循环体内的代码，使函数正确计算爬到第 n 阶楼顶的方法数。

1. 爬楼梯问题的递推关系：设 \`f(n)\` 为爬到第 n 阶的方法数。最后一步要么跨 1 阶到达（第 n-1 阶再跨 1 步），要么跨 2 阶到达（第 n-2 阶再跨 2 步），因此 \`f(n) = f(n-1) + f(n-2)\`。
2. 代码已初始化 \`f1 = 1\`（即 \`f(1)\`）和 \`f2 = 2\`（即 \`f(2)\`），并将 \`res\` 置 0。
3. 循环从 \`i = 3\` 开始，每次迭代计算第 i 阶的方法数：
   - 计算当前结果：\`res = f1 + f2\`（即 \`f(i) = f(i-2) + f(i-1)\`）
   - 更新滑动窗口：\`f1 = f2\`（将 f(n-1) 移为新的 f(n-2)），\`f2 = res\`（将新计算的 f(n) 移为新的 f(n-1)）
4. 循环结束后 \`res\` 保存 \`f(n)\` 的值，函数返回。

**选项逐项分析：**
- **A res += f1+f2; f1 = f2; f2 = res;**：❌ 错误。\`res += f1 + f2\` 会将每次计算的结果累加到 \`res\` 上，导致最终结果远超正确答案。例如循环 3、4、5 三次后 \`res\` 会变成 \`f(3) + f(4) + f(5)\` 而非 \`f(5)\`。
- **B res = f1+f2; f1 = f2; f2 = res;**：✅ 正确。每次循环将当前计算的 \`f1+f2\` 赋值给 \`res\`（非累加），然后正确更新滑动窗口的两个变量，为下一轮迭代做准备。
- **C res += f1+f2; f2 = res; f1 = f2;**：❌ 错误。同样使用 \`res +=\`，会错误累加结果；而且赋值顺序 \`f2 = res; f1 = f2;\` 导致 \`f1\` 和 \`f2\` 最终值相同，破坏了滑动窗口逻辑。
- **D res = f1+f2; f2 = res; f1 = f2;**：❌ 错误。虽然 \`res = f1+f2\` 正确，但赋值顺序 \`f2 = res; f1 = f2;\` 会导致 \`f1\` 和 \`f2\` 都被赋值为 \`res\`，而非分别保存前一次和当前的结果值，递推逻辑错误。

**易错提醒：**
- 递推过程中变量更新必须遵循"先保存旧值，再更新"的顺序，防止数据被覆盖。
- 滑动窗口 technique 要求每次迭代后两个变量分别保存相邻的两个状态值，切不可让它们相等。
- \`res\` 在每次循环中应被覆盖而非累加，除非题目明确要求计算累计值。

**知识延伸：**
爬楼梯问题的递推公式 \`f(n) = f(n-1) + f(n-2)\` 实际上是斐波那契数列的变体。初始条件不同导致序列偏移：标准斐波那契 \`F(0)=0, F(1)=1\`，而本题中 \`f(1)=1, f(2)=2\` 相当于 \`F(2)=1, F(3)=2\`。理解这一对应关系可简化代码编写。

**考点：** 递推算法的实现（滑动窗口 technique）、斐波那契数列与爬楼梯问题的关联。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 11,
            type: "single",
            question: `给定如下算法，其时间复杂度为（ ）。\n\`\`\`cpp\nbool f(int arr[], int n, int target) {\n for (int i = 0; i < (1 << n); i++) {\n int sum = 0;\n for (int j = 0; j < n; j++) {\n if (i & (1 << j)) {\n sum += arr[j];\n }\n }\n if (sum == target) return true;\n }\n return false;\n}\n\`\`\``,
            options: [
                "O($2^n$)",
                "O(n * $2^n$)",
                "$O(N^2)$",
                "$O(N)$",
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (O(n * $2^n$))**

**核心解析：**
1. 外层循环：\`i\` 从 0 遍历到 \`(1 << n) - 1\`，即 \`2^n\` 次迭代。位运算 \`1 << n\` 表示 $2^n$。
2. 内层循环：\`j\` 从 0 到 \`n-1\`，每次迭代检查 \`i\` 的第 \`j\` 位是否为 1，共执行 $n$ 次。
3. 内层循环体中对每一项 \`arr[j]\` 的判断和累加属于常数时间操作。
4. 总基本操作次数约为外层循环次数 × 内层循环次数 = $n \\times 2^n$。

**选项逐项分析：**
- **A O($2^n$)**：❌ 错误。只考虑了外层循环的迭代次数，忽略了内层对每个 \`i\` 都要遍历全部 $n$ 个元素进行累加判断。
- **B O(n * $2^n$)**：✅ 正确。完整计数了内外两层循环的复杂度。
- **C $O(n^2)$**：❌ 错误。该复杂度适用于嵌套循环各变量独立增长的情况（如 $n$ 层循环各 $n$ 次），与本题 $2^n$ 的指数级增长不符。
- **D $O(n)$**：❌ 错误。线性复杂度无法描述这种双重嵌套结构。

**易错提醒：**
- 外层循环的终止条件 \`1 << n\` 容易被误读为线性增长，实际表示 $2^n$。
- 算法本质是枚举所有 $n$ 元素数组的子集（每个元素选或不选），子集总数为 $2^n$，每个子集求和需 $O(n)$。

**知识延伸：**
子集枚举是典型的指数级算法，在 GESP 四级中常结合位运算考察。类似地，递归生成所有子集也会产生 $O(2^n)$ 的时间复杂度。

**考点：** 位运算实现子集枚举的时间复杂度分析。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 12,
            type: "single",
            question: `下面关于排序稳定性的描述，正确的是（ ）。`,
            options: [
                "稳定性指算法的时间复杂度恒定",
                "稳定排序保证相同元素的相对顺序不变",
                "选择排序是稳定排序",
                "插入排序不是稳定排序",
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (稳定排序保证相同元素的相对顺序不变)**

**核心解析：**
排序算法的稳定性（Stability）是指：在排序过程中，如果两个元素的关键字值相等，排序后它们之间的相对顺序是否保持不变。若保持不变，则称该排序算法是稳定的；若可能改变，则是不稳定的。

**选项逐项分析：**
- **A 稳定性指算法的时间复杂度恒定**：❌ 错误。该选项将“稳定性”与“时间复杂度”概念混淆。稳定性是针对排序结果中相同关键字元素的相对位置而言，与算法运行时间的波动无关。
- **B 稳定排序保证相同元素的相对顺序不变**：✅ 正确。这是排序稳定性的标准定义。注意这里强调“相对顺序”——即初始时靠前的相同元素，排序后仍应靠前。
- **C 选择排序是稳定排序**：❌ 错误。以数组 \`{5a, 3, 5b, 1}\` 为例，选择排序在第一轮找到最小值 1 后会与位置 0 的 5a 交换，此时两个 5 的相对位置可能被颠倒，因此选择排序是不稳定的。
- **D 插入排序不是稳定排序**：❌ 错误。插入排序在标准实现下是稳定的。当新元素与已排序部分的元素比较时，通常使用 \`<=\` 而非 \`<\`，可以保证相等元素不后移，从而维持相对顺序。

**易错提醒：**
- 稳定性只讨论“相同关键字元素”的相对位置，与不同关键字元素的排序结果无关。
- 一种排序算法可以在特定实现下稳定，在另一种实现下不稳定（如冒泡排序的稳定版本使用 \`>\` 而非 \`>=\` 判断是否交换）。

**知识延伸：**
GESP 常考的稳定排序包括：冒泡排序、插入排序、归并排序；不稳定排序包括：选择排序、快速排序（取决于划分方式）、堆排序。稳定排序在需要维持多级排序条件时尤为重要。

**考点：** 排序算法的稳定性定义及其常见算法特性。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 13,
            type: "single",
            question: `对数组arr[]={5, 3, 8, 1}进⾏升序排序，执⾏第一轮冒泡排序后数组arr中的内容为（ ）。`,
            options: [
                "3, 5, 1, 8",
                "3, 1, 5, 8",
                "3, 5, 8, 1",
                "5, 3, 8, 1",
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：A (3, 5, 1, 8)**

**核心解析：**
冒泡排序的每一轮通过相邻元素两两比较，将当前未排序部分的最大值“冒泡”到序列末端。

1. **初始数组**：\`{5, 3, 8, 1}\`
2. **第一轮冒泡过程**（$n-1=3$ 次比较）：
   - **第1次比较**：比较 \`arr[0]=5\` 与 \`arr[1]=3\`，\`5 > 3\`，交换 → \`{3, 5, 8, 1}\`
   - **第2次比较**：比较 \`arr[1]=5\` 与 \`arr[2]=8\`，\`5 < 8\`，不交换 → \`{3, 5, 8, 1}\`
   - **第3次比较**：比较 \`arr[2]=8\` 与 \`arr[3]=1\`，\`8 > 1\`，交换 → \`{3, 5, 1, 8}\`
3. **第一轮结束**：最大值 8 已固定在末尾，数组变为 \`{3, 5, 1, 8}\`。

**选项逐项分析：**
- **A 3, 5, 1, 8**：✅ 正确。完整执行了一轮冒泡后的正确结果。
- **B 3, 1, 5, 8**：❌ 错误。该结果是执行两轮冒泡后的状态。第一轮结束后若再进行一轮，\`{3, 5, 1, 8}\` 会变为 \`{3, 1, 5, 8}\`。
- **C 3, 5, 8, 1**：❌ 错误。此结果仅完成了第1次和第2次比较，漏掉了关键的第3次比较（8 与 1 的交换）。
- **D 5, 3, 8, 1**：❌ 错误。该数组与初始状态完全相同，说明未执行任何交换，不符合第一轮冒泡的执行过程。

**易错提醒：**
- 冒泡排序第一轮需要 $n-1$ 次比较（本题 $4-1=3$ 次），不能遗漏最后一次 8 与 1 的比较交换。
- 注意区分“一轮冒泡”和“一次比较”：一次比较只涉及相邻两个元素，一轮冒泡包含多次比较直到最大元素就位。

**知识延伸：**
冒泡排序共需 $n-1$ 轮，时间复杂度为 $O(n^2)$。若某轮未发生任何交换，可提前终止（优化版冒泡）。GESP 中常通过模拟执行过程来考察对排序算法的理解。

**考点：** 冒泡排序（Bubble Sort）的单趟运行逻辑。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 14,
            type: "single",
            question: `运⾏下面的代码，将输出（ ）。\n\`\`\`cpp\ndouble hmean(double a, double b) {\n if (a == -b) throw std::runtime_error("Runtime error occurred.");\n return 2.0*a*b/(a+b);\n}\nint main() {\n double x = 10;\n double y = -10;\n try {\n int result = hmean(x, y);\n std::cout << "hmean: " << result << std::endl;\n } catch (const std::runtime_error& e) {\n std::cout << "Caught: " << e.what() << std::endl;\n } catch (...) {\n std::cout << "Caught an unknown exception." << std::endl;\n }\n return 0;\n}\n\`\`\``,
            options: [
                "Caught: Runtime error occurred.",
                "Caught an unknown exception.",
                "hmean: 0",
                "编译错误",
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：A (Caught: Runtime error occurred.)**

**核心解析：**
1. \`main()\` 中初始化 \`double x = 10, y = -10\`，调用 \`hmean(x, y)\` 即 \`hmean(10, -10)\`。
2. 进入 \`hmean\` 函数体：\`a = 10, b = -10\`。
3. 条件判断 \`a == -b\`：计算 \`-b\` 即 \`-(-10) = 10\`，判断 \`10 == 10\`，结果为 \`true\`。
4. 条件成立，执行 \`throw std::runtime_error("Runtime error occurred.")\`，抛出异常。
5. 异常传播至 \`main()\` 的 \`try\` 块，被 \`catch (const std::runtime_error& e)\` 捕获。
6. 输出语句 \`std::cout << "Caught: " << e.what() << std::endl;\` 执行，\`e.what()\` 返回构造时传入的字符串 \`"Runtime error occurred."\`。
7. 最终输出：\`Caught: Runtime error occurred.\`

**选项逐项分析：**
- **A Caught: Runtime error occurred.**：✅ 正确。\`std::runtime_error\` 异常被第一个匹配的 \`catch\` 子句捕获，\`e.what()\` 返回构造时的字符串。
- **B Caught an unknown exception.**：❌ 错误。该输出来自 \`catch (...)\` 子句，用于捕获除前两个 \`catch\` 匹配类型之外的异常。但 \`std::runtime_error\` 属于 \`std::exception\` 派生类，会被更具体的 \`catch (const std::runtime_error& e)\` 捕获，不会到达 \`catch (...)\`。
- **C hmean: 0**：❌ 错误。代码不会执行到 \`return\` 语句，因为在 \`return\` 之前已抛出异常并被 \`try-catch\` 捕获。即使执行了返回语句，\`int result = hmean(...)\` 也会因函数返回 \`double\` 类型而触发隐式转换，但这与实际执行流程无关。
- **D 编译错误**：❌ 错误。代码语法正确，\`std::runtime_error\` 是标准库 \`<stdexcept>\` 中声明的异常类型，\`try-catch-throw\` 语法符合 C++ 标准。

**易错提醒：**
- \`catch\` 子句按声明顺序匹配，应将具体异常类型放在前面，通用类型 \`catch (...)\` 放在最后。本题中 \`std::runtime_error\` 会被第一个 \`catch\` 捕获。
- \`std::runtime_error::what()\` 返回的是 \`const char*\` 类型的错误描述字符串，直接拼接在 \`"Caught: "\` 后输出。

**知识延伸：**
C++ 标准库提供多种异常类：\`std::exception\`（基类）、\`std::runtime_error\`、\`std::logic_error\`、\`std::bad_alloc\` 等。在 GESP 四级中，异常处理属于面向对象编程部分的重要考点。

**考点：** C++ 异常处理机制（try-catch-throw）与标准异常类。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 15,
            type: "single",
            question: `下面哪种方式不能实现将字符串"Happy Spring!"输出重定向到文件log.txt（ ）。`,
            options: [
                "freopen(\"log.txt\", \"w\", stdout); std::cout << \"Happy Spring!\" << std::endl;",
                "std::ofstream outFile(\"log.txt\"); outFile << \"Happy Spring!\"; outFile.close();",
                "std::ofstream outFile(\"log.txt\"); std::cout << \"Happy Spring!\"; outFile.close();",
                "使用 rdbuf() 将 cout 的缓冲区重定向到文件的缓冲区。",
            ],
            answer: 2,
            score: 2,
            explanation: `**答案：C (std::ofstream outFile("log.txt"); std::cout << "Happy Spring!"; outFile.close();)**

**核心解析：**
将字符串输出到文件，有两类实现思路：一是让 \`cout\`（标准输出流）指向文件；二是直接使用文件流对象写入。选项 C 虽然创建了 \`ofstream\` 文件流 \`outFile\`，但后续的 \`std::cout\` 操作并未与 \`outFile\` 建立任何关联，\`cout\` 仍指向默认的标准输出（屏幕），因此无法实现向文件 \`log.txt\` 的输出重定向。

**选项逐项分析：**
- **A freopen("log.txt", "w", stdout)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。\`freopen\` 是 C 语言标准库函数，成功调用后将 \`stdout\` 文件指针重定向到指定文件 \`"log.txt"\`，后续所有 \`cout\` 输出都会写入该文件。
- **B outFile << "Happy Spring!"**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。直接使用文件流对象 \`outFile\` 的插入运算符写入字符串到文件，这是最直接的写文件方式。虽然不是对 \`cout\` 的重定向，但同样实现了“将内容输出到文件 log.txt”的目标。
- **C std::cout << "Happy Spring!"**：✅ 正确。错误。该代码定义了 \`ofstream\` 对象 \`outFile\`，但随后使用 \`std::cout\` 进行输出。\`cout\` 仍绑定到标准输出设备（屏幕），与 \`outFile\` 没有任何关联，两者独立运作。\`outFile.close()\` 只是关闭了从未被写入的文件流。
- **D 使用 rdbuf() 将 cout 的缓冲区重定向到文件的缓冲区**：❌ 错误。该项本身符合规则，因此不是本题要选的项。正确。\`std::cout.rdbuf(file_stream.rdbuf())\` 是 C++ 中通过获取和替换流缓冲区实现重定向的标准方法，可将 \`cout\` 的输出目标切换为文件流。

**易错提醒：**
- 定义文件流对象并不等同于重定向 \`cout\`。两者是完全独立的操作：文件流对象有自己独立的缓冲区，\`cout\` 有独立的缓冲区。
- 常见的重定向手段有：\`freopen()\`（C 风格）、\`rdbuf()\`（C++ 风格）、直接使用文件流对象写入。

**知识延伸：**
\`std::ofstream\` 是 C++ 文件输入输出的一部分，属于标准库 \`<fstream>\`。创建 \`ofstream\` 对象时若指定文件名，构造函数会自动打开文件；若未指定或打开失败，可用 \`open()\` 方法手动打开。文件流使用完毕后应调用 \`close()\` 关闭，或利用析构函数自动关闭。

**考点：** C++ 输入输出重定向的各种实现方式。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 16,
            type: "judge",
            question: `函数是 C++ 中的核⼼概念，用于封装可重用的代码块。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
函数的本质作用是将一段完成特定功能的代码逻辑封装为一个具有名称的独立单元。程序可以在任意位置通过函数名调用该代码块，无需重复编写相同逻辑。这正是结构化程序设计的基本特征，也是 C++ 实现代码复用和模块化开发的核心手段。

**易错提醒：**
- 容易将“函数”与“类的方法”混淆。成员函数只是函数的一种特殊形式，普通函数同样承担封装代码的职责。
- 不能将“封装”仅理解为面向对象的特性，函数在面向过程编程中同样是封装的基本单位。

**知识延伸：**
函数的封装不仅实现代码重用，还便于维护——当需要修改某段逻辑时，只需在一个地方改动，而不是搜索整个代码库。

**考点：** C++ 函数的基础定义与作用。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 17,
            type: "judge",
            question: `在 C++ 中，函数的返回类型可以省略，默认为int。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
现代 C++（C++11 及后续标准）强制要求函数必须显式声明返回类型。合法的函数声明形式包括：
- 明确指定类型：如 \`int func()\`、\`void process()\`
- 使用 \`auto\` 推导：编译器根据 \`return\` 语句推断返回类型
- 使用 \`decltype(auto)\`：根据表达式类型推导

C++ 不支持省略返回类型后默认推断为 \`int\`。这与某些历史版本的 C 语言规则不同，切勿混淆。

**易错提醒：**
- 部分教材基于旧版 C 语言标准编写，可能提到“隐式 int”，但该规则在 C++ 中从未合法。
- C++11 引入的 \`auto\` 关键字虽然可以省略显式类型声明，但必须写出 \`auto\`，而不是完全省略。

**知识延伸：**
C++ 语法要求严格程度高于 C 语言。任何省略返回类型的写法在 C++ 编译时都会报语法错误，而非警告。

**考点：** C++ 函数定义的语法要求。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 18,
            type: "judge",
            question: `结构体的成员默认是public访问权限。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
C++ 中 \`struct\` 和 \`class\` 的唯一本质区别就在于默认访问权限：
- \`struct\` 默认成员访问权限为 \`public\`
- \`class\` 默认成员访问权限为 \`private\`

这意味着，在结构体中若不显式声明访问修饰符，成员可以直接从结构体外部访问；而在类中则必须显式声明为 \`public\` 才能在类外访问。

**易错提醒：**
- 容易误以为 struct 和 class 功能完全相同，只是名称不同。实际上默认访问权限这一差异会影响代码行为。
- 结构体同样可以包含构造函数、析构函数、成员函数，也支持继承，其默认继承方式同样是 \`public\`。

**知识延伸：**
在 GESP 四级考核中，struct 与 class 的选择主要考虑语义：若需要数据聚合且默认公开访问，用 struct；若需要封装和隐藏数据，用 class。

**考点：** C++ 结构体与类的默认访问权限区别。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 19,
            type: "judge",
            question: `假设整数数组arr[4]= {0, 1, 2, 3};的第一个元素在内存中的地址为0x7ffee4065820, 经过int* p = arr; p += 1;后，指针p的值是 1 。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. \`int* p = arr;\` 将指针 \`p\` 初始化为数组首元素的地址（值为 \`0x7ffee4065820\`）。
2. \`p += 1;\` 执行的是指针算术运算，将 \`p\` 向前移动一个 \`int\` 单位的存储空间。
3. 在典型系统中 \`sizeof(int) = 4\` 字节，因此 \`p\` 的新值为 \`0x7ffee4065820 + 4 = 0x7ffee4065824\`，这是数组第二个元素 \`arr[1]\` 的地址。
4. 题干说“指针 p 的值是 1”，这是将指针值与所指向的元素内容混淆了。指针的值始终是一个内存地址，而非该地址中存储的数据。

**易错提醒：**
- 指针算术运算的单位是“指针指向类型的元素个数”，而非字节数。\`p += 1\` 表示移动一个 \`int\`，而非一个字节。
- 访问 \`p\` 指向的内容应使用解引用操作符 \`*p\`，其值为 \`arr[1]\`，即整数 \`1\`。但 \`p\` 本身的值是地址，不是 \`1\`。

**知识延伸：**
不同类型的指针在算术运算中移动的单位不同：\`int*\` 移动 4 字节，\`double*\` 移动 8 字节，\`char*\` 移动 1 字节。编译器根据指针类型自动调整偏移量。

**考点：** C++ 指针算术运算及其物理意义。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 20,
            type: "judge",
            question: `二维数组作为函数参数时，必须显式指定所有维度的大小。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
C++ 规定：二维数组作为函数参数时，**第一维（行数）可以省略**，但**第二维（列数）必须显式指定**。

原因在于内存布局。二维数组在内存中是连续的一维空间，访问 \`arr[i][j]\` 时，编译器需要计算偏移量 \`i × 列数 + j\` 来定位元素。如果不指定列数，就无法确定每行占用的空间，从而无法完成地址计算。

合法示例：
\`\`\`cpp
void print(int arr[][3], int rows) { ... }  // 列数 3 必须写，行数可省略
\`\`\`

非法示例：
\`\`\`cpp
void print(int arr[][], int rows, int cols) { ... }  // 编译错误
\`\`\`

**易错提醒：**
- 不要将“省略维度”与“完全不写”混淆。函数参数中 \`int arr[][]\` 是不合法的，必须至少保留一对方括号并填入列数。
- 行数虽然在声明中可以省略，但通常需要额外参数（如 \`rows\`）传递给函数，以便在函数体内控制循环范围。

**知识延伸：**
三维或更高维数组作为参数时，除了第一维可以省略外，其余各维的大小都必须显式指定，规则一致。

**考点：** 二维数组作为函数参数的语法规则。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 21,
            type: "judge",
            question: `递推是一种通过已知的初始值和递推公式，逐步求解目标值的算法。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
递推（Recurrence）是解决数学和算法问题的一种基本方法。它通过定义一个初始状态（基准情况），以及一个将当前状态与之前状态关联起来的规则（递推公式），来逐步计算出更复杂或更大规模情况的结果。例如，斐波那契数列 F(n) = F(n-1) + F(n-2)，其中 F(0)=0, F(1)=1 就是典型的递推算法。

**易错提醒：**
- 递推必须包含明确的初始值，缺少初始值的递推公式无法求解。
- 递推与递归不同：递推是自底向上（从已知推未知），递归是自顶向下（从未知逐步拆解为已知）。

**知识延伸：**
递推是动态规划的基础，许多 GESP 四级难度的动态规划题目（如爬楼梯、斐波那契数列计数）都依赖递推思想建立状态转移方程。

**考点：** 递推算法的核心定义与基本要素`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 22,
            type: "judge",
            question: `考虑最坏情况下冒泡排序算法的时间复杂度， T(n) 为待排序数字的数目为 n 的复杂度，则其递推关系式为 T(n) = T($n-1$)+($n-1$)，T(1) = 0。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. 在最坏情况下（数据逆序），冒泡排序每趟需要进行 $n-i$ 次比较（$i$ 从 0 开始计数）。
2. 总比较次数 = $(n-1) + (n-2) + \\cdots + 2 + 1 = \\frac{n(n-1)}{2}$。
3. 对应的递推关系式 $T(n) = T(n-1) + (n-1)$ 准确地描述了这一过程：$T(n)$ 是对 $n$ 个数排序所需的比较次数，它等于对前 $n-1$ 个数排序所需的次数加上处理最后一个元素所需的 $n-1$ 次比较。基准情况 $T(1) = 0$ 表示单个元素无需比较。

**易错提醒：**
- 冒泡排序的递推式 $T(n) = T(n-1) + (n-1)$ 描述的是比较次数，不是交换次数。
- 复杂度计算结果为 $O(n^2)$，与直接推导的 $\\frac{n(n-1)}{2}$ 一致。

**知识延伸：**
类似地，快速排序的递推关系为 $T(n) = T(k) + T(n-k-1) + O(n)$，其中 $k$ 是划分子问题的大小。这体现了分治思想与递推的结合。

**考点：** 冒泡排序的时间复杂度推导与递推关系`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 23,
            type: "judge",
            question: `插入排序在最好情况（已有序）下的时间复杂度是 $O(N^2)$。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
插入排序在最好情况（数组已有序）下的时间复杂度是 $O(N)$，而非 $O(N^2)$。原因如下：当数组已有序时，对于每个待插入的元素，内层 while 循环只需进行 1 次比较（与前一个元素比较后立即判定无需移动）即可确定插入位置。因此总比较次数为 $n-1$ 次，时间复杂度为线性 $O(N)$。

**易错提醒：**
- 插入排序的 $O(N^2)$ 是平均情况和最坏情况（逆序）的复杂度，最好情况是 $O(N)$。
- 不要混淆“算法名称”与“具体复杂度”，同一个算法在不同输入下复杂度可能不同。

**知识延伸：**
选择排序无论最好还是最坏情况都是 $O(N^2)$，因为每趟都要在剩余未排序区间查找最小值，无法利用“已有序”这一条件优化。

**考点：** 插入排序的时间复杂度分析（最好、最坏、平均情况）`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 24,
            type: "judge",
            question: `对数组arr[]={4, 3, 1, 5, 2}进⾏升序排序，执⾏第一轮选择排序后数组 arr 中的内容是{1, 4, 3, 5, 2}。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
对数组 \`arr[] = {4, 3, 1, 5, 2}\` 执行第一轮选择排序的过程：
1. 初始数组：\`{4, 3, 1, 5, 2}\`
2. 在 \`[0, 4]\` 范围内寻找最小值，找到最小值为 1（下标为 2）
3. 将最小值 1 与当前位置 arr[0] 的元素 4 交换
4. 第一轮结束后的数组应为：\`{1, 3, 4, 5, 2}\`

题目所述结果 \`{1, 4, 3, 5, 2}\` 中，3 和 4 的位置与简单选择排序逻辑不符。正确的第一轮结果将最小值 1 换到首位后，剩余元素应保持相对顺序不变。

**易错提醒：**
- 选择排序每轮只做一次交换（将最小值与当前位置交换），不是多次交换。
- 交换后，剩余未排序部分的元素相对顺序保持不变（因为是整体交换）。

**知识延伸：**
选择排序无论初始顺序如何，每轮都进行 $n-i$ 次比较，时间复杂度恒定为 $O(N^2)$，无法像插入排序那样利用“部分有序”的特点进行优化。

**考点：** 选择排序（Selection Sort）的算法过程模拟`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 25,
            type: "judge",
            question: `未捕获异常会调用 std::terminate 终⽌程序。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
当程序抛出异常且没有对应的 \`catch\` 块能捕获它时，异常会“逃逸”出 \`main\` 函数。按照 C++ 标准，这会导致运行库调用全局函数 \`std::terminate()\`，该函数默认会调用 \`std::abort()\` 立即异常终结程序的运行。这是一个严重错误状态，程序无法恢复。

**易错提醒：**
- 异常必须被捕获，否则程序会终止。编译器不会自动“消化”未捕获的异常。
- \`std::terminate()\` 调用的默认行为是 \`std::abort()\`，但程序员可以通过 \`std::set_terminate()\` 自定义终止函数。

**知识延伸：**
C++ 异常处理机制中，即使 \`catch\` 块重新抛出异常（\`throw;\`），若外层仍无捕获者，同样会触发 \`std::terminate()\`。因此异常传播路径上必须有对应的处理逻辑。

**考点：** C++ 异常处理生命周期与未捕获异常的后果`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
      id: 26,
      type: 'programming',
      samples: [
        { input: `3 5
.....
.#..#
.....`, output: `11` }
      ],
      question: `
# [GESP202503 四级] 荒地开垦

## 题目描述

小杨有一大片荒地，可以表示为一个 $n$ 行 $m$ 列的网格图。

小杨想要开垦这块荒地，但荒地中一些位置存在杂物，对于一块不存在杂物的荒地，该荒地可以开垦当且仅当其上下左右四个方向相邻的格子均不存在杂物。

小杨可以选择至多一个位置，清除该位置的杂物，移除杂物后该位置变为荒地。小杨想知道在清除至多一个位置的杂物的情况下，最多能够开垦多少块荒地。

## 输入格式

第一行包含两个正整数 $n, m$，含义如题面所示。

之后 $n$ 行，每行包含一个长度为 $m$ 且仅包含字符 \`.\` 和 \`#\` 的字符串。如果为 \`.\`，代表该位置为荒地；如果为 \`#\`，代表该位置为杂物。

## 输出格式

输出一个整数，代表在清除至多一个位置的杂物的情况下，最多能够开垦的荒地块数。
`,
      score: 25,
      explanation: `**解析：**
本题考察对二维网格的模拟、判断与优化。
1.  **开垦条件**：一个格子 $(i, j)$ 可开垦，需满足该位置及其上下左右四个邻居均为 \`.\`（荒地）。
2.  **初始状态**：先统计初始状态下可开垦的格子总数。
3.  **移除杂物的影响**：
    - 如果我们将位置 $(r, c)$ 的 \`#\` 移除，那么该位置 $(r, c)$ 本身可能变得可开垦。
    - 此外，$(r, c)$ 的上下左右邻居也可能因为障碍物的移除而变得可开垦。
4.  **枚举优化**：
    - 遍历所有的 \`#\`，计算移除该点后带来的“新增”可开垦数量。
    - 新增数量 = 移除后 $(r, c)$ 及其邻居中新变更为可开垦的格子数。
5.  **结果**：初始数量 + 最大新增数量。

**核心逻辑提示：**
\`\`\`cpp
// 判断 (r, c) 在当前 grid 下是否符合开垦条件
bool can(int r, int c, const vector<string>& grid) {
    if (grid[r][c] == '#') return false;
    for (int i = 0; i < 4; i++) {
        int nr = r + dr[i], nc = c + dc[i];
        if (nr >= 0 && nr < n && nc >= 0 && nc < m && grid[nr][nc] == '#') return false;
    }
    return true;
}
\`\`\``,
      tags: ["编程题", "GESP4级"],
      template: "#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n\n    // 在此编写代码\n\n    return 0;\n}",
      answer: '',
    },
        {
      id: 27,
      type: 'programming',
      samples: [
        { input: `3 4
1 2 1 0
2 4 2 1
0 3 3 0`, output: `2` }
      ],
      question: `
# [GESP202503 四级] 二阶矩阵

## 题目描述

小 A 有一个 $n$ 行 $m$ 列的矩阵 $A$。

小 A 认为一个 $2 \\times 2$ 的矩阵 $D$ 是好的，当且仅当 $D_{1,1} \\times D_{2,2} = D_{1,2} \\times D_{2,1}$。其中 $D_{i,j}$ 表示矩阵 $D$ 的第 $i$ 行第 $j$ 列的元素。

小 A 想知道 $A$ 中有多少个好的子矩阵。

## 输入格式

第一行，两个正整数 $n, m$。

接下来 $n$ 行，每行 $m$ 个整数 $A_{i,1}, A_{i,2}, \\ldots, A_{i,m}$。

## 输出格式

一行，一个整数，表示 $A$ 中好的子矩阵的数量。
`,
      score: 25,
      explanation: `**解析：**
本题考察对二维数组子矩阵的性质判断。
1.  **二阶子矩阵定义**：在 $n \times m$ 的矩阵中，任意一个 $2 \times 2$ 的连续区域。
2.  **“好”的条件**：左上 $\times$ 右下 = 右上 $\times$ 左下，即 $D_{1,1} \times D_{2,2} = D_{1,2} \times D_{2,1}$。
3.  **枚举逻辑**：
    - 遍历行索引 $i$ 从 0 到 $n-2$。
    - 遍历列索引 $j$ 从 0 到 $m-2$。
    - 提取 $D_{1,1} = A[i][j], D_{1,2} = A[i][j+1], D_{2,1} = A[i+1][j], D_{2,2} = A[i+1][j+1]$。
    - 进行乘积比较，若满足条件则计数。

**核心逻辑提示：**
\`\`\`cpp
long long count = 0;
for (int i = 0; i < n - 1; i++) {
    for (int j = 0; j < m - 1; j++) {
        if ((long long)a[i][j] * a[i+1][j+1] == (long long)a[i][j+1] * a[i+1][j]) {
            count++;
        }
    }
}
\`\`\``,
      tags: ["编程题", "GESP4级"],
      template: "#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n\n    // 在此编写代码\n\n    return 0;\n}",
      answer: '',
    }
    ]
};
