// 2024年12月 GESP C++ 四级真题

const programmingQuestions = [
    {
      id: 26,
      type: 'programming',
      samples: [
        { input: `5`, output: `1 2 3 6 7` },
        { input: `8`, output: `1 2 3 6 7 12 13 20` }
      ],
      question: `
# [GESP202412 四级] Recamán

## 题目描述

小杨最近发现了有趣的 Recamán 数列，这个数列是这样生成的：

- 数列的第一项 $a_1$ 是 $1$；
- 如果 $a_{k-1}-k$ 是正整数并且没有在数列中出现过，那么数列的第 $k$ 项 $a_k$ 为 $a_{k-1}-k$，否则为 $a_{k-1}+k$。

小杨想知道 Recamán 数列的前 $n$ 项从小到大排序后的结果。手动计算非常困难，小杨希望你能帮他解决这个问题。

## 输入格式

第一行，一个正整数 $n$。

## 输出格式

一行，$n$ 个空格分隔的整数，表示 Recamán 数列的前 $n$ 项从小到大排序后的结果。
`,
      score: 25,
      explanation: `**解析：**
本题考察递推算法的设计与状态记录。
1.  **递推生成**：严格按照题目给出的 Recamán 数列定义进行计算。
    - $a_k = a_{k-1} - k$ （如果结果为正且未出现过）
    - $a_k = a_{k-1} + k$ （否则）
2.  **去重判断**：为了快速检查一个数是否“在数列中出现过”，可以使用布尔数组（由于 $n \le 10^5$，数值可能达到 $7 \times 10^5$，布尔数组需开够空间）或 \`std::set\`。
3.  **排序输出**：题目要求输出前 $n$ 项“从小到大排序”后的结果，因此生成完 $n$ 个数后需要使用 \`std::sort\`。

**核心逻辑提示：**
\`\`\`cpp
for (int k = 1; k < n; k++) {
    long long next = a[k-1] - k;
    if (next > 0 && !vis[next]) a[k] = next;
    else a[k] = a[k-1] + k;
    if (a[k] < MAX_VAL) vis[a[k]] = true;
}
sort(a.begin(), a.end());
\`\`\``,
      tags: ["编程题", "递推", "排序", "哈希/布尔数组"],
      template: "#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    int n;\n    cin >> n;\n    // 在此编写代码\n    return 0;\n}",
      referenceCode: "#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <set>\nusing namespace std;\nconst int MAX_VAL = 10000000;\nbool vis[MAX_VAL];\nint main() {\n    int n; cin >> n;\n    vector<long long> a(n);\n    a[0] = 0;\n    vis[0] = true;\n    for (int k = 1; k < n; k++) {\n        long long prev = a[k-1];\n        long long next = prev-k;\n        if (next > 0 && !vis[next]) {\n            a[k] = next;\n        } else {\n            a[k] = prev+k;\n        }\n        if (a[k] < MAX_VAL) vis[a[k]] = true;\n    }\n    sort(a.begin(), a.end());\n    for (int i = 0; i < n; i++) cout << a[i] << (i == n-1 ? \"\" : \" \");\n    cout << endl;\n    return 0;\n}",
      answer: '',
    },
    {
      id: 27,
      type: 'programming',
      samples: [
        { input: `3
3
aa
ac
de
2
aac
bc
1
gesp`, output: `1
0
0` }
      ],
      question: `
# [GESP202412 四级] 字符排序

## 题目描述

小杨有 $n$ 个仅包含小写字母的字符串 $s_1,s_2,\\ldots,s_n$，小杨想将这些字符串按一定顺序排列后拼接到一起构成字符串 $t$。小杨希望最后构成的字符串 $t$ 满足：

- 假设 $t_i$ 为字符串 $t$ 的第 $i$ 个字符，对于所有的 $j\\lt i$ 均有 $t_j\\le t_i$。两个字符的大小关系与其在字母表中的顺序一致，例如 $\\texttt{e}\\lt \\texttt{g}\\lt \\texttt{p} \\lt \\texttt{s}$。

小杨想知道是否存在满足条件的字符串排列顺序。

## 输入格式

第一行包含一个正整数 $T$，代表测试数据组数。

对于每组测试数据，第一行包含一个正整数 $n$，含义如题面所示。

之后 $n$ 行，每行包含一个字符串 $s_i$。

## 输出格式

对于每组测试数据，如果存在满足条件的排列顺序，输出（一行一个）$\\texttt{1}$，否则输出（一行一个） $\\texttt{0}$。
`,
      score: 25,
      explanation: `**解析：**
本题考察贪心策略与字符串处理。
1.  **内部有序性**：若拼接后的总字符串 $t$ 是不降序列（如 "aaaccde"），那么构成它的每一个子字符串内部也必须是不降序列。如果任何一个子串内部存在逆序（如 "ba"），则无论怎么排列，拼接后总会存在 $t_j > t_i$。
2.  **贪心排序**：如果所有单个字符串内部都有序，我们需要决定它们的拼接顺序。显然，应该按照首字母的大小进行升序排列。
3.  **最终验证**：将排序后的字符串拼接，再做一次全局不降检测。

**核心逻辑提示：**
\`\`\`cpp
// 检查单个字符串内部是否非递减
bool isNonDescending(string s) {
    for (int i = 1; i < s.size(); i++) if (s[i] < s[i-1]) return false;
    return true;
}
// 贪心排序：按首字母排序后拼接
sort(s.begin(), s.end());
\`\`\``,
      tags: ["编程题", "字符串", "排序", "贪心"],
      template: "#include <iostream>\n#include <string>\n#include <vector>\n\nusing namespace std;\n\nint main() {\n    int T;\n    cin >> T;\n    // 在此编写代码\n    return 0;\n}",
      referenceCode: "#include <iostream>\n#include <string>\n#include <vector>\n#include <algorithm>\nusing namespace std;\nbool isNonDescending(string s) {\n    for (int i = 1; i < s.size(); i++) if (s[i] < s[i-1]) return false;\n    return true;\n}\nvoid solve() {\n    int n; cin >> n;\n    vector<string> s(n);\n    bool possible = true;\n    for (int i = 0; i < n; i++) {\n        cin >> s[i];\n        if (!isNonDescending(s[i])) possible = false;\n    }\n    if (!possible) { cout << 0 << endl; return; }\n    sort(s.begin(), s.end());\n    string t = \"\";\n    for (auto x : s) t += x;\n    if (isNonDescending(t)) cout << 1 << endl;\n    else cout << 0 << endl;\n}\nint main() {\n    int T; cin >> T;\n    while (T--) solve();\n    return 0;\n}",
      answer: '',
    }
];

export const paperData = {
    id: '2024-12-l4',
    title: '2024年12月 GESP C++ 四级真题',
    level: 4,
    year: 2024,
    month: 12,
    session: 4,
    timeLimit: 5400,
    backfilled: true,
    questions: [
        {
            id: 1,
            type: "single",
            question: `下面的语句中，（ ）正确定义了一个计算浮点数 x 的平方 (x^2) 的函数, 并成功调用该函数。`,
            options: [
                "float square(float x) { return x * x; } float area = square(2.0);",
                "void square(float x) { return x * x; } float area = square(2.0);",
                "float square(x) { return x * x; } float area = square(2.0);",
                "float square(float x) { x * x; } float area = square(2.0);"
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：A (float square(float x) { return x * x; } float area = square(2.0);)**

**选项逐项分析：**
- **A**：✅ 正确。该选项完整包含了函数定义的三个要素：返回类型（float）、参数列表（float x）和返回语句（return x * x），且调用时参数类型匹配。
- **B**：❌ 错误。函数定义为 \`void\` 类型却尝试 \`return\` 一个值，且调用处尝试将 \`void\` 返回值赋给 \`float\` 变量，会导致编译错误。
- **C**：❌ 错误。C++ 是强类型语言，定义函数参数时必须指定类型（应为 \`float x\` 而非仅 \`x\`）。
- **D**：❌ 错误。函数体内缺少 \`return\` 关键字，计算结果 \`x * x\` 只是一个表达式，没有被返回给调用者。

**考点：** C++ 函数定义的语法规范（返回类型、参数类型、return 语句）。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 2,
            type: "single",
            question: `下面代码的描述中，正确的是（ ）。\n\`\`\`cpp\nint main() {\n int a = 10, b = 20;\n int *p1 = &a, *p2 = &b;\n *p1 = *p2;\n cout << a << " " << b << endl;\n return 0;\n}\n\`\`\``,
            options: ["输出 10 20", "输出 20 20", "输出 10 10", "输出 20 10"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (输出 20 20)**

**选项逐项分析：**
- **A 输出 10 20**：❌ 错误。忽略了 \`*p1 = *p2;\` 这一行对变量 \`a\` 值的修改作用。
- **B 输出 20 20**：✅ 正确。程序执行过程：
  1. \`p1\` 指向 \`a\` (10)，\`p2\` 指向 \`b\` (20)。
  2. \`*p1 = *p2\` 表示将 \`p2\` 指向的值（即 \`b\` 的 20）赋给 \`p1\` 指向的变量（即 \`a\`）。
  3. 执行后 \`a\` 的值变为 20，\`b\` 的值保持 20 不变。
  4. \`cout << a << " " << b\` 输出 20 20。
- **C 输出 10 10**：❌ 错误。误以为是 \`*p2 = *p1\`，即将 \`a\` 的值赋给了 \`b\`。
- **D 输出 20 10**：❌ 错误。误以为交换了两个指针指向的变量，但实际上只是修改了 \`a\` 的内容。

**考点：** 指针解引用（Dereference）与间接赋值运算。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 3,
            type: "single",
            question: `在 C++ 中，下列关于引用的说法，错误的是（ ）。`,
            options: [
                "引用必须在定义时初始化。",
                "引用一旦绑定到一个变量，就不能再绑定到另一个变量。",
                "引用可以指向空值（NULL）。",
                "引用的本质是变量的别名。"
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：C (引用可以指向空值（NULL）。)**

**选项逐项分析：**
- **A 引用必须在定义时初始化**：❌ 错误（描述正确）。引用在 C++ 中是变量的别名，必须在声明时就指定它是哪个变量的别名。
- **B 引用一旦绑定...不能再绑定**：❌ 错误（描述正确）。引用的绑定关系是永久性的，后续对引用的赋值只会改变被引用对象的值。
- **C 引用可以指向空值**：✅ 正确（描述错误）。C++ 标准规定引用必须绑定到有效的对象，不存在所谓的“空引用”。这与指针有显著区别（指针可以为 \`nullptr\`）。
- **D 引用的本质是变量的别名**：❌ 错误（描述正确）。这是引用的核心定义。

**考点：** C++ 引用的基本特性及其与指针的区别。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 4,
            type: "single",
            question: `有关下面代码，说法错误的是（ ）。\n\`\`\`cpp\nvoid swap(int &a, int &b) {\n int temp = a;\n a = b;\n b = temp;\n}\n\`\`\``,
            options: [
                "该函数使用引用传递参数。",
                "调用该函数后，实参的值会发生交换。",
                "函数内的修改直接影响外部变量。",
                "该函数无法通过编译，因为引用不能作为参数。"
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：D (该函数无法通过编译，因为引用不能作为参数。)**

**选项逐项分析：**
- **A 该函数使用引用传递参数**：❌ 错误（描述正确）。参数类型 \`int &a\` 明确使用了引用传递。
- **B 调用该函数后...会发生交换**：❌ 错误（描述正确）。由于使用了引用传递，函数内部对 \`a\` 和 \`b\` 的修改会直接作用于实参。
- **C 函数内的修改直接影响外部变量**：❌ 错误（描述正确）。这是引用传递的核心优势。
- **D 该函数无法通过编译...**：✅ 正确（描述错误）。引用不仅可以作为参数，还是 C++ 实现“按引用传递”的标准方式。

**考点：** 引用作为函数参数的使用及其对实参的影响。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 5,
            type: "single",
            question: `下列关于 C++ 中 static 关键字的说法，错误的是（ ）。`,
            options: [
                "静态局部变量只初始化一次。",
                "静态全局变量仅在定义它的文件中可见。",
                "类中的静态成员函数没有 this 指针。",
                "静态成员变量不能在类外初始化。"
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (静态成员变量不能在类外初始化。)**

**选项逐项分析：**
- **A 静态局部变量只初始化一次**：❌ 错误（描述正确）。静态局部变量存储在静态存储区，其生命周期贯穿程序运行，且只在第一次执行到定义处时初始化。
- **B 静态全局变量仅在定义它的文件中可见**：❌ 错误（描述正确）。\`static\` 修饰全局变量会限制其链接属性，使其具有文件作用域。
- **C 类中的静态成员函数没有 this 指针**：❌ 错误（描述正确）。静态成员函数属于类本身而非具体对象，因此无法访问非静态成员，也没有 \`this\` 指针。
- **D 静态成员变量不能在类外初始化**：✅ 正确（描述错误）。静态成员变量通常必须在类外进行定义和初始化（除非是 \`const static int\` 等特殊情况）。

**考点：** static 关键字在局部变量、全局变量和类成员中的不同语义。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 6,
            type: "single",
            question: `下列关于递归函数的说法，正确的是（ ）。`,
            options: [
                "递归函数不需要终止条件。",
                "递归调用不会消耗栈空间。",
                "任何递归都可以改写为循环。",
                "递归深度过大可能会导致栈溢出。"
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (递归深度过大可能会导致栈溢出。)**

**选项逐项分析：**
- **A 递归函数不需要终止条件**：❌ 错误。没有终止条件的递归会导致无限调用，最终引发栈溢出。
- **B 递归调用不会消耗栈空间**：❌ 错误。每一次递归调用都需要在系统栈上保存当前函数的现场（参数、返回地址等），因此会消耗栈空间。
- **C 任何递归都可以改写为循环**：❌ 错误。虽然理论上大部分递归可以用栈+循环模拟，但有些高度复杂的递归结构转换起来极具挑战，且该说法过于绝对。
- **D 递归深度过大...**：✅ 正确。系统栈的大小是有限的，如果递归深度超过限制，就会产生著名的 \`Stack Overflow\` 错误。

**考点：** 递归函数的执行机制、栈空间开销及潜在风险。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 7,
            type: "single",
            question: `选择排序在最坏情况下的比较次数是（ ）。`,
            options: ["$O(log n)$", "$O(N)$", "$O(n log n)$", "$O(N^2)$"],
            answer: 3,
            score: 2,
            explanation: `**答案：D ($O(N^2)$)**

**选项逐项分析：**
- **A $O(log n)$**：❌ 错误。选择排序不具有对数级的复杂度特征。
- **B $O(N)$**：❌ 错误。即使数组已经有序，选择排序仍需通过两层循环来确定每一位的最小值，无法达到线性复杂度。
- **C $O(n log n)$**：❌ 错误。这是快速排序或归并排序的平均复杂度，不适用于选择排序。
- **D $O(N^2)$**：✅ 正确。选择排序的工作原理是：第一轮比较 $N-1$ 次，第二轮 $N-2$ 次... 总比较次数为 $N(N-1)/2$，因此无论数据初始状态如何，时间复杂度始终为 $O(N^2)$。

**考点：** 选择排序的时间复杂度分析。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 8,
            type: "single",
            question: `下列排序算法中，不稳定的是（ ）。`,
            options: ["冒泡排序", "插入排序", "归并排序", "快速排序"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (快速排序)**

**选项逐项分析：**
- **A 冒泡排序**：❌ 错误（它是稳定的）。冒泡排序通过相邻元素交换，相等元素不会错位。
- **B 插入排序**：❌ 错误（它是稳定的）。插入排序将元素移到已排序序列时，相等元素会保持原有相对位置。
- **C 归并排序**：❌ 错误（它是稳定的）。在 merge 过程中，可以保证相等元素的顺序不改变。
- **D 快速排序**：✅ 正确（它是不稳定的）。在快速排序的划分（Partition）过程中，为了效率，基准元素（Pivot）可能会与远处的元素交换，从而打乱相等元素的相对顺序。

**考点：** 常见排序算法的稳定性（Stability）特性。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 9,
            type: "single",
            question: `在 C++ 中，\`std::sort\` 函数默认采用的排序方法（底层实现）通常不包括（ ）。`,
            options: ["插入排序", "快速排序", "堆排序", "冒泡排序"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (冒泡排序)**

**选项逐项分析：**
- **A 插入排序**：❌ 错误（它是包含的）。当数据量非常小时，\`std::sort\` 会切换到插入排序以提高效率。
- **B 快速排序**：❌ 错误（它是包含的）。这是 \`std::sort\` 最核心的底层逻辑（内省排序的前半部分）。
- **C 堆排序**：❌ 错误（它是包含的）。当递归深度过深时，为了保证最坏情况下的 $O(N log N)$，会切换到堆排序。
- **D 冒泡排序**：✅ 正确。\`std::sort\` 追求高效率，而冒泡排序的性能相对较低，从未作为标准库排序算法的底层实现。

**考点：** C++ 标准库 \`std::sort\` 的混合排序策略（IntroSort）。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 10,
            type: "single",
            question: `关于 C++ 中的 \`try-catch\` 异常处理，下列说法正确的是（ ）。`,
            options: [
                "一个 try 块只能对应一个 catch 块。",
                "catch 块必须出现在 try 块之后。",
                "任何类型的异常都可以由 `catch(...)` 捕获并自动修复错误。",
                "如果没有对应的 catch 块，异常会被静默忽略。"
            ],
            answer: 2,
            score: 2,
            explanation: `**答案：B (catch 块必须出现在 try 块之后。)**

**选项逐项分析：**
- **A 一个 try 块只能对应一个 catch 块**：❌ 错误。一个 \`try\` 块可以后接多个 \`catch\` 块，用于捕获不同类型的异常。
- **B catch 块必须出现在 try 块之后**：✅ 正确。异常处理的语法结构要求 \`try\` 先界定可能出错的范围，\`catch\` 紧随其后处理错误。
- **C catch(...) 捕获并自动修复**：❌ 错误。\`catch(...)\` 虽能捕获所有类型的异常，但它只是一层“兜底”，并不能“自动修复”逻辑错误。
- **D 异常会被静默忽略**：❌ 错误。如果没有匹配的 \`catch\` 块，异常会继续向外层抛出（Unwinding），如果最终仍未捕获，程序会调用 \`std::terminate\` 终止。

**考点：** C++ 异常处理机制（try-catch 语法及执行流）。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 11,
            type: "single",
            question: `二维数组 \`int a[3][4]\` 的存储方式是（ ）。`,
            options: ["按列存储", "按行存储", "随机存储", "树形存储"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (按行存储)**

**选项逐项分析：**
- **A 按列存储**：❌ 错误。这是某些语言（如 Fortran 或 MATLAB）采用的存储方式，但在 C/C++ 中不是。
- **B 按行存储**：✅ 正确。C/C++ 规定二维数组在内存中按行主序（Row-Major Order）连续排列，即先存完第一行，再存第二行。
- **C 随机存储**：❌ 错误。数组在内存中必须是连续存储的，以便通过下标计算偏移量。
- **D 树形存储**：❌ 错误。数组是线性数据结构，树形结构用于存储具有层级关系的数据。

**考点：** C++ 二维数组的内存布局与存储原理。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 12,
            type: "single",
            question: `分治算法中，通常将问题分解为（ ）个子问题。`,
            options: ["1", "2 或多个", "N (数据总量)", "不确定"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (2 或多个)**

**选项逐项分析：**
- **A 1**：❌ 错误。如果只分解为 1 个子问题，通常演变为减治法（Decrease and Conquer），如二分查找。
- **B 2 或多个**：✅ 正确。分治算法的核心在于将原问题分解为若干个（通常是 2 个或更多）规模较小但结构相似的子问题，递归解决后再合并。
- **C N (数据总量)**：❌ 错误。如果分解为 $N$ 个子问题，通常会导致指数级的效率降低（除非子问题极简单）。
- **D 不确定**：❌ 错误。分治算法在设计时必须明确分解出的子问题个数。

**考点：** 分治算法（Divide and Conquer）的基本定义与特征。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 13,
            type: "single",
            question: `在 C++ 中，下列哪个关键字用于抛出异常（ ）。`,
            options: ["throw", "catch", "try", "finally"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (throw)**

**选项逐项分析：**
- **A throw**：✅ 正确。在 C++ 中，\`throw\` 关键字用于在程序运行出错时显式地抛出一个异常对象。
- **B catch**：❌ 错误。\`catch\` 用于捕获并处理异常，而不是抛出。
- **C try**：❌ 错误。\`try\` 用于界定可能抛出异常的代码范围。
- **D finally**：❌ 错误。这是 Java/Python 中的关键字，C++ 标准库中没有 \`finally\` 关键字。

**考点：** C++ 异常处理关键字的含义与用途。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 14,
            type: "single",
            question: `下列哪种算法采用了递归思想实现（ ）。`,
            options: ["顺序查找", "二分查找", "归并排序", "以上都是"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (归并排序)**

**选项逐项分析：**
- **A 顺序查找**：❌ 错误。顺序查找通常使用简单的 \`for\` 或 \`while\` 循环实现，不需要递归。
- **B 二分查找**：❌ 错误（虽然可以递归实现，但非唯一）。二分查找既可以用递归，也可以用循环，而归并排序在定义上就高度依赖递归分解。
- **C 归并排序**：✅ 正确。归并排序是分治思想的典型应用，其核心逻辑（Divide, Conquer, Merge）必须通过递归实现。
- **D 以上都是**：❌ 错误。

**考点：** 递归思想在经典算法中的应用（排序与搜索）。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 15,
            type: "single",
            question: `GESP 4 级认证不包含下列哪个知识点（ ）。`,
            options: ["一维数组", "结构体", "动态规划", "函数嵌套调用"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (动态规划)**

**选项逐项分析：**
- **A 一维数组**：❌ 错误（它是 1-2 级的要求）。
- **B 结构体**：❌ 错误（它是 3-4 级的要求）。
- **C 动态规划**：✅ 正确。动态规划（DP）是 GESP 6 级及以上的高阶考点，4 级大纲中不包含此内容。
- **D 函数嵌套调用**：❌ 错误（它是 4 级的核心考点之一）。

**考点：** GESP 大纲知识点分布（4 级核心范围）。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 16,
            type: "judge",
            question: `在 C++ 中，下面代码可以正确定义指针和初始化指针：\`int *p = nullptr;\`。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
在 C++11 之后，引入了关键字 \`nullptr\` 作为空指针常量，相比传统的 \`NULL\` 或 \`0\`，它具有更强的类型安全（指针类型），使用其初始化指针是现代 C++ 的推荐做法。

**考点：** C++11 指针初始化规范与 nullptr 的意义。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 17,
            type: "judge",
            question: `一个函数必须在调用之前既声明又定义。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
在 C++ 中，函数在使用前必须被**声明**（以便编译器知道其签名），但**定义**（具体实现）可以放在调用的后面，甚至放在不同的源文件中。

**考点：** 函数声明（Declaration）与定义（Definition）的分离机制。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 18,
            type: "judge",
            question: `函数参数可以通过值传递、引用传递和指针传递，这样函数内对参数的修改可以直接修改传入变量的值。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
本题说法过于笼统。函数参数传递中，**值传递**（Pass by Value）会创建参数的副本，函数内部对副本的修改**不会**影响外部实参。只有引用传递和指针传递才能直接修改原变量。

**考点：** C++ 参数传递机制（值传递 vs 引用传递 vs 指针传递）。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 19,
            type: "judge",
            question: `int arr[3][] 是一个正确的二维数组的声明。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
在 C++ 声明多维数组时，除了第一维可以根据初始化列表自动推导外，**后续所有维度（如列数）都必须显式指定**，以便编译器计算内存偏移量。\`int arr[3][]\` 缺少第二维的大小，无法通过编译。

**考点：** 二维数组的声明规范与内存寻址逻辑。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 20,
            type: "judge",
            question: `递推是一种通过已知的初始值和递推公式，逐步求解目标值的算法。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
递推（Recurrence）的核心思想就是“由已知推未知”。它通过确定的初始条件（边界）和数学递推公式，按一定顺序计算出后续项，是动态规划等高级算法的基础。

**考点：** 递推算法的基本原理与实施步骤。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 21,
            type: "judge",
            question: `冒泡排序的平均时间复杂度为 $O(N^2)$，但最优情况下为 $O(N)$。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
冒泡排序的平均和最坏时间复杂度均为 $O(N^2)$。但在一种常见的优化方案中（增加 \`swapped\` 标记），如果在一轮遍历中没有发生任何交换，说明数组已经有序，程序立即提前退出。对于已完全有序的数组，只需遍历一次即可完成，时间复杂度为 $O(N)$。

**考点：** 冒泡排序的算法优化与最好情况复杂度分析。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 22,
            type: "judge",
            question: `冒泡排序和插入排序都是稳定的排序算法。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
稳定性是指相等元素在排序前后的相对位置保持不变。冒泡排序仅在相邻元素“严格大于”时才交换，插入排序仅在“严格小于”时才移动，因此两者都能保证相等元素的顺序不被打乱，属于稳定排序。

**考点：** 排序算法的稳定性判定标准。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 23,
            type: "judge",
            question: `选择排序是稳定的排序算法。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
选择排序的工作方式是找到剩余部分的最小值并与当前位置交换。这种“跨距离”的交换可能会导致原本排在前面的相等元素被换到后面。例如序列 \`[5, 8, 5, 2, 9]\`，第一轮将第一个 \`5\` 与 \`2\` 交换后，原本在前的 \`5\` 就跑到了第二个 \`5\` 的后面。

**考点：** 选择排序的不稳定性反例分析。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 24,
            type: "judge",
            question: `在 C++ 语言中，如果一个函数可能抛出异常，那么一定要在 try 子句里调用这个函数。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
虽然 \`try-catch\` 是处理可能抛出异常代码的标准方式，但在 C++ 中并不存在“一定要在 try 子句里调用”的硬性规定。如果不使用 \`try-catch\` 捕获，异常会自动向函数的调用者（上层栈帧）传递，直到被捕获或最终导致程序异常终止。

**考点：** 异常传播（Propagation）机制与处理策略。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 25,
            type: "judge",
            question: `GESP 认证分为 1 到 8 级，数字越大难度越高。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
这是 GESP 官方认证体系的常识。GESP 共设 8 个级别，1-4 级侧重编程基础与简单算法，5-8 级则引入了动态规划、数据结构优化等更复杂的知识体系，难度随级别递增。

**考点：** GESP 认证体系常识。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        ...programmingQuestions
    ]
};
