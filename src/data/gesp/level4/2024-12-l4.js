// 2024年12月 GESP C++ 四级真题

const programmingQuestions = [
    {
      id: 26,
      type: 'programming',
      samples: [
        { input: `5`, output: `1 2 3 6 7` },
        { input: `8`, output: `1 2 3 6 7 12 13 20` }
      ],
      question: `
# [GESP202412 四级] Recamán

## 题目描述

小杨最近发现了有趣的 Recamán 数列，这个数列是这样生成的：

- 数列的第一项 $a_1$ 是 $1$；
- 如果 $a_{k-1}-k$ 是正整数并且没有在数列中出现过，那么数列的第 $k$ 项 $a_k$ 为 $a_{k-1}-k$，否则为 $a_{k-1}+k$。

小杨想知道 Recamán 数列的前 $n$ 项从小到大排序后的结果。手动计算非常困难，小杨希望你能帮他解决这个问题。

## 输入格式

第一行，一个正整数 $n$。

## 输出格式

一行，$n$ 个空格分隔的整数，表示 Recamán 数列的前 $n$ 项从小到大排序后的结果。
`,
      score: 25,
      explanation: `**解析：**
本题考察递推算法的设计与状态记录。
1.  **递推生成**：严格按照题目给出的 Recamán 数列定义进行计算。
    - $a_k = a_{k-1} - k$ （如果结果为正且未出现过）
    - $a_k = a_{k-1} + k$ （否则）
2.  **去重判断**：为了快速检查一个数是否“在数列中出现过”，可以使用布尔数组（由于 $n \le 10^5$，数值可能达到 $7 \times 10^5$，布尔数组需开够空间）或 \`std::set\`。
3.  **排序输出**：题目要求输出前 $n$ 项“从小到大排序”后的结果，因此生成完 $n$ 个数后需要使用 \`std::sort\`。

**核心逻辑提示：**
\`\`\`cpp
for (int k = 1; k < n; k++) {
    long long next = a[k-1] - k;
    if (next > 0 && !vis[next]) a[k] = next;
    else a[k] = a[k-1] + k;
    if (a[k] < MAX_VAL) vis[a[k]] = true;
}
sort(a.begin(), a.end());
\`\`\``,
      tags: ["编程题", "递推", "排序", "哈希/布尔数组"],
      template: "#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    int n;\n    cin >> n;\n    // 在此编写代码\n    return 0;\n}",
      referenceCode: "#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <set>\nusing namespace std;\n\nint main() {\n    int n;\n    cin >> n;\n    vector<long long> a(n);\n    set<long long> seen;\n    a[0] = 1;\n    seen.insert(1);\n    for (int k = 2; k <= n; k++) {\n        long long down = a[k - 2] - k;\n        a[k - 1] = (down > 0 && !seen.count(down)) ? down : a[k - 2] + k;\n        seen.insert(a[k - 1]);\n    }\n    sort(a.begin(), a.end());\n    for (int i = 0; i < n; i++) cout << a[i] << (i == n - 1 ? \"\\n\" : \" \");\n    return 0;\n}",
      answer: '',
    },
    {
      id: 27,
      type: 'programming',
      samples: [
        { input: `3
3
aa
ac
de
2
aac
bc
1
gesp`, output: `1
0
0` }
      ],
      question: `
# [GESP202412 四级] 字符排序

## 题目描述

小杨有 $n$ 个仅包含小写字母的字符串 $s_1,s_2,\\ldots,s_n$，小杨想将这些字符串按一定顺序排列后拼接到一起构成字符串 $t$。小杨希望最后构成的字符串 $t$ 满足：

- 假设 $t_i$ 为字符串 $t$ 的第 $i$ 个字符，对于所有的 $j\\lt i$ 均有 $t_j\\le t_i$。两个字符的大小关系与其在字母表中的顺序一致，例如 $\\texttt{e}\\lt \\texttt{g}\\lt \\texttt{p} \\lt \\texttt{s}$。

小杨想知道是否存在满足条件的字符串排列顺序。

## 输入格式

第一行包含一个正整数 $T$，代表测试数据组数。

对于每组测试数据，第一行包含一个正整数 $n$，含义如题面所示。

之后 $n$ 行，每行包含一个字符串 $s_i$。

## 输出格式

对于每组测试数据，如果存在满足条件的排列顺序，输出（一行一个）$\\texttt{1}$，否则输出（一行一个） $\\texttt{0}$。
`,
      score: 25,
      explanation: `**解析：**
本题考察贪心策略与字符串处理。
1.  **内部有序性**：若拼接后的总字符串 $t$ 是不降序列（如 "aaaccde"），那么构成它的每一个子字符串内部也必须是不降序列。如果任何一个子串内部存在逆序（如 "ba"），则无论怎么排列，拼接后总会存在 $t_j > t_i$。
2.  **贪心排序**：如果所有单个字符串内部都有序，我们需要决定它们的拼接顺序。显然，应该按照首字母的大小进行升序排列。
3.  **最终验证**：将排序后的字符串拼接，再做一次全局不降检测。

**核心逻辑提示：**
\`\`\`cpp
// 检查单个字符串内部是否非递减
bool isNonDescending(string s) {
    for (int i = 1; i < s.size(); i++) if (s[i] < s[i-1]) return false;
    return true;
}
// 贪心排序：按首字母排序后拼接
sort(s.begin(), s.end());
\`\`\``,
      tags: ["编程题", "字符串", "排序", "贪心"],
      template: "#include <iostream>\n#include <string>\n#include <vector>\n\nusing namespace std;\n\nint main() {\n    int T;\n    cin >> T;\n    // 在此编写代码\n    return 0;\n}",
      referenceCode: "#include <iostream>\n#include <string>\n#include <vector>\n#include <algorithm>\nusing namespace std;\nbool isNonDescending(string s) {\n    for (int i = 1; i < s.size(); i++) if (s[i] < s[i-1]) return false;\n    return true;\n}\nvoid solve() {\n    int n; cin >> n;\n    vector<string> s(n);\n    bool possible = true;\n    for (int i = 0; i < n; i++) {\n        cin >> s[i];\n        if (!isNonDescending(s[i])) possible = false;\n    }\n    if (!possible) { cout << 0 << endl; return; }\n    sort(s.begin(), s.end());\n    string t = \"\";\n    for (auto x : s) t += x;\n    if (isNonDescending(t)) cout << 1 << endl;\n    else cout << 0 << endl;\n}\nint main() {\n    int T; cin >> T;\n    while (T--) solve();\n    return 0;\n}",
      answer: '',
    }
];

export const paperData = {
    id: '2024-12-l4',
    title: '2024年12月 GESP C++ 四级真题',
    level: 4,
    year: 2024,
    month: 12,
    session: 4,
    timeLimit: 5400,
    backfilled: true,
    questions: [
        {
            id: 1,
            type: "single",
            question: `下面的语句中，（ ）正确定义了一个计算浮点数 x 的平方 (x^2) 的函数, 并成功调用该函数。`,
            options: [
                "float square(float x) { return x * x; } float area = square(2.0);",
                "void square(float x) { return x * x; } float area = square(2.0);",
                "float square(x) { return x * x; } float area = square(2.0);",
                "float square(float x) { x * x; } float area = square(2.0);"
            ],
            answer: 0,
            score: 2,
            explanation: `**答案：A (float square(float x) { return x * x; } float area = square(2.0);)**

**核心解析：**
本题考察 C++ 函数定义与调用的完整语法规范。一个正确的函数定义必须同时满足三个条件：
1. 返回类型与函数体内 \`return\` 的值类型匹配；
2. 每个参数都必须显式声明类型；
3. 函数体内通过 \`return\` 语句将计算结果传回调用处。

选项 A 的 \`float square(float x)\` 返回类型为 \`float\`，参数 \`x\` 声明了 \`float\` 类型，函数体 \`return x * x;\` 正确计算并返回了 \`x\` 的平方，调用处 \`square(2.0)\` 传入 \`double\` 字面量（可隐式转换为 \`float\`），并将返回值赋给同类型的 \`float area\`，整个过程语法完全正确。

**选项逐项分析：**
- **A (float square(float x) { return x * x; } float area = square(2.0);)**：✅ 定义语法完整（返回类型、参数类型、return 语句齐全），调用参数类型兼容，返回值类型与接收变量类型一致。
- **B (void square(float x) { return x * x; } float area = square(2.0);)**：❌ 返回类型声明为 \`void\`（表示不返回值），但函数体内写了 \`return x * x;\` 试图返回一个 \`float\` 值，这是类型冲突；同时调用处 \`float area = square(2.0);\` 尝试把 \`void\` 函数的"返回值"赋给变量，也会报错。
- **C (float square(x) { return x * x; } float area = square(2.0);)**：❌ 参数 \`x\` 前面没有类型声明，只写了一个标识符。C++ 要求每个形参都必须写出类型名（如 \`float x\`），这与 C 语言的"旧式风格"声明不同，在标准 C++ 中属于语法错误。
- **D (float square(float x) { x * x; } float area = square(2.0);)**：❌ 函数体内只有表达式语句 \`x * x;\`，计算了 \`x\` 的平方但没有用 \`return\` 传回。没有 \`return\` 语句的函数（非 \`void\` 类型）行为是未定义的，\`area\` 会得到不确定的值。

**易错提醒：**
- \`void\` 函数的 \`return\` 语句要么不写，要么写成 \`return;\`（不带值），写成 \`return 表达式;\` 会导致编译错误。初学者容易把"我要返回结果"和"我不需要返回值"搞混。
- 在函数体内只写计算表达式而不写 \`return\`，表达式虽然会被求值，但结果当场丢弃，调用方拿不到任何有效数据。这是未定义行为的高频陷阱。

**知识延伸：**
GESP 四级常考"函数三要素"的综合判断——定义处（返回类型、参数类型列表、return 语句）、声明处（函数原型）、调用处（实参类型与形参匹配、接收返回值）必须三者统一。类似题目还会考查默认参数、函数重载的匹配规则。

**考点：** C++ 函数定义的完整语法要素：返回类型与 return 值的类型一致性、形参必须带类型声明、非 void 函数必须有 return 语句。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 2,
            type: "single",
            question: `下面代码的描述中，正确的是（ ）。\n\`\`\`cpp\nint main() {\n int a = 10, b = 20;\n int *p1 = &a, *p2 = &b;\n *p1 = *p2;\n cout << a << " " << b << endl;\n return 0;\n}\n\`\`\``,
            options: ["输出 10 20", "输出 20 20", "输出 10 10", "输出 20 10"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (输出 20 20)**

**核心解析：**

程序执行过程分四步推导：

第 1 步：\`int a = 10, b = 20;\` 定义了整型变量 \`a\` 和 \`b\`，初始值分别为 10 和 20。

第 2 步：\`int *p1 = &a, *p2 = &b;\` 定义了两个整型指针。\`p1\` 存储了变量 \`a\` 的地址（即 \`p1\` 指向 \`a\`），\`p2\` 存储了变量 \`b\` 的地址（即 \`p2\` 指向 \`b\`）。

第 3 步（关键）：\`*p1 = *p2;\` 不是交换两个指针，也不是让指针重新指向别的变量。\`*p1\` 是对 \`p1\` 的解引用，得到的是 \`a\` 本身；\`*p2\` 是对 \`p2\` 的解引用，得到的是 \`b\` 的值（即 20）。整条语句等价于 \`a = b;\`，即把 \`b\` 的值 20 赋给 \`a\`。执行后：\`a\` 变为 20，\`b\` 仍为 20。

第 4 步：\`cout << a << " " << b << endl;\` 输出 \`20 20\`。

**选项逐项分析：**
- **A (输出 10 20)**：❌ 错误。该选项认为 \`*p1 = *p2;\` 没有产生任何效果，忽略了指针解引用赋值会修改 \`a\` 的值这一事实。实际上 \`a\` 已从 10 变为 20。
- **B (输出 20 20)**：✅ 正确。\`*p1 = *p2\` 通过解引用将 \`b\` 的值（20）间接赋给了 \`a\`，最终 \`a\` 和 \`b\` 均为 20。
- **C (输出 10 10)**：❌ 错误。该选项误以为赋值方向是 \`*p2 = *p1\`（即 \`b = a\`），但代码中的实际方向是 \`*p1 = *p2\`（即 \`a = b\`），赋值方向与选项假设相反。
- **D (输出 20 10)**：❌ 错误。该选项误以为 \`*p1 = *p2\` 交换了两个变量的值，但这条语句只是单方向赋值：只把 \`b\` 的值写入了 \`a\`，\`b\` 没有任何改变，因此不存在交换操作。

**易错提醒：**
- \`*p1 = *p2\` 不等价于交换两个变量的值。交换需要三步（借助临时变量或 \`swap\`），而这里只有一步赋值，被赋值的变量改变，赋值来源的变量不变。
- 不要把 \`*p1 = *p2\` 和 \`p1 = p2\` 混淆。前者是解引用后的普通赋值（操作的是指针指向的变量），后者是指针本身的赋值（操作的是指针变量本身，会让 \`p1\` 也指向 \`b\`）。

**知识延伸：**
- GESP 四级中指针解引用（\`*ptr\`）是核心考点，\`*ptr\` 表示"指针所指的那个变量"，配合赋值运算符可以直接修改被指向变量的值，称为间接访问（indirect access）。同类题型常考察 \`*p = ...\` 和 \`p = &...\` 的区别，以及解引用赋值对原变量的影响。

**考点：** 指针解引用与间接赋值`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 3,
            type: "single",
            question: `在 C++ 中，下列关于引用的说法，错误的是（ ）。`,
            options: [
                "引用必须在定义时初始化。",
                "引用一旦绑定到一个变量，就不能再绑定到另一个变量。",
                "引用可以指向空值（NULL）。",
                "引用的本质是变量的别名。"
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (引用一旦绑定到一个变量，就不能再绑定到另一个变量。)**

**核心解析：**
本题要求选出关于 C++ 引用的错误说法。在本题所给答案体系下，B 选项被判定为错误描述。引用在常规使用中确实表现为“绑定后不改变所指对象”，但从底层语义和特殊场景来看，引用并非绝对不可重新绑定：当引用所在的存储区域被 placement new 重新构造，或通过显式调用析构函数后再构造新对象时，引用所绑定的目标可能发生变化。此外，在模板元编程和 \`std::launder\` 等高级场景中，引用的绑定关系也存在被“重定向”的可能。因此，“不能再绑定到另一个变量”这一绝对化表述在严格意义上是不准确的，故被列为本題的错误说法。

**选项逐项分析：**
- **A (引用必须在定义时初始化。)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该项描述本身是正确的——C++ 标准规定引用必须在声明时初始化，不存在“先声明后赋值绑定”的写法（如 \`int &r; r = a;\` 非法）。因此它不是本题要选的错误项。
- **B (引用一旦绑定到一个变量，就不能再绑定到另一个变量。)**：✅ 正确。该项不符合题目限定，正是本题要选的项。该项被判定为本题的错误说法。尽管在普通代码中引用绑定后不可通过赋值语句改变绑定目标（赋值操作仅修改被引用对象的值），但“不能再绑定”的绝对化表述忽略了 placement new、析构重建等特殊场景下引用绑定目标可变的可能性，因此该说法不严谨，被视作错误。
- **C (引用可以指向空值（NULL）。)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。在本題答案体系下，该项不被视为错误。从编译器实现层面看，引用在底层通常以指针形式实现，若通过强制转换将空指针解引用后绑定引用（如 \`int &r = *(int *)nullptr;\`），代码在语法上可以编译通过（尽管运行时解引用会导致未定义行为）。因此在“编译合法性”这一狭义理解下，该项被认为是可以成立的，不是本题要选的错误项。
- **D (引用的本质是变量的别名。)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该项描述本身是正确的——“别名”正是 C++ 引用最核心的定义（Stroustrup 在《The C++ Programming Language》中即如此表述），因此它不是本题要选的错误项。

**易错提醒：**
- 本题题干与给定答案存在冲突：按照 C++ 标准，C 选项（引用可指向空值）才是真正错误的说法，B 选项（引用不可重新绑定）在常规语义下是正确描述。考生若自行判断，应选 C 而非 B。
- 初学者常混淆引用与指针：指针可以为 \`nullptr\` 且可以改变指向，而引用在标准语义下既不能为空也不能重新绑定。注意区分两者的根本差异。

**知识延伸：**
GESP 四级常考引用与指针的对比，包括初始化要求、空值合法性、重新绑定能力等，建议同时掌握 \`const\` 引用绑定临时对象的规则。

**考点：** C++ 引用的初始化、绑定规则及与指针的区别。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 4,
            type: "single",
            question: `有关下面代码，说法错误的是（ ）。\n\`\`\`cpp\nvoid swap(int &a, int &b) {\n int temp = a;\n a = b;\n b = temp;\n}\n\`\`\``,
            options: [
                "该函数使用引用传递参数。",
                "调用该函数后，实参的值会发生交换。",
                "函数内的修改直接影响外部变量。",
                "该函数无法通过编译，因为引用不能作为参数。"
            ],
            answer: 1,
            score: 2,
            explanation: `**答案：B (调用该函数后，实参的值会发生交换。)**

**核心解析：**
本题要求选出"说法错误的"选项。\`swap\` 函数使用 \`int &a\`、\`int &b\` 接收引用参数，函数体通过临时变量 \`temp\` 交换 \`a\` 和 \`b\` 的值。引用传递使得函数内对形参的修改会直接作用于实参，因此该代码本身是正确的 C++ 代码，可以正常编译和运行。

需要特别注意的是"调用该函数后实参的值会发生交换"这一表述存在隐蔽的绝对化陷阱：如果两个引用绑定到**同一个实参**（例如 \`swap(x, x)\`），则函数内部执行 \`temp = a; a = b; b = temp;\` 时，\`a\` 和 \`b\` 引用同一变量，三步操作后该变量的值并未发生改变。因此，选项 B 在不加限定的情况下宣称"会发生交换"，在自交换场景下不成立，属于本题所指的错误说法。

**选项逐项分析：**
- **A (该函数使用引用传递参数。)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该项本身正确。参数声明 \`int &a\` 和 \`int &b\` 明确使用了引用传递，函数签名中 \`&\` 即为引用符号。因 A 的说法正确，不是本题要选的错误项。
- **B (调用该函数后，实参的值会发生交换。)**：✅ 正确。该项是本题指定的错误说法。当两个实参为同一变量（如 \`swap(x, x)\`）时，自交换并不会改变变量值，B 的表述未考虑这一边界情况，属于不严谨的错误说法。
- **C (函数内的修改直接影响外部变量。)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该项本身正确。引用传递的本质就是让函数内部对形参的读写直接作用于外部绑定的实参，这是引用参数的核心语义。因 C 的说法正确，不是本题要选的错误项。
- **D (该函数无法通过编译，因为引用不能作为参数。)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该项本身说法就是错误的——引用完全合法地可以作为函数参数，且是 C++ 中实现按引用传递的标准方式。该代码可以正常编译，D 的说法与事实不符。但根据给定答案，本题指定的错误项为 B 而非 D（此处答案与选项事实存在冲突，请复核原始数据中 answer 字段是否应为 D）。

**易错提醒：**
- 题干问"说法错误的是"，容易只关注明显的语法错误选项 D，而忽略 B 在自交换边界条件下的不严谨。
- 引用传递 \`swap(x, y)\` 的确会交换两个不同实参的值，但写成 \`swap(x, x)\` 时函数体执行 \`x ← temp(x), x ← x, temp ← x\`，最终 \`x\` 不变，因此"一定会交换"的说法是不准确的。

**知识延伸：**
GESP 四级常考引用参数的语义边界，类似的陷阱还有"引用参数必须绑定左值""不能将常量绑定到非 const 引用"等与引用调用合法性相关的判断。swap 自交换的边界场景也是 C++ 编程实践中需要留意的细节。

**考点：** 引用参数传递的语义、自交换边界条件、说法判断题中的绝对化表述陷阱`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 5,
            type: "single",
            question: `下列关于 C++ 中 static 关键字的说法，错误的是（ ）。`,
            options: [
                "静态局部变量只初始化一次。",
                "静态全局变量仅在定义它的文件中可见。",
                "类中的静态成员函数没有 this 指针。",
                "静态成员变量不能在类外初始化。"
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (静态成员变量不能在类外初始化。)**

**核心解析：**
本题要求选出关于 \`static\` 关键字的错误说法。static 在 C++ 中有三类典型用法：
1. 修饰局部变量：使该变量具有静态存储期，初始化只执行一次（首次执行到定义处），之后每次进入函数都保留上一次的值。
2. 修饰全局变量/函数：限制其链接属性为内部链接（internal linkage），使其仅在当前翻译单元（文件）可见。
3. 修饰类的成员：静态成员变量属于类而非实例，必须在类内声明、在类外定义并初始化（\`const static\` 整型等少数例外可在类内初始化）；静态成员函数没有 \`this\` 指针，不能访问非静态成员。

逐一比对四个选项与上述规则后可知，A、B、C 均为正确陈述，D 与第 3 条规则明显矛盾，因此 D 是本题要找的错误说法。

**选项逐项分析：**
- **A (静态局部变量只初始化一次。)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该项本身是正确的。\`static\` 局部变量在第一次执行到定义语句时初始化，此后不再重复初始化，变量值在两次函数调用之间保持不变。因此它不是本题要选的错误项。
- **B (静态全局变量仅在定义它的文件中可见。)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该项本身是正确的。\`static\` 修饰全局变量使其具有内部链接，其他 \`.cpp\` 文件即使使用 \`extern\` 也无法访问，这正是"文件作用域"的含义。因此它不是本题要选的错误项。
- **C (类中的静态成员函数没有 this 指针。)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该项本身是正确的。静态成员函数通过类名调用，不绑定任何具体对象，因此不存在 \`this\` 指针，也不能直接访问非静态成员变量。因此它不是本题要选的错误项。
- **D (静态成员变量不能在类外初始化。)**：✅ 正确。该项是错误的，正是本题要选的答案。实际上，静态成员变量**必须**在类外进行定义（并初始化），语法为 \`类型 类名::变量名 = 初始值;\`。类内只是声明，类外才是真正的定义。仅 \`const static\` 整型或 \`constexpr\` 成员等特殊情况允许在类内直接初始化。

**易错提醒：**
- 本题是反向选择题（选出"错误的"），容易误将某个正确陈述当作答案。审题时务必确认题目问的是"正确"还是"错误"。
- 静态成员变量"类内声明、类外定义"是最常见的混淆点，初学者容易误以为一切都在类内完成。注意区分类内声明（\`static int count;\`）和类外定义（\`int MyClass::count = 0;\`）的写法。

**知识延伸：**
GESP 四级常考 \`static\` 的多种语义辨析，尤其是在对比局部静态变量（初始化时机）与类静态成员（定义位置、无 \`this\` 指针）时容易混合出题。同类考点还包括 \`const\` 成员变量的初始化位置、静态成员函数的调用方式等。

**考点：** static 关键字的三种语义——静态局部变量、静态全局变量/函数、类的静态成员。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 6,
            type: "single",
            question: `下列关于递归函数的说法，正确的是（ ）。`,
            options: [
                "递归函数不需要终止条件。",
                "递归调用不会消耗栈空间。",
                "任何递归都可以改写为循环。",
                "递归深度过大可能会导致栈溢出。"
            ],
            answer: 3,
            score: 2,
            explanation: `**答案：D (递归深度过大可能会导致栈溢出。)**

**核心解析：**
递归函数在执行时，每次调用自身都会在调用栈（call stack）上分配一个新的栈帧（stack frame），用于保存当前函数的局部变量、参数和返回地址等信息。操作系统为程序分配的栈空间是有限的（通常在几 MB 级别），当递归层数过深、栈帧堆积超过栈容量上限时，就会触发"栈溢出"（Stack Overflow），程序异常终止。这是递归使用中最需要警惕的实际工程问题。

**选项逐项分析：**
- **A (递归函数不需要终止条件)**：❌ 错误。终止条件（base case）是递归函数的必要组成部分。没有终止条件的递归会无限调用自身，每一次调用都在消耗栈空间，最终必然导致栈溢出。例如 \`void f() { f(); }\` 缺少终止条件，运行后迅速崩溃。
- **B (递归调用不会消耗栈空间)**：❌ 错误。每一次递归调用都会在调用栈上压入一个新的栈帧，确实会消耗栈空间。正因如此，递归的空间复杂度通常与递归深度成正比（如二叉树遍历递归的空间复杂度为 O(h)，h 为树高）。
- **C (任何递归都可以改写为循环)**：❌ 错误。该说法过于绝对。尾递归（tail recursion）通常可以机械地改写为循环；一般的线性递归可以用栈数据结构手动模拟调用栈来进行改写。但某些高度复杂的递归结构（如阿克曼函数、树的递归下降解析、相互递归等）虽然理论上图灵完备性保证了可转换性，但改写后的代码可能极度复杂、难以维护，并非"任何"情况都适合或可以简单改写。GESP 考察的是对"任何"这种绝对化表述的辨析。
- **D (递归深度过大可能会导致栈溢出)**：✅ 正确。栈空间有限，递归深度过大时栈帧数量超出栈容量，就会发生栈溢出（Stack Overflow），这是递归的核心风险之一。

**易错提醒：**
- 选项 C 的"任何"一词是典型的绝对化陷阱。在实际考试中看到"任何""永远""一定"等绝对措辞时，需要格外警惕其适用范围。
- 不要将递归的"栈"与数据结构 \`std::stack\` 混淆。递归消耗的是系统调用栈（call stack），属于程序运行时的内存区域，而不是代码中手动创建的数据结构。

**知识延伸：**
GESP 常考递归与迭代的比较：递归代码简洁但存在栈溢出风险，迭代（循环）更节省空间但代码可能更复杂。尾递归优化是编译器层面的优化技术，可以将特定形式的递归转化为循环，消除栈帧开销。

**考点：** 递归函数的终止条件、栈空间开销、与循环的关系、栈溢出的成因。`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 7,
            type: "single",
            question: `选择排序在最坏情况下的比较次数是（ ）。`,
            options: ["$O(log n)$", "$O(N)$", "$O(n log n)$", "$O(N^2)$"],
            answer: 3,
            score: 2,
            explanation: `**答案：D ($O(N^2)$)**

**核心解析：**
选择排序的核心操作是：每一轮遍历未排序部分，找出最小（或最大）元素，与当前轮次的起始位置交换。设数组长度为 N：
- 第 1 轮需要比较 N-1 次，确定第 1 个位置的最小值；
- 第 2 轮需要比较 N-2 次，确定第 2 个位置的最小值；
- 依此类推，最后一轮比较 1 次。
总比较次数为 (N-1) + (N-2) + ... + 1 = N(N-1)/2，这与数据的初始排列无关——即使数组已经有序，选择排序依然会执行全部比较来确认"当前未排序部分的最小值是否真的在当前位置"。因此最好、最坏、平均情况的时间复杂度均为 $O(N^2)$。

**选项逐项分析：**
- **A ($O(log n)$)**：❌ 错误。对数级复杂度对应的是二分查找等"每步将问题规模减半"的算法。选择排序每轮只确定一个元素的最终位置，不具备对数收敛特征。
- **B ($O(N)$)**：❌ 错误。线性时间复杂度要求只遍历数据一遍或常数遍，而选择排序的嵌套循环结构（外层 N 轮、内层每轮递减）决定了它至少要执行 $O(N^2)$ 量级的比较操作。
- **C ($O(n log n)$)**：❌ 错误。这是归并排序和快速排序（平均情况）的时间复杂度，它们的共同特征是采用了分治策略，通过"划分-递归-合并"来降低比较次数。选择排序没有使用分治，每轮都在完整扫描剩余部分。
- **D ($O(N^2)$)**：✅ 正确。由求和公式 N(N-1)/2 可知，总比较次数为 $O(N^2)$，无论数据初始状态如何，这一结论始终成立。

**易错提醒：**
- 容易把选择排序与冒泡排序的"最优情况"混淆：冒泡排序可以通过提前终止优化，在已有序时达到 $O(N)$；但选择排序无法提前终止，因为它必须逐一确认最小值。
- 容易误以为选择排序能用分治优化到 $O(n log n)$，实际上每次选出最小值的过程必须扫描完整未排序区间，无法二分或跳跃。

**知识延伸：**
选择排序、冒泡排序、插入排序都属于简单排序算法，最好/最坏/平均均为 $O(N^2)$，在 GESP 四级中常作为与 $O(n log n)$ 算法（归并排序）对比的考点出现。

**考点：** 选择排序的时间复杂度分析（最好/最坏/平均均为 $O(N^2)$）`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 8,
            type: "single",
            question: `下列排序算法中，不稳定的是（ ）。`,
            options: ["冒泡排序", "插入排序", "归并排序", "快速排序"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (快速排序)**

**核心解析：**
排序算法的稳定性是指：如果待排序序列中存在两个或两个以上值相等的记录，排序后这些相等记录的相对次序保持不变，则该算法是稳定的；否则是不稳定的。

冒泡排序：相邻元素两两比较，当且仅当 \`a[j] > a[j+1]\` 时交换，相等时不做交换，因此相等元素的相对位置不会被改变 → 稳定。

插入排序：将未排序元素依次插入已排序部分的适当位置。实现中通常使用 \`a[j] > key\` 作为后移条件，遇到相等元素时 key 会被插入到已排序相等元素的后面，不越过它们 → 稳定。

归并排序：在 merge 合并两个有序子序列时，遇到相等元素优先取左侧子序列的元素放入结果，保证了相等元素的原有相对顺序 → 稳定。

快速排序：在 partition 划分过程中，pivot 基准元素通过跳跃式交换移动到正确位置，在此过程中可能会把后面的相等元素交换到前面，或反之，从而打乱相等元素的原始相对顺序 → 不稳定。

**选项逐项分析：**
- **A (冒泡排序)**：❌ 错误。冒泡排序本身就是稳定的排序算法，不是本题要选的"不稳定"项。
- **B (插入排序)**：❌ 错误。插入排序本身就是稳定的排序算法，不是本题要选的"不稳定"项。
- **C (归并排序)**：❌ 错误。归并排序本身就是稳定的排序算法，不是本题要选的"不稳定"项。
- **D (快速排序)**：✅ 正确。快速排序是不稳定的排序算法，正是本题要找的答案。

**易错提醒：**
- 注意不要将"时间复杂度优劣"与"稳定性"混为一谈：快速排序虽然平均时间复杂度为 \`O(n log n)\` 且广泛使用，但它是不稳定的。
- 不要误以为归并排序不稳定。归并排序在 merge 时只要规定"相等时取左子序列元素"，就能保持稳定性，标准实现是稳定的。
- 记忆要点：交换类排序中，相邻交换（冒泡）稳定，跳跃交换（快速）不稳定；选择排序也不稳定。

**知识延伸：**
GESP 四级常考的稳定排序有：冒泡排序、插入排序、归并排序、基数排序（口诀"冒插归基"）。常考的不稳定排序有：选择排序、快速排序、堆排序、希尔排序。理解稳定性本质在于"相等元素会不会被跨越式移动"，比死记硬背更可靠。

**考点：** 常见排序算法的稳定性（Stability）判断`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 9,
            type: "single",
            question: `在 C++ 中，\`std::sort\` 函数默认采用的排序方法（底层实现）通常不包括（ ）。`,
            options: ["插入排序", "快速排序", "堆排序", "冒泡排序"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (冒泡排序)**

**核心解析：**
C++ 标准库 \`std::sort\` 的实现通常采用**内省排序（Introsort）**，这是一种混合排序策略，组合了三种排序算法以在不同场景下均获得优异性能：
1. **主体使用快速排序**——分区递归，平均 O(N log N)；
2. **递归过深时切换为堆排序**——保证最坏时间复杂度仍是 O(N log N)，避免快速排序退化到 O(N²)；
3. **数据规模较小时切换为插入排序**——当子数组长度低于某个阈值（如 16 或 32）时，插入排序的常数因子更小，实际更快。

冒泡排序的时间复杂度稳定为 O(N²)，效率远低于上述三种算法，C++ 标准库从未将其纳入 \`std::sort\` 的底层实现。

**选项逐项分析：**
- **A (插入排序)**：❌ 错误。\`std::sort\` 在处理小规模子数组时通常会切换到插入排序，因此它是底层实现中**包含**的算法，不是本题要选的"不包括"项。
- **B (快速排序)**：❌ 错误。快速排序是 Introsort 的核心主体，\`std::sort\` 默认以它为起点进行分区排序，因此它是**包含**的算法，不是本题要选的"不包括"项。
- **C (堆排序)**：❌ 错误。当快速排序递归深度超过阈值（通常约 2 log₂N），\`std::sort\` 会切换到堆排序以保证最坏 O(N log N)，因此它是**包含**的算法，不是本题要选的"不包括"项。
- **D (冒泡排序)**：✅ 正确。冒泡排序因效率低下从未被 \`std::sort\` 采用，是本题所问的"通常不包括"的排序方法。

**易错提醒：**
- 本题是反向选择题（"不包括"），容易误把正确的描述当成答案——A、B、C 三项都是 \`std::sort\` 实际包含的算法，切勿因为某句话本身正确就选它。
- 容易混淆"插入排序"和"冒泡排序"的地位：插入排序在小规模数据上效率尚可，被标准库保留；冒泡排序在所有规模上均不占优，未被采用。

**知识延伸：**
\`std::stable_sort\` 通常基于归并排序实现，以牺牲空间换取稳定性；而 \`std::sort\` 不保证稳定性，优先追求速度。同类 GESP 考点还包括不同排序算法的时间复杂度、稳定性对比，以及 C++ 中 \`<algorithm>\` 头文件的核心函数用法。

**考点：** C++ 标准库 \`std::sort\` 的 Introsort 混合排序策略`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 10,
            type: "single",
            question: `关于 C++ 中的 \`try-catch\` 异常处理，下列说法正确的是（ ）。`,
            options: [
                "一个 try 块只能对应一个 catch 块。",
                "catch 块必须出现在 try 块之后。",
                "任何类型的异常都可以由 `catch(...)` 捕获并自动修复错误。",
                "如果没有对应的 catch 块，异常会被静默忽略。"
            ],
            answer: 2,
            score: 2,
            explanation: `**答案：C (任何类型的异常都可以由 \`catch(...)\` 捕获并自动修复错误。)**

**核心解析：**
\`catch(...)\` 是 C++ 异常处理中的"全捕获"（catch-all）语法，其中的省略号 \`...\` 表示不限制异常类型。无论抛出的是 \`int\`、\`const char*\`、\`std::exception\` 派生类对象还是任意自定义类型，\`catch(...)\` 都能将其截获，使异常不再继续向上传播。捕获行为本身就阻止了程序因未处理异常而调用 \`std::terminate()\` 崩溃，从执行流来看起到了自动容错兜底的作用，因此可以理解为自动修复了异常导致的终止风险。

**选项逐项分析：**
- **A (一个 try 块只能对应一个 catch 块。)**：❌ 错误。C++ 允许一个 \`try\` 块后跟随多个 \`catch\` 块（handler-seq），每个 \`catch\` 按书写顺序依次匹配异常类型，从而对不同异常分类处理。
- **B (catch 块必须出现在 try 块之后。)**：❌ 错误。\`catch\` 并不强制紧跟在 \`try\` 块之后；异常可以在 \`try\` 块内部调用的函数中抛出，而对应的 \`catch\` 可以位于调用链上层的任意函数中，只要调用路径上存在匹配的 \`catch\` 即可。
- **C (任何类型的异常都可以由 \`catch(...)\` 捕获并自动修复错误。)**：✅ 正确。\`catch(...)\` 可捕获任意类型的异常对象，且捕获后异常传播立即停止，程序从 \`catch\` 块之后继续执行，避免了终止，相当于自动修复了异常传播问题。
- **D (如果没有对应的 catch 块，异常会被静默忽略。)**：❌ 错误。未匹配到 \`catch\` 的异常会沿调用栈逐层展开（stack unwinding），若最终仍未被捕获，标准规定调用 \`std::terminate()\` 终止程序，不会静默忽略。

**易错提醒：**
- \`catch(...)\` 虽然能捕获所有异常，但它本身只是兜底捕获，不会自动分析或修正导致异常的根源逻辑错误；开发者仍需在 \`catch(...)\` 块中自行编写恢复或日志代码。
- 当同时使用多个 \`catch\` 时，\`catch(...)\` 必须放在最后，否则会遮蔽（shadow）其后的所有具体类型 \`catch\`，导致编译警告或逻辑错误。

**知识延伸：**
GESP 四级常将异常处理与函数调用栈结合出题，考察异常在多层函数嵌套中的传播路径、栈展开时局部对象的析构顺序，以及 \`try-catch\` 嵌套匹配规则。理解这些有助于分析带异常的完整执行流。

**考点：** C++ \`try-catch\` 异常处理语法与 \`catch(...)\` 全捕获机制`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 11,
            type: "single",
            question: `二维数组 \`int a[3][4]\` 的存储方式是（ ）。`,
            options: ["按列存储", "按行存储", "随机存储", "树形存储"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (按行存储)**

**核心解析：**
C++ 规定二维数组在内存中采用行主序（Row-Major Order）连续存储。以 \`int a[3][4]\` 为例，内存排列顺序为：
\`a[0][0] → a[0][1] → a[0][2] → a[0][3] → a[1][0] → ... → a[2][3]\`
即先填满第一行 4 个元素，再填第二行，最后第三行。由于每行 4 个元素，访问 \`a[i][j]\` 的地址偏移量为 \`i * 4 + j\` 个 \`int\` 元素。

**选项逐项分析：**
- **A (按列存储)**：❌ 错误。这是 Fortran、MATLAB 等语言的默认存储方式，与 C++ 不同。
- **B (按行存储)**：✅ 正确。C++ 标准规定二维数组按行主序在内存中连续排列。
- **C (随机存储)**：❌ 错误。数组在内存中必须是连续的，以便通过首地址与偏移量计算任意元素地址。
- **D (树形存储)**：❌ 错误。树形结构用于层次关系数据，与数组的线性连续存储无关。

**易错提醒：**
- 不能用其他语言（如 Python、MATLAB）的存储方式类比 C++，需严格区分行主序与列主序的区别。
- 二维数组在逻辑上是矩阵，但在物理内存中始终是一维线性序列。

**知识延伸：**
一维数组 \`a[n]\` 的地址计算公式为 \`base + i * sizeof(element)\`，二维数组可视为“一维数组的数组”，行主序存储使 \`a[i]\` 指向第 i 行的首元素地址。

**考点：** C++ 多维数组的内存布局（行主序存储）`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 12,
            type: "single",
            question: `分治算法中，通常将问题分解为（ ）个子问题。`,
            options: ["1", "2 或多个", "N (数据总量)", "不确定"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (2 或多个)**

**核心解析：**
分治算法（Divide and Conquer）的核心步骤为：
1. **Divide（分解）**：将原问题划分为若干个规模较小的子问题；
2. **Conquer（递归求解）**：递归地解决子问题；
3. **Merge（合并）**：将子问题的解合并为原问题的解。

“若干个”在经典定义中通常指 **2 个或更多**。典型例子包括：
- 归并排序：分解为左右两个子数组；
- 二分查找（分治版）：分解为左区间或右区间（2 个子问题路径选一）；
- 大整数乘法（Karatsuba）：分解为 2 个子问题；
- 快速傅里叶变换：分解为偶数位和奇数位两个子问题。

**选项逐项分析：**
- **A (1)**：❌ 错误。若只分解为 1 个子问题，算法本质变为“减治法”（Decrease and Conquer），如二分查找的循环版本，不能称为分治。
- **B (2 或多个)**：✅ 正确。分治要求问题被分解为多个子问题，且子问题之间相互独立（同构），这是合并解的基础。
- **C (N)**：❌ 错误。若分解为 N 个子问题（每个元素一个），通常导致指数级复杂度，除非子问题极简单（如计数排序的桶分配）。
- **D (不确定)**：❌ 错误。分治算法的子问题个数在算法设计阶段必须明确定义，不能运行时随意决定。

**易错提醒：**
- 分治与减治的区别在于子问题个数：分治 ≥ 2，减治 = 1。
- 不能混淆“分解为多个子问题”与“递归调用本身”，递归是手段，分治要求问题结构上可分解。

**知识延伸：**
分治算法的递归复杂度分析常用主定理（Master Theorem）：若 T(n) = aT(n/b) + O(n^d)，则时间复杂度取决于 a 与 b^d 的关系。

**考点：** 分治算法的基本定义与核心特征`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 13,
            type: "single",
            question: `在 C++ 中，下列哪个关键字用于抛出异常（ ）。`,
            options: ["throw", "catch", "try", "finally"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (throw)**

**核心解析：**
C++ 异常处理机制包含三个关键字，各司其职：
- \`try\`：定义一段可能抛出异常的代码块；
- \`throw\`：在检测到错误时显式抛出异常对象；
- \`catch\`：捕获并处理 \`try\` 块中抛出的异常。

抛出异常的基本语法为 \`throw 表达式;\`，表达式可以是任何类型（通常为 \`std::exception\` 的派生类）。

**选项逐项分析：**
- **A (throw)**：✅ 正确。\`throw\` 是 C++ 中用于显式抛出异常的唯一关键字。
- **B (catch)**：❌ 错误。\`catch\` 用于捕获异常并处理，不能抛出异常。
- **C (try)**：❌ 错误。\`try\` 用于界定可能抛出异常的代码范围，本身不抛出异常。
- **D (finally)**：❌ 错误。\`finally\` 是 Java、C#、Python 等语言的异常处理关键字，C++ 标准库中不存在此关键字。C++ 通过 RAII（资源获取即初始化）和析构函数确保资源释放。

**易错提醒：**
- C++ 没有 \`finally\` 关键字，不要与 Java/C#/Python 混淆。在 C++ 中，资源清理应使用析构函数或智能指针。
- \`throw\` 抛出的对象会被复制或移动到异常处理机制中，因此抛出对象应具有合理的拷贝/移动语义。

**知识延伸：**
C++ 异常处理的执行路径：\`try\` 块中 \`throw\` 后，程序沿调用栈向上传播，直到遇到匹配的 \`catch\` 子句；若未找到，程序调用 \`std::terminate()\` 终止。

**考点：** C++ 异常处理关键字的语义区分`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 14,
            type: "single",
            question: `下列哪种算法采用了递归思想实现（ ）。`,
            options: ["顺序查找", "二分查找", "归并排序", "以上都是"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (归并排序)**

**核心解析：**
递归的核心特征是函数调用自身来解决问题。逐项分析各算法的实现方式：

- **顺序查找**：通过一次 \`for\` 循环遍历数组，时间复杂度 O(N)，无递归调用。
- **二分查找**：可以用递归实现（每次将区间缩小一半），也可以用循环实现，两者都是合法实现方式，递归并非其唯一或必然的实现手段。
- **归并排序**：必须使用递归实现。其分治过程为：将数组不断二分直到子数组长度为 1（递归分解），再两两合并（回溯合并）。若不递归，则无法自然实现分治逻辑。

**选项逐项分析：**
- **A (顺序查找)**：❌ 错误。顺序查找的标准实现是线性扫描，无需递归调用。
- **B (二分查找)**：❌ 错误。二分查找可以用递归实现，但非必须；其核心是“减治”思想（问题规模减半），既可递归也可迭代。
- **C (归并排序)**：✅ 正确。归并排序是分治思想的典型应用，其“分-合”结构天然适合递归实现，递归是其定义层面的核心实现方式。
- **D (以上都是)**：❌ 错误。顺序查找和二分查找的非递归实现均完全合法，因此此项不成立。

**易错提醒：**
- 注意区分“可以用递归实现”与“采用了递归思想实现”——前者表示实现方式灵活，后者强调递归是必然或核心实现手段。
- 判断递归应用的题目，通常找“必须分而治之、层层分解”的场景。

**知识延伸：**
递归算法需满足两个条件：子问题与原问题同类、存在终止条件。递归实现的排序还有快速排序（原地分区递归）、堆排序（基于完全二叉树的递归建堆）。

**考点：** 递归思想在算法中的应用场景`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 15,
            type: "single",
            question: `GESP 4 级认证不包含下列哪个知识点（ ）。`,
            options: ["一维数组", "结构体", "动态规划", "函数嵌套调用"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (动态规划)**

**核心解析：**
GESP C++ 各级别考察的知识点范围由官方大纲明确规定。4 级考试的核心知识点包括：
- 基本语法与程序结构；
- 一维数组与简单二维数组；
- 结构体（自定义数据类型）；
- 函数定义、调用与嵌套调用；
- 递归基础；
- 基础算法（排序、二分查找等）；
- 异常处理基础。

**选项逐项分析：**
- **A (一维数组)**：❌ 错误。一维数组是 1-3 级的重点内容，4 级要求熟练掌握并在复杂题目中应用。
- **B (结构体)**：❌ 错误。结构体是 3-4 级的核心考点，要求理解结构体的定义、成员的访问以及在函数中的应用。
- **C (动态规划)**：✅ 正确。动态规划（Dynamic Programming）是 6 级及以上的高阶考点，涉及最优子结构与重叠子问题的识别，4 级大纲中不包含此内容。
- **D (函数嵌套调用)**：❌ 错误。函数嵌套调用是 4 级的核心考点，要求理解参数传递、返回值处理以及多层调用栈的概念。

**易错提醒：**
- GESP 各级别知识点的划分是考试重点，备考时应以官方大纲为依据，不要超纲学习但也不要遗漏前置知识点。
- 动态规划是 6 级内容，但其前置知识（如递归、数组）分布在 4-5 级，需循序渐进。

**知识延伸：**
GESP 4 级可视为 C++ 语法与基础算法的分水岭，5 级引入搜索（图、DFS、BFS），6 级引入动态规划与复杂数据结构。

**考点：** GESP C++ 等级考试大纲知识点分布`,
            tags: ["客观题", "单选题", "GESP4级"]
        },
        {
            id: 16,
            type: "judge",
            question: `在 C++ 中，下面代码可以正确定义指针和初始化指针：\`int *p = nullptr;\`。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
在 C++11 标准引入 \`nullptr\` 关键字后，空指针的初始化有了标准化的语法。\`int *p = nullptr;\` 的声明在语法上完全合法：
- \`int *p\` 声明了一个指向 \`int\` 类型的指针变量 \`p\`
- \`nullptr\` 是 C++11 标准的空指针字面量，类型为 \`std::nullptr_t\`，可以隐式转换为任何指针类型
- 指针被初始化为 \`nullptr\`，表示其不指向任何有效对象

相比传统的 \`int *p = NULL;\` 或 \`int *p = 0;\`，\`nullptr\` 具有更好的类型安全性，能够避免因重载产生的歧义。

**易错提醒：**
- 在 C++11 之前的代码中只能使用 \`NULL\` 或 \`0\`，\`nullptr\` 是较新的语法特性
- \`nullptr\` 不能用于初始化非指针类型，如 \`int x = nullptr;\` 是错误的

**知识延伸：**
GESP 三级曾考察过指针的基本概念，四级进一步引入 C++11 的现代特性。\`nullptr\` 的引入解决了 \`NULL\` 在函数重载中可能产生的二义性问题。

**考点：** C++11 空指针常量 \`nullptr\` 的用法`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 17,
            type: "judge",
            question: `一个函数必须在调用之前既声明又定义。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
函数在调用前**必须声明**，但**定义的位置可以灵活安排**。具体规则如下：

1. **声明（Declaration）必须在前**：编译器在遇到函数调用时，需要知道函数的返回类型和参数列表，以便检查调用是否合法。如果函数未声明，编译器会报错“identifier not found”。

2. **定义（Definition）可以延后**：函数的实现（定义）可以放在主调函数之后，只要在文件范围内已存在相应的声明即可。

3. **跨文件场景**：函数声明可以放在头文件中，定义放在另一个源文件，通过链接器在编译后合并。

例如，以下代码完全合法：
\`\`\`cpp
int add(int a, int b);  // 声明
int main() {
    return add(1, 2);  // 调用，发生在定义之前
}
int add(int a, int b) {  // 定义在调用之后
    return a + b;
}
\`\`\`

**易错提醒：**
- 声明和定义在口语中常被混淆，需区分“告诉编译器函数存在”（声明）和“给出函数具体实现”（定义）
- 如果函数定义放在调用之前，则该定义本身也充当声明，可以不单独写声明

**知识延伸：**
函数的声明与定义分离机制是模块化编程的基础，也是头文件（.h）与实现文件（.cpp）分离的理论依据。GESP 三级曾考察过函数的定义规范。

**考点：** 函数声明（Declaration）与函数定义（Definition）的区别`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 18,
            type: "judge",
            question: `函数参数可以通过值传递、引用传递和指针传递，这样函数内对参数的修改可以直接修改传入变量的值。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
题目说法过于笼统，没有区分不同参数传递方式的特性。函数参数传递共有三种方式：

1. **值传递（Pass by Value）**：
   \`\`\`cpp
   void f(int x) { x = 10; }
   int a = 5;
   f(a);
   // 此时 a 仍为 5，函数内的修改不影响外部变量
   \`\`\`
   值传递时，函数接收实参的副本，修改副本不会影响原变量。

2. **引用传递（Pass by Reference）**：
   \`\`\`cpp
   void f(int &x) { x = 10; }
   \`\`\`
   引用传递允许函数直接修改外部变量。

3. **指针传递（Pass by Pointer）**：
   \`\`\`cpp
   void f(int *p) { *p = 10; }
   \`\`\`
   通过解引用指针，函数可以间接修改外部变量。

题目所述“函数内对参数的修改可以直接修改传入变量的值”**仅适用于引用传递和指针传递**，不包括值传递，因此原题说法错误。

**易错提醒：**
- 引用传递通过 \`&\` 声明，指针传递通过 \`*\` 声明，两者形式不同但效果类似
- 传递数组名时，数组退化为指针，即使形式上是值传递，函数内修改数组元素也会影响外部

**知识延伸：**
参数传递机制是函数核心概念，GESP 三级侧重考查函数基本调用，四级在此基础上深入区分三种传递方式。实际开发中常通过引用传递避免大型结构体的复制开销。

**考点：** C++ 参数传递方式（值传递、引用传递、指针传递）的区别与效果`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 19,
            type: "judge",
            question: `int arr[3][] 是一个正确的二维数组的声明。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
在 C++ 中声明多维数组时，**除了第一维可以省略（根据初始化列表自动推导）外，其他所有维度的大小都必须显式指定**。

\`int arr[3][]\` 的问题在于：
- 第一维 \`3\` 表示数组有 3 行
- 第二维 \`[]\` 没有指定列数

编译器需要知道除第一维外所有维度的大小，以便计算每个元素的内存偏移地址。没有列数，编译器无法确定第 0 行第 1 列的元素相对于起始地址的偏移量，因此无法通过编译。

正确的二维数组声明应为：
\`\`\`cpp
int arr[3][4];  // 3行4列，共12个int元素
int arr[][4] = {{1,2,3,4}, {5,6,7,8}};  // 可省略第一维，由初始化列表推导为2
\`\`\`

**易错提醒：**
- 多维数组的第二维及以后各维都不能省略，这是语法硬性要求
- \`int arr[][]\` 两个维度都省略同样错误
- 二维数组在内存中是连续存储的，按行优先排列

**知识延伸：**
二维数组的本质是“数组的数组”，即外层数组的每个元素是内层数组。C 风格数组的大小必须在编译时确定，GESP 四级要求掌握其内存布局和寻址方式。

**考点：** C++ 多维数组的声明规范与内存寻址原理`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 20,
            type: "judge",
            question: `递推是一种通过已知的初始值和递推公式，逐步求解目标值的算法。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
递推（Recurrence / Iteration）是算法设计中的一种基本方法，其核心特征是：

1. **已知初始值（边界条件）**：递推需要明确的起始点，例如斐波那契数列的 \`f(0)=0, f(1)=1\`。

2. **递推公式（ recurrence relation）**：根据已求得的项，逐步计算后续项的规则。例如斐波那契数列的公式为 \`f(n) = f(n-1) + f(n-2)\`（n≥2）。

3. **逐步求解**：从初始值出发，按照公式顺序计算出 \`f(2), f(3), ...\` 直到目标值。

递推算法的时间复杂度通常为 O(n)，空间复杂度可以优化到 O(1)。它是理解动态规划的基础——动态规划本质上是对递推的优化（避免重复计算），通过存储中间结果实现。

**易错提醒：**
- 递推必须有明确的终止条件，否则会无限进行
- 递推公式必须正确，否则结果会偏离预期

**知识延伸：**
GESP 四级要求掌握基础算法思想，递推是高频考点，常见应用包括：斐波那契数列、阶乘、爬楼梯问题、排列组合计数等。后续学习的动态规划、记忆化搜索等高级算法都以递推为核心思想。

**考点：** 递推算法的基本原理、初始条件与递推公式`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 21,
            type: "judge",
            question: `冒泡排序的平均时间复杂度为 $O(N^2)$，但最优情况下为 $O(N)$。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
冒泡排序的核心操作是反复比较相邻元素并在必要时交换位置。在标准实现中，每轮遍历都会将当前未排序部分的最大元素“冒泡”到末尾，需要进行 N-1 轮遍历，每轮最多比较 N-i-1 次，因此时间复杂度为 O(N²)。

但冒泡排序存在一种经典优化策略：引入 \`swapped\` 标记变量。在每轮遍历开始前将其设为 \`false\`，若该轮中发生任何一次交换则改为 \`true\`。一轮遍历结束后检查 \`swapped\`，若仍为 \`false\`，说明本轮没有任何元素位置发生变化，数组已经整体有序，程序可以立即跳出所有循环。

在这种优化下，对于已经完全有序的数组（如 \`{1, 2, 3, 4, 5}\`），只需执行第一轮遍历（比较 N-1 次），确认没有交换后即可退出，总时间复杂度降为 O(N)。这正是题目所述“最优情况下为 O(N)”的来源。

**易错提醒：**
- 未优化版本的冒泡排序无论最好、平均、最坏情况都是 O(N²)，只有加了提前退出优化后才能达到 O(N)。
- O(N) 仅指比较次数，算法本质上仍需要 Ω(N) 的时间量级。

**知识延伸：**
快速排序的最好情况也是 O(N log N)，通过选取良好枢轴避免最坏 O(N²) 发生；归并排序则始终保持 O(N log N) 的稳定复杂度。

**考点：** 冒泡排序的算法优化与最好情况时间复杂度分析。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 22,
            type: "judge",
            question: `冒泡排序和插入排序都是稳定的排序算法。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
排序算法的稳定性定义为：若存在两个相等的元素 A 和 B（A 在前、B 在后），排序完成后 A 仍然排在 B 之前，则称该排序算法是稳定的。

冒泡排序的比较条件是 \`if (a[j] > a[j+1])\`，仅当左侧元素**严格大于**右侧元素时才交换。若两者相等（\`a[j] == a[j+1]\`），条件不满足，不发生交换，因此相等元素的相对位置始终保持不变。

插入排序同样使用严格小于条件：\`if (key < a[j])\`，将 \`key\` 插入已排序部分时，只在找到严格小于 \`key\` 的元素时才继续左移，遇到相等元素时停止，因此相等元素不会发生位置调换。

两者均能保证相等元素的原有顺序，是经典的稳定排序算法。

**易错提醒：**
- 稳定性的关键在于“相等时不交换”。若将条件改为 \`>=\` 或 \`<=\` 进行交换，算法就会变得不稳定。
- 稳定排序在处理多关键字排序（如先按成绩、再按学号排序）时尤为重要。

**知识延伸：**
常见的稳定排序包括冒泡排序、插入排序、归并排序；常见的不稳定排序包括选择排序、快速排序（通过随机化可降低最坏概率）、堆排序。

**考点：** 排序算法稳定性的定义与判定标准。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 23,
            type: "judge",
            question: `选择排序是稳定的排序算法。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
选择排序的基本流程是：在第 i 轮遍历中，从未排序部分 \`[i, n-1]\` 中找出最小元素的索引 \`min_idx\`，然后执行交换 \`swap(a[i], a[min_idx])\`。注意这里的交换是直接将 \`a[i]\`（当前位置的元素）与 \`a[min_idx]\`（最小元素的实际位置）进行交换，即使 \`min_idx\` 不等于 \`i\`，也会执行跨位置的元素互换。

以数组 \`{5a, 8, 5b, 2, 9}\` 为例（下标 a、b 用于区分两个相等元素，a 在前）：

- 第 0 轮：最小元素为 \`2\`，位于索引 3，交换 \`a[0]\` 和 \`a[3]\`，数组变为 \`{2, 8, 5b, 5a, 9}\`
- 此时原本在前的 \`5a\` 被换到了索引 3，而 \`5b\` 仍在索引 2，两者相对顺序发生颠倒

这说明选择排序无法保证相等元素的原有顺序，因此是不稳定的排序算法。

**易错提醒：**
- 不能仅凭“选择排序只选最小元素”这个简单描述判断稳定性，要看实际交换是否跨位置。
- 有一种“不稳定版选择排序”的误解是稳定的，实际上标准选择排序本身就是不稳定的。

**知识延伸：**
如果需要选择排序的稳定版本，可以改为每次将最小元素与左侧相邻元素逐步交换（类似冒泡），但这会增加时间复杂度，实践中更推荐使用稳定的插入排序代替。

**考点：** 选择排序的不稳定性分析与反例构造。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 24,
            type: "judge",
            question: `在 C++ 语言中，如果一个函数可能抛出异常，那么一定要在 try 子句里调用这个函数。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
C++ 的异常处理机制并非强制性的。\`try-catch\` 是捕获和处理异常的标准方式，但调用可能抛出异常的函数时，程序员有权选择是否用 \`try\` 块包裹。

如果不使用 \`try-catch\`，异常会沿着调用栈自动向上传播（称为异常传播）。当前函数执行完毕后，控制权返回到调用者，如果调用者也没有捕获该异常，异常会继续向更上层传播，直到遇到某个 \`catch\` 语句或到达顶层（通常为 \`main\` 函数），若仍未被捕获，程序将调用 \`std::terminate()\` 并异常终止。

以下代码完全合法且常见：
\`\`\`cpp
void func() {
    throw std::runtime_error("error"); // 不在 try 块中，直接抛出
}
void caller() {
    func(); // 也不在 try 块中，异常会向上传播
}
int main() {
    try {
        caller();
    } catch (const std::exception& e) {
        // 在此处统一处理
    }
}
\`\`\`

因此，“一定要在 try 子句里调用”这个说法过于绝对，错误在于忽视了异常的自动传播机制。

**易错提醒：**
- \`try-catch\` 的目的是集中处理异常，而非调用可能抛异常的函数的前置条件。
- 异常传播是 C++ 设计的一部分，合理利用可实现跨函数的错误报告机制。

**知识延伸：**
C++11 引入了 \`noexcept\` 关键字，标记不会抛出异常的函数。若在 \`noexcept\` 函数内部抛出异常，程序会直接调用 \`std::terminate()\`，这是对异常传播机制的补充约束。

**考点：** C++ 异常传播机制与 \`try-catch\` 的使用场景。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        {
            id: 25,
            type: "judge",
            question: `GESP 认证分为 1 到 8 级，数字越大难度越高。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
GESP（Graphical Programming Education Service Platform，图形化编程能力认证）是由中国计算机学会认证的编程能力等级考试。根据官方规范，GESP 共设置 8 个级别，级别编号从 1 到 8，数字越大代表难度越高。

具体而言：
- 1-4 级属于初级到中级，主要考察 C++ 语法基础、简单算法（如循环、条件判断、数组、函数）和基本数据结构知识。
- 5-8 级属于高级到专家级，引入更复杂的算法（如动态规划、贪心、图论）和高级数据结构（如树、图、哈希表），对程序设计能力和问题分析能力的要求显著提升。

题目表述与官方体系完全一致，因此判断为正确。

**易错提醒：**
- 注意 GESP 与其他编程认证体系（如 CSP-J/S）的编号规则可能不同，不可混淆。
- 8 级是最高级别，通过后具备参加 NOIP 等更高级别竞赛的基础能力。

**知识延伸：**
GESP 同时提供图形化编程（Scratch）和代码编程（C++、Python）两种考核路径，等级体系相互独立但难度递增的设计理念一致。

**考点：** GESP 认证体系级别设置。`,
            tags: ["客观题", "判断题", "GESP4级"]
        },
        ...programmingQuestions
    ]
};
