// 2024年3月 GESP C++ 二级真题 (第5次认证)
export const paperData = {
    id: '2024-03-l2',
    title: '2024年3月 GESP C++ 二级真题',
    level: 2,
    year: 2024,
    month: 3,
    session: 5,
    note: '2024年首场',
    timeLimit: 90 * 60,
    questions: [
        {
            id: 1,
            type: 'single',
            question: `下列流程图的输出结果是（ ）。`,
            options: ["5 12", "12 5", "5 5", "没有输出"],
            answer: 1,
            score: 2,
            explanation: "**答案：B (12 5)**\n\n**核心解析：**\n流程图先读入两个数，再按图中的赋值顺序把第二个数赋给 `a`，把第一个数赋给 `b`，最后输出 `a` 和 `b`。\n\n执行过程：\n- 初始输入可对应为 `x = 5`、`y = 12`。\n- 执行 `a = y` 后，`a = 12`。\n- 执行 `b = x` 后，`b = 5`。\n- 因此输出为 `12 5`。\n\n**考点：** 流程图阅读、变量赋值顺序",
            tags: ['流程图', '程序分析']
        },
        {
            id: 2,
            type: 'single',
            question: `以下选项中不符合 C++ 变量命名规则的是？（ ）`,
            options: ["student", "2_from", "_to", "Text"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (2_from)**

**核心解析：**
C++ 标识符（变量名、函数名等）的命名必须遵守以下规则：
- 第一个字符必须是字母（\`A-Z\`、\`a-z\`）或下划线（\`_\`）
- 后续字符可以是字母、数字或下划线
- 不能使用 C++ 关键字作为标识符

**选项逐项分析：**
- **A (student)**：❌ 错误。合法。以字母 \`s\` 开头，后续字符均为合法字符。
- **B (2_from)**：✅ 正确。不合法。以数字 \`2\` 开头，违反了"标识符必须以字母或下划线开头"的基本规则。
- **C (_to)**：❌ 错误。合法。以下划线 \`_\` 开头是允许的，这是许多系统变量常用的命名风格。
- **D (Text)**：❌ 错误。合法。以字母 \`T\` 开头，且 C++ 区分大小写，\`Text\` 与 \`text\` 是不同的标识符。

**易错提醒：**
- 认为"数字可以出现在变量名中"就意味着"可以开头"——这是混淆
- 误以为下划线开头的变量名不合法——实际上这是允许的

**知识延伸：**
虽然 \`_to\` 合法，但在实际编码中，应避免使用仅以下划线开头的标识符，因为以下划线开头的名称可能与编译器或标准库的内部标识符冲突。

**考点：** C++ 标识符命名规范`,
            tags: ['变量与标识符']
        },
        {
            id: 3,
            type: 'single',
            question: `以下选项中，不能用于表示分支结构的 C++ 保留字是？（ ）`,
            options: ["switch", "return", "else", "if"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (return)**

**核心解析：**
C++ 中的分支结构（选择结构）用于根据条件是否满足来选择执行哪段代码。分支结构相关的关键字包括：
- \`if\` / \`else if\` / \`else\`：条件分支
- \`switch\`：\`switch\` 语句是多分支选择结构
- \`? :\`（三目运算符）：条件表达式的简洁形式

而 \`return\` 的作用是结束当前函数的执行，并将控制权返回给调用者。它不属于任何一种分支结构的语法形式。

**选项逐项分析：**
- **A (switch)**：❌ 错误。可用于分支结构。\`switch\` 语句是经典的多分支选择结构，根据表达式的值跳转到对应的 \`case\` 分支执行。
- **B (return)**：✅ 正确。该项不符合题目限定，正是本题要选的项。不能用于分支结构。\`return\` 是函数返回语句，用于终止函数执行并返回值，与条件判断无关，不属于分支结构关键字。
- **C (else)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。可用于分支结构。\`else\` 是 \`if\` 语句的配套关键字，用于处理条件不满足时的情况，是分支结构的组成部分。
- **D (if)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。可用于分支结构。\`if\` 是条件分支的核心关键字，根据条件的真假决定是否执行对应代码块。

**易错提醒：**
- 误以为 \`return\` 可以作为分支选项——它是无条件返回，不做条件判断
- 将"退出循环的 \`break\`"与"退出函数的 \`return\`"混淆，两者作用范围不同

**知识延伸：**
\`return\` 语句有两种形式：有返回值 \`return 表达式;\` 和无返回值 \`return;\`（用于 \`void\` 类型函数）。

**考点：** C++ 流程控制关键字分类（分支结构 vs 非分支结构）`,
            tags: ['基础语法']
        },
        {
            id: 4,
            type: 'single',
            question: `下列说法错误的是？（ ）`,
            options: ["while 循环满足条件时不断运行", "if 语句通常用于执行条件判断", "在 C++ 中可以使用 foreach 循环", "break 和 continue 都可以用在 for/while 中"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (在 C++ 中可以使用 foreach 循环)**

**核心解析：**
逐一分析各选项说法的正确性：

| 选项 | 说法 | 判断 | 说明 |
|------|------|------|------|
| A | while 循环满足条件时不断运行 | ✅ 正确 | while 的执行机制就是"条件为真时重复执行循环体" |
| B | if 语句通常用于执行条件判断 | ✅ 正确 | if 是最基础的分支语句，专门用于条件判断 |
| C | 在 C++ 中可以使用 foreach 循环 | ❌ 错误 | C++ 标准语法中没有 \`foreach\` 这个关键字 |
| D | break 和 continue 都可以用在 for/while 中 | ✅ 正确 | 两者都是合法的循环控制语句 |

关于 C 选项的详细说明：C++11 引入了"基于范围的 for 循环"（Range-based for loop），其功能与许多语言中的 \`foreach\` 类似，但语法上仍然使用 \`for\` 关键字，不能称为 \`foreach\` 循环。

**选项逐项分析：**
- **A (while 循环满足条件时不断运行)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该说法本身正确。while 循环的条件判断机制决定了这一点。
- **B (if 语句通常用于执行条件判断)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该说法本身正确。if 是条件分支语句的典型代表。
- **C (在 C++ 中可以使用 foreach 循环)**：✅ 正确。该说法本身错误。C++ 中不存在 \`foreach\` 关键字，基于范围的 for 循环使用 \`for\` 而非 \`foreach\`。
- **D (break 和 continue 都可以用在 for/while 中)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。该说法本身正确。两者均可用于 for 和 while 循环控制。

**易错提醒：**
- 将"功能类似 foreach"误认为"语法上就是 foreach"
- 混淆"关键字名称"与"语法形式"，C++ 用 \`for\` 实现类似功能

**知识延伸：**
C++ 基于范围的 for 循环语法：\`for (auto x : container)\`，其中 \`auto\` 自动推导类型，\`:\` 表示"在...范围内遍历"。

**考点：** C++ 标准循环结构语法与关键字辨析`,
            tags: ['基础语法']
        },
        {
            id: 5,
            type: 'single',
            question: `下列 4 个表达式中，答案不是整数 8 的是？（ ）`,
            options: ["abs(-8)", "min(max(8, 9), 10)", "int(8.88)", "sqrt(64)"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (min(max(8, 9), 10))**

**核心解析：**
本题要求找出"答案不是整数 8"的表达式，需要逐一计算每个选项的结果：

**A. \`abs(-8)\`**
- \`abs()\` 为绝对值函数
- \`abs(-8) = 8\`
- 结果：**8**（整数）

**B. \`min(max(8, 9), 10)\`**（分步计算）
- 第一步：\`max(8, 9)\` → 取较大值 → 结果为 **9**
- 第二步：\`min(9, 10)\` → 取较小值 → 结果为 **9**
- 结果：**9**（不是 8）

**C. \`int(8.88)\`**
- \`int()\` 为强制类型转换，向零取整（截断小数部分）
- \`int(8.88) = 8\`
- 结果：**8**（整数）

**D. \`sqrt(64)\`**
- \`sqrt()\` 为平方根函数
- \`sqrt(64) = 8.0\`（\`double\` 类型）
- 数值上等于 8，但若严格要求"整数 8"，需注意类型差异
- 结果：**8**（数值等值于 8）

**选项逐项分析：**
- **A (abs(-8))**：❌ 错误。结果为 8，符合题目要求。
- **B (min(max(8, 9), 10))**：✅ 正确。该项不符合题目限定，正是本题要选的项。结果为 9，不是 8，正是本题要找的选项。
- **C (int(8.88))**：❌ 错误。结果为 8（截断小数部分），符合要求。
- **D (sqrt(64))**：❌ 错误。结果在数值上等于 8，符合要求。

**易错提醒：**
- 误以为 \`min(max(8, 9), 10)\` 中 \`max(8, 9)\` 结果是 8（应该取较大值）
- 忽略 \`int()\` 的截断特性，误以为 \`int(8.88)\` 结果不是 8
- 混淆 \`sqrt()\` 返回的 \`double\` 类型与整数 8 的严格区别

**知识延伸：**
常用数学函数：\`abs(x)\` 求绝对值、\`max(a, b)\` 取较大值、\`min(a, b)\` 取较小值、\`sqrt(x)\` 求平方根、\`pow(x, n)\` 求幂。

**考点：** C++ 常用数学函数（abs、max、min、sqrt）的返回值分析`,
            tags: ['基础语法']
        },
        {
            id: 6,
            type: 'single',
            question: "下面 C++ 代码执行后的输出是（ ）。\n```cpp\nint cnt = 0, x = 1;\nwhile (x < 10) {\n    x += 2;\n    if (x % 3 == 0) continue;\n    cnt++;\n}\ncout << cnt;\n```",
            options: ["1", "2", "3", "4"],
            answer: 2,
            score: 2,
            explanation: "**答案：C (3)**\n\n**核心解析：**\n本题要注意两点：`x += 2` 在判断 `x % 3 == 0` 之前执行，`continue` 只会跳过本轮剩余的 `cnt++`。\n\n逐轮追踪：\n- 初始：`cnt = 0, x = 1`\n- 第 1 轮：`x = 1 < 10`，先变为 3，`3 % 3 == 0`，执行 `continue`，`cnt` 仍为 0\n- 第 2 轮：`x = 3 < 10`，先变为 5，条件不成立，`cnt = 1`\n- 第 3 轮：`x = 5 < 10`，先变为 7，条件不成立，`cnt = 2`\n- 第 4 轮：`x = 7 < 10`，先变为 9，`9 % 3 == 0`，执行 `continue`，`cnt` 仍为 2\n- 第 5 轮：`x = 9 < 10`，先变为 11，`11 % 3 != 0`，`cnt = 3`\n- 下一次判断 `x = 11`，不满足 `x < 10`，循环结束\n\n所以最终输出 `3`。\n\n**易错提醒：**\n不要在 `x` 变成 9 后立刻结束循环；循环条件是在每一轮开始前判断的，`x = 9` 时仍会进入循环。\n\n**考点：** while 循环追踪、continue 语句",
            tags: ['循环', '程序分析']
        },
        {
            id: 7,
            sourceIntegrity: 'missing-figure',
            integrityNote: "原卷该题的代码/图以图片形式给出，官方 PDF 无文本层，本站无法提取；题干、选项与答案均取自官方原卷，但缺少代码部分，暂不足以独立作答。",
            type: 'single',
            question: `下面 C++ 代码执行后的输出是？（）

> ⚠️ 原卷此处配有代码或图片。官方 PDF 中该部分为图片，或其文本层与相邻试题混排、无法可靠切分，本站尚未还原。请对照原卷阅读代码。`,
            options: ["16", "36", "49", "81"],
            answer: 3,
            score: 2,
            explanation: `**答案：D（81）**

**依据**：官方真题 PDF 第 1 页答案表。本题题干与选项均已按官方原卷回填。

> ⚠️ 原卷该题的代码/图以图片形式给出，官方 PDF 无文本层，本站无法提取；题干、选项与答案均取自官方原卷，但缺少代码部分，暂不足以独立作答。因此本站不对该代码做推测性讲解，请对照原卷阅读代码。`,
            tags: ['循环']
        },
        {
            id: 8,
            type: 'single',
            question: `下面 C++ 代码执行后的输出是（ ）。\n\`\`\`cpp\nint n, a, m, i;\nn = 3, a = 5;\nm = (a - 1) * 2;\nfor (i = 0; i < n - 1; i++)\n    m = (m - 1) * 2;\ncout << m;\n\`\`\``,
            options: ["8", "26", "28", "30"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (26)**

**核心解析：**
for 循环迭代计算、复合算术表达式分析

**逐行推导变量状态：**
- 初始状态：n = 3, a = 5, m = (5-1)*2 = 8, i = 0
- 循环条件：i < n - 1，即 i < 2

**详细推导：**
1. 初始化：\`m = (a - 1) * 2 = (5 - 1) * 2 = 8\`
2. **第 1 次循环** (i = 0)：i < 2 → m = (8 - 1) * 2 = 14
3. **第 2 次循环** (i = 1)：i < 2 → m = (14 - 1) * 2 = 26
4. **第 3 次循环** (i = 2)：i < 2 为假，循环结束

最终 m = 26

**选项逐项分析：**
- **A (8)**：❌ 错误。该选项只考虑了初始值 m=8，忽略了 for 循环的两次迭代执行。
- **B (26)**：✅ 正确。该选项正确计算了初始值并经过两次迭代后的结果。
- **C (28)**：❌ 错误。若循环条件误用 i <= n-1（导致 3 次循环），或表达式计算有误（如 (m-1)*3），可能得到该结果。
- **D (30)**：❌ 错误。若循环条件误用 i <= n（导致 3 次循环），且表达式计算有误，可能得到该结果。

**易错提醒：**
- 看清 for 循环条件 i < n-1 的边界（n=3 时，实际执行 2 次循环：i=0, i=1）
- 复合赋值表达式中，先计算括号内，再乘以 2

**考点：** for 循环迭代计算、复合算术表达式分析`,
            tags: ['循环计算']
        },
        {
            id: 9,
            type: 'single',
            question: `下面 C++ 代码执行后的输出是（ ）。\n\`\`\`cpp\nint n, i, result;\nn = 81; i = 1, result = 1;\nwhile (i * i <= n) {\n    if (n % (i * i) == 0) result = i * i;\n    i += 1;\n}\ncout << result;\n\`\`\``,
            options: ["9", "27", "81", "1"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (81)**

**核心解析：**
循环逻辑分析、完全平方因子的性质

**逐行推导变量状态：**
- 初始状态：n = 81, i = 1, result = 1
- 循环条件：i * i <= n，即 i <= 9

**详细推导：**
循环寻找 n = 81 的最大完全平方因子 i²。

1. **i = 1**：1² = 1 ≤ 81，81 % 1 = 0，result = 1
2. **i = 2**：2² = 4 ≤ 81，81 % 4 ≠ 0，result 保持为 1
3. **i = 3**：3² = 9 ≤ 81，81 % 9 = 0，result = 9
4. **i = 4**：4² = 16 ≤ 81，81 % 16 ≠ 0，result 保持为 9
5. **i = 5**：5² = 25 ≤ 81，81 % 25 ≠ 0，result 保持为 9
6. **i = 6**：6² = 36 ≤ 81，81 % 36 ≠ 0，result 保持为 9
7. **i = 7**：7² = 49 ≤ 81，81 % 49 ≠ 0，result 保持为 9
8. **i = 8**：8² = 64 ≤ 81，81 % 64 ≠ 0，result 保持为 9
9. **i = 9**：9² = 81 ≤ 81，81 % 81 = 0，result = 81
10. **i = 10**：10² = 100 > 81，循环结束

最终 result = 81

**选项逐项分析：**
- **A (9)**：❌ 错误。该选项是 81 的不完全平方因子（3²=9），但代码会继续查找直到找到更大的完全平方因子。
- **B (27)**：❌ 错误。27 不是完全平方数（√27 ≈ 5.2），不可能是 i*i 的形式。
- **C (81)**：✅ 正确。该选项是 81 本身（9²），是 81 的最大完全平方因子。
- **D (1)**：❌ 错误。1 是初始值，但代码会不断更新 result 为更大的完全平方因子，最终不会是 1。

**易错提醒：**
- 循环条件 i*i <= n 使用的是 i*i，而非 i
- result 会不断被更新为更大的完全平方因子，因此最终 result 是满足条件的最大值

**考点：** 循环逻辑分析、完全平方因子的性质`,
            tags: ['循环']
        },
        {
            id: 10,
            type: 'single',
            question: `下面 C++ 代码执行后的输出是（ ）。\n\`\`\`cpp\nfor (int i = 0; i < 4; i++) {\n    for (int j = 0; j <= i; j++) {\n        cout << j;\n    }\n}\n\`\`\``,
            options: ["0010120123", "0123", "010120123", "0012012301234"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (0010120123)**

**核心解析：**
嵌套循环的执行顺序与输出累加

**逐行推导变量状态：**
- 外层循环：i = 0, 1, 2, 3
- 内层循环：j = 0 到 i（含 i）

**详细推导：**

**外层 i = 0：**
- 内层 j = 0：j <= 0 ✓ → 输出 "0"
- 内层循环结束

**外层 i = 1：**
- 内层 j = 0：j <= 1 ✓ → 输出 "0"
- 内层 j = 1：j <= 1 ✓ → 输出 "1"
- 内层循环结束

**外层 i = 2：**
- 内层 j = 0：j <= 2 ✓ → 输出 "0"
- 内层 j = 1：j <= 2 ✓ → 输出 "1"
- 内层 j = 2：j <= 2 ✓ → 输出 "2"
- 内层循环结束

**外层 i = 3：**
- 内层 j = 0：j <= 3 ✓ → 输出 "0"
- 内层 j = 1：j <= 3 ✓ → 输出 "1"
- 内层 j = 2：j <= 3 ✓ → 输出 "2"
- 内层 j = 3：j <= 3 ✓ → 输出 "3"
- 内层循环结束

**输出拼接：** 0 + 01 + 012 + 0123 = 0010120123

**选项逐项分析：**
- **A (0010120123)**：✅ 正确。该选项完整输出了所有内层循环的 j 值，格式完全匹配。
- **B (0123)**：❌ 错误。该选项只包含了最后一轮外层循环（i=3）的输出，忽略了 i=0,1,2 时的输出。
- **C (010120123)**：❌ 错误。该选项的输出序列不连续，缺少了部分 j 值的输出。
- **D (0012012301234)**：❌ 错误。该选项包含了多余的 "12" 和 "34" 部分，且最后多了 "4"，与代码逻辑不符。

**易错提醒：**
- 嵌套循环中，外层循环的每次迭代都会执行完整的内层循环
- j 的取值范围是 0 ≤ j ≤ i，不是 j < i
- 输出是累加的，不是替换的

**考点：** 嵌套循环的执行顺序与输出累加`,
            tags: ['嵌套循环']
        },
        {
            id: 11,
            type: 'single',
            question: `下面 C++ 代码用于实现九九乘法表，横线处应填入（ ）。\n\`\`\`cpp\nfor (int i = 1; i <= 9; i++) {\n    for (int j = 1; j <= ________; j++) {\n        cout << j << "*" << i << "=" << i * j << "\t";\n    }\n    cout << endl;\n}\n\`\`\``,
            options: ["9", "10", "i-1", "i"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (i)**

**核心解析：**
九九乘法表的规律是：第 i 行（i 从 1 到 9）只打印到 j = i，即打印 j×i = ? 的形式。内层循环 \`j\` 的上界应随外层循环变量 \`i\` 变化，因此填入 \`i\`。

逐行推导验证：
- i=1：内层 j 从 1 到 1，打印 "1*1=1"（1 行 1 列）
- i=2：内层 j 从 1 到 2，打印 "1*2=2 2*2=4"（2 行 2 列）
- i=9：内层 j 从 1 到 9，打印 "1*9=9 2*9=18 ... 9*9=81"（9 行 9 列）
完整输出正是标准的下三角九九乘法表。

**选项逐项分析：**
- **A 9**：❌ 错误。若填 9，则每行都打印 j=1 到 9，即 "1*1=1 2*1=2 ... 9*1=9"，变成列方向的乘法，与九九乘法表格式不符。
- **B 10**：❌ 错误。填 10 会导致 j<=10，多打印一列（如 i=1 时多打印 10*1=10），输出变成 10×9 的矩形，破坏了九九乘法表的下三角结构。
- **C i-1**：❌ 错误。当 i=1 时，i-1=0，内层循环不执行，第 1 行空白；i=2 时 j 只到 1，只打印 "1*2=2"，丢失 "2*2=4"。行首数据缺失，不符合九九乘法表。
- **D i**：✅ 正确。j 的上界随 i 递增，确保每行从 1 打印到 i，形成标准下三角九九乘法表。

**易错提醒：**
- 误以为九九乘法表每行都要打印到 9（选 A），忽略了"第 i 行只到 i 列"的定义。
- 对 \`i-1\` 的边界情况（i=1 时循环不执行）未做验证。

**知识延伸：**
九九乘法表是嵌套循环的经典应用，内层循环上界依赖外层循环变量的模式称为"可变上界循环"，常用于打印三角形、数塔等图案。

**考点：** 嵌套循环、可变上界循环、九九乘法表结构`,
            tags: ['程序分析']
        },
        {
            id: 12,
            type: 'single',
            question: `一个数的所有数字倒序排列后保持不变，就是回文数。横线处应填写（ ）。\n\`\`\`cpp\nwhile (n > 0) {\n    a = ________;\n    n /= 10;\n}\n\`\`\``,
            options: ["10 * a + n % 10", "a + n % 10", "10 * a + n / 10", "a + n / 10"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (10 * a + n % 10)**

**核心解析：**
代码实现的是"数位反转"算法：依次取出 \`n\` 的最低位数字，拼接到 \`a\` 的末尾。

逐轮推导（以 n=123 为例，初始 a=0）：
- 第 1 轮：n=123，a = 10*0 + 123%10 = 10*0 + 3 = 3，n /= 10 → n=12
- 第 2 轮：n=12，a = 10*3 + 12%10 = 30 + 2 = 32，n /= 10 → n=1
- 第 3 轮：n=1，a = 10*32 + 1%10 = 320 + 1 = 321，n /= 10 → n=0
循环结束，a=321，正好是 123 的反转。

**选项逐项分析：**
- **A 10 * a + n % 10**：✅ 正确。每轮将已累积的 \`a\` 左移一位（乘 10），加上 \`n\` 的当前最低位（n%10），是标准的数位反转递推式。
- **B a + n % 10**：❌ 错误。缺少"乘 10 左移"操作，例如第 2 轮变成 a=3+2=5，无法累积成正确的高位数。
- **C 10 * a + n / 10**：❌ 错误。\`n/10\` 取的是十位及以上数字（不是最低位），且未加上当前个位，导致数字错位或丢失。
- **D a + n / 10**：❌ 错误。同时缺少乘 10 左移和取最低位两个关键操作，结果完全错误。

**易错提醒：**
- 混淆 \`n % 10\`（取个位）与 \`n / 10\`（取高位），数位反转必须用 \`% 10\` 取当前最低位。
- 忘记 \`a\` 需要乘 10 左移，导致各位数字叠加在同一位置。

**知识延伸：**
数位反转算法的核心是"左移叠加"：\`a = a * 10 + digit\`。类似结构也用于求二进制位数和、数字各位求和等问题。

**考点：** 数位反转算法、模运算与整除运算的区分、变量累积模式`,
            tags: ['程序分析', '回文数']
        },
        {
            id: 13,
            type: 'single',
            question: `给定两个整数 N 与 M，打印栅栏图形。横线处填入（ ）。\n\`\`\`cpp\nfor (int i = 0; i < N; i++) {\n    for (int j = 0; j < M; j++) cout << "-";\n    ________;\n}\n\`\`\``,
            options: ["cout << '+' << endl;", "cout << '+' << ' ' << endl;", "cout << '+';", "cout << '+' << ' ';"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (cout << '+';)**

**核心解析：**
代码打印 N 行栅栏，每行由 M 个 "-" 字符组成，末尾需要加 "+" 标记边界。

逐行推导（以 N=3, M=5 为例）：
- 外层循环第 1 次：内层打印 "-----"，然后应输出 "+" 换行，即 "+" 出现在行末而非行首
- 外层循环第 2 次：同样打印 "-----" 后跟 "+"
- 外层循环第 3 次：打印 "-----" 后跟 "+"

最终输出格式：
\`\`\`
-----+
-----+
-----+
\`\`\`
（每行末尾 + 后换行）

**选项逐项分析：**
- **A cout << '+' << endl;**：❌ 错误。虽然输出 '+' 和换行，但行末会多一个空格（因为 M 个 '-' 后面没有空格，而 endl 会换行），导致格式不紧凑。
- **B cout << '+' << ' ' << endl;**：❌ 错误。'+' 后多输出一个空格，每行变成 "----- + "，末尾有多余空格，不符合紧凑的栅栏样式。
- **C cout << '+';**：✅ 正确。只输出 '+' 字符（无空格），直接进入下一次外层循环的换行（cout << endl），形成 "-----+" 的紧凑格式。
- **D cout << '+' << ' ';**：❌ 错误。'+' 后有空格，导致每行末尾多一个空格，且没有换行符 endl，所有行会连成一行。

**易错提醒：**
- 误以为要在 '+' 后加空格（D），忽略了题目要求紧凑格式。
- 混淆 endl（换行）与分号的作用：选项 A/B 用了 endl 但多输出了空格。

**知识延伸：**
图形打印题要注意两点：(1) 每行内容是否紧凑（无多余空格）；(2) 换行时机是否正确。本题的换行由外层循环末尾的 \`cout << endl\` 控制，内层只需输出 '+'。

**考点：** 嵌套循环、字符画输出格式、换行与分号的配合`,
            tags: ['图形绘制']
        },
        {
            id: 14,
            type: 'single',
            question: `华为手表上跑的是鸿蒙，这个鸿蒙是（ ）。`,
            options: ["小程序", "计时器", "操作系统", "神话人物"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (操作系统)**

**核心解析：**
鸿蒙（HarmonyOS）是华为公司自主研发的操作系统，主要应用于智能手机、平板、智能手表、智能家居等设备。它并非小程序、计时器或神话人物。

**选项逐项分析：**
- **A 小程序**：❌ 错误。小程序是运行在操作系统之上的轻量级应用（如微信小程序），鸿蒙本身是操作系统，不是小程序。
- **B 计时器**：❌ 错误。计时器是一种功能或设备，鸿蒙是完整的操作系统平台，具备进程管理、内存管理、设备驱动等功能，不只是计时功能。
- **C 操作系统**：✅ 正确。鸿蒙是华为研发的操作系统，支持分布式架构，可在不同设备间协同工作。
- **D 神话人物**：❌ 错误。虽然"鸿蒙"一词来源于中国神话中的宇宙初始状态，但这里的"鸿蒙"是操作系统品牌名，不是指神话人物。

**易错提醒：**
- "鸿蒙"这个词确实源自中国神话，但作为科技产品名称时，它指的是操作系统，不能望文生义选 D。
- 注意区分操作系统（平台软件）与运行其上的应用程序（小程序等）。

**知识延伸：**
常见的操作系统包括 Windows、macOS、Linux、Android（安卓）、iOS、鸿蒙等。操作系统是管理计算机硬件与软件资源的系统软件，是应用程序运行的基础平台。

**考点：** 计算机基础知识、操作系统概念、鸿蒙系统认知`,
            tags: ['操作系统']
        },
        {
            id: 15,
            type: 'single',
            question: "链环重 3, 4, 6 克，每 12 克一组。链依次为 G3, G4, G6...重复。说法正确的是（ ）。",
            options: ["如果 N 输入 10，输出 36", "如果 N 输入 5，输出 15", "如果 N 输入 9，输出 30", "以上说法均不对"],
            answer: 2,
            score: 2,
            explanation: "**答案：C (如果 N 输入 9，输出 30)**\n\n**核心解析：**\n本题应把每个选项给出的 `N` 代入题目中的链环规则或对应程序，比较实际输出。\n\n逐项判断：\n- **A**：`N = 10` 时输出不是 36。\n- **B**：`N = 5` 时输出不是 15。\n- **C**：`N = 9` 时输出为 30，符合题意。\n- **D**：因为 C 正确，所以“以上说法均不对”错误。\n\n**易错提醒：**\n遇到这类具体输入判断题，不要只看数字是否接近，要逐项代入规则或程序执行。\n\n**考点：** 周期规律、代入验证",
            tags: ['循环', '数学逻辑']
        },
        {
            id: 16,
            type: 'judge',
            question: `执行代码 double s = 18.5; int t = int(s) + 10; 之后，cout << t 的结果为 28.5。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. 语句 \`double s = 18.5;\` 定义双精度浮点变量 \`s\` 并赋值为 18.5。
2. \`int(s)\` 执行显式类型转换，将 18.5 转换为整型，遵循“截断”规则，小数部分直接舍弃，结果为整数 18。
3. 计算 \`int(s) + 10\` → \`18 + 10\` → 28。
4. 变量 \`t\` 的类型为 \`int\`，只能存储整数值，28.5 不是整数，无法存入 \`t\` 中。
5. \`cout << t\` 输出的是整数 \`28\`，而非 \`28.5\`。

**易错提醒：**
- 类型转换 \`int(s)\` 只取整数部分，不会进行四舍五入。
- \`cout\` 输出的是变量 \`t\` 的实际值，而 \`t\` 是整型变量，输出格式必然为整数。

**知识延伸：**
C++ 中强制类型转换分为显式（如 \`int(s)\`）和隐式两种，整数截断是显式转换的默认行为，与数学中的“四舍五入”概念不同。

**考点：** 强制类型转换规则、整型变量的存储特性`,
            tags: ['数据类型']
        },
        {
            id: 17,
            type: 'judge',
            question: `Xyz，xyz，xyZ 是三个不同的变量。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. C++ 语言对标识符采用区分大小写的规则（Case-sensitive）。
2. 标识符 \`Xyz\`、\`xyz\`、\`xyZ\` 三个名称中，每个字母的大小写位置均不完全相同：
   - \`Xyz\` 首字母大写，其余小写
   - \`xyz\` 三个字母全部小写
   - \`xyZ\` 前两个字母小写，末字母大写
3. 编译器将这三个标识符视为三个完全不同的符号，各自独立占用命名空间。

**易错提醒：**
- 初学者常误以为仅部分字母不同就是同一变量，实际上只要大小写有任何差异，就是不同的标识符。
- C++ 与某些语言（如某些脚本语言）不同，后者可能不区分大小写。

**知识延伸：**
C++ 标准规定标识符由字母、数字和下划线组成，且第一个字符不能是数字。大小写敏感性是 C++ 的基本特性，这一规则同样适用于函数名、类名等所有标识符。

**考点：** C++ 标识符的大小写敏感性`,
            tags: ['变量与标识符']
        },
        {
            id: 18,
            type: 'judge',
            question: `cout << (8 < 9 < 10) 的输出结果为 true。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. 表达式 \`(8 < 9 < 10)\` 包含两个关系运算符，运算从左至右结合。
2. 第一步计算 \`8 < 9\`，结果为布尔值 \`true\`。
3. 第二步计算 \`true < 10\`：在 C++ 中，布尔值 \`true\` 在数值比较语境下自动转换为整数 \`1\`，因此等价于 \`1 < 10\`，结果仍为 \`true\`（对应整数值为 \`1\`）。
4. \`cout\` 在输出布尔表达式结果时，默认以整数形式显示——\`true\` 输出为 \`1\`，\`false\` 输出为 \`0\`。
5. 因此实际输出为整数 \`1\`，而非单词 \`true\`。

**易错提醒：**
- C++ 中 \`cout\` 输出布尔值不会打印 "true" 或 "false" 字符串（需使用 \`boolalpha\` 格式控制符才会输出单词形式）。
- 链式比较 \`8 < 9 < 10\` 在 C++ 中并非数学意义上的“9 在 8 和 10 之间”的含义。

**知识延伸：**
C++ 关系运算符的返回类型是布尔值，布尔值到数值的隐式转换规则是 GESP 二级的常见考点。若需输出 "true"/"false" 字符串，应使用 \`cout << boolalpha << (8 < 9 < 10);\`。

**考点：** 关系运算符结合性、布尔值与整数的隐式转换、\`cout\` 的布尔输出格式`,
            tags: ['逻辑运算']
        },
        {
            id: 19,
            type: 'judge',
            question: `for (i = 0; i < 100; i += 2); 语句中变量 i 的取值范围是 0 到 99。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. for 循环 \`for (i = 0; i < 100; i += 2)\` 的迭代过程如下：
   - 初始：\`i = 0\`，满足 \`i < 100\`，进入循环体
   - 第1次迭代后：\`i += 2\` → \`i = 2\`
   - 第2次迭代后：\`i += 2\` → \`i = 4\`
   - ……依次类推
2. i 在循环体内实际取过的值为：0, 2, 4, 6, ..., 98。
3. 当 \`i\` 增长至 100 时，\`i < 100\` 条件不满足，循环终止，100 是终止后的值，不属于循环体执行期间的范围。
4. 数值 99 在整个循环过程中从未被 \`i\` 赋值过。

**易错提醒：**
- 循环条件 \`i < 100\` 控制的是“进入循环体”的条件，而非“循环体取值”的范围上界。
- 步长为 2 时，i 只能取偶数值，不会出现奇数 99。

**知识延伸：**
分析 for 循环取值范围的口诀：初值决定首个元素，步长决定增量，终止条件决定边界。对于 \`i += 2\` 且 \`i < 100\`，最大值是小于 100 的最大偶数 98。

**考点：** for 循环的迭代规律、循环变量取值范围的判定`,
            tags: ['循环']
        },
        {
            id: 20,
            type: 'judge',
            question: `C++ 中 cout << float(2022) 与 cout << float('2022') 运行后的输出结果均为 2022。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. \`float(2022)\` 将整数 2022 显式转换为 float 类型，结果为浮点数 2022.0，\`cout\` 输出 2022（浮点数格式通常不显示小数点后的零，除非特别设置精度）。
2. \`float('2022')\` 中的 \`'2022'\` 是非法的字符字面量。在 C++ 中，单引号 \`'\` 用于包围单个字符常量，如 \`'a'\`、\`'2'\`。单引号内只能包含恰好一个字符。
3. 将多个字符（如 \`'2022'\`）放入单引号中，会产生编译错误（不符合语法规则）。若编译器允许通过，通常也只取最后若干字节，行为不可预测。
4. 因此第二个 \`cout\` 语句根本无法正常执行，更谈不上输出 2022。

**易错提醒：**
- 字符常量用单引号 \`' '\`，字符串常量用双引号 \`" "\`。
- 单引号内只能有单个字符，字符串必须使用双引号。

**知识延伸：**
GESP 二级常考的字面量区分点：单个字符用单引号（如 \`'A'\`、\`'5'\`），字符串（零个或多个字符）用双引号（如 \`"A"\`、\`"hello"\`、\`""\`）。混淆引号类型是常见错误。

**考点：** 字符字面量与字符串字面量的语法区别`,
            tags: ['数据类型']
        },
        {
            id: 21,
            type: 'judge',
            question: `已知 'A' 的 ASCII 码值为 65，表达式 int('C') + abs(-5.8) 的值为 72.8。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. \`int('C')\` 获取字符 'C' 的 ASCII 码值。已知 'A' 为 65，'C' 与 'A' 相隔 2 个位置，因此 \`int('C') = 65 + 2 = 67\`。
2. \`abs(-5.8)\` 是 \`<cmath>\` 中的绝对值函数，返回负数的正值，即 \`abs(-5.8) = 5.8\`。
3. 将两部分相加：\`67 + 5.8 = 72.8\`，与题目所述结果一致。

**易错提醒：**
- 部分考生可能混淆字符的 ASCII 码顺序，误以为 'B'=66 后 'C' 应该是 68，但实际上 ASCII 码是连续递增的。
- \`int('C')\` 返回的是整数值 67，而非字符 'C' 本身，这一点容易与字符常量本身混淆。

**知识延伸：**
常用 ASCII 码应熟记：\`'A'(65)\`、\`'Z'(90)\`、\`'a'(97)\`、\`'z'(122)\`、\`'0'(48)\`。大写字母与小写字母的 ASCII 码相差 32。

**考点：** 字符 ASCII 码计算、abs 绝对值函数运算`,
            tags: ['字符与ASCII']
        },
        {
            id: 22,
            type: 'judge',
            question: `bool 函数用于将给定参数或表达式转换为布尔类型。语句 bool(-1) 返回的是 false 值。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
在 C++ 中，数值类型向布尔类型的转换遵循以下规则：零值转换为 \`false\`，非零值（包括正数和负数）转换为 \`true\`。\`-1\` 是非零值，因此 \`bool(-1)\` 的结果为 \`true\`（实际内部值为 1），而非 \`false\`。

**易错提醒：**
- 考生容易误以为负数会被当作"假"处理，但 C++ 的布尔转换只看是否为零，不看正负。
- \`bool\` 函数的返回值是布尔值 \`true\` 或 \`false\`，而非整数 0 或 1（尽管在条件判断中可等价使用）。

**知识延伸：**
\`bool\` 函数的转换规则同样适用于其他非布尔类型向布尔的转换，例如 \`bool(0.0)\` 返回 \`false\`，而 \`bool(3.14)\` 返回 \`true\`。

**考点：** 数值向布尔类型的隐式转换规则`,
            tags: ['数据类型']
        },
        {
            id: 23,
            type: 'judge',
            question: `如果变量 a 的值使得 C++ 表达式 sqrt(a) == abs(a) 成立，则 a 的值必须为 0。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
设 \`a = 1\` 进行验证：
- \`sqrt(1) = 1.0\`（平方根函数）
- \`abs(1) = 1\`（绝对值函数）
- 比较 \`1.0 == 1\`，由于 C++ 会进行隐式类型转换，结果为 \`true\`。

因此，当 \`a = 1\` 时等式同样成立，说明 \`a\` 的值不一定必须为 0。原命题的"必须"一词过于绝对，故判断为错误。

**易错提醒：**
- 考生可能只考虑 \`a = 0\` 的情况，忽略 \`a = 1\` 同样满足等式。
- 需要注意 \`sqrt(a)\` 返回的是 \`double\` 类型，而 \`abs(a)\` 对整数参数返回 \`int\` 类型，两者比较时会发生类型提升，但结果仍为 \`true\`。

**知识延伸：**
实际上，满足 \`sqrt(a) == abs(a)\` 的条件为 \`a ≥ 0\`（因为开平方根的结果非负，与绝对值结果相等）。当 \`a = 1\` 时两边均为 1；当 \`a = 0\` 时两边均为 0；当 \`a = 4\` 时 \`sqrt(4) = 2\`，\`abs(4) = 4\`，不相等。

**考点：** 数学函数运算与等式成立的边界条件分析`,
            tags: ['数学函数']
        },
        {
            id: 24,
            type: 'judge',
            question: `具有为设备分配 IP 地址并接入 WIFI 功能的盒子具有路由器功能。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
路由器的核心功能之一是通过 DHCP 协议为接入局域网的各种设备（如手机、电脑、物联网设备等）动态分配 IP 地址，从而实现网络互联。"具有为设备分配 IP 地址并接入 WIFI 功能的盒子"正是实现了路由器的这一关键功能：
- **IP 地址分配**：通过内置 DHCP 服务器为 LAN 侧的设备分配私有 IP 地址。
- **WIFI 接入**：提供无线接入点（AP）功能，使设备能够通过无线方式连接网络。

具备这两项功能的设备在功能定义上覆盖了路由器的基本职责，因此描述正确。

**易错提醒：**
- 考生可能将路由器与交换机、AP（单纯无线接入点）混淆。交换机不具备 IP 分配功能；单纯 AP 需配合上级路由使用。
- 部分"智能网关"或"路由器盒子"功能更丰富（如防火墙、QoS），但核心功能仍是路由。

**知识延伸：**
家庭路由器通常集成了多种功能：路由（Routing）、NAT（网络地址转换）、DHCP 服务器、无线 AP 等，是一个小型的网络功能集合设备。

**考点：** 计算机网络基础知识、路由器功能定义`,
            tags: ['网络基础']
        },
        {
            id: 25,
            type: 'judge',
            question: `任何一个 for 循环都可以转化为等效的 while 循环。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
\`for\` 循环的标准形式为 \`for(初始化; 条件; 更新) { 循环体; }\`，其执行流程完全可以用 \`while\` 循环等价表达。转化规则如下：

\`\`\`
for (init; cond; step) {
    body;
}
\`\`\`
等价于：
\`\`\`
init;
while (cond) {
    body;
    step;
}
\`\`\`

两种循环结构在表达能力和执行效果上是完全对等的，任何 \`for\` 循环都可以按上述规则改写为等效的 \`while\` 循环，反之亦然。

**易错提醒：**
- 虽然两者可以等价转化，但实际编程中应根据语义选择：循环次数明确时用 \`for\`，循环条件依赖变量变化时用 \`while\`。
- 转化时需注意更新表达式 \`step\` 在 \`while\` 版本中应放在循环体末尾，以确保在每次迭代结束后执行。

**知识延伸：**
GESP 二级要求掌握 \`for\` 和 \`while\` 循环的语法及使用场景。除了相互转化，还需注意 \`do-while\` 循环（先执行后判断，至少执行一次）与前两者的区别。

**考点：** for 循环与 while 循环的等价转化`,
            tags: ['循环']
        }
    ],
    programmingQuestions: [
        {
            id: 26,
            type: 'programming',
            title: "乘法问题",
            samples: [
                {
                    input: `2\n3\n5`,
                    output: `15`
                },
                {
                    input: `3\n100\n100\n100`,
                    output: `1000000`
                },
                {
                    input: `4\n100\n100\n100\n2`,
                    output: `>1000000`
                }
            ],
            question: `
# [GESP202403 二级] 乘法问题

## 题目描述

小 A 最初刚刚学习了乘法，为了帮助他练习，我们给他若干个正整数，并要求他将这些数乘起来。

对于大部分题目，小 A 可以精确地算出答案，不过，若这些数的乘积超过 $10^6$，小 A 就不会做了。

请你写一个程序，告诉我们小 A 会如何作答。

## 输入格式

第一行一个整数 $n$，表示正整数的个数。

接下来 $n$ 行，每行一个整数 $a$。小 A 需要将所有的 $a$ 乘起来。

## 输出格式

输出一行，如果乘积超过 $10^6$，则输出 \`>1000000\`；否则输出所有数的乘积。
`,
            score: 25,
            explanation: `**解析：**
本题考察基础模拟与数值溢出监控。
1. **输入处理**：首先读取整数个数 $n$。
2. **累乘过程**：
   - 初始化一个长整型变量 \`result = 1\`。
   - 使用循环依次读取每个数并相乘。
3. **溢出监控**：
   - 题目要求的阈值是 $10^6$。
   - **关键技巧**：在循环内部进行判断。一旦 \`result\` 超过了 $1,000,000$，就可以立即停止计算并标记为溢出。
4. **输出处理**：根据是否溢出的标记，输出对应的结果。

**核心逻辑提示：**
\`\`\`cpp
long long res = 1;
bool overflow = false;
for (int i = 0; i < n; i++) {
    int a; cin >> a;
    res *= a;
    if (res > 1000000) {
        overflow = true;
        // 注意：即使 overflow 为真，通常也要读完剩下的输入
    }
}
if (overflow) cout << ">1000000";
else cout << res;
\`\`\``,
            tags: ['编程题', '模拟', '数值范围'],
            template: "#include <iostream>\nusing namespace std;\nint main() {\n    // 在此编写代码\n    return 0;\n}",
            referenceCode: "#include <iostream>\nusing namespace std;\nint main() {\n    int n;\n    cin >> n;\n    long long res = 1;\n    bool over = false;\n    for (int i = 0; i < n; i++) {\n        int a;\n        cin >> a;\n        res *= a;\n        if (res > 1000000) over = true;\n    }\n    if (over) cout << \">1000000\" << endl;\n    else cout << res << endl;\n    return 0;\n}"
        },
        {
            id: 27,
            type: 'programming',
            title: "小杨的日字矩阵",
            samples: [
                {
                    input: `5`,
                    output: `|---|\n|xxx|\n|---|\n|xxx|\n|---|`
                },
                {
                    input: `7`,
                    output: `|-----|\n|xxxxx|\n|xxxxx|\n|-----|\n|xxxxx|\n|xxxxx|\n|-----|`
                }
            ],
            question: `
# [GESP202403 二级] 小杨的日字矩阵

## 题目描述

小杨想要构造一个 $N \times N$ 的日字矩阵（$N$ 为奇数），具体来说，这个矩阵共有 $N$ 行，每行 $N$ 个字符，其中最左列、最右列都是 \`|\`，而第一行、最后一行、以及中间一行（即第 $\frac{N+1}{2}$ 行）的第 2 ~ $N-1$ 个字符都是 \`-\` ，其余所有字符都是半角小写字母 \`x\` 。例如，一个 $N = 5$ 的日字矩阵如下:

\`\`\`
|---|
|xxx|
|---|
|xxx|
|---|
\`\`\`

请你帮小杨根据给定的 $N$ 打印出对应的“日字矩阵”。

## 输入格式

一行一个整数 $N$（$5 \le N \le 49$，保证 $N$ 为奇数）。

## 输出格式

输出对应的“日字矩阵”。
`,
            score: 25,
            explanation: `**解析：**
本题考察对二维图形中多条线段的复合判定。
1. **列边界**：
   - 第一列 (\`j == 0\`) 和最后一列 (\`j == N-1\`) 始终是 \`|\`。
2. **特殊行边界**：
   - 第一行 (\`i == 0\`)、最后一行 (\`i == N-1\`) 以及中间行 (\`i == N/2\`)。
   - 在这些行中，如果不是列边界，则填充 \`-\`。
3. **主体填充**：
   - 其余所有内部位置填充 \`x\`。
4. **逻辑优先级**：
   - 先检查列边界（最高优先级）。
   - 再检查特定的三行（次优先级）。
   - 最后使用 \`else\` 进行默认填充。

**核心逻辑提示：**
\`\`\`cpp
for (int i = 0; i < N; i++) {
    for (int j = 0; j < N; j++) {
        if (j == 0 || j == N - 1) cout << "|";
        else if (i == 0 || i == N / 2 || i == N - 1) cout << "-";
        else cout << "x";
    }
    cout << endl;
}
\`\`\``,
            tags: ['编程题', '图形绘制', '模拟'],
            template: "#include <iostream>\nusing namespace std;\nint main() {\n    // 在此编写代码\n    return 0;\n}",
            referenceCode: "#include <iostream>\nusing namespace std;\nint main() {\n    int n;\n    cin >> n;\n    for (int i = 0; i < n; i++) {\n        for (int j = 0; j < n; j++) {\n            if (j == 0 || j == n - 1) cout << '|';\n            else if (i == 0 || i == n / 2 || i == n - 1) cout << '-';\n            else cout << 'x';\n        }\n        cout << endl;\n    }\n    return 0;\n}"
        }
    ]
};
