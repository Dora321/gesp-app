// 2025年6月 GESP C++ 二级真题 (第10次认证)
export const paperData = {
    id: '2025-06-l2',
    title: '2025年6月 GESP C++ 二级真题',
    level: 2,
    year: 2025,
    month: 6,
    session: 10,
    timeLimit: 90 * 60,
    questions: [
        {
            id: 1,
            type: 'single',
            question: `2025年4月19日在北京举行了颇为瞩目的人形机器人半程马拉松赛。跑动着的机器人利用传感器反馈数据调整姿态，这类传感器类似于计算机的（ ）。`,
            options: ["处理器", "存储器", "输入设备", "输出设备"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (输入设备)**

**核心解析：**
传感器是向计算机系统输入信息的装置。题目中提到机器人利用传感器"反馈数据"——即传感器采集外部环境信息（如速度、姿态、温度等）后将这些数据传送给计算机的控制系统，这与键盘、鼠标、麦克风等输入设备将外界信息输入计算机的原理一致。

**选项逐项分析：**
- **A 处理器**：❌ 错误。处理器（CPU）是执行计算和指令的核心部件，负责处理数据而非采集数据。传感器并不执行计算任务。
- **B 存储器**：❌ 错误。存储器（如硬盘、内存）用于保存数据，本身不采集外部信息。传感器是主动获取数据的设备。
- **C 输入设备**：✅ 正确。输入设备的功能是将外部信息转换为计算机能处理的信号并传入系统，传感器正是此类装置。
- **D 输出设备**：❌ 错误。输出设备（如显示器、扬声器）将计算机处理后的结果向外传达，而传感器的作用方向相反，是将外界信息传入计算机。

**易错提醒：**
- 不能将"传感器"与"处理器"混淆：处理器是计算中枢，传感器是信息采集前端。
- 机器人的"反馈调整"依赖传感器采集的姿态数据，这些数据首先需要被输入系统。

**知识延伸：**
在计算机系统中，传感器（如温度传感器、光敏传感器、加速度计等）都属于输入设备，其本质功能与键盘、鼠标一致——将外部物理量转换为数字信号供计算机处理。

**考点：** 计算机硬件分类——输入设备与输出设备的区别`,
            tags: ["计算机基础"]
        },
        {
            id: 2,
            type: 'single',
            question: `小明购置的计算机内存不够用了，想购置一个容量更大的内存条。内存条在计算机术语中通常指（ ）。`,
            options: ["RAM", "ROM", "CACHE", "EPROM"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (RAM)**

**核心解析：**
"内存条"是普通消费者在电脑城购买的用于扩展内存的硬件，其标准英文名称为 RAM（Random Access Memory，随机存取存储器）。RAM 的特点是可随机读写、断电后数据丢失，用于存放操作系统和应用程序运行时的工作数据，这与"内存条"的实际用途完全吻合。

**选项逐项分析：**
- **A RAM**：✅ 正确。RAM 是内存条的正式名称，具有可读写、容量可扩展（插不同容量内存条）的特性，正是小明需要购买以扩展容量的硬件。
- **B ROM**：❌ 错误。ROM（Read Only Memory，只读存储器）用于存放主板固件（如 BIOS/UEFI），其内容在出厂时固化，用户无法像加内存条那样自行扩展容量。
- **C CACHE**：❌ 错误。CACHE（高速缓存）是位于 CPU 内部或附近的高速小容量存储器，集成在芯片上，无法以"内存条"形式购买。
- **D EPROM**：❌ 错误。EPROM（可擦除可编程只读存储器）是一种早期可重写的 ROM，技术上已是淘汰产品，且不存在"内存条"形态的产品。

**易错提醒：**
- 不能混淆"内存条"（RAM）和"硬盘"：硬盘用于长期存储（断电不丢失数据），内存条用于临时运行程序（断电数据丢失）。
- "内存不够用"指的是 RAM 不足，不是 ROM 或硬盘空间不足。

**知识延伸：**
计算机存储体系按速度由快到慢排列为：寄存器 → Cache → RAM → 硬盘。其中 RAM 是程序员直接操作的主要内存区域，GESP 编程中使用的变量都存放在 RAM 中。

**考点：** 存储器分类及 RAM 的物理形态`,
            tags: ["计算机基础"]
        },
        {
            id: 3,
            type: 'single',
            question: `下面 C++ 代码执行后的输出是（ ）。\n\`\`\`cpp\nint a = 3;\nfloat b = 3.5;\ncout << (a *= b);\n\`\`\``,
            options: ["3", "3.5", "10", "11"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (10)**

**核心解析：**
本题需要逐行分析复合赋值运算符和隐式类型转换的执行过程。

\`\`\`cpp
int a = 3;        // 第1行：a 初始化为整数 3
float b = 3.5;    // 第2行：b 初始化为浮点数 3.5
cout << (a *= b); // 第3行：执行复合赋值并输出
\`\`\`

逐步推导第3行表达式 \`a *= b\`：
1. \`a *= b\` 等价于 \`a = a * b\`
2. 计算 \`a * b\`：整数 3 与浮点数 3.5 相乘，结果为浮点数 10.5
3. 隐式类型转换：将浮点数 10.5 赋给整数变量 a，根据 C++ 规则，浮点数到整数的转换会**截断**小数部分，保留整数部分
4. 最终 a 的值为 10（而非四舍五入的 11）
5. \`cout\` 输出 a 的值，即 10

**选项逐项分析：**
- **A 3**：❌ 错误。这是 a 的初始值，但复合赋值运算符会改变 a 的值。
- **B 3.5**：❌ 错误。这是 b 的值，与输出无关；且 a 是 int 类型，即使赋值为 3.5 也会被截断。
- **C 10**：✅ 正确。3 × 3.5 = 10.5，截断后得到 10。
- **D 11**：❌ 错误。误以为 10.5 会四舍五入为 11，但 C++ 中浮点数到整数的转换是截断而非四舍五入。

**易错提醒：**
- 混淆"截断"与"四舍五入"：C++ 中 \`int x = 10.7\` 的结果是 10，不是 11。
- 忽略复合赋值运算符会修改左操作数的副作用：\`a *= b\` 相当于 \`a = a * b\`，a 的值被永久改变。

**知识延伸：**
复合赋值运算符（如 \`+=\`、\`-=\`、\`*=\`、\`/=\`、\`%=\`）的优先级与普通赋值运算符相同，但会先完成右侧的算术运算。当赋值左右类型不同时，会发生隐式类型转换，浮点数赋给整数变量时总是截断小数部分。

**考点：** 复合赋值运算符的执行过程 + 隐式类型转换（截断规则）`,
            tags: ["算术运算"]
        },
        {
            id: 4,
            type: 'single',
            question: `要获得正整数 N 的百位数（例如输入 1234，输出 2），横线处填入（ ）。`,
            options: ["N % 1000 / 100", "N / 1000 % 100", "N / 1000 / 100", "N % 100 / 100"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (N % 1000 / 100)**

**核心解析：**
本题要求从正整数 N 中提取百位数。以 N = 1234 为例，百位数是 2（因为千位是 1，百位是 2，十位是 3，个位是 4）。

分析选项 A 的表达式 \`N % 1000 / 100\`：
1. 第一步 \`N % 1000\`：N 对 1000 取模，等价于保留 N 的后三位数字
   - 以 1234 为例：1234 % 1000 = 234
2. 第二步 \`234 / 100\`：整数除法，去掉后两位，保留最高位（即百位）
   - 234 / 100 = 2（整除，向下取整）

验证结果：N = 1234 时得到百位数 2，符合题目要求。

**选项逐项分析：**
- **A N % 1000 / 100**：✅ 正确。取后三位再除以 100，能正确提取百位数。完整逻辑链：保留后三位 → 整除 100 提取最高位。
- **B N / 1000 % 100**：❌ 错误。\`1234 / 1000 = 1\`，\`1 % 100 = 1\`，得到的是千位数而非百位数。
- **C N / 1000 / 100**：❌ 错误。\`1234 / 1000 = 1\`，\`1 / 100 = 0\`，结果恒为 0 或 1，无法通用提取百位。
- **D N % 100 / 100**：❌ 错误。\`1234 % 100 = 34\`，\`34 / 100 = 0\`，提取的是十位和个位，再除以 100 几乎总是 0。

**易错提醒：**
- 取模和除法的顺序很重要：先取模保留哪几位，再除以相应的 10 的幂次来提取目标数位。
- 选项 D 看似类似，但 \`% 100\` 保留的是后两位而非后三位，导致百位信息丢失。
- 对于位数不足 4 位的数（如 123），百位是 0，需要根据题目隐含的规则判断是否接受 0。

**知识延伸：**
数位提取的通用方法是：用 \`%\` 保留低位数字，用 \`/\` 去除低位数字。若要提取第 k 位（从右往左，从 1 开始），常用公式为 \`(N / 10^(k-1)) % 10\`。例如提取百位（k=3），公式为 \`N / 100 % 10\`。

**考点：** 数位提取算法——取模与整除的配合使用`,
            tags: ["数位处理"]
        },
        {
            id: 5,
            type: 'single',
            question: `下面 C++ 代码执行，其输出是（ ）。\n\`\`\`cpp\nint a = 6, b = 28;\na = b;\nb = a;\ncout << a << " " << b;\n\`\`\``,
            options: ["6 28", "6 6", "28 6", "28 28"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (28 28)**

**核心解析：**
本题需要追踪变量赋值过程中值的变化，顺序执行每条语句：

\`\`\`cpp
int a = 6, b = 28;  // 初始状态：a = 6, b = 28
a = b;              // 第3行：把 b 的值赋给 a，a 变为 28，b 保持 28
b = a;              // 第4行：把 a 的值赋给 b，此时 a 是 28，所以 b 变为 28
cout << a << " " << b;  // 输出：28 28
\`\`\`

关键点分析：
- 第3行执行后：a = 28，b = 28（b 未被修改）
- 第4行执行后：a = 28，b = 28（b 被修改为 a 当前的值 28）
- 最终输出的是 a 和 b 的当前值，均为 28

**选项逐项分析：**
- **A 6 28**：❌ 错误。误以为 a 保持初始值 6。但第3行 \`a = b\` 将覆盖 a 的原值。
- **B 6 6**：❌ 错误。混淆了变量赋值的方向，误认为 a 保持不变而 b 被 a 赋值。
- **C 28 6**：❌ 错误。忽略了第4行 \`b = a\` 会将 b 的值更新为 28。
- **D 28 28**：✅ 正确。严格按顺序执行赋值语句，最终 a 和 b 都等于 28。

**易错提醒：**
- 不能仅看变量名判断结果：变量名只是标识符，赋值操作会覆盖原有值。
- 第4行 \`b = a\` 执行时，a 已经是 28（被第3行修改），所以 b 被赋值为 28。
- 连续赋值的场景中，每一步都必须基于当前变量状态计算，不能回溯初始状态。

**知识延伸：**
赋值运算符（\`=\`）的作用是将右侧表达式的值写入左侧变量，右侧可以是常量、变量或表达式。注意区分赋值（\`=\`）与相等判断（\`==\`），在条件语句中常用 \`==\` 进行比较。

**考点：** 变量赋值的顺序性与值覆盖`,
            tags: ["程序分析"]
        },
        {
            id: 6,
            type: 'single',
            question: `已知今天星期六，求其后第 N 天是星期几。下缘代码横线处填入（ ）。`,
            options: ["(N + 6) / 7", "(N + 6) // 7", "N % 7", "(N + 6) % 7"],
            answer: 3,
            score: 2,
            explanation: `**答案：D ((N + 6) % 7)**

**核心解析：**
星期计算是典型的周期性问题，需要将“第N天后”转换为数值运算。

1. 将星期映射为数字：星期日=0，星期一=1，...，星期六=6。
2. 今天星期六对应数字6。
3. 第N天后的数字 = (6 + N) % 7。
4. 用 % 7 取余，保证结果始终在 0~6 范围内。

验证：
- N=0（今天）：(6+0)%7=6，星期六 ✓
- N=1（明天）：(6+1)%7=0，星期日 ✓
- N=7（再过一周）：(6+7)%7=6，星期六 ✓

**选项逐项分析：**
- **A ((N + 6) / 7)**：❌ 错误。该表达式是整数除法，结果只能是0或1，无法正确映射到7个不同的星期值。例如N=1时结果为1，但明天应该是星期日（数字0）。
- **B ((N + 6) // 7)**：❌ 错误。C++中没有//作为整数除法运算符，这是Python语法，C++编译器会报错。
- **C (N % 7)**：❌ 错误。该表达式以N本身为基准，缺少了“今天星期六”这一偏移量。例如N=0时结果为0，表示星期日，但今天就是星期六，不会变化。
- **D ((N + 6) % 7)**：✅ 正确加入了基准偏移6，并以7取余保证周期性。

**易错提醒：**
- 忘记加上今天的基准值6，导致N=0时出错
- 混淆 %（取余）和 /（除法）在周期问题中的不同作用

**知识延伸：**
这类周期性问题在GESP中常见，如日期推算、周期性重复等，取模运算能将任意整数映射到固定范围内，是解决周期问题的核心工具。

**考点：** 取模运算在周期性问题中的应用`,
            tags: ["算术运算"]
        },
        {
            id: 7,
            type: 'single',
            question: `下面 C++ 代码执行后其输出是（ ）。\n\`\`\`cpp\nint cnt = 0;\nfor (int i = 1; i < 10; i++) {\n    if (i % 3 == 0) cnt++;\n}\ncout << cnt;\n\`\`\``,
            options: ["45", "28", "3", "0"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (3)**

**核心解析：**
本题考察 for 循环的遍历逻辑与取模条件的结合。

\`\`\`cpp
int cnt = 0;
for (int i = 1; i < 10; i++) {
    if (i % 3 == 0) cnt++;
}
cout << cnt;
\`\`\`

逐轮追踪：
- 循环变量 i 的取值范围：1, 2, 3, 4, 5, 6, 7, 8, 9（共9次）
- 判断条件 i % 3 == 0：表示 i 能被3整除
- 满足条件的 i 值：3（3%3=0）、6（6%3=0）、9（9%3=0）
- cnt 逐次递增：0→1→2→3

**选项逐项分析：**
- **A 45**：❌ 错误。这是将所有 i 值求和（1+2+...+9=45），混淆了累加与计数两种不同操作。
- **B 28**：❌ 错误。可能是某种错误计算结果，与实际满足条件的次数无关。
- **C 3**：✅ 正确。循环中1~9共9个数，能被3整除的有3、6、9共3个，cnt最终为3。
- **D 0**：❌ 错误。完全忽略了 i % 3 == 0 这一条件，实际上有多个数满足此条件。

**易错提醒：**
- 循环边界是 i < 10 而不是 i <= 9，范围是 [1, 9]，共9次
- % 3 == 0 判断的是“能否被3整除”，而非“除以3的余数”

**知识延伸：**
计数类循环的核心是明确两点：①遍历范围（循环变量的起止）；②筛选条件（if判断）。本题条件 i % 3 == 0 在GESP中经常出现，需熟练掌握。

**考点：** for 循环计数、整除判定`,
            tags: ["循环分析"]
        },
        {
            id: 8,
            type: 'single',
            question: `下面 C++ 代码执行后其输出是（ ）。\n\`\`\`cpp\nint s = 0;\nfor (int i = 1; i < 5; i++) s += i * 2;\ncout << s;\n\`\`\``,
            options: ["110", "22", "20", "3"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (20)**

**核心解析：**
本题考察 for 循环中累加操作的执行过程。

\`\`\`cpp
int s = 0;
for (int i = 1; i < 5; i++) s += i * 2;
cout << s;
\`\`\`

逐轮追踪：
- 循环变量 i 的取值：1, 2, 3, 4（i < 5 时进入循环，i=5 时退出）
- 每次累加的值为 i * 2
- 第1轮：s = 0 + 1×2 = 2
- 第2轮：s = 2 + 2×2 = 6
- 第3轮：s = 6 + 3×2 = 12
- 第4轮：s = 12 + 4×2 = 20

**选项逐项分析：**
- **A 110**：❌ 错误。可能是误将循环范围当作 i < 100 并计算 2+4+...+200，或者计算了错误的累加次数。
- **B 22**：❌ 错误。可能是遗漏了最后一轮（i=4），或者将 i < 5 误判为 i <= 4 而漏掉了某个值。
- **C 20**：✅ 正确。i 从1到4共4次累加，结果为 2+4+6+8=20。
- **D 3**：❌ 错误。可能是混淆了循环次数（4次）和变量个数等概念。

**易错提醒：**
- 循环边界 i < 5 意味着 i 的最大值为4，不是5
- s += i * 2 是先计算 i*2 的值，再与 s 相加，不能拆开理解

**知识延伸：**
循环累加是GESP二级的经典考点，关键在于：①明确循环次数（由起止条件决定）；②每轮累加的值是什么（表达式解析）；③避免将循环次数与累加结果混淆。

**考点：** 循环累加逻辑追踪`,
            tags: ["循环分析"]
        },
        {
            id: 9,
            type: 'single',
            question: `下面 C++ 代码执行后输出是（ ）。\n\`\`\`cpp\nint i = 0;\nfor (i = 0; i < 100; i++);\ncout << i;\n\`\`\``,
            options: ["101", "100", "99", "98"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (100)**

**核心解析：**
本题考察 for 循环语法中容易被忽略的空语句陷阱。

\`\`\`cpp
int i = 0;
for (i = 0; i < 100; i++);
cout << i;
\`\`\`

关键点：for 循环后有一个独立的分号 \`;\`，这是一个空语句（空循环体）。

执行过程分析：
1. 初始化：i = 0
2. 条件判断：i < 100？是的，进入循环
3. 执行 i++，i 变为 1
4. 回到条件判断：i < 100？是的，进入循环
5. ... 重复执行 i++ ...
6. 当 i 变为 100 时：i < 100？不成立，退出循环
7. 输出 i 的值：100

注意：循环体是空的，只有分号，所以每次循环只执行 i++，没有任何输出或赋值操作。

**选项逐项分析：**
- **A 101**：❌ 错误。可能误以为循环执行到 i=100 后还会再执行一次，但 i=100 时条件 i < 100 已不成立，循环终止。
- **B 100**：✅ 正确。循环执行了100次（i从0增到99），退出时 i=100。
- **C 99**：❌ 错误。可能将循环条件误读为 i <= 100，或误认为 i=99 时退出。
- **D 98**：❌ 错误。与实际循环次数完全不符。

**易错提醒：**
- for 循环后的分号 \`;\` 表示空语句，是合法语法但常被忽略
- 循环结束后 i 的值等于“第一个不满足条件的值”，而非最后满足条件的值
- 不要被选项中的101迷惑，那是 i=100时继续执行 i++ 后的值，但此时循环早已退出

**知识延伸：**
for 循环的空语句陷阱是GESP常见的语法陷阱。类似地，\`if(x>0);\` 后跟语句也会被误认为受条件控制。解题时要特别注意分号的位置和作用。

**考点：** for 循环语法中的空语句（分号）陷阱`,
            tags: ["循环追踪"]
        },
        {
            id: 10,
            type: 'single',
            question: `下面 C++ 代码执行后输出是（ ）。\n\`\`\`cpp\nfor (int i = 0; i < 2; i++) cout << "A#";\n\`\`\``,
            options: ["A#A#", "A#0#A#0", "A#A#1", "A#0#A#0#1"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (A#A#)**

**核心解析：**
本题考察 for 循环的基本执行与输出操作。

\`\`\`cpp
for (int i = 0; i < 2; i++) cout << "A#";
\`\`\`

执行过程：
- 第1轮：i = 0，i < 2 为真，执行 \`cout << "A#"\`，输出 "A#"
- 第2轮：i = 1，i < 2 为真，执行 \`cout << "A#"\`，输出 "A#"
- 第3轮：i = 2，i < 2 为假，循环结束

输出拼接结果："A#" + "A#" = "A#A#"

**选项逐项分析：**
- **A A#A#**：✅ 正确。循环执行2次，每次输出"A#"，合并为"A#A#"。
- **B A#0#A#0**：❌ 错误。可能误以为循环会输出 i 的值，但代码中 \`cout << "A#"\` 是直接输出字符串，不包含变量 i。
- **C A#A#1**：❌ 错误。可能误将循环变量的最终值（i=2）附加到输出中，但代码中并无此操作。
- **D A#0#A#0#1**：❌ 错误。同时误加了循环变量的值，且多了一次输出，逻辑完全错误。

**易错提醒：**
- \`cout << "A#"\` 输出的是字符串 "A#"，不是变量
- 循环体只有一条语句 \`cout << "A#";\`，没有分号隔开也没有花括号包裹
- 循环执行2次，不是1次（i=0, i=1）

**知识延伸：**
for 循环的循环体可以是单条语句（无需花括号），也可以是多条语句（需花括号）。本题循环体是单条语句，直接跟在 for 后面，注意不要与空语句陷阱混淆（空语句是单独的分号）。

**考点：** 基础循环控制`,
            tags: ["程序逻辑"]
        },
        {
            id: 11,
            type: 'single',
            question: `执行下面 C++ 代码，其输出是（ ）。\n\`\`\`cpp\nfor (int i = 0; i < 3; i++) {\n    for (int j = 0; j < i + 1; j++) printf("%d-%d#", i, j);\n    printf("END\\n");\n}\n\`\`\``,
            options: ["0-0#END\\n1-0#END\\n2-0#END", "0-0#1-0#2-0#END", "0-0#END\\n1-1#END\\n2-2#END", "0-0#END\\n1-0#1-1#END\\n2-0#2-1#2-2#END"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (0-0#END\\n1-0#1-1#END\\n2-0#2-1#2-2#END)**

**核心解析：**
本题需追踪嵌套循环中 i 和 j 的取值及输出序列。

逐轮推导执行过程：
- **i = 0**：\`j < 1\`，内层循环仅执行 j = 0 一次，输出 \`0-0#\`；然后执行 \`printf("END\\n")\`
- **i = 1**：\`j < 2\`，内层循环执行 j = 0, 1，依次输出 \`1-0#1-1#\`；然后执行 \`printf("END\\n")\`
- **i = 2**：\`j < 3\`，内层循环执行 j = 0, 1, 2，依次输出 \`2-0#2-1#2-2#\`；然后执行 \`printf("END\\n")\`

注意：内层 \`printf("%d-%d#", i, j)\` 与外层 \`printf("END\\n")\` 是顺序执行的，每轮外层循环的 END 都在该轮所有 \`#\` 输出完之后才打印，因此每行末尾都有 END。

**选项逐项分析：**
- **A (0-0#END\\n1-0#END\\n2-0#END)**：❌ 错误。内层循环只取 j = 0，未考虑 j 的完整取值范围。
- **B (0-0#1-0#2-0#END)**：❌ 错误。三个 \`#\` 出现在同一行，不符合代码的换行逻辑。
- **C (0-0#END\\n1-1#END\\n2-2#END)**：❌ 错误。j 的起始值应为 0 而非 1。
- **D (0-0#END\\n1-0#1-1#END\\n2-0#2-1#2-2#END)**：✅ 正确。完全符合上述逐行推导结果。

**易错提醒：**
- 混淆 \`j < i\` 与 \`j < i + 1\`，导致 j 的取值少算一个
- 忽略内层循环结束后才执行 \`printf("END\\n")\` 的顺序，误将 END 放在每轮开头

**知识延伸：**
嵌套循环中，外层循环控制"行"或"轮次"，内层循环控制"列"或"每轮的重复操作"，需分别追踪两层循环变量的取值范围。

**考点：** 嵌套循环的执行流追踪`,
            tags: ["嵌套循环"]
        },
        {
            id: 12,
            type: 'single',
            question: `输出不能被 3 整除且除以 5 余数为 2 的数。下列选项中不能实现的是（ ）。`,
            options: ["(i % 3 != 0) && (i % 5 == 2)", "(i % 3) && (i % 5 == 2)", "(i % 3) && !(i % 5 != 2)", "!(i % 3) && (i % 5 == 2)"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (!(i % 3) && (i % 5 == 2))**

**核心解析：**
分析各选项对"不能被3整除"的表达是否正确。

关键规则：\`i % 3\` 的结果为 0 表示"能被3整除"，结果非 0 表示"不能被3整除"。在 C++ 逻辑表达式中，0 等价于 false，非 0 等价于 true。

**选项逐项分析：**
- **A ((i % 3 != 0) && (i % 5 == 2))**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（逻辑正确）。\`(i % 3 != 0)\` 直接表达"余数不为0"，即不能被3整除。
- **B ((i % 3) && (i % 5 == 2))**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（逻辑正确）。\`i % 3\` 本身在布尔上下文中，非0值为真，等价于"不能被3整除"。
- **C ((i % 3) && !(i % 5 != 2))**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（逻辑正确）。\`(i % 5 != 2)\` 的逻辑非即 \`(i % 5 == 2)\`，与第一项组合后等价于选项 A。
- **D (!(i % 3) && (i % 5 == 2))**：✅ 正确。该项不符合题目限定，正是本题要选的项。正确（逻辑错误）。\`!(i % 3)\` 等价于 \`(i % 3 == 0)\`，表示"能被3整除"，与题意要求的"不能被3整除"完全相反。

**易错提醒：**
- 误以为 \`i % 3\` 本身即表示"不能被3整除"，而未意识到其逻辑非才是正确的表达
- 忽略 \`!(i % 3)\` 的实际含义是"取反后等于0"，即"能被3整除"

**知识延伸：**
模运算与逻辑非的组合是 GESP 二级高频考点。\`!(a)\` 将"非零/真"变为"零/假"，将"零/假"变为"非零/真"，务必明确操作对象的语义。

**考点：** 逻辑非运算符的真假判定、整除性的逻辑表达`,
            tags: ["逻辑运算"]
        },
        {
            id: 13,
            type: 'single',
            question: `判断一个正整数是几位数。横线处应填入代码先后是（ ）。\n\`\`\`cpp\nwhile (________) {\n    cnt++;\n    ________;\n}\n\`\`\``,
            options: ["N > 1; N = N / 10", "N > 1; N /= 10", "N == 0; N /= 10", "N > 0; N /= 10"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (N > 0; N /= 10)**

**核心解析：**
数位统计的标准结构：每轮循环将 N 除以 10（丢弃末位）并计数一次，直到 N 变为 0 时停止。

以 N = 123 为例逐轮验证：
- 第1轮：N = 123 > 0，cnt = 1，N /= 10 → N = 12
- 第2轮：N = 12 > 0，cnt = 2，N /= 10 → N = 1
- 第3轮：N = 1 > 0，cnt = 3，N /= 10 → N = 0
- 第4轮：N = 0 不满足 > 0，循环结束，cnt = 3（正确）

**选项逐项分析：**
- **A (N > 1; N = N / 10)**：❌ 错误。条件 \`N > 1\` 会导致个位数 N = 1 时提前终止，漏计最后一位。
- **B (N > 1; N /= 10)**：❌ 错误。理由同 A，边界条件设置不当。
- **C (N == 0; N /= 10)**：❌ 错误。初始 N 为正数时不满足 \`N == 0\`，循环体一次都不执行。
- **D (N > 0; N /= 10)**：✅ 正确。循环条件 \`N > 0\` 确保所有正整数位都能被统计，\`N /= 10\` 正确更新变量。

**易错提醒：**
- 将循环条件设为 \`N > 1\` 会漏计个位数，应为 \`N > 0\`
- 复合赋值运算符 \`N /= 10\` 与 \`N = N / 10\` 在数学意义上等价

**知识延伸：**
数位统计与数值反转是 GESP 常考结构，其核心思想是通过除以10取整来逐位剥离数字。

**考点：** 经典的数位统计循环结构`,
            tags: ["数位处理"]
        },
        {
            id: 14,
            type: 'single',
            question: `判断自守数（指一个数的平方的尾数等于自身，如 $25^2 = 625$）。说法错误的是（ ）。`,
            options: ["如果标志位 Flag 不变则说明是自守数", "使用 N1 % 10 != M1 % 10 逐位比对", "N1 /= 10 移除末位以便下次比对", "将循环条件中的 N1 > 0 直接修改为原变量 N > 0 效果完全相同"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (将循环条件中的 N1 > 0 直接修改为原变量 N > 0 效果完全相同)**

**核心解析：**
自守数判断算法通常备份原变量：令 \`N1 = N\`（原数）和 \`M1 = N * N\`（平方数），逐位比对末位。

以 25 为例（25² = 625）：
- N1 = 25，M1 = 625，flag = 1
- 第1轮：N1 % 10 = 5，M1 % 10 = 5，相等则 flag 不变；N1 /= 10 → N1 = 2，M1 /= 10 → M1 = 62
- 第2轮：N1 % 10 = 2，M1 % 10 = 2，相等则 flag 不变；N1 /= 10 → N1 = 0，M1 /= 10 → M1 = 6
- 第3轮：N1 = 0，循环终止。flag = 1 未改变，判断为自守数。

**关键问题：** 循环条件必须基于不断被修改的 \`N1\`，而非原变量 \`N\`。
- \`N1 > 0\`：随着 \`N1 /= 10\` 执行，N1 从 25 → 2 → 0，条件逐轮变化，循环3次
- \`N > 0\`：原变量 N 始终为 25，第一轮即不满足 \`25 > 0\` 为假（循环0次），循环根本不执行

**选项逐项分析：**
- **A (如果标志位 Flag 不变则说明是自守数)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（说法正确）。若逐位比对全部相等，flag 保持初值 1，程序据此输出"是自守数"。
- **B (使用 N1 % 10 != M1 % 10 逐位比对)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（说法正确）。\`% 10\` 取末位数字进行比对，是标准逐位提取方法。
- **C (N1 /= 10 移除末位以便下次比对)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（说法正确）。\`/= 10\` 将数字右移一位，准备下一轮取新的末位。
- **D (将循环条件中的 N1 > 0 直接修改为原变量 N > 0 效果完全相同)**：✅ 正确（说法错误）。N 被保留用于原始值记录，N1 被不断修改，两者语义完全不同。

**易错提醒：**
- 混淆备份变量与工作变量：备份变量（如 N）用于保留原始值，工作变量（如 N1）用于循环中不断修改
- 循环条件必须基于当前正在变化的工作变量，而非原始备份变量

**知识延伸：**
备份变量是 GESP 编程中的重要习惯，防止循环过程中丢失关键原始数据。

**考点：** 变量值保持的重要性、备份变量编程习惯`,
            tags: ["自守数"]
        },
        {
            id: 15,
            type: 'single',
            question: `关于输出图形的代码设计，说法错误的是（ ）。`,
            options: ["将计数器 now_number = 0 移到外层循环内侧效果不变", "now_number += 1 与 now_number = 1 + now_number 等价", "now_number == 10 与 now_number > 9 在此时逻辑等价", "cout << endl 与 cout << \"\\n\" 在此功能一致"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (将计数器 now_number = 0 移到外层循环内侧效果不变)**

**核心解析：**
分析 \`now_number = 0\` 初始化位置对输出序列的影响。

假设外层循环为 \`for (int i = 0; i < 3; i++)\`，内层循环输出递增数字：
- **原位置（外层循环外侧）**：now_number = 0 仅执行一次，各行连续编号
  - 第1行：输出 1, 2, 3
  - 第2行：输出 4, 5, 6
  - 第3行：输出 7, 8, 9
- **移至外层循环内侧后**：now_number = 0 每行重新初始化
  - 第1行：输出 1, 2, 3
  - 第2行：输出 1, 2, 3（重置后重新开始）
  - 第3行：输出 1, 2, 3

两种初始化位置的输出结果完全不同，因此 A 选项的"效果不变"说法错误。

**选项逐项分析：**
- **A (将计数器 now_number = 0 移到外层循环内侧效果不变)**：✅ 正确（说法**错误**）。该说法本身不正确，因为移到内侧会导致每行重新从 0 开始计数，输出序列完全不同。
- **B (now_number += 1 与 now_number = 1 + now_number 等价)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（说法正确）。两者均表示将 now_number 增加 1，在功能上完全等价。
- **C (now_number == 10 与 now_number > 9 在此时逻辑等价)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（说法正确）。对于整数 now_number，"等于10"与"大于9"判断的是同一组数（值为10的数）。
- **D (cout << endl 与 cout << "\\n" 在此功能一致)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（说法正确）。两者均实现换行功能，在输出图形题目中没有区别。

**易错提醒：**
- 变量初始化位置决定其作用域和生命周期，放在循环内会随每次迭代重置
- 复合赋值运算符 \`+=\` 与普通赋值加法在数学结果上等价，但前者更简洁

**知识延伸：**
变量作用域是 C++ 基础概念：外层声明的变量在内层可见但可能被修改，内层声明的变量仅在内层有效。

**考点：** 变量初始化位置对程序逻辑的影响`,
            tags: ["程序设计"]
        },
        {
            id: 16,
            type: 'judge',
            question: `人们现在参加很多闭卷考试时通常都不允许带智能手机、平板电脑等，此外很多种智能手表同样因为具有嵌入操作系统及通信等功能，所以也不允许随身携带。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
本题考查的是信息技术常识与社会规范。题干明确指出两点：
1. 闭卷考试不允许携带智能手机、平板电脑等智能设备；
2. 智能手表因具备嵌入操作系统及通信功能，同样不允许随身携带。

智能手表具备独立的计算能力、网络连接和应用扩展功能，属于移动智能终端的范畴，在严格规范下的考试场所确实被列为违禁物品。这一描述与现实中各类标准化考试的安检规定相符。

**易错提醒：**
- 部分考生可能仅关注手机、平板，而忽略智能手表也属于同类受限设备；
- 需注意题干强调的是"很多种智能手表"，并非所有手表，重点在于其智能化程度。

**知识延伸：**
GESP 二级考试强调考生对信息技术社会应用的认知，理解哪些设备具有存储、计算、通信能力，对于正确作答此类常识题至关重要。

**考点：** 信息技术社会常识、考场纪律规范`,
            tags: ["判断题"]
        },
        {
            id: 17,
            type: 'judge',
            question: `在 C++ 代码中，假设 N 为正整数，则 N / 10 舍弃个位数。如果 N 小于 10，则其值为 0，大于 10 则是舍弃个位数的数。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
本题考查 C++ 整数除法的截断特性。在 C++ 中，当两个整数相除时，结果自动截断小数部分（向零取整），不会进行四舍五入。

具体分析：
- \`N / 10\` 表示整数 N 除以 10 的商。
- 对于正整数 N，整除 10 的本质是将 N 的十进制表示整体右移一位，即丢弃个位数。
- **N < 10 时**：N 的个位数即为 N 本身，十位数不存在，因此商为 0。例如 \`9 / 10 = 0\`。
- **N ≥ 10 时**：商的整数部分就是 N 舍弃个位数后的结果。例如 \`45 / 10 = 4\`，\`123 / 10 = 12\`。

该描述完全符合 C++ 整数除法的语义规则。

**易错提醒：**
- 不要与数学除法混淆，C++ 整除会直接丢弃余数；
- 对于负数的整除，向零取整的规则与"舍弃"略有不同，但本题假设 N 为正整数，不涉及此情况。

**知识延伸：**
整数除法是 GESP 二级的高频考点，常与取模运算 \`%\`（获取余数/个位数）配合使用，如 \`N / 10\` 取高位、\`N % 10\` 取个位。

**考点：** 整数除法的截断特性`,
            tags: ["判断题"]
        },
        {
            id: 18,
            type: 'judge',
            question: `下列 C++ 代码执行后，其输出为 10 20 ，即 a == b 和 b == a 对 a 和 b 的值没有任何影响：\n\`int a = 10, b = 20; a == b; b == a; cout << (a, b);\``,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
本题代码为：
\`\`\`cpp
int a = 10, b = 20;
a == b;
b == a;
cout << (a, b);
\`\`\`

逐行分析：
1. \`int a = 10, b = 20;\`：初始化后，a = 10，b = 20。
2. \`a == b;\`：比较表达式，比较 10 与 20 是否相等，结果为 false（0）。**比较运算不会修改变量值**。
3. \`b == a;\`：同样是比较表达式，结果为 false。**也不会修改变量值**。
4. \`cout << (a, b);\`：此处使用了**逗号运算符**，而非同时输出两个值。逗号运算符的含义是：从左到右依次求值，最终**整个表达式的结果是最后一个表达式的值**。因此 \`(a, b)\` 的结果就是 b 的值，即 20。

所以输出为 \`20\`，而不是 \`10 20\`。

**易错提醒：**
- 误认为 \`(a, b)\` 会同时输出两个值，实际上逗号运算符只保留最右侧的值；
- 误以为比较表达式会交换两个变量的值，实际上 \`==\` 是比较运算符，不是赋值。

**知识延伸：**
逗号运算符在 GESP 二级中属于理解性考点，常见考法包括：\`cout << (a, b);\`、\`int x = (a++, b++);\` 等，需牢记其"取最右侧表达式值"的规则。

**考点：** 逗号运算符的返回值规则、比较运算的副作用判定`,
            tags: ["判断题"]
        },
        {
            id: 19,
            type: 'judge',
            question: `a 和 b 分别是 C++ 的整型变量，如果表达式 max(a, b) == min(a, b) 的值为真，则说明 a 和 b 相等。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
本题考查逻辑推理与 C++ 标准库函数的联合应用。

数学逻辑分析：
- \`max(a, b)\` 表示 a 和 b 中的较大值。
- \`min(a, b)\` 表示 a 和 b 中的较小值。
- 若 \`max(a, b) == min(a, b)\` 成立，意味着"较大值等于较小值"。
- 在数学上，若一个数同时是"最大"和"最小"，则该数必须与集合中的所有元素相等。
- 因此，\`max(a, b) == min(a, b)\` 成立时，a 和 b 必然相等。

代码语义验证：
- \`max(a, b)\` 和 \`min(a, b)\` 均来自 \`<algorithm>\` 头文件。
- 当 a == b 时，\`max(a, b)\` 返回 a（或 b），\`min(a, b)\` 也返回 a（或 b），两者相等。
- 当 a ≠ b 时，较大的那个严格大于较小的，两者不相等。

所以，表达式值为真时，可以确定 a 和 b 相等。

**易错提醒：**
- 部分考生可能误认为需要讨论 a、b 的正负或大小方向，实际上相等关系与这些无关；
- 需注意 max/min 函数本身不要求 a、b 是正整数，任意可比较类型均适用。

**知识延伸：**
此题为逻辑推理型考点，GESP 二级中常将数学逻辑与 C++ 标准库函数结合，如利用 \`max\`/\`min\` 进行值交换、条件判断等。

**考点：** 数学逻辑与标准库函数应用`,
            tags: ["判断题"]
        },
        {
            id: 20,
            type: 'judge',
            question: `下面 C++ 代码编译时将报错，因为字符变量 a 被赋值了浮点值：\n\`char a = '1'; a = 45.6; cout << a;\``,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
本题代码为：
\`\`\`cpp
char a = '1';
a = 45.6;
cout << a;
\`\`\`

逐行分析：
1. \`char a = '1';\`：初始化字符变量 a，存储字符 '1'（ASCII 码 49）。
2. \`a = 45.6;\`：将浮点常量 45.6 赋值给字符变量 a。
   - C++ 在赋值时允许从 \`double\` 到 \`char\` 的**隐式类型转换**。
   - 转换过程：首先将 45.6 截断为整数 45（取整），然后将整数 45 视为 ASCII 码。
   - ASCII 码 45 对应的字符是 '-'（减号）。
3. \`cout << a;\`：输出字符 '-'。

编译器对第 2 行的赋值可能产生**警告**（如"从 'double' 转换到 'char'，可能丢失数据"），但绝不会产生**编译错误**。C++ 标准允许这种隐式转换，代码可以正常编译和运行。

**易错提醒：**
- 混淆"警告"与"错误"：编译器警告不会阻止编译，代码仍可正常运行；
- 误以为 char 只能存储字符字面量，实际上 char 可以存储任意 0~255（或有符号 -128~127）范围内的整数值，这些值会被解释为 ASCII 字符。

**知识延伸：**
隐式类型转换是 GESP 二级的重要考点。赋值时的类型转换规则为：从高精度向低精度转换时，编译器自动完成截断/舍入，但可能伴随警告。常见转换还有 \`double\` → \`int\`、\`int\` → \`char\` 等。

**考点：** 隐式类型转换、字符存储原理`,
            tags: ["判断题"]
        },
        {
            id: 21,
            type: 'judge',
            question: `下面 C++ 代码执行时如输入 59.99，将输出“及格”两个汉字：\n\`int score; cin >> score; if (score < 60) cout << "不及格"; else cout << "及格";\``,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
变量 \`score\` 的类型为 \`int\`（整型）。当执行 \`cin >> score\` 时：
1. \`cin\` 会从输入流中读取字符，直到遇到第一个无法构成整数的字符（小数点 \`.\`）。
2. 因此，输入 \`59.99\` 时，\`cin\` 读取到 \`59\` 后停止（点号不是整型合法字符）。
3. \`score\` 被赋值为整数 \`59\`，小数部分 \`.99\` 留在输入缓冲区中未被读取。
4. 判断条件 \`score < 60\` 为真（\`59 < 60\` 成立），程序执行 \`cout << "不及格"\`。

**易错提醒：**
- 误以为 \`59.99\` 会被四舍五入或取整存入 \`score\`，实际上 \`cin\` 对整型的读取是**严格按字符边界截断**的，遇到小数点立即停止。
- 循环或后续代码若继续读取同一输入流，可能读到被遗弃的 \`.99\`，导致意外行为。

**知识延伸：**
\`cin\` 对不同类型的读取行为不同：整型遇到非数字字符截断，浮点型会跳过前导空白并解析小数。此特性在 GESP 二级的输入处理题中常作为考点。

**考点：** cin 读取整型的截断规则、整数类型与浮点输入的交互`,
            tags: ["判断题"]
        },
        {
            id: 22,
            type: 'judge',
            question: `在下面的 C++ 代码中，因为 continue 将被执行，因此不会有任何输出：\n\`for(int i=0; i<10; i++) { if(i < 10) continue; } if(i == 10) cout << "END";\``,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
分析代码的执行流程：
1. \`for\` 循环头部 \`(int i = 0; i < 10; i++)\` 定义了局部变量 \`i\`，循环会依次执行 i=0,1,2,...,9 共10次。
2. 循环体中的 \`if(i < 10) continue;\`：由于循环条件本身保证 \`i < 10\` 恒成立（i 最大为9），每次迭代都会执行 \`continue\`。
3. \`continue\` 的作用是**跳过本次循环体剩余部分**，直接进入下一次迭代。它不会终止整个循环。
4. 10次迭代全部执行完毕后，\`i\` 的值变为 10，循环条件 \`i < 10\` 为假，循环结束。
5. 循环结束后，\`i\` 的值仍然是 10（虽然作用域可能已结束，但在某些编译器/标准下仍可访问或通过其他方式验证），条件 \`if(i == 10)\` 成立，执行 \`cout << "END"\`。

因此，程序会输出 "END"，并非"没有任何输出"。

**易错提醒：**
- \`continue\` 只作用于**当前一次**循环迭代，不会提前结束整个循环。
- 循环变量 \`i\` 在循环结束后若超出作用域，其值不可直接访问，但本题讨论的是程序逻辑上循环必然完成。

**知识延伸：**
\`continue\` 与 \`break\` 的区别是 GESP 二级的常见考点：\`continue\` 跳过本次循环剩余语句，\`break\` 提前结束整个循环。在嵌套循环中，作用范围仅限于最内层循环。

**考点：** continue 语句的作用范围、循环完整执行与提前终止的区别`,
            tags: ["判断题"]
        },
        {
            id: 23,
            type: 'judge',
            question: `下面的 C++ 代码执行后将输出 15：\n\`int Sum = 0; for (int i = 0; i < 5; i++) Sum += i; cout << Sum;\``,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
逐行追踪代码执行过程：

初始状态：\`Sum = 0\`

循环迭代过程：
- i=0：\`Sum += 0\`，\`Sum = 0 + 0 = 0\`
- i=1：\`Sum += 1\`，\`Sum = 0 + 1 = 1\`
- i=2：\`Sum += 2\`，\`Sum = 1 + 2 = 3\`
- i=3：\`Sum += 3\`，\`Sum = 3 + 3 = 6\`
- i=4：\`Sum += 4\`，\`Sum = 6 + 4 = 10\`
- i=5：\`5 < 5\` 为假，循环结束

最终 \`Sum = 0 + 1 + 2 + 3 + 4 = 10\`，而非 15。

**易错提醒：**
- 常见错误是误以为循环会累加到 5，导致计算 \`0+1+2+3+4+5=15\`。
- 循环条件是 \`i < 5\`，当 \`i\` 达到 5 时条件不满足，循环立即终止，**不执行 i=5 的那次迭代**。

**知识延伸：**
for 循环的三个表达式中，第二表达式（循环条件）决定了循环的**上界/下界是否包含**。GESP 题目中常见陷阱是混淆 \`<\` 与 \`<=\`、或混淆 \`++i\` 与 \`i++\` 在累加中的细微差异（虽然后者在单纯累加中结果相同）。

**考点：** for 循环边界条件与迭代次数分析、变量状态追踪`,
            tags: ["判断题"]
        },
        {
            id: 24,
            type: 'judge',
            question: `将下面 C++ 代码中的 (int i = 5; i > 1; i--) 调整为 (int i = 1; i < 5; i++) 输出结果相同，因为 5 到 1 与 1 到 5 的求和相同。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
原循环：\`for(int i = 5; i > 1; i--)\` 迭代为 i=5,4,3,2（4次），累加值为 \`5 + 4 + 3 + 2 = 14\`。

调整后循环：\`for(int i = 1; i < 5; i++)\` 迭代为 i=1,2,3,4（4次），累加值为 \`1 + 2 + 3 + 4 = 10\`。

两次累加结果**不相同**（14 ≠ 10），因此原题干的结论在数值上是错误的。

但是，如果原题干的**意图**是比较"从5递减到2"与"从1递增到4"的求和是否相同（且假设两种遍历方式在某种对称性上有相同结果），则需要更仔细的分析。

实际上，题干描述"5 到 1 与 1 到 5 的求和相同"在数学上是不成立的：
- 5 到 1（不含1）：5+4+3+2 = 14
- 1 到 5（不含5）：1+2+3+4 = 10

**答案应为：错误**（题干结论不正确）。

**易错提醒：**
- 不要被"遍历方向不同，结果相同"的惯性思维误导，必须实际计算。
- 递减循环和递增循环即使迭代次数相同，累加的数值序列也不同（一个是从n到2，一个是从1到n-1）。

**知识延伸：**
循环等价性需要满足两个条件：迭代次数相同，且每次迭代对应的累加值相同。仅靠遍历方向相同不能保证结果相同，这在 GESP 三级的数组操作题中尤为重要。

**考点：** 循环遍历范围分析、数列求和的边界确定`,
            tags: ["判断题"]
        },
        {
            id: 25,
            type: 'judge',
            question: `为实现 N 行 N 列字符阵列，当输入奇数时中间列为 * 其余为 -，输入偶数时中间两列为 * 其余为 -，代码利用 \`if(abs(j - (n-1)/2.0) <= 0.5)\` 逻辑可以实现。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
分析条件表达式 \`abs(j - (n-1)/2.0) <= 0.5\` 的数学含义：

**核心公式解读：**
- \`(n-1)/2.0\`：当 n 为行列数时，这是矩阵**中轴线的浮点坐标**。
- \`abs(j - 中轴) <= 0.5\`：表示列索引 j 与中轴的距离不超过 0.5。

**奇数 n 的情况（n=5）：**
- 中轴坐标 = \`(5-1)/2.0 = 2.0\`
- \`abs(j - 2.0) <= 0.5\` 等价于 \`-0.5 <= j-2 <= 0.5\`
- 唯一满足的整数 j = 2
- 结果：只有第 3 列（索引2）为 \`*\`，其余为 \`-\`——符合"奇数时中间一列"的要求

**偶数 n 的情况（n=4）：**
- 中轴坐标 = \`(4-1)/2.0 = 1.5\`
- \`abs(j - 1.5) <= 0.5\` 等价于 \`-0.5 <= j-1.5 <= 0.5\`
- 满足的整数 j = 1, 2
- 结果：第 2、3 列（索引1和2）为 \`*\`，其余为 \`-\`——符合"偶数时中间两列"的要求

**易错提醒：**
- 常见错误是用 \`(n-1)/2\`（整数除法）代替 \`(n-1)/2.0\`，导致偶数时中轴坐标错误（如 n=4 时变成 1 而非 1.5）。
- 浮点比较使用 \`<= 0.5\` 而非 \`< 0.5\` 是关键，它确保了边界情况（距离恰好为 0.5）也被包含。

**知识延伸：**
此类"中点判定"逻辑在 GESP 图形输出题中非常典型。另一种常见写法是 \`j == n/2\`（奇数）或 \`j == n/2 || j == n/2 - 1\`（偶数），但用浮点距离判定的方式更加统一简洁，体现了数学思维在编程中的应用。

**考点：** 浮点坐标中点判定、奇偶数情况的统一处理、矩阵下标与数学坐标的转换`,
            tags: ["判断题"]
        }
    ],
    programmingQuestions: [
        {
            id: 26,
            type: 'programming',
            title: "数三角形",
            samples: [
                {
                    input: `3`,
                    output: `3`
                },
                {
                    input: `5`,
                    output: `9`
                }
            ],
            question: `
# [GESP202506 二级] 数三角形

## 题目描述

直角三角形有两条直角边与一条斜边，设两条直角边的长度分别为 $a, b$，则直角三角形的面积为 $\\frac{ab}{2}$。

请你计算当直角边长 $a, b$ 均取不超过 $n$ 的正整数时，有多少个不同的面积为整数的直角三角形。直角边长分别为 $a, b$ 和 $a', b'$ 的两个直角三角形相同，当且仅当 $a = a', b = b'$ 或者 $a = b', b = a'$。

## 输入格式

一行，一个整数 $n$，表示直角边长的最大值。

## 输出格式

输出一行，一个整数，表示不同的直角三角形数量。
`,
            score: 25,
            explanation: `**解析：**
本题考察组合计数与数学判定。
1. **去重规则**：题目规定 $(a, b)$ 与 $(b, a)$ 视为同一个。为了不重不漏地计数，我们设定枚举顺序为 $1 \le a \le b \le n$。
2. **面积判定**：面积 $ab/2$ 为整数，意味着乘积 $a \times b$ 必须是偶数。
3. **算法设计**：
   - 使用两层嵌套循环枚举 $a$ 和 $b$。
   - 如果 $a \times b \% 2 == 0$，则计数器加 1。
4. **复杂度**：$n$ 较小时直接枚举即可（$O(n^2)$）。

**核心逻辑提示：**
\`\`\`cpp
for (int a = 1; a <= n; a++) {
    for (int b = a; b <= n; b++) {
        if ((a * b) % 2 == 0) ans++;
    }
}
\`\`\``,
            tags: ["编程题", "枚举", "数学"],
            template: "#include <iostream>\nusing namespace std;\nint main() {\n    // 在此编写代码\n    return 0;\n}",
            referenceCode: "#include <iostream>\nusing namespace std;\nint main() {\n    int n; cin >> n;\n    long long ans = 0;\n    for (int a = 1; a <= n; a++) {\n        for (int b = a; b <= n; b++) {\n            if ((1LL * a * b) % 2 == 0) ans++;\n        }\n    }\n    cout << ans << endl;\n    return 0;\n}"
        },
        {
            id: 27,
            type: 'programming',
            title: "幂和数",
            samples: [
                {
                    input: `2 8`,
                    output: `6`
                },
                {
                    input: `10 100`,
                    output: `20`
                }
            ],
            question: `
# [GESP202506 二级] 幂和数

## 题目描述

对于正整数 $n$，如果 $n$ 可以表为两个 $2$ 的次幂之和，即 $n = 2^x + 2^y$（$x, y$ 均为非负整数），那么称 $n$ 为幂和数。

给定正整数 $l, r$，请你求出满足 $l \leq n \leq r$ 的整数 $n$ 中有多少个幂和数。

## 输入格式

一行，两个正整数 $l, r$。

## 输出格式

输出一行，一个整数，表示 $l, r$ 之间幂和数的数量。
`,
            score: 25,
            explanation: `**解析：**
本题考察枚举与集合去重。
1. **幂的范围**：$2^x$ 增长非常快。在 $r$ 范围内，$x$ 的取值大约只有 0 到 30 多。
2. **生成策略**：
   - 先生成所有 $2^0, 2^1, 2^2, ...$ 直到超过 $r$。
   - 双重循环枚举这两个幂的和 $sum = 2^x + 2^y$。
3. **去重与区间统计**：
   - 使用 \`std::set\` 存储生成的 $sum$，可以自动去重。
   - 最后遍历集合，统计落在 $[l, r]$ 闭区间内的元素个数。

**核心逻辑提示：**
\`\`\`cpp
vector<long long> powers;
for (long long p = 1; p <= r; p *= 2) powers.push_back(p);

set<long long> results;
for (auto x : powers) {
    for (auto y : powers) {
        long long sum = x + y;
        if (sum >= l && sum <= r) results.insert(sum);
    }
}
cout << results.size();
\`\`\``,
            tags: ["编程题", "枚举", "集合"],
            template: "#include <iostream>\n#include <vector>\n#include <set>\nusing namespace std;\nint main() {\n    // 在此编写代码\n    return 0;\n}",
            referenceCode: "#include <iostream>\n#include <vector>\n#include <set>\nusing namespace std;\nint main() {\n    long long l, r; cin >> l >> r;\n    vector<long long> p;\n    for (long long val = 1; val <= r; ) {\n        p.push_back(val);\n        if (val > r / 2) break;\n        val *= 2;\n    }\n    set<long long> s;\n    for (long long a : p) {\n        for (long long b : p) {\n            if (a + b >= l && a + b <= r) s.insert(a + b);\n        }\n    }\n    cout << s.size() << endl;\n    return 0;\n}"
        }
    ]
};
