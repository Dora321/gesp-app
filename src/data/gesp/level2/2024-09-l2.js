// 2024年9月 GESP C++ 二级真题 (第7次认证)
export const paperData = {
    id: '2024-09-l2',
    title: '2024年9月 GESP C++ 二级真题',
    level: 2,
    year: 2024,
    month: 9,
    session: 7,
    timeLimit: 90 * 60,
    questions: [
        {
            id: 1,
            type: 'single',
            question: `山东大学于 1972 年研制成功 DJL-1 计算机，设计存贮周期为 2μs，相当于现代计算机的（ ）。`,
            options: ["内存", "磁盘", "CPU", "显示器"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (内存)**

**核心解析：**
DJL-1 计算机是 1972 年山东大学研制的早期国产计算机。"存贮周期"指的是存储器进行一次读写操作所需的时间，2μs（微秒，即百万分之一秒）是当时半导体存储器典型的访问时间尺度。

- 内存（RAM）访问时间通常在纳秒到微秒级别，与 2μs 吻合
- 磁盘访问时间远慢于内存，通常在毫秒级（比微秒慢约 1000 倍）
- CPU 时钟周期在纳秒甚至皮秒级，远快于 2μs
- 显示器与"存贮周期"概念无关

因此，设计存贮周期为 2μs 对应的是现代计算机的**内存**部件。

**选项逐项分析：**
- **A 内存**：✅ 正确。2μs 的存贮周期与内存的访问时间特性相符。
- **B 磁盘**：❌ 错误。磁盘的机械读写时间通常在毫秒级，与 2μs 差距巨大。
- **C CPU**：❌ 错误。CPU 内部时钟周期在纳秒量级，比 2μs 快 1000 倍以上。
- **D 显示器**：❌ 错误。显示器是输出设备，其刷新周期与"存贮周期"概念完全不同。

**易错提醒：**
- "存贮"易被误写为"存储"，但两者都指 memory
- 存贮周期（存储周期）是内存专有术语，与外存（磁盘）无关

**知识延伸：**
现代 DDR4 内存的访问周期已降至约 0.4-0.6 纳秒，比 1972 年的 DJL-1 快了约 4000 倍，体现了半导体技术的飞速发展。

**考点：** 计算机发展史常识、硬件组成部件功能区分`,
            tags: ["计算机历史"]
        },
        {
            id: 2,
            type: 'single',
            question: `IPv4 版本的因特网总共有（ ）个 A 类地址网络。`,
            options: ["65000", "200 万", "126", "128"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (126)**

**核心解析：**
IPv4 地址采用 32 位二进制表示，按网络号长度分为 A、B、C、D 四类。

A 类地址的特征：
- 第一个字节的最高位固定为 0
- 网络号占 1 个字节（8 位），但最高位固定为 0，实际可用网络号只有 7 位
- 主机号占剩余 24 位

A 类地址的网络号取值范围：
- 最小：00000000₂ = 0（保留，用于默认路由）
- 最大：01111111₂ = 127（保留，用于本机环回测试）

因此，A 类地址网络的可分配范围是 1~126，共 **126 个**网络。

**选项逐项分析：**
- **A 65000**：❌ 错误。这个数字与 A 类地址数量无关，属于无依据的干扰项。
- **B 200 万**：❌ 错误。这是 C 类地址的理论总数（约 209 万），与 A 类地址数量不符。
- **C 126**：✅ 正确。A 类地址网络号从 1 到 126，共计 126 个。
- **D 128**：❌ 错误。虽然接近正确答案，但 128 是 8 位能表示的全部数量，未扣除保留的 0 号和 127 号网络。

**易错提醒：**
- 不要忘记 0.0.0.0（保留）和 127.0.0.0（环回）不参与 A 类网络编号
- A 类地址少但每个网络能容纳的主机多（1677 万多），这是设计初衷

**知识延伸：**
IPv4 地址分类中，A 类地址数量最少（126 个）但每个网络规模最大；C 类地址数量最多（约 209 万）但每个网络规模较小（254 台主机）。这种设计体现了"大网络少、小网络多"的地址分配策略。

**考点：** IPv4 地址分类规则、A 类地址网络数量计算`,
            tags: ["计算机常识"]
        },
        {
            id: 3,
            type: 'single',
            question: `在 C++ 中，下列不可做变量名的是（ ）。`,
            options: ["ccf-gesp", "ccf_gesp", "ccfGesp", "_ccfGesp"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (ccf-gesp)**

**核心解析：**
C++ 标识符（变量名、函数名等）的命名规则：
1. 由字母（a-z, A-Z）、数字（0-9）和下划线（_）组成
2. 不能以数字开头
3. 不能使用 C++ 关键字
4. **不能包含其他符号**，如连字符 \`-\`、空格、@、# 等

分析各选项：
- \`ccf-gesp\`：包含连字符 \`-\`，违反规则 1
- \`ccf_gesp\`：由字母和下划线组成，合法
- \`ccfGesp\`：由字母组成，符合驼峰命名习惯，合法
- \`_ccfGesp\`：以下划线开头，合法（下划线开头的标识符在 C++ 中允许，但通常保留给编译器或库使用）

**选项逐项分析：**
- **A ccf-gesp**：✅ 正确。不可做变量名。标识符中不能包含减号 \`-\`，违反 C++ 标识符命名规则。
- **B ccf_gesp**：❌ 错误。合法。可作为变量名，由字母和下划线组成，符合所有命名规则。
- **C ccfGesp**：❌ 错误。合法。可作为变量名，全部由字母组成，符合命名规则。
- **D _ccfGesp**：❌ 错误。合法。以下划线开头的标识符是允许的，虽然某些以下划线开头的名称被标准库保留，但用户自定义标识符使用下划线开头本身不违法。

**易错提醒：**
- C++ 标识符只允许下划线 \`_\`，不允许连字符 \`-\` 或其他符号
- 区分合法字符：字母（A-Z, a-z）、数字（0-9）、下划线（_）
- C++ 是大小写敏感的，\`ccfGesp\` 和 \`ccfgesp\` 是不同的标识符

**知识延伸：**
在 Python 等语言中，连字符 \`-\` 同样不能用于标识符，但 Python 使用下划线连接单词（如 \`ccf_gesp\`）的约定与 C++ 一致。C++ 程序员通常也采用 snake_case（下划线分隔）或 camelCase（驼峰命名）来命名标识符。

**考点：** C++ 标识符命名规范`,
            tags: ["变量命名"]
        },
        {
            id: 4,
            type: 'single',
            question: `在 C++ 中，与 for (int i = 1; i < 10; i++) 效果相同的是（ ）。`,
            options: ["for (int i = 0; i < 10; i++)", "for (int i = 0; i < 11; i++)", "for (int i = 1; i < 10; ++i)", "for (int i = 0; i < 11; ++i)"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (for (int i = 1; i < 10; ++i))**

**核心解析：**
逐行分析原循环 \`for (int i = 1; i < 10; i++)\`：
- 初始化：\`i = 1\`
- 条件：\`i < 10\` 为真时继续
- 循环体：执行 1 次
- 迭代：\`i++\`（后置自增）
- 终止：\`i\` 增到 10 时，\`i < 10\` 为假，循环结束

循环执行顺序：
\`\`\`
第1次：i=1, 条件成立 → 循环体 → i++ → i=2
第2次：i=2, 条件成立 → 循环体 → i++ → i=3
...
第9次：i=9, 条件成立 → 循环体 → i++ → i=10
第10次：i=10, 条件不成立 → 循环结束
\`\`\`
共执行 **10 次**，\`i\` 最终值为 10。

选项 C 使用 \`++i\`（前置自增）。在 for 循环的第三部分中，前置自增和后置自增的效果完全相同——都是在循环体执行后对 \`i\` 进行加 1 操作，区别仅在于表达式的返回值（但循环第三部分不使用该返回值）。

**选项逐项分析：**
- **A for (int i = 0; i < 10; i++)**：❌ 错误。\`i\` 从 0 开始，循环体执行的 10 次中 \`i\` 的值是 0~9，而原循环中是 1~9。
- **B for (int i = 0; i < 11; i++)**：❌ 错误。\`i\` 从 0 到 10 共 11 次，比原循环多执行 1 次。
- **C for (int i = 1; i < 10; ++i)**：✅ 正确。完全等效：相同初始值、相同终止条件、相同迭代方式（仅自增运算符形式不同）。
- **D for (int i = 0; i < 11; ++i)**：❌ 错误。\`i\` 从 0 到 10 共 11 次，比原循环多执行 1 次。

**易错提醒：**
- 在 for 循环的第三部分，自增运算符的前置与后置形式对循环次数**没有影响**（因为不使用表达式值）
- 但在独立表达式中（如 \`int a = i++\`），前置与后置的返回值不同，可能导致不同结果

**知识延伸：**
自增运算符 \`++i\`（前置）与 \`i++\`（后置）的区别在于返回值：
- \`++i\` 返回递增后的值（引用）
- \`i++\` 返回递增前的值的副本

在 for 循环的迭代部分，这个返回值不被使用，所以两者等效。但在 \`int b = a++\` 这样的语句中，前置与后置会产生不同的结果。

**考点：** for 循环语法、前置与后置自增运算符的行为差异`,
            tags: ["循环结构"]
        },
        {
            id: 5,
            type: 'single',
            question: `在 C++ 中，\`cout << (5 / 2 + 5 % 3)\` 的输出是（ ）。`,
            options: ["1", "2", "4", "5"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (4)**

**核心解析：**
表达式 \`5 / 2 + 5 % 3\` 的求值过程：

**步骤1：计算 \`5 / 2\`（整数除法）**
- 5 和 2 都是整数，执行整数除法
- 结果：5 ÷ 2 = 2（余 1），取整数部分，商为 **2**

**步骤2：计算 \`5 % 3\`（取模运算）**
- 5 % 3 表示 5 除以 3 的余数
- 5 ÷ 3 = 1（余 2），余数为 **2**

**步骤3：计算 \`2 + 2\`**
- 2 + 2 = **4**

因此，\`cout << (5 / 2 + 5 % 3)\` 的输出是 **4**。

**选项逐项分析：**
- **A 1**：❌ 错误。如果误以为 \`5 / 2 = 2.5\` 然后截断，可能算出错误结果；或混淆了运算符优先级。
- **B 2**：❌ 错误。可能是只算了 \`5 / 2\` 的结果（2），忽略了后半部分的 \`5 % 3\`。
- **C 4**：✅ 正确。\`(5 / 2) + (5 % 3) = 2 + 2 = 4\`。
- **D 5**：❌ 错误。可能是将 \`5 / 2\` 当作 \`5 - 2 = 3\`，或 \`5 % 3\` 当作 \`5 / 3 = 1\`，相加得 4 但误选。

**易错提醒：**
- 整数除法 \`5 / 2\` 结果是 2（截断小数部分），而不是 2.5
- \`%\` 是取模（求余数），不是整除；\`5 % 3 = 2\`，而不是 1
- 表达式中没有括号时，除法 \`/\` 和取模 \`%\` 的优先级相同（高于加法），且从左到右结合

**知识延伸：**
在 C++ 中，整数除法和取模运算有紧密关系：对于任意整数 \`a\` 和非零 \`b\`，有恒等式 \`a == (a / b) * b + (a % b)\`。例如 \`5 = (5/2)*2 + (5%2) = 2*2 + 1 = 5\`。这个恒等式常用于理解负数除法的取整规则。

**考点：** 算术运算符优先级、整数除法截断特性、取模运算规则`,
            tags: ["算术运算"]
        },
        {
            id: 6,
            type: 'single',
            question: `假定变量 a 和 b 可能是整型、字符型或浮点型，输入 -2 和 3.14 后，输出不可能是（ ）。`,
            options: ["1", "1.14", "47", "将触发异常"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (将触发异常)**

**核心解析：**
本题要求分析在变量 a、b 分别为不同类型（整型、字符型、浮点型），输入 -2 和 3.14 时各种运算结果的输出可能性。

**选项逐项分析：**
- **A (1)**：❌ 错误。可能。当 a=-2（整型）、b=3（浮点型3.14取整），或相关算术表达式得到结果为1时会出现。
- **B (1.14)**：❌ 错误。可能。当 a=-2（整型）、b=3.14（浮点型）进行混合运算，如 a+b 得到 -2+3.14=1.14。
- **C (47)**：❌ 错误。可能。在涉及字符运算或某些特定表达式时可能得到47，如 char 类型参与运算。
- **D (将触发异常)**：✅ 正确。标准 C++ 输入流对常见数据类型有良好容错性，使用 cin >> 进行基本类型输入不会触发异常。即使类型不完全匹配，也会尝试转换或按默认方式处理，不会"将触发异常"这种不可预期的结果。

**易错提醒：**
- 误以为 C++ 输入流在类型不完全匹配时会抛出异常，实际上标准输入操作有容错机制
- 混淆了异常处理机制与普通输入失败的区别

**知识延伸：**
C++ 输入流的错误处理通过 failbit、badbit 等状态位标记，而非抛出异常。这是 GESP 二级要求掌握的基础内容。

**考点：** C++ 输入流特性与基本数据类型转换`,
            tags: ["输入输出"]
        },
        {
            id: 7,
            type: 'single',
            question: `在 C++ 代码中假设 N 为正整数，则下面代码能获得其十进制个位数的是（ ）。`,
            options: ["N % 10", "N / 10", "N && 10", "以上选项均不正确"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (N % 10)**

**核心解析：**
取模运算符 % 用于求两个整数相除的余数。对于任意正整数 N：
- N / 10 得到的是 N 去掉最后一位后的数（即 N 整除 10 的结果）
- N % 10 得到的是 N 除以 10 的余数，即 N 的个位数

例如：N=47 时，47/10=4，47%10=7，个位数正是7。

**选项逐项分析：**
- **A (N % 10)**：✅ 正确。N % 10 恰好取出 N 的个位数。
- **B (N / 10)**：❌ 错误。整数除法会截断小数部分，N/10 得到的是 N 去掉个位数后的结果（如 47/10=4），不是个位数本身。
- **C (N && 10)**：❌ 错误。这是逻辑与运算符，N && 10 的结果是真（1）或假（0），取决于 N 是否为非零值，与获取个位数无关。
- **D (以上选项均不正确)**：❌ 错误。因为 A 选项本身是正确的，否定选项随之不成立。

**易错提醒：**
- 混淆整数除法（/）和取模运算（%）的作用：/ 取高位，% 取低位
- 将逻辑运算（&&）误当作算术运算理解

**知识延伸：**
取模运算常用于判断整除性（如 N%3==0 判断能否被3整除）、循环数组下标等场景，是 GESP 各级别的高频考点。

**考点：** 取模运算符 % 的应用`,
            tags: ["算术运算"]
        },
        {
            id: 8,
            type: 'single',
            question: `下面 C++ 代码执行后的输出是（ ）。\n\`\`\`cpp\nint N = 5, i = 0;\nwhile (N > 0) {\n    if (N % 3 == 0) {\n        cout << i << "#";\n        break;\n    }\n    N--;\n}\n\`\`\``,
            options: ["0#", "1#", "0#0#1", "没有输出"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (0#)**

**核心解析：**
逐行追踪代码执行过程：

初始化：N=5, i=0

第1轮循环：
- 条件 N>0 (5>0) 为真，进入循环体
- 检查 N%3==0：5%3=2，条件为假
- 执行 N--，N 变为 4

第2轮循环：
- 条件 N>0 (4>0) 为真，进入循环体
- 检查 N%3==0：4%3=1，条件为假
- 执行 N--，N 变为 3

第3轮循环：
- 条件 N>0 (3>0) 为真，进入循环体
- 检查 N%3==0：3%3=0，条件为真
- 执行 cout << i << "#"，输出 "0#"
- 执行 break，跳出循环

**选项逐项分析：**
- **A (0#)**：✅ 正确。第3轮循环时 N=3，N%3==0 成立，输出 i 的值（0）后跟符号 "#"。
- **B (1#)**：❌ 错误。i 始终为0未变，输出不会是 "1#"。
- **C (0#0#1)**：❌ 错误。循环中有 break，命中条件后会立即跳出，不会有多次输出。
- **D (没有输出)**：❌ 错误。N 从5递减，依次经过4、3，在 N=3 时 N%3==0 成立，必然有输出。

**易错提醒：**
- 忽略 break 语句的作用：一旦执行 break，立即跳出整个循环，不再继续后续迭代
- 未仔细追踪 N 的递减过程，导致判断命中时机错误

**知识延伸：**
break 语句只能跳出其所在的最内层循环体。在嵌套循环中，break 只退出一层循环。这是 GESP 二级要求掌握的控制语句。

**考点：** while 循环与 break 语句的执行逻辑`,
            tags: ["程序分析"]
        },
        {
            id: 9,
            type: 'single',
            question: `执行下面 C++ 代码并输入 1 和 0，有关说法正确的是（ ）。\n\`\`\`cpp\nint a, b;\ncin >> a >> b;\nif (a && b) cout << "1";\nelse if (!(a || b)) cout << "2";\nelse if (a || b) cout << "3";\nelse cout << "4";\n\`\`\``,
            options: ["输出 1", "输出 2", "输出 3", "输出 4"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (输出 3)**

**核心解析：**
根据题目条件：输入 a=1, b=0

逐步分析各条件判断：

1. 第一个 if：a && b = 1 && 0 = 0（假），不执行

2. 第二个 else if：!(a || b) = !(1 || 0) = !(1) = 0（假），不执行

3. 第三个 else if：a || b = 1 || 0 = 1（真），执行，输出 "3"

4. 第4个 else：因第3个条件已命中，不执行

**选项逐项分析：**
- **A (输出 1)**：❌ 错误。输出1需要满足 a && b 为真，即 a 和 b 同时非零。但 b=0，所以 a && b=0，不满足。
- **B (输出 2)**：❌ 错误。输出2需要满足 !(a || b) 为真，即 a 和 b 同时为0。但 a=1，所以 a||b=1，!(a||b)=0，不满足。
- **C (输出 3)**：✅ 正确。输出3需要满足 a || b 为真且前两个条件都不满足。a=1 使得 a||b=1，同时 !(a||b)=0，前两个条件都不成立。
- **D (输出 4)**：❌ 错误。输出4是最后的 else 分支，只有当前面所有条件都不满足时才执行。但 a||b=1 为真，第3个条件已命中。

**易错提醒：**
- 混淆 if-else if 链与独立 if 语句的区别：else if 链是互斥的，只会执行其中一个分支
- 误以为 else if 后的 else 会在"所有条件都不满足"时执行，但第三个 else if (a||b) 在 a=1 时必然为真

**知识延伸：**
逻辑运算符的优先级：!（非）> &&（与）> ||（或）。在复杂表达式中，括号可以明确优先级，使代码更清晰。

**考点：** 逻辑运算符与 if-else if 多重分支结构`,
            tags: ["逻辑运算"]
        },
        {
            id: 10,
            type: 'single',
            question: `下面 C++ 代码执行后的输出是（ ）。\n\`\`\`cpp\nint loopCount = 0;\nfor (int i = 1; i < 5; i += 2)\n    loopCount += 1;\ncout << (loopCount);\n\`\`\``,
            options: ["1", "2", "3", "5"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (2)**

**核心解析：**
分析 for 循环的迭代过程：

循环初始化：int i=1
循环条件：i < 5
步进更新：i += 2（即 i = i + 2）

逐次迭代追踪：

第1次迭代：
- i=1，条件 1<5 为真
- 执行循环体：loopCount += 1，loopCount=1
- 执行 i+=2，i=3

第2次迭代：
- i=3，条件 3<5 为真
- 执行循环体：loopCount += 1，loopCount=2
- 执行 i+=2，i=5

第3次迭代：
- i=5，条件 5<5 为假
- 循环结束，不执行循环体

最终 loopCount = 2

**选项逐项分析：**
- **A (1)**：❌ 错误。只算了第1次迭代，忽略了第2次。
- **B (2)**：✅ 正确。循环执行了2次，loopCount 累加2次。
- **C (3)**：❌ 错误。误以为循环会执行3次，实际上 i=5 时条件不满足。
- **D (5)**：❌ 错误。i 的初值是1，不是5。

**易错提醒：**
- 遗漏循环步进：i 每次增加2，导致循环次数比预期少
- 将 i 的终值（5）与 loopCount 的值混淆

**知识延伸：**
for 循环的三个部分都可以包含复杂表达式。步进部分 i += 2 等价于 i = i + 2，是常见的简写形式。理解循环变量的变化规律是分析循环执行次数的关键。

**考点：** for 循环的执行次数与循环变量追踪`,
            tags: ["循环分析"]
        },
        {
            id: 11,
            type: 'single',
            question: `下图是 C++ 程序执行后的输出（右对角线对齐图案）。横线处应填入代码是（ ）。\n\`\`\`cpp\nfor (int i = 1; i < 6; i++) {\n    for (int j = ________; j < i * 2; j++) \n        cout << j;\n    cout << endl;\n}\n\`\`\``,
            options: ["j = i; j < i", "j = 1; j < i", "j = i", "j = i + 1"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (j = i)**

**核心解析：**
分析这段代码的输出规律。\`i\` 从 1 到 5，外层循环共执行 5 次，每次输出 \`j\` 的值直到 \`j < i * 2\`。若要形成"右对角线对齐图案"，每行的数字个数应与行号 \`i\` 相等（因为 \`i * 2 - i = i\`）。

- i=1 时：j 从 i=1 开始，j<2，打印 j=1 → 输出 "1"
- i=2 时：j 从 i=2 开始，j<4，打印 j=2,3 → 输出 "23"
- i=3 时：j 从 i=3 开始，j<6，打印 j=3,4,5 → 输出 "345"
- i=4 时：j 从 i=4 开始，j<8，打印 j=4,5,6,7 → 输出 "4567"
- i=5 时：j 从 i=5 开始，j<10，打印 j=5,6,7,8,9 → 输出 "56789"

每行输出的数字个数恰好等于行号 i，且起始数字随行号递增而递增，形成右对齐的对角线视觉效果。

**选项逐项分析：**
- **A (j = i; j < i)**：❌ 错误。初始值 \`j = i\` 等于循环条件上界 \`j < i\`，首次判断即为 false，循环体一次都不执行，无法输出任何字符。
- **B (j = 1; j < i)**：❌ 错误。若 \`j\` 从 1 开始，i=2 时会打印 j=1（而不是从 2 开始），输出变成 "12" 而非 "23"，图案规律被破坏。
- **C (j = i)**：✅ 正确。满足每行打印 i 个连续数字的要求，形成右对齐效果。
- **D (j = i + 1)**：❌ 错误。起始值比 i 大 1，i=1 时循环体不执行（1 < 2 不成立），首行为空，图案从第二行开始，与预期不符。

**易错提醒：**
- 误以为右对齐需要空格填充，忽略了每行数字个数递增本身就能形成对角线视觉效果。
- 循环边界用 \`<\` 还是 \`<=\` 不清楚：这里 \`j < i * 2\` 是正确的，若错写成 \`j <= i * 2\` 会多输出一个数字。

**知识延伸：**
打印右对齐三角形的核心是控制每行的起始位置和字符数量。可以用起始空格数（\`6 - i\` 个空格）或控制循环变量范围来实现，另一种常见写法是内层 \`for (int j = 1; j <= i; j++)\` 打印前 i 个数字。

**考点：** 嵌套循环中循环变量的范围控制`,
            tags: ["嵌套循环"]
        },
        {
            id: 12,
            type: 'single',
            question: `下面 C++ 代码用于输出逆序数。横线处应填入（ ）。\n\`\`\`cpp\nint N, rst = 0;\ncin >> N;\nwhile (N) {\n    ________________;\n    ________________;\n}\ncout << rst;\n\`\`\``,
            options: ["rst = rst * 10 + N % 10; N = N / 10;", "rst += N % 10; N = N / 10;", "rst = rst * 10 + N / 10; N = N % 10;", "rst += N / 10; N = N % 10;"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (rst = rst * 10 + N % 10; N = N / 10;)**

**核心解析：**
逆序数的生成原理：每次取出原数 N 的最低位（\`N % 10\`），将其追加到结果 rst 的最低位（\`rst * 10\` 将已有位左移），然后将 N 的最低位移除（\`N / 10\`）。

以输入 N=123 为例，手动推演：

| 轮次 | N 的值 | N % 10 | rst = rst * 10 + N % 10 | N = N / 10 |
|------|--------|--------|-------------------------|------------|
| 初始 | 123    | —      | rst = 0                 | —          |
| 第1轮 | 123    | 3      | rst = 0*10+3 = 3        | N = 12     |
| 第2轮 | 12     | 2      | rst = 3*10+2 = 32       | N = 1      |
| 第3轮 | 1      | 1      | rst = 32*10+1 = 321     | N = 0      |

N=0 跳出循环，输出 rst=321，逆序成功。

**选项逐项分析：**
- **A (rst = rst * 10 + N % 10; N = N / 10;)**：✅ 正确。\`rst * 10\` 将已有位左移腾出末位，\`N % 10\` 取最低位，\`N / 10\` 移除已处理的位，三步缺一不可。
- **B (rst += N % 10; N = N / 10;)**：❌ 错误。\`rst += N % 10\` 是简单的加法操作，没有"左移十位"的效果。以 N=123 为例：rst=0+3=3，再加 2 得 5，再加 1 得 6，输出 6 而非 321。
- **C (rst = rst * 10 + N / 10; N = N % 10;)**：❌ 错误。\`N / 10\` 取的是去掉最低位后的值（如 123/10=12），而不是最低位（应该是 3）。这会导致位的位置完全错乱。
- **D (rst += N / 10; N = N % 10;)**：❌ 错误。同时犯了两处错误：加法没有进位，且取的是高位数字而非低位数字，结果完全错误。

**易错提醒：**
- 混淆"取低位"（\`% 10\`）和"去低位"（\`/ 10\`）的用途：前者用于获取当前位，后者用于更新 N。
- 忘记 \`rst * 10\` 左移操作，导致所有位叠加在同一位置上。

**知识延伸：**
逆序数也可以用字符串实现：先 \`to_string(N)\` 转字符串，再反向遍历拼接。另一种扩展是正序输出各位数字（逐位取高位）。

**考点：** 整数的位运算思维（取模/除法）`,
            tags: ["数位处理"]
        },
        {
            id: 13,
            type: 'single',
            question: `下面 C++ 代码用于输入学生成绩计算平均成绩。说法错误的是（ ）。\n\`\`\`cpp\nfloat Sum = 0;\nint cnt = 0, score;\nwhile (1) {\n    cin >> score;\n    if (score < 0) break;\n    Sum += score;\n    cnt++;\n}\ncout << Sum / cnt;\n\`\`\``,
            options: ["代码 while (1) 写法语法错误", "如果输入负数，将结束输入并输出", "变量 int score 初始值不确定不影响逻辑", "若第一个输入即为负数，可能导致除零错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (代码 while (1) 写法语法错误)**

**核心解析：**
本题要求选出"说法错误的是"，即找出不正确的说法。逐项分析：

**关于 while(1) 语法：** \`while(1)\` 是 C++ 中合法且常见的无限循环写法。整型常量 1 被隐式转换为布尔值 \`true\`，因此 \`while(1)\` 等价于 \`while(true)\`。编译器不会报错，这是标准用法。

**以 N=123 为例验证程序逻辑：**
\`\`\`cpp
while (1) {                    // 无限循环
    cin >> score;              // 输入 123（正数）
    if (score < 0) break;      // 123 < 0 为 false，不执行 break
    Sum += score;              // Sum = 123
    cnt++;                     // cnt = 1
}
// 若再输入负数，触发 break，输出 Sum/cnt
\`\`\`

**选项逐项分析：**
- **A (代码 while (1) 写法语法错误)**：✅ 正确。该项不符合题目限定，正是本题要选的项。正确（**该说法错误**）。\`while(1)\` 语法完全合法，不是错误说法，因此作为"错误的说法"是正确的选项。
- **B (如果输入负数，将结束输入并输出)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（该说法**正确**）。代码中 \`if (score < 0) break;\` 明确表示遇到负数时退出循环。
- **C (变量 int score 初始值不确定不影响逻辑)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（该说法**正确**）。\`score\` 在使用前通过 \`cin >> score\` 被赋值，初始值不确定不影响程序正确性。
- **D (若第一个输入即为负数，可能导致除零错误)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（该说法**正确**）。若首次输入负数，\`cnt\` 保持为 0，最后执行 \`Sum / cnt\` 会触发除零错误（浮点数除以零在 C++ 中是未定义行为或产生 inf）。

**易错提醒：**
- 注意审题：本题要求选出"说法错误的是"，而非"正确的说法"，A 选项描述本身是错误的（因为 while(1) 合法），所以 A 是要找的答案。
- 区分语法错误（编译失败）和逻辑错误（运行结果不对）。

**知识延伸：**
C++ 中表示无限循环的三种常见写法：\`while(1)\`、\`while(true)\`、\`for(;;)\`，三者等价。\`break\` 语句是退出循环的唯一正常出口。

**考点：** while 循环语法、程序鲁棒性分析`,
            tags: ["循环控制"]
        },
        {
            id: 14,
            type: 'single',
            question: `以下 C++ 代码判断正整数 N 是否为质数。横线上填写（ ）。\n\`\`\`cpp\nint N;\ncin >> N;\nbool Flag = (N >= 2);\nfor (int i = 2; i * i <= N; i++) {\n    if (________________) {\n        Flag = false;\n        break;\n    }\n}\n\`\`\``,
            options: ["num % i", "N % i == 0", "N / i", "N / i == 0"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (N % i == 0)**

**核心解析：**
质数的定义：一个大于等于 2 的正整数，如果只能被 1 和它本身整除，则为质数。程序的核心思路是：遍历 2 到 √N 之间的所有整数 i，如果存在某个 i 能整除 N（\`N % i == 0\`），则 N 不是质数。

**为什么只需遍历到 √N：**
如果 N 是合数，它必有至少一个因子不大于 √N（否则两个因子都大于 √N，乘积会大于 N）。因此只需检查到 \`i * i <= N\` 即可，大幅减少循环次数。

**逐项验证选项逻辑：**
- 假设 N=15，i=3 时：15 % 3 = 0（余数为 0），\`N % i == 0\` 为 true，触发 \`Flag = false\`，正确识别为合数。
- 假设 N=7（质数）：遍历 i=2,3 时 7%2≠0、7%3≠0，均不满足条件，循环结束 \`Flag\` 保持 true，正确识别为质数。

**选项逐项分析：**
- **A (num % i)**：❌ 错误。变量 \`num\` 未在代码中定义（题目用的是 N），且表达式缺少比较运算符，不能作为 \`if\` 的条件。
- **B (N % i == 0)**：✅ 正确。\`N % i\` 表示 N 除以 i 的余数，为 0 时说明 i 是 N 的因子，N 不是质数。
- **C (N / i)**：❌ 错误。\`N / i\` 是整数除法的商，始终是确定的非零值（非 0 即真），但不能用来判断整除。15/3=5 为真，但这无法反映 3 整除 15 的事实。
- **D (N / i == 0)**：❌ 错误。\`N / i\` 的结果不可能为 0（因为 i ≤ √N < N），该条件永远为 false，永远不会触发 break，逻辑完全错误。

**易错提醒：**
- 区分取模运算符 \`%\`（求余数）和除法运算符 \`/\`（求商）：判断整除必须用 \`%\`，商为 0 没有意义。
- 注意变量名的大小写：代码中定义的是 \`N\`，不是 \`num\`。

**知识延伸：**
质数判定的优化还包括：跳过偶数（除 2 外）、使用埃拉托斯特尼筛法批量筛选质数、判断到 √N 即可停止等，这些是 GESP 三级常考点。

**考点：** 质数判定算法、取模运算`,
            tags: ["质数判定"]
        },
        {
            id: 15,
            type: 'single',
            question: `假设输入正整数 N，以下 C++ 代码用于实现十进制转二进制。横线处应填写（ ）。\n\`\`\`cpp\nint N;\ncin >> N;\nstring s = "";\nwhile (N > 0) {\n    ________________;\n    N /= 2;\n}\ncout << s;\n\`\`\``,
            options: ["s = to_string(N % 2) + s", "s = s + to_string(N % 2)", "s = to_string(N / 2) + s", "s = s + to_string(N / 2)"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (s = to_string(N % 2) + s)**

**核心解析：**
十进制转二进制的原理：反复将 N 除以 2，依次得到最低位、次低位、……最高位。余数序列的逆序即为二进制结果。

**以 N=13 为例验证（13 的二进制为 1101）：**

| 轮次 | N 的值 | N % 2（余数） | N / 2（商） | s = to_string(N%2) + s 的执行结果 |
|------|--------|--------------|-------------|-----------------------------------|
| 初始 | —      | —            | —           | s = ""                            |
| 第1轮 | 13     | 1            | 6           | s = "1" + "" = "1"                |
| 第2轮 | 6      | 0            | 3           | s = "0" + "1" = "01"              |
| 第3轮 | 3      | 1            | 1           | s = "1" + "01" = "101"            |
| 第4轮 | 1      | 1            | 0           | s = "1" + "101" = "1101"          |

N=0 循环结束，输出 s="1101"，正确。

**核心技巧：** 每得到一个余数就加到字符串 s 的**前面**（\`... + s\`），这样先得到的低位自然被挤到后面，最终得到正序的二进制字符串。

**选项逐项分析：**
- **A (s = to_string(N % 2) + s)**：✅ 正确。将新余数拼接到字符串左侧，随着循环进行，低位逐渐被推到右侧，最终得到正序结果。
- **B (s = s + to_string(N % 2))**：❌ 错误。拼接在右侧会导致字符顺序反转。仍以 N=13 为例：第1轮 s="1"，第2轮 s="10"，第3轮 s="101"，第4轮 s="1011"，输出 "1011"（逆序），错误。
- **C (s = to_string(N / 2) + s)**：❌ 错误。使用 \`N / 2\`（商）而非 \`N % 2\`（余数）。以 N=13 为例，第1轮取 6（商）而非 1（余），从第一步就错了。
- **D (s = s + to_string(N / 2))**：❌ 错误。同时犯了两处错误：拼接在右侧（逆序）且取的是商而非余数。

**易错提醒：**
- 进制转换中"拼接顺序"是关键：每次得到的是低位，应放在字符串左侧（前面）。
- 区分取余（\`%\`，得到当前位）和整除（\`/\`，更新 N）。

**知识延伸：**
进制转换可扩展到其他进制（八进制、十六进制）。核心都是"反复取余、累积结果"，只是进制基数和字符集不同。另一种等价的写法是用数组暂存余数，最后逆序输出。

**考点：** 进制转换算法、字符串操作`,
            tags: ["数制转换"]
        },
        {
            id: 16,
            type: 'judge',
            question: `小杨最近开始学习 C++ 编程，老师说 C++ 是一门面向对象的编程语言，也是一门高级语言。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. 面向对象编程（OOP）是 C++ 的核心特性之一。C++ 支持封装（encapsulation）、继承（inheritance）和多态（polymorphism）三大 OOP 支柱，开发者可以定义类（class）来组织数据和行为。
2. 高级语言是指相对于汇编语言等低级语言而言，具备更高抽象层次、不直接操作硬件寄存器和内存地址的语言。C++ 虽然保留了直接操作内存的能力（如指针），但其整体设计目标是提供高级抽象，同时保留系统级编程能力，因此被归类为高级语言。
3. 综合以上两点，题目描述完全符合 C++ 语言的实际情况。

**易错提醒：**
- 部分学员可能混淆“高级语言”与“完全高级语言”的概念。C++ 兼具高级特性与低级特性，属于多范式语言。
- 还有学员可能只关注 C++ 的面向过程特性，忽视了它同样强大的面向对象能力。

**知识延伸：**
C++ 既支持面向对象编程（通过 class 和继承机制），也支持面向过程编程（通过函数），还支持泛型编程（通过模板），是多范式语言的典型代表。

**考点：** 编程语言基本特性常识`,
            tags: ["判断题"]
        },
        {
            id: 17,
            type: 'judge',
            question: `在 C++ 中，\`cout << (3, 4, 5);\` 可以输出 \`3 4 5\`，且每个输出项之间用空格分开。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. 表达式 \`(3, 4, 5)\` 是 C++ 中的**逗号表达式**。
2. 逗号表达式的求值规则是：从左到右依次计算各个子表达式，整个逗号表达式的结果是**最后一个子表达式的值**。
3. 因此 \`(3, 4, 5)\` 的计算过程为：先计算 3（丢弃），再计算 4（丢弃），最后计算 5，整个表达式的结果为 5。
4. \`cout << (3, 4, 5)\` 实际输出的是单个整数 5，而不是 "3 4 5"。
5. 即使要输出三个数字，也需要使用多个 \`cout\` 语句或 \`<<\` 操作符分别输出每个数字。

**易错提醒：**
- 逗号表达式的结果是最右侧操作数，这一点极易与数组初始化或函数参数的逗号用法混淆。
- 有学员可能误以为逗号表达式会把所有值都输出，实际上逗号只是分隔符，只取最后一个值。

**知识延伸：**
逗号表达式在 C++ 中优先级最低，且具有从左到右的结合性。常见的陷阱场景是在条件表达式中误用逗号，例如 \`if (a = 5, a > 3)\` 实际判断的是 \`a > 3\` 部分。

**考点：** 逗号运算符的结合性与返回值规则`,
            tags: ["判断题"]
        },
        {
            id: 18,
            type: 'judge',
            question: `C++ 表达式 \`12 % 10 % 10\` 的值为 2。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. 算术运算符 \`%\`（取模）遵循从左到右的结合性。
2. 第一步计算：\`12 % 10\`
   - 12 除以 10 商 1 余 2
   - 结果为 2
3. 第二步计算：\`2 % 10\`
   - 2 除以 10 商 0 余 2（因为 2 < 10，被除数本身就是余数）
   - 结果为 2
4. 最终结果为 2，与题目描述一致。

**易错提醒：**
- 当被除数小于除数时，取模结果就是被除数本身（因为不够除），这一点容易忽略。
- 有学员可能错误地认为 \`12 % 10 % 10\` 等价于 \`12 % (10 % 10) = 12 % 0\`，导致除零错误。实际上取模运算符是左结合的。

**知识延伸：**
取模运算的结果符号与被除数一致（C++ 中规定），且除数不能为 0。在 GESP 二级考试中，理解运算符结合性是常考点。

**考点：** 算术运算符结合性及取模运算基础`,
            tags: ["判断题"]
        },
        {
            id: 19,
            type: 'judge',
            question: `C++ 语句 \`cout << rand() << ' ' << rand();\` 的第二个输出值一定较大。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. \`rand()\` 函数是 C++ 标准库 \`<cstdlib>\` 提供的伪随机数生成函数。
2. 伪随机数是通过确定性算法生成的数列，看似随机但实际可预测。其“随机性”体现在数值在设定范围内呈均匀分布，但不保证任意两次调用的大小关系。
3. 连续两次调用 \`rand()\`，第二次的返回值可能大于第一次、可能小于第一次、也可能等于第一次（如在极小概率下），不存在“一定较大”的必然关系。
4. 题目中的“一定较大”是强加的确定性格式要求，与随机数本质不符。

**易错提醒：**
- 学员容易将“随机”与“交替”“递增”等概念混淆。随机意味着每个结果独立，不受前序结果影响。
- 如果需要生成更大范围的随机数序列，通常的做法是调用 \`srand()\` 播种不同随机种子，而非依赖调用顺序。

**知识延伸：**
C++11 引入了更强大的随机数库 \`<random>\`，提供了多种随机数引擎（如 \`mt19937\`）和分布类型，可生成更优质的随机数序列，比传统的 \`rand()\` 更推荐使用。

**考点：** 随机数函数的特性与局限性`,
            tags: ["判断题"]
        },
        {
            id: 20,
            type: 'judge',
            question: `定义 C++ 的 int 类型的变量 ch，而且值为 '1'，则语句 \`cout << int(ch);\` 的输出为 1。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. 题目设定：\`char ch = '1';\`，这里 \`'1'\` 是字符字面量，而非数字 1。
2. 在 C++ 中，字符在内存中以 ASCII 码值存储。字符 \`'1'\` 对应的 ASCII 码是十进制 49（二进制为 110001）。
3. \`int(ch)\` 执行的是将字符变量 \`ch\` 的值（ASCII 码 49）强制转换为 \`int\` 类型。
4. 转换后输出的是 ASCII 码值 49，而不是数字 1。
5. 如果要让 \`cout << int(ch)\` 输出 1，则 \`ch\` 应该存储字符 \`'1'\`（ASCII 49），但需要通过 \`'1' - '0'\` 或 \`'1' - 48\` 等运算来获取数值 1。

**易错提醒：**
- 字符 \`'1'\` 与数字 \`1\` 在 C++ 中是完全不同的值，混淆两者的 ASCII 码是常见错误。
- 初学者常误以为 \`int('1')\` 会直接得到数值 1，实际上得到的是字符 '1' 的编码 49。
- 另一个易错点：字符 \`'0'\` 的 ASCII 码是 48，因此 \`'1' - '0'\` 才等于 1（利用相邻字符的 ASCII 码差值为 1）。

**知识延伸：**
C++ 中常用字符的 ASCII 码需要记忆：'0'-'9' 对应 48-57，'A'-'Z' 对应 65-90，'a'-'z' 对应 97-122。掌握这些基础编码有助于理解字符与整数之间的转换关系。

**考点：** 字符存储与 ASCII 码映射、类型转换输出`,
            tags: ["判断题"]
        },
        {
            id: 21,
            type: 'judge',
            question: `下面 C++ 代码执行后将输出 10：\n\`\`\`cpp\nint i;\nfor (i = 0; i < 10; i++) continue;\nif (i == 10) cout << 10;\n\`\`\``,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. \`int i;\` 定义整型变量 \`i\`，初始值未定义。
2. 进入 \`for\` 循环：\`i = 0\`，判断 \`0 < 10\` 为真，执行循环体。
3. 循环体只有 \`continue\`，表示跳过本次循环剩余语句，直接进入下一次循环。
4. 每次循环结束后执行 \`i++\`，\`i\` 依次变为 1, 2, 3, ... 9。
5. 当 \`i = 9\` 时判断 \`9 < 10\` 为真，进入循环后 \`continue\`，然后 \`i++\` 变为 10。
6. 当 \`i = 10\` 时判断 \`10 < 10\` 为假，循环终止，\`i\` 的最终值为 10。
7. 执行 \`if (i == 10)\`，条件成立，输出 10。

**易错提醒：**
- \`continue\` 只是跳过本次循环体的剩余语句，不会终止循环，也不会阻止 \`i++\` 的执行。
- 循环变量在循环终止后的值容易误判为循环上限减 1（9），实际上应为上限（10）。

**知识延伸：**
for 循环的执行顺序为：初始化 → 条件判断 → 循环体 → 迭代表达式，循环终止后循环变量的值会比最后一次条件为真的值多一次 \`i++\`。

**考点：** for 循环终止状态下循环变量的精确值判定`,
            tags: ["判断题"]
        },
        {
            id: 22,
            type: 'judge',
            question: `下面 C++ 代码用于求正整数 N 到 M 之间所有整数之和（包含 N 和 M）：\n\`\`\`cpp\nint N, M, Sum;\ncin >> N >> M;\nfor (int i = N; i <= M; i++) Sum += i;\ncout << Sum;\n\`\`\``,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. 代码中 \`int N, M, Sum;\` 定义了 \`Sum\` 变量但未初始化。
2. 在 C++ 中，局部变量（函数内部的变量）的值是未定义的随机值（垃圾值），不是 0。
3. 执行 \`Sum += i\` 时，会将 \`Sum\` 的随机值与 \`i\` 累加，导致结果不可预测。
4. 只有当 \`Sum\` 初始化为 0 时，\`Sum += i\` 才能正确计算累加和。
5. 正确的做法是在定义时写成 \`int Sum = 0;\`。

**易错提醒：**
- 全局变量和静态变量会自动初始化为 0，但局部变量不会。
- 即使变量名暗示用途（如 \`Sum\` 表示求和），也不代表会自动初始化为 0。

**知识延伸：**
变量初始化是程序正确性的基础。在 GESP 二级编程实践中，建议养成定义变量时立即初始化的习惯，尤其是用于累加或计数的变量。

**考点：** 变量初始化、程序缺陷分析`,
            tags: ["判断题"]
        },
        {
            id: 23,
            type: 'judge',
            question: `下面 C++ 代码调整了外层循环的范围，由 \`for (int i = 1; i <= 4; i++)\` 调整为 \`for (int i = 0; i <= 4; i++)\`，输出结果相同。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. 原循环为 \`for (int i = 1; i <= 4; i++)\`，循环变量 \`i\` 依次取 1, 2, 3, 4。
2. 调整为 \`for (int i = 0; i <= 4; i++)\` 后，\`i\` 依次取 0, 1, 2, 3, 4。
3. 若循环体内进行的是求和运算（如 \`sum += i\`），当 \`i = 0\` 时，\`0\` 对求和结果没有任何贡献。
4. 因此两种写法的求和结果相同，都是 1+2+3+4 = 10。
5. 该结论同样适用于其他满足交换律和结合律的运算。

**易错提醒：**
- 若循环体内不是求和操作（如 \`cout << i\` 输出每个 \`i\` 的值），调整起始值会改变输出序列。
- 只有当 \`i=0\` 对运算结果无影响时，该调整才成立。

**知识延伸：**
在枚举类题目中，灵活调整循环起始值可以简化边界处理，但需确认 0 对当前运算是否"中性"。

**考点：** 循环边界变动对结果影响的分析`,
            tags: ["判断题"]
        },
        {
            id: 24,
            type: 'judge',
            question: `下面 C++ 代码用于求满足条件的第 N 个正整数：\n\`\`\`cpp\nint N, cnt = 0, num = 0;\ncin >> N;\nwhile (cnt < N) {\n    num++;\n    if (满足条件) cnt++;\n}\ncout << num;\n\`\`\``,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. \`cnt\` 初始化为 0，用于记录已找到的满足条件的整数个数。
2. \`num\` 初始化为 0，表示当前尝试的正整数。
3. 进入 \`while (cnt < N)\` 循环：当 \`cnt\` 小于目标 \`N\` 时继续。
4. 循环体内 \`num++\`，将 \`num\` 增大 1（从 0 变为 1, 2, 3, ... 依次尝试）。
5. 若当前 \`num\` 满足条件，则 \`cnt++\`；否则 \`cnt\` 不变。
6. 重复步骤 3-5，直到 \`cnt\` 达到 \`N\`，即找到第 \`N\` 个满足条件的数。
7. 循环结束时 \`num\` 正好是第 \`N\` 个满足条件的正整数，输出 \`num\`。

**易错提醒：**
- \`num\` 从 0 开始递增，但题目要求的是"第 N 个正整数"，第 1 个正整数是 1。当 \`num=1\` 满足条件时 \`cnt\` 变为 1，逻辑正确。
- 该模式适用于查找第 K 个满足特定条件的数，是枚举法的经典结构。

**知识延伸：**
"枚举+计数"模式常用于解决"第 N 个满足条件的数"类问题，时间复杂度为 O(答案的数值大小)，适合答案数值不太大的场景。

**考点：** 计数器循环逻辑的实现`,
            tags: ["判断题"]
        },
        {
            id: 25,
            type: 'judge',
            question: "下面 C++ 代码执行时如果输入 2024，则输出是 4202。（ ）\n```cpp\nint N, flag = 0;\ncin >> N;\nwhile (N) {\n    if (!flag)\n        cout << N % 10;\n    N /= 10;\n    flag = (flag + 1) % 2;\n}\n```",
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. 题目声称代码执行后，输入 2024 输出 4202。
2. 分析 2024 的数字组成：千位 2、百位 0、十位 4、个位 2。
3. 数字逆序输出的正确结果应为 4202（个位 2 → 十位 4 → 百位 0 → 千位 2）。
4. 若代码中存在对数字 0 的错误处理（如跳过中间出现的 0、或者未正确处理进位），则可能输出错误值。
5. 常见的错误包括：在 \`while (n > 0)\` 循环中当某位为 0 时被跳过，或未考虑首位为 0 的情况，导致输出 422 或其他不完整结果。
6. 题目给出的预期输出与题目描述不一致，故判定为错误。

**易错提醒：**
- 逆序输出数字时，特别要注意数字中间或末尾的 0 不能丢失。
- 若使用 \`while (n > 0)\` 循环，当 n=2024 时处理到某一步会得到 n=20，\`20 % 10 = 0\`，必须正确输出这个 0。
- 不要将"2024 逆序"误写成"422"（漏掉中间的 0）。

**知识延伸：**
逆序输出整数的常见正确写法为：
\`\`\`cpp
while (n > 0) {
    cout << n % 10;
    n /= 10;
}
\`\`\`
需确保循环能正确处理包含 0 的数字，如 2024、1050 等。

**考点：** 算法追踪、逆序数细节处理`,
            tags: ["判断题"]
        }
    ],
    programmingQuestions: [
        {
            id: 26,
            type: 'programming',
            title: "美丽数字",
            samples: [
                {
                    input: `3\n7\n52\n103`,
                    output: `Yes\nYes\nNo`
                }
            ],
            question: `
# [GESP202409 二级] 数位之和

## 题目描述

小杨有 $n$ 个正整数，他认为一个正整数是美丽数字当且仅当该正整数每一位数字的总和是 $7$ 的倍数。

小杨想请你编写一个程序判断 $n$ 个正整数哪些是美丽数字。

## 输入格式

第一行包含一个正整数 $n$，表示正整数个数。
之后 $n$ 行，每行包含一个正整数 $a_i$。

## 输出格式

对于每个正整数输出一行一个字符串，如果是美丽数字则输出 \`Yes\`，否则输出 \`No\`。
`,
            score: 25,
            explanation: `**解析：**
本题考察数位拆解与模运算。
1. **数位之和计算**：使用 \`while(x)\` 循环，通过 \`x % 10\` 累加每一位数字，再通过 \`x /= 10\` 降位。
2. **倍数判断**：计算得到的总和如果对 7 取模等于 0（\`sum % 7 == 0\`），则说明是 7 的倍数。
3. **多组数据处理**：外层使用 \`while(n--)\` 处理多组输入。

**核心逻辑提示：**
\`\`\`cpp
int sum = 0;
while (num > 0) {
    sum += num % 10;
    num /= 10;
}
if (sum % 7 == 0) cout << "Yes";
else cout << "No";
\`\`\``,
            tags: ["编程题", "模拟", "数位分解"],
            template: "#include <iostream>\nusing namespace std;\nint main() {\n    // 在此编写代码\n    return 0;\n}",
            referenceCode: "#include <iostream>\nusing namespace std;\nint main() {\n    int n; cin >> n;\n    while(n--) {\n        long long a; cin >> a;\n        int s = 0;\n        while(a > 0) { s += a % 10; a /= 10; }\n        if (s % 7 == 0) cout << \"Yes\" << endl;\n        else cout << \"No\" << endl;\n    }\n    return 0;\n}"
        },
        {
            id: 27,
            type: 'programming',
            title: "小杨的 N 字矩阵",
            samples: [
                {
                    input: `5`,
                    output: `+---+\n++--+\n+-+-+\n+--++\n+---+\n`
                }
            ],
            question: `
# [GESP202409 二级] 小杨的 N 字矩阵

## 题目描述

小杨想要构造一个 $m \times m$ 的 N 字矩阵（$m$ 为奇数），这个矩阵的从左上角到右下角的对角线、第 $1$ 列和第 $m$ 列都是半角加号 \`+\` ，其余都是半角减号 \`-\` 。例如，一个 $5 \times 5$ 的 N 字矩阵如下：

\`\`\`plain
+---+
++--+
+-+-+
+--++
+---+
\`\`\`

请你帮小杨根据给定的 $m$ 打印出对应的 N 字矩阵。

## 输入格式

输入只有一行包含一个正整数 $m$。

## 输出格式

输出对应的 N 字矩阵。
`,
            score: 25,
            explanation: `**解析：**
本题考察对二维坐标逻辑的精准把控。
1. **行/列索引**：设行索引为 \`i\`，列索引为 \`j\`（均从 0 开始）。
2. **条件判定**：
   - **第一列**：\`j == 0\`。
   - **最后一列**：\`j == m - 1\`。
   - **主对角线**：\`i == j\`（从左上角到右下角的连线特征是行号等于列号）。
3. **输出规则**：
   - 如果满足以上任意一个条件，输出 \`+\`。
   - 否则输出 \`-\`。
4. **细节注意**：矩阵是 $m \times m$ 的正方形，每行结束后记得换行。

**核心逻辑提示：**
\`\`\`cpp
for (int i = 0; i < m; i++) {\n    for (int j = 0; j < m; j++) {\n        if (j == 0 || j == m - 1 || i == j) cout << "+";\n        else cout << "-";\n    }\n    cout << endl;\n}\n\`\`\``,
            tags: ["编程题", "字符画", "模拟"],
            template: "#include <iostream>\nusing namespace std;\nint main() {\n    // 在此编写代码\n    return 0;\n}",
            referenceCode: "#include <iostream>\nusing namespace std;\nint main() {\n    int m; cin >> m;\n    for (int i = 0; i < m; i++) {\n        for (int j = 0; j < m; j++) {\n            if (j == 0 || j == m-1 || i == j) cout << '+';\n            else cout << '-';\n        }\n        cout << endl;\n    }\n    return 0;\n}"
        }
    ]
};
