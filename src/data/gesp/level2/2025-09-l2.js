// 2025年9月 GESP C++ 二级真题 (第11次认证)
export const paperData = {
    id: '2025-09-l2',
    title: '2025年9月 GESP C++ 二级真题',
    level: 2,
    year: 2025,
    month: 9,
    session: 11,
    timeLimit: 90 * 60,
    questions: [
        {
            id: 1,
            type: 'single',
            question: `人工智能（AI）现在非常火，小杨就想多了解一下，其中就经常听人提到“大模型”。那么请问这里说的“大模型”最贴切是指（ ）。`,
            options: ['大电脑模型', '大规模智能', '智能的单位', '大语言模型'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (大语言模型)**

**核心解析：**
"大模型"（Large Model）是近年来人工智能领域的核心概念。其中最广为人知、应用最广泛的是"大语言模型"（Large Language Model，简称 LLM）。大语言模型通过海量文本数据训练，拥有强大的自然语言理解和生成能力。题目中提到的"DeepSeek"、"GPT"等都是大语言模型的典型代表。虽然"大规模智能"听起来与"大模型"有些关联，但它并非业界约定俗成的术语，也不准确地反映"大模型"的本质——即通过大规模参数和海量数据训练的语言模型。

**选项逐项分析：**
- **A (大电脑模型)**：❌ 错误。该选项望文生义。"大模型"的"大"指模型参数量大、训练数据规模大，而非物理尺寸大。计算机或服务器等硬件设备有其专门术语，不会被称为"大电脑模型"。
- **B (大规模智能)**：❌ 错误。该选项过于笼统。"大规模智能"可以指很多不同的技术方向，不能准确对应"大模型"这一特定术语。
- **C (智能的单位)**：❌ 错误。该选项明显错误。"大模型"不是任何计量单位，智能的量化也没有统一的"大模型"标准。
- **D (大语言模型)**：✅ 正确。"大模型"在人工智能领域最常指的就是大语言模型（Large Language Model），这是当前 AI 研究和应用的核心方向。

**易错提醒：**
- 不能仅凭字面意思理解科技术语，需了解其在行业中的实际含义。
- "大模型"中的"大"指参数量和训练数据规模，而非物理大小。

**知识延伸：**
GESP 二级考试中常涉及计算机前沿科技常识，如人工智能、网络协议等。建议学员关注科技新闻，了解业界常用术语的准确含义。

**考点：** 计算机前沿科技常识（AI 大模型概念）`,
            tags: ['计算机基础']
        },
        {
            id: 2,
            type: 'single',
            question: `在 TCP 协议中，完成连接建立需要通过（ ）握手。`,
            options: ['一次', '二次', '三次', '四次'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (三次)**

**核心解析：**
TCP（传输控制协议）是一种面向连接的可靠传输协议。在数据传输之前，必须先建立连接。TCP 采用"三次握手"机制来建立连接，具体过程如下：

1. **第一次握手**：客户端向服务器发送 SYN（同步）报文，请求建立连接。此时客户端进入 SYN_SENT 状态。
2. **第二次握手**：服务器收到 SYN 后，回复 SYN+ACK（同步确认）报文，表示同意建立连接。此时服务器进入 SYN_RCVD 状态。
3. **第三次握手**：客户端收到 SYN+ACK 后，发送 ACK（确认）报文，双方建立连接，开始传输数据。

这一机制确保了双方都确认对方的发送和接收能力正常，是可靠传输的基础。

**选项逐项分析：**
- **A (一次)**：❌ 错误。一次握手无法确认双方的接收能力。如果只进行一次握手，服务器不知道客户端是否收到了自己的响应，连接可能建立不可靠。
- **B (二次)**：❌ 错误。二次握手同样存在问题。服务器发送 SYN+ACK 后，无法确认客户端是否收到了该报文，此时服务器处于不确定状态。
- **C (三次)**：✅ 正确。三次握手是 TCP 协议的标准规定，能确保双方都确认对方已准备好建立连接。
- **D (四次)**：❌ 错误。四次握手是多余且不必要的。TCP 的三次握手已经足够完成可靠的连接建立，四次握手不会带来额外好处，反而降低效率。

**易错提醒：**
- TCP 的连接建立是三次握手，但连接释放是四次挥手（因为 TCP 是全双工通信，关闭时需要双向分别确认）。
- 不能将建立连接和关闭连接的过程混淆。

**知识延伸：**
TCP 三次握手与 UDP 的无连接特性形成对比。UDP 不需要握手，直接发送数据，效率高但不可靠，适用于实时性要求高、对少量丢包不敏感的场景（如视频通话）。

**考点：** 计算机网络基础协议（TCP/IP 协议）`,
            tags: ['网络基础']
        },
        {
            id: 3,
            type: 'single',
            question: `下面的 C++ 代码用于输入姓名并输出，正确的说法是（ ）。\n\`\`\`cpp\nstring XingMing; \ncin >> XingMing; \ncout << XingMing;\n\`\`\``,
            options: ['XingMing 是汉语拼音，不能作为变量名称', '可以将 XingMing 改为 Xing Ming', '可以将 XingMing 改为 xingming', '可以将 XingMing 改为 Xing-Ming'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (可以将 XingMing 改为 xingming)**

**核心解析：**
本题考察 C++ 标识符（变量名）的命名规则。C++ 中，标识符必须符合以下规则：
- 只能由字母、数字和下划线组成
- 不能以数字开头
- 不能包含空格或特殊符号（如连字符 \`-\`）
- 区分大小写

原代码中 \`XingMing\` 是一个合法的标识符，汉语拼音本身可以作为标识符。将其改为全小写的 \`xingming\` 仍然合法，且符合许多程序员偏好的命名习惯（变量名通常用小写）。

**选项逐项分析：**
- **A (XingMing 是汉语拼音，不能作为变量名称)**：❌ 错误。该项本身错误。C++ 标识符可以使用任何合法的字母组合，包括汉语拼音。标识符只要求符合语法规则，不限制语言种类。
- **B (可以将 XingMing 改为 Xing Ming)**：❌ 错误。该项本身不合法。变量名中不能包含空格。如果写成 \`Xing Ming\`，编译器会将它视为两个标识符 \`Xing\` 和 \`Ming\`，导致语法错误。
- **C (可以将 XingMing 改为 xingming)**：✅ 正确。\`xingming\` 符合所有标识符命名规则，且大小写与原标识符不同（C++ 区分大小写，\`XingMing\` 和 \`xingming\` 是两个不同的变量）。
- **D (可以将 XingMing 改为 Xing-Ming)**：❌ 错误。该项本身不合法。变量名中不能包含减号 \`-\`。如果写成 \`Xing-Ming\`，编译器会将其解析为标识符 \`Xing\` 减去标识符 \`Ming\` 的表达式，导致语法错误。

**易错提醒：**
- C++ 标识符区分大小写，\`XingMing\` 和 \`xingming\` 是完全不同的变量名。
- 不能想当然地往变量名里加空格或连字符，必须严格遵守语法规则。
- 汉语拼音可以作为变量名，但要注意命名应当具有描述性，便于代码阅读。

**知识延伸：**
C++ 中常见的命名风格有：驼峰命名法（如 \`myVariable\`、\`XingMing\`）、下划线命名法（如 \`my_variable\`、\`xing_ming\`）和帕斯卡命名法（如 \`MyVariable\`）。不同的代码风格指南可能偏好不同的命名方式，但都要求标识符合法且有意义。

**考点：** C++ 标识符命名规则`,
            tags: ['变量与标识符']
        },
        {
            id: 4,
            type: 'single',
            question: `下面 C++ 代码用于获得正整数 N 的第 M 位数（从右往左数，如 M=1 为个位），横线处应填入的是（ ）。\n\`\`\`cpp\nlong long div = 1;\nfor (int i = 0; i < (M-1); i++) div *= 10;\ncout << (______________);\n\`\`\``,
            options: ['N % div / 10', 'N / div / 10', 'N % div % 10', 'N / div % 10'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (N / div % 10)**

**核心解析：**
本题要求提取正整数 N 的第 M 位数（从右往左数，M=1 为个位）。

代码中 \`div\` 的作用是通过循环计算得到 10 的 (M-1) 次方。例如：
- 当 M=1 时，循环不执行，div=1
- 当 M=2 时，循环执行 1 次，div=10
- 当 M=3 时，循环执行 2 次，div=100

**提取第 M 位数的原理：**
1. \`N / div\`：将 N 除以 div，相当于"去掉"右边的 (M-1) 位，得到形如 \`xxx\` 的数（以第 M 位为新的个位）
2. \`% 10\`：再对结果取模 10，得到新的个位数字

**验证示例：** 设 N=12345，M=3（第 3 位是数字 3）
- 循环后 div = 100（因为 M-1=2，执行 2 次 ×10）
- \`N / div\` = 12345 / 100 = 123
- \`123 % 10\` = 3 ✓

**选项逐项分析：**
- **A (N % div / 10)**：❌ 错误。\`N % div\` 得到的只是 N 最右边 (M-1) 位的数字（如 M=3 时得到 45），再除以 10 会丢失信息，无法得到第 M 位。
- **B (N / div / 10)**：❌ 错误。\`N / div / 10\` 相当于连续除以 div 和 10，会多去掉一位，正确结果应该是 \`N / (div * 10)\`。
- **C (N % div % 10)**：❌ 错误。\`N % div\` 已经得到了最右边 (M-1) 位的数字，再 \`% 10\` 只是取这个数的个位，与第 M 位无关。
- **D (N / div % 10)**：✅ 正确。先除以 div 移位，再取模 10 取个位，恰好得到第 M 位数字。

**易错提醒：**
- \`N / div\` 和 \`N % div\` 的区别：前者是"移位取高位"，后者是"取低位"。
- 连续运算时要注意运算顺序，避免多除或少除一位。

**知识延伸：**
数位提取是 C++ 中的高频考点，常与循环结合使用。类似地，提取所有数位的典型方法是使用 \`while (n > 0) { digit = n % 10; n /= 10; }\`，其中 \`% 10\` 取个位，\`/= 10\` 去掉个位。

**考点：** 数位提取算法`,
            tags: ['程序分析']
        },
        {
            id: 5,
            type: 'single',
            question: `下面 C++ 代码执行，其输出是（ ）。\n\`\`\`cpp\nint a, b, c;\na, b = 3, 4;\nc = a == b;\ncout << a << ' ' << b << ' ' << c;\n\`\`\``,
            options: ['3 4 0', '3 3 1', '4 4 1', '以上都不对'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (以上都不对)**

**核心解析：**
本题有两个关键考点：逗号表达式和变量初始化。

**逐行分析代码：**
\`\`\`cpp
int a, b, c;        // 声明三个整型变量，a 和 b 未初始化（值为随机数）
a, b = 3, 4;        // 逗号表达式：依次求值，最终结果为 4
                    // a 不参与赋值，保持未初始化状态
                    // b 被赋值为 3
c = a == b;         // 比较未初始化的 a 和 b=3 的值
                    // 由于 a 是随机数，表达式结果不确定
cout << a << ' ' << b << ' ' << c;  // 输出结果不可预测
\`\`\`

**关键分析：**
1. \`a, b = 3, 4\` 是一个逗号表达式，等价于 \`((a), (b = 3), 4)\`，最终值为 \`4\`。
2. \`a\` 在声明后未初始化，其值为未定义的随机数（任何整数都有可能）。
3. \`b\` 被赋值为 3。
4. \`c = a == b\` 的结果取决于 \`a\` 的随机值：
   - 若 a=3，则 a==b 为 true（1），c=1
   - 若 a≠3，则 a==b 为 false（0），c=0
5. 因此，输出结果可能是 \`随机数 3 0\` 或 \`随机数 3 1\`，无法确定。

**选项逐项分析：**
- **A (3 4 0)**：❌ 错误。a 的值不是 3（未初始化），b 被赋值为 3 而非 4。
- **B (3 3 1)**：❌ 错误。a 的值不是 3（未初始化），且 b 被赋值为 3。
- **C (4 4 1)**：❌ 错误。a 未初始化且逗号表达式的结果 4 并没有赋给任何变量，b 被赋值为 3 而非 4。
- **D (以上都不对)**：✅ 正确。由于 a 未初始化，所有涉及 a 的输出都不可预测，题目中的三个选项都无法保证正确。

**易错提醒：**
- 逗号表达式的优先级低于赋值运算符，\`a, b = 3, 4\` 等价于 \`(a), (b = 3), 4\`，而不是 \`a, b = (3, 4)\`。
- 未初始化的局部变量（普通变量，非全局/静态）的值是未定义的，读取它们会导致未定义行为。
- 不能假设未初始化变量的值为 0。

**知识延伸：**
在 C++ 中，建议始终初始化变量。可以使用 \`int a = 0, b = 0, c = 0;\` 或者 C++11 的统一初始化 \`int a{}, b{}, c{};\`。如果希望逗号表达式将最终值赋给某个变量，需要显式写出，如 \`a = (b = 3, c = 4);\`。

**考点：** 逗号运算符的优先级、变量初始化`,
            tags: ['表达式']
        },
        {
            id: 6,
            type: 'single',
            question: `生成第 N 个编号（规则：XX-Y，XX 从 00 到 11，Y 从 0 到 9）的代码，横线处应填入的是（ ）。\n\`\`\`cpp\npart1 = N / 10 % ____; \npart2 = N % ____;\n\`\`\``,
            options: ['12 10', '10 10', '11 9', '9 9'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (12 10)**

**核心解析：**
题目要求生成形如 "XX-Y" 的编号，其中 XX 的取值范围是 00 到 11（共 12 个值），Y 的取值范围是 0 到 9（共 10 个值）。这本质上是一个双位进制的编号系统——高两位用"十二进制"（0-11），低一位用十进制（0-9）。

设 N 从 0 开始计数（第 0 个编号对应 "00-0"）。按数学映射关系：

- \`part1\`（对应 XX）= N 中有多少完整的"十进制十位数" → 公式为 \`N / 10 % 12\`
- \`part2\`（对应 Y）= N 除以 10 的余数 → 公式为 \`N % 10\`

**逐项验证：**
- 当 N=0 时：part1 = 0/10%12 = 0，part2 = 0%10 = 0 → "00-0" ✓
- 当 N=9 时：part1 = 9/10%12 = 0，part2 = 9%10 = 9 → "00-9" ✓
- 当 N=10 时：part1 = 10/10%12 = 1，part2 = 10%10 = 0 → "01-0" ✓
- 当 N=109 时：part1 = 109/10%12 = 10%12 = 10，part2 = 109%10 = 9 → "10-9" ✓
- 当 N=119 时：part1 = 119/10%12 = 11%12 = 11，part2 = 119%10 = 9 → "11-9" ✓
- 当 N=120 时：part1 = 120/10%12 = 12%12 = 0（循环回开头）✓

**选项逐项分析：**
- **A (12 10)**：✅ 正确。第一空 \`12\` 保证 part1 在 0-11 范围循环，第二空 \`10\` 保证 part2 在 0-9 范围。
- **B (10 10)**：❌ 错误。\`N/10%10\` 的结果只能是 0-9，无法表示编号的高两位 XX（需要 0-11）。
- **C (11 9)**：❌ 错误。\`N%9\` 的结果只能是 0-8，无法表示 Y（需要 0-9）。
- **D (9 9)**：❌ 错误。两个空都无法满足编号规则的基本范围要求。

**易错提醒：**
- 第一空填 10 会导致编号只能到 "09-Y"，无法表示 "10-XX" 和 "11-XX" 的情况。
- 忽略 \`%\` 取模的"循环归零"特性，会漏掉高位循环的设计意图。

**知识延伸：**
本题本质是进制转换与取模运算的组合应用。GESP 二级常见考点还有：将十进制数转换为其他进制（如二进制、八进制）也使用 \`num % base\` 配合 \`num /= base\` 的循环结构。

**考点：** 取模运算的循环特性、编号生成中的进制映射`,
            tags: ['算术运算']
        },
        {
            id: 7,
            type: 'single',
            question: `下面代码执行后其输出是（ ）。\n\`\`\`cpp\nint cnt = 0; \nfor (int i = -10; i < 10; i++) \n    for (int j = 0; j < i; j++) \n        cnt += 1; \ncout << cnt;\n\`\`\``,
            options: ['145', '125', '55', '45'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (45)**

**核心解析：**
外层循环 \`for (i=-10; i<10; i++)\` 的变量 i 从 -10 到 9，共 19 次迭代。内层循环 \`for (j=0; j<i; j++)\` 在每次外层迭代中执行，j 从 0 到 i-1（若 i>0）。注意 i 为负数时 \`j < i\` 条件在 j=0 时就不满足，内层循环不执行。

**逐行追踪 cnt 的变化：**
| i 值 | 内层循环执行次数 | cnt 增量 | 本轮结束后 cnt |
|------|-----------------|---------|---------------|
| -10 到 -1 | 0 | 0 | 0 |
| 1 | j=0 执行 1 次 | +1 | 1 |
| 2 | j=0,1 执行 2 次 | +2 | 3 |
| 3 | 执行 3 次 | +3 | 6 |
| ... | ... | ... | ... |
| 9 | 执行 9 次 | +9 | 45 |

**数学求和验证：**
cnt = 1 + 2 + 3 + ... + 9 = 9 × (1+9) / 2 = 9 × 5 = 45

**选项逐项分析：**
- **A (145)**：❌ 错误。该值约为 1+2+...+16，远大于实际循环次数。
- **B (125)**：❌ 错误。包含了对 i 为负数情况的错误累加。
- **C (55)**：❌ 错误。这是 1+2+...+10 的结果，但外层循环 i<10，最大只累加到 9。
- **D (45)**：✅ 正确。精确等于 1+2+...+9 的等差数列和。

**易错提醒：**
- 误以为 i=-10 到 i=9 共 20 次迭代，错误累加 10 次或把起点算成 0。
- 混淆 \`<\` 和 \`<=\`：若条件写成 \`i <= 9\` 会多执行一次 i=10 的情况。

**知识延伸：**
GESP 二级中常见"双重循环累加"题型，本质是求"求和公式"的运用：连续整数 1 到 n 的和 = n(n+1)/2。

**考点：** 循环边界条件、等差数列求和`,
            tags: ['循环']
        },
        {
            id: 8,
            type: 'single',
            question: `下面代码执行后其输出是（ ）。\n\`\`\`cpp\nint i, j;\nfor (i = 1; i < 12; i++){\n    if (i % 2 == 0) continue;\n    for (j = 0; j < i; j++)\n        if (i * j % 2) break;\n}\nif(i >= 12) cout << (i * j);\n\`\`\``,
            options: ['110', '12', '不确定', '无输出'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (12)**

**核心解析：**
外层循环 \`for (i=1; i<12; i++)\` 控制 i 从 1 到 11。关键逻辑：
- 若 i 为偶数，执行 \`continue\`，跳过内层循环。
- 若 i 为奇数，进入内层循环 \`for (j=0; j<i; j++)\`。

**逐项追踪（重点分析奇数 i 的情况）：**
- **i=1（奇数）**：j 从 0 开始，j=0 时 1×0=0（非奇数，不 break），j=1 时 1×1=1（是奇数），触发 \`break\`，此时 j=1。**循环结束后 j=1**。
- **i=2（偶数）**：continue 跳过。
- **i=3（奇数）**：j 从 1 开始（沿用上次的值），j=1 时 3×1=3（是奇数），立即触发 \`break\`，j 保持为 1。
- **i=4（偶数）**：continue 跳过。
- **i=5（奇数）**：j 从 1 开始，j=1 时 5×1=5（是奇数），触发 \`break\`，j=1。
- ... **所有奇数 i 都是如此**（因为奇数×1=奇数，100%触发 break），j 始终保持为 1。

**循环结束判定：**
当 i=12 时，外层循环条件 \`i < 12\` 不满足，循环终止。**最终状态：i=12, j=1**。

**输出判定：**
\`if(i >= 12)\` → 12 >= 12 为真，执行 \`cout << (i * j)\` → 输出 12×1 = **12**。

**选项逐项分析：**
- **A (110)**：❌ 错误。可能是误以为 j 会累加到 10 或 11，实际上奇数 i 时 j 总是在第一次内层循环就 break（j=1）。
- **B (12)**：✅ 正确。精确推导得出 i=12, j=1，输出 12。
- **C (不确定)**：❌ 错误。虽然 j 在奇数 i 时被重新赋值，但本题中所有奇数 i 都导致 j=1，结果是确定的。
- **D (无输出)**：❌ 错误。\`i >= 12\` 条件成立（i=12），会执行输出。

**易错提醒：**
- 内层循环的 j **沿用上次值**，不是每次都从 0 开始。这是本题的核心易错点。
- 忽略 \`continue\` 导致 i=2,4,6,8,10 跳过了内层循环的检查。

**知识延伸：**
本题考察嵌套循环中变量的"记忆性"——内层循环变量不会自动重置，会保留上一次循环结束时的值。GESP 二级还常考 \`break\` 和 \`continue\` 对循环流程控制的影响。

**考点：** 嵌套循环变量状态追踪、break 与 continue 的区别、变量值的沿用`,
            tags: ['程序分析']
        },
        {
            id: 9,
            type: 'single',
            question: `阅读代码，说法正确的是（ ）。\n\`\`\`cpp\nwhile (b != 0) {\n    remainder = a % b; \n    a = b; \n    b = remainder; \n}\n\`\`\``,
            options: ['b 不能为 0', 'a 必须小于 b', 'a 和 b 必须为正整数', '如果 a 输入为 0，则输出值的绝对值都是 abs(b)'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (如果 a 输入为 0，则输出值的绝对值都是 abs(b))**

**核心解析：**
该代码是经典的"辗转相除法"（欧几里得算法），用于求两个正整数的最大公约数。算法逻辑：\`a % b\` 得到余数 remainder，然后用 b 替代 a，remainder 替代 b，循环直到 b=0。

**选项逐项分析：**

- **A (b 不能为 0)**：❌ 错误（该项本身错误）。代码第一行就是 \`while (b != 0)\`，这意味着只要进入循环体，b 必然非零。虽然 b **不能为 0** 这一点从逻辑上是对的（为 0 则无法继续），但说法不够准确——更准确的表述是"代码已用条件判断防止 b 为 0 的情况"。本题要求选出"正确说法"，A 选项的措辞本身存在歧义。

- **B (a 必须小于 b)**：❌ 错误（该项本身错误）。若 a < b（如 a=3, b=5），第一轮取模 \`remainder = 3 % 5 = 3\`，然后 a=5, b=3，两数自动互换位置。辗转相除法对 a、b 的大小顺序没有要求。

- **C (a 和 b 必须为正整数)**：❌ 错误（该项本身错误）。C++ 的取模运算支持负整数。虽然辗转相除法通常用于正整数，但代码本身**语法上支持负数**（结果的正负取决于实现）。

- **D (如果 a 输入为 0，则输出值的绝对值都是 abs(b))**：✅ 正确。设 b ≠ 0：
- 第一轮：\`remainder = 0 % b = 0\`，\`a = b\`，\`b = 0\`
- 第二轮：\`while (b != 0)\` 条件不满足，循环结束
- 最终 \`a\` 等于原来的 \`b\`，因此输出值的绝对值为 \`abs(b)\`

**易错提醒：**
- 混淆"算法设计上的要求"与"C++ 语法的限制"。辗转相除法通常用于正整数，但代码不强制检查。
- 误以为 a=0 会导致除零错误，实际上 \`0 % b\` 在 b≠0 时结果为 0，不会出错。

**知识延伸：**
辗转相除法是 GESP 三级的高频考点，可扩展为求最小公倍数（LCM）：lcm(a,b) = |a×b| / gcd(a,b)。

**考点：** 辗转相除法逻辑分析、边界条件分析（a=0 的情况）`,
            tags: ['算法思维']
        },
        {
            id: 10,
            type: 'single',
            question: `下面 C++ 代码执行后输出是（ ）。\n\`\`\`cpp\nint num = 0; \nwhile (num <= 5) {\n    num += 1;\n    if (num == 3) continue;\n    printf("%d#", num);\n}\n\`\`\``,
            options: ['1#2#4#5#6#', '1#2#4#5#6', '1#2#3#4#5#6#', '1#2#3#4#5#6'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (1#2#4#5#6#)**

**核心解析：**
循环 \`while (num <= 5)\` 配合 \`num += 1\` 使 num 从 1 递增到 6。关键点在于 \`continue\` 语句：当 \`num == 3\` 时跳过本次循环的剩余语句（即不执行 \`printf\`）。

**逐次迭代追踪：**
| 迭代 | num 初始 | num+=1 后 | if(num==3) | 是否 continue | printf 输出 |
|------|---------|----------|------------|--------------|-------------|
| 第1次 | 0 | 1 | 1≠3，不触发 | 否 | 输出 "1#" |
| 第2次 | 1 | 2 | 2≠3，不触发 | 否 | 输出 "2#" |
| 第3次 | 2 | 3 | 3==3，触发 | **是**，跳过 printf | 无输出 |
| 第4次 | 3 | 4 | 4≠3，不触发 | 否 | 输出 "4#" |
| 第5次 | 4 | 5 | 5≠3，不触发 | 否 | 输出 "5#" |
| 第6次 | 5 | 6 | 6≠3，不触发 | 否 | 输出 "6#" |
| 第7次 | 6 | — | 6>5，循环终止 | — | — |

**最终输出序列：** "1#2#4#5#6#"

注意：最后一次迭代 num=6 时输出 "6#"，但 6 > 5 后循环终止，**不额外输出**，所以没有尾部空格或换行。

**选项逐项分析：**
- **A (1#2#4#5#6#)**：✅ 正确。逐行推导结果，num=3 时被 continue 跳过。
- **B (1#2#4#5#6)**：❌ 错误。该项漏掉了最后的 "6#"，但实际上 num=6 时 6≤5 为真（因为先执行 \`num+=1\` 使 num 变为 6），此时 \`printf\` 执行后才判断 \`num <= 5\` 为假而退出。
- **C (1#2#3#4#5#6#)**：❌ 错误。该项包含了 "3#"，但 num=3 时被 continue 跳过，不会输出。
- **D (1#2#3#4#5#6)**：❌ 错误。既包含了不应有的 "3#"，又漏掉了最后的 "6#"。

**易错提醒：**
- **误以为 continue 会跳过本次所有操作**：实际上 \`num += 1\` 在 continue **之前**执行，所以 num 仍然会从 3 变成 4。
- **误以为 num=6 时不输出**：因为 \`num += 1\` 先执行（num 从 5 变成 6），此时 \`num <= 5\` 判断为假之前，\`printf\` 已经执行过了。

**知识延伸：**
\`continue\` 只跳过本次循环剩余语句，不影响循环变量自增。GESP 二级还常考 \`break\` 与 \`continue\` 的区别：break 直接终止整个循环，continue 只跳过当前轮次剩余代码。

**考点：** continue 语句的执行时机、循环控制流程、printf 输出累积`,
            tags: ['循环']
        },
        {
            id: 11,
            type: 'single',
            question: "下面 C++ 代码用于记录多个输入数中的最大数和最小数（输入 -999 则输入结束），相关说法“错误”的是（ ）。\n\n```cpp\nint now_num, min_num, max_num;\ncin >> now_num;\nmin_num = max_num = now_num;\nwhile (now_num != -999) {\n    if (max_num < now_num)\n        max_num = now_num;\n    if (min_num > now_num)\n        min_num = now_num;\n    cin >> now_num;\n}\ncout << min_num << ' ' << max_num;\n```",
            options: ['输入第一个数为 -999 时，输出 -999 -999', '若不输入 -999，程序能求最大最小值', '输入成绩时能求最高最低分', '可以将输入语句移到循环内第一行而不改变结果'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (可以将输入语句移到循环内第一行而不改变结果)**

**核心解析：**
本题考查“哨兵值”控制循环。典型写法是先读入第一个数，用它初始化最大值和最小值；之后只要读到的数不是哨兵值 \`-999\`，就参与最大值、最小值更新，再继续读入下一个数。

这类代码的关键不是“有没有循环”，而是“当前读入的数据是否已经参与比较”。如果把输入语句移动到循环体内第一行，程序会先读新数据再比较，原本用于初始化的第一个数可能被跳过；如果新读入的正好是 \`-999\`，还可能把哨兵值错误地拿去更新最大/最小值。因此“移动输入语句不改变结果”这个说法是错误的。

**选项逐项分析：**
- **A (输入第一个数为 -999 时，输出 -999 -999)**：❌ 错误。该说法本身正确，因此不是本题要选的错误项。若第一个输入就是哨兵值，最大值和最小值都以 \`-999\` 初始化，循环不再处理有效数据，输出为 \`-999 -999\`。
- **B (若不输入 -999，程序能求最大最小值)**：❌ 错误。该说法本身基本正确，因此不是本题要选的错误项。只要持续输入有效数据，程序会不断更新最大值和最小值；不过实际运行中仍需要某种方式终止输入。
- **C (输入成绩时能求最高最低分)**：❌ 错误。该说法本身正确，因此不是本题要选的错误项。正常成绩不会等于 \`-999\`，可作为普通数值参与最高分、最低分比较。
- **D (可以将输入语句移到循环内第一行而不改变结果)**：✅ 正确。这是本题要选的错误说法。输入语句的位置决定哪一个数被初始化、哪一个数被比较，移动后会改变哨兵值判断和数据更新顺序。

**易错提醒：**
- 反向题问的是“错误的是”，正确选项对应的是“错误说法”，不是事实正确的说法。
- 哨兵值 \`-999\` 只负责结束输入，不应作为有效数据参与最大值、最小值更新。

**知识延伸：**
处理哨兵循环时常见模式是“先读第一个数据初始化，再在循环末尾读下一个数据”。如果把读入位置改到循环开头，必须重新设计初始化和终止判断，否则很容易漏处理第一项或误处理哨兵值。

**考点：** 哨兵循环、最大最小值维护、输入语句位置对程序逻辑的影响`,
            tags: ['程序分析']
        },
        {
            id: 12,
            type: 'single',
            question: "下面 C++ 代码执行后输出与 5 有关数的数量。“与 5 有关的数”定义为含有 5 或者能被 5 整除的数。相关说法正确的是（ ）。\n\n```cpp\nint cnt = 0, i, j;\nfor (i = 1; i < 1000; i++) {\n    if (i % 5 == 0) {\n        cnt += 1;\n        continue;\n    }\n    j = i;\n    while (j > 0)\n        if (j % 10 == 5) {\n            cnt += 1;\n            break;\n        } else {\n            j /= 10;\n        }\n}\ncout << cnt;\n```",
            options: ['删除 continue 不影响结果', '删除 j = i 不影响结果', '将 break 改为 j = 0 不影响结果', '将 j > 0 改为 j >= 0 不影响结果'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (将 break 改为 j = 0 不影响结果)**

**核心解析：**
“与 5 有关的数”通常包括两类：能被 5 整除的数，以及十进制数位中含有数字 5 的数。程序一般先判断 \`i % 5 == 0\`，若成立则计数并用 \`continue\` 进入下一轮，避免同一个数被重复统计；否则再用临时变量 \`j = i\` 分解各位数字，检查是否含有 5。

当分解数位时，发现某一位是 5 后，使用 \`break\` 跳出 \`while (j > 0)\`。如果把 \`break\` 改成 \`j = 0\`，下一次判断 \`j > 0\` 也会失败，同样能结束内层 while 循环，因此结果不变。

**选项逐项分析：**
- **A (删除 continue 不影响结果)**：❌ 错误。删除 \`continue\` 后，像 5、15、25、55 这类既是 5 的倍数又含有数字 5 的数，可能被重复计数。
- **B (删除 j = i 不影响结果)**：❌ 错误。需要用 \`j\` 作为临时变量分解数位，避免直接修改外层循环变量 \`i\`；删除后会破坏数位检查逻辑。
- **C (将 break 改为 j = 0 不影响结果)**：✅ 正确。发现含有数字 5 后，\`break\` 是立刻退出循环；把 \`j\` 设为 0 也会让 \`while (j > 0)\` 的条件失效，最终效果相同。
- **D (将 j > 0 改为 j >= 0 不影响结果)**：❌ 错误。当 \`j\` 变为 0 后，\`j >= 0\` 仍为真，且 \`j /= 10\` 后仍是 0，可能导致死循环。

**易错提醒：**
- \`continue\` 在本题中不是装饰，它用于防止同一个数被重复统计。
- 分解数位时要用临时变量，直接改外层循环变量会影响循环本身。

**知识延伸：**
判断一个整数是否包含某个数字，常用循环：每次取 \`x % 10\` 得到个位，再令 \`x /= 10\` 去掉个位。循环条件一般写 \`x > 0\`，不能随意改成 \`x >= 0\`。

**考点：** continue、break、数位分离、避免重复计数`,
            tags: ['程序分析']
        },
        {
            id: 13,
            type: 'single',
            question: "下面 C++ 代码实现输出如下数字三角形，横线处应填入的是（ ）。\n\n```text\n请输入层数：10\n1\n23\n456\n7891\n23456\n789123\n4567891\n23456789\n123456789\n1234567891\n```\n\n```cpp\ncout << \"请输入层数：\";\ncin >> N;\nK = 1;\nfor (i = 1; i < N + 1; i++) {\n    for (int _ = 1; _ < ___________; _++)\n        cout << \" \";\n    for (int _ = 1; _ < ________; _++) {\n        cout << K;\n        K += 1;\n        if (K == 10)\n            K = 1;\n    }\n    cout << '\\n';\n}\n```",
            options: ['N-i+1 和 i+1', 'N-i 和 i', 'N 和 i', 'N-i 和 i+1'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (N-i+1 和 i+1)**

**核心解析：**
图形打印题要先确定“第 i 行”的规律。题目中的行号 \`i\` 从 1 开始：随着行号增加，左侧空格逐行减少，数字个数逐行增加。

若循环条件采用“小于号”形式，例如 \`j < 某个上界\`，要输出 \`k\` 次，常见上界应写成 \`k + 1\`。因此第 i 行若需要输出 \`N - i\` 个空格，对应循环上界可写成 \`N - i + 1\`；第 i 行若需要输出 \`i\` 个数字，对应循环上界可写成 \`i + 1\`。

**选项逐项分析：**
- **A (N-i+1 和 i+1)**：✅ 正确。配合从 1 开始、使用 \`<\` 判断的循环，可以分别控制空格数和数字个数。
- **B (N-i 和 i)**：❌ 错误。若循环变量从 1 开始且条件为小于上界，会分别少输出一次，出现空格和数字都偏少的情况。
- **C (N 和 i)**：❌ 错误。空格数固定为 N 相关，不会随行号递减，无法形成三角形缩进变化。
- **D (N-i 和 i+1)**：❌ 错误。数字个数部分可能正确，但空格上界少 1，会导致整体位置偏移。

**易错提醒：**
- 图形题不要只看公式形状，要代入第 1 行、第 2 行验证输出次数。
- 使用 \`<\` 时，上界通常要比实际次数多 1；使用 \`<=\` 时则不用这样调整。

**知识延伸：**
嵌套循环图形题常分两层：外层控制行，内层控制列。每个内层循环都要回答“这一行到底输出几个字符”，再把次数翻译成循环边界。

**考点：** 嵌套循环、图形打印、循环边界与输出次数`,
            tags: ['循环']
        },
        {
            id: 14,
            type: 'single',
            question: `下面 C++ 代码执行，其输出是（ ）。\n\`\`\`cpp\nint a = 9, b = 27; \na = 'a' + 'b'; \nb = 'a' - 'b'; \na = a - b; \ncout << a << ' ' << b;\n\`\`\``,
            options: ['196 -1', '27 9', '98 97', '不确定'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (196 -1)**

**核心解析：**
这段代码中 \`a\`、\`b\` 是 int 变量，虽然右侧出现字符常量 \`'a'\`、\`'b'\`，但字符参与算术运算时会按 ASCII 码转换为整数。已知 \`'a'\` 的 ASCII 码是 97，\`'b'\` 的 ASCII 码是 98。

逐行执行：
1. 初始：\`a = 9, b = 27\`。
2. \`a = 'a' + 'b';\`，即 \`a = 97 + 98 = 195\`。
3. \`b = 'a' - 'b';\`，即 \`b = 97 - 98 = -1\`。
4. \`a = a - b;\`，此时 \`a = 195 - (-1) = 196\`。
5. \`cout << a << ' ' << b;\` 中 \`' '\` 是空格字符，所以输出为 \`196 -1\`。

**选项逐项分析：**
- **A (196 -1)**：✅ 正确。逐行追踪变量后，最终 a 为 196，b 为 -1，中间以空格分隔输出。
- **B (27 9)**：❌ 错误。该选项像是保留或交换了初始值，没有执行后续字符运算赋值。
- **C (98 97)**：❌ 错误。这是把 \`'b'\` 和 \`'a'\` 的 ASCII 码当成最终变量值，忽略了赋值和减法更新。
- **D (不确定)**：❌ 错误。ASCII 编码下字符常量参与整数运算的结果确定，代码没有未定义行为。

**易错提醒：**
- 单引号字符 \`'a'\` 参与算术时是 ASCII 数值 97，不是字符串。
- \`195 - (-1)\` 要注意负负得正，结果是 196。

**知识延伸：**
C++ 中 \`char\` 本质上是整数类型的一种，既可以按字符输出，也可以参与整数运算。表达式结果赋给 int 时，会保存其整数值。

**考点：** ASCII 码、字符常量参与运算、变量逐行追踪`,
            tags: ['字符与ASCII']
        },
        {
            id: 15,
            type: 'single',
            question: "有个无限长的链，由 3 种外形相同但材质不同的环链组成。3 种环的重量分别是 3、4、6 克，相同材质的多个环每 12 克一组，分别记为 G3、G4、G6。链依次 G3、G4、G6、G3、G4、G6、……。同时对链上所有环从头依次编号 1、2、3、4……。输入正整数代表环编号，求该编号前所有环（不含该环本身）的重量。下面 C++ 代码的实现，正确说法是（ ）。\n\n```cpp\nint N, G, R;\nint wc = 0;\ncin >> N; // 输入正整数\nG = (N - 1) / 9; // L1\nR = (N - 1) % 9; // L2：保存余数\nwc += 36 * G;\nif ((1 <= R) && (R <= 4))\n    wc += 3 * R; // L3\nelse if ((5 <= R) && (R <= 7))\n    wc += 4 * R; // L4\nelse if (R == 8)\n    wc += 6 * (R - 1); // L5\ncout << wc << endl;\n```",
            options: ['须修改 L1 和 L2', '须修改 L3 和 L4', '须修改 L3 和 L5', '其他说法都不对'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (其他说法都不对)**

**核心解析：**
本题考查环链重量计算代码中的分组和余数逻辑。相关代码通常先计算 \`G = (N - 1) / 9\` 和 \`R = (N - 1) % 9\`：\`G\` 表示完整经过了多少组 9 个位置，\`R\` 表示当前在这一组中的余数位置。后续分支根据 \`R\` 的不同范围决定本环链应增加哪一部分重量。

三个干扰选项都声称“必须同时修改某两行才能实现功能”，但从分组公式和余数分支看，并没有证据表明必须成对修改 L1/L2、L3/L4 或 L3/L5。若原代码的分组和余数判断已经能覆盖各类位置，那么这些“必须修改”的说法都过于绝对，因此应选“其他说法都不对”。

**选项逐项分析：**
- **A (须修改 L1 和 L2)**：❌ 错误。没有依据说明这两处必须同时修改；若分组和余数判断本身正确，强行修改反而可能破坏逻辑。
- **B (须修改 L3 和 L4)**：❌ 错误。该说法同样把局部行号绑定成必要条件，但题目代码的核心是 \`G\`、\`R\` 的分解及分支覆盖。
- **C (须修改 L3 和 L5)**：❌ 错误。只指出两处行号不足以证明能修复或必须修复，属于没有充分依据的判断。
- **D (其他说法都不对)**：✅ 正确。前三个选项都缺少必要性，因此选择 D。

**易错提醒：**
- 看到 L1、L2 这类行号题时，不要只按“看起来像要改两处”判断，要回到变量含义和分支覆盖。
- \`(N - 1) / 9\` 与 \`(N - 1) % 9\` 是配套使用的：一个确定组数，一个确定组内位置。

**知识延伸：**
周期类问题常把编号先减 1 再整除/取模，这是为了把第 1 个元素映射到余数 0，方便按 0 到周期长度减 1 的范围分类。

**考点：** 整除与取模、周期分组、代码修改必要性判断`,
            tags: ['程序分析']
        },
        {
            id: 16,
            type: 'judge',
            question: `在 IDE 中调试时，要注意不能修改源程序，否则要重新打开才能调试。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
现代集成开发环境（IDE）在调试过程中允许用户随时修改源代码。修改后通常只需重新编译或重新启动调试会话即可继续调试，完全不需要退出并"重新打开"整个 IDE。题中表述将调试器的限制过度夸大，与实际开发体验不符。

**易错提醒：**
- 部分学员可能将"需要重新编译"误解为"需要重新打开 IDE"
- 某些深层修改（如修改断点位置、调试时无法修改的函数内部逻辑）可能需要停止调试会话重试，但这仍属于同一 IDE 窗口内的操作

**知识延伸：**
主流 IDE（如 Dev-C++、Code::Blocks、Visual Studio 等）均支持"编辑并继续"（Edit and Continue）功能或热重载机制，允许在调试状态下修改代码继续运行。

**考点：** 软件开发工具的基本使用常识`,
            tags: ['开发环境']
        },
        {
            id: 17,
            type: 'judge',
            question: `N 为正整数且大于 100，N / 100 将舍弃个位和十位。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
在 C++ 中，整数除法运算符 \`/\` 会对结果执行向下取整（即向零取整）。当正整数 N 除以 100 时，其商恰好等于 N 去掉最后两位数字（十位和个位）。例如：N = 12345，N / 100 = 123，余数为 45（被舍弃）。因此题目所述"舍弃个位和十位"在数学含义上成立。

**易错提醒：**
- 若 N 为负整数，向下取整可能导致结果与直觉不同（如 -123 / 100 = -2），但题目明确 N 为正整数
- 需注意"舍弃"一词在数学上的精确含义——即丢弃最后两位，不进行四舍五入

**知识延伸：**
整数除以 10^n（n 为正整数）相当于在十进制表示下去掉最后 n 位。这一特性在数字分离、进制转换等算法中经常用到。

**考点：** 算术整除特性`,
            tags: ['算术运算']
        },
        {
            id: 18,
            type: 'judge',
            question: `C++ 表达式 \`cout << (a < 10 and 20);\` 在 a = 5 时输出为 1。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
1. \`and\` 是 C++ 的关键字等效符，等价于逻辑运算符 \`&&\`，这是 GESP 二级要求掌握的基础语法知识。
2. 当 a = 5 时，表达式 \`a < 10\` 成立，结果为 \`true\`（布尔值 1）。
3. 在 C++ 逻辑运算中，非零整数（如 20）自动转换为布尔值 \`true\`。
4. \`true && true\` 的结果为 \`true\`，\`cout\` 在输出布尔值时会将其转换为整数 1。

**易错提醒：**
- 部分学员可能将 \`and 20\` 误解为"同时判断 a < 10 且 a < 20"，但原表达式中 \`20\` 是独立操作数
- C++ 中 \`-5\`、\`0.0\` 等值为假，非零数值（含负数）均为真

**知识延伸：**
C++ 提供了三个替代表示符：\`and\`（替代 \`&&\`）、\`or\`（替代 \`||\`）、\`not\`（替代 \`!\`）。在 GESP 考纲中，这些关键字属于二级知识点。

**考点：** 逻辑运算符关键字（and/or/not）、数值的布尔转换规则`,
            tags: ['布尔运算']
        },
        {
            id: 19,
            type: 'judge',
            question: `执行 \`int x=5, y=10, z=15; bool result = x < y < z;\` 后，result 的值为 1。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
表达式 \`x < y < z\` 遵循从左到右的结合性，分两步求值：
1. 计算 \`x < y\`：5 < 10 结果为 \`true\`，在数值上等于 1。
2. 计算 \`1 < z\`：将上一步的布尔结果 1 与 z 比较，1 < 15 结果为 \`true\`。
3. 最终结果为 \`true\`，赋值给 \`bool result\`，输出或显示时为 1。

**易错提醒：**
- 本题的核心易错点是：将 \`x < y < z\` 理解为"x、y、z 三者同时满足不等式关系"，即 \`x < y && y < z\`
- 实际上 C++ 没有"链式比较"语法，必须显式写成 \`x < y && y < z\` 才能实现该逻辑

**知识延伸：**
若要判断 y 是否严格介于 x 和 z 之间，正确的 C++ 表达式应为 \`x < y && y < z\`。Python 等语言支持链式比较 \`x < y < z\`，但 C++ 不支持这一语法。

**考点：** 关系运算符的左结合性陷阱`,
            tags: ['比较运算']
        },
        {
            id: 20,
            type: 'judge',
            question: `如果将浮点数 99.99 赋值给 int 变量，随后判断是否及格（>=60），会输出“及格”。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
浮点数向整型转换时，C++ 执行"截断"而非"四舍五入"：
1. 将 99.99 赋值给 \`int\` 变量：整数部分 99 被保留，小数部分 0.99 被直接丢弃。
2. 执行及格判断：\`99 >= 60\`，条件成立。
3. 程序输出"及格"。

**易错提醒：**
- 截断方向总是向零方向（对于正数即取整数部分），而非向下取整。99.99 截断为 99，而非 -100
- 若题目改为"将浮点数 -99.99 赋值给 int"，结果为 -99，仍 >= 60 为假

**知识延伸：**
显式类型转换（强制类型转换）也遵循相同规则：\`int(99.99)\` 结果为 99。显式转换的语法形式为 \`static_cast<int>(99.99)\`，含义与隐式转换相同。

**考点：** 类型转换截断特性`,
            tags: ['数据类型']
        },
        {
            id: 21,
            type: 'judge',
            question: "输入 123 时，通过 `while(a) { cout << (char)('A' + a % 10); a /= 10; }` 输出的结果是 DCB。",
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: "**答案：正确**\n\n**判定依据：**\n输入 `123` 后，循环每次取当前数字的个位：\n\n1. `a = 123`，`a % 10 = 3`，`(char)('A' + 3)` 输出 `D`，随后 `a` 变为 12。\n2. `a = 12`，`a % 10 = 2`，输出 `C`，随后 `a` 变为 1。\n3. `a = 1`，`a % 10 = 1`，输出 `B`，随后 `a` 变为 0。\n\n循环结束，整体输出为 `DCB`，题干说法正确。\n\n**考点：** 取个位、整数除法去个位、字符映射",
            tags: ['循环', '字符与ASCII']
        },
        {
            id: 22,
            type: 'judge',
            question: `代码 \`for (int i=0; i<3; i++) { if (i==2) continue; cout << "#"; } cout << i;\` 输出结果包含 \`##3\`。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
逐步追踪代码执行：

\`\`\`
for (int i = 0; i < 3; i++) {
    if (i == 2) continue;  // i=2时跳过本次循环体
    cout << "#";
}
cout << i;  // 循环结束后输出i的值
\`\`\`

- **i = 0**：i==2 为 false，不执行 continue，执行 \`cout << "#"\`，输出 \`#\`。
- **i = 1**：i==2 为 false，不执行 continue，执行 \`cout << "#"\`，输出 \`#\`。
- **i = 2**：i==2 为 true，执行 continue，**跳过** \`cout << "#"\` 语句，本次不输出任何内容。
- 循环结束条件 \`i < 3\`，退出时 i 自增为 3。
- 循环外 \`cout << i;\` 输出变量 i 的当前值 \`3\`。

最终输出结果为 \`##3\`，**完全包含题目所述的 "##3"**，因此该判断正确。

**易错提醒：**
- \`continue\` 跳过的是**本次循环剩余语句**（即 \`cout << "#"\`），而非整个循环。for 循环仍会执行 \`i++\` 并判断条件。
- for 循环结束后，变量 i 的作用域仍在（因为 i 在 for 语句内声明），此时 i 的值为 3。

**知识延伸：**
continue 语句的作用是跳回循环条件判断，常用于"遇到特定值时跳过处理"的场景。在实际编程中，continue 不会使循环提前终止，循环是否终止取决于条件表达式。

**考点：** for 循环执行流程、continue 跳转行为、循环结束后变量的终值`,
            tags: ['循环']
        },
        {
            id: 23,
            type: 'judge',
            question: `通过 \`a = a + b; b = a - b; a = a - b;\` 可以不利用中间变量实现变量 a 和 b 的交换。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
使用加减法在不借助第三变量的情况下完成交换，逻辑如下：

1. 初始：假设 a = A, b = B（A、B 为原始值）。
2. \`a = a + b\` 执行后：a = A + B，b 仍为 B。
3. \`b = a - b\` 执行后：b = (A + B) - B = A（获得原 a 的值），a 仍为 A + B。
4. \`a = a - b\` 执行后：a = (A + B) - A = B（获得原 b 的值），b 仍为 A。

最终结果：a 变为原 b 的值 B，b 变为原 a 的值 A，交换成功。

**易错提醒：**
- 该算法要求 a 和 b 的数据类型支持加法和减法运算，且运算结果不能溢出。
- 当 a 和 b 相等时，该算法仍能正确工作（a = b 时，a = a + b = 2a，b = 2a - a = a，a = 2a - a = b）。
- 交换两个相同值的变量时，结果看起来"没交换"，但算法本身是正确的。

**知识延伸：**
除加减法外，异或运算 \`a ^= b; b ^= a; a ^= b;\` 同样可在不借助第三变量的情况下完成交换，三种方法都是 GESP 二级的高频考点。

**考点：** 变量交换算法、不使用临时变量`,
            tags: ['算法思维']
        },
        {
            id: 24,
            type: 'judge',
            question: `在嵌套循环输出矩阵时，将换行语句 \`cout << endl;\` 放置在内层循环结束之后、外层循环结束之前是正确的。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
嵌套循环输出矩阵时，正确的结构应如下：

\`\`\`cpp
for (int i = 0; i < m; i++) {      // 外层循环控制行
    for (int j = 0; j < n; j++) {  // 内层循环控制列
        cout << element;
    }
    cout << endl;                  // 内层循环结束后、外层循环结束前换行
}
\`\`\`

内层循环负责输出该行的所有列元素；内层循环结束后立即换行，准备输出下一行；外层循环继续处理下一行。这种结构能确保每行末尾恰好换行一次，形成规整的矩阵。

**易错提醒：**
- 如果 \`cout << endl;\` 放在内层循环内部，则每输出一个元素就换行，矩阵会变成纵向排列。
- 如果 \`cout << endl;\` 放在外层循环结束后，则最后一行之后会多出一个不必要的换行。
- 注意区分"图形输出"和"矩阵遍历"场景，前者换行位置尤为重要。

**知识延伸：**
类似的换行时机控制也出现在字符串遍历、表格打印、九九乘法表等 GESP 常考图形输出题中，核心原则是：每处理完一组相关元素后换行。

**考点：** 嵌套循环结构、循环体语句位置、图形输出格式控制`,
            tags: ['图形绘制']
        },
        {
            id: 25,
            type: 'judge',
            question: `C++ 语句 \`cout << ('5' + 4);\` 执行后输出为 9。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
字符常量 \`'5'\` 在 C++ 中并非数值 5，而是以 ASCII 码形式存储在内存中的整数值。字符 \`'5'\` 对应的 ASCII 码是十进制 53（十六进制 0x35）。

执行 \`cout << ('5' + 4)\` 时：
1. \`'5'\` 自动提升为整型 ASCII 码值 53。
2. 计算 \`53 + 4\`，得到整数 57。
3. \`cout\` 按整数格式输出 57。

因此输出结果为 57，而非数字 9。如果希望得到 9，应写作 \`'5' - '0' + 4\`（即 53 - 48 + 4 = 9）。

**易错提醒：**
- \`'5'\` 是字符，不是字符串（字符串应为 \`"5"\`）。
- 字符参与算术运算时使用的是其 ASCII 码值，而非字符本身表示的数值。
- \`cout\` 根据上下文推断输出格式：单独输出字符时打印字符，字符参与算术表达式后输出整数。

**知识延伸：**
常见字符的 ASCII 码值：'0'~'9' 对应 48~57，'A'~'Z' 对应 65~90，'a'~'z' 对应 97~122。掌握这些基础值有助于快速判断字符运算结果。

**考点：** 字符型数据的存储本质、ASCII 码、字符与整数的隐式转换`,
            tags: ['数据类型']
        }
    ],
    programmingQuestions: [
        {
            id: 26,
            type: 'programming',
            title: "优美的数字",
            samples: [
                {
                    input: `6`,
                    output: `6`
                },
                {
                    input: `2025`,
                    output: `28`
                }
            ],
            question: `
# [GESP202509 二级] 优美的数字

## 题目描述

如果一个正整数在十进制下的所有数位都相同，小 A 就会觉得这个正整数很优美。例如，正整数 $6$ 的数位都是 $6$，所以 $6$ 是优美的。正整数 $99$ 的数位都是 $9$，所以 $99$ 是优美的。正整数 $123$ 的数位不都相同，所以 $123$ 并不优美。

小 A 想知道不超过 $n$ 的正整数中有多少优美的数字。你能帮他数一数吗？

## 输入格式

一行，一个正整数 $n$。

## 输出格式

一行，一个正整数，表示不超过 $n$ 的优美正整数的数量。
`,
            score: 25,
            explanation: `**解析：**
本题考察数字构造与范围判定。
1. **数字规律**：优美数字形如 $d, dd, ddd, ...$（其中 $d \in [1, 9]$）。
2. **生成策略**：
   - 枚举每一个数字 $d$ (1-9)。
   - 不断构造其重复形式：$x = x \times 10 + d$。
   - 只要 $x \le n$，计数器就加 1。
3. **复杂度**：这种构造方法非常高效，因为每个数字的重复形式在 $10^{18}$ 范围内也只有 18 个左右。

**核心逻辑提示：**
\`\`\`cpp
for (int d = 1; d <= 9; d++) {
    long long x = d;
    while (x <= n) {
        ans++;
        if (x > n / 10) break; // 防止溢出
        x = x * 10 + d;
    }
}
\`\`\``,
            tags: ['编程题', '模拟', '构造'],
            template: "#include <iostream>\nusing namespace std;\nint main() {\n    // 在此编写代码\n    return 0;\n}",
            referenceCode: "#include <iostream>\nusing namespace std;\nint main() {\n    long long n; cin >> n;\n    int ans = 0;\n    for (int d = 1; d <= 9; d++) {\n        long long cur = d;\n        while (cur <= n) {\n            ans++;\n            if (cur > 2e18 / 10) break; // 安全边界\n            cur = cur * 10 + d;\n        }\n    }\n    cout << ans << endl;\n    return 0;\n}"
        },
        {
            id: 27,
            type: 'programming',
            title: "菱形",
            samples: [
                {
                    input: `3`,
                    output: `.#.\n#.#\n.#.`
                },
                {
                    input: `9`,
                    output: `....#....\n...#.#...\n..#...#..\n.#.....#.\n#.......#\n.#.....#.\n..#...#..\n...#.#...\n....#....`
                }
            ],
            question: `
# [GESP202509 二级] 菱形

## 题目描述

小 A 想绘制一个菱形。具体来说，需要绘制的菱形是一个 $n$ 行 $n$ 列的字符画，$n$ 是一个大于 $1$ 的奇数。菱形的四个顶点依次位于第 $1$ 行、第 $1$ 列、第 $n$ 行、第 $n$ 列的正中间，使用 \`#\` 绘制。相邻顶点之间也用 \`#\` 连接。其余位置都是 \`.\`。

例如，一个 $5$ 行 $5$ 列的菱形字符画是这样的：

\`\`\`
..#..
.#.#.
#...#
.#.#.
..#..
\`\`\`

给定 $n$，请你帮小 A 绘制对应的菱形。

## 输入格式

一行，一个正整数 $n$。

## 输出格式

输出共 $n$ 行，表示对应的菱形。
`,
            score: 25,
            explanation: `**解析：**
本题考察二维图形的数学特征刻画。
1. **中心定位**：矩阵大小为 $n \times n$，中心点坐标为 $(mid, mid)$，其中 $mid = (n-1)/2$（索引从 0 开始）。
2. **菱形边缘特征**：菱形边缘上的点到中心点的**曼哈顿距离**（即 $|x1 - x2| + |y1 - y2|$）是一个定值。
3. **距离判定**：对于菱形顶点到中心的距离，恰好是 $mid$。
   - 如果 $|i - mid| + |j - mid| == mid$，则输出 \`#\`。
   - 否则输出 \`.\`。

**核心逻辑提示：**
\`\`\`cpp
int mid = n / 2;
for (int i = 0; i < n; i++) {
    for (int j = 0; j < n; j++) {
        if (abs(i - mid) + abs(j - mid) == mid) cout << "#";\n        else cout << ".";
    }\n    cout << endl;\n}
\`\`\``,
            tags: ['编程题', '循环', '曼哈顿距离'],
            template: "#include <iostream>\n#include <cmath>\nusing namespace std;\nint main() {\n    // 在此编写代码\n    return 0;\n}",
            referenceCode: "#include <iostream>\n#include <cmath>\nusing namespace std;\nint main() {\n    int n; cin >> n;\n    int mid = n / 2;\n    for (int i = 0; i < n; i++) {\n        for (int j = 0; j < n; j++) {\n            if (abs(i - mid) + abs(j - mid) == mid) cout << '#';\n            else cout << '.';\n        }\n        cout << endl;\n    }\n    return 0;\n}"
        }
    ]
};
