// 2023年6月 GESP C++ 二级真题 (第2次认证)
export const paperData = {
    id: '2023-06-l2',
    title: '2023年6月 GESP C++ 二级真题',
    level: 2,
    year: 2023,
    month: 6,
    session: 2,
    note: '首次开启5-8级',
    timeLimit: 90 * 60,
    questions: [
        {
            id: 1,
            type: 'single',
            question: `高级语言编写的程序需要经过以下（ ）操作，才可以生成在计算机上运行的可执行代码。`,
            options: ['编辑', '保存', '调试', '编译'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (编译)**

**选项逐项分析：**
- **A 编辑**：❌ 错误。编辑是指使用文本编辑器编写或修改源代码（如 .cpp 文件），此时代码仍是文本形式。
- **B 保存**：❌ 错误。保存只是将内存中的源代码写入磁盘，不会改变其非执行的本质。
- **C 调试**：❌ 错误。调试（Debug）是查找并修复程序错误的过程，前提通常是程序已经可以运行或至少可以通过初步处理。
- **D 编译**：✅ 正确。编译（Compilation）是将人类可读的高级语言源代码翻译成机器可执行的指令（机器码）的核心过程。

**考点：** 程序开发生命周期（编辑、编译、连接、运行）的基本概念`,
            tags: ['基础语法']
        },
        {
            id: 2,
            type: 'single',
            question: `能够实现“当条件不成立时反复执行语句块、条件成立时结束循环”流程图功能的伪代码是（ ）。`,
            options: ['if 条件判断 then 语句块', 'if 条件判断 then 什么也不做 else 语句块', 'while 条件判断 do 语句块', 'while not 条件判断 do 语句块'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (while not 条件判断 do 语句块)**

**选项逐项分析：**
- **A / B**：❌ 错误。\`if\` 是分支结构，只能执行一次，无法实现“反复执行”的循环功能。
- **C while 条件判断 do 语句块**：❌ 错误。这是“当条件**成立**时反复执行”，与题意要求的“条件不成立时反复执行”逻辑相反。
- **D while not 条件判断 do 语句块**：✅ 正确。使用 \`not\` 对条件取反，使得当原条件为“假”（不成立）时，循环判断为“真”，从而进入循环体。

**考点：** 循环结构的逻辑控制、逻辑非运算符的应用`,
            tags: ['循环', '条件判断']
        },
        {
            id: 3,
            type: 'single',
            question: `下列关于 C++ 语言的叙述，正确的是（ ）。`,
            options: ['char 类型变量不能赋值给 int 类型变量', '两个 int 类型变量相乘，结果仍为 int 类型', '两个 int 相乘溢出时程序会报错崩溃', 'double 相除且除数为 0.0 时程序会报错崩溃'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (两个 int 类型变量相乘，结果仍为 int 类型)**

**选项逐项分析：**
- **A char 类型变量不能赋值给 int 类型变量**：❌ 错误。\`char\` 本质是小整数，可以通过隐式类型转换直接赋值给 \`int\`（对应其 ASCII 码）。
- **B 两个 int 类型变量相乘，结果仍为 int 类型**：✅ 正确。C++ 规定，同类型数据运算的结果类型保持不变。
- **C 两个 int 相乘溢出时程序会报错崩溃**：❌ 错误。整数溢出在 C++ 中属于“未定义行为”，通常会发生回绕（变成一个负数或错误正数），但通常**不会**导致程序直接崩溃报错。
- **D double 相除且除数为 0.0 时程序会报错崩溃**：❌ 错误。浮点数除以 0.0 在 IEEE 754 标准下会得到正无穷（inf）或负无穷，通常**不会**导致程序崩溃退出（与整数除以 0 不同）。

**考点：** 数据类型运算规则、溢出处理、浮点数标准`,
            tags: ['变量与标识符', '程序分析']
        },
        {
            id: 4,
            type: 'single',
            question: `下列关于 C++ 语句规则的叙述，不正确的是（ ）。`,
            options: ['if 的判断条件必须放在小括号中', 'for 语句中间循环条件可省略，表示恒为真', '循环体有多条语句时用缩进即可消除二义性', '运算符有优先级，不仅有先乘除后加减'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (循环体有多条语句时用缩进即可消除二义性)**

**选项逐项分析：**
- **A if 的判断条件必须放在小括号中**：❌ 错误（说法**正确**）。这是 C++ 的强制语法要求。
- **B for 语句中间循环条件可省略，表示恒为真**：❌ 错误（说法**正确**）。\`for(;;)\` 是合法的死循环写法，中间缺省默认为真。
- **C 循环体有多条语句时用缩进即可消除二义性**：✅ 正确（该说法**错误**）。C++ 是自由格式语言，不依赖缩进识别代码块。如果循环体有多个语句且不用 \`{}\` 括起来，只有紧随其后的第一条语句属于循环体。
- **D 运算符有优先级，不仅有先乘除后加减**：❌ 错误（说法**正确**）。C++ 拥有数十种运算符，如位运算、逻辑运算等均有严密的优先级顺序。

**考点：** C++ 基本语法规范、代码块的使用规则（花括号 vs 缩进）`,
            tags: ['循环', '基础语法']
        },
        {
            id: 5,
            type: 'single',
            question: `以下哪个是 C++ 语言关键字（ ）。`,
            options: ['main', 'max', 'double', 'sqrt'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (double)**

**选项逐项分析：**
- **A main**：❌ 错误。\`main\` 是程序的入口函数名，虽然特殊，但它是预定义的标识符，而非编译器保留的关键字。
- **B max**：❌ 错误。\`max\` 是标准库 (\`algorithm\`) 中的函数名。
- **C double**：✅ 正确。\`double\` 是 C++ 内置的双精度浮点数类型关键字。
- **D sqrt**：❌ 错误。\`sqrt\` 是数学库 (\`cmath\`) 中的函数名。

- **A main**：❌ 错误。\`main\` 是标准入口函数名，它是预定义的标识符而非关键字，用户甚至可以覆盖重命名它（虽然不推荐）。
- **B max**：❌ 错误。\`max\` 是 \`<algorithm>\` 头文件中的标准函数模板。
- **C double**：✅ 正确。\`double\` 是内置的双精度浮点数类型描述符，属于 C++ 语言保留关键字。
- **D sqrt**：❌ 错误。\`sqrt\` 是 \`<cmath>\` 库中的数学函数。

**考点：** 关键字与预定义标识符、标准库函数的区分`,
            tags: ['变量与标识符']
        },
        {
            id: 6,
            type: 'single',
            question: `以下哪个不是 C++ 语言运算符（ ）。`,
            options: ['>=', '/=', '||', '<>'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (<>)**

**选项逐项分析：**
- **A >=**：❌ 错误。这是合法的关系运算符。
- **B /=**：❌ 错误。这是合法的复合赋值运算符。
- **C ||**：❌ 错误。这是合法的逻辑或运算符。
- **D <>**：✅ 正确。C++ 中不等于必须使用 \`!=\`。虽然部分语言使用 \`<>\`，但在 C++ 中此符号非法（仅在模板参数语法中作为界定符）。

**考点：** C++ 标准运算符集`,
            tags: ['运算符']
        },
        {
            id: 7,
            type: 'single',
            question: `若 a 为 int、b 为 char，下列哪个语句不符合 C++ 语法（ ）。`,
            options: ['a = a+1.0;', 'a = (int)(b-\'0\');', 'b = (char)(a+\'0\');', '(int)b = a;'],
            answer: 3,
            score: 2,
            explanation: `**答案：D ((int)b = a;)**

**选项逐项分析：**
- **A a = a+1.0;**：❌ 错误（语法**正确**）。\`a+1.0\` 结果为 double，赋值给 int 时会发生隐式截断转换。
- **B a = (int)(b-'0');**：❌ 错误（语法**正确**）。这是将字符数字转换为对应数值的常用写法。
- **C b = (char)(a+'0');**：❌ 错误（语法**正确**）。这是将 0-9 的数值转换为对应数字字符的写法。
- **D (int)b = a;**：✅ 正确（语法**错误**）。强制类型转换的结果是一个“右值”（临时值），不能出现在赋值号的左边（左值必须是可写的内存空间）。

**考点：** 左值与右值概念、强制类型转换的限制`,
            tags: ['变量与标识符', '运算符']
        },
        {
            id: 8,
            type: 'single',
            question: `用 int 变量 a、b 表示平行四边形边长，int 变量 h 表示 a 边对应高，以下哪个表达式不能用于计算 b 边对应高（ ）。`,
            options: ['a / b * (0.0+h)', '(0.0+a * h) / b', 'a * h / (b+0.0)', '(a+0.0) * h / b'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (a / b * (0.0+h))**

**选项逐项分析：**
- **A a / b * (0.0+h)**：✅ 正确（该式计算**错误**）。由于 \`a\` 和 \`b\` 都是 \`int\`，它们先执行整数除法 \`a / b\`。如果 $a < b$，结果直接变为 0，导致整体结果错误。
- **B (0.0+a * h) / b**：❌ 错误（该式计算正确）。\`0.0+...\` 确保了计算是以浮点数形式进行的。
- **C a * h / (b+0.0)**：❌ 错误（该式计算正确）。分母加 0.0 使除法变为浮点除法。
- **D (a+0.0) * h / b**：❌ 错误（该式计算正确）。由于 \`a+0.0\` 是 double，后续的所有运算都会自动提升为 double。

**考点：** 混合运算中的类型提升规则、整数除法的精度损失陷阱`,
            tags: ['运算符', '程序分析']
        },
        {
            id: 9,
            type: 'single',
            question: `以下哪个循环语句会无限次执行（ ）。`,
            options: ['for (int a = 0; a; a++) ;', 'for (bool b = false; b <= true; b++) ;', 'for (char c = \'A\'; c < \'z\'; c++) ;', 'for (double d = 0.0; d < 10.0; d += 0.001) ;'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (for (bool b = false; b <= true; b++) ;)**

**选项逐项分析：**
- **A for (int a = 0; a; a++) ;**：❌ 错误。初始 \`a=0\` 为假，循环一次都不执行。
- **B for (bool b = false; b <= true; b++) ;**：✅ 正确。\`bool\` 变量 \`b\` 只有 0 (\`false\`) 和 1 (\`true\`) 两种状态。当执行 \`b++\` 时，\`b\` 的值从 0 变 1。在 1 之后再执行 \`b++\`，在许多编译器中 \`b\` 仍保持为 1。因此 \`b <= true\` (即 \`1 <= 1\`) 始终成立，形成死循环。
- **C for (char c = 'A'; c < 'z'; c++) ;**：❌ 错误。\`char\` 会按 ASCII 码逐个增加，最终会超过 \`'z'\` 从而终止。
- **D for (double d = 0.0; d < 10.0; d += 0.001) ;**：❌ 错误。尽管浮点数有精度问题，但最终 \`d\` 会累加超过 10.0 并正常退出。

**考点：** bool 类型的自增特性、循环终止条件的精确性判定`,
            tags: ['循环', '程序分析']
        },
        {
            id: 10,
            type: 'single',
            question: `若 char 变量 a 的值为 'C'（ASCII 67），执行 cout << (a+2); 输出（ ）。`,
            options: ['E', 'C+2', 'C2', '69'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (69)**

**选项逐项分析：**
- **A E**：❌ 错误。虽然 'C'+2 在逻辑上对应 'E'，但由于 \`a+2\` 中存在整数常量，整条表达式会发生类型提升，结果变为了 \`int\` 类型的 69。\`cout\` 输出 \`int\` 时会打印数值。
- **B / C**：❌ 错误。无此输出逻辑。
- **D 69**：✅ 正确。\`char\` 类型在与 \`int\` 进行算术运算时，会自动转换为对应的 ASCII 码参与计算，结果类型为 \`int\`。

**考点：** C++ 自动类型提升规则、cout 对 different 类型数据的默认输出行为`,
            tags: ['输入输出', '运算符']
        },
        {
            id: 11,
            type: 'single',
            question: `若 a、b 均为 int，能正确判断“a 等于 1 且 b 等于 1”的表达式是（ ）。`,
            options: ['(a == b) && (b == 1)', '(a && b)', '(a == b == 1)', '(a * b == 1)'],
            answer: 0,
            score: 2,
            explanation: `**答案：A ((a == b) && (b == 1))**

**选项逐项分析：**
- **A (a == b) && (b == 1)**：✅ 正确。逻辑为：如果 $a=b$ 且 $b=1$，推导得出 $a=1, b=1$。这是严密的逻辑表达。
- **B (a && b)**：❌ 错误。这只能判断 $a$ 和 $b$ 是否都非零（如 $a=2, b=3$ 也会判定为真），无法精准锁定到 1。
- **C (a == b == 1)**：❌ 错误。C++ 的连等陷阱：先计算 \`a == b\` 得到 0 或 1，再用这个 0 或 1 与 1 比较。
- **D (a * b == 1)**：❌ 错误。虽然在正整数中成立，但如果是负数（如 $a=-1, b=-1$），结果也是 1，无法保证 $a, b$ 各自为 1。

**考点：** 逻辑与 (\`&&\`)、相等判断 (\`==\`) 的组合逻辑`,
            tags: ['条件判断', '运算符']
        },
        {
            id: 12,
            type: 'single',
            question: `若 a 为 char，哪个表达式可正确判断“a 是数字字符”（ ）。`,
            options: ['\'0\' <= a && a <= \'9\'', '\'1\' <= a && a <= \'0\'', '\'0\' <= a <= \'9\'', '\'1\' <= a <= \'0\''],
            answer: 0,
            score: 2,
            explanation: `**答案：A ('0' <= a && a <= '9')**

**选项逐项分析：**
- **A**：✅ 正确。利用 ASCII 码的连续性，使用 \`&&\` 连接两个闭区间边界。
- **B**：❌ 错误。范围颠倒。
- **C / D**：❌ 错误。连写比较陷阱。\`'0' <= a <= '9'\` 会先计算 \`'0' <= a\` 得到布尔值 0 或 1，而 0 或 1 永远小于 \`'9'\`（其 ASCII 码为 57），因此表达式恒为真，失去判断意义。

**考点：** 字符在内存中的数值表示、关系运算符的结合性`,
            tags: ['条件判断', '运算符']
        },
        {
            id: 13,
            type: 'single',
            question: `在给定代码横线处填入（ ），使得最终输出字符是 9。\n\`\`\`cpp\nchar a = '3', b = '6';\ncout << ________;\n\`\`\``,
            options: ['(a+b)', '(a+b-\'0\')', '(char)(a+b)', '(char)(a+b-\'0\')'],
            answer: 3,
            score: 2,
            explanation: `**答案：D ((char)(a+b-'0'))**

**选项逐项分析：**
- **A (a+b)**：❌ 错误。输出 \`int\` 类型的数值（51+54=105）。
- **B (a+b-'0')**：❌ 错误。输出 \`int\` 类型的数值（105-48=57）。虽然 57 对应字符 '9'，但 \`cout\` 遇到 \`int\` 表达式会直接打印数字 57。
- **C (char)(a+b)**：❌ 错误。将 105 强转为 \`char\`，输出 ASCII 码为 105 的字符（'i'）。
- **D (char)(a+b-'0')**：✅ 正确。计算得出数值 57，再强转为 \`char\` 类型，输出字符 '9'。

**考点：** 字符数字与真实数值的转换 (\`-'0'\`)、类型强转对 \`cout\` 输出行为的影响`,
            tags: ['程序分析', '运算符']
        },
        {
            id: 14,
            type: 'single',
            question: `在给定循环代码横线处填写（ ），可以使输出为 42。\n\`\`\`cpp\nint ans = 0;\nfor (int i = 1; i <= 10; i++) {\n    if (________) ans += i;\n}\ncout << ans;\n\`\`\``,
            options: ['i % 3 == 0', '20 % i == 0', 'i <= 8', 'i >= 18'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (20 % i == 0)**

**选项逐项分析：**
- **A i % 3 == 0**：❌ 错误。符合条件的 $i$ 为 $\{3, 6, 9\}$，和为 $3+6+9=18$。
- **B 20 % i == 0**：✅ 正确。寻找 1-10 之间 20 的约数，符合条件的 $i$ 为 $\{1, 2, 4, 5, 10\}$。和为 $1+2+4+5+10=22$。
- **C i <= 8**：❌ 错误。和为 $1+2+...+8 = 36$。
- **D i >= 18**：❌ 错误。循环范围只有 1-10，该条件永远不成立。

**考点：** 循环累加逻辑、条件过滤、数值模拟`,
            tags: ['循环', '程序分析']
        },
        {
            id: 15,
            type: 'single',
            question: `执行给定 C++ 程序后，输出结果是（ ）。\n\`\`\`cpp\nint a = 5, b = 2;\nif (a > b) a = b;\nelse b = a;\ncout << a << b;\n\`\`\``,
            options: ['52', '22', '25', '55'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (22)**

**选项逐项分析：**
- **A 52**：❌ 错误。这是初始值，忽略了 \`if\` 语句的执行。
- **B 22**：✅ 正确。逻辑追踪：
  1. 初始 $a=5, b=2$。
  2. 判断 \`a > b\` (5 > 2) 为“真”。
  3. 执行 \`if\` 分支：\`a = b\`，此时变量 \`a\` 的值变为 2。
  4. \`else\` 分支被跳过。
  5. \`cout << a << b\` 输出当前值，即 \`2\` 和 \`2\`，结果为 22。
- **C 25**：❌ 错误。这可能是误认为执行了 \`else\` 分支。
- **D 55**：❌ 错误。逻辑上不成立。

**考点：** \`if-else\` 分支逻辑、变量赋值顺序与覆盖原理`,
            tags: ['程序分析']
        },
        {
            id: 16,
            type: 'judge',
            question: `内存 64KB 的 CEC-I 若存放 GB2312 的 6763 个汉字（每字 2 字节），占用不超过 1/5 内存。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. 计算汉字占用空间：$6763 \times 2 = 13526$ 字节。
2. 转换为 KB：$13526 / 1024 \approx 13.21$ KB。
3. 计算内存占比：$13.21 / 64 \approx 0.206$。
4. 判定：$0.206$ 略大于 $1/5$ ($0.2$)，因此“不超过 1/5”的说法是错误的。

**考点：** 计算机存储容量单位转换（B, KB）、存储空间计算`,
            tags: ['计算机基础']
        },
        {
            id: 17,
            type: 'judge',
            question: `域名 gesp.ccf.org.cn 的顶级域名是 gesp。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
域名层级是从右向左读取的。在 \`gesp.ccf.org.cn\` 中：
- \`cn\` 是顶级域名（国家顶级域名）。
- \`org.cn\` 是二级域名。
- \`ccf.org.cn\` 是三级域名。
\`gesp\` 只是该完整域名下的主机名或最底层子域名。

**考点：** 域名系统 (DNS) 的层级结构概念`,
            tags: ['网络基础']
        },
        {
            id: 18,
            type: 'judge',
            question: `在 C++ 中，使用 sqrt、abs 等数学函数前需包含 <cmath> 或 <math.h>。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
C++ 标准要求所有的库函数必须在对应的头文件中声明后才能使用。\`sqrt\`（开方）和 \`abs\`（整数绝对值/浮点绝对值）等数学工具定义在 \`cmath\` 标准库中。

**考点：** 标准头文件的作用、数学库函数的使用规范`,
            tags: ['基础语法']
        },
        {
            id: 19,
            type: 'judge',
            question: `在 C++ 中，下划线“_”既可用于标识符命名，也是运算符。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
下划线 \`_\` 在 C++ 中仅能作为**标识符**（变量名、函数名等）的组成部分。它本身不具备任何运算功能，不属于 C++ 运算符集。

**考点：** C++ 标识符组成规则、运算符符号集`,
            tags: ['基础语法']
        },
        {
            id: 20,
            type: 'judge',
            question: `若 a 为 double 且 a=3.5，则 a*10 结果是 35 且类型为 int。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
在 C++ 混合运算中，遵循“低级向高级提升”的原则。\`a\` 是 \`double\` 类型，数值 \`10\` 是 \`int\` 类型。计算 \`a * 10\` 时，\`10\` 会先提升为 \`double\` 类型的 \`10.0\`，运算结果为 \`double\` 类型的 \`35.0\`。

**考点：** 算术运算中的自动类型提升规则`,
            tags: ['数据类型']
        },
        {
            id: 21,
            type: 'judge',
            question: `if 语句中，若条件成立需执行多条语句，可用花括号把这些语句括起来。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
在 C++ 中，\`if\`, \`for\`, \`while\` 等控制结构默认只关联其后的第一条语句。如果需要关联多条语句，必须使用 \`{}\` 构成复合语句（代码块）。

**考点：** 代码块的作用、自由格式语言的控制范围`,
            tags: ['基础语法']
        },
        {
            id: 22,
            type: 'judge',
            question: `循环语句的循环体有可能无限制执行下去。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
如果循环条件永远保持为“真”（如 \`while(true)\` 或错误的循环变量更新逻辑），循环体将无限制执行下去，形成“死循环”。

**考点：** 循环终止条件的控制逻辑`,
            tags: ['循环结构']
        },
        {
            id: 23,
            type: 'judge',
            question: `++ 和 == 是 C++ 运算符，但 += 不是。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
\`++\` 是自增运算符，\`==\` 是相等关系运算符。而 \`+=\` 是典型的**复合赋值运算符**，用于简写 \`a = a + b\`，它是 C++ 语言中非常重要的一个运算符。

**考点：** 运算符分类、赋值运算符的使用`,
            tags: ['运算符']
        },
        {
            id: 24,
            type: 'judge',
            question: `若 a 为 char 且值为大写字母 F，执行 a = a+1 后，a 变为 G。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
在 ASCII 码表中，英文字母是按顺序连续排列的。'F' 的 ASCII 码是 70，'G' 的 ASCII 码是 71。执行 \`a+1\` 运算后结果为 71，再存回 \`char\` 变量 \`a\`，\`a\` 即变为 'G'。

**考点：** 字符的算术运算原理、ASCII 码顺序性`,
            tags: ['数据类型']
        },
        {
            id: 25,
            type: 'judge',
            question: `表达式 sqrt(9.0) 的结果为 3，且结果类型为 int。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
\`sqrt()\` 函数的返回值类型始终为浮点数（通常是 \`double\`）。虽然 \`sqrt(9.0)\` 的数学结果是 3，但在程序中它是一个 \`double\` 类型的 \`3.0\`，而不是 \`int\` 类型的 3。

**考点：** 库函数返回值类型判定、浮点数与整数的区别`,
            tags: ['数据类型']
        },
    ],
    programmingQuestions: [
        {
            id: 26,
            type: 'programming',
            title: "找素数",
            samples: [
                {
                    input: `2 10`,
                    output: `4`
                },
                {
                    input: `98 100`,
                    output: `0`
                }
            ],
            question: `
# [GESP202306 二级] 找素数

## 题目描述

小明刚刚学习了素数的概念：如果一个大于 $1$ 的正整数，除了 $1$ 和它自身外，不能被其他正整数整除，则这个正整数是素数。现在，小明想找到两个正整数 $A$ 和 $B$ 之间（包括 $A$ 和 $B$）有多少个素数。

## 输入格式

输入只有一行两个正整数 $A, B$。约定 $2 \\le A \\le B \\le 1000$。

## 输出格式

输出一行，包含一个整数 $C$，表示找到 $C$ 个素数。
`,
            score: 25,
            explanation: `**解析：**
本题考察素数判定算法。
1. **素数定义**：只能被 1 和自身整除的且大于 1 的正整数。
2. **判定方法**：
   - 对于待检测数 $x$，尝试从 2 循环到 $\sqrt{x}$。
   - 如果 $x$ 能被其中任何一个数整除，则不是素数。
3. **遍历计数**：
   - 使用 \`for\` 循环从 $A$ 遍历到 $B$。
   - 对每个数调用素数判定逻辑，如果为真则计数器 \`ans++\`。

**核心代码提示：**
\`\`\`cpp
bool isPrime(int x) {
    if (x < 2) return false;
    for (int i = 2; i * i <= x; i++) {
        if (x % i == 0) return false;
    }
    return true;
}
\`\`\``,
            tags: ["编程题", "枚举", "素数"],
            template: "#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    int a, b;\n    cin >> a >> b;\n    // \n    return 0;\n}",
            referenceCode: "#include <bits/stdc++.h>\nusing namespace std;\n\nbool isPrime(int x) {\n    if (x < 2) return false;\n    for (int d = 2; d * d <= x; ++d) {\n        if (x % d == 0) return false;\n    }\n    return true;\n}\n\nint main() {\n    int A, B;\n    cin >> A >> B;\n    int ans = 0;\n    for (int x = A; x <= B; ++x) if (isPrime(x)) ++ans;\n    cout << ans << endl;\n    return 0;\n}",
            answer: '',
        },
        {
            id: 27,
            type: 'programming',
            title: "自幂数判断",
            samples: [
                {
                    input: `3\n152\n111\n153`,
                    output: `F\nF\nT`
                }
            ],
            question: `
# [GESP202306 二级] 自幂数判断

## 题目描述

自幂数是指，一个 $N$ 位数，满足各位数字 $N$ 次方之和是本身。
例如，$153$ 是 $3$ 位数，$1^3+5^3+3^3=153$，因此 $153$ 是自幂数。

## 输入格式

第一行是一个正整数 $M$。之后 $M$ 行，每行一个待判断的正整数。

## 输出格式

输出 $M$ 行，如果是自幂数输出 T，否则输出 F。
`,
            score: 25,
            explanation: `**解析：**
本题考察数位拆解与幂运算的应用。
1. **确定位数**：
   - 使用一个临时变量拷贝原数，通过反复除以 10 计算其位数 $N$。
2. **拆分与累加**：
   - 再次通过 \`% 10\` 和 \`/ 10\` 拆出每一位。
   - 对每一位计算其 $N$ 次方（建议手写循环或使用 \`pow\` 函数，注意 \`pow\` 返回浮点数需处理精度）。
   - 将结果累加。
3. **判定**：
   - 比较累加和与原数是否相等。

**核心逻辑提示：**
\`\`\`cpp
int countDigits(long long x) {
    int cnt = 0;
    while(x) { cnt++; x /= 10; }
    return cnt;
}
long long myPow(int a, int b) {
    long long res = 1;
    for(int i=0; i<b; i++) res *= a;
    return res;
}
\`\`\``,
            tags: ["编程题", "模拟", "数位分解"],
            template: "#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    int m;\n    cin >> m;\n    // \n    return 0;\n}",
            referenceCode: "#include <bits/stdc++.h>\nusing namespace std;\n\nlong long ipow(long long a, int b) {\n    long long r = 1;\n    while (b--) r *= a;\n    return r;\n}\n\nbool ok(long long x) {\n    if (x == 0) return true;\n    int len = 0;\n    long long t = x;\n    while (t > 0) { len++; t /= 10; }\n    long long sum = 0; t = x;\n    while (t > 0) {\n        sum += ipow(t % 10, len);\n        t /= 10;\n    }\n    return sum == x;\n}\n\nint main() {\n    int M;\n    cin >> M;\n    while (M--) {\n        long long x;\n        cin >> x;\n        cout << (ok(x) ? 'T' : 'F') << endl;\n    }\n    return 0;\n}",
            answer: '',
        }
    ]
};
