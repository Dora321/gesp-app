// 2023年6月 GESP C++ 二级真题 (第2次认证)
export const paperData = {
    id: '2023-06-l2',
    title: '2023年6月 GESP C++ 二级真题',
    level: 2,
    year: 2023,
    month: 6,
    session: 2,
    note: '首次开启5-8级',
    timeLimit: 90 * 60,
    questions: [
        {
            id: 1,
            type: 'single',
            question: `高级语言编写的程序需要经过以下（ ）操作，才可以生成在计算机上运行的可执行代码。`,
            options: ['编辑', '保存', '调试', '编译'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (编译)**

**核心解析：**
高级语言程序从源代码到可执行文件的转换过程如下：
1. **编辑**：使用文本编辑器（如 Dev-C++）编写源代码，生成 .cpp 文件，此时文件内容仅为文本字符。
2. **保存**：将内存中的代码写入磁盘存储，文件格式和内容均未改变。
3. **编译**：编译器将源代码翻译成目标机器指令，生成 .obj 文件，这是实现"从文本到机器码"转换的关键步骤。
4. **调试**：在编译/运行阶段排查错误，与代码能否生成可执行文件无直接关系。

从技术流程看，从高级语言源代码到可执行代码，必须经过"编译"这一核心步骤，没有其他选项能完成这一转换。

**选项逐项分析：**
- **A (编辑)**：❌ 错误。编辑只是创建或修改源代码文本，源代码仍然是人类可读的文本格式，无法被计算机直接执行。
- **B (保存)**：❌ 错误。保存操作将内存中的数据写入磁盘存储介质，但这仅是文件持久化操作，不改变文件的本质属性——仍然是不可执行的文本。
- **C (调试)**：❌ 错误。调试（Debug）是使用调试器排查程序逻辑错误或运行时错误的过程，此时程序已经可以通过编译（至少是部分通过），与"生成可执行代码"无直接逻辑关系。
- **D (编译)**：✅ 正确。编译（Compilation）是将高级语言源代码通过词法分析、语法分析、语义分析、代码优化等步骤，生成目标机器码的核心过程，是生成可执行代码的必要条件。

**易错提醒：**
- 容易误选"调试"，因为调试是编程中的常见环节，但它是针对已存在代码的后续操作，不属于生成可执行代码的必要步骤。
- 编辑和保存属于编程的前置准备工作，编译才是实现代码转换的关键环节。

**知识延伸：**
完整的 C++ 程序开发流程包括：编辑（源代码）→ 编译（生成目标文件 .obj）→ 链接（连接库函数生成 .exe）→ 运行。GESP 二级主要考察编译这一环节。

**考点：** 程序开发基本流程（编辑、编译、运行）`,
            tags: ['基础语法']
        },
        {
            id: 2,
            type: 'single',
            question: `能够实现“当条件不成立时反复执行语句块、条件成立时结束循环”流程图功能的伪代码是（ ）。`,
            options: ['if 条件判断 then 语句块', 'if 条件判断 then 什么也不做 else 语句块', 'while 条件判断 do 语句块', 'while not 条件判断 do 语句块'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (while not 条件判断 do 语句块)**

**核心解析：**
题目要求实现"当条件不成立时反复执行、条件成立时结束循环"的功能。分析逻辑如下：

- 设原条件为 P
- 需求是：当 P 不成立时执行循环，当 P 成立时结束循环
- 换言之：循环继续的条件是 "not P"（P的否定）
- 即：当 "not P" 为真时，执行循环体

伪代码表示：\`while not P do 语句块\`

逐项验证选项：
- **A**：❌ 错误。if 语句是单次分支，执行后立即结束，不满足"反复执行"要求
- **B**：❌ 错误。if-else 同样是单次分支，无论条件真假都只执行一次
- **C**：❌ 错误。\`while P do\` 表示当 P 成立时执行，与需求相反
- **D**：✅ 正确。\`while not P do\` 正确表达"条件不成立时执行循环"

**选项逐项分析：**
- **A (if 条件判断 then 语句块)**：❌ 错误。if 是分支结构，仅根据条件判断执行或不执行一次语句块，执行后程序流程向下继续，不存在"反复执行"的可能。
- **B (if 条件判断 then 什么也不做 else 语句块)**：❌ 错误。if-else 同样是单次分支结构，仅在条件不成立时执行一次语句块，无法实现循环的反复执行效果。
- **C (while 条件判断 do 语句块)**：❌ 错误。这是标准的"当型循环"，逻辑是"当条件成立时反复执行"，恰好与题目要求的"条件不成立时反复执行"相反。
- **D (while not 条件判断 do 语句块)**：✅ 正确。使用逻辑非运算符 not 将条件取反，实现"条件不成立时进入循环，条件成立时退出循环"的语义。

**易错提醒：**
- 容易误选 C，因为 C 选项看起来像循环结构，但忽略了题目中"条件不成立时执行"这一关键反向条件。
- 循环结构的边界条件（继续执行/退出执行）是此类题目的核心区分点。

**知识延伸：**
C++ 中的 while 循环标准形式是 \`while (条件表达式) { 循环体 }\`，条件为真时继续执行。C++ 没有内置的 \`while not\`，但可以用 \`while (!条件)\` 实现同样的逻辑控制效果。

**考点：** 循环结构的条件控制、逻辑非运算的应用`,
            tags: ['循环', '条件判断']
        },
        {
            id: 3,
            type: 'single',
            question: `下列关于 C++ 语言的叙述，正确的是（ ）。`,
            options: ['char 类型变量不能赋值给 int 类型变量', '两个 int 类型变量相乘，结果仍为 int 类型', '两个 int 相乘溢出时程序会报错崩溃', 'double 相除且除数为 0.0 时程序会报错崩溃'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (两个 int 类型变量相乘，结果仍为 int 类型)**

**核心解析：**
本题考察 C++ 中数据类型运算的基本规则和边界行为。

**逐项分析四个陈述的准确性：**

1. **类型转换规则**：char 类型本质上是一个 8 位整数类型，C++ 允许将 char 隐式转换为 int（取其 ASCII 码值或数值）。因此"A 错误"。

2. **运算结果类型**：C++ 的类型提升规则规定：两个相同类型的操作数运算，结果类型保持不变。int 与 int 相乘，结果类型仍为 int（即使数值可能溢出）。因此"B 正确"。

3. **整数溢出行为**：C++ 标准规定，整数溢出属于"未定义行为"（Undefined Behavior），程序不会因此"报错崩溃"，而是产生不可预期的结果（通常是回绕）。因此"C 错误"。

4. **浮点数除零行为**：根据 IEEE 754 浮点数标准，浮点数除以 0.0 的结果是 ±infinity（无穷大），C++ 会正常产生这个特殊值，程序不会崩溃。因此"D 错误"。

**选项逐项分析：**
- **A (char 类型变量不能赋值给 int 类型变量)**：❌ 错误。该陈述本身错误。char 类型可以隐式转换为 int 类型，例如 \`int a = 'A';\` 会将字符 'A' 的 ASCII 码值 65 赋值给 a。这是 C++ 基础类型转换规则。
- **B (两个 int 类型变量相乘，结果仍为 int 类型)**：✅ 正确。C++ 规定，相同类型的两个操作数进行运算，结果类型与操作数类型一致。int 与 int 运算，结果为 int。
- **C (两个 int 相乘溢出时程序会报错崩溃)**：❌ 错误。该陈述本身错误。整数溢出是 C++ 的未定义行为，程序通常不会崩溃或报错，而是产生回绕效果（例如 INT_MAX + 1 会变成一个负数）。
- **D (double 相除且除数为 0.0 时程序会报错崩溃)**：❌ 错误。该陈述本身错误。浮点数除以 0.0 在 IEEE 754 标准下会得到正无穷大（inf）或负无穷大，C++ 不会因此报错或崩溃，这与整数除以 0（会直接导致程序崩溃）有本质区别。

**易错提醒：**
- C 选项的陷阱在于混淆了"溢出"和"崩溃"的概念。整数溢出不会触发任何异常或错误提示，只是产生错误的数值结果。
- D 选项中，整数除以 0 会崩溃，但浮点数除以 0 不会，两者行为不同，需要区分。

**知识延伸：**
C++ 中不同类型混合运算时会发生"整型提升"（Integral Promotion），char、short 等小型整数类型会自动提升为 int。C++ 标准库提供 \`<climits>\` 头文件中的 INT_MAX、INT_MIN 等常量，可查询整数类型的取值范围。

**考点：** C++ 基本数据类型规则、隐式类型转换、未定义行为`,
            tags: ['变量与标识符', '程序分析']
        },
        {
            id: 4,
            type: 'single',
            question: `下列关于 C++ 语句规则的叙述，不正确的是（ ）。`,
            options: ['if 的判断条件必须放在小括号中', 'for 语句中间循环条件可省略，表示恒为真', '循环体有多条语句时用缩进即可消除二义性', '运算符有优先级，不仅有先乘除后加减'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (循环体有多条语句时用缩进即可消除二义性)**

**核心解析：**
本题要求选出"不正确的叙述"，即找出那个错误的陈述。

**逐项分析四个说法的正误：**

1. **A 选项**：C++ 语法规定，if 语句的判断条件必须放在圆括号 \`()\` 内，例如 \`if (a > b)\`。这是强制语法要求，说法正确。

2. **B 选项**：\`for\` 循环的三个表达式都可以省略，中间的循环条件省略后默认为真（等价于 \`for(;;)\` 表示死循环）。说法正确。

3. **C 选项**：这是本题要找的错误答案。C++ 是自由格式语言，编译器不依赖缩进来识别代码块。即使代码缩进整齐，如果没有用花括号 \`{}\` 括起来，只有紧跟在循环关键字后的**第一条语句**属于循环体。缩进仅用于提高人类可读性，不具备语法约束力。

4. **D 选项**：C++ 拥有丰富的运算符和明确的优先级规则，优先级不仅包含基本的先乘除后加减，还包括逻辑运算、位运算、三目运算等多种优先级层次。说法正确。

**选项逐项分析：**
- **A (if 的判断条件必须放在小括号中)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（说法正确）。C++ 语法强制要求 if 后跟圆括号，这是正确的语言规则。
- **B (for 语句中间循环条件可省略，表示恒为真)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（说法正确）。\`for(;;)\` 是合法的死循环写法，中间条件省略默认为 true。
- **C (循环体有多条语句时用缩进即可消除二义性)**：✅ 正确（该说法错误）。C++ 不使用缩进识别代码块。若循环体包含多条语句，必须使用 \`{ }\` 将其括起来构成复合语句；否则只有紧随其后的第一条语句属于循环体，其余语句将成为循环体外的独立语句。
- **D (运算符有优先级，不仅有先乘除后加减)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（说法正确）。C++ 运算符优先级包含多个层次，例如逻辑非 (!) 高于乘除 (* / %)，乘除高于加减 (+ -)。

**易错提醒：**
- 本题是"反向选择题"，要找的是"不正确的叙述"。审题时务必注意题目中的否定词"不"。
- C++ 中缩进仅是代码风格约定，不具备语法意义。许多初学者误以为只要缩进对齐，多条语句就会自动成为循环体，这是常见错误。

**知识延伸：**
C++ 代码块必须使用 \`{ }\` 明确定义，例如：
\`\`\`cpp
// 错误写法：只有 a++ 属于循环体
while (cond)
    a++;
    b++;  // 这条语句不在循环内

// 正确写法
while (cond) {
    a++;
    b++;  // 两条语句都在循环内
}
\`\`\`

**考点：** C++ 基本语法规则、代码块的界定（花括号 vs 缩进）`,
            tags: ['循环', '基础语法']
        },
        {
            id: 5,
            type: 'single',
            question: `以下哪个是 C++ 语言关键字（ ）。`,
            options: ['main', 'max', 'double', 'sqrt'],
            answer: 2,
            score: 2,
            explanation: `**答案：C (double)**

**核心解析：**
C++ 语言关键字是编译器保留的、有特殊语法的、不能再用作标识符的单词。判断一个词是否为关键字，需要看它是否由 C++ 标准规定为保留词。

逐项分析：
- **main**：程序入口函数名，但它是预定义的标识符，不是编译器保留的关键字。
- **max**：标准库 \`<algorithm>\` 中的函数名或模板名，可以作为变量名使用。
- **double**：C++ 内置关键字，表示双精度浮点数类型，编译器将其保留。
- **sqrt**：标准库 \`<cmath>\` 中的数学函数名，可以作为标识符使用。

**选项逐项分析：**
- **A (main)**：❌ 错误。\`main\` 是程序的入口函数名称，由程序员在编写程序时必须使用，但它是预定义的标识符而非关键字。关键字用于表达语法结构，而 \`main\` 只是一个约定俗成的函数名。
- **B (max)**：❌ 错误。\`max\` 是标准库 \`<algorithm>\` 中定义的函数模板，可以被正常覆盖或用作变量名（如 \`int max = 10;\`），不属于 C++ 关键字。
- **C (double)**：✅ 正确。\`double\` 是 C++ 的基础数据类型关键字，表示双精度浮点数类型，属于编译器保留的 48 个（截至 C++20）关键字之一。
- **D (sqrt)**：❌ 错误。\`sqrt\` 是标准库 \`<cmath>\` 中定义的数学函数（平方根函数），属于库函数名而非语言关键字，可以被正常覆盖。

**易错提醒：**
- 容易将 \`main\` 误认为关键字，因为它在所有程序中都出现，但实际上 \`main\` 只是具有特殊用途的标识符，不是 C++ 语言语法的组成部分。
- 标准库函数名（如 \`max\`、\`sqrt\`、\`printf\`）和关键字的区别在于：关键字是语言语法的一部分，而库函数只是标准库提供的工具。

**知识延伸：**
C++ 关键字（Keywords）列表包含：int、float、double、char、bool、void、return、if、else、while、for、do、switch、case、break、continue、struct、class、public、private、protected、new、delete、try、catch、throw、const、static、sizeof 等。这些词在编辑器中通常会高亮显示。

**考点：** C++ 关键字与标识符的区分`,
            tags: ['变量与标识符']
        },
        {
            id: 6,
            type: 'single',
            question: `以下哪个不是 C++ 语言运算符（ ）。`,
            options: ['>=', '/=', '||', '<>'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (<>)**

**核心解析：**
本题考察 C++ 语言标准运算符的识别能力。需要逐一判断四个选项是否为 C++ 合法的运算符。

1. **>=**：关系运算符，用于判断"大于等于"，例如 \`if (a >= b)\`，在 C++ 中完全合法。
2. **/=**：复合赋值运算符，相当于 \`a = a / b\`，先除后赋值的简写形式，C++ 标准支持。
3. **||**：逻辑或运算符，用于布尔表达式，例如 \`if (a > 0 || b > 0)\`，C++ 标准支持。
4. **<>**：此符号在 C++ 中并非运算符。虽然在某些编程语言（如 Pascal、Python 早期版本）中 \`<>\` 表示"不等于"，但在 C++ 中不等于运算符是 \`!=\`。\`<>\` 在 C++ 里仅作为模板参数的界定符（如 \`vector<int>\`），而非运算符。

因此，选项 D 的 \`<>\` 不是 C++ 语言运算符。

**选项逐项分析：**
- **A (>=)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。合法。C++ 关系运算符，用于"大于等于"比较。
- **B (/=)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。合法。C++ 复合赋值运算符，执行除法并赋值。
- **C (||)**：❌ 错误。该项本身符合规则，因此不是本题要选的项。合法。C++ 逻辑或运算符，用于连接布尔条件。
- **D (<>)**：✅ 正确。该项不符合题目限定，正是本题要选的项。不是 C++ 运算符。在 C++ 中不等于应使用 \`!=\`，\`<>\` 仅在模板语法中作为尖括号使用。

**易错提醒：**
- 部分学员可能因其他语言经验（如 SQL、Pascal）误以为 \`<>\` 是"不等于"，但本题明确限定为 C++ 语言。
- C++ 中不等于的唯一合法写法是 \`!=\`，两者不可混淆。

**知识延伸：**
C++ 的比较运算符包括 \`==\`（等于）、\`!=\`（不等于）、\`>\`、\`<\`、\`>=\`、\`<=\`，其中"不等于"没有 \`<>\` 这种写法。

**考点：** C++ 标准运算符集`,
            tags: ['运算符']
        },
        {
            id: 7,
            type: 'single',
            question: `若 a 为 int、b 为 char，下列哪个语句不符合 C++ 语法（ ）。`,
            options: ['a = a+1.0;', 'a = (int)(b-\'0\');', 'b = (char)(a+\'0\');', '(int)b = a;'],
            answer: 3,
            score: 2,
            explanation: `**答案：D ((int)b = a;)**

**核心解析：**
本题考察强制类型转换的结果性质以及左值/右值的概念。

逐行分析各选项的语法合法性：

**A \`a = a+1.0;\`**
- \`a\` 为 int，\`a+1.0\` 产生 double 类型。
- C++ 允许将 double 隐式转换为 int（虽然会丢失小数部分），属于合法的隐式类型转换。
- 语法正确，可以编译执行。

**B \`a = (int)(b-'0');\`**
- \`b\` 为 char，\`b-'0'\` 计算字符对应的数字差值（int 类型）。
- 整个表达式强制转为 int 后赋值给 \`a\`，语法完全合法。
- 这是字符转数字的常用写法，例如将字符 '5' 转为数值 5。

**C \`b = (char)(a+'0');\`**
- \`a\` 为 int，\`a+'0'\` 产生 int 结果（字符 '0' 的 ASCII 码 48 加上数值）。
- 强制转为 char 后赋值给 \`b\`，语法合法。
- 这是数字转字符的常用写法，例如将数值 5 转为字符 '5'。

**D \`(int)b = a;\`**
- \`(int)b\` 是将 char 变量 \`b\` 的值取出，转换为 int 类型的**临时结果**（右值）。
- 右值是计算过程中产生的临时值，没有对应的内存地址，无法被赋值。
- 赋值运算符 \`=\` 的左侧必须是可写的内存位置（左值），临时量不能出现在左侧。
- 因此此语句违反 C++ 语法规则，无法编译。

**选项逐项分析：**
- **A (a = a+1.0;)**：❌ 错误。语法正确。隐式类型转换允许 int 接收 double 值（截断小数部分）。
- **B (a = (int)(b-'0');)**：❌ 错误。语法正确。将字符数字转换为对应数值，合法。
- **C (b = (char)(a+'0');)**：❌ 错误。语法正确。将数值转换为对应数字字符，合法。
- **D ((int)b = a;)**：✅ 正确。语法错误。\`(int)b\` 是右值（临时量），不能作为赋值目标。

**易错提醒：**
- 强制类型转换的结果是一个临时值（右侧值），永远不能被赋值。赋值号左边必须是具名的、可写的存储位置。
- 区分"类型转换合法"和"赋值语句合法"：B 和 C 转换本身合法且赋值也合法；D 转换形式合法但赋值操作非法。

**知识延伸：**
左值（lvalue）指可以取地址的表达式，可以出现在赋值左边或作为 \`const\` 引用绑定对象；右值（rvalue）是临时结果，只能出现在赋值右边。C++11 引入了右值引用（\`&&\`），允许延长临时对象生命周期，但本题不涉及。

**考点：** 左值与右值概念、强制类型转换的限制`,
            tags: ['变量与标识符', '运算符']
        },
        {
            id: 8,
            type: 'single',
            question: `用 int 变量 a、b 表示平行四边形边长，int 变量 h 表示 a 边对应高，以下哪个表达式不能用于计算 b 边对应高（ ）。`,
            options: ['a / b * (0.0+h)', '(0.0+a * h) / b', 'a * h / (b+0.0)', '(a+0.0) * h / b'],
            answer: 0,
            score: 2,
            explanation: `**答案：A (a / b * (0.0+h))**

**核心解析：**
本题考察平行四边形面积公式及混合运算中的类型提升规则。平行四边形面积 = 底 × 对应高，两条边的高可以互相计算：已知 a 边和其高 h，面积 = a × h，则 b 边对应的高 = (a × h) / b。

逐项分析四个表达式的计算正确性：

**A \`a / b * (0.0+h)\`**
- \`a\` 和 \`b\` 都是 int，先执行 \`a / b\`，这是**整数除法**。
- 若 a < b，整数除法结果为 0，导致后续乘以任何数仍为 0。
- 即使 a > b，整数除法也会截断小数部分，产生精度损失。
- 该表达式**不能正确计算** b 边对应高。

**B \`(0.0+a * h) / b\`**
- \`0.0 + a * h\`：先计算 \`a * h\`（int），结果加上 0.0 立即提升为 double。
- 后续除以 \`b\`（int）时，b 会自动提升为 double，执行浮点除法。
- 计算正确，结果为浮点数。

**C \`a * h / (b+0.0)\`**
- \`a * h\` 先计算（int × int = int）。
- \`(b+0.0)\` 将 b 提升为 double。
- int 结果除以 double 时被提升为 double，执行浮点除法。
- 计算正确，结果为浮点数。

**D \`(a+0.0) * h / b\`**
- \`a + 0.0\` 将 a 提升为 double。
- 后续所有运算（乘以 h、除以 b）都会自动提升为 double。
- 计算正确，结果为浮点数。

**选项逐项分析：**
- **A (a / b * (0.0+h))**：✅ 该式计算**错误**。整数除法 \`a / b\` 先执行，若 a < b 则结果为 0，导致最终答案错误。
- **B ((0.0+a * h) / b)**：❌ 该式计算**正确**。通过 \`0.0+...\` 触发浮点运算，保证精度。
- **C (a * h / (b+0.0))**：❌ 该式计算**正确**。分母加 0.0 确保浮点除法。
- **D ((a+0.0) * h / b)**：❌ 该式计算**正确**。分子加 0.0 确保浮点运算。

**易错提醒：**
- 运算符优先级：\`/\` 和 \`*\` 同级，从左到右结合，所以 \`a / b * (0.0+h)\` 等价于 \`(a / b) * (0.0+h)\`，不是 \`a / (b * (0.0+h))\`。
- 加 0.0 的位置很重要：放在分母使分母变浮点数即可，放在分子则整个过程都变成浮点，但放在乘积之后（如 A 选项）整数除法已经发生，无法补救。

**知识延伸：**
C++ 中混合类型运算遵循"提升"规则：int 与 double 运算时，int 提升为 double。防止整数除法的常用技巧包括：乘以 1.0、加 0.0、或使用 \`static_cast<double>()\`。

**考点：** 混合运算中的类型提升规则、整数除法的精度损失陷阱`,
            tags: ['运算符', '程序分析']
        },
        {
            id: 9,
            type: 'single',
            question: `以下哪个循环语句会无限次执行（ ）。`,
            options: ['for (int a = 0; a; a++) ;', 'for (bool b = false; b <= true; b++) ;', 'for (char c = \'A\'; c < \'z\'; c++) ;', 'for (double d = 0.0; d < 10.0; d += 0.001) ;'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (for (bool b = false; b <= true; b++) ;)**

**核心解析：**
本题考察循环条件的终止判定，需要分析每个 for 循环的三个表达式及迭代变量特性。

**A \`for (int a = 0; a; a++) ;\`**
- 初始值 \`a = 0\`，条件 \`a\` 为 0（false）。
- 条件判断在第一次执行前就已失败，循环体**一次也不执行**。
- 不形成死循环。

**B \`for (bool b = false; b <= true; b++) ;\`**
- 初始值 \`b = false\`（0）。
- 条件 \`b <= true\`：false 对应 0，true 对应 1。0 <= 1 为真，进入循环。
- 循环体执行后执行 \`b++\`：false 变为 true。
- 再次判断：1 <= 1 为真，继续循环。
- **关键**：\`bool\` 类型只有 0 和 1 两个值。对 true 再执行 \`++\`，在大多数编译器实现中，true（1）自增后仍为 1（bool 无法表示 2）。
- 因此 \`b <= true\` 恒为真，**形成死循环**。

**C \`for (char c = 'A'; c < 'z'; c++) ;\`**
- 初始值 \`c = 'A'\`（ASCII 65）。
- 条件 \`c < 'z'\`（ASCII 122），每次迭代 c++。
- 'A' 到 'z' 之间包含所有大写字母（A-Z, 91-96）、小写字母（a-z, 97-122）及其他字符。
- 最终 c 会超过 'z'，条件变为 false，循环终止。
- 不形成死循环。

**D \`for (double d = 0.0; d < 10.0; d += 0.001) ;\`**
- 从 0.0 开始，每次增加 0.001。
- 虽然浮点数存在精度误差（如 0.001 无法精确表示），但累加足够次数后（10000 次），d 将超过 10.0。
- 循环会正常终止，不形成死循环。

**选项逐项分析：**
- **A (for (int a = 0; a; a++) ;)**：❌ 错误。不会死循环。初始条件 a=0 即为 false，循环体不执行。
- **B (for (bool b = false; b <= true; b++) ;)**：✅ 正确。死循环。bool 自增后仍为 1，条件始终满足。
- **C (for (char c = 'A'; c < 'z'; c++) ;)**：❌ 错误。不会死循环。char 逐次递增，最终会超过 'z'。
- **D (for (double d = 0.0; d < 10.0; d += 0.001) ;)**：❌ 错误。不会死循环。d 最终会累加超过 10.0。

**易错提醒：**
- \`bool\` 类型的自增行为特殊：++ 操作会将 false 变 true，但 true 再 ++ 通常仍为 true（实现相关，C++ 标准未规定）。
- 循环条件应避免使用 bool 变量与常量比较，尽量使用能可靠变化的整型或浮点型控制变量。

**知识延伸：**
C++ 中 \`bool\` 类型的字面值为 \`true\`（1）和 \`false\`（0）。对 bool 执行 \`++\` 运算符时，false 变为 1，true 变为 2（在某些实现中），但输出或比较时仍按布尔规则处理。循环控制推荐使用 int 或 double 类型。

**考点：** bool 类型的自增特性、循环终止条件的精确性判定`,
            tags: ['循环', '程序分析']
        },
        {
            id: 10,
            type: 'single',
            question: `若 char 变量 a 的值为 'C'（ASCII 67），执行 cout << (a+2); 输出（ ）。`,
            options: ['E', 'C+2', 'C2', '69'],
            answer: 3,
            score: 2,
            explanation: `**答案：D (69)**

**核心解析：**
本题考察 char 类型与 int 混合运算时的自动类型提升规则，以及 cout 对不同类型的输出行为。

**逐行推导变量状态：**
1. \`char a = 'C';\` — a 存储字符 'C'，其 ASCII 码值为 67。
2. 表达式 \`a + 2\`：\`a\` 为 char 类型，2 为 int 类型。
3. 根据 C++ 类型提升规则：char 与 int 进行算术运算时，char 自动提升为 int。
4. 提升过程：a 的值 67（ASCII 码）参与运算，表达式变为 \`67 + 2 = 69\`，结果类型为 **int**（值 69）。
5. \`cout << (a+2)\` 输出 int 类型的值 69，因此屏幕上显示 **69**。

**输出格式分析：**
- 若要输出字符 'E'，需要将结果强制转为 char 并以字符形式输出。
- 本题直接输出表达式 \`(a+2)\` 的值，该值为 int 类型。
- cout 对 int 类型默认以数值形式输出，而非字符形式。

**选项逐项分析：**
- **A (E)**：❌ 错误。虽然逻辑上 'C'+2 对应字符 'E'，但表达式结果是 int 类型 69，cout 会输出数值 69 而非字符。
- **B (C+2)**：❌ 错误。没有任何 C++ 语法支持这种输出格式。
- **C (C2)**：❌ 错误。没有任何 C++ 语法支持这种输出格式。
- **D (69)**：✅ 正确。char 提升为 int 后参与运算，结果为 int，cout 输出数值 69。

**易错提醒：**
- 区分"值"和"字符表示"：'C' 的 ASCII 码是 67，加 2 得 69，这是一个数值，可以解释为字符 'E'，但本身是 int 类型。
- cout 的输出行为由**表达式的类型**决定，而非语义上的"应该输出什么"。
- 如果期望输出字符 'E'，应写 \`cout << char(a+2);\` 或 \`cout << (char)(a+2);\`。

**知识延伸：**
char 与 int 的混合运算遵循"整型提升"规则：char、short、bool、unsigned char 等小于 int 的类型，在算术运算中自动提升为 int 或 unsigned int（取两者能容纳原始值较大的那个）。这是 C++ 类型系统的基础规则。

**考点：** C++ 自动类型提升规则、cout 对不同类型数据的默认输出行为`,
            tags: ['输入输出', '运算符']
        },
        {
            id: 11,
            type: 'single',
            question: `若 a、b 均为 int，能正确判断“a 等于 1 且 b 等于 1”的表达式是（ ）。`,
            options: ['(a == b) && (b == 1)', '(a && b)', '(a == b == 1)', '(a * b == 1)'],
            answer: 0,
            score: 2,
            explanation: `**答案：A ((a == b) && (b == 1))**

**核心解析：**
1. 题目要求同时满足两个条件：a 等于 1 **且** b 等于 1。逻辑"且"对应的运算符是 \`&&\`。
2. 选项 A 的表达式 \`(a == b) && (b == 1)\` 拆解如下：
   - 先判断 \`a == b\`，即 a 与 b 相等；
   - 再判断 \`b == 1\`，即 b 等于 1；
   - 两个子表达式都用 \`&&\` 连接，必须**同时为真**，整个表达式才为真。
   - 若两个子表达式均为真，则 a = b = 1，严格满足题目要求。
3. 逐项分析其他选项的缺陷（见下）。

**选项逐项分析：**
- **A ((a == b) && (b == 1))**：✅ 正确。先确保 a 与 b 相等，再确保这个公共值为 1，等价于 a=1 且 b=1。
- **B ((a && b))**：❌ 错误。该表达式只判断 a 和 b 是否**都非零**。例如 a=2、b=3 时表达式为真，但并不满足 a=1 且 b=1。无法精确判断是否等于特定值 1。
- **C ((a == b == 1))**：❌ 错误。C++ 中关系运算符从左至右结合。先计算 \`a == b\`，得到 0 或 1；再与 1 比较。例如 a=2、b=2 时，\`a==b\` 为真（值为 1），\`1==1\` 为真，但 a 并不等于 1。
- **D ((a * b == 1))**：❌ 错误。该表达式要求 a 与 b 的乘积为 1。例如 a=-1、b=-1 时，乘积为 1，但 a 和 b 均不等于 1。无法保证各自等于 1。

**易错提醒：**
- 选项 C 是典型的"连等陷阱"，\`a == b == 1\` 并不等价于 \`a == 1 && b == 1\`。
- 选项 B 只判断"非零"，与"等于 1"是完全不同的概念，切勿混淆。

**知识延伸：**
逻辑与 \`&&\` 要求左右操作数均为真（值非零），而相等判断 \`==\` 用于精确比较两个值是否相等。在复合条件判断中，应先拆分条件再用 \`&&\` 连接，而非试图用一个表达式"投机取巧"。

**考点：** 逻辑与 \`&&\`、相等判断 \`==\` 的组合逻辑`,
            tags: ['条件判断', '运算符']
        },
        {
            id: 12,
            type: 'single',
            question: `若 a 为 char，哪个表达式可正确判断“a 是数字字符”（ ）。`,
            options: ['\'0\' <= a && a <= \'9\'', '\'1\' <= a && a <= \'0\'', '\'0\' <= a <= \'9\'', '\'1\' <= a <= \'0\''],
            answer: 0,
            score: 2,
            explanation: `**答案：A ('0' <= a && a <= '9')**

**核心解析：**
1. 在 C++ 中，\`char\` 类型本质上存储的是整数（字符的 ASCII 码）。字符 '0'～'9' 的 ASCII 码分别是 48～57，恰好构成连续整数区间。
2. 判断 a 是否为数字字符，等价于判断其 ASCII 码是否落在 48～57 区间，即 \`'0' <= a && a <= '9'\`。
3. 由于 \`&&\` 是短路求值，必须左侧 \`'0' <= a\` 为真，右侧 \`a <= '9'\` 才会被求值，两个条件必须**同时满足**。

**选项逐项分析：**
- **A ('0' <= a && a <= '9')**：✅ 正确。利用 ASCII 码连续性，拆分为两个闭区间边界判断，逻辑严密。
- **B ('1' <= a && a <= '0')**：❌ 错误。区间下限 '1' 大于上限 '0'，构成非法范围，无论 a 取何值都不可能同时满足两个条件。
- **C ('0' <= a <= '9')**：❌ 错误。这是典型的"连写比较陷阱"。C++ 从左至右结合：先计算 \`'0' <= a\`，得到布尔值 0 或 1；再与 '9' 比较。由于 0 或 1 均小于 '9' 的 ASCII 码 57，表达式**恒为真**，失去判断意义。
- **D ('1' <= a <= '0')**：❌ 错误。同选项 C 的连写陷阱问题，且区间本身非法，表达式同样恒为真。

**易错提醒：**
- 选项 C 和 D 看似"简洁"，实则是 C++ 语法中的经典陷阱。连写 \`x <= a <= y\` 不会按数学含义工作，而是先求布尔值再比较整数。
- 字符比较必须拆成两个独立比较并用 \`&&\` 连接，才能正确表达"在区间内"。

**知识延伸：**
字符在内存中以 ASCII 码（整数）存储，因此可以用关系运算符直接比较。常用字符的 ASCII 码值：'0'=48, '9'=57, 'A'=65, 'Z'=90, 'a'=97, 'z'=122。掌握这些有助于理解字符的大小比较。

**考点：** 字符的 ASCII 码表示、关系运算符的结合性、\`char\` 类型本质`,
            tags: ['条件判断', '运算符']
        },
        {
            id: 13,
            type: 'single',
            question: `在给定代码横线处填入（ ），使得最终输出字符是 9。\n\`\`\`cpp\nchar a = '3', b = '6';\ncout << ________;\n\`\`\``,
            options: ['(a+b)', '(a+b-\'0\')', '(char)(a+b)', '(char)(a+b-\'0\')'],
            answer: 3,
            score: 2,
            explanation: `**答案：D ((char)(a+b-'0'))**

**核心解析：**

1. 首先确定字符常量对应的 ASCII 码值：
   - \`char a = '3'\` 中，\`'3'\` 的 ASCII 码是 51
   - \`char b = '6'\` 中，\`'6'\` 的 ASCII 码是 54

2. 分析目标：要输出字符 \`'9'\`，需要让 \`cout\` 输出 ASCII 码为 57 的字符。

3. 逐步计算 \`(char)(a+b-'0')\`：
   - \`a + b\` = 51 + 54 = 105（int 类型，因为 char 相加自动提升为 int）
   - \`a + b - '0'\` = 105 - 48 = 57（\`'0'\` 的 ASCII 码是 48）
   - \`(char)(57)\` 将 int 值 57 强转为 char 类型
   - cout 遇到 char 类型时按字符输出，ASCII 57 对应字符 \`'9'\`

**选项逐项分析：**

- **A ((a+b))**：❌ 错误。表达式类型为 int，值为 105，cout 直接输出数字 105。

- **B ((a+b-'0'))**：❌ 错误。虽然计算结果 57 对应字符 '9' 的 ASCII 码，但表达式类型为 int，cout 会输出数字 57 而非字符。

- **C ((char)(a+b))**：❌ 错误。将 105 强转为 char，输出 ASCII 码 105 对应的字符 \`'i'\`，不是 \`'9'\`。

- **D ((char)(a+b-'0'))**：✅ 正确。先计算得到数值 57，再用 char 强转后输出，cout 按字符输出 ASCII 57，即 \`'9'\`。

**易错提醒：**
- char 类型相加会自动提升为 int，这是选项 B 的陷阱——即使值正确，类型仍是 int，cout 会输出数字而非字符。
- 强转 \`(char)\` 的作用是告诉 cout 按字符方式输出，而非改变数值本身。

**知识延伸：**
字符数字（'0'-'9'）与真实数值（0-9）的转换通过 \`-'0'\` 实现，这是 GESP 二级的高频考点。类似的，\`-'A'+'a'\` 可实现大小写转换。

**考点：** ASCII 码与字符转换、char 与 int 的类型提升、类型强转对 cout 输出行为的影响`,
            tags: ['程序分析', '运算符']
        },
        {
            id: 14,
            type: 'single',
            question: "在给定循环代码横线处填写（ ），可以使输出为 22。\n```cpp\nint ans = 0;\nfor (int i = 1; i <= 10; i++) {\n    if (________) ans += i;\n}\ncout << ans;\n```",
            options: ["i % 3 == 0", "20 % i == 0", "i <= 8", "i >= 18"],
            answer: 1,
            score: 2,
            explanation: "**答案：B (20 % i == 0)**\n\n**核心解析：**\n循环中 `i` 从 1 到 10。若填写 `20 % i == 0`，表示筛选 20 的因数，在这个范围内满足条件的是 1、2、4、5、10。\n\n累加结果为：`1 + 2 + 4 + 5 + 10 = 22`。\n\n**选项逐项分析：**\n- **A**：满足条件的是 3、6、9，和为 18。\n- **B**：满足条件的是 1、2、4、5、10，和为 22。\n- **C**：满足条件的是 1 到 8，和为 36。\n- **D**：循环范围只有 1 到 10，条件永远不成立，和为 0。\n\n**考点：** 循环累加、取模判断因数",
            tags: ["循环", "运算符"]
        },
        {
            id: 15,
            type: 'single',
            question: `执行给定 C++ 程序后，输出结果是（ ）。\n\`\`\`cpp\nint a = 5, b = 2;\nif (a > b) a = b;\nelse b = a;\ncout << a << b;\n\`\`\``,
            options: ['52', '22', '25', '55'],
            answer: 1,
            score: 2,
            explanation: `**答案：B (22)**

**核心解析：**
1. 初始化阶段：\`int a = 5, b = 2;\` 执行后，变量的初始值为 a=5, b=2。
2. 条件判断：\`if (a > b)\` 即判断 \`5 > 2\`，结果为真（true）。
3. 分支执行：由于条件为真，执行 \`if\` 内的语句 \`a = b;\`，将 b 的值（2）赋给 a，此时 a 的值变为 2，b 保持不变仍为 2。
4. \`else\` 分支被跳过，不执行任何操作。
5. 输出语句 \`cout << a << b;\` 将变量按顺序输出，由于 << 是连续输出字符，结果为 "22"。

**选项逐项分析：**
- **A (52)**：❌ 错误。该选项是未执行分支语句前的初始值 5 和 2，但程序中 \`if\` 条件为真导致 \`a = b\` 执行，a 的值被覆盖为 2。
- **B (22)**：✅ 正确。分析过程见核心解析，输出结果为 "22"。
- **C (25)**：❌ 错误。该选项可能是混淆了分支逻辑，误以为执行了 \`else\` 分支或进行了其他运算。实际上 \`if\` 条件为真，应执行 if 分支而非 else 分支。
- **D (55)**：❌ 错误。该选项既不符合初始值也不符合执行分支后的结果，在逻辑上不成立。

**易错提醒：**
- 易错点1：未仔细判断条件真假就随意选择分支。本题 \`5 > 2\` 明显为真，必须执行 \`if\` 分支而非 \`else\` 分支。
- 易错点2：混淆赋值语句与比较语句。\`a = b\` 是将 b 的值赋给 a，而不是判断 a 与 b 是否相等。

**知识延伸：**
在 \`if-else\` 结构中，条件判断为真时执行 if 后的语句块，条件为假时执行 else 后的语句块，两者互斥。赋值操作会修改变量原值，后续输出应以修改后的值为准。

**考点：** if-else 分支结构、变量赋值、关系运算`,
            tags: ['程序分析']
        },
        {
            id: 16,
            type: 'judge',
            question: `内存 64KB 的 CEC-I 若存放 GB2312 的 6763 个汉字（每字 2 字节），占用不超过 1/5 内存。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
1. 计算汉字总占用空间：6763 个汉字 × 每字 2 字节 = 13526 字节。
2. 计算 64KB 对应的字节数：64KB = 64 × 1024 = 65536 字节。
3. 计算实际占用比例：13526 ÷ 65536 ≈ 0.2064，即约 20.64%。
4. 比较与 1/5 的大小：1/5 = 0.2，0.2064 > 0.2。
5. 结论：占用比例约为 20.64%，超过 1/5（20%），因此“占用不超过 1/5”的说法是错误的。

**易错提醒：**
- 计算 KB 转换为字节时容易忘记乘以 1024，直接用 64 进行比较导致错误。
- 1/5 = 0.2 是 20%，而 13526/65536 ≈ 20.64%，略大于 20%，容易被忽略。

**知识延伸：**
计算机存储容量换算中，1KB = 1024 字节（B），1MB = 1024KB。考试中若给出具体字节数，需先统一单位再进行比例计算。

**考点：** 计算机存储容量单位换算、比例计算`,
            tags: ['计算机基础']
        },
        {
            id: 17,
            type: 'judge',
            question: `域名 gesp.ccf.org.cn 的顶级域名是 gesp。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
域名的层级结构从右向左依次读取：
- \`gesp.ccf.org.cn\` 包含四个部分，用"."分隔。
- 最右端的 \`cn\` 是国家顶级域名（ccTLD），代表中国。
- 从右向左数：\`cn\` 为顶级域名，\`org.cn\` 为二级域名，\`ccf.org.cn\` 为三级域名，\`gesp\` 为四级域名（即主机名或子域名）。

因此，\`gesp\` 不是顶级域名，它只是该完整域名中最左侧的主机标识部分。

**易错提醒：**
- 容易误认为最左端的 \`gesp\` 是顶级域名，需记住域名从右向左读，最右端才是顶级域名。
- 顶级域名分为国家顶级域名（.cn、.us 等）和通用顶级域名（.com、.org、.net 等）。

**知识延伸：**
DNS 域名的层级结构形如树状，顶级域名（TLD）位于根部。常见顶级域名包括 .com（商业）、.org（非营利组织）、.edu（教育机构）、.gov（政府）等。

**考点：** 域名系统（DNS）层级结构、顶级域名的识别`,
            tags: ['网络基础']
        },
        {
            id: 18,
            type: 'judge',
            question: `在 C++ 中，使用 sqrt、abs 等数学函数前需包含 <cmath> 或 <math.h>。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
C++ 标准库中的所有函数必须在相应的头文件中声明后才能被程序使用。具体而言：
- \`sqrt(x)\`：计算 x 的平方根，声明于 \`<cmath>\`（C++ 风格）或 \`<math.h>\`（C 风格兼容头文件）。
- \`abs(x)\`：计算绝对值，整数版本在 \`<cstdlib>\` 中声明，浮点版本在 \`<cmath>\` 中声明。

使用这些数学函数前包含对应的头文件，是 C++ 程序的正确做法。若不包含，编译器将报错，提示函数未声明。

**易错提醒：**
- \`<cmath>\` 是 C++ 标准库头文件，\`<math.h>\` 是 C 语言兼容头文件，两者都可使用，但建议优先使用 \`<cmath>\`。
- 在旧式编译器或某些竞赛环境中，可能只需要包含 \`<cmath>\` 而不需要 using namespace std。

**知识延伸：**
C++ 标准库头文件通常不带扩展名（如 \`<iostream>\`、\`<vector>\`），而 C 语言的旧头文件带有 \`.h\` 后缀（如 \`<stdio.h>\`、\`<math.h>\`）。混用时应注意版本差异。

**考点：** C++ 标准头文件的使用、库函数的正确调用方式`,
            tags: ['基础语法']
        },
        {
            id: 19,
            type: 'judge',
            question: `在 C++ 中，下划线“_”既可用于标识符命名，也是运算符。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
下划线字符 \`_\` 在 C++ 中的语法功能仅限于以下场景：
1. 作为标识符（identifier）的组成部分，用于命名变量、函数、类、常量等。例如：\`int my_value;\`、\`void _helper(){}\`。
2. 单独的下划线 \`_\` 是合法标识符。

下划线本身**不是运算符**，不具备任何运算功能。C++ 中的运算符包括算术运算符（+、-、*、/、%）、关系运算符（<、>、==、!=）、逻辑运算符（&&、||、!）、赋值运算符（=、+=、-=）等，下划线不在其中。

**易错提醒：**
- C++ 中没有名为"下划线运算符"的概念，切勿将标识符组成规则与运算符混淆。
- 虽然 \`_\` 可用于标识符，但单独的下划线 \`_\` 是 C++ 中的特殊标识符，通常保留给实现使用，建议避免在代码中大量使用。

**知识延伸：**
标识符命名规则：可由字母、数字和下划线组成，且不能以数字开头。C++ 标识符区分大小写，关键字不能作为标识符使用。

**考点：** C++ 标识符组成规则、运算符与标识符的区别`,
            tags: ['基础语法']
        },
        {
            id: 20,
            type: 'judge',
            question: `若 a 为 double 且 a=3.5，则 a*10 结果是 35 且类型为 int。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
分析 \`a * 10\` 的类型和值：
1. 变量 \`a\` 的类型为 \`double\`，值为 3.5。
2. 字面量 \`10\` 的类型为 \`int\`。
3. 在 C++ 混合运算中，当两个不同类型的操作数进行运算时，精度较低的类型会自动向精度较高的类型提升（隐式类型转换）。
4. 因此 \`10\`（int）会被提升为 \`10.0\`（double），再与 \`a\`（double）进行运算。
5. 结果为 \`3.5 * 10.0 = 35.0\`，类型为 \`double\`，而非 \`int\`。

**易错提醒：**
- 不能仅因为结果数值"看起来是整数"就认为类型是 int，类型由变量本身的类型和运算规则决定。
- \`double\` 与 \`int\` 运算的结果类型是 \`double\`，而非 \`int\`。

**知识延伸：**
C++ 类型提升规则：char、short → int → long → float → double → long double。低类型向高类型转换，防止精度损失。若需将结果转为整数，应使用显式类型转换（强制类型转换）或 \`int()\` 函数。

**考点：** 算术运算中的隐式类型提升规则、double 与 int 混合运算的结果类型`,
            tags: ['数据类型']
        },
        {
            id: 21,
            type: 'judge',
            question: `if 语句中，若条件成立需执行多条语句，可用花括号把这些语句括起来。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
在 C++ 中，\`if\` 语句的条件部分只默认控制紧随其后的一条语句。如果需要在条件成立时执行多条语句，必须使用一对花括号 \`{}\` 将这些语句包裹起来，形成一个**复合语句**（也称代码块）。代码块内的所有语句在语法上被视为一条语句。例如：

\`\`\`cpp
if (x > 0) {
    a = 10;
    b = 20;
    cout << a + b;
}
\`\`\`

若不加花括号，只有 \`a = 10;\` 会受 \`if\` 控制，其余语句会"逃逸"到 \`if\` 之外，导致逻辑错误。

**易错提醒：**
- 初学者常忽略花括号，导致只有第一条语句受 \`if\` 控制
- 即使只有一条语句，加花括号也是良好编程习惯，可提高可读性

**知识延伸：**
\`if\`、\`for\`、\`while\` 等控制结构都遵循这一原则——默认只控制一条语句，需要多条时必须用 \`{}\` 构成复合语句。

**考点：** 代码块的作用、控制结构的执行范围`,
            tags: ['基础语法']
        },
        {
            id: 22,
            type: 'judge',
            question: `循环语句的循环体有可能无限制执行下去。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
循环语句能否无限制执行下去，取决于循环条件是否永远为真。常见的死循环情形包括：
- \`while(true)\` 或 \`for(;;)\`：条件始终为真
- 循环条件表达式写错，如 \`while(i <= 10)\` 但 \`i\` 始终未递增
- 循环变量更新方向错误，如递减型变量误写成递增

当循环条件永不满足 "假" 时，循环体将反复执行，形成**死循环**，程序无法正常结束。

**易错提醒：**
- 死循环不一定是"错误"，有时是程序有意设计的（如服务器程序的事件循环）
- 在 GESP 考核中，若题目未说明是死循环，应默认循环会正常终止

**知识延伸：**
循环的三要素：初始条件、循环条件、循环变量的更新。任一环节出错都可能导致死循环。

**考点：** 循环终止条件的控制逻辑`,
            tags: ['循环结构']
        },
        {
            id: 23,
            type: 'judge',
            question: `++ 和 == 是 C++ 运算符，但 += 不是。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
题目断言"\`+=\` 不是 C++ 运算符"，这与事实相悖。\`+=\` 是 C++ 中典型的**复合赋值运算符**，其作用等同于 \`a = a + b\`，用于将右侧表达式结果与左侧变量相加后再赋值回该变量。例如：

\`\`\`cpp
int a = 5;
a += 3;  // 等价于 a = a + 3，执行后 a 的值为 8
\`\`\`

此外，C++ 中还存在 \`-=\`、\`*=\`、\`/=\`、\`%=\` 等同类复合赋值运算符，它们都是合法且常用的运算符。

**易错提醒：**
- 复合赋值运算符（如 \`+=\`）与关系运算符（如 \`==\`）是完全不同的概念
- \`+=\` 的优先级较低，低于算术运算符，但高于赋值运算符 \`=\`

**知识延伸：**
C++ 运算符可分为算术运算符、关系运算符、逻辑运算符、赋值运算符、位运算符等，\`+=\` 属于赋值运算符中的复合赋值运算符。

**考点：** 运算符分类、复合赋值运算符的识别与使用`,
            tags: ['运算符']
        },
        {
            id: 24,
            type: 'judge',
            question: `若 a 为 char 且值为大写字母 F，执行 a = a+1 后，a 变为 G。`,
            options: ['正确', '错误'],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
在 ASCII 编码体系中，所有英文字母（包括大小写）均按字母表顺序连续排列，且相邻字母的 ASCII 码值相差 1。'F' 的 ASCII 码值为 70，'G' 的 ASCII 码值为 71。执行表达式 \`a + 1\` 时，\`a\`（类型为 \`char\`）会提升为 \`int\` 进行算术运算，结果为 71，再隐式转换回 \`char\` 后存储。字符类型在参与算术运算时按其 ASCII 码值处理，因此 \`a\` 由 'F' 变为 'G'。

\`\`\`cpp
char a = 'F';  // a 的 ASCII 码值为 70
a = a + 1;     // 70 + 1 = 71，存回 a 后 a 的字符值为 'G'
\`\`\`

**易错提醒：**
- 只有大写字母之间、小写字母之间的转换才是连续、可预测的
- 大写 'A'(65) 与小写 'a'(97) 之间相差 32，并非紧邻

**知识延伸：**
字符型数据在 C++ 中可参与算术运算，本质上是利用字符对应的 ASCII 码值进行计算，这一特性常用于字符的大小写转换或字母序操作。

**考点：** 字符的算术运算原理、ASCII 码的顺序性`,
            tags: ['数据类型']
        },
        {
            id: 25,
            type: 'judge',
            question: `表达式 sqrt(9.0) 的结果为 3，且结果类型为 int。`,
            options: ['正确', '错误'],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
\`sqrt()\` 是 C++ 标准库 \`<cmath>\` 中的数学函数，用于计算平方根。其函数原型为：

\`\`\`cpp
double sqrt(double x);
\`\`\`

这意味着 \`sqrt()\` 的返回值类型始终是 \`double\`（双精度浮点数），而不是 \`int\`。即使 \`sqrt(9.0)\` 的数学结果在数值上等于整数 3，但在程序内部它表示为 \`double\` 类型的 \`3.0\`，两者在类型上有本质区别。

\`\`\`cpp
double result = sqrt(9.0);  // result 的类型是 double，值为 3.0
\`\`\`

如果将 \`sqrt()\` 的返回值直接赋给 \`int\` 变量，会发生隐式类型转换（小数部分被截断)，但返回值本身的类型仍然是 \`double\`。

**易错提醒：**
- 不能仅凭数学结果想当然地判断程序中的类型
- C++ 是强类型语言，函数返回值的类型由函数定义决定，不由数学结果决定

**知识延伸：**
\`<cmath>\` 中的数学函数（如 \`sqrt\`、\`sin\`、\`cos\`、\`abs\` 等）多数返回 \`double\` 类型。使用时需注意类型匹配，避免隐式转换带来的精度损失或意外结果。

**考点：** 库函数返回值类型判定、浮点数与整数的类型区别`,
            tags: ['数据类型']
        },
    ],
    programmingQuestions: [
        {
            id: 26,
            type: 'programming',
            title: "找素数",
            samples: [
                {
                    input: `2 10`,
                    output: `4`
                },
                {
                    input: `98 100`,
                    output: `0`
                }
            ],
            question: `
# [GESP202306 二级] 找素数

## 题目描述

小明刚刚学习了素数的概念：如果一个大于 $1$ 的正整数，除了 $1$ 和它自身外，不能被其他正整数整除，则这个正整数是素数。现在，小明想找到两个正整数 $A$ 和 $B$ 之间（包括 $A$ 和 $B$）有多少个素数。

## 输入格式

输入只有一行两个正整数 $A, B$。约定 $2 \\le A \\le B \\le 1000$。

## 输出格式

输出一行，包含一个整数 $C$，表示找到 $C$ 个素数。
`,
            score: 25,
            explanation: `**解析：**
本题考察素数判定算法。
1. **素数定义**：只能被 1 和自身整除的且大于 1 的正整数。
2. **判定方法**：
   - 对于待检测数 $x$，尝试从 2 循环到 $\sqrt{x}$。
   - 如果 $x$ 能被其中任何一个数整除，则不是素数。
3. **遍历计数**：
   - 使用 \`for\` 循环从 $A$ 遍历到 $B$。
   - 对每个数调用素数判定逻辑，如果为真则计数器 \`ans++\`。

**核心代码提示：**
\`\`\`cpp
bool isPrime(int x) {
    if (x < 2) return false;
    for (int i = 2; i * i <= x; i++) {
        if (x % i == 0) return false;
    }
    return true;
}
\`\`\``,
            tags: ["编程题", "枚举", "素数"],
            template: "#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    int a, b;\n    cin >> a >> b;\n    // \n    return 0;\n}",
            referenceCode: "#include <bits/stdc++.h>\nusing namespace std;\n\nbool isPrime(int x) {\n    if (x < 2) return false;\n    for (int d = 2; d * d <= x; ++d) {\n        if (x % d == 0) return false;\n    }\n    return true;\n}\n\nint main() {\n    int A, B;\n    cin >> A >> B;\n    int ans = 0;\n    for (int x = A; x <= B; ++x) if (isPrime(x)) ++ans;\n    cout << ans << endl;\n    return 0;\n}",
            answer: '',
        },
        {
            id: 27,
            type: 'programming',
            title: "自幂数判断",
            samples: [
                {
                    input: `3\n152\n111\n153`,
                    output: `F\nF\nT`
                }
            ],
            question: `
# [GESP202306 二级] 自幂数判断

## 题目描述

自幂数是指，一个 $N$ 位数，满足各位数字 $N$ 次方之和是本身。
例如，$153$ 是 $3$ 位数，$1^3+5^3+3^3=153$，因此 $153$ 是自幂数。

## 输入格式

第一行是一个正整数 $M$。之后 $M$ 行，每行一个待判断的正整数。

## 输出格式

输出 $M$ 行，如果是自幂数输出 T，否则输出 F。
`,
            score: 25,
            explanation: `**解析：**
本题考察数位拆解与幂运算的应用。
1. **确定位数**：
   - 使用一个临时变量拷贝原数，通过反复除以 10 计算其位数 $N$。
2. **拆分与累加**：
   - 再次通过 \`% 10\` 和 \`/ 10\` 拆出每一位。
   - 对每一位计算其 $N$ 次方（建议手写循环或使用 \`pow\` 函数，注意 \`pow\` 返回浮点数需处理精度）。
   - 将结果累加。
3. **判定**：
   - 比较累加和与原数是否相等。

**核心逻辑提示：**
\`\`\`cpp
int countDigits(long long x) {
    int cnt = 0;
    while(x) { cnt++; x /= 10; }
    return cnt;
}
long long myPow(int a, int b) {
    long long res = 1;
    for(int i=0; i<b; i++) res *= a;
    return res;
}
\`\`\``,
            tags: ["编程题", "模拟", "数位分解"],
            template: "#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    int m;\n    cin >> m;\n    // \n    return 0;\n}",
            referenceCode: "#include <bits/stdc++.h>\nusing namespace std;\n\nlong long ipow(long long a, int b) {\n    long long r = 1;\n    while (b--) r *= a;\n    return r;\n}\n\nbool ok(long long x) {\n    if (x == 0) return true;\n    int len = 0;\n    long long t = x;\n    while (t > 0) { len++; t /= 10; }\n    long long sum = 0; t = x;\n    while (t > 0) {\n        sum += ipow(t % 10, len);\n        t /= 10;\n    }\n    return sum == x;\n}\n\nint main() {\n    int M;\n    cin >> M;\n    while (M--) {\n        long long x;\n        cin >> x;\n        cout << (ok(x) ? 'T' : 'F') << endl;\n    }\n    return 0;\n}",
            answer: '',
        }
    ]
};
