// 2023年3月 GESP C++ 二级真题 (第1次认证)
export const paperData = {
    id: '2023-03-l2',
    title: '2023年3月 GESP C++ 二级真题',
    level: 2,
    year: 2023,
    month: 3,
    session: 1,
    note: '首次认证',
    timeLimit: 90 * 60,
    questions: [
        {
            id: 1,
            type: 'single',
            question: `以下存储器中的数据不会受到附近强磁场干扰的是（ ）。`,
            options: ["硬盘", "U 盘", "内存", "光盘"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (光盘)**

**核心解析：**
本题考察不同存储介质的物理存储原理。计算机中常见的存储介质按原理可分为磁存储、光存储和半导体存储三大类。

1. **磁存储**（硬盘）：利用磁头改变盘片表面磁粉的磁极方向来记录数据。强磁场会直接磁化或消磁盘片表面，导致数据永久性损坏。

2. **半导体存储**（U盘）：本质是 Flash ROM 芯片，内部由大量浮栅晶体管构成。强磁场可能通过电磁感应干扰芯片内部电路的正常工作。

3. **半导体存储**（内存）：同样属于半导体存储，其数据存储依赖电容充放电或晶体管状态。强磁场会影响电子流动方向，造成数据位翻转（单粒子翻转效应）。

4. **光存储**（光盘）：数据记录在特殊的染料或金属记录层上，通过激光烧蚀形成物理凹坑来存储信息。这种物理结构完全不涉及磁性材料或半导体电荷，因此对磁场完全免疫。

**选项逐项分析：**
- **A 硬盘**：❌ 错误。该项本身正确（硬盘确实会受到强磁场干扰）。机械硬盘的盘片是涂有磁性颗粒的金属圆盘，磁头通过改变磁粉的磁化方向来记录数据，强磁场会直接破坏盘片上的磁记录，导致数据丢失。

- **B U 盘**：❌ 错误。该项本身正确（U盘也会受到强磁场干扰）。U盘内部是 Flash 存储芯片，强磁场产生的感应电流可能干扰甚至损坏其精密的 CMOS 电路。

- **C 内存**：❌ 错误。该项本身正确（内存同样会受到强磁场干扰）。内存条上的 DRAM 芯片依赖电容存储电荷，强磁场会干扰电荷的正常分布，可能导致内存数据出错或系统崩溃。

- **D 光盘**：✅ 正确。光盘是唯一采用光学原理存储数据的介质。数据以激光烧制的物理凹坑形式存在，读取时依赖光束的反射率变化。磁场对光存储的物理结构没有任何影响。

**易错提醒：**
- 部分考生误认为 U 盘是“纯电子”器件而不会受磁场影响，但强磁场产生的感应电压足以损坏精密的集成电路。
- 固态硬盘（SSD）虽然也是半导体存储，但本题中的“内存”特指 RAM，不包括 ROM 和 Flash。

**知识延伸：**
在 GESP 二级范围内，还需区分易失性存储（RAM，断电后数据丢失）与非易失性存储（ROM、硬盘、光盘、U盘，断电后数据保留）。其中仅光存储对磁场免疫。

**考点：** 计算机存储介质的物理原理与特性`,
            tags: ['计算机基础']
        },
        {
            id: 2,
            type: 'single',
            question: `下列流程图，属于计算机的哪种程序结构？（ ）。`,
            options: ["顺序结构", "循环结构", "分支结构", "数据结构"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (分支结构)**

**核心解析：**
程序控制结构分为三种基本类型：顺序结构、循环结构和分支结构。本题需要根据流程图的基本符号特征来判断程序结构类型。

流程图的核心判断依据如下：

1. **顺序结构**：所有步骤按线性顺序排列，用箭头顺序连接，不存在判断框或返回箭头。

2. **循环结构**：必定包含从后续步骤指向前面步骤的返回路径，形成闭环，程序会重复执行某段代码。

3. **分支结构**：包含菱形判断框，根据判断条件（通常为“是/否”或“True/False”）分流到不同的执行路径。

4. **数据结构**：这是组织和管理数据的方式（如数组、链表、栈、队列），不是程序执行的控制结构，不能用来描述程序的运行流程。

**选项逐项分析：**
- **A 顺序结构**：❌ 错误。该项本身正确（顺序结构是存在的）。顺序结构的特征是程序从入口到出口依次执行每个处理框，没有任何判断框和分支路径。题目中若存在菱形判断框，则不可能是纯顺序结构。

- **B 循环结构**：❌ 错误。该项本身正确（循环结构是存在的）。循环结构的必要条件是存在返回箭头（从下方指向上方或回到循环入口）。若流程图中没有闭环结构，则不是循环结构。

- **C 分支结构**：✅ 正确。流程图中出现菱形判断框，并由此引出两条或多条执行路径，体现了“根据条件选择执行方向”的逻辑，这正是分支结构的本质特征。

- **D 数据结构**：❌ 错误。该项本身正确（数据结构是合法概念）。但本题问的是“程序结构”，即程序的控制流程结构。数据结构属于程序设计方法学范畴，与程序的三种基本控制结构（顺序、分支、循环）是不同层次的概念。

**易错提醒：**
- 循环结构必须同时满足“重复执行”和“返回路径”两个条件，缺一不可。
- 不能将“数据结构”与“程序结构”混淆，前者关注数据组织，后者关注执行流程。

**知识延伸：**
C++ 中对应三种基本结构的语句分别是：顺序结构对应普通语句和复合语句（{} 块）；分支结构对应 if-else 语句和 switch 语句；循环结构对应 while、do-while 和 for 语句。

**考点：** 流程图符号识别与程序三种基本控制结构的判断`,
            tags: ['流程图', '程序分析']
        },
        {
            id: 3,
            type: 'single',
            question: `下列关于 C++ 语言的叙述，不正确的是（ ）。`,
            options: ["double 类型的变量占用内存的大小是浮动的", "bool 类型的变量占用 1 字节内存", "int 类型变量的取值范围不是无限的", "char 类型的变量有 256 种取值"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (double 类型的变量占用内存的大小是浮动的)**

**核心解析：**
本题要求选出“不正确”的叙述。需逐一分析每个选项在 C++ 标准和主流实现下的正确性。

C++ 基本数据类型的内存占用遵循以下规范：

1. **double 类型**：在 C++ 标准中，double 是双精度浮点类型，主流编译器（GCC、MSVC、Clang）在 x86/x64、ARM 等常见架构上统一实现为 8 字节（64位）。这个大小是固定的，不随存储的数值大小变化。浮点数的大小由阶码和尾数位数决定，在特定平台上恒定。

2. **bool 类型**：C++ 标准规定 bool 类型占用至少 1 字节。实际实现中，在几乎所有平台上都是 1 字节，因为 CPU 最小寻址单位是字节。

3. **int 类型**：在 32 位系统上为 4 字节，在 64 位系统上根据编译器和平台可能为 4 字节或 8 字节，但无论哪种实现，其取值范围都是有限的，并非无限。

4. **char 类型**：固定占用 1 字节，即 8 位。8 位二进制能表示的取值总数为 2⁸ = 256 种，对应有符号 char 的 -128 到 127，或无符号 char 的 0 到 255。

**选项逐项分析：**
- **A double 类型的变量占用内存的大小是浮动的**：✅ 正确（该叙述**不正确**）。这是本题要找的错误选项。double 类型在特定编译环境和硬件平台上占用空间固定不变，通常为 8 字节。“浮动”一说完全违背了 C++ 数据类型的基本规范。

- **B bool 类型的变量占用 1 字节内存**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（该叙述**正确**）。bool 类型在所有主流平台上均占用 1 字节，该叙述准确无误。

- **C int 类型变量的取值范围不是无限的**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（该叙述**正确**）。int 类型有固定位数（如 32 位），其取值范围受位数限制，不可能无限。约 -21.47亿 到 +21.47亿。

- **D char 类型的变量有 256 种取值**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（该叙述**正确**）。char 占 1 字节即 8 位，能表示 2⁸ = 256 个不同的值。该叙述符合 C++ 标准。

**易错提醒：**
- 审题时需注意“不正确”与“正确”的区别。本题要找的是叙述错误的选项。
- “浮动”一词容易引起误解，误以为 double 会像某些压缩格式那样动态调整大小。实际上 double 是固定长度的 IEEE 754 双精度格式。

**知识延伸：**
C++ 标准仅规定各类型占用空间的**最小值**和**相对大小关系**（如 sizeof(char) ≤ sizeof(short) ≤ sizeof(int) ≤ sizeof(long)），具体实现在不同平台上可能略有差异，但 double 通常保持为 float 的两倍大小。

**考点：** C++ 基本数据类型的内存占用与取值范围`,
            tags: ['数据类型']
        },
        {
            id: 4,
            type: 'single',
            question: `下列关于 C++ 语言的叙述，不正确的是（ ）。`,
            options: ["变量定义后，可以使用赋值语句改变它的值", "变量定义时，必须指定类型", "变量名必须为合法标识符", "合法标识符可以以数字开始"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (合法标识符可以以数字开始)**

**核心解析：**
本题要求选出“不正确”的叙述。C++ 标识符（identifier）是用于命名变量、函数、类等程序实体的符号，其命名规则由 C++ 标准严格规定。

C++ 标识符的合法命名规则：
1. 首字符必须是字母（大写 A-Z 或小写 a-z）或下划线（_）。
2. 首字符不能是数字（0-9）。
3. 其余字符可以是字母、数字（0-9）或下划线。
4. 不能使用 C++ 关键字作为标识符。

**选项逐项分析：**
- **A 变量定义后，可以使用赋值语句改变它的值**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（该叙述**正确**）。这是变量的基本特性。以 \`int a = 5;\` 定义变量后，可以通过 \`a = 10;\` 改变其值。除非变量被 const 修饰，否则赋值操作完全合法。

- **B 变量定义时，必须指定类型**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（该叙述**正确**）。C++ 是静态强类型语言，定义变量时必须显式声明类型（如 \`int x;\` \`double y;\`）。使用 \`auto\` 关键字时虽然表面没有写类型，但编译器会在编译期根据初始化值自动推导具体类型，本质上仍是确定类型的。

- **C 变量名必须为合法标识符**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误（该叙述**正确**）。变量名作为标识符，必须符合 C++ 标识符命名规范。例如 \`int 1abc;\` 非法，因为数字不能作为首字符。

- **D 合法标识符可以以数字开始**：✅ 正确（该叙述**不正确**）。这是本题要找的错误选项。C++ 标识符严格禁止以数字开头。例如 \`int 2value;\` 编译会失败；\`int _value;\` 和 \`int value2;\` 才是合法的。

**易错提醒：**
- 标识符不能以数字开头，但可以包含数字。如 \`a1\`、\`b2\`、\`count3\` 都是合法标识符。
- 以下划线开头的标识符虽然合法，但在 C++ 中可能被编译器或标准库用作特殊用途（如 \`_GLIBCXX\`），一般建议避免使用下划线开头命名用户标识符。

**知识延伸：**
C++ 标识符区分大小写，\`max\`、\`Max\`、\`MAX\` 是三个完全不同的标识符。此外，C++ 标准保留了一些以下划线加大写字母开头的标识符（如 \`_X\`），这些也是编程时应避免使用的。

**考点：** C++ 标识符命名规则（首字符限制）`,
            tags: ['变量与标识符', '标识符']
        },
        {
            id: 5,
            type: 'single',
            question: `以下哪个不是 C++ 语言的关键字？`,
            options: ["return", "max", "else", "case"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (max)**

**核心解析：**
C++ 语言关键字（keyword）是语言语法定义的一部分，具有特殊含义，程序员不能将其用作标识符（变量名、函数名等）。区分关键字与标准库函数名是本题的核心。

C++ 标准定义了大量关键字，包括类型关键字（int、double、bool、char 等）、控制语句关键字（if、else、for、while、return 等）、访问控制关键字（public、private、protected 等）以及 const、static、class、struct、enum 等。

**选项逐项分析：**
- **A return**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误。\`return\` 是 C++ 关键字，用于从函数返回值并结束函数执行。任何函数都不可能将 \`return\` 作为变量名使用。

- **B max**：✅ 正确。该项不符合题目限定，正是本题要选的项。正确。\`max\` 是标准模板库（STL）\`<algorithm>\` 头文件中定义的函数模板，用于比较两个值并返回较大者。它是普通标识符，不是语言关键字。例如：\`std::max(3, 5)\` 返回 5。编程者仍可将 \`max\` 作为普通标识符使用（虽然这不是好习惯）。

- **C else**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误。\`else\` 是 if-else 条件语句的组成部分，属于关键字，用于表示条件不满足时的分支。

- **D case**：❌ 错误。该项本身符合规则，因此不是本题要选的项。错误。\`case\` 是 switch 语句的关键字，用于标记每个分支的入口标签，属于关键字。

**易错提醒：**
- 标准库函数名（如 \`max\`、\`min\`、\`sort\`）虽然功能强大且常用，但它们只是库提供的功能标识符，并非语言内置关键字。
- 常见关键字还包括：\`break\`、\`continue\`、\`switch\`、\`do\`、\`goto\`、\`typedef\`、\`template\`、\`typename\` 等。考试时需注意区分。

**知识延伸：**
C++ 标准库提供了大量实用函数，如 \`<algorithm>\` 中的 \`max\`、\`min\`、\`swap\`、\`sort\`、\`find\`、\`count\` 等，以及 \`<cmath>\` 中的数学函数如 \`abs\`、\`sqrt\`、\`pow\` 等。这些都不是关键字，而是标准库提供的普通函数/函数模板。

**考点：** C++ 语言关键字与标准库标识符的区分`,
            tags: ['关键字']
        },
        {
            id: 6,
            type: 'single',
            question: `以下哪个不是 C++ 语言的运算符？`,
            options: ["=", "==", "/=", "\\="],
            answer: 3,
            score: 2,
            explanation: `**答案：D (\\=)**

**核心解析：**
在 C++ 语言中，反斜杠 \`\\\` 本身是一个普通字符，不构成任何运算符。反斜杠主要用于以下场景：
1. 转义字符序列的一部分（如 \`\\n\` 表示换行，\`\\t\` 表示制表符）
2. 在字符串常量中表示特殊含义
3. 路径分隔符（仅在某些操作系统中作为文件系统路径的惯例，不属于 C++ 语法层面的运算符）

运算符必须是 C++ 语法中定义具有运算功能的符号，包括赋值运算符 \`=\`、复合赋值运算符如 \`+=\`、\`-=\`、\`*=\`、\`/=\`、\`%=\`、位运算赋值 \`&=\`、\`|=\`、\`^=\`、\`<<=\`、\`>>=\` 等。

**选项逐项分析：**
- **A (=)**：❌ 错误。这是最基本的赋值运算符，用于将右侧表达式的值赋给左侧变量。
- **B (==)**：❌ 错误。这是关系运算符（相等比较），用于判断两个值是否相等。
- **C (/=)**：❌ 错误。这是复合赋值运算符，相当于 \`x = x / y\` 的简写形式。
- **D (\\=)**：✅ 正确。C++ 中不存在 \`\\=\` 这种形式的运算符，反斜杠后接等号没有语法含义。

**易错提醒：**
- 容易将转义字符的写法与运算符混淆，看到 \`\\n\` 不要误以为是某种运算符
- 复合赋值运算符必须是单斜杠/无斜杠前缀，如 \`+=\`、\`-=\`、\`/=\`、\`*=\` 等

**知识延伸：**
C++ 中复合赋值运算符共有 10 种：\`+=\`、\`-=\`、\`*=\`、\`/=\`、\`%=\`、\`&=\`、\`|=\`、\`^=\`、\`<<=\`、\`>>=\`。

**考点：** C++ 运算符的识别与分类`,
            tags: ['运算符']
        },
        {
            id: 7,
            type: 'single',
            question: `如果 a 和 b 都是 char 类型的变量，下列哪个语句不符合 C++ 语法？`,
            options: ["b = a+1;", "b = a+'1';", "b = 'a'++;", "b = a++;"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (b = 'a'++;)**

**核心解析：**
本题考察自增运算符 \`++\` 的操作数要求。自增运算符（以及自减运算符 \`--\`）必须作用于**左值（L-value）**，即必须是一个可寻址、可修改的变量，而非常量或临时值。

在表达式 \`b = 'a'++;\` 中：
1. \`'a'\` 是字符字面量，表示 ASCII 码值为 97 的常量
2. 字面量是右值（R-value），没有存储空间，不能被修改
3. 对常量使用 \`++\` 运算符试图修改其值，这在 C++ 语法中是**编译错误**

**选项逐项分析：**
- **A (b = a+1;)**：❌ 错误。合法。\`a\` 是 \`char\` 类型的变量，与整型 \`1\` 相加时，\`a\` 会隐式提升为 \`int\`，运算结果再截断赋值给 \`b\`。例如若 \`a\` 的 ASCII 值为 97，则 \`b\` 得到 98（对应字符 'b'）。
- **B (b = a+'1';)**：❌ 错误。合法。\`'1'\` 是字符 '1' 的 ASCII 码值 49，两个 \`char\` 值相加，结果赋给 \`b\`。这是字符运算的常见用法。
- **C (b = 'a'++;)**：✅ 正确。非法。\`'a'\` 是字符常量，属于右值，不能作为 \`++\` 运算符的操作数。编译时会报错："error: expression is not assignable"。
- **D (b = a++;)**：❌ 错误。合法。\`a\` 是变量，属于左值，可以使用自增运算符。后置自增返回 \`a\` 的原值（ASCII 码）赋给 \`b\`，然后 \`a\` 自增 1。

**易错提醒：**
- 区分字符与字符变量：字符变量 \`a\` 可以自增，字符字面量 \`'a'\` 不能自增
- 后置自增 \`a++\` 表达式本身的返回值是原值，但会附带副作用使变量增加 1
- 自增运算符只能用于数值类型（整型、浮点型）和迭代器等，不能用于常量、表达式结果或临时对象

**知识延伸：**
左值（lvalue）指"在内存中有确定地址、可以取地址"的对象；右值（rvalue）是"临时的、没有确定地址"的值。C++11 引入了左值引用 \`&\` 和右值引用 \`&&\` 的概念，强化了这种区分。

**考点：** 左值与右值、自增运算符的操作数要求`,
            tags: ['基础语法', '变量与标识符']
        },
        {
            id: 8,
            type: 'single',
            question: `如果 a 为 int 变量且值为 1，则执行 a + 1; 之后，cout << a; 的输出是（ ）。`,
            options: ["0", "2", "1", "3"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (1)**

**核心解析：**
本题的关键在于区分**表达式求值**与**变量赋值**两种不同的语法行为。

1. 语句 \`a + 1;\` 是一个**表达式语句**，它计算 \`a + 1\` 的值（即 1 + 1 = 2）
2. 但这个计算结果**没有被任何变量接收**，也没有通过 \`cout\` 等方式输出
3. 更重要的是：这个语句**不会修改变量 \`a\` 本身的值**
4. 因此执行完 \`a + 1;\` 后，\`a\` 的值仍然是题目给出的初始值 **1**

这与赋值表达式 \`a = a + 1;\` 或复合赋值 \`a += 1;\` 有本质区别。后者会将 \`a\` 的值从 1 改为 2。

**选项逐项分析：**
- **A (0)**：❌ 错误。没有任何机制会使 \`a\` 变为 0。\`a + 1\` 的结果是正数 2，不可能变成 0。
- **B (2)**：❌ 错误。这个陷阱选项利用了 \`a + 1\` 计算结果为 2 的特点，但误认为该结果会改变 \`a\` 本身。
- **C (1)**：✅ 正确。\`a + 1;\` 只是计算表达式值，不改变 \`a\`。\`a\` 保持初始值 1。
- **D (3)**：❌ 错误。\`a + 1\` 的结果是 2，不是 3。即使考虑多次运算或进制转换，也不会有这样的结果。

**易错提醒：**
- \`a + 1\` 与 \`a += 1\` 或 \`a = a + 1\` 是完全不同的语句，前者不修改变量
- 区分"表达式的值"与"变量被修改后的值"
- 没有赋值操作（\`=\`、\`+=\`、\`++\` 等）的表达式语句只会产生计算，副作用不会保留

**知识延伸：**
表达式分为两种效果：产生结果值（计算）和产生副作用（如修改变量、输入输出）。纯计算表达式如 \`a + b\` 只产生结果值，而赋值表达式 \`a = b\` 或自增表达式 \`a++\` 则同时产生结果值和副作用。

**考点：** 表达式求值与变量赋值的区别`,
            tags: ['变量与标识符', '赋值']
        },
        {
            id: 9,
            type: 'single',
            question: `如果 a 为 int 变量且值为 9，则执行 a -= 3; 之后，a 的值是（ ）。`,
            options: ["3", "6", "9", "12"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (6)**

**核心解析：**
复合赋值运算符 \`-=\` 的语义是：\`a -= b\` 等价于 \`a = a - b\`。

按照题目条件：
- 初始值：\`a = 9\`
- 执行语句：\`a -= 3;\`
- 计算过程：\`a = a - 3 = 9 - 3 = 6\`
- 最终结果：\`a\` 的值变为 **6**

**选项逐项分析：**
- **A (3)**：❌ 错误。这是 \`a / 3\` 的结果，混淆了除法与减法。复合赋值 \`-=\` 执行的是减法操作，不是除法。
- **B (6)**：✅ 正确。\`9 - 3 = 6\`，符合 \`-=\` 运算符的语义。
- **C (9)**：❌ 错误。这是原值未变的情况。如果写成 \`a -= 0\` 或 \`a -= (某个表达式结果为0)\` 才可能保持原值。
- **D (12)**：❌ 错误。这是 \`a + 3\` 的结果。\`-=\` 是减后赋值，不是加后赋值。

**易错提醒：**
- 复合赋值运算符 \`-=\` 表示"减去右侧值后再赋值"，而非"减去变量当前值"
- 不要混淆 \`a -= b\`（减法赋值）与 \`a = -b\`（取负赋值）
- 复合赋值与普通赋值的优先级相同，但会先计算右侧表达式再与左侧变量进行对应运算

**知识延伸：**
C++ 中的复合赋值运算符共有 10 种：算术类 \`+=\`、\`-=\`、\`*=\`、\`/=\`、\`%=\` 和位运算类 \`&=\`、\`|=\`、\`^=\`、\`<<=\`、\`>>=\`。它们的语义都是"先对左右操作数做对应运算，再将结果赋给左操作数"。

**考点：** 复合赋值运算符 \`-=\` 的含义与计算`,
            tags: ['变量与标识符', '赋值']
        },
        {
            id: 10,
            type: 'single',
            question: `在 C++ 中，小写字母 'a' 到 'z' 的 ASCII 码范围是（ ）。`,
            options: ["65-90", "97-122", "48-57", "以上都不是"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (97-122)**

**核心解析：**
ASCII 码（American Standard Code for Information Interchange）是计算机中表示字符的整数编码体系。每个可见字符都有唯一的 0-127 之间的数值。

小写字母的 ASCII 码规律：
- \`'a'\` 的码值是 97
- \`'z'\` 的码值是 122
- 码值连续，中间无间隔

验证：\`'z' - 'a' = 122 - 97 = 25\`，正好对应 26 个英文字母的数量。

**选项逐项分析：**
- **A (65-90)**：❌ 错误。这是大写字母 'A' 到 'Z' 的 ASCII 码范围。'A' = 65，'Z' = 90。
- **B (97-122)**：✅ 正确。小写字母 'a' = 97，'z' = 122，两者相差 25（26 个字母减 1）。
- **C (48-57)**：❌ 错误。这是数字字符 '0' 到 '9' 的 ASCII 码范围。'0' = 48，'9' = 57。
- **D (以上都不是)**：❌ 错误。选项 B 正确描述了小写字母的范围。

**易错提醒：**
- 常见 ASCII 码值需要记忆：'A'=65、'a'=97（相差 32）、'0'=48
- 大写字母与小写字母之间相差固定的 32（'a' - 'A' = 97 - 65 = 32）
- 数字字符与实际数字的值不同：'5' = 53，而非 5

**知识延伸：**
ASCII 码中 32 个控制字符（0-31 和 127）不可打印，常用如换行 10（'\\n'）、回车 13（'\\r'）。扩展 ASCII（128-255）可表示更多字符，但不同编码系统含义不同，现代 C++ 推荐使用 Unicode（UTF-8）处理多语言文字。

**考点：** ASCII 码值与字符的对应关系`,
            tags: ['字符与ASCII']
        },
        {
            id: 11,
            type: 'single',
            question: `下列表达式能正确判断“a 等于 0 或 b 等于 0”的是（ ）。`,
            options: ["(!a) || (!b)", "(a == b == 0)", "(a == 0) && (b == 0)", "(a == 0)-(b == 0) == 0"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (!a) || (!b)**

**核心解析：**
判断"a等于0或b等于0"，即两个变量中至少有一个为0。

在 C++ 中，\`a == 0\` 等价于 \`!a\`（因为 0 在逻辑上下文中为假，取反得真）。因此：
- \`!a\` 为真 ⟺ a 等于 0
- \`!b\` 为真 ⟺ b 等于 0
- \`!a || !b\` 为真 ⟺ a 等于 0 **或** b 等于 0（至少有一个）

该表达式完整覆盖了题目要求的"或"关系。

**选项逐项分析：**
- **A (!a) || (!b)**：✅ 正确。该式等价于"(a==0) || (b==0)"，精确表达"至少有一个为0"，符合题意。
- **B (a == b == 0)**：❌ 错误。链式比较陷阱。\`a == b\` 先计算，结果为布尔值（0 或 1），再与 0 比较，语义上等价于\`(a == b) == 0\`，即判断"a 和 b 是否不相等"，与题意完全不符。
- **C (a == 0) && (b == 0)**：❌ 错误。逻辑运算符错误。\`&&\` 表示"且"，该式要求 a 和 b **同时**为 0，即"a 等于 0 **且** b 等于 0"，与题目"或"的要求相反。
- **D (a == 0)-(b == 0) == 0**：❌ 错误。该式等价于判断"a 和 b 的零状态是否相同"（同为0 或 同不为0），而非判断"至少有一个为0"。以 a=0, b=1 为例：表达式值为 \`(1)-(0)==0\`，即 1≠0，结果为假，与"至少有一个为0"的预期（应为真）不符。

**易错提醒：**
- C 选项极具迷惑性——学生容易将日常语言"或者"误用为逻辑"且"，需严格区分 \`||\`（或）与 \`&&\`（且）。
- B 选项的连等写法在数学中常见，但在 C++ 中会产生中间布尔值，而非连续比较。

**知识延伸：**
C++ 中关系表达式 \`a == 0\` 的结果本身就是整数 0 或 1，可直接参与算术运算，这是理解选项 D 行为的关键。

**考点：** 逻辑运算符（\`!\`、\`||\`、\`&&\`）的真值语义与数值转换`,
            tags: ['逻辑运算']
        },
        {
            id: 12,
            type: 'single',
            question: `如果需要执行 100 次循环，下列 for 语句头部正确的是（ ）。`,
            options: ["for(int i = 1; i < 100; i++)", "for(int i = 0; i <= 100; i++)", "for(int i = 1; i <= 99; i++)", "for(int i = 0; i < 100; i++)"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (for(int i = 0; i < 100; i++))**

**核心解析：**
for 循环的执行次数由初值、终值和迭代步长共同决定。对于 \`for(int i = 0; i < 100; i++)\`：
- i 从 0 开始
- 每次迭代后 i++，i 的取值为 0, 1, 2, ..., 99
- i=100 时，条件 \`i < 100\` 为假，退出循环

共执行 100 次迭代，符合"左闭右开"计数惯例：\`[0, 100)\` 区间内包含 100 个整数。

**选项逐项分析：**
- **A for(int i = 1; i < 100; i++)**：❌ 错误。i 的取值为 1 到 99，共 99 次，不满足 100 次要求。
- **B for(int i = 0; i <= 100; i++)**：❌ 错误。i 的取值为 0 到 100，共 101 次，超出要求。
- **C for(int i = 1; i <= 99; i++)**：❌ 错误。i 的取值为 1 到 99，共 99 次，不满足 100 次要求。
- **D for(int i = 0; i < 100; i++)**：✅ 正确。i 的取值为 0 到 99，共 100 次，精确满足要求。

**易错提醒：**
- 边界条件的细微差异（\`<\` vs \`<=\`，起始值 0 vs 1）会导致执行次数偏差，需逐一验证。
- 常见错误是误以为 \`i=0\` 会"浪费一次"，实际上从 0 开始计数是计算机科学的标准做法。

**知识延伸：**
循环次数 = 终值 - 初值（当步长为 1 时）。例如 0 到 99：\`99 - 0 + 1 = 100\` 次；或用开区间：\`100 - 0 = 100\` 次。

**考点：** for 循环计数控制（初值、终值与迭代步长关系）`,
            tags: ['循环']
        },
        {
            id: 13,
            type: 'single',
            question: `如果需要判断 a 是否大于 b，若大于则交换 a 和 b 的值，横线处应填入（ ）。\n\`\`\`cpp\nif (________) {\n  int t = a;\n  a = b;\n  b = t;\n}\n\`\`\``,
            options: ["a < b", "a == b", "a > b", "a != b"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (a > b)**

**核心解析：**
题目要求："若 a 大于 b，则交换 a 和 b 的值"。这是一个典型的变量交换（Swap）逻辑：
- 若 \`a > b\` 为真，执行交换，使较大的值换到 b，较小的值换到 a
- 若 \`a > b\` 为假（a ≤ b），不执行交换，保持原有顺序

交换后确保 \`a ≤ b\` 成立，实现升序排列效果。

**选项逐项分析：**
- **A a < b**：❌ 错误。若填写 \`a < b\`，则"小于则交换"，结果会使 \`a\` 最终始终不大于 \`b\`（即 \`a ≤ b\`），与题目"大于则交换"的要求相反。
- **B a == b**：❌ 错误。相等时交换无实际意义（两数相同，交换后值不变），且不满足"大于"的条件。
- **C a > b**：✅ 正确。精确对应题目"若大于则交换"的条件，交换后实现 \`a\` 不大于 \`b\` 的效果。
- **D a != b**：❌ 错误。不等于就交换，包括"小于"和"大于"两种情况，无法精确实现题目要求的特定条件。

**易错提醒：**
- 变量交换是 GESP 高频考点，需熟练掌握"借助临时变量交换"的代码模式。
- 判断条件的方向（\`>\` vs \`<\`）决定了排序的升序/降序效果，不可混淆。

**知识延伸：**
两数交换的标准写法需要三步：保存 \`t = a\`、赋值 \`a = b\`、赋值 \`b = t\`，缺一不可，否则会导致数据丢失。

**考点：** 条件分支判断逻辑与变量交换（Swap）应用场景`,
            tags: ['分支结构']
        },
        {
            id: 14,
            type: 'single',
            question: `下列代码执行后，其输出是（ ）。\n\`\`\`cpp\nfor(int i = 1; i <= 3; i++) {\n  for(int j = 1; j <= i; j++)\n    cout << j;\n}\n\`\`\``,
            options: ["123", "112123", "122333", "112123"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (112123)**

**核心解析：**
分析嵌套循环的执行过程：
\`\`\`cpp
for(int i = 1; i <= 3; i++) {       // 外层循环 i = 1, 2, 3
  for(int j = 1; j <= i; j++)       // 内层循环 j 从 1 到 i
    cout << j;
}
\`\`\`

**逐步追踪：**
- **i = 1**：内层 j 从 1 到 1，输出 \`1\`
- **i = 2**：内层 j 从 1 到 2，输出 \`12\`
- **i = 3**：内层 j 从 1 到 3，输出 \`123\`

所有输出依次拼接，无换行分隔，最终结果为 \`112123\`。

**选项逐项分析：**
- **A (123)**：❌ 错误。这是仅有一层循环 \`for(int j = 1; j <= 3; j++)\` 的输出结果，缺少外层循环对内层循环次数的控制。
- **B (112123)**：❌ 错误。选项内容与 D 重复，但题目指定答案为 D，解析按 D 处理。
- **C (122333)**：❌ 错误。这是误以为输出的是 j 的最大值（或误将循环变量 i 作为输出值）的结果，与实际输出不符。
- **D (112123)**：✅ 正确。完整追踪如上。

**易错提醒：**
- 内层循环的终值是外层循环变量 i，而非固定常数，因此内层循环次数随外层变化。
- 注意 \`cout << j\` 输出的是 j 的值（1, 2, 3...），而非 i 的值。

**知识延伸：**
嵌套循环中，外层循环控制"行数"，内层循环控制"每行的列数/内容"，常用于打印矩形、三角形等规律图案。

**考点：** 嵌套循环的执行顺序与输出结果追踪`,
            tags: ['嵌套循环']
        },
        {
            id: 15,
            type: 'single',
            question: `执行以下程序后，输出结果是（ ）。\n\`\`\`cpp\nint n=17; \nbool isprime=true; \nfor(int i=2; i<=n; i++) \n  if(n%i==0) isprime=false; \ncout<<isprime;\n\`\`\``,
            options: ["false", "true", "0", "1"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (0)**

**核心解析：**
\`\`\`cpp
int n=17;
bool isprime=true;
for(int i=2; i<=n; i++)
  if(n%i==0) isprime=false;
cout<<isprime;
\`\`\`

**逐步分析：**
1. \`n = 17\`，\`isprime\` 初始为 \`true\`
2. 循环变量 i 从 2 到 17（含 17）：
   - i = 2：\`17 % 2 = 1 ≠ 0\`，不执行
   - i = 3：\`17 % 3 = 2 ≠ 0\`，不执行
   - ...（直至 i = 16 均不整除）
   - **i = 17**：\`17 % 17 = 0\`，执行 \`isprime = false\`
3. 循环结束，\`isprime\` 为 \`false\`
4. \`cout << isprime\` 输出布尔变量

**关键陷阱：**
C++ 中 \`cout\` 输出布尔变量时，会自动将 \`false\` 转换为整数 \`0\`，将 \`true\` 转换为整数 \`1\` 输出，而非输出英文单词。

**选项逐项分析：**
- **A (false)**：❌ 错误。\`cout\` 不会输出英文单词 \`false\`，而是输出对应的数值。
- **B (true)**：❌ 错误。同上，且 \`isprime\` 实际为 \`false\`。
- **C (0)**：✅ 正确。\`isprime\` 为 \`false\`，转换为整数输出为 \`0\`。
- **D (1)**：❌ 错误。若 \`isprime\` 为 \`true\` 则输出 \`1\`，但本题中 \`isprime\` 为 \`false\`。

**易错提醒：**
- 循环条件包含 \`i <= n\`，导致 i=17 时进入循环体，\`n % i == 0\` 成立。这是本题的核心陷阱：判断素数的正确上界应为 \`i < n\`（或 \`i <= sqrt(n)\`），否则任何数都会被判定为非质数。
- \`cout\` 输出布尔量的行为容易被忽视，需牢记数值转换规则。

**知识延伸：**
判断质数的正确做法：只需检查 2 到 \`sqrt(n)\` 之间的数是否整除。本题循环范围过大（到 n），且 \`isprime\` 初值应为 \`true\`，一旦发现可整除即置为 \`false\`。

**考点：** 循环边界判定陷阱（\`i <= n\` vs \`i < n\`）、布尔值与整数的自动转换（\`cout\` 输出行为）`,
            tags: ['程序分析', '布尔类型']
        },
        {
            id: 16,
            type: 'judge',
            question: `4GB 和 4096MB 的 U 盘容量大小是一样的。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
计算机存储单位之间的换算遵循 1024 进制规则：
- 1KB = 1024B
- 1MB = 1024KB
- 1GB = 1024MB

因此：4GB = 4 × 1024MB = 4096MB

两者在理论存储容量上完全相等。

**易错提醒：**
- 部分考生可能混淆了十进制进位（1000）与二进制进位（1024）的区别
- 商业存储设备厂商常用 1000 进制标注容量（如标称 4GB U 盘实际可用约 3.72GB），但本题讨论的是理论换算关系

**知识延伸：**
在程序设计中，缓冲区大小、数组容量等场景常用 1024 进制进行内存地址计算，如 \`int buf[1024]\` 表示 1KB 大小的整型数组。

**考点：** 计算机存储单位（B、KB、MB、GB、TB）的换算关系`,
            tags: ['计算机基础']
        },
        {
            id: 17,
            type: 'judge',
            question: `IPv4 地址点分十进制形式中，每一段的范围是 0 到 255。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
IPv4 地址采用 32 位二进制数表示，为了便于人类阅读，将其划分为 4 个字节（每字节 8 位），并以点分十进制形式呈现（如 \`192.168.1.1\`）。

每组 8 位二进制数的取值范围：
- 最小值：00000000₂ = 0
- 最大值：11111111₂ = 255

因此每一段的取值范围必然是 0 到 255，共 256 个可能值。

**易错提醒：**
- 初学者常误以为范围是 1-255 或 0-256
- 255 是最大值，不是最小值，0 才是最小值

**知识延伸：**
IPv6 地址采用 128 位设计，每段为 16 位，用十六进制表示（0000-FFFF），解决了 IPv4 地址空间不足的问题。

**考点：** 网络基础知识、IPv4 地址格式规范`,
            tags: ['网络基础']
        },
        {
            id: 18,
            type: 'judge',
            question: `C++ 语言中，一个程序不能有多个 main 函数。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
在 C++ 程序中，\`main\` 函数是程序执行的全局唯一入口点。当程序启动时，操作系统的程序加载器会查找符号名为 \`main\` 的函数作为起始地址。

如果源代码中定义了多个 \`main\` 函数：
- 在编译阶段可能通过（编译器允许存在多个同名函数，但作用域不能冲突）
- 在链接阶段必然失败，链接器会报告"符号 main 被多次定义"的错误

因此，一个可执行程序有且只能有一个全局 \`main\` 函数。

**易错提醒：**
- C++ 允许不同命名空间中存在同名函数（如命名空间 \`ns1::main\` 和 \`ns2::main\`），但全局作用域只能有一个 \`main\`
- 在某些嵌入式系统中可能使用其他入口点（如 \`WinMain\`、\`mainCRTStartup\`），但标准 C++ 程序必须以 \`main\` 为主入口

**知识延伸：**
链接器还会查找全局对象构造顺序、异常处理等与 \`main\` 函数相关的初始化代码，确保程序在执行用户代码前完成标准库的初始化工作。

**考点：** C++ 程序的基本结构、入口函数 \`main\` 的唯一性`,
            tags: ['基础语法']
        },
        {
            id: 19,
            type: 'judge',
            question: `标识符中可以有下划线，但不能以下划线开头。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
C++ 标识符的命名规则明确规定：标识符可以由字母、数字和下划线组成，且**可以以下划线开头**。

具体规则如下：
- 首字符：字母（a-z, A-Z）或下划线（_）
-后续字符：字母、数字、下划线
- 区分大小写

以下标识符在语法上均合法：
- \`_variable\`
- \`_123\`
- \`__private\`

**易错提醒：**
- 虽然标识符可以以下划线开头，但以下划线开头后跟大写字母的标识符（如 \`_CONST\`）通常被保留给实现（即编译器/标准库）使用，程序员应避免
- 以下划线开头的标识符在标准库实现中大量使用，学习者见到时不必惊讶

**知识延伸：**
标识符的命名规范虽允许下划线开头，但在实际项目中，不同代码风格规范可能有额外约定（如 Google C++ 风格禁止以下划线开头命名），这些是编码规范层面的约束，而非语言语法层面的限制。

**考点：** C++ 标识符命名规范`,
            tags: ['变量与标识符']
        },
        {
            id: 20,
            type: 'judge',
            question: `如果 int a = 1，则表达式 'a' 的值为 '1'。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
本题涉及两个完全不同的语法元素：

1. 变量 \`a\`（不带引号）：声明为 \`int a = 1\`，是一个整型变量，其值为 1，存储在内存中。

2. 字符字面量 \`'a'\`（带单引号）：代表 ASCII 码表中字符 'a'，其 ASCII 码值为 97（十进制）。

\`'a'\` 是一个编译时常量，其值在编译时确定，与变量 \`a\` 的当前值毫无关系。无论变量 \`a\` 被赋为何值，字符字面量 \`'a'\` 的值始终是 97。

同理，\`'1'\` 是字符 '1'，其 ASCII 码值为 49，而非整数值 1。

**易错提醒：**
- 字符字面量 \`'a'\` 的值是 ASCII 码（97），不是变量 \`a\` 的值
- 字符 \`'1'\` 的 ASCII 码是 49，不是整数值 1
- 只有字符 \`'0'\` 到 \`'9'\` 的 ASCII 码是 48 到 57，与数字字符形成对应关系

**知识延伸：**
若要获取字符 \`'a'\` 的 ASCII 码值，可通过 \`int('a')\` 或 \`(int)'a'\` 进行显式转换，结果为 97。在 C++ 中，\`'a' + 1\` 的结果是 98，即字符 \`'b'\` 的 ASCII 码。

**考点：** 字符字面量与变量名的本质区别、ASCII 码概念`,
            tags: ['字符与ASCII']
        },
        {
            id: 21,
            type: 'judge',
            question: `if 子句中不可以嵌套 if...else 语句。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
在 C++ 语言中，控制结构可以自由嵌套，if 语句的"真分支"或"假分支"内部不仅可以嵌套其他 if 语句，还可以嵌套任意合法的 C++ 语句，包括 if-else 结构。这种嵌套能力正是实现多条件分支逻辑的基础。例如，判断一个年份是否为闰年的经典写法就需要在 if 内部嵌套另一层 if-else。

**易错提醒：**
- 部分学员误以为 if...else 是一个不可分割的整体，实际上 if 和 else 是两个独立的关键字，可以出现在任何合法的语句位置上。
- 嵌套层数没有语法限制，但层数过多时建议改用 switch 或合理重构，以提高代码可读性。

**知识延伸：**
if-else 的嵌套与 switch-case 的嵌套类似，都属于控制流语句的组合使用。在 GESP 二级考试中，多重嵌套是高频考点。

**考点：** 分支控制结构的嵌套语法规则`,
            tags: ['条件判断']
        },
        {
            id: 22,
            type: 'judge',
            question: `while 语句的循环体至少会执行一次。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
while 循环采用"入口条件判断"机制，其执行流程是：先求值循环条件表达式，若结果为真（非零）才执行循环体一次，然后再次判断条件。因此，如果条件表达式在首次求值时即为假（零），循环体将不会执行任何一次。相比之下，do-while 循环才是"出口条件判断"，保证循环体至少执行一次。

**易错提醒：**
- 许多初学者将"循环体可能执行多次"与"循环体至少执行一次"混淆。while 循环体可能执行零次（条件初始为假），也可能执行多次（条件多次为真），关键在于首次条件判断的结果。
- 常见错误场景：当循环变量初始值不满足循环条件时，while 循环体不会执行，而学员可能误以为循环已经执行过。

**知识延伸：**
C++ 中三种循环语句（while、do-while、for）的本质区别就在于条件判断的时机不同。for 循环同样是入口测试，等价于 while 的变体形式。

**考点：** while 循环与 do-while 循环的执行语义差异`,
            tags: ['循环']
        },
        {
            id: 23,
            type: 'judge',
            question: `C++ 语言中 >= 是运算符，但 => 不是。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
在 C++ 语法中，\`>=\` 是标准的关系运算符，用于比较左侧操作数是否大于等于右侧操作数，其运算结果为 bool 类型（实际存储为整数值 0 或 1）。而 \`=>\` 并不是 C++ 语言中的任何合法运算符，C++ 编译器会将其视为语法错误。需要特别说明的是，\`=>\` 在其他编程语言（如 C#、TypeScript）中可能被用作箭头函数语法的一部分，但这与 C++ 无关。

**易错提醒：**
- C++ 的关系运算符只有六个：\`==\`、\`!=\`、\`<\`、\`>\`、\`<=\`、\`>=\`。其中 \`<=\` 和 \`>=\` 是合法的，而 \`=<\` 和 \`=>\` 都不是合法运算符。
- 初学者容易混淆比较运算符与赋值运算符，例如将 \`>=\` 误写为 \`=>\` 或 \`=:\`。

**知识延伸：**
C++ 运算符体系中，关系运算符的优先级低于算术运算符，但高于赋值运算符。例如表达式 \`a + b >= c\` 等价于 \`(a + b) >= c\`。

**考点：** C++ 关系运算符的符号形式识别`,
            tags: ['运算符']
        },
        {
            id: 24,
            type: 'judge',
            question: `a = a-'a'+'A' 可以将小写字母转换为对应的大写字母。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
字符型数据在 C++ 中以 ASCII 码值（整数）形式参与运算。英文字母在 ASCII 表中按顺序排列，小写字母 'a' 到 'z' 的码值分别为 97 到 122，大写字母 'A' 到 'Z' 的码值分别为 65 到 90，同一字母的大小写形式之差恒为 32。

以小写字母 'a'（码值 97）为例分析转换过程：
1. \`a - 'a'\` → \`97 - 97\` → 0，得到该字母在其所在字母序列中的偏移量
2. \`0 + 'A'\` → \`0 + 65\` → 65，得到对应大写字母的码值
3. 赋回变量 a，完成大小写转换

该表达式对任意小写字母均成立，因为减法消除了字母间的相对位置差异。

**易错提醒：**
- 该表达式只能用于转换英文字母，对数字字符（如 '5'）或其他符号不会产生有意义的结果。
- 如果字符已经是大写字母，执行该表达式会将其转换为其他字符，而非保持不变。

**知识延伸：**
大小写转换的另一种实现方式是利用 C++ 标准库的 \`toupper()\` 和 \`tolower()\` 函数，它们位于 \`<cctype>\` 头文件中，能够正确处理 locale 设置且对非字母字符不产生副作用。

**考点：** 字符型数据的算术运算与 ASCII 码转换原理`,
            tags: ['字符与ASCII']
        },
        {
            id: 25,
            type: 'judge',
            question: `表达式 (10.0 / 2) 的计算结果类型为 double。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
C++ 的算术运算符在处理不同类型操作数时，会遵循"寻常算术转换"规则：将精度较低的类型自动提升为精度较高的类型。类型的提升顺序大致为：\`int\` → \`long\` → \`float\` → \`double\`。

对于表达式 \`(10.0 / 2)\`：
- 操作数 \`10.0\` 是 double 类型字面量
- 操作数 \`2\` 是 int 类型字面量
- 除法运算 \`/\` 的两个操作数类型不同，\`int\` 类型的 \`2\` 会被提升为 \`double\` 类型
- 提升后的运算是 \`10.0 / 2.0\`，结果是 \`double\` 类型的 \`5.0\`

若将 \`10.0\` 改为 \`10\`，则两个操作数均为 int，运算结果才是 int 类型。

**易错提醒：**
- 整型除法与浮点除法的区别是高频考点：\`10 / 3\` 结果为 3（截断小数部分），而 \`10.0 / 3\` 或 \`10 / 3.0\` 结果约为 3.333...。
- 类型提升发生在运算之前，运算本身不会改变操作数本身的类型（除非通过显式赋值覆盖）。

**知识延伸：**
除了寻常算术转换，C++ 还支持显式类型转换（强制类型转换），语法为 \`type(expression)\` 或传统的 \`(type)expression\`，可手动控制类型转换方向。

**考点：** 混合类型算术运算中的自动类型提升规则`,
            tags: ['数据类型']
        }
    ],
    programmingQuestions: [
        {
            id: 26,
            type: 'programming',
            title: "字母金字塔",
            description: "输入 n (2≤n≤40)，输出 n 行直角三角形。由大写字母 A-Z 按由上至下、由左至右填充，Z 后接 A。",
            inputDescription: "一个正整数 n (2≤n≤40)。",
            outputDescription: "n 行由大写字母组成的直角三角形。",
            samples: [
                {
                    input: "3",
                    output: "A\nBC\nDEF"
                }
            ],
            explanation: `**解析：**
本题考察嵌套循环与字符处理。
1. **外层循环**：控制行数，从 1 遍历到 \`n\`。
2. **内层循环**：控制每行打印的字母个数，第 \`i\` 行打印 \`i\` 个字母。
3. **字符递增**：维护一个变量 \`ch\` 记录当前字符。每打印一个字符，执行 \`ch++\`。
4. **循环重置**：当 \`ch\` 超过 \`'Z'\` 时，通过 \`ch = 'A'\` 使其回到起始位置。

**核心逻辑：**
\`\`\`cpp
char ch = 'A';
for (int i = 1; i <= n; i++) {
    for (int j = 1; j <= i; j++) {
        cout << ch;
        if (++ch > 'Z') ch = 'A';
    }
    cout << endl;
}
\`\`\``,
            tags: ['循环', '字符与ASCII'],
            template: "#include <iostream>\nusing namespace std;\nint main() {\n    // 在此编写代码\n    return 0;\n}",
            referenceCode: "#include <iostream>\nusing namespace std;\nint main() {\n    int n; cin >> n;\n    char ch = 'A';\n    for (int i = 1; i <= n; i++) {\n        for (int j = 1; j <= i; j++) {\n            cout << ch;\n            if (++ch > 'Z') ch = 'A';\n        }\n        cout << endl;\n    }\n    return 0;\n}"
        },
        {
            id: 27,
            type: 'programming',
            title: "扩展百鸡问题",
            description: "公鸡 x 元，母鸡 y 元，z 只小鸡 1 元。有 n 元钱买 m 只鸡，问有多少种方案？",
            inputDescription: "五个正整数 x, y, z, n, m。",
            outputDescription: "方案总数。",
            samples: [
                {
                    input: "5 3 3 100 100",
                    output: "4"
                }
            ],
            explanation: `**解析：**
本题是经典“百钱买百鸡”问题的扩展版。主要考察枚举算法（Brute Force）。
1. **变量定义**：公鸡 \`i\` 只，母鸡 \`j\` 只，小鸡 \`k = m - i - j\` 只。
2. **枚举范围**：公鸡最多买 \`m\` 只（或 \`n/x\` 只），母鸡最多买 \`m-i\` 只。
3. **约束条件**：
   - 鸡的总数：\`i + j + k = m\`（通过 \`k = m - i - j\` 自动满足）。
   - 总钱数：\`x*i + y*j + k/z = n\`。
4. **注意点**：为了避免浮点数精度误差，建议将钱数公式左右同乘以 \`z\`，变为：\`z*x*i + z*y*j + k == z*n\`。

**代码实现思路：**
\`\`\`cpp
for (int i = 0; i <= m; i++) {
    for (int j = 0; j <= m - i; j++) {
        int k = m - i - j;
        if (k >= 0 && z * x * i + z * y * j + k == z * n) {
            cnt++;
        }
    }
}
\`\`\``,
            tags: ['枚举', '模拟'],
            template: "#include <iostream>\nusing namespace std;\nint main() {\n    // 在此编写代码\n    return 0;\n}",
            referenceCode: "#include <iostream>\nusing namespace std;\nint main() {\n    int x, y, z, n, m;\n    cin >> x >> y >> z >> n >> m;\n    int cnt = 0;\n    for (int i = 0; i <= m; i++) {\n        for (int j = 0; j <= m-i; j++) {\n            int k = m-i-j;\n            if (k >= 0 && (z * x * i + z * y * j + k == z * n)) {\n                cnt++;\n            }\n        }\n    }\n    cout << cnt << endl;\n    return 0;\n}"
        }
    ]
};
