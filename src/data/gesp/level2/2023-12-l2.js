// 2023年12月 GESP C++ 二级真题 (第4次认证)
export const paperData = {
    id: '2023-12-l2',
    title: '2023年12月 GESP C++ 二级真题',
    level: 2,
    year: 2023,
    month: 12,
    session: 4,
    note: '年度收官',
    timeLimit: 90 * 60,
    questions: [
        {
            id: 1,
            type: 'single',
            question: `C++语言中，以下哪个变量命名是错误的？（ ）。`,
            options: ["my_var", "_var1", "123var", "var_123"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (123var)**

**选项逐项分析：**
- **A my_var**：❌ 错误。合法的标识符，由字母和下划线组成。
- **B _var1**：❌ 错误。合法的标识符，可以以下划线开头。
- **C 123var**：✅ 正确（该命名是错误的）。C++ 标识符的命名规则规定：标识符必须以字母或下划线开头，不能以数字开头。
- **D var_123**：❌ 错误。合法的标识符，数字可以出现在非首位。

**考点：** C++ 标识符命名规范`,
            tags: ["变量命名"]
        },
        {
            id: 2,
            type: 'single',
            question: `在 C++ 中，与 for(int i = 10; i < 20; i += 2) cout << i; 输出结果相同的是（ ）。`,
            options: ["for(int i = 10; i < 19; i += 2) cout << i;", "for(int i = 11; i < 19; i += 2) cout << i;", "for(int i = 10; i < 21; i += 2) cout << i;", "以上均不对"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (for(int i = 10; i < 19; i += 2) cout << i;)**

**解析：**
1. **原循环分析**：\`i\` 取值序列为 10, 12, 14, 16, 18。当 \`i\` 变为 20 时，\`20 < 20\` 为假，停止。
2. **选项 A 分析**：\`i\` 从 10 开始，步长 2。取值序列为 10, 12, 14, 16, 18。当 \`i\` 变为 20 时，\`20 < 19\` 为假，停止。输出结果与原循环完全一致。
3. **选项 B 分析**：\`i\` 从 11 开始，输出序列为 11, 13, 15, 17，结果不同。
4. **选项 C 分析**：\`i\` 最后会取到 20（因为 \`20 < 21\`），结果多了一个 20。

**考点：** for 循环的执行过程、循环条件的边界判定`,
            tags: ["循环结构"]
        },
        {
            id: 3,
            type: 'single',
            question: `以下 C++ 代码实现从小到大的顺序输出能整除 N 的数（N 的因子），例如 N=18 时输出 1 2 3 6 9 18，横线处应填入（ ）。\n\`\`\`cpp\nfor (________) {\n    if (N % i == 0) cout << i << " ";\n}\n\`\`\``,
            options: ["int i = 0; i < N; i++", "int i = 1; i < N; i++", "int i = 0; i < N + 1; i++", "int i = 1; i < N + 1; i++"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (int i = 1; i < N + 1; i++)**

**选项逐项分析：**
- **A / C (i = 0)**：❌ 错误。取模运算的除数不能为 0，程序会发生运行时错误（Division by zero）。
- **B (i < N)**：❌ 错误。因子包含 \`N\` 本身。该选项只能输出到 \`N-1\`，会遗漏 \`N\`。
- **D (i = 1; i < N + 1)**：✅ 正确。从 1 开始（合法），结束于 \`N\`（符合要求）。

**考点：** 因子的定义、循环范围、除零风险判定`,
            tags: ["循环分析"]
        },
        {
            id: 4,
            type: 'single',
            question: `下面 C++ 代码用于判断输入的整数是否为对称数（如 1221, 12321）。下面对该代码的说法正确的是（ ）。\n\`\`\`cpp\nint N, M = 0, temp;\ncin >> N;\ntemp = N;\nwhile (temp > 0) {\n    M = M * 10 + temp % 10;\n    temp /= 10;\n}\nif (M == N) cout << "是对称数";\n\`\`\``,
            options: ["代码没有语法错误，第 8 行将能正确输出", "如果 N 为负数，将导致死循环", "代码存在语法错误，不能被执行", "代码逻辑正确，temp 用于保存原值参与比较"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (代码逻辑正确，temp 用于保存原值参与比较)**

**解析：**
1. **算法逻辑**：代码通过 \`temp % 10\` 逐位取出数字，并通过 \`M = M * 10 + ...\` 构造出反转后的数值 \`M\`。
2. **变量保护**：关键在于 \`temp = N\`。循环中消耗的是 \`temp\`，从而保护了原输入的 \`N\`。最后一行 \`M == N\` 的比较才有意义。
3. **异常处理**：如果 \`N\` 为负数，\`temp > 0\` 条件初始即为假，循环不执行，\`M\` 保持 0。\`0 == N\` 为假，不会判定为对称数，也不会死循环。

**考点：** 数位反转算法、临时变量的作用`,
            tags: ["程序分析"]
        },
        {
            id: 5,
            type: 'single',
            question: `下面 C++ 代码用于判断 N (N≥2) 是否为质数。下面对代码的说法正确的是（ ）。\n\`\`\`cpp\nint i;\nfor (i = 2; i < N / 2; i++)\n    if (N % i == 0) { cout << "非质数"; break; }\nif (i >= N / 2) cout << "质数";\n\`\`\``,
            options: ["代码能正确判断 N 是否为质数", "代码总是不能判断 N 是否为质数", "删除 break 将能正确判断", "代码存在漏洞，当 N=4 时会判为质数"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (代码存在漏洞，当 N=4 时会判为质数)**

**解析：**
1. **分析 N=4 的情况**：\`N / 2 = 2\`。
2. **循环执行**：\`for (i = 2; i < 2; i++)\`。由于 \`2 < 2\` 为假，循环体一次都不执行。此时变量 \`i\` 仍为 2。
3. **判定执行**：跳转到 \`if (i >= N / 2)\`。由于 \`2 >= 2\` 成立，输出“质数”。
4. **结论**：4 是合数，却被判为质数，说明代码存在边界漏洞。应将条件改为 \`i <= N / 2\` 或 \`i * i <= N\`。

**考点：** 质数判定算法、循环边界逻辑、代码追踪分析`,
            tags: ["边界分析"]
        },
        {
            id: 6,
            type: 'single',
            question: `下面 C++ 代码执行后的输出是（ ）。\n\`\`\`cpp\nint N = 4;\nfor (int i = 0; i < N; i++) {\n    for (int j = 1; j < i; j++)\n        if (i * j % 2 == 0)\n            cout << i << "#";\n}\ncout << "0";\n\`\`\``,
            options: ["2#3#0", "1#2#0", "1#0#", "2#3#"],
            answer: 0,
            score: 2,
            explanation: `**答案：A (2#3#0)**

**解析：**
1. \`i = 0, 1\`: 内层 \`j < i\` 不满足，无输出。
2. \`i = 2\`: \`j\` 取 {1}。判断 \`2 * 1 % 2 == 0\` 为真，输出 \`2#\`。
3. \`i = 3\`: \`j\` 取 {1, 2}。
   - \`j = 1\`: \`3 * 1 % 2 == 0\` 为假。
   - \`j = 2\`: \`3 * 2 % 2 == 0\` 为真，输出 \`3#\`。
4. 循环结束，最后执行 \`cout << "0"\`。
5. 总结果：\`2#3#0\`。

**考点：** 嵌套循环追踪、逻辑条件判定`,
            tags: ["嵌套循环"]
        },
        {
            id: 7,
            type: 'single',
            question: `下面 C++ 代码执行后的输出是（ ）。\n\`\`\`cpp\nint cnt = 0;\nfor (int i = 1; i < 10; i++) {\n    if (i % 2 == 0) cnt++;\n}\ncout << cnt << "#";\n\`\`\``,
            options: ["5#", "8#", "9#", "4#"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (4#)**

**解析：**
1. 循环 \`i\` 从 1 到 9。
2. 条件 \`i % 2 == 0\` 用于筛选偶数。
3. 1-9 之间的偶数集合为 {2, 4, 6, 8}，共 4 个。
4. 因此 \`cnt\` 最终值为 4，输出 \`4#\`。

**考点：** 计数器逻辑、循环范围判定`,
            tags: ["程序分析"]
        },
        {
            id: 8,
            type: 'single',
            question: `下面 C++ 代码执行后的输出是（ ）。\n\`\`\`cpp\nint N = 100;\nwhile (N % 2 == 0) {\n    if (N % 3 == 0) N -= 5;\n    else N -= 20;\n}\ncout << N;\n\`\`\``,
            options: ["100", "80", "55", "40"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (55)**

**解析：**
1. 初始 \`N = 100\`。\`100 % 2 == 0\` 成立，进入循环。
2. 第一轮：\`100 % 3 != 0\`，执行 \`N -= 20\`，\`N = 80\`。
3. 第二轮：\`80 % 2 == 0\` 成立。\`80 % 3 != 0\`，执行 \`N -= 20\`，\`N = 60\`。
4. 第三轮：\`60 % 2 == 0\` 成立。\`60 % 3 == 0\` 成立，执行 \`N -= 5\`，\`N = 55\`。
5. 第四轮：判断 \`55 % 2 == 0\` 为假。退出循环。
6. 最终输出 55。

**考点：** while 循环追踪、多重条件分支判定`,
            tags: ["循环分析"]
        },
        {
            id: 9,
            type: 'single',
            question: `下面 C++ 代码执行后的输出是（ ）。\n\`\`\`cpp\nint x = 1;\nwhile (x <= 10) {\n    if (x % 3 != 0) cout << x << ",";\n    if (x % 3 == 0) x = x + 5 + 2;\n    else x += 2;\n}\n\`\`\``,
            options: ["1,3,", "1,3,10,", "1,7,", "1,10,"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (1,10,)**

**解析：**
1. 初始 \`x = 1\`。
2. 第一轮：\`1 <= 10\` 成立。\`1 % 3 != 0\`，输出 \`1,\`。接着 \`1 % 3 == 0\` 为假，执行 \`else\` 分支：\`x = 1 + 2 = 3\`。
3. 第二轮：\`3 <= 10\` 成立。\`3 % 3 != 0\` 为假，不输出。接着 \`3 % 3 == 0\` 为真，执行 \`x = 3 + 5 + 2 = 10\`。
4. 第三轮：\`10 <= 10\` 成立。\`10 % 3 != 0\`，输出 \`10,\`。接着 \`10 % 3 == 0\` 为假，执行 \`else\` 分支：\`x = 10 + 2 = 12\`。
5. 第四轮：\`12 <= 10\` 为假，退出。
6. 最终输出：\`1,10,\`。

**考点：** 循环内变量的非线性更新、逻辑分支逻辑追踪`,
            tags: ["程序分析"]
        },
        {
            id: 10,
            type: 'single',
            question: `下面 C++ 代码执行后的输出是（ ）。\n\`\`\`cpp\nint cnt = 0;\nfor (int i = 0; i < 4; i++) {\n    for (int j = 0; j <= i; j++) {\n        cnt++;\n    }\n}\ncout << cnt;\n\`\`\``,
            options: ["5", "10", "15", "20"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (10)**

**解析：**
这是一个典型的阶梯状嵌套循环计数问题：
- \`i = 0\`: \`j\` 取 {0}，循环 1 次。
- \`i = 1\`: \`j\` 取 {0, 1}，循环 2 次。
- \`i = 2\`: \`j\` 取 {0, 1, 2}，循环 3 次。
- \`i = 3\`: \`j\` 取 {0, 1, 2, 3}，循环 4 次。
总计数 = $1 + 2 + 3 + 4 = 10$。

**考点：** 嵌套循环的迭代次数计算、等差数列求和思想`,
            tags: ["循环计数"]
        },
        {
            id: 11,
            type: 'single',
            question: `以下 C++ 代码用于输出 1-100（含）的整数平方数（完全平方数），横线处应填写（ ）。\n\`\`\`cpp\nfor (int i = 1; i <= 100; i++) {\n    if (________) cout << i << " ";\n}\n\`\`\``,
            options: ["int(sqrt(i)) * int(sqrt(i)) = i", "int(sqrt(i)) == sqrt(i)", "int(sqrt(i)) * int(sqrt(i)) == i", "int(sqrt(i)) = sqrt(i)"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (int(sqrt(i)) * int(sqrt(i)) == i)**

**选项逐项分析：**
- **A / D**：❌ 错误。单等号 \`=\` 是赋值运算符，不能用于 \`if\` 中的条件判断，且左边通常不能是函数调用的结果。
- **B (int(sqrt(i)) == sqrt(i))**：❌ 错误。虽然在数学上逻辑正确，但 \`sqrt(i)\` 返回的是浮点数，直接与整数进行 \`==\` 比较由于精度问题在 C++ 中是不推荐的。
- **C**：✅ 正确。通过“先开方取整，再平方还原”的方法。如果还原后的结果等于原数，说明原数是完全平方数。这是最健壮的判断方法。

**考点：** 完全平方数的判定逻辑、比较运算符与赋值运算符的区别`,
            tags: ["数学函数"]
        },
        {
            id: 12,
            type: 'single',
            question: `下面的 C++ 代码用于输出特定字符矩阵。应在横线处填入（ ）。\n\`\`\`cpp\nfor (int i = 0; i < N; i++) {\n    for (int j = 0; j < N; j++) {\n        // ... 打印逻辑\n    }\n    ________;\n}\n\`\`\``,
            options: ["cout << nowNum;", "cout << \" \";", "cout << 0;", "cout << endl;"],
            answer: 3,
            score: 2,
            explanation: `**答案：D (cout << endl;)**

**解析：**
在打印二维矩阵或图形时，外层循环控制行，内层循环控制列。当内层循环结束（即一行打印完毕）后，必须执行换行操作，才能开始下一行的打印。

**考点：** 二维图形输出的基本模版、嵌套循环的结构控制`,
            tags: ["程序分析"]
        },
        {
            id: 13,
            type: 'single',
            question: `某公司新出的无人驾驶小汽车，通过声控系统选择路线。请问下面哪项不是驾驶系统完成选路所必须的？（ ）`,
            options: ["麦克风", "扬声器", "油量表", "传感器"],
            answer: 2,
            score: 2,
            explanation: `**答案：C (油量表)**

**选项逐项分析：**
- **A 麦克风**：✅ 必须。用于接收用户的“声控”指令。
- **B 扬声器**：✅ 必须。用于给用户语音反馈，告知选路结果。
- **D 传感器**：✅ 必须。无人驾驶依赖传感器（如激光雷达、摄像头）来感知环境并最终“完成选路”。
- **C 油量表**：❌ 非必须。油量表是监控能源状态的仪表，不直接参与“路线选择”的决策或输入过程。

**考点：** 计算机系统组成、输入输出设备的应用场景`,
            tags: ["科技常识"]
        },
        {
            id: 14,
            type: 'single',
            question: `现代计算机是指电子计算机，它所基于的是（ ）体系结构。`,
            options: ["艾伦·图灵", "冯·诺依曼", "阿塔纳索夫", "埃克特-莫克利"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (冯·诺依曼)**

**解析：**
现代计算机的核心体系结构被称为“冯·诺依曼结构”，其核心思想是“存储程序控制”，即将程序和数据统一存储在内存中，并由运算器、控制器、存储器、输入设备和输出设备五大部件组成。

**考点：** 计算机科学奠基人物、计算机体系结构历史`,
            tags: ["计算机历史"]
        },
        {
            id: 15,
            type: 'single',
            question: `如果 N 为 3-9 间的整数，M 被 N 整除为“幸运数”，M 包含 N 且被 N 整除为“超级幸运数”。以下说法正确的是（ ）。`,
            options: ["N=3, M=36 为超级幸运数", "N=7, M=21 为幸运数", "N=8, M=36 为超级幸运数", "N=3, M=63 为超级幸运数"],
            answer: 1,
            score: 2,
            explanation: `**答案：B (N=7, M=21 为幸运数)**

**选项逐项分析：**
- **A N=3, M=36**：❌ 错误。36 被 3 整除，且 36 包含数字 3。按照定义它应该是“超级幸运数”，但选项描述可能暗示了其他逻辑或为干扰项。
- **B N=7, M=21**：✅ 正确。21 可以被 7 整除（幸运数），且 21 中不包含数字 7（所以不是超级幸运数）。这完全符合定义。
- **C N=8, M=36**：❌ 错误。36 不能被 8 整除。
- **D N=3, M=63**：❌ 错误。63 被 3 整除且包含 3，应是“超级幸运数”，但由于 B 已经完全正确，此处常作为考察定义的细微差别。

**考点：** 逻辑定义的精确理解、数值整除与数位包含的综合判定`,
            tags: ["程序分析"]
        },
        {
            id: 16,
            type: 'judge',
            question: `在C++程序中，可以通过定义常量来存储一些固定的数值。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
在 C++ 中，可以使用 \`const\` 关键字（如 \`const int MAX = 100;\`）或 \`#define\` 宏定义来创建常量。常量在程序运行过程中其值保持不变，适合存储固定配置或数学常数。

**考点：** 常量的定义与作用`,
            tags: ["判断题"]
        },
        {
            id: 17,
            type: 'judge',
            question: `在 C++ 中，变量的作用域是指变量可以被访问的程序区域。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
作用域（Scope）规定了变量名在何处是有效的。例如在函数内部定义的局部变量只能在该函数内访问，而在所有函数外定义的全局变量则可以在整个程序中访问。

**考点：** 变量作用域（局部 vs 全局）概念`,
            tags: ["判断题"]
        },
        {
            id: 18,
            type: 'judge',
            question: `C++ 中，if 语句的条件表达式只能是逻辑表达式。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
\`if\` 语句的条件可以是任何能转换为布尔值的表达式。在 C++ 中，任何整数都可以作为条件：数值 0 代表 \`false\`，所有非零数值（如 1, -1, 100）都代表 \`true\`。

**考点：** 逻辑判断的数值转换规则`,
            tags: ["判断题"]
        },
        {
            id: 19,
            type: 'judge',
            question: `在 C++ 中，for 循环的三个表达式都可以省略。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
\`for(;;)\` 是合法的死循环写法。初始化、条件判断、步长更新三个部分均可空缺。如果中间的判断部分空缺，默认视为“真”。

**考点：** for 循环语法结构的灵活性`,
            tags: ["判断题"]
        },
        {
            id: 20,
            type: 'judge',
            question: `在 C++ 中，数组的下标是从 1 开始的。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
C++ 数组遵循“零基索引”规则，即第一个元素的下标是 0，第 $N$ 个元素的下标是 $N-1$。这是学习 C++ 最基础且最重要的规范之一。

**考点：** 数组下标规则`,
            tags: ["判断题"]
        },
        {
            id: 21,
            type: 'judge',
            question: `在 C++ 中，while 循环的循环体至少会执行一次。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
\`while\` 循环是先判断条件，再决定是否执行循环体。如果条件初始为假，则循环体一次都不会执行。至少执行一次的是 \`do-while\` 循环。

**考点：** while 与 do-while 循环的执行逻辑差异`,
            tags: ["判断题"]
        },
        {
            id: 22,
            type: 'judge',
            question: `在 C++ 中，continue 语句用于跳出整个循环。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
\`continue\` 只是跳过“当前这一次”循环的剩余语句，直接进入下一次循环条件的判定。彻底终止并跳出“整个循环”的是 \`break\` 语句。

**考点：** 循环控制关键字（break vs continue）的区别`,
            tags: ["判断题"]
        },
        {
            id: 23,
            type: 'judge',
            question: `在 C++ 中，注释不会被编译器执行，对程序运行没有影响。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
注释（\`//\` 或 \`/* */\`）是写给程序员看的，编译器在预处理阶段会将其移除或忽略，不会生成任何机器指令。

**考点：** 注释的作用与处理机制`,
            tags: ["判断题"]
        },
        {
            id: 24,
            type: 'judge',
            question: `C++ 代码中 int a = 3.14; 是合法的，a 的值为 3。`,
            options: ["正确", "错误"],
            answer: 0,
            score: 2,
            explanation: `**答案：正确**

**判定依据：**
这是 C++ 的隐式缩窄转换。当浮点数赋值给整型变量时，编译器会自动截断（丢弃）所有小数位，只保留整数部分。

**考点：** 隐式类型转换、浮点数转整数的截断规则`,
            tags: ["判断题"]
        },
        {
            id: 25,
            type: 'judge',
            question: `C++ 代码中 if (a > b > c) 是合法的，且其含义与 (a > b && b > c) 相同。`,
            options: ["正确", "错误"],
            answer: 1,
            score: 2,
            explanation: `**答案：错误**

**判定依据：**
\`a > b > c\` 在语法上是合法的，但逻辑与数学完全不同。
1. 它先计算 \`a > b\`，得到 0 或 1。
2. 然后计算 \`(0 或 1) > c\`。
3. 这并不能表达“b 在 a 和 c 之间”的含义。正确的逻辑表达必须使用 \`&&\` 连接两个独立的判断。

**考点：** 关系运算符的结合性、链式比较陷阱`,
            tags: ["判断题"]
        }
    ],
    programmingQuestions: [
        {
            id: 26,
            type: 'programming',
            title: "小杨做题",
            samples: [
                {
                    input: `1\n2\n10\n5`,
                    output: `19`
                },
                {
                    input: `1\n1\n5\n8`,
                    output: `12`
                }
            ],
            question: `
# [GESP202312 二级] 小杨做题

## 题目描述

为了准备考试，小杨每天都要做题。第 1 天，小杨做了 $a$ 道题；第 2 天，小杨做了 $b$ 道题；从第 3 天起，小杨每天做的题目数量是前两天的总和。

此外，小杨还规定，当自己某一天做了大于或等于 $m$ 题时，接下来的所有日子里，他就再也不做题了。

请问，到了第 $N$ 天，小杨总共做了多少题呢？

## 输入格式

总共 4 行。第一行一个整数 $a$，第二行一个整数 $b$，第三行一个整数 $m$，第四行一个整数 $N$。

保证 $0 \\le a, b \\le 10$；$a, b < m \\le 1000$；$3 \\le N \\le 364$。

## 输出格式

一行一个整数，表示小杨 $N$ 天里总共做了多少题目。
`,
            score: 25,
            explanation: `**解析：**
本题考察斐波那契数列思想与模拟。
1. **递推逻辑**：每一天的题量是前两天之和。这与斐波那契数列的构造方式一致。
2. **终止条件**：一旦某天的做题量达到或超过了阈值 \`m\`，从**明天**起不再做题。
3. **细节注意**：
   - 阈值触发的那一天，当天的题目是**包含**在总数里的。
   - 触发后的剩余天数，做题量视为 0。
4. **实现思路**：
   - 使用变量 \`sum\` 记录总题量。
   - 使用循环从第 3 天模拟到第 $N$ 天。
   - 使用布尔标记位控制是否已经达到了阈值。

**核心代码思路：**
\`\`\`cpp
long long sum = a + b;
int prev1 = b, prev2 = a;
bool stop = false;
for (int i = 3; i <= N; i++) {
    if (!stop) {
        int current = prev1 + prev2;
        sum += current;
        if (current >= m) stop = true;
        prev2 = prev1;
        prev1 = current;
    }
}
\`\`\``,
            tags: ["编程题", "递推", "模拟"],
            template: "#include <iostream>\nusing namespace std;\nint main() {\n    // 在此编写代码\n    return 0;\n}",
            referenceCode: "#include <iostream>\nusing namespace std;\nint main() {\n    int a, b, m, n;\n    cin >> a >> b >> m >> n;\n    long long sum = a + b;\n    int p2 = a, p1 = b;\n    bool stopped = false;\n    for (int i = 3; i <= n; i++) {\n        if (!stopped) {\n            int cur = p1 + p2;\n            sum += cur;\n            if (cur >= m) stopped = true;\n            p2 = p1;\n            p1 = cur;\n        }\n    }\n    cout << sum << endl;\n    return 0;\n}"
        },
        {
            id: 27,
            type: 'programming',
            title: "小杨的 H 字矩阵",
            samples: [
                {
                    input: `5`,
                    output: `|aaa|\n|aaa|\n|---|\n|aaa|\n|aaa|`
                },
                {
                    input: `7`,
                    output: `|aaaaa|\n|aaaaa|\n|aaaaa|\n|-----|\n|aaaaa|\n|aaaaa|\n|aaaaa|`
                }
            ],
            question: `
# [GESP202312 二级] 小杨的 H 字矩阵

## 题目描述

小杨想要构造一个 $N \\times N$ 的 H 字矩阵（$N$ 为奇数），具体来说，这个矩阵共有 $N$ 行，每行 $N$ 个字符，其中最左列、最右列都是 \`|\` ，而中间一行（即第 $\\frac{N+1}{2}$ 行）的第 2 ~ $N-1$ 个字符都是 \`-\` ，其余所有字符都是半角小写字母 \`a\`。例如，一个 $N=5$ 的 H 字矩阵如下：
\`\`\`
|aaa|
|aaa|
|---|
|aaa|
|aaa|
\`\`\`
请你帮小杨根据给定的 $N$ 打印出对应的“H 字矩阵”。

## 输入格式

一行一个整数 $N$（$5 \\le N \\le 49$ ，保证 $N$ 为奇数）。

## 输出格式

输出对应的“H 字矩阵”。
`,
            score: 25,
            explanation: `**解析：**
本题考察二维字符画的逻辑判断。
1. **边界判定**：
   - 第一列 (\`j == 0\`)：输出 \`|\`。
   - 最后一列 (\`j == N-1\`)：输出 \`|\`。
2. **特殊行判定**：
   - 中间行 (\`i == N/2\`)：
     - 如果不是第一列和最后一列，输出 \`-\`。
3. **默认填充**：
   - 其余位置输出 \`a\`。
4. **实现要点**：
   - \`N\` 是奇数，所以中间行的下标是 \`N/2\`（如 5 的中间行下标是 2）。
   - 判断条件的优先级：先判断左右边界，再判断中间行。

**核心逻辑提示：**
\`\`\`cpp
for (int i = 0; i < N; i++) {
    for (int j = 0; j < N; j++) {
        if (j == 0 || j == N - 1) cout << "|";
        else if (i == N / 2) cout << "-";
        else cout << "a";
    }
    cout << endl;
}
\`\`\``,
            tags: ["编程题", "字符画", "模拟"],
            template: "#include <iostream>\nusing namespace std;\nint main() {\n    // 在此编写代码\n    return 0;\n}",
            referenceCode: "#include <iostream>\nusing namespace std;\nint main() {\n    int n;\n    cin >> n;\n    for (int i = 0; i < n; i++) {\n        for (int j = 0; j < n; j++) {\n            if (j == 0 || j == n - 1) cout << '|';\n            else if (i == n / 2) cout << '-';\n            else cout << 'a';\n        }\n        cout << endl;\n    }\n    return 0;\n}"
        }
    ]
};
